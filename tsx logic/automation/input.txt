```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21_PrepaymentEntry',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21_PrepaymentEntry: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const doorSlide = interpolate(sp(20), [0, 1], [-1200, 0]);
  const wordSmash = sp(45, { damping: 8 });
  const killerReveal = sp(120);
  const stampRot = interpolate(sp(160), [0, 1], [-25, -8]);
  const cameraZoom = 1 + Math.sin(frame * 0.008) * 0.015;

  return (
    <AbsoluteFill style={{ backgroundColor: '#C4763A', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 40%, #FF9B51 0%, #A85E28 100%)' }} />

      {/* Shape layer: drifting geometric forms */}
      <div style={{ position: 'absolute', top: 100, left: 80, width: 400, height: 400, border: '6px solid #25343F', opacity: 0.08, transform: `rotate(${frame * 0.15}deg)` }} />
      <div style={{ position: 'absolute', bottom: 60, right: 120, width: 500, height: 500, borderRadius: '50%', border: '8px solid #FFF449', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.09 }}>
        <defs>
          <pattern id="g21" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M80 0 L0 0 0 80" fill="none" stroke="#25343F" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g21)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const y = ((frame * (0.6 + r * 0.8) + i * 60) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 8, height: 8, backgroundColor: i % 2 === 0 ? '#FFF449' : '#25343F',
            opacity: 0.4, transform: `rotate(${frame * 2}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <div style={{ position: 'absolute', inset: 0, transform: `scale(${cameraZoom})` }}>
        {/* Question door slides in */}
        <div style={{
          position: 'absolute', top: 100, left: 100,
          transform: `translateX(${doorSlide}px) rotate(-2deg)`,
        }}>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '18px 40px', fontFamily: 'Yantra One',
            fontSize: 68, letterSpacing: 2,
            border: '6px solid #FFF449',
            boxShadow: '14px 14px 0 #25343F',
          }}>
            रास्ता क्या है?
          </div>
        </div>

        {/* Hero word smash */}
        <div style={{
          position: 'absolute', top: 260, left: 100,
          transform: `scale(${wordSmash}) rotate(${(1 - wordSmash) * -8}deg)`,
          transformOrigin: 'left center',
        }}>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900,
            fontSize: 272, color: '#FFF449',
            letterSpacing: -8, lineHeight: 0.9,
            textShadow: '10px 10px 0 #25343F',
            WebkitTextStroke: '3px #25343F',
          }}>
            PREPAY
          </div>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900,
            fontSize: 272, color: '#25343F',
            letterSpacing: -8, lineHeight: 0.9,
            marginTop: -40,
          }}>
            MENT.
          </div>
        </div>

        {/* Silent wealth killer stamp */}
        <div style={{
          position: 'absolute', top: 320, right: 90,
          opacity: killerReveal,
          transform: `rotate(${stampRot}deg) scale(${0.6 + killerReveal * 0.4})`,
        }}>
          <div style={{
            backgroundColor: '#FFF449', color: '#25343F',
            padding: '28px 44px', border: '7px solid #25343F',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 58,
            letterSpacing: 3, boxShadow: '10px 10px 0 #A85E28',
          }}>
            20-YR COMPOUND
          </div>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '18px 40px', marginTop: 12,
            fontFamily: 'Yantra One', fontSize: 54,
            border: '5px solid #FFF449',
            transform: 'rotate(3deg)',
          }}>
            = SILENT WEALTH KILLER
          </div>
        </div>

        {/* Bottom info strip */}
        <div style={{
          position: 'absolute', bottom: 90, left: 100, right: 100,
          opacity: sp(200), transform: `translateY(${(1 - sp(200)) * 60}px)`,
        }}>
          <div style={{
            display: 'flex', gap: 30, alignItems: 'stretch',
          }}>
            <div style={{
              flex: 1, backgroundColor: '#25343F',
              padding: 34, border: '5px solid #FFF449',
              transform: 'rotate(-1deg)',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, color: '#FFF449', letterSpacing: 4 }}>01 / RULE</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F5F5F5', fontWeight: 800, marginTop: 10 }}>
                100% Principal घटता है
              </div>
            </div>
            <div style={{
              flex: 1, backgroundColor: '#FFF449',
              padding: 34, border: '5px solid #25343F',
              transform: 'rotate(1deg)',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, color: '#25343F', letterSpacing: 4 }}>02 / MATH</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#25343F', fontWeight: 800, marginTop: 10 }}>
                Reducing Balance
              </div>
            </div>
            <div style={{
              flex: 1, backgroundColor: '#A85E28',
              padding: 34, border: '5px solid #25343F',
              transform: 'rotate(-1deg)',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, color: '#FFF449', letterSpacing: 4 }}>03 / EFFECT</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 42, color: '#F5F5F5', fontWeight: 800, marginTop: 10 }}>
                छोटी Payment → बड़ी बचत
              </div>
            </div>
          </div>
        </div>

        {/* Skull icon (SVG) for killer */}
        <svg width="140" height="140" style={{ position: 'absolute', top: 210, right: 700, opacity: killerReveal * 0.85, transform: `rotate(${Math.sin(frame * 0.05) * 8}deg)` }} viewBox="0 0 100 100">
          <circle cx="50" cy="45" r="30" fill="#25343F" stroke="#FFF449" strokeWidth="3" />
          <circle cx="40" cy="42" r="6" fill="#FFF449" />
          <circle cx="60" cy="42" r="6" fill="#FFF449" />
          <rect x="42" y="60" width="4" height="10" fill="#FFF449" />
          <rect x="50" y="60" width="4" height="10" fill="#FFF449" />
          <rect x="58" y="60" width="4" height="10" fill="#FFF449" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene21_PrepaymentEntry;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22_MultiplierMath',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22_MultiplierMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rupeeCount = Math.floor(interpolate(frame, [30, 120], [1, 10], { extrapolateRight: 'clamp' }));
  const barGrow = interpolate(frame, [180, 300], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const savingsNum = Math.floor(interpolate(frame, [340, 460], [0, 8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#B36A32', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #FF9B51 0%, #8A4E20 100%)' }} />

      {/* Shape layer */}
      <div style={{ position: 'absolute', top: -100, right: -100, width: 600, height: 600, backgroundColor: '#25343F', opacity: 0.05, transform: `rotate(${frame * 0.1}deg)` }} />
      <div style={{ position: 'absolute', bottom: -80, left: -80, width: 500, height: 500, borderRadius: '50%', backgroundColor: '#FFF449', opacity: 0.05 }} />

      {/* Dot texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <defs>
          <pattern id="d22" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#25343F" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#d22)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const x = ((frame * (0.4 + r * 0.6) + i * 90) % 2020) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: r * 1080,
            fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFF449', opacity: 0.25,
            transform: `rotate(${frame * 1.5}deg)`,
          }}>₹</div>
        );
      })}

      {/* Main content */}
      <div style={{ position: 'absolute', top: 60, left: 100, right: 100 }}>
        {/* Header */}
        <div style={{
          transform: `translateY(${(1 - sp(10)) * -80}px)`,
          opacity: sp(10),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '10px 26px', fontFamily: 'JetBrains Mono',
            fontSize: 28, letterSpacing: 5,
          }}>
            /// GOLDEN 7 YEARS ///
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 88, color: '#25343F',
            marginTop: 12, lineHeight: 1,
            textShadow: '5px 5px 0 #FFF449',
          }}>
            पहले 7 साल का जादू
          </div>
        </div>

        {/* Multiplier equation */}
        <div style={{
          marginTop: 50,
          display: 'flex', alignItems: 'center', gap: 30,
          opacity: sp(35), transform: `scale(${sp(35)})`,
        }}>
          <div style={{
            backgroundColor: '#FFF449', color: '#25343F',
            padding: '30px 44px', border: '6px solid #25343F',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 96,
            transform: 'rotate(-3deg)', boxShadow: '10px 10px 0 #25343F',
          }}>
            ₹1
          </div>
          <div style={{
            fontFamily: 'Inter', fontWeight: 900, fontSize: 120, color: '#25343F',
            transform: `rotate(${Math.sin(frame * 0.1) * 5}deg)`,
          }}>→</div>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '30px 44px', border: '6px solid #FFF449',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 96,
            transform: 'rotate(3deg)', boxShadow: '10px 10px 0 #FFF449',
          }}>
            ₹{rupeeCount}
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, color: '#25343F',
            fontWeight: 800, maxWidth: 380,
          }}>
            Future Interest ELIMINATED
          </div>
        </div>

        {/* Bar chart: prepayment → savings */}
        <div style={{ marginTop: 60, display: 'flex', gap: 50, alignItems: 'flex-end', height: 340 }}>
          {[
            { label: 'Prepay', val: '₹50K', h: 80, color: '#FFF449', text: '#25343F' },
            { label: 'Prepay', val: '₹1L', h: 140, color: '#FFF449', text: '#25343F' },
            { label: 'Saved', val: '₹4L', h: 260, color: '#25343F', text: '#FFF449' },
            { label: 'Saved', val: '₹8L', h: 320, color: '#25343F', text: '#FFF449' },
          ].map((b, i) => (
            <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{
                fontFamily: 'Inter', fontWeight: 900, fontSize: 54, color: '#25343F',
                marginBottom: 8, opacity: barGrow,
              }}>
                {b.val}
              </div>
              <div style={{
                width: '100%', height: b.h * barGrow,
                backgroundColor: b.color, border: '5px solid #25343F',
                display: 'flex', alignItems: 'flex-end', justifyContent: 'center',
                paddingBottom: 12,
                boxShadow: '6px 6px 0 rgba(0,0,0,0.4)',
              }}>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: b.text, letterSpacing: 2 }}>
                  {b.label}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom summary */}
        <div style={{
          marginTop: 30, textAlign: 'center',
          opacity: sp(340),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '18px 40px', border: '5px solid #FFF449',
            fontFamily: 'Yantra One', fontSize: 54,
            transform: 'rotate(-1deg)',
          }}>
            Year 1–3 का ₹1L = ₹{savingsNum}L बचत
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene22_MultiplierMath;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23_ChoiceRoads',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23_ChoiceRoads: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftIn = sp(30);
  const rightIn = sp(60);
  const vsPulse = 1 + Math.sin(frame * 0.15) * 0.08;
  const ageBar = interpolate(frame, [280, 420], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A85E28', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #FF9B51 0%, #7A421E 100%)' }} />

      {/* Diagonal stripes texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="s23" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#25343F" strokeWidth="4" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#s23)" />
      </svg>

      {/* Shape layers */}
      <div style={{ position: 'absolute', top: 200, left: 400, width: 300, height: 300, border: '5px solid #FFF449', opacity: 0.07, transform: `rotate(${frame * 0.3}deg)` }} />
      <div style={{ position: 'absolute', bottom: 100, right: 300, width: 400, height: 400, border: '5px solid #25343F', opacity: 0.07, borderRadius: '50%' }} />

      {/* Money paper particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 100);
        const y = ((frame * (0.5 + r * 0.7) + i * 80) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 26, height: 14, backgroundColor: '#FFF449', opacity: 0.3,
            transform: `rotate(${frame * 3 + i * 30}deg)`,
            border: '1px solid #25343F',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 50, left: 0, right: 0,
        textAlign: 'center', opacity: sp(5),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Yantra One', fontSize: 78, color: '#FFF449',
          backgroundColor: '#25343F', padding: '14px 44px',
          border: '5px solid #FFF449', letterSpacing: 3,
          transform: 'rotate(-1deg)',
        }}>
          ₹50,000 — कहाँ जाए?
        </div>
      </div>

      {/* Two roads */}
      <div style={{ position: 'absolute', top: 220, left: 80, right: 80, display: 'flex', gap: 40 }}>
        {/* LEFT: Wrong path */}
        <div style={{
          flex: 1,
          transform: `translateX(${(1 - leftIn) * -400}px) rotate(-2deg)`,
          opacity: leftIn,
        }}>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '30px', border: '6px solid #FFF449',
            fontFamily: 'JetBrains Mono', fontSize: 30, letterSpacing: 4,
            textAlign: 'center', textDecoration: 'line-through',
          }}>
            THE TRAP
          </div>
          <div style={{
            backgroundColor: '#FFF449', color: '#25343F',
            padding: 34, border: '6px solid #25343F', marginTop: -6,
          }}>
            {['Gadgets', 'Vacation', 'Idle FD'].map((t, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 20,
                padding: '18px 0', borderBottom: i < 2 ? '3px dashed #25343F' : 'none',
              }}>
                <svg width="50" height="50" viewBox="0 0 50 50">
                  <circle cx="25" cy="25" r="22" fill="#25343F" />
                  <path d="M15 15 L35 35 M35 15 L15 35" stroke="#FF9B51" strokeWidth="5" strokeLinecap="round" />
                </svg>
                <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 46, color: '#25343F' }}>
                  {t}
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* VS badge */}
        <div style={{
          position: 'absolute', top: '30%', left: '50%',
          transform: `translate(-50%, -50%) scale(${vsPulse}) rotate(${Math.sin(frame * 0.1) * 10}deg)`,
          zIndex: 10,
        }}>
          <div style={{
            width: 130, height: 130, borderRadius: '50%',
            backgroundColor: '#FFF449', border: '7px solid #25343F',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 58, color: '#25343F',
            boxShadow: '8px 8px 0 #25343F',
          }}>
            VS
          </div>
        </div>

        {/* RIGHT: Right path */}
        <div style={{
          flex: 1,
          transform: `translateX(${(1 - rightIn) * 400}px) rotate(2deg)`,
          opacity: rightIn,
        }}>
          <div style={{
            backgroundColor: '#FFF449', color: '#25343F',
            padding: '30px', border: '6px solid #25343F',
            fontFamily: 'JetBrains Mono', fontSize: 30, letterSpacing: 4,
            textAlign: 'center', fontWeight: 900,
          }}>
            THE WIN
          </div>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: 34, border: '6px solid #FFF449', marginTop: -6,
          }}>
            {['Loan Prepay', '4–5 Yr Cut', 'Freedom'].map((t, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 20,
                padding: '18px 0', borderBottom: i < 2 ? '3px dashed #FFF449' : 'none',
              }}>
                <svg width="50" height="50" viewBox="0 0 50 50">
                  <circle cx="25" cy="25" r="22" fill="#FFF449" />
                  <path d="M15 26 L22 33 L36 18" stroke="#25343F" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
                <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 46, color: '#FFF449' }}>
                  {t}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Bottom age timeline */}
      <div style={{ position: 'absolute', bottom: 60, left: 100, right: 100, opacity: sp(260) }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#FFF449', letterSpacing: 4, marginBottom: 10 }}>
          DEBT-FREE AGE
        </div>
        <div style={{ position: 'relative', height: 60 }}>
          <div style={{ position: 'absolute', top: 25, left: 0, right: 0, height: 10, backgroundColor: '#25343F' }} />
          <div style={{ position: 'absolute', top: 25, left: 0, height: 10, width: `${ageBar * 55}%`, backgroundColor: '#FFF449' }} />
          <div style={{ position: 'absolute', top: 0, left: '55%', transform: 'translateX(-50%)', textAlign: 'center' }}>
            <div style={{ width: 16, height: 60, backgroundColor: '#FFF449', border: '3px solid #25343F' }} />
          </div>
          <div style={{ position: 'absolute', top: 0, left: '100%', transform: 'translateX(-100%)', textAlign: 'center' }}>
            <div style={{ width: 16, height: 60, backgroundColor: '#F5F5F5', border: '3px solid #25343F' }} />
          </div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 8 }}>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 44, color: '#FFF449' }}>55 <span style={{ fontSize: 24 }}>(Smart)</span></div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 44, color: '#25343F' }}>60 <span style={{ fontSize: 24 }}>(Passive)</span></div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene23_ChoiceRoads;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24_ZeroCharges',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24_ZeroCharges: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const stampDrop = sp(60, { damping: 6 });
  const stampRot = interpolate(stampDrop, [0, 1], [-45, -10]);
  const paperShake = frame > 60 && frame < 90 ? Math.sin(frame * 2) * 4 : 0;
  const circularReveal = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#C4763A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #FF9B51 0%, #8A4E20 100%)' }} />

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="c24" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M0 20 L20 0" stroke="#25343F" strokeWidth="1" />
            <path d="M0 0 L20 20" stroke="#25343F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#c24)" />
      </svg>

      {/* Shape drifters */}
      <div style={{ position: 'absolute', top: 80, right: 60, width: 300, height: 300, backgroundColor: '#FFF449', opacity: 0.08, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: 40, left: 40, width: 350, height: 350, border: '8px solid #25343F', opacity: 0.07, borderRadius: '50%' }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 200);
        const x = ((frame * (0.3 + r * 0.7) + i * 100) % 2020) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: r * 1080,
            width: 6, height: 6, backgroundColor: '#25343F', opacity: 0.4,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* Header ribbon */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'JetBrains Mono', fontSize: 26,
          backgroundColor: '#25343F', color: '#FFF449',
          padding: '10px 30px', letterSpacing: 6,
          opacity: sp(0),
        }}>
          RBI GUIDELINE / JAN 2026
        </div>
      </div>

      {/* Document paper mockup */}
      <div style={{
        position: 'absolute', top: 150, left: '50%',
        transform: `translateX(-50%) translateX(${paperShake}px) rotate(-1deg)`,
        width: 1100, opacity: sp(15),
      }}>
        <div style={{
          backgroundColor: '#F5F5F5',
          border: '8px solid #25343F',
          padding: '50px 60px',
          boxShadow: '18px 18px 0 #25343F',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#25343F',
            letterSpacing: 3, opacity: 0.6,
          }}>
            RESERVE BANK OF INDIA — CIRCULAR
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 78, color: '#25343F',
            marginTop: 14, lineHeight: 1,
          }}>
            ZERO PREPAYMENT
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 78, color: '#C4763A',
            marginTop: -6, lineHeight: 1,
          }}>
            CHARGES
          </div>
          <div style={{ height: 4, backgroundColor: '#25343F', margin: '20px 0' }} />
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42,
            color: '#25343F', fontWeight: 700, lineHeight: 1.3,
          }}>
            Individual Floating-Rate Home Loans पर<br/>
            <span style={{ backgroundColor: '#FFF449', padding: '4px 10px' }}>
              Penalty नहीं लग सकती
            </span>
          </div>
          <div style={{
            display: 'flex', gap: 20, marginTop: 24, alignItems: 'center',
          }}>
            <svg width="60" height="60" viewBox="0 0 60 60">
              <circle cx="30" cy="30" r="26" fill="#25343F" />
              <path d="M18 30 L27 39 L44 22" stroke="#FFF449" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontWeight: 800, fontSize: 36, color: '#25343F' }}>
              यह आपका RIGHT है
            </div>
          </div>
        </div>

        {/* APPROVED stamp */}
        <div style={{
          position: 'absolute', top: 60, right: -30,
          transform: `rotate(${stampRot}deg) scale(${stampDrop})`,
        }}>
          <div style={{
            border: '8px solid #C0392B', color: '#C0392B',
            padding: '20px 34px',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 62,
            letterSpacing: 6, backgroundColor: 'rgba(255,255,255,0.15)',
          }}>
            APPROVED
          </div>
          <div style={{
            textAlign: 'center', color: '#C0392B',
            fontFamily: 'JetBrains Mono', fontSize: 18, marginTop: 4, letterSpacing: 3,
          }}>
            RBI / JAN 2026
          </div>
        </div>
      </div>

      {/* Bottom fee reminder strip */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        opacity: circularReveal,
        transform: `translateY(${(1 - circularReveal) * 60}px)`,
      }}>
        <div style={{
          backgroundColor: '#25343F', border: '5px solid #FFF449',
          padding: '22px 40px',
          display: 'flex', alignItems: 'center', gap: 30,
          transform: 'rotate(-0.5deg)',
        }}>
          <svg width="70" height="70" viewBox="0 0 70 70">
            <path d="M35 8 L62 58 L8 58 Z" fill="#FFF449" stroke="#25343F" strokeWidth="3" />
            <rect x="32" y="24" width="6" height="20" fill="#25343F" />
            <circle cx="35" cy="50" r="3.5" fill="#25343F" />
          </svg>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FFF449', letterSpacing: 4 }}>
              IF BANK ASKS FEE →
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, color: '#F5F5F5', fontWeight: 800 }}>
              January 2026 Circular याद दिलाएँ
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene24_ZeroCharges;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25_DisclaimerIllusion',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25_DisclaimerIllusion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const factors = ['LOAN AMT', 'RATE', 'INCOME', 'EXPENSES', 'GOALS'];
  const illusionShake = interpolate(frame, [400, 500], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeVal = illusionShake > 0 ? Math.sin(frame * 1.2) * 6 * illusionShake : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B36A32', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 80%, #FF9B51 0%, #7A421E 100%)' }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.09 }}>
        <defs>
          <pattern id="g25" width="60" height="60" patternUnits="userSpaceOnUse">
            <rect width="60" height="60" fill="none" stroke="#25343F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g25)" />
      </svg>

      {/* Shape drift */}
      <div style={{ position: 'absolute', top: 150, right: 100, width: 260, height: 260, border: '6px solid #FFF449', opacity: 0.08, transform: `rotate(${frame * 0.25}deg)` }} />
      <div style={{ position: 'absolute', bottom: 150, left: 80, width: 380, height: 380, backgroundColor: '#25343F', opacity: 0.05, borderRadius: '50%' }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 300);
        const y = ((frame * (0.5 + r * 0.6) + i * 70) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 4, height: 4, backgroundColor: '#FFF449', opacity: 0.5,
          }} />
        );
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 50, left: 100, right: 100, opacity: sp(0) }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#25343F', color: '#FFF449',
          fontFamily: 'JetBrains Mono', fontSize: 26,
          padding: '10px 26px', letterSpacing: 5,
        }}>
          /// DISCLAIMER ///
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 88, color: '#FFF449',
          marginTop: 12, textShadow: '5px 5px 0 #25343F', lineHeight: 1,
        }}>
          हर स्थिति अलग है
        </div>
      </div>

      {/* Factor tags floating */}
      <div style={{
        position: 'absolute', top: 260, left: 100, right: 100,
        display: 'flex', flexWrap: 'wrap', gap: 22, justifyContent: 'center',
      }}>
        {factors.map((f, i) => {
          const rot = (i % 2 === 0 ? -1 : 1) * 3;
          const app = sp(30 + i * 12);
          return (
            <div key={i} style={{
              opacity: app,
              transform: `scale(${app}) rotate(${rot + Math.sin(frame * 0.05 + i) * 2}deg)`,
            }}>
              <div style={{
                backgroundColor: i % 2 === 0 ? '#FFF449' : '#25343F',
                color: i % 2 === 0 ? '#25343F' : '#FFF449',
                border: '5px solid #25343F',
                padding: '18px 32px',
                fontFamily: 'Inter', fontWeight: 900, fontSize: 44,
                letterSpacing: 2,
                boxShadow: '7px 7px 0 rgba(0,0,0,0.35)',
              }}>
                {f}
              </div>
            </div>
          );
        })}
      </div>

      {/* Advisor CTA card */}
      <div style={{
        position: 'absolute', top: 460, left: 100, right: 100,
        opacity: sp(160),
        transform: `translateY(${(1 - sp(160)) * 50}px)`,
      }}>
        <div style={{
          backgroundColor: '#25343F', border: '6px solid #FFF449',
          padding: '30px 40px',
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <svg width="90" height="90" viewBox="0 0 90 90">
            <circle cx="45" cy="45" r="42" fill="#FFF449" stroke="#25343F" strokeWidth="4" />
            <rect x="30" y="24" width="30" height="42" fill="#25343F" />
            <path d="M35 32 L55 32 M35 40 L55 40 M35 48 L50 48" stroke="#FFF449" strokeWidth="2.5" />
          </svg>
          <div style={{ flex: 1 }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFF449', letterSpacing: 4 }}>
              BEFORE ANY BIG DECISION
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 52, color: '#F5F5F5', fontWeight: 800, marginTop: 6 }}>
              CA या Certified Financial Planner से बात करें
            </div>
          </div>
        </div>
      </div>

      {/* Illusion transition */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        transform: `translateX(${shakeVal}px)`,
        opacity: sp(340),
      }}>
        <div style={{
          backgroundColor: '#FFF449', border: '6px solid #25343F',
          padding: '24px 40px',
          display: 'flex', alignItems: 'center', gap: 22,
          boxShadow: '10px 10px 0 #25343F',
        }}>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '8px 16px', fontFamily: 'JetBrains Mono',
            fontSize: 24, letterSpacing: 3,
          }}>
            NEXT
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, color: '#25343F', lineHeight: 1 }}>
            "Rates और गिरेंगे" — यह है ILLUSION
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene25_DisclaimerIllusion;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26_FlawedThinking',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26_FlawedThinking: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const gdpArrow = interpolate(frame, [100, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const inflArrow = interpolate(frame, [180, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const dangerPulse = 1 + Math.sin(frame * 0.2) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A85E28', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #FF9B51 0%, #6E3B1A 100%)' }} />

      {/* Line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="h26" width="100" height="4" patternUnits="userSpaceOnUse">
            <line x1="0" y1="2" x2="100" y2="2" stroke="#25343F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#h26)" />
      </svg>

      {/* Drift shapes */}
      <div style={{ position: 'absolute', top: -80, left: '30%', width: 400, height: 400, border: '6px solid #FFF449', opacity: 0.08, transform: `rotate(${frame * 0.15}deg)` }} />
      <div style={{ position: 'absolute', bottom: -50, right: '25%', width: 380, height: 380, backgroundColor: '#25343F', opacity: 0.06, transform: `rotate(${45 + frame * 0.1}deg)` }} />

      {/* Danger particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 400);
        const y = ((frame * (0.6 + r * 0.6) + i * 70) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 8, height: 8, backgroundColor: '#FFF449', opacity: 0.35,
            transform: `rotate(45deg)`,
          }} />
        );
      })}

      {/* Header stamp */}
      <div style={{ position: 'absolute', top: 50, left: 100, opacity: sp(0), transform: `rotate(-2deg)` }}>
        <div style={{
          backgroundColor: '#25343F', color: '#FFF449',
          padding: '14px 32px', fontFamily: 'Yantra One', fontSize: 62,
          border: '5px solid #FFF449', boxShadow: '10px 10px 0 rgba(0,0,0,0.4)',
        }}>
          FUNDAMENTALLY FLAWED
        </div>
      </div>

      {/* Two arrow charts */}
      <div style={{ position: 'absolute', top: 200, left: 100, right: 100, display: 'flex', gap: 40 }}>
        {/* GDP down */}
        <div style={{
          flex: 1, backgroundColor: '#FFF449',
          border: '6px solid #25343F', padding: 30,
          transform: 'rotate(-1deg)',
          opacity: sp(30),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#25343F', letterSpacing: 5 }}>
            FY27 GDP FORECAST
          </div>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 20, marginTop: 20 }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 96, color: '#25343F', textDecoration: 'line-through', opacity: 0.5 }}>
              6.9%
            </div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 44, color: '#25343F' }}>→</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 120, color: '#C0392B' }}>
              6.6%
            </div>
          </div>
          {/* Downward line chart */}
          <svg width="100%" height="120" viewBox="0 0 400 120">
            <path d={`M20 30 L${20 + gdpArrow * 360} ${30 + gdpArrow * 60}`} stroke="#25343F" strokeWidth="5" fill="none" strokeLinecap="round" />
            <polygon points={`${20 + gdpArrow * 360},${30 + gdpArrow * 60} ${20 + gdpArrow * 360 - 12},${20 + gdpArrow * 60} ${20 + gdpArrow * 360},${42 + gdpArrow * 60}`} fill="#25343F" opacity={gdpArrow} />
            <text x="20" y="115" fontFamily="JetBrains Mono" fontSize="18" fill="#25343F">GROWTH SLOWING</text>
          </svg>
        </div>

        {/* Inflation up */}
        <div style={{
          flex: 1, backgroundColor: '#25343F',
          border: '6px solid #FFF449', padding: 30,
          transform: 'rotate(1deg)',
          opacity: sp(60),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#FFF449', letterSpacing: 5 }}>
            INFLATION ESTIMATE
          </div>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 20, marginTop: 20 }}>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 96, color: '#FFF449', textDecoration: 'line-through', opacity: 0.5 }}>
              4.6%
            </div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 44, color: '#FFF449' }}>→</div>
            <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 120, color: '#FFF449' }}>
              5.1%
            </div>
          </div>
          <svg width="100%" height="120" viewBox="0 0 400 120">
            <path d={`M20 90 L${20 + inflArrow * 360} ${90 - inflArrow * 60}`} stroke="#FFF449" strokeWidth="5" fill="none" strokeLinecap="round" />
            <polygon points={`${20 + inflArrow * 360},${90 - inflArrow * 60} ${20 + inflArrow * 360 - 12},${100 - inflArrow * 60} ${20 + inflArrow * 360},${78 - inflArrow * 60}`} fill="#FFF449" opacity={inflArrow} />
            <text x="20" y="115" fontFamily="JetBrains Mono" fontSize="18" fill="#FFF449">INFLATION RISING</text>
          </svg>
        </div>
      </div>

      {/* Commodity chips */}
      <div style={{
        position: 'absolute', top: 600, left: 100, right: 100,
        display: 'flex', gap: 18, justifyContent: 'center', flexWrap: 'wrap',
        opacity: sp(220),
      }}>
        {['LPG', 'METALS', 'PLASTIC', 'RUBBER'].map((c, i) => (
          <div key={i} style={{
            backgroundColor: '#C0392B', color: '#FFF449',
            padding: '14px 30px', border: '4px solid #25343F',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 40,
            transform: `rotate(${(i % 2 === 0 ? -1 : 1) * 2}deg)`,
            boxShadow: '6px 6px 0 #25343F',
          }}>
            {c} ↑
          </div>
        ))}
      </div>

      {/* Danger conclusion */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) scale(${dangerPulse})`,
        opacity: sp(300),
      }}>
        <div style={{
          backgroundColor: '#C0392B', color: '#FFF449',
          border: '6px solid #25343F',
          padding: '20px 44px',
          fontFamily: 'Yantra One', fontSize: 62,
          letterSpacing: 3, textAlign: 'center',
          boxShadow: '12px 12px 0 #25343F',
        }}>
          ⚠  COMBINATION = DANGEROUS
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene26_FlawedThinking;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27_Stagflation',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27_Stagflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleReveal = sp(15);
  const chartProgress = interpolate(frame, [200, 500], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B36A32', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(45deg, #8A4E20 0%, #FF9B51 60%, #5C3315 100%)' }} />

      {/* Wavy texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="w27" width="120" height="30" patternUnits="userSpaceOnUse">
            <path d="M0 15 Q30 0 60 15 T120 15" stroke="#25343F" strokeWidth="1.5" fill="none" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#w27)" />
      </svg>

      {/* Shapes */}
      <div style={{ position: 'absolute', top: 100, right: 200, width: 300, height: 300, backgroundColor: '#FFF449', opacity: 0.06, borderRadius: '50%' }} />
      <div style={{ position: 'absolute', bottom: 80, left: 200, width: 260, height: 260, border: '6px solid #25343F', opacity: 0.08, transform: `rotate(${frame * 0.2}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 500);
        const x = ((frame * (0.4 + r * 0.7) + i * 90) % 2020) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: r * 1080,
            width: 5, height: 5, backgroundColor: '#FFF449', opacity: 0.4,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Compound label */}
      <div style={{
        position: 'absolute', top: 60, left: 100,
        opacity: titleReveal,
        transform: `translateY(${(1 - titleReveal) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 28,
          backgroundColor: '#25343F', color: '#FFF449',
          padding: '10px 26px', letterSpacing: 5, display: 'inline-block',
        }}>
          ECON WARNING TERM
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 152, color: '#FFF449',
          lineHeight: 0.95, marginTop: 14, letterSpacing: -3,
          textShadow: '8px 8px 0 #25343F',
          WebkitTextStroke: '2px #25343F',
        }}>
          STAG<span style={{ color: '#C0392B' }}>FLATION</span>
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 48, color: '#25343F',
          fontWeight: 800, marginTop: 8,
          backgroundColor: '#FFF449', display: 'inline-block',
          padding: '6px 20px', border: '4px solid #25343F',
        }}>
          Growth Down + Inflation Up
        </div>
      </div>

      {/* Central bank reluctance box */}
      <div style={{
        position: 'absolute', top: 460, left: 100, width: 720,
        opacity: sp(80),
        transform: `translateX(${(1 - sp(80)) * -80}px)`,
      }}>
        <div style={{
          backgroundColor: '#25343F', border: '5px solid #FFF449',
          padding: 30,
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 20 }}>
            <svg width="80" height="80" viewBox="0 0 80 80">
              <rect x="10" y="30" width="60" height="42" fill="#FFF449" stroke="#F5F5F5" strokeWidth="2" />
              <polygon points="40,10 70,30 10,30" fill="#FFF449" />
              <rect x="20" y="42" width="8" height="20" fill="#25343F" />
              <rect x="36" y="42" width="8" height="20" fill="#25343F" />
              <rect x="52" y="42" width="8" height="20" fill="#25343F" />
            </svg>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFF449', letterSpacing: 4 }}>
                CENTRAL BANK
              </div>
              <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 58, color: '#F5F5F5' }}>
                Rate Cut से हिचकेगा
              </div>
            </div>
          </div>
          <div style={{
            marginTop: 16, fontFamily: 'Noto Sans Devanagari', fontSize: 34,
            color: '#FFF449', lineHeight: 1.3,
          }}>
            Cut = Inflation और भी fuel
          </div>
        </div>
      </div>

      {/* Declining trajectory chart */}
      <div style={{
        position: 'absolute', top: 460, right: 100, width: 720,
        opacity: sp(140),
        transform: `translateX(${(1 - sp(140)) * 80}px)`,
      }}>
        <div style={{
          backgroundColor: '#FFF449', border: '5px solid #25343F',
          padding: 30,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#25343F', letterSpacing: 4 }}>
            NEXT 6 MONTHS / GDP
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 42, color: '#25343F', marginTop: 6 }}>
            Declining Trajectory
          </div>
          <svg width="100%" height="240" viewBox="0 0 600 240" style={{ marginTop: 14 }}>
            {/* Axes */}
            <line x1="30" y1="20" x2="30" y2="200" stroke="#25343F" strokeWidth="3" />
            <line x1="30" y1="200" x2="580" y2="200" stroke="#25343F" strokeWidth="3" />
            {/* Grid */}
            {[0, 1, 2, 3].map(i => (
              <line key={i} x1="30" y1={50 + i * 40} x2="580" y2={50 + i * 40} stroke="#25343F" strokeWidth="0.5" opacity="0.3" />
            ))}
            {/* Path */}
            {(() => {
              const points = [
                [80, 40], [180, 60], [280, 90], [380, 130], [480, 165], [560, 190]
              ];
              const drawPts = points.slice(0, Math.floor(chartProgress * points.length) + 1);
              const path = drawPts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p[0]} ${p[1]}`).join(' ');
              return (
                <>
                  <path d={path} stroke="#C0392B" strokeWidth="6" fill="none" strokeLinecap="round" />
                  {drawPts.map((p, i) => (
                    <circle key={i} cx={p[0]} cy={p[1]} r="9" fill="#25343F" stroke="#FFF449" strokeWidth="3" />
                  ))}
                </>
              );
            })()}
            <text x="80" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M1</text>
            <text x="180" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M2</text>
            <text x="280" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M3</text>
            <text x="380" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M4</text>
            <text x="480" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M5</text>
            <text x="555" y="225" fontFamily="JetBrains Mono" fontSize="16" fill="#25343F">M6</text>
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene27_Stagflation;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28_QuarterlyData',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28_QuarterlyData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const barAnim = (i: number) => interpolate(frame, [80 + i * 15, 160 + i * 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const inflAnim = (i: number) => interpolate(frame, [220 + i * 15, 300 + i * 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const zeroStamp = sp(420, { damping: 6 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A85E28', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 30%, #FF9B51 0%, #6E3B1A 100%)' }} />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="g28" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M50 0 L0 0 0 50" fill="none" stroke="#25343F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g28)" />
      </svg>

      {/* Shape drift */}
      <div style={{ position: 'absolute', top: -80, left: 200, width: 400, height: 400, border: '6px solid #FFF449', opacity: 0.07, borderRadius: '50%' }} />
      <div style={{ position: 'absolute', bottom: -60, right: 300, width: 320, height: 320, backgroundColor: '#25343F', opacity: 0.05, transform: `rotate(${frame * 0.15}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 600);
        const y = ((frame * (0.5 + r * 0.6) + i * 80) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            fontFamily: 'JetBrains Mono', fontSize: 18, color: '#FFF449', opacity: 0.3,
          }}>%</div>
        );
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 40, left: 100, right: 100, opacity: sp(0) }}>
        <div style={{ display: 'flex', gap: 20, alignItems: 'center' }}>
          <div style={{
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '8px 22px', fontFamily: 'JetBrains Mono',
            fontSize: 24, letterSpacing: 4,
          }}>
            FY27 DATA GRID
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 62, color: '#FFF449', textShadow: '4px 4px 0 #25343F' }}>
            Quarterly Break-Down
          </div>
        </div>
      </div>

      {/* GDP chart (top) */}
      <div style={{
        position: 'absolute', top: 160, left: 100, right: 100,
        backgroundColor: '#FFF449', border: '5px solid #25343F',
        padding: 24, transform: 'rotate(-0.5deg)',
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#25343F', letterSpacing: 4 }}>
          GDP GROWTH ↓
        </div>
        <div style={{ display: 'flex', gap: 20, alignItems: 'flex-end', height: 180, marginTop: 10 }}>
          {[{q: 'Q1', v: 6.8, h: 160}, {q: 'Q2', v: 6.7, h: 155}].map((d, i) => (
            <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 52, color: '#25343F' }}>
                {d.v}%
              </div>
              <div style={{
                width: '80%', height: d.h * barAnim(i),
                backgroundColor: '#25343F', border: '3px solid #FFF449',
                marginTop: 8, boxShadow: '4px 4px 0 rgba(0,0,0,0.4)',
              }} />
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#25343F', marginTop: 6 }}>{d.q}</div>
            </div>
          ))}
          {/* Blank slots for balance */}
          <div style={{ flex: 2 }} />
        </div>
      </div>

      {/* Inflation chart (bottom) */}
      <div style={{
        position: 'absolute', top: 470, left: 100, right: 640,
        backgroundColor: '#25343F', border: '5px solid #FFF449',
        padding: 24, transform: 'rotate(0.5deg)',
      }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FFF449', letterSpacing: 4 }}>
          INFLATION QUARTERLY
        </div>
        <div style={{ display: 'flex', gap: 16, alignItems: 'flex-end', height: 200, marginTop: 10 }}>
          {[
            {q: 'Q1', v: 4.0, h: 100, spike: false},
            {q: 'Q2', v: 4.4, h: 120, spike: false},
            {q: 'Q3', v: 5.2, h: 180, spike: true},
            {q: 'Q4', v: 4.7, h: 140, spike: false},
          ].map((d, i) => (
            <div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 44, color: d.spike ? '#C0392B' : '#FFF449' }}>
                {d.v}%
              </div>
              <div style={{
                width: '80%', height: d.h * inflAnim(i),
                backgroundColor: d.spike ? '#C0392B' : '#FFF449',
                border: '3px solid #25343F', marginTop: 6,
              }} />
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#FFF449', marginTop: 6 }}>{d.q}</div>
            </div>
          ))}
        </div>
        {/* Spike arrow */}
        <div style={{
          position: 'absolute', top: 60, right: 130,
          opacity: inflAnim(2),
          transform: `rotate(-20deg)`,
        }}>
          <div style={{
            backgroundColor: '#C0392B', color: '#FFF449',
            padding: '6px 14px', fontFamily: 'Inter', fontWeight: 900,
            fontSize: 24, border: '3px solid #FFF449',
          }}>
            SPIKE ↑
          </div>
        </div>
      </div>

      {/* Rate cut zero stamp */}
      <div style={{
        position: 'absolute', top: 470, right: 100, width: 480,
        opacity: zeroStamp, transform: `scale(${zeroStamp})`,
      }}>
        <div style={{
          backgroundColor: '#C0392B', color: '#FFF449',
          border: '6px solid #25343F', padding: 28,
          transform: 'rotate(-3deg)',
          boxShadow: '10px 10px 0 #25343F',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, letterSpacing: 4 }}>
            RATE-CUT PROBABILITY
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 200, color: '#FFF449', lineHeight: 0.9, textAlign: 'center', textShadow: '6px 6px 0 #25343F' }}>
            0%
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: '#F5F5F5', marginTop: 4, textAlign: 'center' }}>
            Capital Economics: August MPC में HIKE
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene28_QuarterlyData;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29_HikeConsequence',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29_HikeConsequence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const impactShake = frame < 30 ? Math.sin(frame * 3) * 8 * (1 - frame / 30) : 0;
  const forkGrow = sp(60);
  const toolReveal = sp(300);

  return (
    <AbsoluteFill style={{ backgroundColor: '#8A4E20', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 20%, #FF9B51 0%, #5C3315 100%)' }} />

      {/* Zigzag texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.09 }}>
        <defs>
          <pattern id="z29" width="60" height="20" patternUnits="userSpaceOnUse">
            <path d="M0 10 L15 0 L30 10 L45 0 L60 10" stroke="#25343F" strokeWidth="1.5" fill="none" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#z29)" />
      </svg>

      {/* Shapes */}
      <div style={{ position: 'absolute', top: 200, left: -100, width: 400, height: 400, backgroundColor: '#FFF449', opacity: 0.06, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: -100, right: -80, width: 500, height: 500, border: '7px solid #25343F', opacity: 0.08, borderRadius: '50%' }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 700);
        const y = ((frame * (0.5 + r * 0.6) + i * 70) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            width: 6, height: 6, backgroundColor: '#FFF449', opacity: 0.35,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* Impact title */}
      <div style={{
        position: 'absolute', top: 60, left: 100, right: 100,
        transform: `translateX(${impactShake}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, letterSpacing: 5,
          backgroundColor: '#C0392B', color: '#FFF449',
          padding: '10px 26px', display: 'inline-block',
        }}>
          IF RATE HIKES →
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 110, color: '#FFF449',
          marginTop: 10, lineHeight: 0.95, textShadow: '6px 6px 0 #25343F',
        }}>
          दोनों रास्तों पर <span style={{ color: '#25343F' }}>नुकसान</span>
        </div>
      </div>

      {/* Two forks */}
      <div style={{
        position: 'absolute', top: 300, left: 100, right: 100,
        display: 'flex', gap: 40, justifyContent: 'center',
        opacity: forkGrow, transform: `scale(${forkGrow})`,
      }}>
        <div style={{
          flex: 1, backgroundColor: '#FFF449',
          border: '6px solid #25343F', padding: 30,
          transform: 'rotate(-2deg)',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#25343F',
            letterSpacing: 4, marginBottom: 10,
          }}>
            PATH A
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 96, color: '#25343F', lineHeight: 1 }}>
            EMI ↑
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, color: '#25343F', marginTop: 10 }}>
            Monthly outgo बढ़ेगा
          </div>
          <div style={{
            marginTop: 14,
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '8px 16px', display: 'inline-block',
            fontFamily: 'JetBrains Mono', fontSize: 22,
          }}>
            + INTEREST
          </div>
        </div>
        <div style={{
          flex: 1, backgroundColor: '#25343F',
          border: '6px solid #FFF449', padding: 30,
          transform: 'rotate(2deg)',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#FFF449',
            letterSpacing: 4, marginBottom: 10,
          }}>
            PATH B
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 96, color: '#FFF449', lineHeight: 1 }}>
            TENURE ↑
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, color: '#F5F5F5', marginTop: 10 }}>
            Loan और लंबा खिंचेगा
          </div>
          <div style={{
            marginTop: 14,
            backgroundColor: '#FFF449', color: '#25343F',
            padding: '8px 16px', display: 'inline-block',
            fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 900,
          }}>
            + INTEREST
          </div>
        </div>
      </div>

      {/* Bottom control tool */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        opacity: toolReveal,
        transform: `translateY(${(1 - toolReveal) * 60}px)`,
      }}>
        <div style={{
          backgroundColor: '#C0392B', border: '6px solid #FFF449',
          padding: '24px 40px',
          display: 'flex', alignItems: 'center', gap: 26,
          boxShadow: '10px 10px 0 #25343F',
        }}>
          <svg width="90" height="90" viewBox="0 0 90 90">
            <circle cx="45" cy="45" r="42" fill="#FFF449" stroke="#25343F" strokeWidth="4" />
            <path d="M45 22 L45 45 L60 55" stroke="#25343F" strokeWidth="6" fill="none" strokeLinecap="round" />
            <circle cx="45" cy="45" r="6" fill="#25343F" />
          </svg>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFF449', letterSpacing: 4 }}>
              YOUR ONLY WEAPON
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 62, color: '#FFF449', lineHeight: 1 }}>
              PREPAYMENT — आपके control में
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene29_HikeConsequence;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30_PrepayVsFund',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30_PrepayVsFund: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const outCount = Math.min(3, Math.floor(interpolate(frame, [20, 120], [0, 3.99], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));
  const questionShow = sp(140);
  const prepayReturn = interpolate(frame, [260, 380], [0, 8.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const effectiveReturn = interpolate(frame, [420, 520], [0, 6.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const outControl = ['RATES', 'INFLATION', 'RBI DECISIONS'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B36A32', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #FF9B51 0%, #6E3B1A 100%)' }} />

      {/* Circuit texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.09 }}>
        <defs>
          <pattern id="c30" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="3" fill="#25343F" />
            <line x1="40" y1="0" x2="40" y2="80" stroke="#25343F" strokeWidth="1" />
            <line x1="0" y1="40" x2="80" y2="40" stroke="#25343F" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#c30)" />
      </svg>

      {/* Shapes */}
      <div style={{ position: 'absolute', top: -60, right: -60, width: 400, height: 400, border: '6px solid #FFF449', opacity: 0.08, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', bottom: -60, left: -60, width: 480, height: 480, backgroundColor: '#25343F', opacity: 0.06, borderRadius: '50%' }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 800);
        const y = ((frame * (0.5 + r * 0.7) + i * 80) % 1200) - 100;
        return (
          <div key={i} style={{
            position: 'absolute', left: r * 1920, top: y,
            fontFamily: 'JetBrains Mono', fontSize: 22, color: '#FFF449', opacity: 0.3,
          }}>%</div>
        );
      })}

      {/* Header: out of control */}
      <div style={{ position: 'absolute', top: 40, left: 100, right: 100, opacity: sp(0) }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#25343F', color: '#FFF449',
          padding: '10px 24px', fontFamily: 'JetBrains Mono',
          fontSize: 26, letterSpacing: 5,
        }}>
          OUT OF YOUR CONTROL
        </div>
        <div style={{ display: 'flex', gap: 16, marginTop: 14, flexWrap: 'wrap' }}>
          {outControl.slice(0, outCount + 1).map((t, i) => (
            <div key={i} style={{
              backgroundColor: '#FFF449', color: '#25343F',
              border: '4px solid #25343F',
              padding: '12px 26px',
              fontFamily: 'Inter', fontWeight: 900, fontSize: 46,
              textDecoration: 'line-through',
              textDecorationThickness: '4px',
              textDecorationColor: '#C0392B',
              transform: `rotate(${(i % 2 === 0 ? -1 : 1) * 2}deg)`,
              boxShadow: '6px 6px 0 #25343F',
            }}>
              {t}
            </div>
          ))}
        </div>
      </div>

      {/* Question */}
      <div style={{
        position: 'absolute', top: 270, left: 100, right: 100,
        opacity: questionShow, transform: `scale(${questionShow})`,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#25343F', color: '#FFF449',
          padding: '20px 40px', border: '6px solid #FFF449',
          fontFamily: 'Yantra One', fontSize: 66,
          transform: 'rotate(-1deg)',
          boxShadow: '10px 10px 0 rgba(0,0,0,0.4)',
        }}>
          ₹1L prepay करें या Mutual Fund?
        </div>
      </div>

      {/* Two return meters */}
      <div style={{
        position: 'absolute', top: 420, left: 100, right: 100,
        display: 'flex', gap: 40,
      }}>
        {/* Prepayment card */}
        <div style={{
          flex: 1, backgroundColor: '#FFF449',
          border: '6px solid #25343F', padding: 30,
          transform: 'rotate(-1deg)',
          opacity: sp(220),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#25343F', letterSpacing: 4 }}>
            PREPAYMENT
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 160, color: '#25343F', lineHeight: 0.9, marginTop: 8 }}>
            {prepayReturn.toFixed(1)}%
          </div>
          <div style={{
            marginTop: 8,
            display: 'inline-block',
            backgroundColor: '#25343F', color: '#FFF449',
            padding: '8px 16px', fontFamily: 'JetBrains Mono',
            fontSize: 22, letterSpacing: 3,
          }}>
            GUARANTEED · RISK-FREE
          </div>
          {/* Simple meter */}
          <svg width="100%" height="30" viewBox="0 0 400 30" style={{ marginTop: 14 }}>
            <rect x="0" y="10" width="400" height="10" fill="#25343F" opacity="0.2" />
            <rect x="0" y="10" width={(prepayReturn / 10) * 400} height="10" fill="#25343F" />
          </svg>
        </div>

        {/* Effective card */}
        <div style={{
          flex: 1, backgroundColor: '#25343F',
          border: '6px solid #FFF449', padding: 30,
          transform: 'rotate(1deg)',
          opacity: sp(380),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, color: '#FFF449', letterSpacing: 4 }}>
            POST-TAX EFFECTIVE
          </div>
          <div style={{ fontFamily: 'Inter', fontWeight: 900, fontSize: 160, color: '#FFF449', lineHeight: 0.9, marginTop: 8 }}>
            {effectiveReturn.toFixed(1)}%
          </div>
          <div style={{
            marginTop: 8,
            display: 'inline-block',
            backgroundColor: '#FFF449', color: '#25343F',
            padding: '8px 16px', fontFamily: 'JetBrains Mono',
            fontSize: 22, letterSpacing: 3, fontWeight: 900,
          }}>
            6–7% NET RETURN
          </div>
          <svg width="100%" height="30" viewBox="0 0 400 30" style={{ marginTop: 14 }}>
            <rect x="0" y="10" width="400" height="10" fill="#FFF449" opacity="0.2" />
            <rect x="0" y="10" width={(effectiveReturn / 10) * 400} height="10" fill="#FFF449" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#25343F', opacity: 0.5, letterSpacing: 2,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene30_PrepayVsFund;
```

