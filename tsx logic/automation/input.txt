```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'FeesRisingTornado',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const FeesRisingTornado: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camZoom = interpolate(frame, [0, 200, 400, 630], [1, 1.05, 1.02, 1.08], { extrapolateRight: 'clamp' });

  // Bar chart heights for fee growth
  const bar1H = sp(50) * 140;
  const bar2H = sp(70) * 220;
  const bar3H = sp(90) * 310;
  const bar4H = sp(110) * 420;
  const bar5H = sp(130) * 540;

  // Big number reveals
  const num1Op = interpolate(frame, [40, 70], [0, 1], { extrapolateRight: 'clamp' });
  const num2Op = interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' });
  const num3Op = interpolate(frame, [340, 370], [0, 1], { extrapolateRight: 'clamp' });
  const num4Op = interpolate(frame, [490, 520], [0, 1], { extrapolateRight: 'clamp' });

  // Counter animation 1.68 crore
  const counterProgress = interpolate(frame, [200, 290], [0, 1.68], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Percentage counter 7-15%
  const pctCounter = interpolate(frame, [60, 150], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Arrow rise
  const arrowY = interpolate(frame, [100, 180], [0, -80], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(31,21,12,0.08) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(8,6,22,0.06) 0%, transparent 50%)',
      }} />

      {/* Shape layers - drifting geometric */}
      <div style={{
        position: 'absolute', top: '10%', left: '5%',
        width: 380, height: 380, borderRadius: '50%',
        border: '2px solid #1F150C', opacity: 0.06,
        transform: `translate(${Math.sin(frame * 0.01) * 30}px, ${Math.cos(frame * 0.012) * 20}px) rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '60%', right: '8%',
        width: 280, height: 280,
        border: '2px solid #080616', opacity: 0.05,
        transform: `translate(${Math.sin(frame * 0.015) * 25}px, ${Math.cos(frame * 0.01) * 15}px) rotate(${-frame * 0.3}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', left: '50%',
        width: 200, height: 200,
        border: '2px solid #1F150C', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `translate(-50%, -50%) rotate(${frame * 0.4}deg) scale(${1 + Math.sin(frame * 0.02) * 0.1})`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r1 = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const r3 = seededRandom(i + 100);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 6 + r3 * 8,
            height: 6 + r3 * 8,
            borderRadius: '50%',
            backgroundColor: r3 > 0.5 ? '#1F150C' : '#080616',
            opacity: 0.15,
            transform: `translate(${Math.sin(frame * 0.02 + i) * 20}px, ${Math.cos(frame * 0.018 + i) * 25}px)`,
          }} />
        );
      })}

      {/* Main content container */}
      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camZoom})`,
        display: 'flex', flexDirection: 'column',
        padding: '60px 90px',
      }}>

        {/* HEADER - Beat 1: 0-180 */}
        <div style={{
          opacity: interpolate(frame, [0, 25, 170, 195], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          transform: `translateY(${(1 - sp(5)) * -40}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '12px 28px',
            backgroundColor: '#1F150C',
            color: '#DFF1F1',
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 700,
            letterSpacing: '4px',
            marginBottom: 30,
          }}>
            FEE INFLATION REPORT
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1.05,
            letterSpacing: '-2px',
          }}>
            फीस का<br />
            <span style={{ color: '#080616' }}>बवंडर</span>
          </div>
        </div>

        {/* BEAT 1: Percentage bars 0-180 */}
        <div style={{
          position: 'absolute',
          right: 120, top: 180,
          opacity: interpolate(frame, [40, 70, 170, 195], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          display: 'flex', alignItems: 'flex-end', gap: 24,
        }}>
          {[
            { h: bar1H, label: '2019', val: 7 },
            { h: bar2H, label: '2020', val: 9 },
            { h: bar3H, label: '2022', val: 11 },
            { h: bar4H, label: '2024', val: 13 },
            { h: bar5H, label: '2026', val: 15 },
          ].map((b, i) => (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 32,
                fontWeight: 800,
                color: '#080616',
              }}>{b.val}%</div>
              <div style={{
                width: 70,
                height: b.h,
                background: `linear-gradient(180deg, #080616 0%, #1F150C 100%)`,
                borderRadius: '8px 8px 0 0',
                boxShadow: '0 0 20px rgba(8,6,22,0.3)',
              }} />
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 600,
                color: '#1F150C',
              }}>{b.label}</div>
            </div>
          ))}
        </div>

        {/* BEAT 1 caption */}
        <div style={{
          position: 'absolute',
          bottom: 130, left: 90,
          opacity: interpolate(frame, [80, 110, 170, 195], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 47,
          fontWeight: 700,
          color: '#1F150C',
          maxWidth: 900,
          lineHeight: 1.3,
        }}>
          स्कूल फीस सालाना <span style={{ color: '#080616', fontSize: 64, fontWeight: 900 }}>{Math.floor(pctCounter)}%</span> तक बढ़ रही है
        </div>

      </div>

      {/* BEAT 2: 1.68 करोड - 200-380 */}
      <div style={{
        position: 'absolute', inset: 0,
        opacity: interpolate(frame, [195, 230, 360, 385], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
        display: 'flex', flexDirection: 'column',
        alignItems: 'center', justifyContent: 'center',
        padding: 80,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 51,
          fontWeight: 600,
          color: '#1F150C',
          marginBottom: 20,
          opacity: 0.7,
        }}>
          एलीट स्कूल — KG से 12वीं तक
        </div>

        <div style={{
          display: 'flex', alignItems: 'baseline', gap: 20,
          transform: `scale(${0.7 + sp(210) * 0.3})`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 36,
            color: '#080616',
            fontWeight: 700,
          }}>₹</div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 221,
            fontWeight: 900,
            color: '#080616',
            lineHeight: 1,
            textShadow: '6px 6px 0 rgba(31,21,12,0.15)',
          }}>
            {counterProgress.toFixed(2)}
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 800,
            color: '#1F150C',
          }}>करोड़</div>
        </div>

        <div style={{
          marginTop: 40,
          padding: '16px 40px',
          backgroundColor: '#1F150C',
          color: '#DFF1F1',
          fontFamily: 'Inter',
          fontSize: 32,
          fontWeight: 700,
          letterSpacing: '2px',
          transform: `translateY(${arrowY * 0.3}px)`,
        }}>
          एक बच्चे की पढ़ाई की कीमत
        </div>

        {/* Rising arrow SVG */}
        <svg width="120" height="160" style={{
          position: 'absolute',
          right: 200, top: 200,
          transform: `translateY(${arrowY}px) rotate(15deg)`,
          opacity: interpolate(frame, [240, 280], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <path d="M60 150 L60 30 M30 60 L60 30 L90 60" stroke="#080616" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* BEAT 3: MBBS Govt vs Private comparison - 390-560 */}
      <div style={{
        position: 'absolute', inset: 0,
        opacity: interpolate(frame, [385, 420, 550, 580], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
        display: 'flex', flexDirection: 'column',
        padding: '80px 100px',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 85,
          fontWeight: 900,
          color: '#1F150C',
          marginBottom: 50,
          transform: `translateX(${(1 - sp(395)) * -50}px)`,
        }}>
          MBBS की <span style={{ color: '#080616' }}>हकीकत</span>
        </div>

        <div style={{ display: 'flex', gap: 40, flex: 1 }}>
          {/* Govt card */}
          <div style={{
            flex: 1,
            backgroundColor: 'rgba(31,21,12,0.08)',
            border: '3px solid #1F150C',
            borderRadius: 24,
            padding: 50,
            display: 'flex', flexDirection: 'column', justifyContent: 'center',
            transform: `translateY(${(1 - sp(410)) * 60}px) scale(${0.9 + sp(410) * 0.1})`,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#1F150C',
              letterSpacing: '3px',
              marginBottom: 20,
            }}>GOVT COLLEGE</div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 153,
              fontWeight: 900,
              color: '#1F150C',
              lineHeight: 1,
            }}>1.9<span style={{ fontSize: 64 }}> लाख</span></div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              fontWeight: 600,
              color: '#1F150C',
              marginTop: 16,
              opacity: 0.7,
            }}>5 साल की कुल फीस</div>

            {/* Mini icon */}
            <svg width="80" height="80" style={{ position: 'absolute', top: 30, right: 30, opacity: 0.4 }}>
              <rect x="10" y="30" width="60" height="40" fill="none" stroke="#1F150C" strokeWidth="3" />
              <polygon points="10,30 40,10 70,30" fill="none" stroke="#1F150C" strokeWidth="3" />
              <rect x="30" y="45" width="20" height="25" fill="#1F150C" />
            </svg>
          </div>

          {/* Private card */}
          <div style={{
            flex: 1,
            backgroundColor: '#080616',
            border: '3px solid #080616',
            borderRadius: 24,
            padding: 50,
            display: 'flex', flexDirection: 'column', justifyContent: 'center',
            transform: `translateY(${(1 - sp(440)) * 60}px) scale(${0.9 + sp(440) * 0.1})`,
            boxShadow: '0 20px 60px rgba(8,6,22,0.3)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#DFF1F1',
              letterSpacing: '3px',
              marginBottom: 20,
            }}>PRIVATE COLLEGE</div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1,
            }}>50L – 1Cr</div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              fontWeight: 600,
              color: '#DFF1F1',
              marginTop: 16,
              opacity: 0.85,
            }}>50 गुना ज्यादा</div>

            <svg width="80" height="80" style={{ position: 'absolute', top: 30, right: 30, opacity: 0.4 }}>
              <rect x="5" y="25" width="70" height="50" fill="none" stroke="#DFF1F1" strokeWidth="3" />
              <rect x="20" y="40" width="12" height="35" fill="#DFF1F1" />
              <rect x="48" y="40" width="12" height="35" fill="#DFF1F1" />
              <polygon points="5,25 40,5 75,25" fill="none" stroke="#DFF1F1" strokeWidth="3" />
            </svg>
          </div>
        </div>
      </div>

      {/* BEAT 4: Final hard hitting line - 570-630 */}
      <div style={{
        position: 'absolute', inset: 0,
        opacity: interpolate(frame, [580, 605, 615, 630], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        padding: 100,
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 119,
          fontWeight: 900,
          color: '#1F150C',
          textAlign: 'center',
          lineHeight: 1.1,
          transform: `scale(${0.9 + sp(585) * 0.1})`,
        }}>
          हर पैटर्न<br />
          <span style={{ color: '#080616', borderBottom: '6px solid #080616' }}>एक ही है</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default FeesRisingTornado;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'WhereMoneyGoes',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WhereMoneyGoes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.5, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, 510], [0, -15], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 250, 510], [1.02, 1.04, 1.07], { extrapolateRight: 'clamp' });

  // Course grid stagger
  const courses = [
    { name: 'MBBS', range: '50L–1Cr', delay: 30 },
    { name: 'MBA', range: '25L–80L', delay: 50 },
    { name: 'ENGINEERING', range: '15L–50L', delay: 70 },
    { name: 'LAW', range: '10L–40L', delay: 90 },
  ];

  // Pie segments for "where money goes"
  const pieDelay = 270;
  const segments = [
    { label: 'BRANDING', pct: 28, color: '#080616', delay: pieDelay + 30 },
    { label: 'ADMIN', pct: 32, color: '#1F150C', delay: pieDelay + 50 },
    { label: 'INFRA', pct: 22, color: '#3a2a18', delay: pieDelay + 70 },
    { label: 'TEACHING', pct: 18, color: '#5a4028', delay: pieDelay + 90 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(8,6,22,0.07) 0%, transparent 55%)',
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diag" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '-5%',
        width: 500, height: 500,
        border: '3px solid #080616', opacity: 0.05,
        borderRadius: '40%',
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.015) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-8%',
        width: 600, height: 600,
        border: '3px solid #1F150C', opacity: 0.06,
        transform: `rotate(${-frame * 0.1}deg)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 200);
        const r2 = seededRandom(i + 250);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4 + seededRandom(i + 300) * 8,
            height: 4 + seededRandom(i + 300) * 8,
            backgroundColor: '#1F150C',
            opacity: 0.1,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.025 + i) * 20}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `translateX(${camX}px) scale(${camScale})`,
      }}>

        {/* BEAT 1: Course pattern grid - 0-260 */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: interpolate(frame, [0, 25, 240, 265], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          padding: '80px 100px',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 700,
            color: '#080616',
            letterSpacing: '5px',
            marginBottom: 20,
            transform: `translateX(${(1 - sp(0)) * -30}px)`,
          }}>EVERY COURSE — SAME STORY</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1.05,
            marginBottom: 50,
            transform: `translateY(${(1 - sp(10)) * 30}px)`,
          }}>
            हर जगह <span style={{ color: '#080616' }}>एक ही</span><br />
            पैटर्न
          </div>

          {/* 2x2 grid */}
          <div style={{
            display: 'grid',
            gridTemplateColumns: '1fr 1fr',
            gap: 30,
            maxWidth: 1400,
          }}>
            {courses.map((c, i) => (
              <div key={i} style={{
                backgroundColor: i % 2 === 0 ? '#1F150C' : '#080616',
                borderRadius: 16,
                padding: '36px 44px',
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                opacity: sp(c.delay),
                transform: `translateX(${(1 - sp(c.delay)) * (i % 2 === 0 ? -100 : 100)}px)`,
                boxShadow: '0 12px 40px rgba(8,6,22,0.2)',
              }}>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 47,
                  fontWeight: 800,
                  color: '#DFF1F1',
                  letterSpacing: '2px',
                }}>{c.name}</div>
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 51,
                  fontWeight: 700,
                  color: '#DFF1F1',
                }}>₹{c.range}</div>
              </div>
            ))}
          </div>
        </div>

        {/* BEAT 2: THE REAL QUESTION - 265-380 */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: interpolate(frame, [265, 295, 365, 390], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          padding: 100,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 700,
            letterSpacing: '6px',
            color: '#080616',
            marginBottom: 30,
            opacity: sp(275),
          }}>BUT THE REAL QUESTION</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 153,
            fontWeight: 900,
            color: '#1F150C',
            textAlign: 'center',
            lineHeight: 1.05,
            transform: `scale(${0.85 + sp(285) * 0.15})`,
          }}>
            पैसा जा<br />
            <span style={{
              color: '#DFF1F1',
              backgroundColor: '#080616',
              padding: '8px 32px',
              display: 'inline-block',
              marginTop: 16,
            }}>कहाँ रहा है?</span>
          </div>

          {/* Question mark accent */}
          <div style={{
            position: 'absolute',
            right: 200, top: 150,
            fontFamily: 'Yantra One',
            fontSize: 340,
            fontWeight: 900,
            color: '#080616',
            opacity: 0.08,
            transform: `rotate(${15 + Math.sin(frame * 0.04) * 3}deg)`,
          }}>?</div>
        </div>

        {/* BEAT 3: Where the money flows - PIE/FLOW - 390-510 */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: interpolate(frame, [390, 420, 495, 510], [0, 1, 1, 0], { extrapolateRight: 'clamp' }),
          display: 'flex',
          padding: '80px 100px',
          gap: 60,
        }}>
          {/* Left: Pie chart */}
          <div style={{
            width: 580,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              letterSpacing: '4px',
              color: '#080616',
              marginBottom: 24,
            }}>FEE BREAKDOWN</div>

            <svg width="440" height="440" viewBox="-110 -110 220 220" style={{
              transform: `rotate(${interpolate(frame, [395, 480], [-90, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })}deg)`,
            }}>
              {segments.reduce<{ start: number; arcs: React.ReactElement[] }>((acc, s, i) => {
                const progress = sp(s.delay);
                const sweep = (s.pct / 100) * 360 * progress;
                const startAngle = (acc.start * Math.PI) / 180;
                const endAngle = ((acc.start + sweep) * Math.PI) / 180;
                const r = 90;
                const x1 = Math.cos(startAngle) * r;
                const y1 = Math.sin(startAngle) * r;
                const x2 = Math.cos(endAngle) * r;
                const y2 = Math.sin(endAngle) * r;
                const largeArc = sweep > 180 ? 1 : 0;
                const path = `M 0 0 L ${x1} ${y1} A ${r} ${r} 0 ${largeArc} 1 ${x2} ${y2} Z`;
                acc.arcs.push(
                  <path key={i} d={path} fill={s.color} stroke="#DFF1F1" strokeWidth="2" />
                );
                acc.start += (s.pct / 100) * 360;
                return acc;
              }, { start: 0, arcs: [] }).arcs}
              <circle r="35" fill="#DFF1F1" />
            </svg>
          </div>

          {/* Right: Legend with bars */}
          <div style={{
            flex: 1,
            display: 'flex', flexDirection: 'column',
            justifyContent: 'center', gap: 18,
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 68,
              fontWeight: 900,
              color: '#1F150C',
              marginBottom: 20,
              transform: `translateX(${(1 - sp(420)) * 40}px)`,
            }}>क्लासरूम तक?</div>

            {segments.map((s, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 20,
                opacity: sp(s.delay + 20),
                transform: `translateX(${(1 - sp(s.delay + 20)) * 50}px)`,
              }}>
                <div style={{ width: 24, height: 24, backgroundColor: s.color, borderRadius: 4 }} />
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 30,
                  fontWeight: 700,
                  color: '#1F150C',
                  letterSpacing: '2px',
                  width: 200,
                }}>{s.label}</div>
                <div style={{
                  flex: 1, height: 18,
                  backgroundColor: 'rgba(31,21,12,0.1)',
                  borderRadius: 9, overflow: 'hidden',
                }}>
                  <div style={{
                    width: `${sp(s.delay + 30) * s.pct * 2}%`,
                    height: '100%',
                    backgroundColor: s.color,
                    borderRadius: 9,
                  }} />
                </div>
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 32,
                  fontWeight: 800,
                  color: s.color,
                  width: 80,
                  textAlign: 'right',
                }}>{s.pct}%</div>
              </div>
            ))}

            <div style={{
              marginTop: 20,
              padding: '14px 24px',
              borderLeft: '6px solid #080616',
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 600,
              color: '#080616',
              opacity: sp(470),
            }}>
              पढ़ाई पर सिर्फ 18%
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default WhereMoneyGoes;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CheckOrCross',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CheckOrCross: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.45, stiffness: 115, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, 570], [0, -0.5], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 570], [1, 1.06], { extrapolateRight: 'clamp' });

  // Items to evaluate
  const items = [
    { label: 'प्रोफेसर बेहतर?', status: 'cross', delay: 30 },
    { label: 'इंफ्रास्ट्रक्चर अपग्रेड?', status: 'cross', delay: 70 },
    { label: 'एडमिन ऑफिसेज बड़े', status: 'check', delay: 180 },
    { label: 'नए इवेंट हॉल्स', status: 'check', delay: 220 },
    { label: 'ब्रांडिंग पर करोड़ों', status: 'check', delay: 260 },
  ];

  // Final question reveal
  const qOp = interpolate(frame, [380, 420], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(31,21,12,0.06) 0%, transparent 50%, rgba(8,6,22,0.08) 100%)',
      }} />

      {/* Circle texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="dots" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="2" fill="#1F150C" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '15%', left: '8%',
        width: 320, height: 320,
        border: '2px solid #080616', opacity: 0.06,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '5%',
        width: 420, height: 420,
        border: '2px solid #1F150C', opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', right: '40%',
        width: 180, height: 180,
        backgroundColor: '#080616', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${-frame * 0.3}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r1 = seededRandom(i + 400);
        const r2 = seededRandom(i + 450);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.018 + i) * 30}px, ${Math.cos(frame * 0.022 + i) * 22}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale}) rotate(${camRot}deg)`,
        padding: '60px 100px',
      }}>

        {/* Header */}
        <div style={{
          opacity: interpolate(frame, [0, 25], [0, 1], { extrapolateRight: 'clamp' }),
          transform: `translateY(${(1 - sp(0)) * -30}px)`,
          marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 24px',
            backgroundColor: '#080616',
            color: '#DFF1F1',
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: '5px',
            marginBottom: 16,
          }}>REALITY CHECK</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
          }}>
            फीस का <span style={{ color: '#080616' }}>हिसाब</span>
          </div>
        </div>

        {/* Check/Cross list */}
        <div style={{
          display: 'flex', flexDirection: 'column',
          gap: 18,
          maxWidth: 1500,
        }}>
          {items.map((item, i) => {
            const isCheck = item.status === 'check';
            return (
              <div key={i} style={{
                display: 'flex', alignItems: 'center',
                gap: 30,
                padding: '20px 36px',
                backgroundColor: isCheck ? 'rgba(8,6,22,0.08)' : 'rgba(31,21,12,0.05)',
                border: `3px solid ${isCheck ? '#080616' : '#1F150C'}`,
                borderRadius: 16,
                opacity: sp(item.delay),
                transform: `translateX(${(1 - sp(item.delay)) * (isCheck ? 80 : -80)}px)`,
              }}>
                {/* Icon */}
                <div style={{
                  width: 70, height: 70,
                  backgroundColor: isCheck ? '#080616' : '#1F150C',
                  borderRadius: 12,
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  transform: `scale(${0.5 + sp(item.delay + 10) * 0.5})`,
                }}>
                  {isCheck ? (
                    <svg width="40" height="40" viewBox="0 0 40 40">
                      <path d="M8 20 L18 30 L34 10" stroke="#DFF1F1" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                    </svg>
                  ) : (
                    <svg width="40" height="40" viewBox="0 0 40 40">
                      <line x1="10" y1="10" x2="30" y2="30" stroke="#DFF1F1" strokeWidth="6" strokeLinecap="round" />
                      <line x1="30" y1="10" x2="10" y2="30" stroke="#DFF1F1" strokeWidth="6" strokeLinecap="round" />
                    </svg>
                  )}
                </div>

                {/* Label */}
                <div style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 51,
                  fontWeight: 700,
                  color: isCheck ? '#080616' : '#1F150C',
                  flex: 1,
                }}>{item.label}</div>

                {/* Status text */}
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 24,
                  fontWeight: 800,
                  letterSpacing: '3px',
                  padding: '8px 18px',
                  color: '#DFF1F1',
                  backgroundColor: isCheck ? '#080616' : '#1F150C',
                  borderRadius: 6,
                }}>
                  {isCheck ? 'YES — पैसा यहाँ' : 'NO — झूठा वादा'}
                </div>
              </div>
            );
          })}
        </div>

        {/* Beat 2: Final question - 380-540 */}
        <div style={{
          position: 'absolute',
          bottom: 70, left: 100, right: 100,
          opacity: qOp,
          transform: `translateY(${(1 - sp(380)) * 50}px)`,
        }}>
          <div style={{
            backgroundColor: '#1F150C',
            padding: '30px 50px',
            borderRadius: 20,
            borderLeft: '12px solid #080616',
            display: 'flex', alignItems: 'center', gap: 40,
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 140,
              fontWeight: 900,
              color: '#DFF1F1',
              opacity: 0.3,
              lineHeight: 1,
            }}>?</div>
            <div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '4px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 10,
              }}>आपसे सवाल</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1.2,
              }}>
                क्या आपको लगा फीस का<br />
                <span style={{ color: '#DFF1F1', textDecoration: 'underline', textDecorationColor: '#DFF1F1', textDecorationThickness: 4 }}>पूरा मूल्य मिला?</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default CheckOrCross;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TimeBomb85',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TimeBomb85: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on impact at frame 460
  const shakeX = frame > 460 && frame < 490 ? Math.sin(frame * 2) * (490 - frame) * 0.4 : 0;
  const shakeY = frame > 460 && frame < 490 ? Math.cos(frame * 2.3) * (490 - frame) * 0.3 : 0;

  // Camera zoom on bomb
  const camScale = interpolate(frame, [0, 300, 460, 600], [1, 1.03, 1.12, 1.08], { extrapolateRight: 'clamp' });

  // 85% counter
  const pctCounter = interpolate(frame, [40, 160], [0, 85], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bomb fuse - shrinks over time
  const fuseProgress = interpolate(frame, [200, 460], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Ring fill
  const ringFill = interpolate(frame, [40, 200], [0, 0.85], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const circumference = 2 * Math.PI * 180;

  // Tick tick pulse
  const tickPulse = Math.sin(frame * 0.3) * 0.5 + 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: frame > 460
          ? 'radial-gradient(circle at 50% 50%, rgba(8,6,22,0.25) 0%, transparent 60%)'
          : 'radial-gradient(circle at 50% 50%, rgba(31,21,12,0.08) 0%, transparent 50%)',
      }} />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{
        position: 'absolute', opacity: 0.05,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}>
        {[100, 200, 300, 400, 500, 600].map((r, i) => (
          <circle key={i} cx="960" cy="540" r={r + Math.sin(frame * 0.03 + i) * 10} fill="none" stroke="#080616" strokeWidth="1" />
        ))}
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '8%',
        width: 350, height: 350,
        border: '3px solid #1F150C', opacity: 0.05,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', left: '5%',
        width: 280, height: 280,
        backgroundColor: '#080616', opacity: 0.04,
        borderRadius: '50%',
        transform: `scale(${1 + tickPulse * 0.1})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const r1 = seededRandom(i + 600);
        const r2 = seededRandom(i + 650);
        const r3 = seededRandom(i + 700);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 4 + r3 * 6,
            height: 4 + r3 * 6,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.025 + i) * 20}px) translate(${shakeX}px, ${shakeY}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale}) translate(${shakeX}px, ${shakeY}px)`,
        padding: '70px 100px',
      }}>

        {/* Header */}
        <div style={{
          opacity: interpolate(frame, [0, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 24px',
            backgroundColor: '#080616',
            color: '#DFF1F1',
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: '5px',
            marginBottom: 20,
          }}>NBFC ALERT</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
          }}>
            टाइम <span style={{ color: '#080616' }}>बम</span>
          </div>
        </div>

        {/* Main content - left circle, right text */}
        <div style={{
          display: 'flex', alignItems: 'center',
          marginTop: 40,
          gap: 80,
        }}>
          {/* Left: Ring progress with bomb */}
          <div style={{
            width: 500, height: 500,
            position: 'relative',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            transform: `scale(${0.7 + sp(20) * 0.3})`,
          }}>
            {/* Outer ring */}
            <svg width="500" height="500" viewBox="-250 -250 500 500" style={{ position: 'absolute', transform: 'rotate(-90deg)' }}>
              <circle r="180" fill="none" stroke="rgba(31,21,12,0.15)" strokeWidth="32" />
              <circle r="180" fill="none" stroke="#080616" strokeWidth="32"
                strokeDasharray={circumference}
                strokeDashoffset={circumference * (1 - ringFill)}
                strokeLinecap="round" />
            </svg>

            {/* Center bomb icon */}
            <div style={{
              position: 'absolute',
              transform: `scale(${1 + tickPulse * 0.05})`,
            }}>
              <svg width="160" height="180" viewBox="0 0 160 180">
                {/* Fuse */}
                <path d={`M 80 ${30 - fuseProgress * 20} Q 100 ${20 - fuseProgress * 15} 110 ${10 - fuseProgress * 8}`}
                  stroke="#1F150C" strokeWidth="4" fill="none" strokeLinecap="round" />
                {/* Spark */}
                {fuseProgress > 0 && (
                  <circle cx={110} cy={10 - fuseProgress * 8} r={6 + tickPulse * 3} fill="#080616" />
                )}
                {/* Bomb body */}
                <circle cx="80" cy="100" r="65" fill="#080616" />
                {/* Highlight */}
                <ellipse cx="65" cy="85" rx="15" ry="20" fill="#1F150C" opacity="0.5" />
                {/* Top piece */}
                <rect x="70" y="30" width="20" height="15" fill="#1F150C" />
              </svg>
            </div>

            {/* Percentage on top */}
            <div style={{
              position: 'absolute',
              bottom: -30, left: '50%',
              transform: 'translateX(-50%)',
              fontFamily: 'Yantra One',
              fontSize: 102,
              fontWeight: 900,
              color: '#080616',
              backgroundColor: '#DFF1F1',
              padding: '0 20px',
            }}>{Math.floor(pctCounter)}%</div>
          </div>

          {/* Right: Text content */}
          <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 30 }}>
            <div style={{
              opacity: sp(60),
              transform: `translateX(${(1 - sp(60)) * 60}px)`,
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                letterSpacing: '4px',
                color: '#080616',
                marginBottom: 10,
              }}>NBFC LOAN BOOK</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 60,
                fontWeight: 800,
                color: '#1F150C',
                lineHeight: 1.15,
              }}>
                <span style={{
                  backgroundColor: '#080616',
                  color: '#DFF1F1',
                  padding: '4px 16px',
                }}>85%+</span> अभी<br />
                मॉरटोरियम में
              </div>
            </div>

            <div style={{
              opacity: sp(140),
              padding: '20px 28px',
              borderLeft: '8px solid #080616',
              backgroundColor: 'rgba(8,6,22,0.06)',
              transform: `translateX(${(1 - sp(140)) * 60}px)`,
            }}>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 600,
                color: '#1F150C',
                lineHeight: 1.3,
              }}>
                असली स्ट्रेस अभी सामने<br />
                आना <strong style={{ color: '#080616' }}>बाकी है</strong>
              </div>
            </div>

            {/* Tick tick indicator */}
            <div style={{
              opacity: sp(220),
              display: 'flex', alignItems: 'center', gap: 16,
            }}>
              <div style={{
                width: 18, height: 18,
                borderRadius: '50%',
                backgroundColor: '#080616',
                transform: `scale(${1 + tickPulse * 0.4})`,
                opacity: 0.3 + tickPulse * 0.7,
              }} />
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 36,
                fontWeight: 700,
                color: '#080616',
                letterSpacing: '4px',
              }}>TICK · TICK · TICK</div>
            </div>
          </div>
        </div>

        {/* Bottom warning - impact moment at frame 460 */}
        <div style={{
          position: 'absolute',
          bottom: 60, left: 100, right: 100,
          opacity: interpolate(frame, [430, 470], [0, 1], { extrapolateRight: 'clamp' }),
          transform: `scale(${0.9 + sp(440) * 0.1})`,
        }}>
          <div style={{
            backgroundColor: '#080616',
            padding: '24px 48px',
            borderRadius: 16,
            textAlign: 'center',
            fontFamily: 'Yantra One',
            fontSize: 64,
            fontWeight: 900,
            color: '#DFF1F1',
            letterSpacing: '1px',
            boxShadow: '0 20px 60px rgba(8,6,22,0.4)',
          }}>
            जब फूटेगा — किसी को कम्फर्टेबल नहीं लगेगा
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(31,21,12,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default TimeBomb85;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ThreeArrows',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ThreeArrows: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit
  const camScale = interpolate(frame, [0, 480], [1, 1.05], { extrapolateRight: 'clamp' });
  const camY = interpolate(frame, [0, 480], [0, -10], { extrapolateRight: 'clamp' });

  // Budget cut announcement 0-130
  const budgetOp = interpolate(frame, [0, 30, 120, 145], [0, 1, 1, 0], { extrapolateRight: 'clamp' });

  // Three forces reveal 150-450
  const force1Op = sp(160);
  const force2Op = sp(220);
  const force3Op = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(31,21,12,0.05) 0%, transparent 40%, rgba(8,6,22,0.1) 100%)',
      }} />

      {/* Hexagonal grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hex" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,0 60,15 60,37 30,52 0,37 0,15" fill="none" stroke="#1F150C" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '40%',
        width: 400, height: 400,
        border: '2px solid #080616', opacity: 0.05,
        borderRadius: '50%',
        transform: `translate(-50%, 0) scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '10%',
        width: 240, height: 240,
        border: '2px solid #1F150C', opacity: 0.06,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r1 = seededRandom(i + 800);
        const r2 = seededRandom(i + 850);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 28}px, ${Math.cos(frame * 0.024 + i) * 22}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale}) translateY(${camY}px)`,
        padding: '60px 80px',
      }}>

        {/* BEAT 1: Budget 2024-25 announcement */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: budgetOp,
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          padding: 100,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 700,
            letterSpacing: '6px',
            color: '#080616',
            marginBottom: 20,
            transform: `translateY(${(1 - sp(0)) * -30}px)`,
          }}>UNION BUDGET 2024-25</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 136,
            fontWeight: 900,
            color: '#1F150C',
            textAlign: 'center',
            lineHeight: 1.05,
            transform: `scale(${0.8 + sp(10) * 0.2})`,
          }}>
            स्कॉलरशिप<br />
            <span style={{
              color: '#DFF1F1',
              backgroundColor: '#080616',
              padding: '4px 32px',
              display: 'inline-block',
              marginTop: 12,
            }}>बंद</span>
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            fontWeight: 600,
            color: '#1F150C',
            marginTop: 30,
            opacity: 0.7,
            transform: `translateY(${(1 - sp(40)) * 20}px)`,
          }}>
            कमजोर वर्ग की अफोर्डेबिलिटी और कम
          </div>
        </div>

        {/* BEAT 2: Three forces converging */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: interpolate(frame, [145, 175], [0, 1], { extrapolateRight: 'clamp' }),
          padding: '60px 80px',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: '5px',
            color: '#080616',
            marginBottom: 14,
          }}>THE TRIPLE BLOW</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 85,
            fontWeight: 900,
            color: '#1F150C',
            marginBottom: 50,
            lineHeight: 1,
          }}>
            तिहरी <span style={{ color: '#080616' }}>मार</span>
          </div>

          {/* Three cards horizontal */}
          <div style={{ display: 'flex', gap: 30, alignItems: 'stretch' }}>
            {/* Force 1: Fees rising */}
            <div style={{
              flex: 1,
              backgroundColor: '#1F150C',
              borderRadius: 20,
              padding: 36,
              opacity: force1Op,
              transform: `translateY(${(1 - force1Op) * 80}px)`,
              boxShadow: '0 16px 50px rgba(31,21,12,0.3)',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                fontWeight: 900,
                color: '#DFF1F1',
                opacity: 0.4,
                marginBottom: 16,
              }}>01</div>

              {/* Arrow up */}
              <svg width="80" height="100" viewBox="0 0 80 100" style={{ marginBottom: 16 }}>
                <line x1="40" y1="95" x2="40" y2="20" stroke="#DFF1F1" strokeWidth="8" strokeLinecap="round" />
                <polyline points="15,40 40,20 65,40" stroke="#DFF1F1" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>

              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.7,
                marginBottom: 12,
              }}>FORCE 01</div>

              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 800,
                color: '#DFF1F1',
                lineHeight: 1.1,
              }}>फीस<br />बढ़ रही है</div>
            </div>

            {/* Force 2: Scholarship down */}
            <div style={{
              flex: 1,
              backgroundColor: '#080616',
              borderRadius: 20,
              padding: 36,
              opacity: force2Op,
              transform: `translateY(${(1 - force2Op) * 80}px)`,
              boxShadow: '0 16px 50px rgba(8,6,22,0.3)',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                fontWeight: 900,
                color: '#DFF1F1',
                opacity: 0.4,
                marginBottom: 16,
              }}>02</div>

              <svg width="80" height="100" viewBox="0 0 80 100" style={{ marginBottom: 16 }}>
                <line x1="40" y1="5" x2="40" y2="80" stroke="#DFF1F1" strokeWidth="8" strokeLinecap="round" />
                <polyline points="15,60 40,80 65,60" stroke="#DFF1F1" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>

              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.7,
                marginBottom: 12,
              }}>FORCE 02</div>

              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 800,
                color: '#DFF1F1',
                lineHeight: 1.1,
              }}>स्कॉलरशिप<br />घट रही</div>
            </div>

            {/* Force 3: Banks cautious */}
            <div style={{
              flex: 1,
              backgroundColor: '#3a2818',
              borderRadius: 20,
              padding: 36,
              opacity: force3Op,
              transform: `translateY(${(1 - force3Op) * 80}px)`,
              boxShadow: '0 16px 50px rgba(58,40,24,0.3)',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                fontWeight: 900,
                color: '#DFF1F1',
                opacity: 0.4,
                marginBottom: 16,
              }}>03</div>

              {/* Bank/shield icon */}
              <svg width="80" height="100" viewBox="0 0 80 100" style={{ marginBottom: 16 }}>
                <path d="M40 10 L70 25 L70 55 Q70 80 40 95 Q10 80 10 55 L10 25 Z"
                  fill="none" stroke="#DFF1F1" strokeWidth="6" strokeLinejoin="round" />
                <line x1="30" y1="45" x2="50" y2="45" stroke="#DFF1F1" strokeWidth="4" />
                <line x1="30" y1="60" x2="50" y2="60" stroke="#DFF1F1" strokeWidth="4" />
              </svg>

              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.7,
                marginBottom: 12,
              }}>FORCE 03</div>

              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 800,
                color: '#DFF1F1',
                lineHeight: 1.1,
              }}>बैंक<br />सतर्क</div>
            </div>
          </div>

          {/* Bottom collision message */}
          <div style={{
            marginTop: 40,
            opacity: sp(360),
            transform: `translateY(${(1 - sp(360)) * 30}px)`,
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 60,
              fontWeight: 900,
              color: '#080616',
              lineHeight: 1.1,
            }}>
              तीनों एक साथ — <span style={{
                textDecoration: 'underline',
                textDecorationThickness: 6,
                textDecorationColor: '#1F150C',
              }}>गरीब परिवार पर</span>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default ThreeArrows;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'FamilyImpact',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const FamilyImpact: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.45, stiffness: 115, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, 300], [1, 1.08], { extrapolateRight: 'clamp' });

  // House under pressure
  const pressureY = interpolate(frame, [60, 180], [0, 12], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Arrows pressing down
  const arrowDelay1 = 80;
  const arrowDelay2 = 110;
  const arrowDelay3 = 140;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 80%, rgba(8,6,22,0.12) 0%, transparent 60%)',
      }} />

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="cross" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="50" y2="50" stroke="#1F150C" strokeWidth="0.6" />
            <line x1="0" y1="50" x2="50" y2="0" stroke="#1F150C" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '8%',
        width: 300, height: 300,
        border: '2px solid #1F150C', opacity: 0.05,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', top: '15%', right: '10%',
        width: 240, height: 240,
        backgroundColor: '#080616', opacity: 0.04,
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Particles falling */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 1000);
        const r2 = seededRandom(i + 1050);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${((r2 * 100 + frame * 0.5) % 110) - 5}%`,
            width: 4, height: 4,
            backgroundColor: '#080616',
            opacity: 0.15,
            borderRadius: '50%',
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        display: 'flex',
        padding: '60px 100px',
      }}>

        {/* Left side: house with arrows pressing down */}
        <div style={{
          flex: 1,
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          position: 'relative',
        }}>
          {/* Three arrows from top */}
          <div style={{ display: 'flex', gap: 30, marginBottom: 20 }}>
            {[
              { color: '#1F150C', label: 'FEES↑', delay: arrowDelay1 },
              { color: '#080616', label: 'SCHOLARSHIP↓', delay: arrowDelay2 },
              { color: '#3a2818', label: 'BANK BLOCK', delay: arrowDelay3 },
            ].map((a, i) => (
              <div key={i} style={{
                opacity: sp(a.delay),
                transform: `translateY(${(1 - sp(a.delay)) * -100}px)`,
                display: 'flex', flexDirection: 'column', alignItems: 'center',
              }}>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 18,
                  fontWeight: 800,
                  letterSpacing: '2px',
                  color: a.color,
                  marginBottom: 8,
                }}>{a.label}</div>
                <svg width="50" height="120" viewBox="0 0 50 120">
                  <line x1="25" y1="5" x2="25" y2="95" stroke={a.color} strokeWidth="8" strokeLinecap="round" />
                  <polyline points="5,75 25,100 45,75" stroke={a.color} strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </div>
            ))}
          </div>

          {/* House SVG */}
          <div style={{
            transform: `translateY(${pressureY}px) scale(${0.9 + sp(40) * 0.1})`,
            opacity: sp(40),
            position: 'relative',
          }}>
            <svg width="420" height="320" viewBox="0 0 420 320">
              {/* House body */}
              <rect x="60" y="160" width="300" height="150" fill="#1F150C" />
              {/* Roof */}
              <polygon points="40,160 210,40 380,160" fill="#080616" />
              {/* Door */}
              <rect x="180" y="220" width="60" height="90" fill="#DFF1F1" />
              <circle cx="225" cy="270" r="4" fill="#1F150C" />
              {/* Windows */}
              <rect x="90" y="190" width="50" height="50" fill="#DFF1F1" />
              <line x1="115" y1="190" x2="115" y2="240" stroke="#1F150C" strokeWidth="3" />
              <line x1="90" y1="215" x2="140" y2="215" stroke="#1F150C" strokeWidth="3" />

              <rect x="280" y="190" width="50" height="50" fill="#DFF1F1" />
              <line x1="305" y1="190" x2="305" y2="240" stroke="#1F150C" strokeWidth="3" />
              <line x1="280" y1="215" x2="330" y2="215" stroke="#1F150C" strokeWidth="3" />

              {/* Chimney */}
              <rect x="290" y="70" width="35" height="60" fill="#080616" />
            </svg>

            {/* Family count badge */}
            <div style={{
              position: 'absolute',
              bottom: -20, left: '50%',
              transform: 'translateX(-50%)',
              padding: '8px 20px',
              backgroundColor: '#080616',
              color: '#DFF1F1',
              fontFamily: 'Inter',
              fontSize: 22,
              fontWeight: 700,
              letterSpacing: '3px',
              borderRadius: 6,
            }}>एक परिवार</div>
          </div>

          {/* Pressure lines underneath */}
          <div style={{ display: 'flex', gap: 6, marginTop: 40 }}>
            {Array.from({ length: 12 }).map((_, i) => (
              <div key={i} style={{
                width: 4,
                height: 30 + (i % 3) * 10,
                backgroundColor: '#080616',
                opacity: sp(180 + i * 5) * 0.4,
              }} />
            ))}
          </div>
        </div>

        {/* Right side: Text */}
        <div style={{
          flex: 1,
          display: 'flex', flexDirection: 'column',
          justifyContent: 'center',
          paddingLeft: 60,
        }}>
          <div style={{
            opacity: sp(0),
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 700,
            letterSpacing: '5px',
            color: '#080616',
            marginBottom: 16,
          }}>SEEDHE PARIVAAR PAR</div>

          <div style={{
            opacity: sp(20),
            transform: `translateX(${(1 - sp(20)) * 40}px)`,
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
            marginBottom: 30,
          }}>
            तंगी में जी<br />
            <span style={{ color: '#080616' }}>रहा परिवार</span>
          </div>

          <div style={{
            opacity: sp(160),
            transform: `translateX(${(1 - sp(160)) * 40}px)`,
            padding: '24px 32px',
            backgroundColor: '#080616',
            borderRadius: 14,
            borderLeft: '10px solid #1F150C',
            maxWidth: 600,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 43,
              fontWeight: 700,
              color: '#DFF1F1',
              lineHeight: 1.25,
            }}>
              ये किसी और की<br />
              समस्या <em style={{ color: '#DFF1F1', textDecoration: 'underline' }}>नहीं है</em>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default FamilyImpact;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'GERComparison',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const GERComparison: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera
  const camScale = interpolate(frame, [0, 450], [1, 1.06], { extrapolateRight: 'clamp' });

  // India 27 counter
  const indiaCounter = interpolate(frame, [60, 150], [0, 27], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Global counter
  const globalCounter = interpolate(frame, [180, 270], [0, 42], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Country bars
  const countries = [
    { name: 'USA', val: 88, color: '#080616' },
    { name: 'UK', val: 70, color: '#1F150C' },
    { name: 'KOREA', val: 96, color: '#080616' },
    { name: 'GLOBAL AVG', val: 42, color: '#3a2818' },
    { name: 'INDIA', val: 27, color: '#5a4028' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(31,21,12,0.05) 0%, transparent 50%)',
      }} />

      {/* World map dotted texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 800 }).map((_, i) => {
          const r1 = seededRandom(i + 2000);
          const r2 = seededRandom(i + 2500);
          return (
            <circle key={i} cx={r1 * 1920} cy={r2 * 1080} r="1.5" fill="#1F150C" />
          );
        })}
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', right: '5%',
        width: 380, height: 380,
        border: '2px solid #080616', opacity: 0.06,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.015) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', left: '8%',
        width: 250, height: 250,
        border: '2px solid #1F150C', opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r1 = seededRandom(i + 3000);
        const r2 = seededRandom(i + 3050);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.022 + i) * 20}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        padding: '60px 80px',
      }}>

        {/* Header */}
        <div style={{
          opacity: sp(0),
          transform: `translateY(${(1 - sp(0)) * -30}px)`,
          marginBottom: 30,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: '5px',
            color: '#080616',
            marginBottom: 12,
          }}>GROSS ENROLLMENT RATIO — HIGHER ED</div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 85,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
          }}>
            हम <span style={{ color: '#080616' }}>कहाँ खड़े हैं?</span>
          </div>
        </div>

        {/* Horizontal bar comparison */}
        <div style={{
          display: 'flex', flexDirection: 'column',
          gap: 14,
          marginTop: 20,
          maxWidth: 1700,
        }}>
          {countries.map((c, i) => {
            const isIndia = c.name === 'INDIA';
            const isGlobal = c.name === 'GLOBAL AVG';
            return (
              <div key={i} style={{
                display: 'flex', alignItems: 'center',
                gap: 24,
                opacity: sp(40 + i * 25),
                transform: `translateX(${(1 - sp(40 + i * 25)) * -60}px)`,
              }}>
                <div style={{
                  width: 230,
                  fontFamily: 'Inter',
                  fontSize: 32,
                  fontWeight: isIndia || isGlobal ? 900 : 700,
                  color: isIndia ? '#080616' : '#1F150C',
                  letterSpacing: '2px',
                }}>{c.name}</div>

                <div style={{
                  flex: 1,
                  height: isIndia ? 64 : 50,
                  backgroundColor: 'rgba(31,21,12,0.08)',
                  borderRadius: 8,
                  overflow: 'hidden',
                  position: 'relative',
                  border: isIndia ? '3px solid #080616' : 'none',
                }}>
                  <div style={{
                    width: `${sp(50 + i * 25) * c.val}%`,
                    height: '100%',
                    backgroundColor: c.color,
                    transition: 'width 0.3s',
                    display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
                    paddingRight: 20,
                  }}>
                    {sp(50 + i * 25) > 0.5 && (
                      <span style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: isIndia ? 34 : 28,
                        fontWeight: 900,
                        color: '#DFF1F1',
                      }}>{c.val}%</span>
                    )}
                  </div>
                </div>

                {isIndia && (
                  <div style={{
                    padding: '6px 16px',
                    backgroundColor: '#080616',
                    color: '#DFF1F1',
                    fontFamily: 'Inter',
                    fontSize: 22,
                    fontWeight: 700,
                    letterSpacing: '2px',
                    borderRadius: 6,
                    opacity: sp(150),
                  }}>YOU ARE HERE</div>
                )}
              </div>
            );
          })}
        </div>

        {/* Bottom insight */}
        <div style={{
          position: 'absolute',
          bottom: 80, left: 80, right: 80,
          opacity: sp(280),
          transform: `translateY(${(1 - sp(280)) * 40}px)`,
        }}>
          <div style={{
            display: 'flex', gap: 30, alignItems: 'stretch',
          }}>
            <div style={{
              flex: 1,
              padding: '24px 30px',
              backgroundColor: '#1F150C',
              borderRadius: 14,
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 8,
              }}>LIMITED ACCESS</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1.2,
              }}>सिर्फ 27% तक शिक्षा पहुंची</div>
            </div>

            <div style={{
              flex: 1,
              padding: '24px 30px',
              backgroundColor: '#080616',
              borderRadius: 14,
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 8,
              }}>LOAN PENETRATION</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1.2,
              }}>उसमें भी loan पहुंच मिनिमल</div>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default GERComparison;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'InformationWeapon',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const InformationWeapon: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera
  const camScale = interpolate(frame, [0, 450], [1, 1.06], { extrapolateRight: 'clamp' });
  const camX = interpolate(frame, [0, 450], [0, -10], { extrapolateRight: 'clamp' });

  // Beat 1: Question - 0-110
  const q1Op = interpolate(frame, [0, 25, 100, 125], [0, 1, 1, 0], { extrapolateRight: 'clamp' });

  // Beat 2: Information = weapon - 125-230
  const beat2Op = interpolate(frame, [125, 150, 220, 245], [0, 1, 1, 0], { extrapolateRight: 'clamp' });

  // Beat 3: PM Vidyalakshmi card - 245-450
  const beat3Op = interpolate(frame, [245, 275], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(8,6,22,0.08) 0%, transparent 60%)',
      }} />

      {/* Vertical lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="vlines" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="50" y1="0" x2="50" y2="100" stroke="#1F150C" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '8%', left: '5%',
        width: 360, height: 360,
        border: '3px solid #080616', opacity: 0.06,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '8%',
        width: 320, height: 320,
        border: '3px solid #1F150C', opacity: 0.05,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.018) * 0.06})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r1 = seededRandom(i + 4000);
        const r2 = seededRandom(i + 4050);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.022 + i) * 22}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale}) translateX(${camX}px)`,
      }}>

        {/* BEAT 1: रास्ता क्या है */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: q1Op,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          padding: 100,
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              letterSpacing: '6px',
              color: '#080616',
              marginBottom: 30,
              opacity: sp(0),
            }}>SO — WHAT NOW?</div>

            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 187,
              fontWeight: 900,
              color: '#1F150C',
              lineHeight: 1,
              transform: `scale(${0.7 + sp(20) * 0.3})`,
            }}>
              तो रास्ता<br />
              <span style={{ color: '#080616' }}>क्या है?</span>
            </div>
          </div>
        </div>

        {/* BEAT 2: Information = Weapon */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: beat2Op,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          padding: 100,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 50,
            transform: `scale(${0.85 + sp(140) * 0.15})`,
          }}>
            {/* Information icon */}
            <div style={{
              width: 240, height: 240,
              borderRadius: 30,
              backgroundColor: '#1F150C',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              boxShadow: '0 20px 50px rgba(31,21,12,0.3)',
            }}>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 187,
                fontWeight: 900,
                color: '#DFF1F1',
              }}>i</div>
            </div>

            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#080616',
              lineHeight: 1,
            }}>=</div>

            {/* Weapon visual */}
            <div style={{
              display: 'flex', flexDirection: 'column',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                letterSpacing: '4px',
                color: '#080616',
                marginBottom: 10,
              }}>YOUR ONLY</div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 153,
                fontWeight: 900,
                color: '#1F150C',
                lineHeight: 0.9,
              }}>हथियार</div>
            </div>
          </div>
        </div>

        {/* BEAT 3: PM Vidyalakshmi card */}
        <div style={{
          position: 'absolute', inset: 0,
          opacity: beat3Op,
          padding: '60px 80px',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: '5px',
            color: '#080616',
            marginBottom: 14,
            opacity: sp(255),
          }}>GOVERNMENT SCHEME 2025-26</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#1F150C',
            marginBottom: 40,
            transform: `translateX(${(1 - sp(265)) * -40}px)`,
            lineHeight: 1,
          }}>
            PM <span style={{ color: '#080616' }}>विद्यालक्ष्मी</span>
          </div>

          {/* Three feature cards */}
          <div style={{ display: 'flex', gap: 24 }}>
            {/* Card 1: Income */}
            <div style={{
              flex: 1,
              padding: 36,
              backgroundColor: '#1F150C',
              borderRadius: 20,
              opacity: sp(290),
              transform: `translateY(${(1 - sp(290)) * 60}px)`,
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 14,
              }}>FAMILY INCOME</div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 102,
                fontWeight: 900,
                color: '#DFF1F1',
                lineHeight: 1,
              }}>₹8L</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                fontWeight: 600,
                color: '#DFF1F1',
                opacity: 0.7,
                marginTop: 8,
              }}>तक eligible</div>
            </div>

            {/* Card 2: Subsidy */}
            <div style={{
              flex: 1,
              padding: 36,
              backgroundColor: '#080616',
              borderRadius: 20,
              opacity: sp(320),
              transform: `translateY(${(1 - sp(320)) * 60}px)`,
              boxShadow: '0 16px 50px rgba(8,6,22,0.4)',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 14,
              }}>INTEREST SUBSIDY</div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 102,
                fontWeight: 900,
                color: '#DFF1F1',
                lineHeight: 1,
              }}>3%</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                fontWeight: 600,
                color: '#DFF1F1',
                opacity: 0.7,
                marginTop: 8,
              }}>सरकार देगी</div>
            </div>

            {/* Card 3: Institutions */}
            <div style={{
              flex: 1,
              padding: 36,
              backgroundColor: '#3a2818',
              borderRadius: 20,
              opacity: sp(350),
              transform: `translateY(${(1 - sp(350)) * 60}px)`,
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 700,
                letterSpacing: '3px',
                color: '#DFF1F1',
                opacity: 0.6,
                marginBottom: 14,
              }}>ELIGIBLE INSTITUTIONS</div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 102,
                fontWeight: 900,
                color: '#DFF1F1',
                lineHeight: 1,
              }}>TOP NIRF</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                fontWeight: 600,
                color: '#DFF1F1',
                opacity: 0.7,
                marginTop: 8,
              }}>860+ संस्थान</div>
            </div>
          </div>

          {/* Bottom badge */}
          <div style={{
            position: 'absolute',
            bottom: 70, left: 80, right: 80,
            opacity: sp(390),
            transform: `translateY(${(1 - sp(390)) * 30}px)`,
            padding: '20px 36px',
            backgroundColor: 'rgba(31,21,12,0.08)',
            borderLeft: '10px solid #080616',
            borderRadius: 12,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 700,
              color: '#1F150C',
            }}>
              जानकारी ही फायदा है — कागज सब पास हैं, बस पढ़ने वाले कम हैं
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default InformationWeapon;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PortalCompare',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PortalCompare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera
  const camScale = interpolate(frame, [0, 480], [1, 1.06], { extrapolateRight: 'clamp' });

  // Bank counter 0-38
  const bankCounter = interpolate(frame, [40, 140], [0, 38], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Scheme counter 0-100
  const schemeCounter = interpolate(frame, [160, 280], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // 8 bank logo placeholders animation
  const bankBoxes = Array.from({ length: 8 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, rgba(31,21,12,0.07) 0%, transparent 50%)',
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="gridP" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#1F150C" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridP)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '15%', right: '5%',
        width: 280, height: 280,
        border: '3px solid #1F150C', opacity: 0.05,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '5%',
        width: 360, height: 360,
        backgroundColor: '#080616', opacity: 0.04,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.015) * 0.05})`,
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 5000);
        const r2 = seededRandom(i + 5050);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.023 + i) * 20}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        padding: '60px 90px',
      }}>

        {/* Header */}
        <div style={{
          opacity: sp(0),
          marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 24px',
            backgroundColor: '#080616',
            color: '#DFF1F1',
            fontFamily: 'Inter',
            fontSize: 24,
            fontWeight: 700,
            letterSpacing: '4px',
            marginBottom: 16,
          }}>VIDYALAKSHMI.CO.IN</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
          }}>
            एक पोर्टल — <span style={{ color: '#080616' }}>सब Banks</span>
          </div>
        </div>

        {/* Two big stat cards */}
        <div style={{
          display: 'flex', gap: 30,
          marginTop: 30,
        }}>
          {/* 38+ Banks */}
          <div style={{
            flex: 1,
            backgroundColor: '#1F150C',
            borderRadius: 24,
            padding: 50,
            opacity: sp(20),
            transform: `translateX(${(1 - sp(20)) * -60}px)`,
            boxShadow: '0 20px 60px rgba(31,21,12,0.3)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 700,
              letterSpacing: '4px',
              color: '#DFF1F1',
              opacity: 0.6,
              marginBottom: 20,
            }}>BANKS LISTED</div>

            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 238,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1,
              display: 'flex', alignItems: 'baseline',
            }}>
              {Math.floor(bankCounter)}<span style={{ fontSize: 102, color: '#DFF1F1', opacity: 0.7 }}>+</span>
            </div>

            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 600,
              color: '#DFF1F1',
              opacity: 0.8,
              marginTop: 16,
            }}>सरकारी + निजी banks</div>

            {/* Bank logo placeholders */}
            <div style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(4, 1fr)',
              gap: 12,
              marginTop: 30,
            }}>
              {bankBoxes.map((b, i) => (
                <div key={i} style={{
                  height: 50,
                  backgroundColor: 'rgba(245,245,245,0.15)',
                  borderRadius: 8,
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  opacity: sp(140 + i * 8),
                  transform: `scale(${0.7 + sp(140 + i * 8) * 0.3})`,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 18,
                  fontWeight: 700,
                  color: '#DFF1F1',
                  letterSpacing: '1px',
                }}>
                  {['SBI', 'HDFC', 'ICICI', 'AXIS', 'PNB', 'BOB', 'CANR', 'UCO'][i]}
                </div>
              ))}
            </div>
          </div>

          {/* 100+ Schemes */}
          <div style={{
            flex: 1,
            backgroundColor: '#080616',
            borderRadius: 24,
            padding: 50,
            opacity: sp(180),
            transform: `translateX(${(1 - sp(180)) * 60}px)`,
            boxShadow: '0 20px 60px rgba(8,6,22,0.4)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 700,
              letterSpacing: '4px',
              color: '#DFF1F1',
              opacity: 0.6,
              marginBottom: 20,
            }}>LOAN SCHEMES</div>

            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 238,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1,
              display: 'flex', alignItems: 'baseline',
            }}>
              {Math.floor(schemeCounter)}<span style={{ fontSize: 102, color: '#DFF1F1', opacity: 0.7 }}>+</span>
            </div>

            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 600,
              color: '#DFF1F1',
              opacity: 0.8,
              marginTop: 16,
            }}>एक ही जगह compare</div>

            {/* Scheme row visualization */}
            <div style={{
              marginTop: 30,
              display: 'flex', flexDirection: 'column', gap: 10,
            }}>
              {[
                { name: 'EDU LOAN A', rate: '8.5%' },
                { name: 'STUDENT PRO', rate: '9.2%' },
                { name: 'VIDYA SAATHI', rate: '7.9%' },
                { name: 'SCHOLAR ELITE', rate: '10.1%' },
              ].map((s, i) => (
                <div key={i} style={{
                  display: 'flex', justifyContent: 'space-between',
                  padding: '10px 16px',
                  backgroundColor: 'rgba(245,245,245,0.1)',
                  borderRadius: 8,
                  opacity: sp(260 + i * 15),
                  transform: `translateX(${(1 - sp(260 + i * 15)) * 30}px)`,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  fontWeight: 700,
                  color: '#DFF1F1',
                }}>
                  <span>{s.name}</span>
                  <span>{s.rate}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Bottom message */}
        <div style={{
          position: 'absolute',
          bottom: 70, left: 90, right: 90,
          opacity: sp(360),
          transform: `translateY(${(1 - sp(360)) * 30}px)`,
          textAlign: 'center',
        }}>
          <div style={{
            display: 'inline-block',
            padding: '18px 40px',
            backgroundColor: '#DFF1F1',
            border: '4px solid #1F150C',
            borderRadius: 14,
            fontFamily: 'Yantra One',
            fontSize: 51,
            fontWeight: 900,
            color: '#1F150C',
          }}>
            तुलना करो — फिर चुनो
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default PortalCompare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CSISHiddenScheme',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CSISHiddenScheme: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera
  const camScale = interpolate(frame, [0, 390], [1, 1.06], { extrapolateRight: 'clamp' });

  // CSIS reveal animation
  const csisOp = interpolate(frame, [60, 100], [0, 1], { extrapolateRight: 'clamp' });

  // Lock unlock at frame 200
  const lockProgress = interpolate(frame, [200, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bank silence vs borrower action
  const silenceOp = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(8,6,22,0.08) 0%, transparent 40%, rgba(31,21,12,0.05) 100%)',
      }} />

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="ch2" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="20" x2="40" y2="20" stroke="#1F150C" strokeWidth="0.6" />
            <line x1="20" y1="0" x2="20" y2="40" stroke="#1F150C" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#ch2)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', left: '40%',
        width: 380, height: 380,
        border: '2px solid #080616', opacity: 0.05,
        borderRadius: '50%',
        transform: `translate(-50%, 0) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '8%',
        width: 240, height: 240,
        border: '2px solid #1F150C', opacity: 0.05,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 6000);
        const r2 = seededRandom(i + 6050);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r1 * 100}%`,
            top: `${r2 * 100}%`,
            width: 5, height: 5,
            backgroundColor: '#080616',
            opacity: 0.12,
            borderRadius: '50%',
            transform: `translate(${Math.sin(frame * 0.02 + i) * 25}px, ${Math.cos(frame * 0.025 + i) * 20}px)`,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        padding: '60px 90px',
      }}>

        {/* Header */}
        <div style={{
          opacity: sp(0),
          marginBottom: 30,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 24px',
            backgroundColor: '#080616',
            color: '#DFF1F1',
            fontFamily: 'Inter',
            fontSize: 24,
            fontWeight: 700,
            letterSpacing: '4px',
            marginBottom: 16,
          }}>HIDDEN SCHEME</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1,
          }}>
            <span style={{ color: '#080616' }}>CSIS</span> Scheme
          </div>
        </div>

        {/* Main layout: left card, right warning */}
        <div style={{
          display: 'flex', gap: 40,
          marginTop: 30,
          alignItems: 'stretch',
        }}>
          {/* Left: Eligibility + benefit card */}
          <div style={{
            flex: 1.2,
            opacity: csisOp,
            transform: `translateX(${(1 - csisOp) * -50}px)`,
            backgroundColor: '#1F150C',
            borderRadius: 24,
            padding: 44,
            boxShadow: '0 20px 60px rgba(31,21,12,0.3)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 22,
              fontWeight: 700,
              letterSpacing: '3px',
              color: '#DFF1F1',
              opacity: 0.6,
              marginBottom: 14,
            }}>ELIGIBILITY</div>

            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 600,
              color: '#DFF1F1',
              marginBottom: 8,
            }}>Family Income</div>

            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 153,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1,
              display: 'flex', alignItems: 'baseline',
            }}>
              &lt; ₹4.5L<span style={{ fontSize: 51, opacity: 0.7, marginLeft: 12 }}>/year</span>
            </div>

            <div style={{
              height: 2,
              backgroundColor: 'rgba(245,245,245,0.2)',
              margin: '30px 0',
            }} />

            <div style={{
              fontFamily: 'Inter',
              fontSize: 22,
              fontWeight: 700,
              letterSpacing: '3px',
              color: '#DFF1F1',
              opacity: 0.6,
              marginBottom: 14,
            }}>WHAT YOU GET</div>

            <div style={{
              display: 'flex', alignItems: 'baseline', gap: 16,
            }}>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 119,
                fontWeight: 900,
                color: '#DFF1F1',
                lineHeight: 1,
              }}>100%</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1.2,
              }}>Interest Subsidy<br />
                <span style={{ fontSize: 24, opacity: 0.7 }}>Moratorium period में</span>
              </div>
            </div>
          </div>

          {/* Right: The catch */}
          <div style={{
            flex: 1,
            display: 'flex', flexDirection: 'column',
            gap: 20,
          }}>
            {/* Bank silence card */}
            <div style={{
              flex: 1,
              padding: 36,
              backgroundColor: 'rgba(31,21,12,0.08)',
              border: '3px dashed #1F150C',
              borderRadius: 20,
              opacity: silenceOp,
              transform: `translateY(${(1 - silenceOp) * -30}px)`,
              display: 'flex', flexDirection: 'column', justifyContent: 'center',
            }}>
              <div style={{
                display: 'flex', alignItems: 'center', gap: 16, marginBottom: 16,
              }}>
                {/* Silence/mute icon */}
                <svg width="60" height="60" viewBox="0 0 60 60">
                  <circle cx="30" cy="30" r="26" fill="none" stroke="#1F150C" strokeWidth="4" />
                  <line x1="14" y1="14" x2="46" y2="46" stroke="#1F150C" strokeWidth="4" strokeLinecap="round" />
                  <path d="M22 22 L22 38 L30 32 L38 38 L38 22" fill="none" stroke="#1F150C" strokeWidth="3" />
                </svg>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 800,
                  letterSpacing: '3px',
                  color: '#1F150C',
                }}>BANK WILL NOT TELL</div>
              </div>

              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#1F150C',
                lineHeight: 1.2,
              }}>
                Bank खुद से<br />
                <span style={{ color: '#080616' }}>नहीं बताता</span>
              </div>
            </div>

            {/* Your action card */}
            <div style={{
              flex: 1,
              padding: 36,
              backgroundColor: '#080616',
              borderRadius: 20,
              opacity: sp(330),
              transform: `translateY(${(1 - sp(330)) * 30}px)`,
              display: 'flex', flexDirection: 'column', justifyContent: 'center',
              boxShadow: '0 16px 50px rgba(8,6,22,0.4)',
            }}>
              <div style={{
                display: 'flex', alignItems: 'center', gap: 16, marginBottom: 16,
              }}>
                {/* Hand raise icon */}
                <svg width="60" height="60" viewBox="0 0 60 60">
                  <path d="M30 10 L30 35 M20 20 L20 35 M40 20 L40 35 M25 25 L25 35 M35 25 L35 35"
                    stroke="#DFF1F1" strokeWidth="4" strokeLinecap="round" />
                  <path d="M15 35 Q15 50 30 50 Q45 50 45 35"
                    stroke="#DFF1F1" strokeWidth="4" fill="none" />
                </svg>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 800,
                  letterSpacing: '3px',
                  color: '#DFF1F1',
                }}>YOU MUST ASK</div>
              </div>

              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1.2,
              }}>
                आपको खुद<br />
                <span style={{
                  textDecoration: 'underline',
                  textDecorationThickness: 4,
                  textDecorationColor: '#DFF1F1',
                }}>request करना</span> है
              </div>
            </div>
          </div>
        </div>

        {/* Bottom punchline */}
        <div style={{
          position: 'absolute',
          bottom: 50, left: 90, right: 90,
          opacity: sp(380),
          transform: `translateY(${(1 - sp(380)) * 30}px)`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 60,
            fontWeight: 900,
            color: '#1F150C',
            lineHeight: 1.05,
          }}>
            जो नहीं जानता — <span style={{ color: '#080616' }}>वो चुकाता है</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(31,21,12,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30, right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#1F150C',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default CSISHiddenScheme;
```

