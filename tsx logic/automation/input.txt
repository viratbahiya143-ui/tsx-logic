```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow cinematic push-in
  const cam = interpolate(frame, [0, durationInFrames], [1.08, 1], { easing: Easing.inOut(Easing.ease) });

  const kicker = sp(8);
  const line1 = sp(24);
  const line2 = sp(38);
  const cardA = sp(130);
  const cardB = sp(150);
  const tail = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, #8FBFC1 0%, #76ABAE 48%, #5E9297 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 780, height: 780, borderRadius: '50%', border: '3px solid #3F4F44', opacity: 0.06, left: -240, top: -280, transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame / 55) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 540, height: 540, backgroundColor: '#EEEEEE', opacity: 0.04, right: -170, bottom: -190, transform: `rotate(${45 + frame * 0.04}deg)` }} />

      {/* Motion texture: vertical lines */}
      <AbsoluteFill style={{ backgroundImage: 'linear-gradient(90deg, #3F4F44 1px, transparent 1px)', backgroundSize: '120px 100%', opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const sx = seededRandom(i + 1) * 1920;
        const speed = 0.4 + seededRandom(i + 7) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 3) * 1080) % 1240);
        const size = 3 + seededRandom(i + 11) * 5;
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: size, height: size, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ opacity: kicker, transform: `translateY(${(1 - kicker) * 30}px)`, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 700, letterSpacing: 10, color: '#EEEEEE', marginBottom: 24 }}>
          THE VOICE FOR DEBT AND PANIC
        </div>

        <div style={{ overflow: 'hidden' }}>
          <div style={{ transform: `translateY(${(1 - line1) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 185, lineHeight: 1, color: '#3F4F44' }}>WARREN</div>
        </div>
        <div style={{ overflow: 'hidden' }}>
          <div style={{ transform: `translateY(${(1 - line2) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 185, lineHeight: 1, color: '#EEEEEE' }}>BUFFETT</div>
        </div>

        <div style={{ display: 'flex', gap: 30, marginTop: 55 }}>
          <div style={{ transform: `scale(${cardA})`, backgroundColor: '#3F4F44', color: '#EEEEEE', padding: '22px 44px', borderRadius: 14, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 44, fontWeight: 800 }}>
            CEO CHAIR → GREG ABEL
          </div>
          <div style={{ transform: `scale(${cardB})`, backgroundColor: '#EEEEEE', color: '#3F4F44', padding: '22px 44px', borderRadius: 14, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 44, fontWeight: 800 }}>
            STAYS CHAIRMAN
          </div>
        </div>

        <div style={{ opacity: tail, transform: `translateY(${(1 - tail) * 24}px)`, marginTop: 50, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 600, color: '#E9C46A', letterSpacing: 3 }}>
          UNUSUALLY BLUNT ABOUT THE NATIONAL MOOD
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene21;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle lateral drift
  const camX = interpolate(frame, [0, durationInFrames], [-25, 25], { easing: Easing.inOut(Easing.ease) });

  const words = ["WE'VE", 'NEVER', 'HAD', 'PEOPLE', 'IN', 'A', 'MORE', 'GAMBLING', 'MOOD', 'THAN', 'NOW.'];
  const hot = ['GAMBLING', 'MOOD'];
  const pills = ['STOCKS', 'SHORT-TERM OPTIONS', 'PREDICTION MARKETS'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #82B6B8 0%, #76ABAE 50%, #578A8E 100%)' }} />

      {/* Motion texture: volatile market bars rising behind the quote */}
      {Array.from({ length: 22 }).map((_, i) => {
        const h = 120 + seededRandom(i + 5) * 380 + Math.sin(frame / 22 + i) * 60;
        return <div key={i} style={{ position: 'absolute', bottom: 0, left: 40 + i * 86, width: 40, height: h, backgroundColor: i % 3 === 0 ? '#B85C4F' : '#3F4F44', opacity: 0.07, borderRadius: 8 }} />;
      })}

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 650, height: 650, borderRadius: '50%', border: '3px solid #EEEEEE', opacity: 0.05, right: -200, top: -240, transform: `rotate(${-frame * 0.05}deg) scale(${1 + Math.sin(frame / 48) * 0.05})` }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i + 2) * 1920;
        const speed = 0.5 + seededRandom(i + 9) * 0.7;
        const y = 1140 - ((frame * speed + seededRandom(i + 4) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.15 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ opacity: sp(6), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 32, fontWeight: 700, letterSpacing: 9, color: '#3F4F44', marginBottom: 40 }}>
          BERKSHIRE ANNUAL MEETING
        </div>

        <div style={{ width: 1500, display: 'flex', flexWrap: 'wrap', justifyContent: 'center', gap: '10px 26px' }}>
          {words.map((w, i) => {
            const s = sp(20 + i * 8);
            const isHot = hot.includes(w);
            return (
              <span key={i} style={{ display: 'inline-block', transform: `scale(${s}) translateY(${(1 - s) * 40}px)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: isHot ? 118 : 96, lineHeight: 1.05, color: isHot ? '#E9C46A' : '#EEEEEE', textShadow: isHot ? '0 6px 0 rgba(63,79,68,0.45)' : 'none' }}>
                {w}
              </span>
            );
          })}
        </div>

        <div style={{ display: 'flex', gap: 26, marginTop: 62 }}>
          {pills.map((p, i) => {
            const s = sp(220 + i * 18);
            return (
              <div key={i} style={{ transform: `scale(${s})`, border: '4px solid #3F4F44', color: '#3F4F44', padding: '16px 38px', borderRadius: 60, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 38, fontWeight: 800, backgroundColor: 'rgba(238,238,238,0.35)' }}>
                {p}
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene22;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom-out reveal
  const cam = interpolate(frame, [0, durationInFrames], [1.1, 1], { easing: Easing.out(Easing.ease) });

  const cardS = sp(15, { damping: 10 });
  const h1 = sp(60);
  const h2 = sp(78);
  const aprBox = sp(150);
  const apr = Math.round(interpolate(frame, [160, 230], [0, 20], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const tail = sp(290);
  const floatY = Math.sin(frame / 26) * 12;

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 75% 80%, #8FBFC1 0%, #76ABAE 45%, #55868A 100%)' }} />

      {/* Motion texture: dot grid */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#3F4F44 2px, transparent 2px)', backgroundSize: '70px 70px', opacity: 0.06 }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 600, height: 600, border: '3px solid #3F4F44', opacity: 0.06, left: -180, bottom: -220, transform: `rotate(${frame * 0.07}deg)` }} />
      <div style={{ position: 'absolute', width: 400, height: 400, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.05, right: -120, top: -120, transform: `scale(${1 + Math.sin(frame / 50) * 0.06})` }} />

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const sx = seededRandom(i + 3) * 1920;
        const speed = 0.4 + seededRandom(i + 8) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 5) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam})`, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 110 }}>
        {/* Credit card icon (SVG) */}
        <div style={{ transform: `scale(${cardS}) rotate(${(1 - cardS) * -18 - 6}deg) translateY(${floatY}px)` }}>
          <svg width="480" height="300" viewBox="0 0 480 300">
            <rect x="8" y="8" width="464" height="284" rx="28" fill="#3F4F44" />
            <rect x="8" y="64" width="464" height="44" fill="#EEEEEE" opacity="0.92" />
            <rect x="42" y="190" width="120" height="20" rx="10" fill="#76ABAE" />
            <rect x="42" y="226" width="190" height="14" rx="7" fill="#EEEEEE" opacity="0.5" />
            <circle cx="392" cy="216" r="30" fill="#E9C46A" opacity="0.95" />
            <circle cx="352" cy="216" r="30" fill="#B85C4F" opacity="0.88" />
          </svg>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-start' }}>
          <div style={{ overflow: 'hidden' }}>
            <div style={{ transform: `translateY(${(1 - h1) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 108, lineHeight: 1.02, color: '#EEEEEE' }}>PAY OFF</div>
          </div>
          <div style={{ overflow: 'hidden' }}>
            <div style={{ transform: `translateY(${(1 - h2) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 108, lineHeight: 1.02, color: '#3F4F44' }}>CARDS FIRST</div>
          </div>

          <div style={{ transform: `scale(${aprBox})`, marginTop: 40, backgroundColor: '#B85C4F', color: '#EEEEEE', padding: '20px 46px', borderRadius: 16, fontFamily: "'JetBrains Mono', monospace", fontSize: 74, fontWeight: 800, boxShadow: '0 12px 0 rgba(63,79,68,0.35)' }}>
            APR {apr}%+
          </div>

          <div style={{ opacity: tail, transform: `translateY(${(1 - tail) * 20}px)`, marginTop: 34, fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 600, color: '#EEEEEE', letterSpacing: 2 }}>
            CRUSHINGLY HIGH INTEREST — BUFFETT'S FIRST RULE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene23;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera push + impact shake on the stamp
  const cam = interpolate(frame, [0, durationInFrames], [1.05, 1.12], { easing: Easing.inOut(Easing.ease) });
  const impact = 250;
  const shakeAmp = interpolate(frame, [impact, impact + 14], [10, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = frame >= impact ? Math.sin(frame * 53) * shakeAmp : 0;
  const shakeY = frame >= impact ? Math.cos(frame * 47) * shakeAmp : 0;

  const l1 = sp(20);
  const l2 = sp(80);
  const strike = interpolate(frame, [160, 195], [0, 100], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const stamp = sp(impact, { damping: 9, stiffness: 180 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #7FB3B6 0%, #76ABAE 55%, #517F83 100%)' }} />

      {/* Motion texture: diagonal lines */}
      <AbsoluteFill style={{ backgroundImage: 'repeating-linear-gradient(-45deg, #3F4F44 0px, #3F4F44 2px, transparent 2px, transparent 110px)', opacity: 0.05 }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '2px solid #EEEEEE', opacity: 0.05, left: 510, top: -350, transform: `scale(${1 + Math.sin(frame / 60) * 0.05})` }} />
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '260px solid transparent', borderRight: '260px solid transparent', borderBottom: '440px solid #3F4F44', opacity: 0.05, left: -140, bottom: -160, transform: `rotate(${frame * 0.03}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const sx = seededRandom(i + 6) * 1920;
        const speed = 0.4 + seededRandom(i + 2) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 9) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam}) translate(${shakeX}px, ${shakeY}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ opacity: sp(6), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 32, fontWeight: 700, letterSpacing: 9, color: '#EEEEEE', marginBottom: 36 }}>
          BUFFETT'S DISCIPLINE = SIMPLE MATH
        </div>

        <div style={{ transform: `scale(${l1}) translateY(${(1 - l1) * 40}px)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 112, color: '#3F4F44', lineHeight: 1.05 }}>
          KILL THE <span style={{ color: '#B85C4F' }}>GUARANTEED LOSS</span>
        </div>

        <div style={{ transform: `scale(${l2}) translateY(${(1 - l2) * 40}px)`, marginTop: 26, position: 'relative', fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 84, color: '#EEEEEE', lineHeight: 1.05 }}>
          BEFORE THE UNCERTAIN GAIN
          {/* Strike-through draws across the phrase */}
          <div style={{ position: 'absolute', left: 0, top: '52%', width: `${strike}%`, height: 12, backgroundColor: '#B85C4F', borderRadius: 6 }} />
        </div>

        <div style={{ transform: `scale(${stamp}) rotate(-6deg)`, marginTop: 70, border: '8px solid #B85C4F', color: '#B85C4F', backgroundColor: 'rgba(238,238,238,0.9)', padding: '18px 60px', borderRadius: 18, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 96, letterSpacing: 4 }}>
          ATTACK IT FIRST
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene24;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow rise (tilt feel)
  const camY = interpolate(frame, [0, durationInFrames], [30, -20], { easing: Easing.inOut(Easing.ease) });

  const bars = [
    { label: 'AUTO LOANS', pct: 40, color: '#3F4F44', delay: 110 },
    { label: 'CREDIT CARDS', pct: 56, color: '#B85C4F', delay: 150 },
    { label: 'MORTGAGES', pct: 20, color: '#EEEEEE', delay: 190 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 110%, #8FBFC1 0%, #76ABAE 50%, #5E9297 100%)' }} />

      {/* Motion texture: horizontal grid (chart paper) */}
      <AbsoluteFill style={{ backgroundImage: 'linear-gradient(#3F4F44 1px, transparent 1px)', backgroundSize: '100% 90px', opacity: 0.06 }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 700, height: 700, borderRadius: '50%', border: '3px solid #3F4F44', opacity: 0.05, right: -260, bottom: -300, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i + 4) * 1920;
        const speed = 0.4 + seededRandom(i + 6) * 0.7;
        const y = 1140 - ((frame * speed + seededRandom(i + 2) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ transform: `scale(${sp(10)})`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 96, color: '#3F4F44', lineHeight: 1 }}>
          IT DOESN'T STAY IN THE BOX
        </div>
        <div style={{ opacity: sp(50), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 36, fontWeight: 600, color: '#EEEEEE', letterSpacing: 4, marginTop: 18 }}>
          DELINQUENCY RATES OF NEWLY DEFAULTED BORROWERS
        </div>

        {/* Animated bar chart */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 120, height: 480, marginTop: 60 }}>
          {bars.map((b, i) => {
            const grow = interpolate(frame, [b.delay, b.delay + 55], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const h = (b.pct / 60) * 380 * grow;
            const val = Math.round(b.pct * grow);
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'flex-end', height: '100%' }}>
                <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 84, fontWeight: 800, color: b.color === '#EEEEEE' ? '#EEEEEE' : b.color, marginBottom: 14, opacity: grow }}>
                  {val}%
                </div>
                <div style={{ width: 190, height: h, backgroundColor: b.color, borderRadius: '14px 14px 0 0', boxShadow: '0 10px 0 rgba(63,79,68,0.25)' }} />
                <div style={{ marginTop: 20, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 34, fontWeight: 800, color: '#3F4F44', letterSpacing: 2, opacity: grow }}>
                  {b.label}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene25;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const val = Math.round(interpolate(frame, [15, 85], [0, 56], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Impact shake when the counter lands
  const impact = 85;
  const shakeAmp = interpolate(frame, [impact, impact + 12], [9, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = frame >= impact ? Math.sin(frame * 50) * shakeAmp : 0;

  // Crack line draws across the screen when the budget "snaps"
  const crack = interpolate(frame, [200, 245], [1, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const cam = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, #83B7BA 0%, #76ABAE 55%, #4E7C80 100%)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 950, height: 950, borderRadius: '50%', border: '2px solid #EEEEEE', opacity: 0.06, left: 485, top: -120, transform: `scale(${1 + Math.sin(frame / 45) * 0.05}) rotate(${frame * 0.03}deg)` }} />
      <div style={{ position: 'absolute', width: 620, height: 620, borderRadius: '50%', border: '2px solid #3F4F44', opacity: 0.07, left: 650, top: 45, transform: `scale(${1 + Math.cos(frame / 40) * 0.04})` }} />

      {/* Motion texture: concentric focus rings via repeating gradient */}
      <AbsoluteFill style={{ background: 'repeating-radial-gradient(circle at 50% 45%, transparent 0px, transparent 120px, rgba(63,79,68,0.04) 122px)' }} />

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const sx = seededRandom(i + 8) * 1920;
        const speed = 0.4 + seededRandom(i + 1) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 7) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam}) translateX(${shakeX}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 215, lineHeight: 1, color: '#EEEEEE', textShadow: '0 14px 0 rgba(63,79,68,0.4)' }}>
          {val}<span style={{ color: '#B85C4F' }}>%</span>
        </div>

        <div style={{ opacity: sp(110), transform: `translateY(${(1 - sp(110)) * 26}px)`, marginTop: 26, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 58, fontWeight: 800, color: '#3F4F44', textAlign: 'center', width: 1350, lineHeight: 1.2 }}>
          MORE THAN HALF ARE ALSO DROWNING ON CREDIT CARDS
        </div>

        {/* Crack SVG — the household budget snapping */}
        <svg width="1500" height="130" viewBox="0 0 1500 130" style={{ marginTop: 36 }}>
          <path
            d="M 0 65 L 320 65 L 400 30 L 470 95 L 560 40 L 640 85 L 750 20 L 850 100 L 950 45 L 1060 80 L 1180 65 L 1500 65"
            fill="none" stroke="#B85C4F" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round"
            pathLength={1} strokeDasharray={1} strokeDashoffset={crack}
          />
        </svg>

        <div style={{ opacity: sp(260), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 42, fontWeight: 700, color: '#EEEEEE', letterSpacing: 3 }}>
          A HOUSEHOLD BUDGET SNAPPING IN REAL TIME
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene26;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const cam = interpolate(frame, [0, durationInFrames], [1.08, 1], { easing: Easing.out(Easing.ease) });
  const low = interpolate(frame, [30, 95], [0, 3.1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const high = interpolate(frame, [70, 145], [0, 8.5], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const fill = interpolate(frame, [90, 160], [0, 100], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const drains = [
    { label: 'GROCERIES', delay: 210 },
    { label: 'GAS STATIONS', delay: 235 },
    { label: 'RENT CHECKS', delay: 260 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #86B9BB 0%, #76ABAE 45%, #537F83 100%)' }} />

      {/* Motion texture: falling dollar streaks (thin vertical dashes drifting DOWN) */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = seededRandom(i + 12) * 1920;
        const speed = 1.5 + seededRandom(i + 5) * 2;
        const y = ((frame * speed + seededRandom(i + 3) * 1080) % 1240) - 80;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 46, backgroundColor: '#3F4F44', opacity: 0.1, borderRadius: 2 }} />;
      })}

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 720, height: 720, borderRadius: '50%', border: '3px solid #EEEEEE', opacity: 0.05, left: -230, top: -260, transform: `rotate(${frame * 0.04}deg) scale(${1 + Math.sin(frame / 52) * 0.04})` }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const sx = seededRandom(i + 20) * 1920;
        const speed = 0.4 + seededRandom(i + 15) * 0.6;
        const y = 1140 - ((frame * speed + seededRandom(i + 18) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.13 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ opacity: sp(8), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 700, letterSpacing: 8, color: '#EEEEEE', marginBottom: 30 }}>
          JPMORGAN RESEARCH — MONTHLY DRAIN ON DISPOSABLE INCOME
        </div>

        <div style={{ transform: `scale(${sp(25)})`, fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 800, color: '#3F4F44', lineHeight: 1 }}>
          ${low.toFixed(1)}B <span style={{ color: '#EEEEEE' }}>—</span> ${high.toFixed(1)}B
        </div>

        {/* Range bar filling between the two figures */}
        <div style={{ width: 1100, height: 34, backgroundColor: 'rgba(238,238,238,0.3)', borderRadius: 17, marginTop: 50, overflow: 'hidden' }}>
          <div style={{ width: `${fill}%`, height: '100%', background: 'linear-gradient(90deg, #E9C46A 0%, #B85C4F 100%)', borderRadius: 17 }} />
        </div>
        <div style={{ opacity: sp(150), fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 44, fontWeight: 800, color: '#EEEEEE', marginTop: 24, letterSpacing: 3 }}>
          PULLED OUT EVERY SINGLE MONTH
        </div>

        {/* Where it comes from — icon cards */}
        <div style={{ display: 'flex', gap: 40, marginTop: 55 }}>
          {drains.map((d, i) => {
            const s = sp(d.delay);
            return (
              <div key={i} style={{ transform: `scale(${s}) translateY(${(1 - s) * 40}px)`, backgroundColor: '#3F4F44', borderRadius: 18, padding: '26px 40px', display: 'flex', alignItems: 'center', gap: 22 }}>
                <svg width="52" height="52" viewBox="0 0 52 52">
                  <path d="M26 6 L26 30 M14 20 L26 32 L38 20" fill="none" stroke="#B85C4F" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
                  <rect x="10" y="38" width="32" height="6" rx="3" fill="#EEEEEE" />
                </svg>
                <span style={{ fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 36, fontWeight: 800, color: '#EEEEEE', letterSpacing: 2 }}>{d.label}</span>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene27;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [40, -40], { easing: Easing.inOut(Easing.ease) });

  // Donut ring progress → 25% of portfolio
  const R = 165;
  const C = 2 * Math.PI * R;
  const prog = interpolate(frame, [60, 160], [0, 0.25], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const pct = Math.round(prog * 100);

  // Stamp impact shake
  const impact = 270;
  const shakeAmp = interpolate(frame, [impact, impact + 12], [9, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeY = frame >= impact ? Math.sin(frame * 52) * shakeAmp : 0;
  const stamp = sp(impact, { damping: 9, stiffness: 190 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 80% 25%, #8ABBBE 0%, #76ABAE 50%, #517F83 100%)' }} />

      {/* Motion texture: fine grid */}
      <AbsoluteFill style={{ backgroundImage: 'linear-gradient(#3F4F44 1px, transparent 1px), linear-gradient(90deg, #3F4F44 1px, transparent 1px)', backgroundSize: '95px 95px', opacity: 0.05 }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 560, height: 560, backgroundColor: '#3F4F44', opacity: 0.04, left: -180, bottom: -200, transform: `rotate(${30 + frame * 0.05}deg)`, borderRadius: 60 }} />
      <div style={{ position: 'absolute', width: 480, height: 480, borderRadius: '50%', border: '3px solid #EEEEEE', opacity: 0.06, right: -140, top: -150, transform: `scale(${1 + Math.sin(frame / 46) * 0.05})` }} />

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const sx = seededRandom(i + 30) * 1920;
        const speed = 0.4 + seededRandom(i + 25) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 22) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translate(${camX}px, ${shakeY}px)`, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 130 }}>
        {/* Left: typographic stack */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-start' }}>
          <div style={{ opacity: sp(8), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 700, letterSpacing: 8, color: '#EEEEEE', marginBottom: 20 }}>
            THE WAVE IS STILL BUILDING
          </div>
          <div style={{ overflow: 'hidden' }}>
            <div style={{ transform: `translateY(${(1 - sp(24)) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 168, lineHeight: 1, color: '#3F4F44' }}>10 MILLION</div>
          </div>
          <div style={{ overflow: 'hidden' }}>
            <div style={{ transform: `translateY(${(1 - sp(40)) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 92, lineHeight: 1.1, color: '#EEEEEE' }}>BORROWERS IN DEFAULT</div>
          </div>
          <div style={{ transform: `scale(${stamp}) rotate(-5deg)`, transformOrigin: 'left center', marginTop: 44, border: '7px solid #B85C4F', color: '#B85C4F', backgroundColor: 'rgba(238,238,238,0.92)', padding: '12px 42px', borderRadius: 14, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 72, letterSpacing: 3 }}>
            A RECORD
          </div>
        </div>

        {/* Right: donut chart to 25% */}
        <div style={{ transform: `scale(${sp(70)})`, position: 'relative', width: 420, height: 420 }}>
          <svg width="420" height="420" viewBox="0 0 420 420" style={{ transform: 'rotate(-90deg)' }}>
            <circle cx="210" cy="210" r={R} fill="none" stroke="rgba(238,238,238,0.3)" strokeWidth="46" />
            <circle cx="210" cy="210" r={R} fill="none" stroke="#3F4F44" strokeWidth="46" strokeLinecap="round" strokeDasharray={`${prog * C} ${C}`} />
          </svg>
          <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 104, fontWeight: 800, color: '#EEEEEE', lineHeight: 1 }}>{pct}%</div>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 28, fontWeight: 700, color: '#3F4F44', letterSpacing: 2, marginTop: 8 }}>OF THE PORTFOLIO</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene28;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const cam = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.inOut(Easing.ease) });

  const panel = sp(250, { damping: 13 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #85B8BB 0%, #76ABAE 50%, #578A8E 100%)' }} />

      {/* Motion texture: soft rings */}
      <AbsoluteFill style={{ background: 'repeating-radial-gradient(circle at 25% 30%, transparent 0px, transparent 140px, rgba(238,238,238,0.05) 142px)' }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 680, height: 680, borderRadius: '50%', border: '3px solid #3F4F44', opacity: 0.06, right: -220, bottom: -260, transform: `rotate(${-frame * 0.05}deg) scale(${1 + Math.sin(frame / 58) * 0.04})` }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i + 40) * 1920;
        const speed = 0.4 + seededRandom(i + 35) * 0.7;
        const y = 1140 - ((frame * speed + seededRandom(i + 33) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam})`, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 140 }}>
        {/* Left: 2x2 grid — one in four highlighted */}
        <div style={{ display: 'grid', gridTemplateColumns: '210px 210px', gap: 30 }}>
          {[0, 1, 2, 3].map((i) => {
            const s = sp(25 + i * 16);
            const isHot = i === 3;
            const pulse = isHot ? 1 + Math.sin(Math.max(0, frame - 120) / 12) * 0.03 : 1;
            return (
              <div key={i} style={{ width: 210, height: 210, borderRadius: 26, transform: `scale(${s * pulse})`, backgroundColor: isHot ? '#B85C4F' : 'rgba(238,238,238,0.28)', border: isHot ? 'none' : '4px solid #3F4F44', boxShadow: isHot ? '0 14px 0 rgba(63,79,68,0.35)' : 'none' }} />
            );
          })}
        </div>

        {/* Right: typographic stack + disclaimer panel */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-start', width: 860 }}>
          <div style={{ overflow: 'hidden' }}>
            <div style={{ transform: `translateY(${(1 - sp(90)) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 158, lineHeight: 1, color: '#3F4F44' }}>ONE IN FOUR.</div>
          </div>
          <div style={{ opacity: sp(150), transform: `translateY(${(1 - sp(150)) * 22}px)`, marginTop: 26, fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 58, fontWeight: 800, color: '#EEEEEE', lineHeight: 1.2 }}>
            SO HERE'S THE BEGINNING OF YOUR DEFENSE
          </div>

          <div style={{ transform: `translateY(${(1 - panel) * 120}px)`, opacity: panel, marginTop: 46, backgroundColor: '#3F4F44', borderRadius: 18, padding: '30px 44px', borderLeft: '12px solid #E9C46A' }}>
            <div style={{ fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 42, fontWeight: 800, color: '#E9C46A', letterSpacing: 2 }}>RESEARCH & EDUCATION</div>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 600, color: '#EEEEEE', marginTop: 10 }}>NOT PERSONALIZED FINANCIAL ADVICE</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene29;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle orbit feel (scale + subtle rotation)
  const cam = interpolate(frame, [0, durationInFrames], [1.07, 1], { easing: Easing.inOut(Easing.ease) });
  const camRot = interpolate(frame, [0, durationInFrames], [0.6, -0.4], { easing: Easing.inOut(Easing.ease) });

  const cards = [
    { title: 'QUALIFIED ADVISOR', sub: 'LICENSED. ACCOUNTABLE.', delay: 160 },
    { title: 'NONPROFIT CREDIT COUNSELOR', sub: 'LOW COST. ON YOUR SIDE.', delay: 190 },
  ];

  const checkDraw = (delay: number) =>
    interpolate(frame, [delay + 20, delay + 50], [1, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#76ABAE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 0%, #8DBEC0 0%, #76ABAE 50%, #4E7C80 100%)' }} />

      {/* Motion texture: sparse crosshair marks */}
      <AbsoluteFill style={{ backgroundImage: 'radial-gradient(#EEEEEE 2px, transparent 2px)', backgroundSize: '160px 160px', opacity: 0.06 }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 820, height: 820, borderRadius: '50%', border: '3px solid #3F4F44', opacity: 0.05, left: -300, top: -320, transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame / 50) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 460, height: 460, backgroundColor: '#EEEEEE', opacity: 0.04, right: -150, bottom: -160, borderRadius: 70, transform: `rotate(${20 - frame * 0.04}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const sx = seededRandom(i + 50) * 1920;
        const speed = 0.4 + seededRandom(i + 44) * 0.8;
        const y = 1140 - ((frame * speed + seededRandom(i + 47) * 1080) % 1240);
        return <div key={i} style={{ position: 'absolute', left: sx, top: y, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#EEEEEE', opacity: 0.14 }} />;
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${cam}) rotate(${camRot}deg)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ overflow: 'hidden' }}>
          <div style={{ transform: `translateY(${(1 - sp(15)) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 118, lineHeight: 1.05, color: '#EEEEEE', textAlign: 'center' }}>
            YOUR SITUATION IS
          </div>
        </div>
        <div style={{ overflow: 'hidden' }}>
          <div style={{ transform: `translateY(${(1 - sp(32)) * 110}%)`, fontFamily: "'Mont Heavy', 'Arial Black', sans-serif", fontSize: 118, lineHeight: 1.05, color: '#3F4F44', textAlign: 'center' }}>
            YOURS ALONE.
          </div>
        </div>

        <div style={{ display: 'flex', gap: 46, marginTop: 68 }}>
          {cards.map((c, i) => {
            const s = sp(c.delay);
            return (
              <div key={i} style={{ transform: `scale(${s}) translateY(${(1 - s) * 50}px)`, backgroundColor: i === 0 ? '#3F4F44' : '#EEEEEE', borderRadius: 22, padding: '36px 48px', width: 620, boxShadow: '0 14px 0 rgba(63,79,68,0.3)', display: 'flex', alignItems: 'center', gap: 30 }}>
                <svg width="64" height="64" viewBox="0 0 64 64">
                  <circle cx="32" cy="32" r="28" fill="none" stroke={i === 0 ? '#E9C46A' : '#76ABAE'} strokeWidth="6" />
                  <path d="M18 33 L28 43 L47 22" fill="none" stroke={i === 0 ? '#E9C46A' : '#3F4F44'} strokeWidth="7" strokeLinecap="round" strokeLinejoin="round" pathLength={1} strokeDasharray={1} strokeDashoffset={checkDraw(c.delay)} />
                </svg>
                <div>
                  <div style={{ fontFamily: "'Nexa Bold', Inter, sans-serif", fontSize: 40, fontWeight: 800, color: i === 0 ? '#EEEEEE' : '#3F4F44', lineHeight: 1.15 }}>{c.title}</div>
                  <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 28, fontWeight: 600, color: i === 0 ? '#E9C46A' : '#5E9297', marginTop: 8, letterSpacing: 2 }}>{c.sub}</div>
                </div>
              </div>
            );
          })}
        </div>

        <div style={{ opacity: sp(300), fontFamily: 'Inter, Poppins, sans-serif', fontSize: 38, fontWeight: 700, color: '#EEEEEE', letterSpacing: 4, marginTop: 56 }}>
          SEE YOUR FULL PICTURE BEFORE MAJOR MONEY MOVES
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,40,42,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>KuxAI.7x</div>
    </AbsoluteFill>
  );
};

export default Scene30;
```

