```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene11_CoreCPI',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_CoreCPI: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], { extrapolateRight: 'clamp' });

  // Title reveal
  const titleY = sp(8) * 1;
  const titleOp = interpolate(frame, [8, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Core number bounce
  const coreScale = sp(60, { damping: 8, stiffness: 140 });
  const coreOp = interpolate(frame, [60, 80], [0, 1], { extrapolateRight: 'clamp' });

  // Old number fade
  const oldOp = interpolate(frame, [120, 145], [0, 1], { extrapolateRight: 'clamp' });
  const arrowOp = interpolate(frame, [160, 185], [0, 1], { extrapolateRight: 'clamp' });

  // Services bar reveal
  const servicesOp = interpolate(frame, [260, 285], [0, 1], { extrapolateRight: 'clamp' });
  const barFill = interpolate(frame, [285, 350], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Strike-through animation for food/energy
  const strikeProgress = interpolate(frame, [40, 75], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(255,255,255,0.12) 0%, transparent 50%), radial-gradient(circle at 80% 90%, rgba(0,0,0,0.35) 0%, transparent 60%)',
        }}
      />

      {/* Animated grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid11" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid11)" transform={`translate(${Math.sin(frame * 0.02) * 20}, ${Math.cos(frame * 0.02) * 20})`} />
      </svg>

      {/* Shape Layers - large drifting geometric forms */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '4px solid #000000',
          opacity: 0.06,
          top: -200 + Math.sin(frame * 0.01) * 30,
          left: -150 + Math.cos(frame * 0.012) * 30,
          transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#EEEEEE',
          opacity: 0.05,
          bottom: -120 + Math.cos(frame * 0.013) * 25,
          right: -80 + Math.sin(frame * 0.011) * 25,
          transform: `rotate(${45 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #000000',
          opacity: 0.04,
          top: 380 + Math.sin(frame * 0.015) * 15,
          right: 280 + Math.cos(frame * 0.014) * 20,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 2) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 15;
        const size = 4 + seededRandom(i + 3) * 6;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#EEEEEE' : '#000000',
              opacity: 0.25,
              top: py + drift,
              left: px + drift * 0.6,
            }}
          />
        );
      })}

      {/* Main content wrapper with camera */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* TOP TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 100,
            opacity: titleOp,
            transform: `translateY(${(1 - titleY) * -40}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '12px 28px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'Mont Heavy, Inter, sans-serif',
              fontSize: 36,
              fontWeight: 900,
              letterSpacing: 4,
            }}
          >
            DEFINITION // 01
          </div>
          <div
            style={{
              marginTop: 18,
              fontFamily: 'Nexa Bold, Inter, sans-serif',
              fontSize: 102,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '4px 4px 0 #000000',
            }}
          >
            CORE CPI
          </div>
        </div>

        {/* STRIKETHROUGH EXCLUSIONS - left side */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: 100,
            opacity: interpolate(frame, [20, 45], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#000000', fontWeight: 700, letterSpacing: 3, marginBottom: 18 }}>
            STRIPPED OUT:
          </div>

          {['FOOD', 'ENERGY'].map((label, i) => (
            <div
              key={label}
              style={{
                position: 'relative',
                marginBottom: 14,
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 76,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: 2,
                width: 380,
              }}
            >
              {label}
              <div
                style={{
                  position: 'absolute',
                  top: '52%',
                  left: 0,
                  height: 8,
                  width: `${strikeProgress * 100}%`,
                  backgroundColor: '#EEEEEE',
                  transform: `translateY(-50%) skewY(-2deg)`,
                  boxShadow: '2px 2px 0 #000000',
                  transitionDelay: `${i * 200}ms`,
                }}
              />
            </div>
          ))}
        </div>

        {/* HUGE CORE NUMBER - center-right */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            right: 120,
            textAlign: 'right',
            transform: `scale(${coreScale})`,
            opacity: coreOp,
            transformOrigin: 'right center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 34,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              marginBottom: 8,
            }}
          >
            APRIL · YoY
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, Mont Heavy, sans-serif',
              fontSize: 340,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.85,
              textShadow: '8px 8px 0 #000000',
            }}
          >
            2.8<span style={{ fontSize: 200 }}>%</span>
          </div>
        </div>

        {/* MARCH -> APRIL COMPARISON */}
        <div
          style={{
            position: 'absolute',
            top: 640,
            right: 120,
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            opacity: oldOp,
          }}
        >
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#000000', letterSpacing: 4, fontWeight: 700 }}>MARCH</div>
            <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontSize: 96, fontWeight: 900, color: '#000000', lineHeight: 1 }}>2.6%</div>
          </div>

          {/* Arrow */}
          <div style={{ opacity: arrowOp, transform: `translateX(${(1 - arrowOp) * -30}px)` }}>
            <svg width="120" height="60" viewBox="0 0 120 60">
              <path d="M 0 30 L 100 30 M 80 10 L 100 30 L 80 50" stroke="#EEEEEE" strokeWidth="8" fill="none" strokeLinecap="square" />
            </svg>
          </div>

          <div style={{ textAlign: 'center', transform: `scale(${1 + Math.sin(frame * 0.1) * 0.02})` }}>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#EEEEEE', letterSpacing: 4, fontWeight: 700 }}>APRIL</div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 96,
                fontWeight: 900,
                color: '#EEEEEE',
                lineHeight: 1,
                textShadow: '3px 3px 0 #000000',
              }}
            >
              2.8%
            </div>
          </div>
        </div>

        {/* SERVICES BAR - bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 100,
            right: 100,
            opacity: servicesOp,
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline', marginBottom: 14 }}>
            <div style={{ fontFamily: 'Mont Heavy, sans-serif', fontSize: 56, fontWeight: 900, color: '#000000', letterSpacing: 2 }}>
              SERVICES INFLATION
            </div>
            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 88,
                fontWeight: 900,
                color: '#EEEEEE',
                textShadow: '3px 3px 0 #000000',
              }}
            >
              3.5%
            </div>
          </div>
          <div
            style={{
              width: '100%',
              height: 28,
              backgroundColor: '#000000',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${barFill * 70}%`,
                height: '100%',
                background: 'linear-gradient(90deg, #EEEEEE 0%, #FFFFFF 100%)',
                boxShadow: 'inset 0 0 0 2px #000000',
              }}
            />
            {/* Tick marks */}
            {[0, 25, 50, 75, 100].map((p) => (
              <div
                key={p}
                style={{
                  position: 'absolute',
                  left: `${p}%`,
                  top: -8,
                  width: 2,
                  height: 44,
                  backgroundColor: '#000000',
                  opacity: 0.6,
                }}
              />
            ))}
          </div>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 8, fontFamily: 'JetBrains Mono, monospace', fontSize: 22, color: '#000000', fontWeight: 700 }}>
            <span>0%</span>
            <span>2%</span>
            <span>4%</span>
            <span>6%</span>
            <span>8%</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_CoreCPI;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene12_ShelterCosts',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_ShelterCosts: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan camera left-to-right
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // House SVG entrance
  const houseScale = sp(15, { damping: 9, stiffness: 100 });
  const houseOp = interpolate(frame, [15, 40], [0, 1], { extrapolateRight: 'clamp' });

  // Rent rising arrow
  const arrowGrow = interpolate(frame, [80, 160], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.exp) });

  // Percentage punch
  const pctScale = sp(170, { damping: 7, stiffness: 160 });
  const pctOp = interpolate(frame, [170, 195], [0, 1], { extrapolateRight: 'clamp' });

  // Screen shake on impact
  const shakeFrame = frame - 170;
  const shake = shakeFrame > 0 && shakeFrame < 15 ? Math.sin(shakeFrame * 3) * (1 - shakeFrame / 15) * 8 : 0;

  // Pie chart sweep
  const pieSweep = interpolate(frame, [240, 320], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const pieOp = interpolate(frame, [240, 270], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom callout
  const calloutOp = interpolate(frame, [380, 410], [0, 1], { extrapolateRight: 'clamp' });
  const calloutY = sp(380);

  // Shelter percentage of budget (visual: ~33%)
  const sheltSegment = pieSweep * 33;
  const radius = 130;
  const circumference = 2 * Math.PI * radius;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Background gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(255,255,255,0.08) 0%, transparent 40%, rgba(0,0,0,0.3) 100%)',
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diag12" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${frame * 0.5}, 0)`}>
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag12)" />
      </svg>

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#000000',
          opacity: 0.06,
          top: 200 + Math.sin(frame * 0.013) * 25,
          left: -100 + Math.cos(frame * 0.011) * 30,
          transform: `rotate(${frame * 0.07}deg)`,
          borderRadius: 40,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 450,
          height: 450,
          borderRadius: '50%',
          border: '3px solid #EEEEEE',
          opacity: 0.07,
          bottom: -100 + Math.cos(frame * 0.014) * 20,
          right: 100 + Math.sin(frame * 0.012) * 25,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />

      {/* Floating particles */}
      {Array.from({ length: 35 }).map((_, i) => {
        const px = seededRandom(i + 11) * 1920;
        const py = (seededRandom(i + 22) * 1080 + frame * 0.4) % 1080;
        const size = 3 + seededRandom(i + 33) * 5;
        return (
          <div
            key={`p12-${i}`}
            style={{
              position: 'absolute',
              width: size,
              height: size,
              borderRadius: i % 2 === 0 ? '50%' : '0',
              backgroundColor: '#EEEEEE',
              opacity: 0.2,
              top: py,
              left: px,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)` }}>
        {/* TOP LABEL */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 100,
            opacity: interpolate(frame, [0, 25], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              padding: '8px 20px',
              backgroundColor: '#EEEEEE',
              display: 'inline-block',
            }}
          >
            CATEGORY · SHELTER
          </div>
        </div>

        {/* MAIN HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 100,
            opacity: interpolate(frame, [10, 35], [0, 1], { extrapolateRight: 'clamp' }),
            transform: `translateY(${(1 - sp(10)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 136,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            YOUR RENT
            <br />
            <span style={{ color: '#000000', textShadow: 'none' }}>IS RISING.</span>
          </div>
        </div>

        {/* HOUSE SVG with rising arrow */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 180,
            transform: `scale(${houseScale * 1.2})`,
            opacity: houseOp,
            transformOrigin: 'center',
          }}
        >
          <svg width="380" height="380" viewBox="0 0 380 380">
            {/* House body */}
            <rect x="80" y="180" width="220" height="160" fill="#EEEEEE" stroke="#000000" strokeWidth="6" />
            {/* Roof */}
            <polygon points="60,180 190,70 320,180" fill="#000000" />
            {/* Door */}
            <rect x="170" y="240" width="50" height="100" fill="#000000" />
            {/* Windows */}
            <rect x="105" y="210" width="45" height="45" fill="#FF5722" stroke="#000000" strokeWidth="4" />
            <rect x="240" y="210" width="45" height="45" fill="#FF5722" stroke="#000000" strokeWidth="4" />
            {/* Chimney */}
            <rect x="240" y="100" width="35" height="50" fill="#000000" />

            {/* Rising arrow */}
            <g style={{ opacity: arrowGrow }}>
              <line
                x1="340"
                y1={350 - arrowGrow * 240}
                x2="340"
                y2="350"
                stroke="#EEEEEE"
                strokeWidth="10"
              />
              <polygon
                points={`325,${130 - 0} 340,${110 - 0} 355,${130 - 0}`}
                fill="#EEEEEE"
                style={{ opacity: arrowGrow > 0.9 ? 1 : 0 }}
              />
            </g>
          </svg>
        </div>

        {/* PERCENTAGE BIG */}
        <div
          style={{
            position: 'absolute',
            top: 410,
            left: 100,
            transform: `scale(${pctScale})`,
            opacity: pctOp,
            transformOrigin: 'left center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 4,
            }}
          >
            ONE MONTH · APRIL
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 280,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.85,
              textShadow: '7px 7px 0 #000000',
            }}
          >
            +0.6<span style={{ fontSize: 170 }}>%</span>
          </div>
        </div>

        {/* BUDGET PIE CHART */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            right: 250,
            opacity: pieOp,
          }}
        >
          <svg width="320" height="320" viewBox="0 0 320 320">
            {/* Full budget circle */}
            <circle cx="160" cy="160" r={radius} fill="none" stroke="#000000" strokeWidth="60" />
            {/* Shelter segment */}
            <circle
              cx="160"
              cy="160"
              r={radius}
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="60"
              strokeDasharray={`${(circumference * sheltSegment) / 100} ${circumference}`}
              transform="rotate(-90 160 160)"
              strokeLinecap="butt"
            />
            <text
              x="160"
              y="155"
              textAnchor="middle"
              fontFamily="Mont Heavy, sans-serif"
              fontSize="50"
              fontWeight="900"
              fill="#EEEEEE"
            >
              ~33%
            </text>
            <text
              x="160"
              y="195"
              textAnchor="middle"
              fontFamily="Inter, sans-serif"
              fontSize="20"
              fontWeight="700"
              fill="#EEEEEE"
              letterSpacing="3"
            >
              OF BUDGET
            </text>
          </svg>
        </div>

        {/* BOTTOM CALLOUT */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 100,
            opacity: calloutOp,
            transform: `translateY(${(1 - calloutY) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 1,
              lineHeight: 1.1,
            }}
          >
            NOT OIL. NOT WAR.
            <br />
            <span
              style={{
                backgroundColor: '#000000',
                color: '#EEEEEE',
                padding: '4px 16px',
                display: 'inline-block',
                marginTop: 6,
              }}
            >
              JUST YOUR LANDLORD.
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_ShelterCosts;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene13_BroadeningCore',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_BroadeningCore: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // Title entrance
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Cards entrance (staggered)
  const cards = [
    { label: 'INSURANCE', value: '+22%', delay: 40 },
    { label: "DOCTOR VISIT", value: '+5.4%', delay: 75 },
    { label: 'RENT', value: '+0.6%', delay: 110 },
    { label: 'SERVICES', value: '+3.5%', delay: 145 },
  ];

  // Bottom warning
  const warnOp = interpolate(frame, [280, 310], [0, 1], { extrapolateRight: 'clamp' });
  const warnY = sp(280);

  // Spreading ripple
  const ripple = (frame % 90) / 90;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Background overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 100%, rgba(0,0,0,0.4) 0%, transparent 60%), radial-gradient(circle at 50% 0%, rgba(255,255,255,0.1) 0%, transparent 50%)',
        }}
      />

      {/* Concentric ripple texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {[0, 0.33, 0.66].map((offset, i) => {
          const r = ((ripple + offset) % 1) * 1000;
          return (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={r}
              fill="none"
              stroke="#000000"
              strokeWidth="3"
              opacity={1 - (r / 1000)}
            />
          );
        })}
      </svg>

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: 60,
          background: '#EEEEEE',
          opacity: 0.04,
          top: 100 + Math.sin(frame * 0.012) * 25,
          right: -200 + Math.cos(frame * 0.01) * 30,
          transform: `rotate(${15 + frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '220px solid transparent',
          borderRight: '220px solid transparent',
          borderBottom: '380px solid #000000',
          opacity: 0.05,
          bottom: -50 + Math.sin(frame * 0.013) * 20,
          left: -50 + Math.cos(frame * 0.014) * 25,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const py = seededRandom(i + 51) * 1080;
        const drift = Math.cos(frame * 0.02 + i) * 18;
        const size = 4 + seededRandom(i + 52) * 7;
        return (
          <div
            key={`p13-${i}`}
            style={{
              position: 'absolute',
              width: size,
              height: size,
              backgroundColor: i % 2 === 0 ? '#000000' : '#EEEEEE',
              opacity: 0.22,
              borderRadius: i % 3 === 0 ? '50%' : 2,
              top: py + drift,
              left: px,
              transform: `rotate(${frame + i * 30}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* TOP HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 10,
              marginBottom: 14,
            }}
          >
            ── THE SPREAD ──
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 120,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 1,
              textShadow: '5px 5px 0 #000000',
              letterSpacing: 4,
            }}
          >
            CORE IS BROADENING
          </div>
        </div>

        {/* CARDS GRID - 2x2 */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 30,
            flexWrap: 'wrap',
            padding: '0 100px',
          }}
        >
          {cards.map((card, i) => {
            const cardSp = sp(card.delay, { damping: 9, stiffness: 130 });
            const cardOp = interpolate(frame, [card.delay, card.delay + 25], [0, 1], { extrapolateRight: 'clamp' });
            const microBounce = Math.sin((frame - card.delay) * 0.05) * 2;
            return (
              <div
                key={card.label}
                style={{
                  width: 380,
                  height: 280,
                  backgroundColor: i % 2 === 0 ? '#000000' : '#EEEEEE',
                  color: i % 2 === 0 ? '#EEEEEE' : '#000000',
                  border: `5px solid ${i % 2 === 0 ? '#EEEEEE' : '#000000'}`,
                  padding: '36px 32px',
                  transform: `scale(${cardSp}) translateY(${microBounce}px)`,
                  opacity: cardOp,
                  position: 'relative',
                  boxShadow: i % 2 === 0 ? '8px 8px 0 #EEEEEE' : '8px 8px 0 #000000',
                }}
              >
                <div
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 26,
                    fontWeight: 700,
                    letterSpacing: 3,
                    opacity: 0.85,
                    marginBottom: 12,
                  }}
                >
                  0{i + 1} //
                </div>
                <div
                  style={{
                    fontFamily: 'Mont Heavy, sans-serif',
                    fontSize: 56,
                    fontWeight: 900,
                    lineHeight: 1,
                    marginBottom: 24,
                    letterSpacing: 1,
                  }}
                >
                  {card.label}
                </div>
                <div
                  style={{
                    fontFamily: 'Nexa Bold, sans-serif',
                    fontSize: 110,
                    fontWeight: 900,
                    lineHeight: 1,
                  }}
                >
                  {card.value}
                </div>
                {/* Arrow tick */}
                <svg
                  width="40"
                  height="40"
                  style={{ position: 'absolute', top: 30, right: 30 }}
                  viewBox="0 0 40 40"
                >
                  <path
                    d="M 8 32 L 32 8 M 16 8 L 32 8 L 32 24"
                    stroke={i % 2 === 0 ? '#EEEEEE' : '#000000'}
                    strokeWidth="4"
                    fill="none"
                    strokeLinecap="square"
                  />
                </svg>
              </div>
            );
          })}
        </div>

        {/* BOTTOM WARNING BAR */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: warnOp,
            transform: `translateY(${(1 - warnY) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '20px 50px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 56,
              fontWeight: 900,
              letterSpacing: 3,
              border: '4px solid #EEEEEE',
            }}
          >
            NOT JUST ENERGY ANYMORE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_BroadeningCore;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene14_FedMeeting',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_FedMeeting: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow orbit
  const camX = interpolate(frame, [0, durationInFrames], [15, -15]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.12]);

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Calendar entry
  const calScale = sp(50, { damping: 9, stiffness: 130 });
  const calOp = interpolate(frame, [50, 75], [0, 1], { extrapolateRight: 'clamp' });

  // Pulse on dates
  const datePulse = 1 + Math.sin(frame * 0.1) * 0.03;

  // Countdown timer
  const countdownOp = interpolate(frame, [140, 165], [0, 1], { extrapolateRight: 'clamp' });

  // Grenade drop animation
  const grenadeDropStart = 280;
  const grenadeY = interpolate(frame, [grenadeDropStart, grenadeDropStart + 50], [-200, 200], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic),
  });
  const grenadeOp = interpolate(frame, [grenadeDropStart, grenadeDropStart + 10], [0, 1], { extrapolateRight: 'clamp' });

  // Impact shake
  const impactFrame = frame - (grenadeDropStart + 50);
  const shake = impactFrame > 0 && impactFrame < 20 ? Math.sin(impactFrame * 4) * (1 - impactFrame / 20) * 12 : 0;

  // Impact flash
  const flashOp = impactFrame > 0 && impactFrame < 15 ? interpolate(impactFrame, [0, 15], [0.6, 0]) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(0,0,0,0.3) 0%, transparent 30%, transparent 70%, rgba(0,0,0,0.4) 100%)',
        }}
      />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <defs>
          <pattern id="dots14" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots14)" />
      </svg>

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          border: '6px dashed #EEEEEE',
          opacity: 0.06,
          top: -200,
          left: '50%',
          marginLeft: -400,
          transform: `rotate(${frame * 0.3}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 550,
          height: 550,
          background: '#000000',
          opacity: 0.05,
          bottom: -150 + Math.sin(frame * 0.012) * 25,
          right: -100,
          transform: `rotate(${30 + frame * 0.05}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 32 }).map((_, i) => {
        const px = seededRandom(i + 100) * 1920;
        const py = seededRandom(i + 101) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 20;
        return (
          <div
            key={`p14-${i}`}
            style={{
              position: 'absolute',
              width: 5,
              height: 5,
              backgroundColor: '#EEEEEE',
              opacity: 0.25,
              borderRadius: '50%',
              top: py + drift,
              left: px,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px) scale(${camScale})` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 100,
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              color: '#000000',
              letterSpacing: 5,
              fontWeight: 700,
              marginBottom: 8,
            }}
          >
            INCOMING // FED FOMC
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 120,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            ON THE TABLE.
          </div>
        </div>

        {/* CALENDAR BLOCK - left */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 100,
            transform: `scale(${calScale})`,
            opacity: calOp,
            transformOrigin: 'left top',
          }}
        >
          <div
            style={{
              width: 460,
              backgroundColor: '#EEEEEE',
              border: '6px solid #000000',
              boxShadow: '10px 10px 0 #000000',
            }}
          >
            {/* Calendar header */}
            <div
              style={{
                backgroundColor: '#000000',
                color: '#EEEEEE',
                padding: '20px 28px',
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 38,
                fontWeight: 900,
                letterSpacing: 4,
              }}
            >
              JUNE 2026
            </div>
            {/* Dates */}
            <div style={{ padding: '30px 28px' }}>
              <div
                style={{
                  display: 'flex',
                  justifyContent: 'space-between',
                  alignItems: 'center',
                  marginBottom: 24,
                  transform: `scale(${datePulse})`,
                }}
              >
                <div>
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#000000', letterSpacing: 3 }}>
                    MAY CPI DROP
                  </div>
                  <div style={{ fontFamily: 'Nexa Bold, sans-serif', fontSize: 92, fontWeight: 900, color: '#FF5722', lineHeight: 1, WebkitTextStroke: '3px #000000' }}>
                    15
                  </div>
                </div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 24, color: '#000000', fontWeight: 700 }}>
                  TUE
                </div>
              </div>

              <div style={{ height: 4, background: '#000000', margin: '0 -28px 24px' }} />

              <div
                style={{
                  display: 'flex',
                  justifyContent: 'space-between',
                  alignItems: 'center',
                }}
              >
                <div>
                  <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#000000', letterSpacing: 3 }}>
                    FOMC MEETING
                  </div>
                  <div style={{ fontFamily: 'Nexa Bold, sans-serif', fontSize: 92, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
                    16-17
                  </div>
                </div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 24, color: '#000000', fontWeight: 700 }}>
                  W-TH
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* COUNTDOWN PANEL - right */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            right: 150,
            opacity: countdownOp,
            transform: `translateY(${(1 - sp(140)) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 5,
              marginBottom: 12,
            }}
          >
            T-MINUS
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 200,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.9,
              textShadow: '6px 6px 0 #000000',
            }}
          >
            04
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 6,
              marginTop: -8,
            }}
          >
            DAYS
          </div>
          <div
            style={{
              marginTop: 24,
              padding: '14px 20px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 700,
              letterSpacing: 3,
              display: 'inline-block',
            }}
          >
            CONSENSUS: 4.2%
          </div>
        </div>

        {/* GRENADE animation */}
        <div
          style={{
            position: 'absolute',
            top: grenadeY + 200,
            left: '50%',
            marginLeft: -60,
            opacity: grenadeOp,
            transform: `rotate(${grenadeY * 0.5}deg)`,
          }}
        >
          <svg width="120" height="160" viewBox="0 0 120 160">
            {/* Pin */}
            <circle cx="60" cy="20" r="14" fill="none" stroke="#EEEEEE" strokeWidth="5" />
            <line x1="60" y1="34" x2="60" y2="55" stroke="#EEEEEE" strokeWidth="6" />
            {/* Body */}
            <ellipse cx="60" cy="100" rx="42" ry="50" fill="#000000" stroke="#EEEEEE" strokeWidth="4" />
            {/* Grid lines */}
            <line x1="25" y1="85" x2="95" y2="85" stroke="#EEEEEE" strokeWidth="2" opacity="0.7" />
            <line x1="25" y1="115" x2="95" y2="115" stroke="#EEEEEE" strokeWidth="2" opacity="0.7" />
            <line x1="60" y1="55" x2="60" y2="148" stroke="#EEEEEE" strokeWidth="2" opacity="0.7" />
          </svg>
        </div>

        {/* IMPACT FLASH */}
        <AbsoluteFill
          style={{
            backgroundColor: '#EEEEEE',
            opacity: flashOp,
            pointerEvents: 'none',
          }}
        />

        {/* BOTTOM LABEL */}
        <div
          style={{
            position: 'absolute',
            bottom: 95,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [grenadeDropStart + 55, grenadeDropStart + 80], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 36px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 48,
              fontWeight: 900,
              letterSpacing: 4,
            }}
          >
            JUNE 15 = IMPACT DAY
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_FedMeeting;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene15_DoubleEdgedSword',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_DoubleEdgedSword: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [0.3, -0.3]);

  // Top number
  const numScale = sp(8, { damping: 8, stiffness: 140 });
  const numOp = interpolate(frame, [8, 35], [0, 1], { extrapolateRight: 'clamp' });

  // Pressure gauge
  const gaugeOp = interpolate(frame, [80, 110], [0, 1], { extrapolateRight: 'clamp' });
  const gaugeValue = interpolate(frame, [110, 220], [0, 0.92], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Sword reveal
  const swordOp = interpolate(frame, [240, 270], [0, 1], { extrapolateRight: 'clamp' });
  const swordRotate = interpolate(frame, [240, 290], [-90, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.back(1.5)) });

  // Two side labels
  const leftSideOp = interpolate(frame, [310, 340], [0, 1], { extrapolateRight: 'clamp' });
  const rightSideOp = interpolate(frame, [340, 370], [0, 1], { extrapolateRight: 'clamp' });

  // Gauge needle angle (gauge from -90 to 90 degrees)
  const needleAngle = -90 + gaugeValue * 180;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 30%, rgba(255,255,255,0.15) 0%, transparent 60%), radial-gradient(circle at 50% 100%, rgba(0,0,0,0.4) 0%, transparent 50%)',
        }}
      />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="cross15" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform={`rotate(${frame * 0.05})`}>
            <line x1="0" y1="0" x2="60" y2="60" stroke="#000000" strokeWidth="1.5" />
            <line x1="60" y1="0" x2="0" y2="60" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross15)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: '#000000',
          opacity: 0.05,
          top: -150 + Math.sin(frame * 0.01) * 25,
          left: 200 + Math.cos(frame * 0.012) * 30,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#EEEEEE',
          opacity: 0.04,
          bottom: -100,
          right: -100,
          transform: `rotate(${15 + frame * 0.06}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 200) * 1920;
        const py = (seededRandom(i + 201) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={`p15-${i}`}
            style={{
              position: 'absolute',
              width: 4,
              height: 4,
              backgroundColor: '#000000',
              opacity: 0.3,
              top: py,
              left: px,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* TOP: IF 4.2% */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: numOp,
            transform: `scale(${numScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 8,
              marginBottom: 4,
            }}
          >
            IF MAY CPI ≥
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 220,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 1,
              textShadow: '6px 6px 0 #000000',
              letterSpacing: 2,
            }}
          >
            4.2%
          </div>
        </div>

        {/* PRESSURE GAUGE */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            left: 130,
            opacity: gaugeOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 16,
            }}
          >
            HIKE PRESSURE
          </div>
          <svg width="500" height="280" viewBox="0 0 500 280">
            {/* Gauge background arc */}
            <path
              d="M 50 250 A 200 200 0 0 1 450 250"
              fill="none"
              stroke="#000000"
              strokeWidth="50"
              strokeLinecap="butt"
            />
            {/* Filled portion */}
            <path
              d="M 50 250 A 200 200 0 0 1 450 250"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="50"
              strokeLinecap="butt"
              strokeDasharray={`${gaugeValue * 628} 628`}
            />
            {/* Tick marks */}
            {[0, 0.25, 0.5, 0.75, 1].map((t, i) => {
              const a = Math.PI - t * Math.PI;
              const x1 = 250 + Math.cos(a) * 175;
              const y1 = 250 - Math.sin(a) * 175;
              const x2 = 250 + Math.cos(a) * 225;
              const y2 = 250 - Math.sin(a) * 225;
              return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="4" />;
            })}
            {/* Needle */}
            <g transform={`rotate(${needleAngle} 250 250)`}>
              <line x1="250" y1="250" x2="250" y2="80" stroke="#000000" strokeWidth="10" strokeLinecap="round" />
              <circle cx="250" cy="250" r="20" fill="#000000" />
              <circle cx="250" cy="250" r="8" fill="#EEEEEE" />
            </g>
            {/* Labels */}
            <text x="50" y="280" fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#000000">LOW</text>
            <text x="430" y="280" fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#000000">MAX</text>
          </svg>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 60,
              fontWeight: 900,
              color: '#EEEEEE',
              textShadow: '3px 3px 0 #000000',
              marginTop: 10,
              letterSpacing: 2,
            }}
          >
            CRITICAL
          </div>
        </div>

        {/* DOUBLE-EDGED SWORD - right */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 200,
            opacity: swordOp,
            transform: `rotate(${swordRotate}deg)`,
            transformOrigin: 'center',
          }}
        >
          <svg width="380" height="380" viewBox="0 0 380 380">
            {/* Top blade */}
            <polygon points="190,20 220,80 190,140 160,80" fill="#EEEEEE" stroke="#000000" strokeWidth="5" />
            {/* Bottom blade */}
            <polygon points="190,360 160,300 190,240 220,300" fill="#EEEEEE" stroke="#000000" strokeWidth="5" />
            {/* Hilt center */}
            <rect x="100" y="170" width="180" height="40" fill="#000000" />
            <rect x="170" y="140" width="40" height="100" fill="#000000" />
            {/* Edge highlights */}
            <line x1="190" y1="20" x2="190" y2="140" stroke="#000000" strokeWidth="2" />
            <line x1="190" y1="240" x2="190" y2="360" stroke="#000000" strokeWidth="2" />
            {/* Center jewel */}
            <circle cx="190" cy="190" r="14" fill="#FF5722" stroke="#EEEEEE" strokeWidth="4" />
          </svg>
        </div>

        {/* LEFT SIDE LABEL */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            left: 130,
            opacity: leftSideOp,
            transform: `translateX(${(1 - leftSideOp) * -30}px)`,
            maxWidth: 480,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 6,
            }}
          >
            HIKE →
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 1.05,
              textShadow: '3px 3px 0 #000000',
            }}
          >
            MORTGAGES + LOANS
            <br />
            GET CRUSHED
          </div>
        </div>

        {/* RIGHT SIDE LABEL */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            right: 130,
            opacity: rightSideOp,
            transform: `translateX(${(1 - rightSideOp) * 30}px)`,
            maxWidth: 480,
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 6,
            }}
          >
            ← HOLD
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.05,
            }}
          >
            INFLATION
            <br />
            RUNS WILD
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_DoubleEdgedSword;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene16_DebtBrutal',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_DebtBrutal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.1]);

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Three stacked rows
  const rows = [
    { label: 'MORTGAGE', sub: '30-YEAR FIXED', value: '7%+', delay: 50, fill: 0.7 },
    { label: 'CAR LOAN', sub: 'AVG NEW VEHICLE', value: '9.5%', delay: 110, fill: 0.85 },
    { label: 'CREDIT CARD', sub: 'AVG APR', value: '22%+', delay: 170, fill: 1.0 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(0,0,0,0.35) 0%, transparent 30%, transparent 70%, rgba(0,0,0,0.35) 100%)',
        }}
      />

      {/* Vertical lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="vert16" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vert16)" transform={`translate(${Math.sin(frame * 0.02) * 10}, 0)`} />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '5px solid #EEEEEE',
          opacity: 0.05,
          top: -200 + Math.sin(frame * 0.011) * 20,
          right: -200 + Math.cos(frame * 0.013) * 25,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '250px solid transparent',
          borderRight: '250px solid transparent',
          borderBottom: '420px solid #000000',
          opacity: 0.04,
          bottom: 100,
          left: -100,
          transform: `rotate(${15 + frame * 0.04}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const px = seededRandom(i + 300) * 1920;
        const py = seededRandom(i + 301) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div
            key={`p16-${i}`}
            style={{
              position: 'absolute',
              width: 5,
              height: 5,
              backgroundColor: '#EEEEEE',
              opacity: 0.25,
              top: py + drift,
              left: px,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* HEADER */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 100,
            right: 100,
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              marginBottom: 8,
            }}
          >
            RATE HIKE = SHOCKWAVE
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 110,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            EVERY LOAN GETS BRUTAL.
          </div>
        </div>

        {/* BAR ROWS */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 100,
            right: 100,
            display: 'flex',
            flexDirection: 'column',
            gap: 30,
          }}
        >
          {rows.map((row, i) => {
            const rowSp = sp(row.delay, { damping: 10, stiffness: 120 });
            const rowOp = interpolate(frame, [row.delay, row.delay + 25], [0, 1], { extrapolateRight: 'clamp' });
            const fillProgress = interpolate(
              frame,
              [row.delay + 20, row.delay + 80],
              [0, row.fill],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }
            );

            const microMove = Math.sin((frame - row.delay) * 0.06) * 2;

            return (
              <div
                key={row.label}
                style={{
                  opacity: rowOp,
                  transform: `translateX(${(1 - rowSp) * -60}px) translateY(${microMove}px)`,
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', gap: 24, marginBottom: 8 }}>
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 26,
                      color: '#000000',
                      fontWeight: 700,
                      letterSpacing: 3,
                    }}
                  >
                    0{i + 1} /
                  </div>
                  <div
                    style={{
                      fontFamily: 'Mont Heavy, sans-serif',
                      fontSize: 54,
                      fontWeight: 900,
                      color: '#EEEEEE',
                      letterSpacing: 2,
                      textShadow: '3px 3px 0 #000000',
                    }}
                  >
                    {row.label}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 22,
                      color: '#000000',
                      fontWeight: 700,
                      letterSpacing: 3,
                      marginLeft: 'auto',
                    }}
                  >
                    {row.sub}
                  </div>
                </div>
                <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
                  {/* Bar */}
                  <div
                    style={{
                      flex: 1,
                      height: 50,
                      backgroundColor: '#000000',
                      position: 'relative',
                      border: '3px solid #000000',
                    }}
                  >
                    <div
                      style={{
                        width: `${fillProgress * 100}%`,
                        height: '100%',
                        backgroundColor: '#EEEEEE',
                        position: 'relative',
                      }}
                    >
                      {/* Stripes */}
                      <div
                        style={{
                          position: 'absolute',
                          inset: 0,
                          background: 'repeating-linear-gradient(45deg, transparent 0, transparent 8px, rgba(0,0,0,0.15) 8px, rgba(0,0,0,0.15) 16px)',
                        }}
                      />
                    </div>
                  </div>
                  {/* Value */}
                  <div
                    style={{
                      fontFamily: 'Nexa Bold, sans-serif',
                      fontSize: 80,
                      fontWeight: 900,
                      color: '#EEEEEE',
                      lineHeight: 1,
                      width: 220,
                      textAlign: 'right',
                      textShadow: '3px 3px 0 #000000',
                    }}
                  >
                    {row.value}
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* BOTTOM STAMP */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            right: 100,
            opacity: interpolate(frame, [240, 275], [0, 1], { extrapolateRight: 'clamp' }),
            transform: `rotate(-4deg) scale(${sp(240)})`,
          }}
        >
          <div
            style={{
              padding: '14px 36px',
              border: '5px solid #000000',
              backgroundColor: '#EEEEEE',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 44,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 4,
            }}
          >
            BORROWERS BLEED
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_DebtBrutal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene17_Unanchored',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_Unanchored: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera drift
  const camY = interpolate(frame, [0, durationInFrames], [10, -20]);

  // Anchor drift - the visual metaphor
  const anchorDriftY = interpolate(frame, [60, 360], [0, 350], { extrapolateRight: 'clamp', easing: Easing.in(Easing.cubic) });
  const anchorOp = interpolate(frame, [30, 60], [0, 1], { extrapolateRight: 'clamp' });
  const anchorRot = Math.sin(frame * 0.04) * 8;

  // Chain breaking
  const chainBreakFrame = 150;
  const chainOp = interpolate(frame, [chainBreakFrame, chainBreakFrame + 15], [1, 0.3], { extrapolateRight: 'clamp' });

  // Rising price tags
  const tags = [
    { x: 200, delay: 180, value: '$5', y: 600 },
    { x: 1500, delay: 210, value: '$8', y: 700 },
    { x: 450, delay: 240, value: '$12', y: 550 },
    { x: 1200, delay: 270, value: '$20', y: 500 },
    { x: 750, delay: 300, value: '$50', y: 450 },
    { x: 1620, delay: 330, value: '$99', y: 600 },
  ];

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom callout
  const calloutOp = interpolate(frame, [370, 400], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(255,255,255,0.1) 0%, transparent 30%, transparent 70%, rgba(0,0,0,0.5) 100%)',
        }}
      />

      {/* Wave texture - representing ocean/floating */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {[400, 500, 600, 700, 800].map((y, i) => (
          <path
            key={i}
            d={`M 0 ${y + Math.sin((frame + i * 30) * 0.03) * 15} Q 480 ${y + Math.cos((frame + i * 30) * 0.03) * 20 - 30} 960 ${y + Math.sin((frame + i * 30) * 0.03) * 15} T 1920 ${y}`}
            fill="none"
            stroke="#000000"
            strokeWidth="2"
          />
        ))}
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          background: '#EEEEEE',
          opacity: 0.04,
          top: -300 + Math.sin(frame * 0.011) * 20,
          left: -100,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          background: '#000000',
          opacity: 0.05,
          bottom: -200,
          right: -200,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {/* Particles - drifting upward like bubbles */}
      {Array.from({ length: 40 }).map((_, i) => {
        const px = seededRandom(i + 400) * 1920;
        const py = (1080 - (frame * (1 + seededRandom(i + 401)) + seededRandom(i + 402) * 1080) % 1080);
        const size = 3 + seededRandom(i + 403) * 6;
        return (
          <div
            key={`p17-${i}`}
            style={{
              position: 'absolute',
              width: size,
              height: size,
              backgroundColor: '#EEEEEE',
              opacity: 0.25,
              borderRadius: '50%',
              top: py,
              left: px,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 100,
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 5,
              marginBottom: 8,
            }}
          >
            // THE OTHER RISK
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            EXPECTATIONS
            <br />
            <span style={{ color: '#000000', textShadow: 'none' }}>UNANCHORED.</span>
          </div>
        </div>

        {/* ANCHOR with breaking chain */}
        <div
          style={{
            position: 'absolute',
            top: 380 + anchorDriftY,
            left: '50%',
            marginLeft: -100,
            opacity: anchorOp,
            transform: `rotate(${anchorRot}deg)`,
          }}
        >
          <svg width="200" height="280" viewBox="0 0 200 280">
            {/* Broken chain links above */}
            <g opacity={chainOp}>
              <ellipse cx="100" cy="20" rx="18" ry="10" fill="none" stroke="#EEEEEE" strokeWidth="6" />
              <ellipse cx="100" cy="45" rx="10" ry="18" fill="none" stroke="#EEEEEE" strokeWidth="6" />
              <ellipse cx="100" cy="70" rx="18" ry="10" fill="none" stroke="#EEEEEE" strokeWidth="6" />
            </g>
            {/* Break point */}
            {frame > chainBreakFrame && (
              <g>
                <line x1="92" y1="85" x2="80" y2="100" stroke="#EEEEEE" strokeWidth="5" />
                <line x1="108" y1="85" x2="120" y2="100" stroke="#EEEEEE" strokeWidth="5" />
              </g>
            )}
            {/* Anchor */}
            <g transform="translate(0, 100)">
              <line x1="100" y1="0" x2="100" y2="140" stroke="#000000" strokeWidth="14" strokeLinecap="round" />
              <circle cx="100" cy="20" r="22" fill="none" stroke="#000000" strokeWidth="10" />
              <path d="M 30 100 Q 30 160 100 160 Q 170 160 170 100" fill="none" stroke="#000000" strokeWidth="14" strokeLinecap="round" />
              <polygon points="30,100 10,90 30,130" fill="#000000" />
              <polygon points="170,100 190,90 170,130" fill="#000000" />
              <line x1="60" y1="60" x2="140" y2="60" stroke="#000000" strokeWidth="14" />
            </g>
          </svg>
        </div>

        {/* RISING PRICE TAGS */}
        {tags.map((tag, i) => {
          const tagOp = interpolate(frame, [tag.delay, tag.delay + 20], [0, 1], { extrapolateRight: 'clamp' });
          const tagRise = interpolate(frame, [tag.delay, tag.delay + 60], [60, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
          const tagFloat = Math.sin((frame - tag.delay) * 0.05) * 4;
          return (
            <div
              key={`tag-${i}`}
              style={{
                position: 'absolute',
                top: tag.y + tagRise + tagFloat,
                left: tag.x,
                opacity: tagOp,
                transform: `rotate(${i % 2 === 0 ? -5 : 5}deg)`,
              }}
            >
              <div
                style={{
                  padding: '12px 22px',
                  backgroundColor: i % 2 === 0 ? '#000000' : '#EEEEEE',
                  color: i % 2 === 0 ? '#EEEEEE' : '#000000',
                  border: `4px solid ${i % 2 === 0 ? '#EEEEEE' : '#000000'}`,
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 56,
                  fontWeight: 900,
                  letterSpacing: 2,
                  boxShadow: i % 2 === 0 ? '5px 5px 0 #EEEEEE' : '5px 5px 0 #000000',
                }}
              >
                {tag.value}
              </div>
            </div>
          );
        })}

        {/* BOTTOM CALLOUT */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: calloutOp,
            transform: `translateY(${(1 - sp(370)) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '18px 44px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 52,
              fontWeight: 900,
              letterSpacing: 3,
              border: '4px solid #EEEEEE',
            }}
          >
            "PRICES WILL JUST KEEP RISING."
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_Unanchored;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene18_WageSpiral',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_WageSpiral: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit slowly
  const camRot = interpolate(frame, [0, durationInFrames], [-1, 1]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Spiral nodes
  const nodes = [
    { label: 'PRICES UP', delay: 50 },
    { label: 'WAGES UP', delay: 95 },
    { label: 'COSTS UP', delay: 140 },
    { label: 'PRICES UP', delay: 185 },
  ];

  // Spiral rotation
  const spiralRot = interpolate(frame, [40, 280], [0, 360], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Buffett quote
  const quoteOp = interpolate(frame, [310, 350], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(0,0,0,0.4) 0%, transparent 60%), radial-gradient(circle at 70% 50%, rgba(255,255,255,0.1) 0%, transparent 50%)',
        }}
      />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {[150, 250, 350, 450, 550].map((r, i) => (
          <circle
            key={i}
            cx="640"
            cy="540"
            r={r + Math.sin(frame * 0.03 + i) * 8}
            fill="none"
            stroke="#000000"
            strokeWidth="2"
          />
        ))}
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: 80,
          background: '#000000',
          opacity: 0.05,
          top: 100,
          right: -250 + Math.sin(frame * 0.012) * 25,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #EEEEEE',
          opacity: 0.04,
          bottom: -50,
          left: 80,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const angle = (i / 30) * Math.PI * 2 + frame * 0.005;
        const r = 400 + Math.sin(frame * 0.02 + i) * 80;
        return (
          <div
            key={`p18-${i}`}
            style={{
              position: 'absolute',
              width: 5,
              height: 5,
              backgroundColor: '#EEEEEE',
              opacity: 0.25,
              borderRadius: '50%',
              top: 540 + Math.sin(angle) * r,
              left: 640 + Math.cos(angle) * r,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* TITLE - right side */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            right: 100,
            textAlign: 'right',
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
            maxWidth: 800,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 5,
              marginBottom: 8,
            }}
          >
            THE FEEDBACK LOOP
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 100,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            WAGE-PRICE
            <br />
            <span style={{ color: '#000000', textShadow: 'none' }}>SPIRAL</span>
          </div>
        </div>

        {/* SPIRAL DIAGRAM - left */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 200,
          }}
        >
          <svg width="640" height="640" viewBox="0 0 640 640" style={{ transform: `rotate(${spiralRot * 0.1}deg)` }}>
            {/* Outer circle */}
            <circle cx="320" cy="320" r="260" fill="none" stroke="#000000" strokeWidth="4" strokeDasharray="12 8" />
            {/* Inner circle */}
            <circle cx="320" cy="320" r="160" fill="none" stroke="#EEEEEE" strokeWidth="4" />

            {/* Arrows going around */}
            {[0, 1, 2, 3].map((i) => {
              const angle = (i / 4) * Math.PI * 2 - Math.PI / 2;
              const x1 = 320 + Math.cos(angle) * 210;
              const y1 = 320 + Math.sin(angle) * 210;
              const tangentAngle = angle + Math.PI / 2;
              const ax = x1 + Math.cos(tangentAngle) * 40;
              const ay = y1 + Math.sin(tangentAngle) * 40;
              return (
                <g key={i}>
                  <polygon
                    points={`${ax},${ay} ${ax - Math.cos(tangentAngle - 0.5) * 20},${ay - Math.sin(tangentAngle - 0.5) * 20} ${ax - Math.cos(tangentAngle + 0.5) * 20},${ay - Math.sin(tangentAngle + 0.5) * 20}`}
                    fill="#000000"
                  />
                </g>
              );
            })}
          </svg>

          {/* Node labels positioned around */}
          {nodes.map((node, i) => {
            const angle = (i / nodes.length) * Math.PI * 2 - Math.PI / 2;
            const x = 320 + Math.cos(angle) * 210;
            const y = 320 + Math.sin(angle) * 210;
            const nodeOp = interpolate(frame, [node.delay, node.delay + 25], [0, 1], { extrapolateRight: 'clamp' });
            const nodeScale = sp(node.delay, { damping: 8, stiffness: 150 });
            const micro = Math.sin((frame - node.delay) * 0.08) * 2;

            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: y - 35 + micro,
                  left: x - 85,
                  width: 170,
                  textAlign: 'center',
                  padding: '12px 8px',
                  backgroundColor: '#000000',
                  color: '#EEEEEE',
                  border: '4px solid #EEEEEE',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 26,
                  fontWeight: 900,
                  letterSpacing: 2,
                  opacity: nodeOp,
                  transform: `scale(${nodeScale})`,
                }}
              >
                {node.label}
              </div>
            );
          })}

          {/* Center label */}
          <div
            style={{
              position: 'absolute',
              top: 290,
              left: 220,
              width: 200,
              textAlign: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 42,
              fontWeight: 900,
              color: '#EEEEEE',
              textShadow: '3px 3px 0 #000000',
              opacity: interpolate(frame, [220, 250], [0, 1], { extrapolateRight: 'clamp' }),
            }}
          >
            SPIRAL
          </div>
        </div>

        {/* BUFFETT QUOTE - bottom right */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            right: 80,
            width: 760,
            opacity: quoteOp,
            transform: `translateY(${(1 - sp(310)) * 30}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#EEEEEE',
              border: '5px solid #000000',
              padding: '24px 28px',
              boxShadow: '8px 8px 0 #000000',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                letterSpacing: 4,
                marginBottom: 8,
              }}
            >
              — WARREN BUFFETT
            </div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 36,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.1,
                letterSpacing: 1,
              }}
            >
              "INFLATION IS A FAR MORE
              <br />
              DEVASTATING TAX."
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_WageSpiral;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene19_TaxNoDiscrimination',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_TaxNoDiscrimination: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // Title entrance
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // CEO panel
  const ceoOp = interpolate(frame, [60, 90], [0, 1], { extrapolateRight: 'clamp' });
  const ceoScale = sp(60, { damping: 9, stiffness: 110 });

  // Cashier panel
  const cashOp = interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' });
  const cashScale = sp(110, { damping: 9, stiffness: 110 });

  // Erosion bars
  const erosion = interpolate(frame, [200, 340], [1, 0.55], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // VS divider
  const vsOp = interpolate(frame, [170, 200], [0, 1], { extrapolateRight: 'clamp' });
  const vsScale = sp(170, { damping: 7, stiffness: 160 });

  // Bottom text
  const bottomOp = interpolate(frame, [380, 410], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(0,0,0,0.25) 0%, transparent 50%, rgba(0,0,0,0.25) 100%)',
        }}
      />

      {/* Stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="stripe19" width="20" height="20" patternUnits="userSpaceOnUse" patternTransform={`translate(${frame * 0.3}, 0)`}>
            <rect width="10" height="20" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stripe19)" />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '5px solid #000000',
          opacity: 0.06,
          top: -150 + Math.sin(frame * 0.012) * 20,
          left: 660,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          background: '#EEEEEE',
          opacity: 0.04,
          bottom: -150,
          left: -150,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 500) * 1920;
        const py = seededRandom(i + 501) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 15;
        return (
          <div
            key={`p19-${i}`}
            style={{
              position: 'absolute',
              width: 4,
              height: 4,
              backgroundColor: '#000000',
              opacity: 0.3,
              borderRadius: '50%',
              top: py + drift,
              left: px,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              marginBottom: 6,
            }}
          >
            ── THE TAX THAT DOESN'T DISCRIMINATE ──
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 112,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
              letterSpacing: 2,
            }}
          >
            EVERYONE PAYS.
          </div>
        </div>

        {/* CEO PANEL - LEFT */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 130,
            opacity: ceoOp,
            transform: `scale(${ceoScale}) translateX(${(1 - ceoScale) * -40}px)`,
            transformOrigin: 'left center',
            width: 680,
          }}
        >
          <div
            style={{
              backgroundColor: '#000000',
              padding: '28px 36px',
              border: '5px solid #EEEEEE',
              boxShadow: '8px 8px 0 #EEEEEE',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginBottom: 24 }}>
              {/* Briefcase icon */}
              <svg width="100" height="100" viewBox="0 0 100 100">
                <rect x="15" y="35" width="70" height="50" fill="#EEEEEE" stroke="#EEEEEE" strokeWidth="3" />
                <rect x="38" y="25" width="24" height="14" fill="none" stroke="#EEEEEE" strokeWidth="5" />
                <line x1="15" y1="55" x2="85" y2="55" stroke="#000000" strokeWidth="3" />
                <rect x="45" y="50" width="10" height="10" fill="#000000" />
              </svg>
              <div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#EEEEEE', letterSpacing: 4, fontWeight: 700, opacity: 0.7 }}>
                  PROFILE 01
                </div>
                <div style={{ fontFamily: 'Mont Heavy', fontSize: 64, color: '#EEEEEE', fontWeight: 900, letterSpacing: 2 }}>
                  THE CEO
                </div>
              </div>
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#EEEEEE', fontWeight: 700, letterSpacing: 2, marginBottom: 10 }}>
              PURCHASING POWER
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 18 }}>
              <div
                style={{
                  flex: 1,
                  height: 36,
                  backgroundColor: '#FF5722',
                  border: '3px solid #EEEEEE',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                <div
                  style={{
                    width: `${erosion * 100}%`,
                    height: '100%',
                    background: 'repeating-linear-gradient(45deg, #EEEEEE 0, #EEEEEE 10px, rgba(238,238,238,0.7) 10px, rgba(238,238,238,0.7) 20px)',
                  }}
                />
              </div>
              <div
                style={{
                  fontFamily: 'Nexa Bold, sans-serif',
                  fontSize: 56,
                  color: '#EEEEEE',
                  fontWeight: 900,
                  width: 130,
                  textAlign: 'right',
                }}
              >
                -45%
              </div>
            </div>
          </div>
        </div>

        {/* VS DIVIDER */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: '50%',
            marginLeft: -50,
            opacity: vsOp,
            transform: `scale(${vsScale}) rotate(${Math.sin(frame * 0.1) * 4}deg)`,
          }}
        >
          <div
            style={{
              width: 100,
              height: 100,
              borderRadius: '50%',
              backgroundColor: '#EEEEEE',
              border: '6px solid #000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 36,
              fontWeight: 900,
              color: '#000000',
              boxShadow: '5px 5px 0 #000000',
            }}
          >
            VS
          </div>
        </div>

        {/* CASHIER PANEL - RIGHT */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            right: 130,
            opacity: cashOp,
            transform: `scale(${cashScale}) translateX(${(1 - cashScale) * 40}px)`,
            transformOrigin: 'right center',
            width: 680,
          }}
        >
          <div
            style={{
              backgroundColor: '#EEEEEE',
              padding: '28px 36px',
              border: '5px solid #000000',
              boxShadow: '8px 8px 0 #000000',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginBottom: 24 }}>
              {/* Receipt icon */}
              <svg width="100" height="100" viewBox="0 0 100 100">
                <path d="M 20 10 L 80 10 L 80 90 L 70 80 L 60 90 L 50 80 L 40 90 L 30 80 L 20 90 Z" fill="#000000" />
                <line x1="30" y1="30" x2="70" y2="30" stroke="#EEEEEE" strokeWidth="3" />
                <line x1="30" y1="45" x2="70" y2="45" stroke="#EEEEEE" strokeWidth="3" />
                <line x1="30" y1="60" x2="70" y2="60" stroke="#EEEEEE" strokeWidth="3" />
              </svg>
              <div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#000000', letterSpacing: 4, fontWeight: 700, opacity: 0.7 }}>
                  PROFILE 02
                </div>
                <div style={{ fontFamily: 'Mont Heavy', fontSize: 64, color: '#000000', fontWeight: 900, letterSpacing: 2 }}>
                  THE CASHIER
                </div>
              </div>
            </div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#000000', fontWeight: 700, letterSpacing: 2, marginBottom: 10 }}>
              PURCHASING POWER
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 18 }}>
              <div
                style={{
                  flex: 1,
                  height: 36,
                  backgroundColor: '#FF5722',
                  border: '3px solid #000000',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                <div
                  style={{
                    width: `${erosion * 100}%`,
                    height: '100%',
                    background: 'repeating-linear-gradient(45deg, #000000 0, #000000 10px, rgba(0,0,0,0.7) 10px, rgba(0,0,0,0.7) 20px)',
                  }}
                />
              </div>
              <div
                style={{
                  fontFamily: 'Nexa Bold, sans-serif',
                  fontSize: 56,
                  color: '#000000',
                  fontWeight: 900,
                  width: 130,
                  textAlign: 'right',
                }}
              >
                -45%
              </div>
            </div>
          </div>
        </div>

        {/* BOTTOM TEXT */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: bottomOp,
            transform: `translateY(${(1 - sp(380)) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 56,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 3,
            }}
          >
            SAME EROSION ·{' '}
            <span style={{ backgroundColor: '#000000', color: '#EEEEEE', padding: '2px 18px' }}>
              ACROSS THE BOARD
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_TaxNoDiscrimination;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene20_HitsHardest',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene20_HitsHardest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.1]);
  const camY = interpolate(frame, [0, durationInFrames], [0, -15]);

  // Title
  const titleOp = interpolate(frame, [5, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Two income bars comparison
  const highBar = interpolate(frame, [80, 180], [0, 0.3], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const lowBar = interpolate(frame, [110, 210], [0, 0.78], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const lowOp = interpolate(frame, [70, 100], [0, 1], { extrapolateRight: 'clamp' });
  const highOp = interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' });

  // Essential categories rising
  const cats = [
    { label: 'FOOD', value: '+9%', delay: 240 },
    { label: 'GAS', value: '+28.4%', delay: 280 },
    { label: 'RENT', value: '+7.2%', delay: 320 },
  ];

  // Final emphasis
  const finalOp = interpolate(frame, [410, 450], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF5722', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(255,255,255,0.08) 0%, transparent 30%, rgba(0,0,0,0.4) 100%)',
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="grid20" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid20)" transform={`translate(0, ${Math.sin(frame * 0.02) * 15})`} />
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 750,
          height: 750,
          borderRadius: '50%',
          background: '#000000',
          opacity: 0.05,
          top: -250 + Math.sin(frame * 0.012) * 25,
          right: -200,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '200px solid transparent',
          borderRight: '200px solid transparent',
          borderBottom: '360px solid #EEEEEE',
          opacity: 0.04,
          bottom: 200,
          left: 50,
          transform: `rotate(${-15 + frame * 0.04}deg)`,
        }}
      />

      {/* Particles falling */}
      {Array.from({ length: 35 }).map((_, i) => {
        const px = seededRandom(i + 600) * 1920;
        const py = ((seededRandom(i + 601) * 1080) + frame * (1.5 + seededRandom(i + 602))) % 1080;
        const size = 3 + seededRandom(i + 603) * 5;
        return (
          <div
            key={`p20-${i}`}
            style={{
              position: 'absolute',
              width: size,
              height: size,
              backgroundColor: '#EEEEEE',
              opacity: 0.3,
              top: py,
              left: px,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 100,
            opacity: titleOp,
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              marginBottom: 8,
            }}
          >
            // THE UNEQUAL HIT
          </div>
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 124,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.95,
              textShadow: '5px 5px 0 #000000',
            }}
          >
            HARDEST AT
            <br />
            <span style={{ color: '#000000', textShadow: 'none' }}>THE BOTTOM.</span>
          </div>
        </div>

        {/* COMPARISON BARS - LEFT */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 100,
            width: 760,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 18,
            }}
          >
            % OF INCOME ON ESSENTIALS
          </div>

          {/* HIGH INCOME */}
          <div style={{ opacity: highOp, marginBottom: 32 }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
              <div style={{ fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900, color: '#000000', letterSpacing: 2 }}>
                HIGH INCOME
              </div>
              <div style={{ fontFamily: 'Nexa Bold', fontSize: 52, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
                ~30%
              </div>
            </div>
            <div
              style={{
                width: '100%',
                height: 44,
                backgroundColor: '#000000',
                border: '3px solid #000000',
                position: 'relative',
              }}
            >
              <div
                style={{
                  width: `${highBar * 100 / 0.78}%`,
                  height: '100%',
                  backgroundColor: '#EEEEEE',
                }}
              />
            </div>
          </div>

          {/* LOW INCOME */}
          <div style={{ opacity: lowOp }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8 }}>
              <div style={{ fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900, color: '#EEEEEE', letterSpacing: 2, textShadow: '2px 2px 0 #000000' }}>
                LOW INCOME
              </div>
              <div style={{ fontFamily: 'Nexa Bold', fontSize: 52, fontWeight: 900, color: '#EEEEEE', lineHeight: 1, textShadow: '2px 2px 0 #000000' }}>
                ~78%
              </div>
            </div>
            <div
              style={{
                width: '100%',
                height: 44,
                backgroundColor: '#000000',
                border: '3px solid #000000',
                position: 'relative',
              }}
            >
              <div
                style={{
                  width: `${lowBar * 100 / 0.78}%`,
                  height: '100%',
                  background: 'repeating-linear-gradient(45deg, #EEEEEE 0, #EEEEEE 12px, #FF5722 12px, #FF5722 24px)',
                }}
              />
            </div>
          </div>
        </div>

        {/* ESSENTIAL CATEGORIES - RIGHT */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 100,
            width: 560,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              marginBottom: 18,
            }}
          >
            CATEGORIES CLIMBING FASTEST
          </div>

          {cats.map((cat, i) => {
            const cOp = interpolate(frame, [cat.delay, cat.delay + 25], [0, 1], { extrapolateRight: 'clamp' });
            const cScale = sp(cat.delay, { damping: 8, stiffness: 140 });
            const micro = Math.sin((frame - cat.delay) * 0.07) * 2;
            return (
              <div
                key={cat.label}
                style={{
                  opacity: cOp,
                  transform: `scale(${cScale}) translateY(${micro}px)`,
                  marginBottom: 16,
                  padding: '18px 24px',
                  backgroundColor: i === 1 ? '#000000' : '#EEEEEE',
                  border: `4px solid ${i === 1 ? '#EEEEEE' : '#000000'}`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'space-between',
                  boxShadow: i === 1 ? '5px 5px 0 #EEEEEE' : '5px 5px 0 #000000',
                }}
              >
                <div
                  style={{
                    fontFamily: 'Mont Heavy, sans-serif',
                    fontSize: 48,
                    fontWeight: 900,
                    color: i === 1 ? '#EEEEEE' : '#000000',
                    letterSpacing: 2,
                  }}
                >
                  {cat.label}
                </div>
                <div
                  style={{
                    fontFamily: 'Nexa Bold, sans-serif',
                    fontSize: 56,
                    fontWeight: 900,
                    color: i === 1 ? '#EEEEEE' : '#000000',
                    lineHeight: 1,
                  }}
                >
                  {cat.value}
                </div>
              </div>
            );
          })}
        </div>

        {/* BOTTOM FINAL STAMP */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: finalOp,
            transform: `translateY(${(1 - sp(410)) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '16px 40px',
              backgroundColor: '#000000',
              color: '#EEEEEE',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 50,
              fontWeight: 900,
              letterSpacing: 4,
              border: '4px solid #EEEEEE',
            }}
          >
            ESSENTIALS = THE STORM ZONE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#EEEEEE',
          opacity: 0.5,
          letterSpacing: 2,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene20_HitsHardest;
```

