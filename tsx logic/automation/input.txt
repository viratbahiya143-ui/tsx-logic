```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21_QuestionSafe',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21_QuestionSafe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const q1 = sp(10);
  const q2 = sp(40);
  const strike = interpolate(frame, [130, 170], [0, 1], { extrapolateRight: 'clamp' });
  const truthIn = sp(180);
  const zoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 40%, #4d9384 0%, #408175 45%, #2f6357 100%)' }} />

      {/* Shape layers */}
      <div style={{ position: 'absolute', top: -180, left: -180, width: 640, height: 640, borderRadius: '50%',
        border: '3px solid #FE7F2D', opacity: 0.06, transform: `rotate(${frame * 0.15}deg)` }} />
      <div style={{ position: 'absolute', bottom: -220, right: -220, width: 780, height: 780, borderRadius: '50%',
        border: '2px solid #0B0909', opacity: 0.08, transform: `rotate(${-frame * 0.1}deg)` }} />
      <div style={{ position: 'absolute', top: '20%', right: '10%', width: 240, height: 240,
        background: '#FE7F2D', opacity: 0.05, transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.03) * 0.1})` }} />

      {/* Motion grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="grid21" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#0B0909" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid21)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const y = (seededRandom(i + 2) * 1080 + frame * (0.5 + seededRandom(i + 3))) % 1080;
        const s = 4 + seededRandom(i + 4) * 6;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: s, height: s,
          borderRadius: '50%', background: '#FE7F2D', opacity: 0.35 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${zoom})`, display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column' }}>
        <div style={{ opacity: q1, transform: `translateY(${(1 - q1) * 40}px)`, fontFamily: 'Yantra One', fontSize: 108, color: '#0B0909', fontWeight: 900, letterSpacing: -2 }}>
          मेरा पैसा
        </div>
        <div style={{ opacity: q2, transform: `translateY(${(1 - q2) * 40}px)`, fontFamily: 'Yantra One', fontSize: 187, color: '#FE7F2D', fontWeight: 900, letterSpacing: -3, textShadow: '0 6px 0 #0B0909' }}>
          Safe है?
        </div>

        {/* 2008 struck through */}
        <div style={{ marginTop: 40, opacity: q2, position: 'relative', fontFamily: 'Inter', fontSize: 76, color: '#0B0909', fontWeight: 800 }}>
          2008 जैसा संकट?
          <div style={{ position: 'absolute', top: '50%', left: 0, width: `${strike * 100}%`, height: 8, background: '#FE7F2D', transform: 'translateY(-50%) rotate(-2deg)' }} />
        </div>

        <div style={{ marginTop: 60, opacity: truthIn, transform: `scale(${0.7 + truthIn * 0.3})`, fontFamily: 'Inter', fontSize: 51, color: '#0B0909', fontWeight: 700, background: '#FE7F2D', padding: '14px 34px', borderRadius: 10 }}>
          डर नहीं — सच
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(11,9,9,0.55)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene21_QuestionSafe;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22_ICRAStable',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22_ICRAStable: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const badge = sp(8);
  const title = sp(30);
  const pillars = [sp(70), sp(105), sp(140)];
  const stampIn = sp(200, { damping: 8 });
  const stampWiggle = Math.sin(frame * 0.08) * 2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #4d9384 0%, #408175 50%, #356c60 100%)' }} />

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="diag22" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#0B0909" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag22)" />
      </svg>

      {/* Big drifting triangle */}
      <div style={{ position: 'absolute', top: 100, right: -100, width: 500, height: 500,
        background: '#FE7F2D', clipPath: 'polygon(50% 0, 100% 100%, 0 100%)', opacity: 0.05,
        transform: `rotate(${frame * 0.08}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const x = seededRandom(i + 10) * 1920;
        const y = (seededRandom(i + 11) * 1080 - frame * 0.6) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: (y + 1080) % 1080, width: 5, height: 5,
          background: '#0B0909', opacity: 0.25 }} />;
      })}

      {/* Header badge */}
      <div style={{ position: 'absolute', top: 90, left: 90, opacity: badge, transform: `translateX(${(1 - badge) * -30}px)`,
        background: '#0B0909', color: '#FE7F2D', fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 800,
        padding: '12px 28px', letterSpacing: 4 }}>
        ICRA REPORT — 2026-27
      </div>

      {/* Title */}
      <div style={{ position: 'absolute', top: 170, left: 90, opacity: title, transform: `translateY(${(1 - title) * 30}px)`,
        fontFamily: 'Yantra One', fontSize: 136, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        बैंकिंग सेक्टर
      </div>
      <div style={{ position: 'absolute', top: 310, left: 90, opacity: title,
        fontFamily: 'Yantra One', fontSize: 136, color: '#FE7F2D', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        स्थिर / STABLE
      </div>

      {/* Three pillars */}
      {['मज़बूत पूंजी', 'Asset Quality', 'ठीक-ठाक मुनाफ़ा'].map((label, i) => (
        <div key={i} style={{ position: 'absolute', bottom: 130, left: 100 + i * 570, width: 500,
          opacity: pillars[i], transform: `translateY(${(1 - pillars[i]) * 60}px)` }}>
          <div style={{ background: '#0B0909', height: 320 - i * 20, display: 'flex', alignItems: 'flex-end',
            padding: 30, borderTop: `10px solid #FE7F2D` }}>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: '#FE7F2D', fontWeight: 700 }}>
                #0{i + 1}
              </div>
              <div style={{ fontFamily: 'Yantra One', fontSize: 60, color: '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
                {label}
              </div>
            </div>
          </div>
        </div>
      ))}

      {/* Stamp */}
      <div style={{ position: 'absolute', top: 440, right: 120, opacity: stampIn,
        transform: `scale(${stampIn}) rotate(${-12 + stampWiggle}deg)`,
        border: '8px solid #FE7F2D', padding: '30px 50px', color: '#FE7F2D', fontFamily: 'Inter', fontSize: 68,
        fontWeight: 900, letterSpacing: 4 }}>
        STABLE
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene22_ICRAStable;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23_GNPAButBig',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23_GNPAButBig: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const num = interpolate(frame, [10, 70], [0, 2.1], { extrapolateRight: 'clamp' });
  const numIn = sp(10);
  const label = sp(60);
  const barW = interpolate(frame, [80, 140], [0, 210], { extrapolateRight: 'clamp' });
  const butIn = sp(170, { damping: 8, stiffness: 200 });
  const shake = frame > 170 && frame < 200 ? Math.sin(frame * 0.8) * 6 : 0;
  const notEq = sp(250);

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 30%, #4d9384, #408175 60%, #2f6357)' }} />

      {/* Dot texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <defs>
          <pattern id="dots23" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#0B0909" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots23)" />
      </svg>

      {/* Big rotating square */}
      <div style={{ position: 'absolute', top: '30%', left: '-8%', width: 380, height: 380,
        border: '4px solid #FE7F2D', opacity: 0.08, transform: `rotate(${frame * 0.12}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = (seededRandom(i + 20) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i + 21) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, borderRadius: '50%', background: '#FE7F2D', opacity: 0.4 }} />;
      })}

      {/* Top: GNPA good */}
      <div style={{ position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center', opacity: numIn }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 34, color: '#0B0909', fontWeight: 700, letterSpacing: 6 }}>
          BAD LOANS / GNPA 2026-27
        </div>
      </div>

      <div style={{ position: 'absolute', top: 145, left: 0, right: 0, textAlign: 'center', opacity: numIn,
        transform: `scale(${numIn * 1})` }}>
        <span style={{ fontFamily: 'JetBrains Mono', fontSize: 221, color: '#FE7F2D', fontWeight: 900, letterSpacing: -6 }}>
          {num.toFixed(1)}%
        </span>
      </div>

      {/* Comfort bar */}
      <div style={{ position: 'absolute', top: 400, left: '50%', transform: 'translateX(-50%)', display: 'flex', alignItems: 'center', gap: 20, opacity: label }}>
        <div style={{ width: 400, height: 22, background: '#0B0909', borderRadius: 4, overflow: 'hidden' }}>
          <div style={{ width: `${barW}px`, height: '100%', background: '#FE7F2D' }} />
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 44, color: '#0B0909', fontWeight: 800 }}>आरामदायक</div>
      </div>

      {/* The BIG BUT */}
      <div style={{ position: 'absolute', bottom: 260, left: 0, right: 0, textAlign: 'center',
        transform: `scale(${butIn}) translateX(${shake}px)`, opacity: butIn }}>
        <span style={{ fontFamily: 'Yantra One', fontSize: 204, fontWeight: 900, color: '#0B0909', background: '#FE7F2D', padding: '10px 60px', letterSpacing: -4 }}>
          लेकिन...
        </span>
      </div>

      {/* Not equal */}
      <div style={{ position: 'absolute', bottom: 110, left: 0, right: 0, textAlign: 'center', opacity: notEq,
        fontFamily: 'Inter', fontSize: 46, color: '#0B0909', fontWeight: 800 }}>
        सिस्टम स्थिर&nbsp;&nbsp;<span style={{ color: '#FE7F2D', fontSize: 60 }}>≠</span>&nbsp;&nbsp;तुम्हारी जेब सुरक्षित
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 280px rgba(11,9,9,0.55)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene23_GNPAButBig;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24_EffectOneEMI',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24_EffectOneEMI: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const header = sp(8);
  const one = sp(40);
  const step1 = sp(90);
  const arr1 = interpolate(frame, [110, 140], [0, 1], { extrapolateRight: 'clamp' });
  const step2 = sp(140);
  const arr2 = interpolate(frame, [160, 190], [0, 1], { extrapolateRight: 'clamp' });
  const step3 = sp(190);
  const arr3 = interpolate(frame, [210, 240], [0, 1], { extrapolateRight: 'clamp' });
  const step4 = sp(240, { damping: 8 });
  const emiPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  const nodes = [
    { label: 'CASA घटा', sub: 'सस्ता पैसा कम', color: '#0B0909' },
    { label: 'TD / CD', sub: 'महंगा पैसा', color: '#0B0909' },
    { label: 'Loan दरें', sub: 'ऊपर चढ़ीं', color: '#0B0909' },
    { label: 'तुम्हारी EMI', sub: 'बढ़ गई', color: '#FE7F2D' },
  ];
  const opacities = [step1, step2, step3, step4];
  const arrows = [arr1, arr2, arr3];

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #4d9384 0%, #408175 50%, #356c60 100%)' }} />

      {/* Circle grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="circ24" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="40" fill="none" stroke="#0B0909" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#circ24)" />
      </svg>

      {/* Drifting shape */}
      <div style={{ position: 'absolute', top: '10%', right: '5%', width: 300, height: 300, borderRadius: '50%',
        background: '#FE7F2D', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.15})` }} />

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = (seededRandom(i + 30) * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 31) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, background: '#0B0909', opacity: 0.3 }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 90, opacity: header,
        fontFamily: 'JetBrains Mono', fontSize: 32, color: '#0B0909', fontWeight: 800, letterSpacing: 5 }}>
        असर पड़ेगा — तीन तरीकों से
      </div>

      {/* Big #1 */}
      <div style={{ position: 'absolute', top: 130, left: 90, opacity: one,
        transform: `scale(${one})` }}>
        <div style={{ fontFamily: 'Yantra One', fontSize: 187, color: '#FE7F2D', fontWeight: 900, letterSpacing: -4, lineHeight: 1 }}>
          #01 पहला असर
        </div>
      </div>

      {/* Chain flow */}
      <div style={{ position: 'absolute', top: 480, left: 0, right: 0, display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 20, padding: '0 60px' }}>
        {nodes.map((n, i) => (
          <React.Fragment key={i}>
            <div style={{ opacity: opacities[i], transform: `scale(${opacities[i]}) ${i === 3 ? `scale(${emiPulse})` : ''}`,
              background: n.color, padding: '28px 24px', borderTop: `8px solid #FE7F2D`, minWidth: 260, textAlign: 'center' }}>
              <div style={{ fontFamily: 'Yantra One', fontSize: 51, color: n.color === '#FE7F2D' ? '#0B0909' : '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
                {n.label}
              </div>
              <div style={{ marginTop: 8, fontFamily: 'Inter', fontSize: 25, color: '#FE7F2D', fontWeight: 600 }}>
                {n.sub}
              </div>
            </div>
            {i < 3 && (
              <div style={{ width: 80, height: 8, background: '#FE7F2D', opacity: arrows[i], transformOrigin: 'left', transform: `scaleX(${arrows[i]})`, position: 'relative' }}>
                <div style={{ position: 'absolute', right: -20, top: -14, width: 0, height: 0,
                  borderLeft: '24px solid #FE7F2D', borderTop: '18px solid transparent', borderBottom: '18px solid transparent', opacity: arrows[i] }} />
              </div>
            )}
          </React.Fragment>
        ))}
      </div>

      {/* Result label */}
      <div style={{ position: 'absolute', bottom: 130, left: 0, right: 0, textAlign: 'center', opacity: step4,
        fontFamily: 'Inter', fontSize: 44, color: '#0B0909', fontWeight: 800 }}>
        सीधे तुम्हारी जेब पर
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene24_EffectOneEMI;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25_EffectTwoFD',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25_EffectTwoFD: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const header = sp(8);
  const barCredit = interpolate(frame, [40, 100], [0, 620], { extrapolateRight: 'clamp' });
  const barDeposit = interpolate(frame, [70, 130], [0, 340], { extrapolateRight: 'clamp' });
  const gap = sp(150);
  const golden = sp(220, { damping: 8 });
  const goldenPulse = 1 + Math.sin(frame * 0.18) * 0.06;

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 80%, #4d9384, #408175 55%, #2f6357)' }} />

      {/* Vertical lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="vlines25" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="30" y1="0" x2="30" y2="60" stroke="#FE7F2D" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines25)" />
      </svg>

      {/* Big transparent chevron */}
      <div style={{ position: 'absolute', bottom: -100, left: -100, width: 400, height: 400,
        background: '#0B0909', opacity: 0.06, clipPath: 'polygon(0 0, 100% 50%, 0 100%)',
        transform: `translateX(${Math.sin(frame * 0.03) * 20}px)` }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = seededRandom(i + 40) * 1920;
        const y = (seededRandom(i + 41) * 1080 - frame * 0.7) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: (y + 1080) % 1080, width: 5, height: 5, background: '#FE7F2D', opacity: 0.35, borderRadius: '50%' }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 90, opacity: header,
        fontFamily: 'JetBrains Mono', fontSize: 30, color: '#0B0909', fontWeight: 800, letterSpacing: 5 }}>
        #02 दूसरा असर
      </div>
      <div style={{ position: 'absolute', top: 115, left: 90, opacity: header,
        fontFamily: 'Yantra One', fontSize: 119, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        FD की <span style={{ color: '#FE7F2D' }}>होड़</span>
      </div>

      {/* Bars comparison */}
      <div style={{ position: 'absolute', top: 340, left: 100 }}>
        <div style={{ fontFamily: 'Inter', fontSize: 38, color: '#0B0909', fontWeight: 800, marginBottom: 8 }}>Credit ↑</div>
        <div style={{ width: `${barCredit}px`, height: 90, background: '#FE7F2D', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', padding: 20 }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 48, color: '#0B0909', fontWeight: 900 }}>तेज़</span>
        </div>
      </div>

      <div style={{ position: 'absolute', top: 500, left: 100 }}>
        <div style={{ fontFamily: 'Inter', fontSize: 38, color: '#0B0909', fontWeight: 800, marginBottom: 8 }}>Deposit ↓</div>
        <div style={{ width: `${barDeposit}px`, height: 90, background: '#0B0909', display: 'flex', alignItems: 'center', justifyContent: 'flex-end', padding: 20 }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 48, color: '#FE7F2D', fontWeight: 900 }}>सुस्त</span>
        </div>
      </div>

      {/* Gap bracket */}
      <div style={{ position: 'absolute', top: 380, left: 780, opacity: gap, transform: `scale(${gap})`,
        fontFamily: 'JetBrains Mono', fontSize: 51, color: '#0B0909', fontWeight: 800 }}>
        GAP
      </div>
      <div style={{ position: 'absolute', top: 450, left: 780, opacity: gap,
        fontFamily: 'Inter', fontSize: 37, color: '#FE7F2D', fontWeight: 700 }}>
        बैंक बेचैन
      </div>

      {/* Golden opportunity card */}
      <div style={{ position: 'absolute', top: 340, right: 100, opacity: golden,
        transform: `scale(${golden * goldenPulse}) rotate(${-2 + Math.sin(frame * 0.05) * 1}deg)`,
        background: '#0B0909', padding: '40px 50px', borderTop: '10px solid #FE7F2D', borderBottom: '10px solid #FE7F2D' }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: '#FE7F2D', fontWeight: 700, letterSpacing: 3 }}>
          तुम्हारे लिए
        </div>
        <div style={{ marginTop: 10, fontFamily: 'Yantra One', fontSize: 102, color: '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
          सुनहरा
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 102, color: '#FE7F2D', fontWeight: 900, lineHeight: 1 }}>
          मौका
        </div>
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene25_EffectTwoFD;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26_EffectThreeShift',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26_EffectThreeShift: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const header = sp(8);
  const donutIn = sp(50);
  const rot1 = interpolate(frame, [70, 200], [0, 1], { extrapolateRight: 'clamp' });
  const num1 = interpolate(frame, [70, 200], [64, 60], { extrapolateRight: 'clamp' });
  const yr1 = sp(60);
  const yr2 = sp(200);
  const corpIn = sp(240);

  const circ = 2 * Math.PI * 130;

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(45deg, #4d9384 0%, #408175 50%, #356c60 100%)' }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="grid26" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#0B0909" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid26)" />
      </svg>

      {/* Drifting ring */}
      <div style={{ position: 'absolute', top: '15%', left: '40%', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid #FE7F2D', opacity: 0.05, transform: `rotate(${frame * 0.1}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const x = (seededRandom(i + 50) * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i + 51) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 4, height: 4, background: '#FE7F2D', opacity: 0.3 }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 90, opacity: header,
        fontFamily: 'JetBrains Mono', fontSize: 30, color: '#0B0909', fontWeight: 800, letterSpacing: 5 }}>
        #03 तीसरा असर
      </div>
      <div style={{ position: 'absolute', top: 115, left: 90, opacity: header,
        fontFamily: 'Yantra One', fontSize: 102, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        Deposit आधार <span style={{ color: '#FE7F2D' }}>खिसक रहा</span>
      </div>

      {/* Left donut - March 2020 */}
      <div style={{ position: 'absolute', top: 380, left: 180, opacity: yr1, transform: `scale(${yr1})`, textAlign: 'center' }}>
        <svg width="340" height="340" viewBox="0 0 340 340">
          <circle cx="170" cy="170" r="130" fill="none" stroke="#0B0909" strokeWidth="40" opacity="0.3" />
          <circle cx="170" cy="170" r="130" fill="none" stroke="#FE7F2D" strokeWidth="40"
            strokeDasharray={`${circ * 0.64} ${circ}`} transform="rotate(-90 170 170)" strokeLinecap="round" />
          <text x="170" y="165" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="76" fontWeight="900" fill="#0B0909">64%</text>
          <text x="170" y="210" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="700" fill="#0B0909">घरेलू</text>
        </svg>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#0B0909', fontWeight: 800, marginTop: 10 }}>MARCH 2020</div>
      </div>

      {/* Arrow */}
      <div style={{ position: 'absolute', top: 480, left: 600, opacity: yr2,
        fontFamily: 'JetBrains Mono', fontSize: 100, color: '#FE7F2D', fontWeight: 900 }}>
        →
      </div>

      {/* Right donut - March 2025 */}
      <div style={{ position: 'absolute', top: 380, left: 780, opacity: yr2, transform: `scale(${yr2})`, textAlign: 'center' }}>
        <svg width="340" height="340" viewBox="0 0 340 340">
          <circle cx="170" cy="170" r="130" fill="none" stroke="#0B0909" strokeWidth="40" opacity="0.3" />
          <circle cx="170" cy="170" r="130" fill="none" stroke="#FE7F2D" strokeWidth="40"
            strokeDasharray={`${circ * (num1 / 100)} ${circ}`} transform="rotate(-90 170 170)" strokeLinecap="round" />
          <text x="170" y="165" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="76" fontWeight="900" fill="#0B0909">{Math.round(num1)}%</text>
          <text x="170" y="210" textAnchor="middle" fontFamily="Inter" fontSize="28" fontWeight="700" fill="#0B0909">घरेलू</text>
        </svg>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#0B0909', fontWeight: 800, marginTop: 10 }}>MARCH 2025</div>
      </div>

      {/* Corporate rising */}
      <div style={{ position: 'absolute', top: 420, right: 100, opacity: corpIn, transform: `translateY(${(1 - corpIn) * 40}px)`,
        background: '#0B0909', padding: '30px 40px', borderLeft: '10px solid #FE7F2D' }}>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: '#FE7F2D', fontWeight: 700, letterSpacing: 3 }}>
          कॉरपोरेट
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 85, color: '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
          हिस्सा
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 85, color: '#FE7F2D', fontWeight: 900, lineHeight: 1 }}>
          बढ़ा ↑
        </div>
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene26_EffectThreeShift;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27_BucketVolatile',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27_BucketVolatile: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const header = sp(8);
  const bucketIn = sp(40);
  const waveShake = frame > 180 ? Math.sin(frame * 0.5) * 8 : 0;
  const splash = sp(180);
  const tag1 = sp(70);
  const tag2 = sp(110);
  const tag3 = sp(220);

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 60%, #4d9384, #408175 60%, #2f6357)' }} />

      {/* Hex texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="hex27" width="80" height="80" patternUnits="userSpaceOnUse">
            <polygon points="40,10 70,25 70,55 40,70 10,55 10,25" fill="none" stroke="#0B0909" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex27)" />
      </svg>

      {/* Drifting shape */}
      <div style={{ position: 'absolute', top: '10%', left: '5%', width: 250, height: 250,
        background: '#FE7F2D', opacity: 0.05, borderRadius: '50%', transform: `translateY(${Math.sin(frame * 0.03) * 15}px)` }} />

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const x = seededRandom(i + 60) * 1920;
        const y = (seededRandom(i + 61) * 1080 + frame * 0.5) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, background: '#FE7F2D', opacity: 0.35, borderRadius: '50%' }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 90, opacity: header,
        fontFamily: 'Yantra One', fontSize: 102, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        बाल्टी का पानी अब <span style={{ color: '#FE7F2D' }}>चंचल</span>
      </div>

      {/* Bucket SVG */}
      <div style={{ position: 'absolute', bottom: 120, left: '50%', transform: `translateX(-50%) translateY(${waveShake}px) scale(${bucketIn})`, opacity: bucketIn }}>
        <svg width="500" height="480" viewBox="0 0 500 480">
          {/* Bucket walls */}
          <path d="M 60 80 L 100 440 L 400 440 L 440 80 Z" fill="none" stroke="#0B0909" strokeWidth="8" />
          {/* Rim */}
          <ellipse cx="250" cy="80" rx="190" ry="26" fill="none" stroke="#0B0909" strokeWidth="8" />
          {/* Water */}
          <path d={`M 78 200 Q 165 ${190 + Math.sin(frame * 0.15) * 10} 250 200 T 422 200 L 402 440 L 98 440 Z`} fill="#FE7F2D" opacity="0.85" />
          {/* Water wave line */}
          <path d={`M 78 200 Q 165 ${190 + Math.sin(frame * 0.15) * 10} 250 200 T 422 200`} fill="none" stroke="#0B0909" strokeWidth="4" />
          {/* Splash drops */}
          <circle cx={100 + splash * 40} cy={150 - splash * 60} r={splash * 12} fill="#FE7F2D" opacity={1 - splash * 0.4} />
          <circle cx={400 - splash * 30} cy={160 - splash * 50} r={splash * 10} fill="#FE7F2D" opacity={1 - splash * 0.4} />
          <circle cx={250} cy={130 - splash * 40} r={splash * 14} fill="#FE7F2D" opacity={1 - splash * 0.4} />
        </svg>
      </div>

      {/* Labels */}
      <div style={{ position: 'absolute', top: 340, left: 90, opacity: tag1,
        background: '#0B0909', color: '#FE7F2D', padding: '14px 24px', fontFamily: 'JetBrains Mono', fontSize: 30, fontWeight: 800 }}>
        कॉरपोरेट जमा
      </div>
      <div style={{ position: 'absolute', top: 410, left: 90, opacity: tag1,
        fontFamily: 'Inter', fontSize: 44, color: '#0B0909', fontWeight: 800 }}>
        ब्याज-दर पर बेहद
      </div>
      <div style={{ position: 'absolute', top: 465, left: 90, opacity: tag1,
        fontFamily: 'Yantra One', fontSize: 85, color: '#FE7F2D', fontWeight: 900, lineHeight: 1 }}>
        संवेदनशील
      </div>

      <div style={{ position: 'absolute', top: 340, right: 90, opacity: tag2, textAlign: 'right',
        fontFamily: 'JetBrains Mono', fontSize: 30, color: '#0B0909', fontWeight: 800, background: '#FE7F2D', padding: '14px 24px', display: 'inline-block' }}>
        छोटी अवधि
      </div>
      <div style={{ position: 'absolute', top: 410, right: 90, opacity: tag2, textAlign: 'right',
        fontFamily: 'Inter', fontSize: 44, color: '#0B0909', fontWeight: 800 }}>
        पहली आहट पर
      </div>
      <div style={{ position: 'absolute', top: 465, right: 90, opacity: tag2, textAlign: 'right',
        fontFamily: 'Yantra One', fontSize: 85, color: '#0B0909', fontWeight: 900, lineHeight: 1 }}>
        छलक जाए
      </div>

      {/* Bottom warning strip */}
      <div style={{ position: 'absolute', bottom: 40, left: 0, right: 0, textAlign: 'center', opacity: tag3,
        fontFamily: 'Inter', fontSize: 37, color: '#0B0909', fontWeight: 800, background: '#FE7F2D', padding: '10px 0' }}>
        बाल्टी वही — पानी बदल गया
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene27_BucketVolatile;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28_BuffettTrust',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28_BuffettTrust: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const quoteIn = sp(20);
  const line1 = sp(60);
  const line2 = sp(120);
  const link1 = sp(200);
  const link2 = sp(240);
  const equalIn = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #4d9384 0%, #408175 50%, #2f6357 100%)' }} />

      {/* Cross texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="cross28" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="50" y1="30" x2="50" y2="70" stroke="#FE7F2D" strokeWidth="2" />
            <line x1="30" y1="50" x2="70" y2="50" stroke="#FE7F2D" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross28)" />
      </svg>

      {/* Big rotating quote mark */}
      <div style={{ position: 'absolute', top: 60, left: 60, fontFamily: 'Yantra One', fontSize: 500,
        color: '#FE7F2D', opacity: 0.08, fontWeight: 900, lineHeight: 1 }}>
        "
      </div>
      <div style={{ position: 'absolute', bottom: -220, right: 60, fontFamily: 'Yantra One', fontSize: 500,
        color: '#0B0909', opacity: 0.08, fontWeight: 900, lineHeight: 1 }}>
        "
      </div>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const x = seededRandom(i + 70) * 1920;
        const y = (seededRandom(i + 71) * 1080 - frame * 0.4) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: (y + 1080) % 1080, width: 4, height: 4, background: '#FE7F2D', opacity: 0.3 }} />;
      })}

      {/* Attribution */}
      <div style={{ position: 'absolute', top: 100, left: 0, right: 0, textAlign: 'center', opacity: quoteIn,
        fontFamily: 'JetBrains Mono', fontSize: 32, color: '#0B0909', fontWeight: 800, letterSpacing: 6 }}>
        — WARREN BUFFETT की सोच
      </div>

      {/* Main quote */}
      <div style={{ position: 'absolute', top: 240, left: 0, right: 0, textAlign: 'center', opacity: line1,
        transform: `translateY(${(1 - line1) * 30}px)`,
        fontFamily: 'Yantra One', fontSize: 119, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1.05 }}>
        बैंकिंग — <span style={{ color: '#FE7F2D' }}>भरोसे</span>
      </div>
      <div style={{ position: 'absolute', top: 370, left: 0, right: 0, textAlign: 'center', opacity: line1,
        fontFamily: 'Yantra One', fontSize: 119, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1.05 }}>
        का कारोबार
      </div>

      {/* Chain */}
      <div style={{ position: 'absolute', bottom: 200, left: 0, right: 0, display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30 }}>
        <div style={{ opacity: link1, transform: `scale(${link1})`,
          background: '#0B0909', padding: '32px 40px', borderTop: '8px solid #FE7F2D' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: '#FE7F2D', fontWeight: 700, letterSpacing: 3 }}>बैंक</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 68, color: '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>मज़बूत</div>
        </div>

        <div style={{ opacity: equalIn, fontFamily: 'Yantra One', fontSize: 119, color: '#FE7F2D', fontWeight: 900 }}>=</div>

        <div style={{ opacity: link2, transform: `scale(${link2})`,
          background: '#FE7F2D', padding: '32px 40px', borderTop: '8px solid #0B0909' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: '#0B0909', fontWeight: 700, letterSpacing: 3 }}>जमाकर्ता का</div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 68, color: '#0B0909', fontWeight: 900, lineHeight: 1 }}>यकीन मज़बूत</div>
        </div>
      </div>

      <div style={{ position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center', opacity: equalIn,
        fontFamily: 'Inter', fontSize: 34, color: '#0B0909', fontWeight: 700, fontStyle: 'italic' }}>
        जब तक भरोसा — तब तक बैंक
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene28_BuffettTrust;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29_ThreeRulesBuffett',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29_ThreeRulesBuffett: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const header = sp(8);
  const timelineIn = sp(30);
  const dot1 = sp(60);
  const dot2 = sp(85);
  const dot3 = sp(110);
  const rule1 = sp(160);
  const rule2 = sp(200);
  const rule3 = sp(240);
  const lineW = interpolate(frame, [40, 120], [0, 1400], { extrapolateRight: 'clamp' });

  const crises = ['1987', '2000', '2008'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 60% 40%, #4d9384, #408175 55%, #2f6357)' }} />

      {/* Diagonal texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="dg29" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(-30)">
            <line x1="0" y1="30" x2="60" y2="30" stroke="#0B0909" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dg29)" />
      </svg>

      {/* Shape */}
      <div style={{ position: 'absolute', top: '10%', right: '8%', width: 320, height: 320,
        border: '4px solid #FE7F2D', opacity: 0.06, transform: `rotate(${frame * 0.15}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const x = seededRandom(i + 80) * 1920;
        const y = (seededRandom(i + 81) * 1080 + frame * 0.5) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 3, height: 3, background: '#FE7F2D', opacity: 0.35, borderRadius: '50%' }} />;
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 90, opacity: header,
        fontFamily: 'JetBrains Mono', fontSize: 32, color: '#0B0909', fontWeight: 800, letterSpacing: 5 }}>
        BUFFETT ने देखे कई संकट
      </div>

      {/* Timeline */}
      <div style={{ position: 'absolute', top: 200, left: 100, right: 100, height: 140 }}>
        <div style={{ position: 'absolute', top: 60, left: 0, width: lineW, height: 6, background: '#0B0909' }} />
        {crises.map((yr, i) => {
          const op = [dot1, dot2, dot3][i];
          return (
            <div key={i} style={{ position: 'absolute', left: `${15 + i * 30}%`, top: 20, opacity: op, transform: `scale(${op})` }}>
              <div style={{ width: 90, height: 90, borderRadius: '50%', background: '#FE7F2D', border: '6px solid #0B0909' }} />
              <div style={{ marginTop: 10, fontFamily: 'JetBrains Mono', fontSize: 40, color: '#0B0909', fontWeight: 900, textAlign: 'center', marginLeft: -20 }}>
                {yr}
              </div>
            </div>
          );
        })}
      </div>

      {/* Sub header */}
      <div style={{ position: 'absolute', top: 400, left: 90,
        fontFamily: 'Yantra One', fontSize: 85, color: '#0B0909', fontWeight: 900, letterSpacing: -2 }}>
        तरीका <span style={{ color: '#FE7F2D' }}>एक ही</span> —
      </div>

      {/* Three rules */}
      <div style={{ position: 'absolute', bottom: 130, left: 90, right: 90, display: 'flex', gap: 30, justifyContent: 'space-between' }}>
        {[
          { n: '01', h: 'घबराओ मत', s: 'Panic never pays', op: rule1 },
          { n: '02', h: 'तैयार रहो', s: 'Prepared > Predicting', op: rule2 },
          { n: '03', h: 'Cash की कीमत', s: 'कभी कम मत आँको', op: rule3 },
        ].map((r, i) => (
          <div key={i} style={{ flex: 1, opacity: r.op, transform: `translateY(${(1 - r.op) * 60}px)`,
            background: i === 2 ? '#FE7F2D' : '#0B0909', padding: '30px 32px', borderLeft: `10px solid ${i === 2 ? '#0B0909' : '#FE7F2D'}` }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, color: i === 2 ? '#0B0909' : '#FE7F2D', fontWeight: 700, letterSpacing: 3 }}>
              RULE #{r.n}
            </div>
            <div style={{ marginTop: 8, fontFamily: 'Yantra One', fontSize: 68, color: i === 2 ? '#0B0909' : '#F5F5F5', fontWeight: 900, lineHeight: 1 }}>
              {r.h}
            </div>
            <div style={{ marginTop: 12, fontFamily: 'Inter', fontSize: 28, color: i === 2 ? '#0B0909' : '#FE7F2D', fontWeight: 600 }}>
              {r.s}
            </div>
          </div>
        ))}
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene29_ThreeRulesBuffett;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30_CashIsPower',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30_CashIsPower: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const head1 = sp(10);
  const cashIn = sp(50, { damping: 8 });
  const opt1 = sp(100);
  const opt2 = sp(135);
  const opt3 = sp(170);
  const berkIn = sp(220);
  const cta = sp(280);

  // Stack heights growing
  const stackH = interpolate(frame, [50, 180], [0, 260], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#408175', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(220deg, #4d9384 0%, #408175 50%, #356c60 100%)' }} />

      {/* Grid + circle texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="mix30" width="90" height="90" patternUnits="userSpaceOnUse">
            <circle cx="45" cy="45" r="3" fill="#FE7F2D" />
            <line x1="0" y1="45" x2="90" y2="45" stroke="#0B0909" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#mix30)" />
      </svg>

      {/* Rotating hex */}
      <div style={{ position: 'absolute', top: '15%', right: '10%', width: 350, height: 350,
        background: '#FE7F2D', opacity: 0.05, clipPath: 'polygon(50% 0, 100% 25%, 100% 75%, 50% 100%, 0 75%, 0 25%)',
        transform: `rotate(${frame * 0.12}deg)` }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const x = seededRandom(i + 90) * 1920;
        const y = (seededRandom(i + 91) * 1080 - frame * 0.6) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: (y + 1080) % 1080, width: 4, height: 4, background: '#FE7F2D', opacity: 0.4, borderRadius: '50%' }} />;
      })}

      {/* Main header */}
      <div style={{ position: 'absolute', top: 80, left: 90, opacity: head1,
        fontFamily: 'Yantra One', fontSize: 136, color: '#0B0909', fontWeight: 900, letterSpacing: -3, lineHeight: 1 }}>
        Cash सिर्फ़ पैसा <span style={{ color: '#FE7F2D' }}>नहीं</span>
      </div>

      {/* CASH giant */}
      <div style={{ position: 'absolute', top: 260, left: 90, opacity: cashIn,
        transform: `scale(${cashIn})`,
        fontFamily: 'Yantra One', fontSize: 221, color: '#FE7F2D', fontWeight: 900, letterSpacing: -5, lineHeight: 1, textShadow: '0 8px 0 #0B0909' }}>
        CASH = ताकत
      </div>

      {/* Three definitions */}
      <div style={{ position: 'absolute', top: 520, left: 90, display: 'flex', gap: 20, flexWrap: 'wrap' }}>
        {[
          { l: 'एक विकल्प', op: opt1 },
          { l: 'एक ताकत', op: opt2 },
          { l: 'एक हथियार', op: opt3 },
        ].map((o, i) => (
          <div key={i} style={{ opacity: o.op, transform: `translateY(${(1 - o.op) * 30}px)`,
            background: '#0B0909', color: '#F5F5F5', padding: '18px 34px',
            fontFamily: 'Inter', fontSize: 44, fontWeight: 800, borderBottom: '6px solid #FE7F2D' }}>
            {o.l}
          </div>
        ))}
      </div>

      {/* Berkshire cash stacks */}
      <div style={{ position: 'absolute', bottom: 180, right: 120, opacity: berkIn, transform: `translateY(${(1 - berkIn) * 40}px)` }}>
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 14, justifyContent: 'center' }}>
          {[0.6, 0.8, 1, 0.85, 0.7].map((h, i) => (
            <div key={i} style={{ width: 46, height: stackH * h, background: '#FE7F2D', border: '3px solid #0B0909' }} />
          ))}
        </div>
        <div style={{ marginTop: 14, textAlign: 'center', fontFamily: 'JetBrains Mono', fontSize: 30, color: '#0B0909', fontWeight: 800, letterSpacing: 3 }}>
          BERKSHIRE का
        </div>
        <div style={{ textAlign: 'center', fontFamily: 'Yantra One', fontSize: 68, color: '#0B0909', fontWeight: 900, lineHeight: 1 }}>
          Cash Pile
        </div>
      </div>

      {/* Bottom CTA */}
      <div style={{ position: 'absolute', bottom: 40, left: 0, right: 0, textAlign: 'center', opacity: cta,
        background: '#FE7F2D', padding: '14px 0',
        fontFamily: 'Inter', fontSize: 38, color: '#0B0909', fontWeight: 800, letterSpacing: 2 }}>
        यही सोच → अपनी निजी ज़िंदगी में उतारो
      </div>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 260px rgba(11,9,9,0.5)', pointerEvents: 'none' }} />

      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene30_CashIsPower;
```

