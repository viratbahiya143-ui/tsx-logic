```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_NotebandiAnnouncement',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_NotebandiAnnouncement: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock sweep animation — 8pm drama
  const clockHand = interpolate(frame, [0, 60], [0, 240], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Screen flicker for TV broadcast feel
  const flicker = frame % 6 < 1 && frame < 30 ? 0.7 : 1;

  // Red alert pulse
  const alertPulse = 0.6 + 0.4 * Math.sin(frame * 0.25);

  // Shockwave ripple on impact
  const shockwave = interpolate(frame, [45, 75], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });
  const shockwaveScale = 1 + shockwave * 4;
  const shockwaveOp = interpolate(frame, [45, 75], [0.8, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Text reveal
  const titleSp = sp(20, { damping: 10, stiffness: 100 });
  const subSp = sp(45);
  const dateSp = sp(60);

  // Scanlines moving
  const scanlineY = (frame * 3) % 1080;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0A0A0A', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep red gradient — emergency broadcast feel */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, #3D0000 0%, #1A0000 45%, #0A0A0A 100%)',
        opacity: flicker,
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(5)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 300 + i * 120,
            height: 300 + i * 120,
            borderRadius: '50%',
            border: `2px solid #ff0000`,
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
            opacity: 0.04 + i * 0.01,
          }} />
        ))}
      </AbsoluteFill>

      {/* Moving scanline texture */}
      <AbsoluteFill style={{ overflow: 'hidden', opacity: 0.04 }}>
        {[...Array(30)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: '100%',
            height: 1,
            backgroundColor: '#ff0000',
            top: (scanlineY + i * 36) % 1080,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles — red embers */}
      <AbsoluteFill style={{ opacity: 0.7 }}>
        {[...Array(18)].map((_, i) => {
          const px = seededRandom(i * 7) * 1920;
          const py = seededRandom(i * 13) * 1080;
          const drift = Math.sin(frame * 0.03 + i) * 15;
          const size = 2 + seededRandom(i * 3) * 4;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: px + drift,
              top: py - (frame * (0.3 + seededRandom(i) * 0.4)) % 1080,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff0000' : '#FFC107',
              opacity: 0.15 + seededRandom(i * 5) * 0.25,
              boxShadow: `0 0 ${size * 2}px ${i % 3 === 0 ? '#ff0000' : '#FFC107'}`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Shockwave ring */}
      <AbsoluteFill style={{
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        opacity: shockwaveOp,
      }}>
        <div style={{
          width: 200,
          height: 200,
          borderRadius: '50%',
          border: '4px solid #ff0000',
          transform: `scale(${shockwaveScale})`,
        }} />
      </AbsoluteFill>

      {/* TV broadcast overlay — top bar */}
      <div style={{
        position: 'absolute',
        top: 0, left: 0, right: 0,
        height: 8,
        background: 'linear-gradient(90deg, #ff0000, #FFC107, #ff0000)',
        opacity: alertPulse,
      }} />
      <div style={{
        position: 'absolute',
        bottom: 0, left: 0, right: 0,
        height: 8,
        background: 'linear-gradient(90deg, #ff0000, #FFC107, #ff0000)',
        opacity: alertPulse,
      }} />

      {/* BREAKING NEWS tag */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 80,
        display: 'flex',
        alignItems: 'center',
        gap: 16,
        opacity: interpolate(frame, [10, 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          backgroundColor: '#ff0000',
          padding: '10px 24px',
          borderRadius: 4,
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>⚡ BREAKING</span>
        </div>
        <span style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#9E9E9E',
          letterSpacing: 2,
        }}>8:00 PM — 8 NOV 2016</span>
      </div>

      {/* Giant clock SVG — center left */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: '50%',
        transform: `translateY(-50%) scale(${sp(10)})`,
        opacity: interpolate(frame, [10, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="340" height="340" viewBox="0 0 340 340">
          {/* Clock face */}
          <circle cx="170" cy="170" r="160" fill="none" stroke="#ff0000" strokeWidth="6" opacity="0.6" />
          <circle cx="170" cy="170" r="150" fill="#1C1C1C" opacity="0.9" />
          <circle cx="170" cy="170" r="8" fill="#FFC107" />

          {/* Hour markers */}
          {[...Array(12)].map((_, i) => {
            const angle = (i * 30) * (Math.PI / 180);
            const x1 = 170 + 130 * Math.sin(angle);
            const y1 = 170 - 130 * Math.cos(angle);
            const x2 = 170 + 145 * Math.sin(angle);
            const y2 = 170 - 145 * Math.cos(angle);
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#9E9E9E" strokeWidth={i % 3 === 0 ? 4 : 2} />;
          })}

          {/* Hour hand pointing to 8 */}
          <line
            x1="170" y1="170"
            x2={170 + 90 * Math.sin((clockHand) * Math.PI / 180)}
            y2={170 - 90 * Math.cos((clockHand) * Math.PI / 180)}
            stroke="#FFC107" strokeWidth="8" strokeLinecap="round"
          />

          {/* Minute hand */}
          <line
            x1="170" y1="170"
            x2={170 + 120 * Math.sin((frame * 6) * Math.PI / 180)}
            y2={170 - 120 * Math.cos((frame * 6) * Math.PI / 180)}
            stroke="#ff0000" strokeWidth="4" strokeLinecap="round"
            opacity={alertPulse}
          />

          {/* 8 PM label */}
          <text x="170" y="290" textAnchor="middle" fill="#ff0000" fontSize="22" fontFamily="JetBrains Mono" fontWeight="bold">रात 8 बजे</text>
        </svg>
      </div>

      {/* Main content — right side */}
      <div style={{
        position: 'absolute',
        left: 520,
        top: '50%',
        transform: 'translateY(-50%)',
        maxWidth: 1200,
      }}>
        {/* INDIA label */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 20,
          marginBottom: 24,
          opacity: interpolate(frame, [15, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateX(${interpolate(frame, [15, 35], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
        }}>
          {/* India flag colors */}
          <div style={{ display: 'flex', gap: 4 }}>
            <div style={{ width: 20, height: 48, backgroundColor: '#FF9933', borderRadius: 2 }} />
            <div style={{ width: 20, height: 48, backgroundColor: '#F5F5F5', borderRadius: 2 }} />
            <div style={{ width: 20, height: 48, backgroundColor: '#138808', borderRadius: 2 }} />
          </div>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 4,
          }}>भारत संदर्भ</span>
        </div>

        {/* Hero headline */}
        <div style={{
          transform: `translateY(${(1 - titleSp) * 60}px)`,
          opacity: titleSp,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 130,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 0.9,
            letterSpacing: -2,
          }}>
            नोटबंदी
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 700,
            color: '#ff0000',
            lineHeight: 1,
            marginTop: 8,
          }}>
            एक रात — एक घोषणा
          </div>
        </div>

        {/* Date badge */}
        <div style={{
          marginTop: 32,
          transform: `scale(${dateSp})`,
          opacity: dateSp,
          display: 'inline-block',
        }}>
          <div style={{
            backgroundColor: '#2D2D2D',
            border: '2px solid #FFC107',
            borderRadius: 12,
            padding: '16px 36px',
            display: 'inline-flex',
            alignItems: 'center',
            gap: 20,
          }}>
            {/* Calendar SVG icon */}
            <svg width="36" height="36" viewBox="0 0 36 36">
              <rect x="2" y="6" width="32" height="28" rx="4" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="2" y1="14" x2="34" y2="14" stroke="#FFC107" strokeWidth="2" />
              <rect x="10" y="2" width="4" height="8" rx="2" fill="#FFC107" />
              <rect x="22" y="2" width="4" height="8" rx="2" fill="#FFC107" />
              <text x="18" y="28" textAnchor="middle" fill="#F5F5F5" fontSize="12" fontFamily="JetBrains Mono" fontWeight="bold">8</text>
            </svg>
            <span style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#FFC107',
            }}>8 नवंबर 2016</span>
          </div>
        </div>

        {/* Sub text */}
        <div style={{
          marginTop: 28,
          opacity: subSp,
          transform: `translateY(${(1 - subSp) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#9E9E9E',
            lineHeight: 1.4,
          }}>
            500 और 1000 के नोट — <span style={{ color: '#ff0000', fontWeight: 700 }}>बंद</span>
          </div>
        </div>
      </div>

      {/* Stick figure — shocked pose */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        right: 120,
        transform: `translateY(${(1 - sp(50)) * 100}px) scale(${sp(50)})`,
        opacity: sp(50),
      }}>
        {/* Golden glow */}
        <div style={{
          position: 'absolute',
          width: 100,
          height: 100,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.25) 0%, transparent 70%)',
          left: -10,
          top: -10,
        }} />
        <svg width="80" height="160" viewBox="0 0 80 160">
          {/* Head */}
          <circle cx="40" cy="22"
            r="18"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.08) * 8}, 40, 22)`}
          />
          {/* Golden glasses */}
          <circle cx="32" cy="21" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="48" cy="21" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="39" y1="21" x2="41" y2="21" stroke="#FFC107" strokeWidth="1.5" />

          {/* Shocked expression */}
          <circle cx="40" cy="27" r="3" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />

          {/* Body — leaning back in shock */}
          <line x1="40" y1="40" x2="40"
            y2={90 + Math.sin(frame * 0.05) * 2}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round"
          />
          {/* Arms raised in shock */}
          <line x1="40" y1="60" x2="10" y2="40" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="60" x2="70" y2="40" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Legs */}
          <line x1="40" y1="90" x2="22" y2="150" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="90" x2="58" y2="150" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 40%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene1_NotebandiAnnouncement;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_CashCrisis',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_CashCrisis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Queue of people — marching animation
  const queueOffset = (frame * 0.8) % 120;

  // Notes falling animation
  const notesFall = interpolate(frame, [0, durationInFrames], [0, 400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // X mark stamp
  const stampScale = sp(40, { damping: 6, stiffness: 200 });

  // Diagonal stripe motion
  const stripeX = (frame * 1.5) % 80;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Charcoal gradient — oppressive feel */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #2D2D2D 0%, #1C1C1C 50%, #0D0D0D 100%)',
      }} />

      {/* Diagonal warning stripes — background texture */}
      <AbsoluteFill style={{ opacity: 0.04, overflow: 'hidden' }}>
        {[...Array(20)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 60,
            height: 1400,
            backgroundColor: '#ff0000',
            left: (i * 80) - stripeX,
            top: -200,
            transform: 'rotate(30deg)',
            transformOrigin: 'top left',
          }} />
        ))}
      </AbsoluteFill>

      {/* Falling banknotes SVG shapes */}
      <AbsoluteFill style={{ overflow: 'hidden', opacity: 0.7 }}>
        {[...Array(12)].map((_, i) => {
          const startX = seededRandom(i * 11) * 1800 + 60;
          const startY = -100 + seededRandom(i * 3) * 200;
          const speed = 0.6 + seededRandom(i * 7) * 1.2;
          const rotation = Math.sin(frame * 0.04 + i) * 25;
          const currentY = (startY + (frame * speed * 2.5) + i * 90) % (1200) - 100;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: startX + Math.sin(frame * 0.02 + i) * 20,
              top: currentY,
              transform: `rotate(${rotation}deg)`,
            }}>
              <svg width="80" height="40" viewBox="0 0 80 40">
                <rect x="0" y="0" width="80" height="40" rx="4"
                  fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
                <text x="40" y="16" textAnchor="middle" fill="#9E9E9E"
                  fontSize="8" fontFamily="JetBrains Mono">₹500</text>
                {/* X mark over note */}
                <line x1="10" y1="10" x2="70" y2="30" stroke="#ff0000" strokeWidth="2" opacity="0.8" />
                <line x1="70" y1="10" x2="10" y2="30" stroke="#ff0000" strokeWidth="2" opacity="0.8" />
              </svg>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(20)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#9E9E9E',
            left: seededRandom(i * 17) * 1920,
            top: seededRandom(i * 23) * 1080,
            opacity: 0.08 + seededRandom(i) * 0.12,
          }} />
        ))}
      </AbsoluteFill>

      {/* Left panel — notes display */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: `translateY(-50%)`,
      }}>
        {/* CANCELLED notes */}
        {[500, 1000].map((val, idx) => (
          <div key={idx} style={{
            marginBottom: 40,
            opacity: sp(15 + idx * 25),
            transform: `translateX(${(1 - sp(15 + idx * 25)) * -80}px)`,
          }}>
            <div style={{
              width: 380,
              height: 180,
              backgroundColor: '#2D2D2D',
              border: '3px solid #9E9E9E',
              borderRadius: 16,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
              overflow: 'hidden',
            }}>
              {/* Note texture lines */}
              {[...Array(6)].map((_, i) => (
                <div key={i} style={{
                  position: 'absolute',
                  width: '100%',
                  height: 1,
                  backgroundColor: '#9E9E9E',
                  top: 20 + i * 28,
                  opacity: 0.1,
                }} />
              ))}
              <span style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 64,
                fontWeight: 900,
                color: '#9E9E9E',
                opacity: 0.5,
              }}>₹{val}</span>

              {/* Big red X stamp */}
              {frame > (30 + idx * 20) && (
                <div style={{
                  position: 'absolute',
                  top: 0, left: 0, right: 0, bottom: 0,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  transform: `scale(${sp(30 + idx * 20, { damping: 6, stiffness: 300 })})`,
                }}>
                  <svg width="200" height="160" viewBox="0 0 200 160">
                    <line x1="20" y1="20" x2="180" y2="140" stroke="#ff0000" strokeWidth="18" strokeLinecap="round" />
                    <line x1="180" y1="20" x2="20" y2="140" stroke="#ff0000" strokeWidth="18" strokeLinecap="round" />
                  </svg>
                </div>
              )}

              {/* CANCELLED stamp text */}
              {frame > 50 && (
                <div style={{
                  position: 'absolute',
                  bottom: 10,
                  right: 12,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 18,
                  fontWeight: 900,
                  color: '#ff0000',
                  letterSpacing: 3,
                  border: '2px solid #ff0000',
                  padding: '2px 8px',
                  borderRadius: 4,
                  opacity: 0.9,
                }}>CANCELLED</div>
              )}
            </div>
          </div>
        ))}
      </div>

      {/* Queue of stick figures — center right */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: sp(20),
      }}>
        {/* Bank building */}
        <svg width="120" height="140" viewBox="0 0 120 140" style={{ marginLeft: 280 }}>
          <rect x="10" y="40" width="100" height="90" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
          <rect x="0" y="30" width="120" height="20" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
          {/* Columns */}
          {[20, 50, 80].map(x => (
            <rect key={x} x={x} y="50" width="10" height="70" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="1" />
          ))}
          {/* Door */}
          <rect x="45" y="100" width="30" height="30" fill="#0A0A0A" stroke="#FFC107" strokeWidth="1.5" />
          {/* Bank label */}
          <text x="60" y="25" textAnchor="middle" fill="#FFC107" fontSize="9" fontFamily="JetBrains Mono" fontWeight="bold">BANK</text>
        </svg>

        {/* Queue line */}
        <div style={{ display: 'flex', alignItems: 'flex-end', marginTop: 10 }}>
          {[...Array(7)].map((_, i) => {
            const figureOffset = Math.sin(frame * 0.04 + i * 0.8) * 3;
            return (
              <div key={i} style={{
                marginRight: i === 0 ? 260 : 20,
                transform: `translateY(${figureOffset}px)`,
                opacity: interpolate(frame, [20 + i * 8, 35 + i * 8], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}>
                <svg width="40" height="90" viewBox="0 0 40 90">
                  <circle cx="20" cy="12" r="10" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
                  {/* Worried face */}
                  <path d="M 14 16 Q 20 12 26 16" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
                  <line x1="20" y1="22" x2="20" y2="55" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
                  <line x1="20" y1="38" x2="8" y2="50" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
                  <line x1="20" y1="38" x2="32" y2="50" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
                  <line x1="20" y1="55" x2="10" y2="85" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
                  <line x1="20" y1="55" x2="30" y2="85" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
                </svg>
              </div>
            );
          })}
        </div>

        {/* Queue arrow */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 12,
          marginTop: 16,
          opacity: sp(70),
        }}>
          <svg width="600" height="30" viewBox="0 0 600 30">
            <line x1="10" y1="15" x2="580" y2="15" stroke="#FFC107" strokeWidth="2" strokeDasharray="12 8" />
            <polygon points="580,8 600,15 580,22" fill="#FFC107" />
          </svg>
        </div>
      </div>

      {/* Center headline */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(10)})`,
        opacity: sp(10),
        textAlign: 'center',
        whiteSpace: 'nowrap',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 90,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: -1,
        }}>घर की नगदी — बेकार</div>
      </div>

      {/* Bottom counter */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: sp(55),
        display: 'flex',
        gap: 40,
        alignItems: 'center',
      }}>
        {[
          { label: 'ATM लाइन', val: '5+ घंटे', color: '#ff0000' },
          { label: 'नगदी सीमा', val: '₹2,000/दिन', color: '#FFC107' },
          { label: 'असर', val: '500 करोड़ लोग', color: '#9E9E9E' },
        ].map((item, i) => (
          <div key={i} style={{
            backgroundColor: '#2D2D2D',
            border: `2px solid ${item.color}`,
            borderRadius: 12,
            padding: '14px 28px',
            textAlign: 'center',
            opacity: interpolate(frame, [55 + i * 12, 70 + i * 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `translateY(${interpolate(frame, [55 + i * 12, 70 + i * 12], [20, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
          }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 900, color: item.color }}>{item.val}</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 22, color: '#9E9E9E', marginTop: 4 }}>{item.label}</div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene2_CashCrisis;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_RulesChange',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_RulesChange: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Day/Night flip — the "overnight" transformation
  const flipProgress = interpolate(frame, [30, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Rotating gavel
  const gavelAngle = interpolate(frame, [50, 75], [-40, 30], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(2)),
  });

  // Screen shake on gavel strike
  const shake = frame >= 74 && frame <= 82 ? Math.sin(frame * 8) * 6 : 0;

  // Grid lines moving
  const gridOffset = (frame * 0.5) % 60;

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${shake}px, ${shake * 0.5}px)`,
    }}>

      {/* Split gradient — before/after */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: `${(1 - flipProgress) * 50 + 50 * flipProgress}%`,
          height: '100%',
          background: 'linear-gradient(180deg, #1A2A1A 0%, #1C1C1C 100%)',
          transition: 'none',
        }} />
        <div style={{
          position: 'absolute',
          right: 0,
          top: 0,
          width: `${flipProgress * 50 + (1 - flipProgress) * 0}%`,
          height: '100%',
          background: 'linear-gradient(180deg, #2A0000 0%, #1C1C1C 100%)',
          transition: 'none',
        }} />
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {[...Array(18)].map((_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 60 + gridOffset} x2="1920" y2={i * 60 + gridOffset} stroke="#F5F5F5" strokeWidth="1" />
          ))}
          {[...Array(33)].map((_, i) => (
            <line key={`v${i}`} x1={i * 60 + gridOffset} y1="0" x2={i * 60 + gridOffset} y2="1080" stroke="#F5F5F5" strokeWidth="1" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(15)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: i < 8 ? '#138808' : '#ff0000',
            left: seededRandom(i * 19) * 1920,
            top: seededRandom(i * 31) * 1080,
            opacity: 0.12 * (i < 8 ? (1 - flipProgress) : flipProgress),
          }} />
        ))}
      </AbsoluteFill>

      {/* BEFORE label — left */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 80,
        opacity: interpolate(frame, [5, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          backgroundColor: '#138808',
          padding: '8px 24px',
          borderRadius: 8,
          display: 'inline-block',
        }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#F5F5F5' }}>✓ कल तक — सुरक्षित</span>
        </div>
      </div>

      {/* AFTER label — right */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 80,
        opacity: flipProgress,
      }}>
        <div style={{
          backgroundColor: '#ff0000',
          padding: '8px 24px',
          borderRadius: 8,
          display: 'inline-block',
        }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#F5F5F5' }}>✗ आज — असुरक्षित</span>
        </div>
      </div>

      {/* Center divider line */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 0,
        bottom: 0,
        width: 4,
        backgroundColor: '#FFC107',
        opacity: 0.4,
        transform: 'translateX(-50%)',
      }} />

      {/* Giant gavel — center */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -60%) rotate(${gavelAngle}deg)`,
        transformOrigin: '80% 80%',
        opacity: sp(25),
      }}>
        <svg width="200" height="280" viewBox="0 0 200 280">
          {/* Gavel head */}
          <rect x="20" y="10" width="160" height="80" rx="14" fill="#2D2D2D" stroke="#FFC107" strokeWidth="4" />
          <rect x="40" y="10" width="120" height="80" rx="8" fill="#FFC107" opacity="0.15" />
          {/* Strike lines */}
          {frame >= 74 && [...Array(5)].map((_, i) => (
            <line key={i}
              x1={100 + i * 20 - 40} y1="90"
              x2={120 + i * 20 - 40} y2="140"
              stroke="#FFC107" strokeWidth="3"
              opacity={interpolate(frame, [74, 85], [0.8, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            />
          ))}
          {/* Handle */}
          <rect x="88" y="90" width="24" height="180" rx="8" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3" />
        </svg>
      </div>

      {/* Left panel — "before" assets */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: 1 - flipProgress * 0.3,
      }}>
        {[
          { icon: '🏦', label: 'बैंक बचत', status: 'सुरक्षित', color: '#138808' },
          { icon: '💰', label: 'नगद पैसा', status: 'सुरक्षित', color: '#138808' },
          { icon: '🏠', label: 'जमीन', status: 'सुरक्षित', color: '#138808' },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            marginBottom: 28,
            opacity: interpolate(frame, [10 + i * 12, 25 + i * 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `translateX(${interpolate(frame, [10 + i * 12, 25 + i * 12], [-40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
          }}>
            <div style={{
              width: 280,
              backgroundColor: '#2D2D2D',
              border: `2px solid ${flipProgress > 0.5 ? '#ff0000' : item.color}`,
              borderRadius: 12,
              padding: '16px 24px',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
            }}>
              <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#F5F5F5' }}>{item.label}</span>
              <div style={{
                padding: '4px 14px',
                borderRadius: 6,
                backgroundColor: flipProgress > 0.5 ? '#3D0000' : '#0D1F0D',
                border: `1px solid ${flipProgress > 0.5 ? '#ff0000' : item.color}`,
              }}>
                <span style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  fontWeight: 700,
                  color: flipProgress > 0.5 ? '#ff0000' : item.color,
                }}>{flipProgress > 0.5 ? 'FROZEN?' : '✓ ' + item.status}</span>
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* Main text — bottom center */}
      <div style={{
        position: 'absolute',
        bottom: 130,
        left: '50%',
        transform: `translateX(-50%)`,
        textAlign: 'center',
        width: 1400,
        opacity: sp(80),
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 72,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.2,
        }}>
          <span style={{ color: '#FFC107' }}>एक रात</span> में बदल सकते हैं <span style={{ color: '#ff0000' }}>सारे नियम</span>
        </div>
      </div>

      {/* Stick figure */}
      <div style={{
        position: 'absolute',
        right: 160,
        bottom: 90,
        opacity: sp(40),
        transform: `translateY(${(1 - sp(40)) * 80}px)`,
      }}>
        <div style={{
          position: 'absolute',
          width: 80,
          height: 80,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -5, top: -5,
        }} />
        <svg width="70" height="140" viewBox="0 0 70 140">
          <circle cx="35" cy="18" r="16" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 4}, 35, 18)`} />
          <circle cx="27" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="43" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="33" y1="17" x2="37" y2="17" stroke="#FFC107" strokeWidth="1.5" />
          {/* Confused expression */}
          <path d="M 28 24 Q 35 20 42 24" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="35" y1="34" x2="35" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="35" y1="52" x2="15" y2="65" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="52" x2="55" y2="65" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="80" x2="20" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="35" y1="80" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Question mark bubble */}
          <text x="55" y="20" fill="#ff0000" fontSize="24" fontFamily="JetBrains Mono" fontWeight="bold">?</text>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 30%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene3_RulesChange;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_CyprusCrisis',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_CyprusCrisis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money drain animation
  const drainProgress = interpolate(frame, [40, 100], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });

  // Pulsing numbers
  const counterVal = Math.floor(interpolate(frame, [30, 90], [100000, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  }));

  // Orbiting circles (background shape layer)
  const orbitAngle = frame * 0.4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0A0A14', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep navy gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #0A0A20 0%, #0A0A14 40%, #1A0010 100%)',
      }} />

      {/* Orbiting large geometric shapes */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        {[0, 1, 2].map(i => (
          <div key={i} style={{
            position: 'absolute',
            width: 500 + i * 200,
            height: 500 + i * 200,
            borderRadius: '50%',
            border: '1px solid #e9d700',
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) rotate(${orbitAngle + i * 60}deg)`,
          }}>
            <div style={{
              position: 'absolute',
              width: 20,
              height: 20,
              borderRadius: '50%',
              backgroundColor: '#e9d700',
              top: -10,
              left: '50%',
              transform: 'translateX(-50%)',
            }} />
          </div>
        ))}
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {[...Array(25)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 2 + seededRandom(i) * 3,
            height: 2 + seededRandom(i) * 3,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#ff0000',
            left: seededRandom(i * 13) * 1920,
            top: seededRandom(i * 7) * 1080,
            opacity: 0.06 + seededRandom(i * 3) * 0.1,
          }} />
        ))}
      </AbsoluteFill>

      {/* Country flag + name — top */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(5)})`,
        opacity: sp(5),
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 12,
      }}>
        {/* Cyprus-style flag colors */}
        <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
          <div style={{ width: 6, height: 50, backgroundColor: '#FF9933', borderRadius: 3 }} />
          <div style={{ width: 6, height: 50, backgroundColor: '#F5F5F5', borderRadius: 3 }} />
          <div style={{ width: 6, height: 50, backgroundColor: '#0038A8', borderRadius: 3 }} />
          <div style={{ width: 6, height: 50, backgroundColor: '#F5F5F5', borderRadius: 3 }} />
          <div style={{ width: 6, height: 50, backgroundColor: '#FF9933', borderRadius: 3 }} />
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 36,
          fontWeight: 700,
          color: '#9E9E9E',
          letterSpacing: 6,
        }}>CYPRUS — 2013</div>
      </div>

      {/* Central Bank Vault visual */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: '50%',
        transform: `translateY(-50%) scale(${sp(15)})`,
        opacity: sp(15),
      }}>
        <svg width="320" height="340" viewBox="0 0 320 340">
          {/* Vault door */}
          <rect x="20" y="20" width="280" height="300" rx="20"
            fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="6" />
          <rect x="40" y="40" width="240" height="260" rx="12"
            fill="#1C1C1C" stroke="#FFC107" strokeWidth="3" />

          {/* Vault dial */}
          <circle cx="160" cy="170" r="80" fill="none" stroke="#9E9E9E" strokeWidth="4" />
          <circle cx="160" cy="170" r="60" fill="#2D2D2D" stroke="#FFC107" strokeWidth="3" />
          {[0, 45, 90, 135, 180, 225, 270, 315].map((angle, i) => {
            const rad = angle * Math.PI / 180;
            return (
              <line key={i}
                x1={160 + 55 * Math.sin(rad)} y1={170 - 55 * Math.cos(rad)}
                x2={160 + 70 * Math.sin(rad)} y2={170 - 70 * Math.cos(rad)}
                stroke="#9E9E9E" strokeWidth="3"
              />
            );
          })}
          {/* Spin indicator */}
          <line x1="160" y1="170"
            x2={160 + 48 * Math.sin(frame * 0.15)}
            y2={170 - 48 * Math.cos(frame * 0.15)}
            stroke="#ff0000" strokeWidth="4" strokeLinecap="round"
          />

          {/* Money drain effect — level dropping */}
          <rect x="55" y={55 + drainProgress * 210}
            width="210" height={Math.max(0, 210 - drainProgress * 210)}
            rx="4" fill="#e9d700" opacity="0.25"
          />

          {/* € symbol */}
          <text x="160" y="180" textAnchor="middle"
            fill="#FFC107" fontSize="36" fontFamily="JetBrains Mono" fontWeight="bold">€</text>

          {/* Vault handle */}
          <circle cx="160" cy="280" r="20" fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3" />
          <line x1="145" y1="280" x2="175" y2="280" stroke="#9E9E9E" strokeWidth="3" />
        </svg>

        {/* SEIZED stamp */}
        {frame > 60 && (
          <div style={{
            position: 'absolute',
            top: 120,
            left: -20,
            transform: `rotate(-15deg) scale(${sp(60, { damping: 5, stiffness: 200 })})`,
            backgroundColor: '#ff0000',
            padding: '12px 30px',
            borderRadius: 8,
            border: '4px solid #8B0000',
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 36,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 4,
            }}>SEIZED</span>
          </div>
        )}
      </div>

      {/* Right — stats breakdown */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 900,
      }}>
        {/* Counter */}
        <div style={{
          marginBottom: 40,
          opacity: sp(20),
          transform: `translateX(${(1 - sp(20)) * 60}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            color: '#9E9E9E',
            marginBottom: 8,
          }}>बैंक में जमा था</div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 110,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
          }}>€{counterVal.toLocaleString()}</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 34,
            color: '#9E9E9E',
          }}>से ज़्यादा की जमा पर टैक्स</div>
        </div>

        {/* What happened breakdown */}
        {[
          { label: 'एक रात में फैसला', value: '✓', color: '#ff0000', delay: 35 },
          { label: 'जीवन भर की कमाई', value: 'सरकार ने ली', color: '#ff0000', delay: 50 },
          { label: 'कोर्ट का फैसला', value: 'राष्ट्रीय संकट', color: '#FFC107', delay: 65 },
          { label: 'कानूनी तरीके से', value: '100%', color: '#ff0000', delay: 80 },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            marginBottom: 20,
            backgroundColor: '#2D2D2D',
            border: `1px solid ${item.color}33`,
            borderLeft: `4px solid ${item.color}`,
            borderRadius: 10,
            padding: '16px 24px',
            opacity: sp(item.delay),
            transform: `translateX(${(1 - sp(item.delay)) * 50}px)`,
          }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#F5F5F5' }}>
              {item.label}
            </span>
            <span style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: item.color,
            }}>{item.value}</span>
          </div>
        ))}
      </div>

      {/* Stick figure — hands up surrendering */}
      <div style={{
        position: 'absolute',
        left: 460,
        bottom: 60,
        opacity: sp(50),
        transform: `scale(${sp(50)})`,
      }}>
        <div style={{
          position: 'absolute',
          width: 100,
          height: 100,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -10, top: -10,
        }} />
        <svg width="80" height="160" viewBox="0 0 80 160">
          <circle cx="40" cy="18" r="16" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 40, 18)`} />
          <circle cx="32" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="48" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="38" y1="17" x2="42" y2="17" stroke="#FFC107" strokeWidth="1.5" />
          {/* Sad face */}
          <path d="M 32 25 Q 40 22 48 25" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="40" y1="34" x2="40" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Hands up — surrendering */}
          <line x1="40" y1="50" x2="8" y2="28" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="50" x2="72" y2="28" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="24" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="80" x2="56" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 35%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene4_CyprusCrisis;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_ArgentinaDollar',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_ArgentinaDollar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Currency swap animation
  const swapProgress = interpolate(frame, [50, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Peso falling chart
  const chartPoints = [...Array(20)].map((_, i) => ({
    x: 120 + i * 60,
    y: 200 + Math.pow(i / 19, 2) * 280,
  }));

  // Floating $ symbols
  const dollarFloat = Math.sin(frame * 0.05) * 8;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — Argentine colors hint */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0D1A2D 0%, #1C1C1C 50%, #1A0D00 100%)',
      }} />

      {/* Animated diagonal lines texture */}
      <AbsoluteFill style={{ opacity: 0.04, overflow: 'hidden' }}>
        {[...Array(25)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 2,
            height: 1400,
            backgroundColor: '#74ACDF',
            left: i * 80 + (frame * 0.3) % 80,
            top: -200,
            transform: 'rotate(20deg)',
            transformOrigin: 'top left',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(20)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#74ACDF' : '#F6B40E',
            left: seededRandom(i * 11) * 1920,
            top: seededRandom(i * 17) * 1080,
            opacity: 0.08 + seededRandom(i) * 0.1,
          }} />
        ))}
      </AbsoluteFill>

      {/* Top — Location + Year */}
      <div style={{
        position: 'absolute',
        top: 55,
        left: 80,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
        opacity: sp(8),
        transform: `translateY(${(1 - sp(8)) * -20}px)`,
      }}>
        {/* Argentine flag stripes */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 3 }}>
          {['#74ACDF', '#F5F5F5', '#74ACDF'].map((c, i) => (
            <div key={i} style={{ width: 70, height: 14, backgroundColor: c, borderRadius: 2 }} />
          ))}
        </div>
        <div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 700, color: '#74ACDF', letterSpacing: 4 }}>
            ARGENTINA
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 44, fontWeight: 900, color: '#ff0000', letterSpacing: 2 }}>
            2001 संकट
          </div>
        </div>
      </div>

      {/* Left — Currency Comparison Visual */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-48%)',
        width: 680,
      }}>
        {/* Dollar bill visual */}
        <div style={{
          opacity: sp(15),
          transform: `translateY(${(1 - sp(15)) * 40}px)`,
          marginBottom: 30,
        }}>
          <div style={{
            width: 600,
            height: 160,
            backgroundColor: '#1A2A1A',
            border: '3px solid #138808',
            borderRadius: 16,
            display: 'flex',
            alignItems: 'center',
            padding: '0 30px',
            gap: 24,
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* $ symbol large */}
            <div style={{
              fontSize: 90,
              fontFamily: 'JetBrains Mono',
              fontWeight: 900,
              color: '#138808',
              opacity: 0.3,
              position: 'absolute',
              right: 20,
              top: '50%',
              transform: `translateY(-50%) translateY(${dollarFloat}px)`,
            }}>$</div>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E' }}>लोगों की बचत</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900, color: '#138808' }}>US DOLLAR</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: '#9E9E9E' }}>"सुरक्षित है" — सबने कहा था</div>
            </div>
          </div>
        </div>

        {/* Peso (after conversion) */}
        <div style={{
          opacity: swapProgress,
          transform: `translateY(${(1 - swapProgress) * 30}px)`,
        }}>
          <div style={{
            width: 600,
            height: 160,
            backgroundColor: '#2A0000',
            border: '3px solid #ff0000',
            borderRadius: 16,
            display: 'flex',
            alignItems: 'center',
            padding: '0 30px',
            gap: 24,
            position: 'relative',
            overflow: 'hidden',
          }}>
            <div style={{
              fontSize: 90,
              fontFamily: 'JetBrains Mono',
              fontWeight: 900,
              color: '#ff0000',
              opacity: 0.2,
              position: 'absolute',
              right: 20,
              top: '50%',
              transform: `translateY(-50%)`,
            }}>₱</div>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: '#9E9E9E' }}>बदल दिया गया</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900, color: '#ff0000' }}>PESO</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, color: '#ff0000' }}>सरकारी रेट पर — बाजार से कम</div>
            </div>
          </div>
        </div>

        {/* Swap arrow */}
        <div style={{
          position: 'absolute',
          left: 620,
          top: '50%',
          transform: 'translateY(-50%)',
          opacity: sp(45),
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80">
            <path d="M 10 35 L 60 35 L 45 20" fill="none" stroke="#FFC107" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
            <path d="M 70 45 L 20 45 L 35 60" fill="none" stroke="#ff0000" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>
      </div>

      {/* Right — Peso collapse chart */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: sp(20),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 38,
          color: '#9E9E9E',
          marginBottom: 16,
          textAlign: 'center',
        }}>पेसो का पतन</div>

        <svg width="700" height="420" viewBox="0 0 1400 420">
          {/* Grid */}
          {[0, 1, 2, 3, 4].map(i => (
            <line key={i} x1="120" y1={80 + i * 60} x2="1320" y2={80 + i * 60}
              stroke="#9E9E9E" strokeWidth="1" opacity="0.15" strokeDasharray="8 8" />
          ))}

          {/* Y axis labels */}
          {['1:1', '1:2', '1:3', '1:4', '1:5'].map((label, i) => (
            <text key={i} x="100" y={88 + i * 60} textAnchor="end"
              fill="#9E9E9E" fontSize="22" fontFamily="JetBrains Mono">{label}</text>
          ))}

          {/* Crash line */}
          <polyline
            points={chartPoints.map(p => `${p.x * 2},${p.y}`).join(' ')}
            fill="none"
            stroke="#ff0000"
            strokeWidth="5"
            strokeLinecap="round"
            strokeDasharray={`${interpolate(frame, [20, 100], [0, 2400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} 3000`}
          />

          {/* Area fill */}
          <path
            d={`M ${chartPoints[0].x * 2},${chartPoints[0].y} ${chartPoints.map(p => `L ${p.x * 2},${p.y}`).join(' ')} L ${chartPoints[chartPoints.length - 1].x * 2},480 L ${chartPoints[0].x * 2},480 Z`}
            fill="url(#redGrad)"
            opacity={interpolate(frame, [20, 100], [0, 0.25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />

          <defs>
            <linearGradient id="redGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#ff0000" stopOpacity="0.8" />
              <stop offset="100%" stopColor="#ff0000" stopOpacity="0" />
            </linearGradient>
          </defs>

          {/* Bottom labels */}
          <text x="240" y="420" fill="#9E9E9E" fontSize="22" fontFamily="JetBrains Mono">1999</text>
          <text x="700" y="420" fill="#9E9E9E" fontSize="22" fontFamily="JetBrains Mono">2001</text>
          <text x="1180" y="420" fill="#ff0000" fontSize="22" fontFamily="JetBrains Mono" fontWeight="bold">संकट</text>

          {/* Crash label */}
          {frame > 70 && (
            <text x="1100" y="380"
              fill="#ff0000" fontSize="28" fontFamily="JetBrains Mono" fontWeight="bold"
              opacity={interpolate(frame, [70, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}>
              -67%
            </text>
          )}
        </svg>
      </div>

      {/* Bottom message */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: sp(90),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 66,
          fontWeight: 900,
          color: '#F5F5F5',
        }}>
          "सुरक्षित" डॉलर — <span style={{ color: '#ff0000' }}>पेसो में बदल दिया</span>
        </div>
      </div>

      {/* Stick figure */}
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: 60,
        opacity: sp(55),
        transform: `scale(${sp(55)})`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="75" height="150" viewBox="0 0 75 150">
          <circle cx="37" cy="17" r="15" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.05) * 5}, 37, 17)`} />
          <circle cx="29" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="45" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="35" y1="16" x2="39" y2="16" stroke="#FFC107" strokeWidth="1.5" />
          <path d="M 30 24 Q 37 21 44 24" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="37" y1="32" x2="37" y2="78" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="37" y1="50" x2="15" y2="62" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="50" x2="59" y2="62" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="22" y2="128" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="52" y2="128" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Dollar sign held */}
          <text x="58" y="60" fill="#138808" fontSize="22" fontFamily="JetBrains Mono" fontWeight="bold"
            opacity={1 - swapProgress}>$</text>
          <text x="58" y="60" fill="#ff0000" fontSize="22" fontFamily="JetBrains Mono" fontWeight="bold"
            opacity={swapProgress}>₱</text>
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 30%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene5_ArgentinaDollar;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_Corralito',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_Corralito: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ice / freeze effect — spreading from center
  const freezeProgress = interpolate(frame, [35, 85], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });

  // $250 limit counter
  const limitHighlight = Math.floor(frame / 8) % 2 === 0;

  // Corralito cage bars dropping
  const barsDown = interpolate(frame, [20, 60], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Iceberg breaking particles
  const iceShards = [...Array(12)].map((_, i) => ({
    x: 800 + seededRandom(i * 7) * 300 - 150,
    y: 400 + seededRandom(i * 11) * 200 - 100,
    angle: seededRandom(i * 3) * 360,
    drift: seededRandom(i * 5),
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#020814', overflow: 'hidden', opacity: exitOp }}>

      {/* Ice blue gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #020C1B 0%, #030F24 40%, #020814 100%)',
      }} />

      {/* Freeze spread overlay */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% 50%, rgba(120,200,255,${freezeProgress * 0.12}) 0%, transparent ${(1 - freezeProgress) * 100 + 30}%)`,
      }} />

      {/* Hexagonal ice texture shapes */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(8)].map((_, i) => {
          const cx = seededRandom(i * 7) * 1920;
          const cy = seededRandom(i * 13) * 1080;
          const size = 80 + seededRandom(i * 5) * 120;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: cx - size / 2,
              top: cy - size / 2,
              width: size,
              height: size,
              border: '1px solid #78C8FF',
              transform: `rotate(${30 + i * 15 + frame * 0.02}deg)`,
              opacity: freezeProgress * 0.8,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles — ice crystals */}
      <AbsoluteFill>
        {[...Array(20)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#78C8FF',
            left: seededRandom(i * 13) * 1920,
            top: seededRandom(i * 7) * 1080 + Math.sin(frame * 0.02 + i) * 15,
            opacity: 0.06 + seededRandom(i) * 0.1,
            boxShadow: '0 0 4px #78C8FF',
          }} />
        ))}
      </AbsoluteFill>

      {/* TOP — CORRALITO word */}
      <div style={{
        position: 'absolute',
        top: 50,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(8)})`,
        opacity: sp(8),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 52,
          fontWeight: 900,
          color: '#78C8FF',
          letterSpacing: 8,
        }}>CORRALITO</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 30,
          color: '#9E9E9E',
          marginTop: 4,
        }}>अर्थ: छोटा बाड़ा — Argentina 2001</div>
      </div>

      {/* Left — Cage / Prison bars visual */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-52%)',
        opacity: sp(12),
      }}>
        <svg width="440" height="480" viewBox="0 0 440 480">
          {/* Bank/vault background */}
          <rect x="20" y="20" width="400" height="440" rx="8"
            fill="#0A1020" stroke="#9E9E9E" strokeWidth="2" />

          {/* Money stack inside — frozen */}
          {[0, 1, 2, 3].map(i => (
            <rect key={i} x="60" y={80 + i * 70} width="320" height="50" rx="6"
              fill="#1A2A1A" stroke="#138808" strokeWidth="1.5" opacity={0.6 - i * 0.1}
            >
              <text />
            </rect>
          ))}
          {[0, 1, 2, 3].map(i => (
            <text key={i} x="220" y={112 + i * 70} textAnchor="middle"
              fill="#138808" fontSize="22" fontFamily="JetBrains Mono" opacity={0.7 - i * 0.1}>
              $ $ $ $ $ $ $
            </text>
          ))}

          {/* FROZEN overlay */}
          <rect x="20" y="20" width="400" height="440" rx="8"
            fill="#78C8FF"
            opacity={freezeProgress * 0.18}
          />

          {/* Cage bars dropping from top */}
          {[...Array(8)].map((_, i) => (
            <rect key={i}
              x={40 + i * 50}
              y={20 - (1 - barsDown) * 500}
              width="14"
              height="440"
              rx="6"
              fill="#2D2D2D"
              stroke="#9E9E9E"
              strokeWidth="1"
              opacity={0.85}
            />
          ))}

          {/* FROZEN text stamp */}
          {frame > 60 && (
            <text x="220" y="260" textAnchor="middle"
              fill="#78C8FF" fontSize="52" fontFamily="JetBrains Mono" fontWeight="900"
              opacity={interpolate(frame, [60, 80], [0, 0.8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
              transform="rotate(-20, 220, 260)">
              FROZEN
            </text>
          )}

          {/* Padlock */}
          {frame > 55 && (
            <g transform={`translate(170, 380) scale(${sp(55, { damping: 6, stiffness: 200 })})`}>
              <rect x="0" y="16" width="80" height="60" rx="8" fill="#2D2D2D" stroke="#ff0000" strokeWidth="3" />
              <path d="M 15 16 C 15 -10 65 -10 65 16" fill="none" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
              <circle cx="40" cy="46" r="8" fill="#ff0000" />
            </g>
          )}
        </svg>
      </div>

      {/* Right — $250 limit info */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 900,
      }}>
        {/* Limit callout */}
        <div style={{
          marginBottom: 40,
          opacity: sp(20),
          transform: `translateY(${(1 - sp(20)) * 40}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#9E9E9E',
            marginBottom: 12,
          }}>हफ्ते में निकाल सकते थे सिर्फ</div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 160,
            fontWeight: 900,
            color: limitHighlight ? '#FFC107' : '#e9d700',
            lineHeight: 0.9,
            textShadow: `0 0 40px ${limitHighlight ? '#FFC10760' : 'transparent'}`,
          }}>$250</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#9E9E9E',
            marginTop: 8,
          }}>प्रति हफ्ता — per account</div>
        </div>

        {/* Restriction breakdown */}
        {[
          { label: 'बाकि बचत', status: 'FROZEN ❄️', color: '#78C8FF', delay: 40 },
          { label: 'विदेश भेजना', status: 'BLOCKED', color: '#ff0000', delay: 55 },
          { label: 'अदालत', status: 'राष्ट्रीय संकट = कुछ नहीं', color: '#9E9E9E', delay: 70 },
          { label: 'लोग', status: 'सड़क पर रोए', color: '#ff0000', delay: 85 },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            marginBottom: 18,
            padding: '14px 24px',
            backgroundColor: '#2D2D2D',
            border: `1px solid ${item.color}40`,
            borderLeft: `4px solid ${item.color}`,
            borderRadius: 10,
            opacity: sp(item.delay),
            transform: `translateX(${(1 - sp(item.delay)) * 50}px)`,
          }}>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#F5F5F5' }}>{item.label}</span>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 24, fontWeight: 700, color: item.color }}>{item.status}</span>
          </div>
        ))}
      </div>

      {/* Stick figure — frozen/shivering */}
      <div style={{
        position: 'absolute',
        left: 550,
        bottom: 60,
        opacity: sp(50),
        transform: `scale(${sp(50)}) translateX(${Math.sin(frame * 0.6) * (freezeProgress * 4)}px)`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(120,200,255,0.15) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="75" height="150" viewBox="0 0 75 150">
          <circle cx="37" cy="17" r="15" fill="none" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="3" />
          <circle cx="29" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="45" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="35" y1="16" x2="39" y2="16" stroke="#FFC107" strokeWidth="1.5" />
          {/* Chattering teeth */}
          <path d="M 30 24 Q 37 27 44 24" fill="none" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="1.5" />
          <line x1="37" y1="32" x2="37" y2="78" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="3" strokeLinecap="round" />
          <line x1="37" y1="50" x2="15" y2="62" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="50" x2="59" y2="62" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="22" y2="128" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="52" y2="128" stroke={freezeProgress > 0.5 ? '#78C8FF' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
          {/* Ice crystals around */}
          {freezeProgress > 0.4 && [...Array(6)].map((_, i) => (
            <line key={i}
              x1={37 + 25 * Math.cos(i * 60 * Math.PI / 180)}
              y1={70 + 25 * Math.sin(i * 60 * Math.PI / 180)}
              x2={37 + 38 * Math.cos(i * 60 * Math.PI / 180)}
              y2={70 + 38 * Math.sin(i * 60 * Math.PI / 180)}
              stroke="#78C8FF" strokeWidth="2" opacity={freezeProgress}
            />
          ))}
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 30%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene6_Corralito;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_WealthHalved',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_WealthHalved: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Wealth bar shrinking
  const barWidth = interpolate(frame, [30, 100], [100, 42], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Chop animation — guillotine style
  const chopY = interpolate(frame, [50, 75], [-200, 200], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.in(Easing.cubic),
  });

  // Slash impact
  const slashOpacity = interpolate(frame, [73, 85], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on chop
  const shake = frame >= 73 && frame <= 80 ? Math.sin(frame * 10) * 8 : 0;

  // Coins falling after chop
  const coinsFall = interpolate(frame, [75, durationInFrames], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rotating background triangles
  const triAngle = frame * 0.3;

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${shake}px, 0px)`,
    }}>

      {/* Dark crimson gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(145deg, #2A0000 0%, #1C1C1C 45%, #0D0D0D 100%)',
      }} />

      {/* Rotating triangle shapes */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        {[0, 1, 2, 3].map(i => (
          <div key={i} style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) rotate(${triAngle + i * 45}deg)`,
          }}>
            <svg width={400 + i * 150} height={400 + i * 150} viewBox="0 0 400 400">
              <polygon points="200,20 380,380 20,380" fill="none" stroke="#ff0000" strokeWidth="2" />
            </svg>
          </div>
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(16)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            left: seededRandom(i * 17) * 1920,
            top: seededRandom(i * 11) * 1080,
            opacity: 0.07 + seededRandom(i) * 0.08,
          }} />
        ))}
      </AbsoluteFill>

      {/* Main wealth comparison — center */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: 'translate(-50%, -50%)',
        width: 1600,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 40,
      }}>

        {/* Title */}
        <div style={{
          opacity: sp(8),
          transform: `translateY(${(1 - sp(8)) * -30}px)`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
          }}>संपत्ति — <span style={{ color: '#ff0000' }}>आधी से कम</span></div>
        </div>

        {/* Wealth bar comparison */}
        <div style={{
          width: '100%',
          opacity: sp(15),
        }}>
          {/* BEFORE bar */}
          <div style={{
            marginBottom: 28,
            opacity: sp(18),
            transform: `translateX(${(1 - sp(18)) * -60}px)`,
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 24, marginBottom: 10 }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#9E9E9E', width: 160 }}>BEFORE</span>
              <div style={{
                width: 1000,
                height: 70,
                backgroundColor: '#138808',
                borderRadius: 8,
                position: 'relative',
                overflow: 'hidden',
              }}>
                {[...Array(8)].map((_, i) => (
                  <div key={i} style={{
                    position: 'absolute',
                    left: 20 + i * 120,
                    top: '50%',
                    transform: 'translateY(-50%)',
                    fontFamily: 'JetBrains Mono',
                    fontSize: 24,
                    color: '#F5F5F5',
                    opacity: 0.5,
                  }}>$</div>
                ))}
              </div>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: '#138808' }}>100%</span>
            </div>
          </div>

          {/* Scissor/chop element */}
          <div style={{
            position: 'relative',
            height: 60,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}>
            <div style={{
              transform: `translateY(${chopY}px)`,
              opacity: interpolate(frame, [35, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}>
              <svg width="120" height="60" viewBox="0 0 120 60">
                {/* Guillotine blade */}
                <rect x="20" y="0" width="80" height="30" rx="4"
                  fill="#ff0000" />
                <polygon points="20,30 100,30 60,55" fill="#ff0000" />
                <rect x="54" y="30" width="12" height="20" fill="#8B0000" />
              </svg>
            </div>

            {/* Slash line on impact */}
            {frame >= 73 && (
              <div style={{
                position: 'absolute',
                width: 1200,
                height: 6,
                backgroundColor: '#ff0000',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                opacity: slashOpacity,
                boxShadow: '0 0 20px #ff0000',
              }} />
            )}
          </div>

          {/* AFTER bar — shrinking */}
          <div style={{
            opacity: sp(25),
            transform: `translateX(${(1 - sp(25)) * -60}px)`,
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#9E9E9E', width: 160 }}>AFTER</span>
              <div style={{ position: 'relative', width: 1000, height: 70 }}>
                {/* Full width ghost */}
                <div style={{
                  position: 'absolute',
                  width: 1000,
                  height: 70,
                  borderRadius: 8,
                  border: '2px dashed #ff000040',
                }} />
                {/* Shrinking real bar */}
                <div style={{
                  width: `${barWidth}%`,
                  height: 70,
                  backgroundColor: '#ff0000',
                  borderRadius: 8,
                  position: 'relative',
                  overflow: 'hidden',
                  transition: 'none',
                }}>
                  {[...Array(4)].map((_, i) => (
                    <div key={i} style={{
                      position: 'absolute',
                      left: 20 + i * 80,
                      top: '50%',
                      transform: 'translateY(-50%)',
                      fontFamily: 'JetBrains Mono',
                      fontSize: 24,
                      color: '#F5F5F5',
                      opacity: 0.5,
                    }}>₱</div>
                  ))}
                </div>
              </div>
              <span style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 36,
                fontWeight: 900,
                color: '#ff0000',
              }}>{Math.round(barWidth)}%</span>
            </div>
          </div>
        </div>

        {/* Rate comparison callout */}
        <div style={{
          display: 'flex',
          gap: 40,
          opacity: sp(90),
          transform: `translateY(${(1 - sp(90)) * 30}px)`,
        }}>
          {[
            { label: 'बाजार रेट', value: '$1 = ₱3.50', color: '#138808' },
            { label: 'सरकारी रेट', value: '$1 = ₱1.40', color: '#ff0000' },
            { label: 'नुकसान', value: '~60%', color: '#ff0000' },
          ].map((item, i) => (
            <div key={i} style={{
              backgroundColor: '#2D2D2D',
              border: `2px solid ${item.color}`,
              borderRadius: 12,
              padding: '16px 32px',
              textAlign: 'center',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, fontWeight: 900, color: item.color }}>{item.value}</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#9E9E9E', marginTop: 6 }}>{item.label}</div>
            </div>
          ))}
        </div>
      </div>

      {/* Falling coins after chop */}
      <AbsoluteFill style={{ overflow: 'hidden', opacity: coinsFall > 0 ? 0.6 : 0 }}>
        {[...Array(10)].map((_, i) => {
          const cx = 600 + i * 100;
          const cy = -50 + (frame - 75) * (4 + seededRandom(i) * 3);
          const rot = (frame - 75) * (20 + seededRandom(i * 3) * 40);
          return cy > -60 && cy < 1200 ? (
            <div key={i} style={{
              position: 'absolute',
              left: cx,
              top: cy,
              transform: `rotate(${rot}deg)`,
            }}>
              <svg width="40" height="40" viewBox="0 0 40 40">
                <circle cx="20" cy="20" r="18" fill="#2D2D2D" stroke="#FFC107" strokeWidth="3" />
                <text x="20" y="26" textAnchor="middle" fill="#FFC107" fontSize="16" fontFamily="JetBrains Mono" fontWeight="bold">₱</text>
              </svg>
            </div>
          ) : null;
        })}
      </AbsoluteFill>

      {/* Stick figure — head in hands */}
      <div style={{
        position: 'absolute',
        right: 120,
        bottom: 60,
        opacity: sp(50),
        transform: `scale(${sp(50)})`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="75" height="150" viewBox="0 0 75 150">
          {/* Bowing head in despair */}
          <circle cx="37" cy="22" r="16" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${30 + Math.sin(frame * 0.03) * 2}, 37, 22)`}
          />
          <circle cx="29" cy="21" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="45" cy="21" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="35" y1="21" x2="39" y2="21" stroke="#FFC107" strokeWidth="1.5" />
          {/* Body bent forward */}
          <line x1="37" y1="38" x2="37" y2="82" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms holding head */}
          <line x1="37" y1="54" x2="18" y2="38" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="54" x2="56" y2="38" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="82" x2="22" y2="132" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="82" x2="52" y2="132" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 25%, rgba(0,0,0,0.88) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene7_WealthHalved;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_CapitalControls',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_CapitalControls: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rupee fall chart
  const rupeeLevel = interpolate(frame, [20, 90], [40, 280], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });

  // Danger meter fill
  const dangerFill = interpolate(frame, [25, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  // Red alert blink
  const alertBlink = Math.floor(frame / 10) % 2 === 0;

  // Concentric radar circles
  const radarAngle = frame * 3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0A0A0A', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep warning gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 40%, #1A0500 0%, #0A0A0A 60%, #000A00 100%)',
      }} />

      {/* Radar / sonar rings — animated */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-start', paddingLeft: 80 }}>
        <div style={{
          position: 'relative',
          width: 500,
          height: 500,
          opacity: 0.12,
        }}>
          {[1, 2, 3, 4, 5].map(i => (
            <div key={i} style={{
              position: 'absolute',
              borderRadius: '50%',
              border: '1px solid #ff0000',
              width: i * 100,
              height: i * 100,
              left: '50%',
              top: '50%',
              transform: 'translate(-50%, -50%)',
            }} />
          ))}
          {/* Radar sweep */}
          <div style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 2,
            height: 250,
            background: 'linear-gradient(to bottom, rgba(255,0,0,0.8), transparent)',
            transformOrigin: 'top center',
            transform: `rotate(${radarAngle}deg)`,
          }} />
        </div>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(18)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#FFC107' : '#9E9E9E',
            left: seededRandom(i * 11) * 1920,
            top: seededRandom(i * 7) * 1080,
            opacity: 0.06 + seededRandom(i) * 0.08,
          }} />
        ))}
      </AbsoluteFill>

      {/* DANGER indicator — top left */}
      <div style={{
        position: 'absolute',
        top: 55,
        left: 80,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
        opacity: sp(5),
      }}>
        {/* Danger icon */}
        <div style={{
          backgroundColor: alertBlink ? '#ff0000' : '#8B0000',
          width: 60,
          height: 60,
          borderRadius: 8,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}>
          <svg width="36" height="36" viewBox="0 0 36 36">
            <polygon points="18,4 34,30 2,30" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <text x="18" y="27" textAnchor="middle" fill="#F5F5F5" fontSize="16" fontFamily="JetBrains Mono" fontWeight="bold">!</text>
          </svg>
        </div>
        <div style={{ fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 700, color: '#ff0000', letterSpacing: 3 }}>
          INDIA — WORST CASE SCENARIO
        </div>
      </div>

      {/* Left — Rupee falling chart */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-42%)',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 34,
          color: '#9E9E9E',
          marginBottom: 12,
        }}>रुपया — लगातार गिरावट</div>
        <svg width="620" height="320" viewBox="0 0 620 320">
          {/* Grid */}
          {[0, 1, 2, 3, 4].map(i => (
            <line key={i} x1="60" y1={20 + i * 60} x2="600" y2={20 + i * 60}
              stroke="#9E9E9E" strokeWidth="1" opacity="0.1" strokeDasharray="6 6" />
          ))}
          {/* Y labels */}
          {['₹65', '₹70', '₹75', '₹80', '₹85'].map((label, i) => (
            <text key={i} x="52" y={28 + i * 60} textAnchor="end"
              fill="#9E9E9E" fontSize="20" fontFamily="JetBrains Mono">{label}</text>
          ))}

          {/* Falling line */}
          <path
            d={`M 80,40 C 200,40 250,${rupeeLevel * 0.3 + 40} 350,${rupeeLevel * 0.5 + 40} S 550,${rupeeLevel} 600,${rupeeLevel}`}
            fill="none"
            stroke="#ff0000"
            strokeWidth="5"
            strokeLinecap="round"
            strokeDasharray={`${interpolate(frame, [20, 90], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} 800`}
          />

          {/* Fill */}
          <path
            d={`M 80,40 C 200,40 250,${rupeeLevel * 0.3 + 40} 350,${rupeeLevel * 0.5 + 40} S 550,${rupeeLevel} 600,${rupeeLevel} L 600,320 L 80,320 Z`}
            fill="#ff0000"
            opacity={interpolate(frame, [20, 90], [0, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />

          {/* Current dot */}
          <circle cx="600" cy={rupeeLevel} r="10" fill="#ff0000" opacity={0.5 + 0.5 * Math.sin(frame * 0.2)} />

          {/* Danger zone */}
          <rect x="60" y="200" width="540" height="100" fill="#ff0000" opacity="0.05" />
          <text x="330" y="260" textAnchor="middle" fill="#ff0000" fontSize="18" fontFamily="JetBrains Mono" opacity="0.4">
            DANGER ZONE
          </text>
        </svg>
      </div>

      {/* Right — What happens next */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 900,
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 80,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.1,
          marginBottom: 30,
          opacity: sp(15),
          transform: `translateX(${(1 - sp(15)) * 50}px)`,
        }}>
          कैपिटल <span style={{ color: '#ff0000' }}>कंट्रोल्स</span>
        </div>

        {/* Restriction list */}
        {[
          { label: 'विदेश पैसा भेजना', icon: '🚫', color: '#ff0000', delay: 30 },
          { label: 'बड़ी रकम निकालना', icon: '🚫', color: '#ff0000', delay: 45 },
          { label: 'विदेशी मुद्रा खरीदना', icon: '🚫', color: '#ff0000', delay: 60 },
          { label: 'सोना export करना', icon: '⚠️', color: '#FFC107', delay: 75 },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            marginBottom: 20,
            backgroundColor: '#2D2D2D',
            border: `1px solid ${item.color}40`,
            borderLeft: `5px solid ${item.color}`,
            borderRadius: 10,
            padding: '18px 24px',
            opacity: sp(item.delay),
            transform: `translateX(${(1 - sp(item.delay)) * 60}px)`,
          }}>
            <svg width="32" height="32" viewBox="0 0 32 32">
              <circle cx="16" cy="16" r="14" fill={item.color} opacity="0.2" />
              <line x1="10" y1="10" x2="22" y2="22" stroke={item.color} strokeWidth="3.5" strokeLinecap="round" />
              <line x1="22" y1="10" x2="10" y2="22" stroke={item.color} strokeWidth="3.5" strokeLinecap="round" />
            </svg>
            <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#F5F5F5' }}>
              {item.label}
            </span>
            <span style={{ marginLeft: 'auto', fontFamily: 'JetBrains Mono', fontSize: 22, color: item.color, fontWeight: 700 }}>
              POSSIBLE
            </span>
          </div>
        ))}

        {/* Warning note */}
        <div style={{
          marginTop: 24,
          padding: '16px 24px',
          backgroundColor: '#1A0500',
          border: '2px solid #FFC107',
          borderRadius: 10,
          opacity: sp(95),
        }}>
          <span style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#FFC107' }}>
            इतिहास में बार बार हुआ है — भारत अलग नहीं
          </span>
        </div>
      </div>

      {/* Danger meter */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        alignItems: 'center',
        gap: 20,
        opacity: sp(40),
      }}>
        <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E' }}>जोखिम स्तर</span>
        <div style={{
          width: 600,
          height: 30,
          backgroundColor: '#2D2D2D',
          borderRadius: 15,
          overflow: 'hidden',
          border: '2px solid #9E9E9E',
        }}>
          <div style={{
            width: `${dangerFill * 100}%`,
            height: '100%',
            background: `linear-gradient(90deg, #138808, #FFC107, #ff0000)`,
            borderRadius: 15,
            transition: 'none',
          }} />
        </div>
        <span style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 30,
          fontWeight: 900,
          color: '#ff0000',
        }}>CRITICAL</span>
      </div>

      {/* Stick figure — alarmed */}
      <div style={{
        position: 'absolute',
        left: 720,
        bottom: 50,
        opacity: sp(50),
        transform: `scale(${sp(50)}) translateX(${Math.sin(frame * 0.15) * 3}px)`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="75" height="150" viewBox="0 0 75 150">
          <circle cx="37" cy="17" r="15" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="29" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="45" cy="16" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="35" y1="16" x2="39" y2="16" stroke="#FFC107" strokeWidth="1.5" />
          <circle cx="37" cy="24" r="3" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="37" y1="32" x2="37" y2="78" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="37" y1="50" x2="10" y2="35" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="50" x2="64" y2="35" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="22" y2="128" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="37" y1="78" x2="52" y2="128" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 28%, rgba(0,0,0,0.88) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene8_CapitalControls;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_PensionNationalized',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_PensionNationalized: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Government hand reaching in
  const handReach = interpolate(frame, [30, 70], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Piggy bank crack
  const crackOpacity = interpolate(frame, [65, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Empty state fade
  const emptyFade = interpolate(frame, [80, 100], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.quad),
  });

  // Timeline circles scroll
  const timelineX = (frame * 0.5) % 100;

  // Background hexagons
  const hexAngle = frame * 0.15;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0A0A0A', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, #0A0A14 0%, #0A0A0A 50%, #14000A 100%)',
      }} />

      {/* Background hexagonal grid */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {[...Array(6)].map((_, row) =>
            [...Array(9)].map((_, col) => {
              const cx = col * 220 + (row % 2 === 0 ? 0 : 110);
              const cy = row * 190;
              const r = 100;
              const points = [...Array(6)].map((__, i) => {
                const angle = (i * 60 + hexAngle) * Math.PI / 180;
                return `${cx + r * Math.cos(angle)},${cy + r * Math.sin(angle)}`;
              }).join(' ');
              return (
                <polygon key={`${row}-${col}`} points={points}
                  fill="none" stroke="#9E9E9E" strokeWidth="1" />
              );
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(16)].map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#ff0000' : '#9E9E9E',
            left: seededRandom(i * 13) * 1920,
            top: seededRandom(i * 9) * 1080,
            opacity: 0.06 + seededRandom(i) * 0.08,
          }} />
        ))}
      </AbsoluteFill>

      {/* Year badge top */}
      <div style={{
        position: 'absolute',
        top: 55,
        left: '50%',
        transform: `translateX(-50%) scale(${sp(5)})`,
        opacity: sp(5),
        display: 'flex',
        gap: 30,
        alignItems: 'center',
      }}>
        <div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
          {['#74ACDF', '#F5F5F5', '#74ACDF'].map((c, i) => (
            <div key={i} style={{ width: 60, height: 12, backgroundColor: c, borderRadius: 2 }} />
          ))}
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 48,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: 4,
        }}>ARGENTINA 2008</div>
      </div>

      {/* Left — Piggy bank being emptied */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: sp(12),
      }}>
        <div style={{ position: 'relative', width: 420, height: 420 }}>
          {/* Piggy bank SVG */}
          <svg width="420" height="420" viewBox="0 0 420 420">
            {/* Body */}
            <ellipse cx="200" cy="240" rx="160" ry="140"
              fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="4" />

            {/* Fill level */}
            <clipPath id="fillClip">
              <ellipse cx="200" cy="240" rx="155" ry="135" />
            </clipPath>
            <rect x="50" y={100 + emptyFade * 180} width="300" height="270"
              fill="#FFC107" opacity={0.25 - emptyFade * 0.2}
              clipPath="url(#fillClip)"
            />

            {/* Coin slot */}
            <rect x="175" y="105" width="50" height="10" rx="5"
              fill="#9E9E9E" opacity="0.7" />

            {/* Snout */}
            <ellipse cx="350" cy="250" rx="45" ry="35"
              fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3" />
            <ellipse cx="342" cy="247" rx="8" ry="6" fill="#9E9E9E" opacity="0.4" />
            <ellipse cx="358" cy="247" rx="8" ry="6" fill="#9E9E9E" opacity="0.4" />

            {/* Ear */}
            <ellipse cx="90" cy="130" rx="30" ry="25"
              fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="3" />

            {/* Legs */}
            {[100, 160, 230, 290].map((x, i) => (
              <rect key={i} x={x} y="360" width="28" height="50" rx="10"
                fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
            ))}

            {/* Crack lines */}
            <g opacity={crackOpacity}>
              <path d="M 180 200 L 165 240 L 190 220 L 175 270"
                fill="none" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
              <path d="M 220 180 L 240 220 L 215 210 L 235 255"
                fill="none" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
            </g>

            {/* EMPTY label */}
            {emptyFade > 0.5 && (
              <text x="200" y="260" textAnchor="middle"
                fill="#ff0000" fontSize="42" fontFamily="JetBrains Mono" fontWeight="900"
                opacity={emptyFade}>
                EMPTY
              </text>
            )}
          </svg>

          {/* Government hand reaching in from top */}
          <div style={{
            position: 'absolute',
            top: -20,
            left: '50%',
            transform: `translateX(-50%) translateY(${-200 + handReach * 180}px)`,
          }}>
            <svg width="80" height="200" viewBox="0 0 80 200">
              {/* Sleeve */}
              <rect x="20" y="0" width="40" height="100" rx="8"
                fill="#1A1A3A" stroke="#9E9E9E" strokeWidth="2" />
              {/* Hand */}
              <ellipse cx="40" cy="110" rx="28" ry="22"
                fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="2" />
              {/* Fingers grabbing */}
              {[15, 25, 35, 45, 55].map((x, i) => (
                <rect key={i} x={x} y="112" width="10" height={20 + i * 3} rx="5"
                  fill="#2D2D2D" stroke="#9E9E9E" strokeWidth="1.5" />
              ))}
              {/* GOV label on sleeve */}
              <text x="40" y="55" textAnchor="middle" fill="#9E9E9E" fontSize="12" fontFamily="JetBrains Mono">GOV</text>
            </svg>
          </div>
        </div>
      </div>

      {/* Right — What happened */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 1000,
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 90,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          marginBottom: 32,
          opacity: sp(15),
          transform: `translateX(${(1 - sp(15)) * 50}px)`,
        }}>
          पेंशन फंड्स <span style={{ color: '#ff0000' }}>राष्ट्रीयकृत</span>
        </div>

        {/* Timeline of events */}
        <div style={{ position: 'relative', paddingLeft: 40 }}>
          {/* Vertical line */}
          <div style={{
            position: 'absolute',
            left: 16,
            top: 0,
            bottom: 0,
            width: 3,
            backgroundColor: '#ff0000',
            opacity: 0.4,
          }} />

          {[
            { event: 'सरकार ने कानून पास किया', detail: 'एक रात में', color: '#ff0000', delay: 25 },
            { event: 'जीवन भर की बचत ली गई', detail: 'करोड़ों लोगों की', color: '#ff0000', delay: 40 },
            { event: 'Promise: पेंशन मिलती रहेगी', detail: 'सरकारी वादा', color: '#FFC107', delay: 55 },
            { event: 'Inflation बढ़ी — पेंशन की वैल्यू खतम', detail: 'असली नुकसान', color: '#ff0000', delay: 70 },
          ].map((item, i) => (
            <div key={i} style={{
              display: 'flex',
              alignItems: 'flex-start',
              gap: 24,
              marginBottom: 28,
              opacity: sp(item.delay),
              transform: `translateX(${(1 - sp(item.delay)) * 40}px)`,
            }}>
              <div style={{
                width: 16,
                height: 16,
                borderRadius: '50%',
                backgroundColor: item.color,
                marginTop: 8,
                flexShrink: 0,
                boxShadow: `0 0 8px ${item.color}`,
              }} />
              <div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700, color: '#F5F5F5', lineHeight: 1.2 }}>
                  {item.event}
                </div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: item.color, marginTop: 4 }}>
                  {item.detail}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom warning */}
        <div style={{
          marginTop: 16,
          backgroundColor: '#2D2D2D',
          border: '2px solid #ff0000',
          borderRadius: 12,
          padding: '16px 28px',
          opacity: sp(90),
        }}>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 42,
            color: '#ff0000',
          }}>यह कल्पना नहीं — इतिहास है</span>
        </div>
      </div>

      {/* Stick figure — old person with walking stick (pensioner) */}
      <div style={{
        position: 'absolute',
        left: 520,
        bottom: 50,
        opacity: sp(55),
        transform: `scale(${sp(55)})`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.2) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="75" height="160" viewBox="0 0 75 160">
          {/* Old person — stooped */}
          <circle cx="37" cy="18" r="15" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <circle cx="29" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <circle cx="45" cy="17" r="6" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="35" y1="17" x2="39" y2="17" stroke="#FFC107" strokeWidth="1.5" />
          {/* Sad lines */}
          <path d="M 30 26 Q 37 23 44 26" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          {/* Bent body */}
          <line x1="37" y1="33" x2="32" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Walking stick arm */}
          <line x1="37" y1="50" x2="60" y2="65" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Walking stick */}
          <line x1="60" y1="65" x2="65" y2="130" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
          {/* Other arm */}
          <line x1="37" y1="50" x2="14" y2="62" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Legs */}
          <line x1="32" y1="80" x2="20" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="32" y1="80" x2="44" y2="130" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 30%, rgba(0,0,0,0.88) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene9_PensionNationalized;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_CouldHappenHere',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_CouldHappenHere: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Map reveal
  const mapReveal = interpolate(frame, [20, 70], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Pulse glow on India
  const indiaGlow = 0.5 + 0.5 * Math.sin(frame * 0.2);

  // Warning bars spreading
  const warnSpread = interpolate(frame, [40, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  // Central message type-on effect
  const charCount = Math.floor(interpolate(frame, [60, 100], [0, 18], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  }));

  const fullText = 'यहां भी हो सकता है';

  // Background wave
  const waveY = Math.sin(frame * 0.03) * 20;

  return (
    <AbsoluteFill style={{ backgroundColor: '#0A0A0A', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient — full blackout mood with India orange accent */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #1A0A00 0%, #0A0A0A 60%, #000A14 100%)',
      }} />

      {/* Animated concentric squares — background */}
      <AbsoluteFill style={{ opacity: 0.04, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        {[1, 2, 3, 4, 5, 6].map(i => (
          <div key={i} style={{
            position: 'absolute',
            width: i * 250,
            height: i * 250,
            border: '1px solid #FF9933',
            transform: `rotate(${frame * 0.2 + i * 15}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {[...Array(22)].map((_, i) => {
          const floatY = Math.sin(frame * 0.02 + i) * 20;
          return (
            <div key={i} style={{
              position: 'absolute',
              width: 3 + seededRandom(i) * 3,
              height: 3 + seededRandom(i) * 3,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FF9933' : i % 3 === 1 ? '#138808' : '#9E9E9E',
              left: seededRandom(i * 11) * 1920,
              top: seededRandom(i * 7) * 1080 + floatY,
              opacity: 0.06 + seededRandom(i) * 0.1,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Left side — India map silhouette simplified */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: mapReveal,
      }}>
        <svg width="480" height="560" viewBox="0 0 480 560">
          {/* Simplified India shape */}
          <path d="M 180 20 L 320 20 L 380 80 L 420 160 L 440 250 L 400 340 L 360 380 L 320 420 L 280 480 L 240 530 L 200 480 L 150 420 L 110 370 L 70 300 L 40 220 L 60 140 L 100 80 Z"
            fill="#2D2D2D"
            stroke="#FF9933"
            strokeWidth="3"
            opacity={mapReveal}
          />

          {/* India glow */}
          <path d="M 180 20 L 320 20 L 380 80 L 420 160 L 440 250 L 400 340 L 360 380 L 320 420 L 280 480 L 240 530 L 200 480 L 150 420 L 110 370 L 70 300 L 40 220 L 60 140 L 100 80 Z"
            fill="#FF9933"
            opacity={indiaGlow * 0.1 * warnSpread}
          />

          {/* Warning dots on map */}
          {warnSpread > 0.3 && [
            { cx: 240, cy: 200 }, { cx: 160, cy: 260 }, { cx: 320, cy: 220 },
            { cx: 200, cy: 340 }, { cx: 300, cy: 300 },
          ].map((pos, i) => (
            <circle key={i} cx={pos.cx} cy={pos.cy} r="12"
              fill="#ff0000"
              opacity={0.6 + 0.4 * Math.sin(frame * 0.2 + i)}
            >
            </circle>
          ))}

          {/* INDIA label */}
          <text x="240" y="290" textAnchor="middle"
            fill="#FF9933" fontSize="38" fontFamily="JetBrains Mono" fontWeight="bold"
            opacity={mapReveal}>
            भारत
          </text>

          {/* India flag colors bar */}
          <g transform="translate(80, 540)">
            {['#FF9933', '#F5F5F5', '#138808'].map((c, i) => (
              <rect key={i} x={i * 107} y="0" width="107" height="16" fill={c} rx={i === 0 ? '4 0 0 4' : i === 2 ? '0 4 4 0' : '0'} />
            ))}
          </g>
        </svg>
      </div>

      {/* Center — Main message */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: 'translate(-15%, -50%)',
        width: 900,
      }}>

        {/* Setup question */}
        <div style={{
          opacity: sp(10),
          transform: `translateY(${(1 - sp(10)) * -30}px)`,
          marginBottom: 32,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#9E9E9E',
            lineHeight: 1.3,
          }}>
            "भारत में ऐसा क्यों होगा?"
          </div>
        </div>

        {/* BIG ANSWER */}
        <div style={{
          marginBottom: 40,
          opacity: sp(30),
          transform: `scale(${sp(30, { damping: 8, stiffness: 80 })})`,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 150,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 0.85,
          }}>
            {fullText.slice(0, charCount)}
            {charCount < fullText.length && (
              <span style={{ opacity: Math.sin(frame * 0.5) > 0 ? 1 : 0, color: '#FFC107' }}>|</span>
            )}
          </div>
        </div>

        {/* Condition chips */}
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          gap: 16,
          opacity: sp(75),
          transform: `translateY(${(1 - sp(75)) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 34,
            color: '#FFC107',
            marginBottom: 8,
          }}>अगर परिस्थितियां बिगड़ें...</div>

          {[
            'विदेशी मुद्रा भंडार संकट',
            'रुपया लगातार गिरे',
            'विदेशी कर्ज चुकाना मुश्किल',
          ].map((cond, i) => (
            <div key={i} style={{
              display: 'flex',
              alignItems: 'center',
              gap: 16,
              opacity: interpolate(frame, [78 + i * 8, 90 + i * 8], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateX(${interpolate(frame, [78 + i * 8, 90 + i * 8], [30, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            }}>
              <div style={{
                width: 8,
                height: 8,
                borderRadius: '50%',
                backgroundColor: '#ff0000',
              }} />
              <span style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 30,
                color: '#F5F5F5',
              }}>{cond}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Right — History reminder */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 380,
        opacity: sp(50),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#9E9E9E',
          marginBottom: 20,
          letterSpacing: 2,
        }}>इतिहास के उदाहरण</div>

        {[
          { country: 'Cyprus', year: '2013', event: 'Bank levy' },
          { country: 'Argentina', year: '2001', event: 'Corralito' },
          { country: 'Argentina', year: '2008', event: 'Pension seized' },
          { country: 'India', year: '2016', event: 'Demonetization' },
          { country: 'USA', year: '1933', event: 'Gold seizure' },
        ].map((item, i) => (
          <div key={i} style={{
            marginBottom: 14,
            padding: '12px 18px',
            backgroundColor: '#2D2D2D',
            borderRadius: 8,
            borderLeft: `3px solid ${i === 3 ? '#FF9933' : '#ff0000'}`,
            opacity: interpolate(frame, [52 + i * 8, 65 + i * 8], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `translateX(${interpolate(frame, [52 + i * 8, 65 + i * 8], [30, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 22, color: i === 3 ? '#FF9933' : '#F5F5F5', fontWeight: 700 }}>
                {item.country}
              </span>
              <span style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#9E9E9E' }}>{item.year}</span>
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#ff0000', marginTop: 2 }}>
              {item.event}
            </div>
          </div>
        ))}
      </div>

      {/* Stick figure — pointing at map, wearing glasses */}
      <div style={{
        position: 'absolute',
        left: 590,
        bottom: 55,
        opacity: sp(45),
        transform: `scale(${sp(45)}) translateY(${waveY * 0.2}px)`,
      }}>
        <div style={{
          position: 'absolute', width: 90, height: 90, borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,193,7,0.3) 0%, transparent 70%)',
          left: -8, top: -5,
        }} />
        <svg width="80" height="160" viewBox="0 0 80 160">
          <circle cx="40" cy="18" r="16" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 4}, 40, 18)`}
          />
          {/* Glasses */}
          <circle cx="32" cy="17" r="7" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="48" cy="17" r="7" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="39" y1="17" x2="41" y2="17" stroke="#FFC107" strokeWidth="2" />
          {/* Serious expression */}
          <line x1="33" y1="25" x2="47" y2="25" stroke="#F5F5F5" strokeWidth="1.5" />
          {/* Body */}
          <line x1="40" y1="34" x2="40" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Pointing arm — left */}
          <line x1="40" y1="55" x2="5" y2="70" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Other arm */}
          <line x1="40" y1="55" x2="65" y2="62" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          {/* Legs */}
          <line x1="40" y1="88" x2="25" y2="145" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="40" y1="88" x2="55" y2="145" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, transparent 28%, rgba(0,0,0,0.9) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default Scene10_CouldHappenHere;
```

