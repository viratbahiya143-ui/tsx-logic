```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21_GoodInflationPitch',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21_GoodInflationPitch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  // Three pillars stagger
  const pillar1 = sp(10);
  const pillar2 = sp(45);
  const pillar3 = sp(80);

  // Title
  const titleY = interpolate(sp(0), [0, 1], [-80, 0]);
  const titleOp = interpolate(sp(0), [0, 1], [0, 1]);

  // Arrow up rising
  const arrowProgress = interpolate(frame, [20, 180], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape Layers - drifting transparent geometric forms */}
      <div style={{
        position: 'absolute', top: -200, right: -150, width: 700, height: 700,
        border: '3px solid #06202B', borderRadius: '50%', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame / 60) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -180, left: -120, width: 500, height: 500,
        backgroundColor: '#06202B', opacity: 0.04,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', left: '45%', width: 400, height: 400,
        border: '2px solid #000000', opacity: 0.05,
        transform: `translate(-50%,-50%) rotate(${-frame * 0.2}deg)`,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
      }} />

      {/* Motion texture - vertical grid lines */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.07 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 0, bottom: 0,
            left: `${(i / 24) * 100}%`,
            width: 1, backgroundColor: '#06202B',
          }} />
        ))}
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={`h${i}`} style={{
            position: 'absolute', left: 0, right: 0,
            top: `${(i / 14) * 100}%`,
            height: 1, backgroundColor: '#06202B',
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = seededRandom(i + 1);
        const x = seed * 1920;
        const y = (seededRandom(i + 50) * 1080 + frame * (0.3 + seed * 0.5)) % 1080;
        const size = 3 + seed * 5;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.15 + seed * 0.2,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
        transformOrigin: 'center center',
      }}>
        {/* TOP BADGE */}
        <div style={{
          position: 'absolute', top: 70, left: '50%',
          transform: `translateX(-50%) translateY(${titleY}px)`,
          opacity: titleOp,
          backgroundColor: '#06202B',
          padding: '14px 38px',
          borderRadius: 4,
          letterSpacing: 4,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26, fontWeight: 700, color: '#EEEEEE',
          }}>
            THE WHITE HOUSE PITCH
          </div>
        </div>

        {/* HEADLINE */}
        <div style={{
          position: 'absolute', top: 180, left: 0, right: 0,
          textAlign: 'center',
          transform: `translateY(${titleY}px)`,
          opacity: titleOp,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
            fontSize: 119, fontWeight: 900,
            color: '#000000', lineHeight: 1,
            letterSpacing: -3,
          }}>
            "GOOD INFLATION"
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 38, fontWeight: 500,
            color: '#06202B', marginTop: 18,
            opacity: 0.7, letterSpacing: 2,
          }}>
            ——— THE THREE ENGINES ———
          </div>
        </div>

        {/* THREE PILLARS */}
        <div style={{
          position: 'absolute', top: 460, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', gap: 60,
        }}>
          {/* Pillar 1: GROWTH */}
          <div style={{
            width: 420, height: 460,
            transform: `translateY(${interpolate(pillar1, [0, 1], [200, 0])}px) scale(${interpolate(pillar1, [0, 1], [0.7, 1])})`,
            opacity: pillar1,
          }}>
            <div style={{
              backgroundColor: '#06202B',
              padding: '24px 0',
              textAlign: 'center',
              borderRadius: '4px 4px 0 0',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22, color: '#EEEEEE', opacity: 0.5,
                letterSpacing: 3,
              }}>01</div>
              <div style={{
                fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
                fontSize: 60, fontWeight: 900, color: '#EEEEEE',
                letterSpacing: -1, marginTop: 4,
              }}>GROWTH</div>
            </div>
            <div style={{
              backgroundColor: '#EEEEEE',
              border: '3px solid #06202B',
              borderTop: 'none',
              padding: '40px 28px',
              height: 320,
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'space-between',
            }}>
              {/* Rising bar chart */}
              <svg width="280" height="160" viewBox="0 0 280 160">
                {[40, 70, 55, 95, 130].map((h, i) => {
                  const grow = interpolate(arrowProgress, [i * 0.1, i * 0.1 + 0.25], [0, h], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
                  return (
                    <rect key={i} x={20 + i * 48} y={160 - grow} width="36" height={grow}
                      fill="#06202B" />
                  );
                })}
                <line x1="10" y1="160" x2="270" y2="160" stroke="#000000" strokeWidth="2" />
              </svg>
              <div style={{
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 32, fontWeight: 600, color: '#06202B',
                textAlign: 'center', lineHeight: 1.2,
              }}>
                Expanding<br />economy
              </div>
            </div>
          </div>

          {/* Pillar 2: TARIFFS */}
          <div style={{
            width: 420, height: 460,
            transform: `translateY(${interpolate(pillar2, [0, 1], [200, 0])}px) scale(${interpolate(pillar2, [0, 1], [0.7, 1])})`,
            opacity: pillar2,
          }}>
            <div style={{
              backgroundColor: '#06202B',
              padding: '24px 0',
              textAlign: 'center',
              borderRadius: '4px 4px 0 0',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22, color: '#EEEEEE', opacity: 0.5,
                letterSpacing: 3,
              }}>02</div>
              <div style={{
                fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
                fontSize: 60, fontWeight: 900, color: '#EEEEEE',
                letterSpacing: -1, marginTop: 4,
              }}>TARIFFS</div>
            </div>
            <div style={{
              backgroundColor: '#EEEEEE',
              border: '3px solid #06202B',
              borderTop: 'none',
              padding: '40px 28px',
              height: 320,
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'space-between',
            }}>
              {/* Treasury flow arrows */}
              <svg width="280" height="160" viewBox="0 0 280 160">
                {Array.from({ length: 4 }).map((_, i) => {
                  const flowY = (frame * 2 + i * 40) % 160;
                  return (
                    <g key={i} opacity={interpolate(flowY, [0, 20, 140, 160], [0, 1, 1, 0])}>
                      <path d={`M 140 ${flowY} L 130 ${flowY - 10} M 140 ${flowY} L 150 ${flowY - 10}`}
                        stroke="#06202B" strokeWidth="4" fill="none" strokeLinecap="round" />
                      <line x1="140" y1={flowY - 15} x2="140" y2={flowY + 5} stroke="#06202B" strokeWidth="4" strokeLinecap="round" />
                    </g>
                  );
                })}
                <rect x="60" y="130" width="160" height="30" fill="#06202B" />
                <text x="140" y="151" textAnchor="middle"
                  fontFamily="JetBrains Mono" fontWeight="700" fontSize="16" fill="#EEEEEE">
                  TREASURY
                </text>
              </svg>
              <div style={{
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 32, fontWeight: 600, color: '#06202B',
                textAlign: 'center', lineHeight: 1.2,
              }}>
                Revenue<br />inflow
              </div>
            </div>
          </div>

          {/* Pillar 3: MANUFACTURING */}
          <div style={{
            width: 420, height: 460,
            transform: `translateY(${interpolate(pillar3, [0, 1], [200, 0])}px) scale(${interpolate(pillar3, [0, 1], [0.7, 1])})`,
            opacity: pillar3,
          }}>
            <div style={{
              backgroundColor: '#000000',
              padding: '24px 0',
              textAlign: 'center',
              borderRadius: '4px 4px 0 0',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22, color: '#EEEEEE', opacity: 0.5,
                letterSpacing: 3,
              }}>03</div>
              <div style={{
                fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
                fontSize: 51, fontWeight: 900, color: '#EEEEEE',
                letterSpacing: -1, marginTop: 4,
              }}>MFG. HOME</div>
            </div>
            <div style={{
              backgroundColor: '#EEEEEE',
              border: '3px solid #000000',
              borderTop: 'none',
              padding: '40px 28px',
              height: 320,
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'space-between',
            }}>
              {/* Factory icon */}
              <svg width="280" height="160" viewBox="0 0 280 160">
                <rect x="40" y="80" width="200" height="70" fill="#06202B" />
                <polygon points="80,80 100,50 120,80" fill="#06202B" />
                <polygon points="120,80 140,50 160,80" fill="#06202B" />
                <polygon points="160,80 180,50 200,80" fill="#06202B" />
                <rect x="60" y="100" width="20" height="20" fill="#EEEEEE" />
                <rect x="100" y="100" width="20" height="20" fill="#EEEEEE" />
                <rect x="140" y="100" width="20" height="20" fill="#EEEEEE" />
                <rect x="180" y="100" width="20" height="20" fill="#EEEEEE" />
                {/* Smoke puffs */}
                {[0, 1, 2].map(i => {
                  const sy = ((frame * 1.5 + i * 25) % 60);
                  return (
                    <circle key={i} cx={100 + i * 40} cy={45 - sy} r={6 + sy / 10}
                      fill="#06202B" opacity={interpolate(sy, [0, 30, 60], [0.8, 0.4, 0])} />
                  );
                })}
              </svg>
              <div style={{
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 32, fontWeight: 600, color: '#06202B',
                textAlign: 'center', lineHeight: 1.2,
              }}>
                Production<br />returns
              </div>
            </div>
          </div>
        </div>

        {/* BOTTOM STRIP */}
        <div style={{
          position: 'absolute', bottom: 60, left: '50%',
          transform: `translateX(-50%) scaleX(${sp(120)})`,
          transformOrigin: 'center',
          backgroundColor: '#000000',
          padding: '18px 50px',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
            fontSize: 36, fontWeight: 900, color: '#EEEEEE',
            letterSpacing: 4,
          }}>
            THE PITCH FROM THE PODIUM
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene21_GoodInflationPitch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22_GoodVsBadInflation',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22_GoodVsBadInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen reveal
  const leftSlide = sp(0);
  const rightSlide = sp(15);
  const vsScale = sp(50, { stiffness: 200 });
  const pitchReveal = sp(110);

  // Pan effect
  const panX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Background gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EEEEEE 0%, #e0e0e0 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '10%', width: 600, height: 600,
        borderRadius: '50%', border: '2px solid #06202B', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame / 50) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '8%', width: 450, height: 450,
        backgroundColor: '#000000', opacity: 0.04,
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Diagonal motion lines texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.08 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: -100, left: `${i * 80 - 200}px`,
            width: 1, height: 1500, backgroundColor: '#06202B',
            transform: `rotate(20deg) translateY(${(frame * 0.5 + i * 30) % 200}px)`,
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 100);
        const x = (seed * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 200) * 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `translateX(${panX}px)`,
      }}>
        {/* TOP LABEL */}
        <div style={{
          position: 'absolute', top: 50, left: '50%',
          transform: `translateX(-50%) scale(${sp(0)})`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 28, fontWeight: 700,
          color: '#06202B', letterSpacing: 5,
          opacity: 0.7,
        }}>
          [ THE NARRATIVE FRAMING ]
        </div>

        {/* SPLIT COMPARISON */}
        <div style={{
          position: 'absolute', top: 150, left: 0, right: 0, bottom: 280,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
        }}>
          {/* LEFT — GOOD INFLATION */}
          <div style={{
            width: 720, height: 580,
            backgroundColor: '#06202B',
            transform: `translateX(${interpolate(leftSlide, [0, 1], [-900, 0])}px)`,
            padding: '50px 50px',
            display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
            position: 'relative', overflow: 'hidden',
          }}>
            <div style={{
              position: 'absolute', top: -50, right: -50,
              fontFamily: 'Mont Heavy', fontSize: 280, fontWeight: 900,
              color: '#EEEEEE', opacity: 0.07,
            }}>
              ✓
            </div>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 23, fontWeight: 700,
                color: '#EEEEEE', opacity: 0.5, letterSpacing: 4,
              }}>
                CLAIMED →
              </div>
              <div style={{
                fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
                color: '#EEEEEE', lineHeight: 0.95, marginTop: 16,
                letterSpacing: -3,
              }}>
                GOOD<br />INFLATION
              </div>
            </div>
            <div style={{
              display: 'flex', flexDirection: 'column', gap: 14,
              fontFamily: 'Inter, Poppins', fontSize: 31, fontWeight: 500,
              color: '#EEEEEE',
            }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                Growth-driven
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                Tariff revenue
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                Manufacturing return
              </div>
            </div>
          </div>

          {/* VS DIVIDER */}
          <div style={{
            width: 120, display: 'flex', flexDirection: 'column', alignItems: 'center',
            transform: `scale(${vsScale})`,
            zIndex: 10,
          }}>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
              color: '#000000', letterSpacing: -2,
            }}>
              VS
            </div>
            <div style={{
              width: 4, height: 120, backgroundColor: '#000000', marginTop: -10,
            }} />
          </div>

          {/* RIGHT — BAD INFLATION */}
          <div style={{
            width: 720, height: 580,
            backgroundColor: '#000000',
            transform: `translateX(${interpolate(rightSlide, [0, 1], [900, 0])}px)`,
            padding: '50px 50px',
            display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
            position: 'relative', overflow: 'hidden',
          }}>
            <div style={{
              position: 'absolute', top: -50, right: -50,
              fontFamily: 'Mont Heavy', fontSize: 280, fontWeight: 900,
              color: '#EEEEEE', opacity: 0.07,
            }}>
              ✕
            </div>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 23, fontWeight: 700,
                color: '#EEEEEE', opacity: 0.5, letterSpacing: 4,
              }}>
                DISMISSED →
              </div>
              <div style={{
                fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
                color: '#EEEEEE', lineHeight: 0.95, marginTop: 16,
                letterSpacing: -3,
              }}>
                BAD<br />INFLATION
              </div>
            </div>
            <div style={{
              display: 'flex', flexDirection: 'column', gap: 14,
              fontFamily: 'Inter, Poppins', fontSize: 31, fontWeight: 500,
              color: '#EEEEEE',
            }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                Supply shocks
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                Economic weakness
              </div>
              <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
                <div style={{ width: 12, height: 12, backgroundColor: '#EEEEEE' }} />
                System fragility
              </div>
            </div>
          </div>
        </div>

        {/* BOTTOM REVEAL — THAT'S THE PITCH */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0,
          textAlign: 'center',
          opacity: pitchReveal,
          transform: `translateY(${interpolate(pitchReveal, [0, 1], [40, 0])}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#EEEEEE',
            border: '4px solid #06202B',
            padding: '22px 70px',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 77, fontWeight: 900,
              color: '#06202B', letterSpacing: -1,
            }}>
              That's the pitch from the podium.
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene22_GoodVsBadInflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23_EIADigging',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23_EIADigging: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineY = interpolate(sp(0), [0, 1], [-100, 0]);
  const headlineOp = sp(0);
  const stamp = sp(40, { stiffness: 200, damping: 8 });
  const data1 = sp(70);
  const data2 = sp(95);
  const data3 = sp(120);

  // Camera dolly
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Subtle gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(6,32,43,0.06) 0%, rgba(238,238,238,0) 70%)',
      }} />

      {/* Shape layers - data dossier feel */}
      <div style={{
        position: 'absolute', top: '15%', right: '8%', width: 500, height: 650,
        border: '2px solid #06202B', opacity: 0.05,
        transform: `rotate(${8 + Math.sin(frame / 80) * 2}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '25%', left: '6%', width: 480, height: 620,
        backgroundColor: '#06202B', opacity: 0.04,
        transform: `rotate(${-6 + Math.sin(frame / 70) * 2}deg)`,
      }} />

      {/* Circular sweep texture */}
      <div style={{ position: 'absolute', top: '50%', left: '50%', opacity: 0.06 }}>
        {[200, 350, 500, 700].map((r, i) => (
          <div key={i} style={{
            position: 'absolute', top: -r, left: -r,
            width: r * 2, height: r * 2,
            border: '1px solid #06202B', borderRadius: '50%',
          }} />
        ))}
      </div>

      {/* Floating data particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 23);
        const x = seed * 1920;
        const y = (seededRandom(i + 55) * 1080 + frame * (0.4 + seed * 0.6)) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            fontFamily: 'JetBrains Mono', fontSize: 12,
            color: '#06202B', opacity: 0.25,
          }}>
            {seed > 0.5 ? '01' : '10'}
          </div>
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
      }}>
        {/* TOP HEADLINE */}
        <div style={{
          position: 'absolute', top: 100, left: 0, right: 0,
          textAlign: 'center',
          opacity: headlineOp,
          transform: `translateY(${headlineY}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30, fontWeight: 700,
            color: '#06202B', letterSpacing: 4,
            opacity: 0.7,
          }}>
            FAILED FINANCE EARNS ITS NAME
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 136, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 20,
            letterSpacing: -4,
          }}>
            THE DEEP DIG
          </div>
        </div>

        {/* CLASSIFIED STAMP */}
        <div style={{
          position: 'absolute', top: 130, right: 140,
          transform: `scale(${stamp}) rotate(-12deg)`,
          opacity: stamp * 0.85,
        }}>
          <div style={{
            border: '6px solid #06202B',
            padding: '16px 32px',
            fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 900,
            color: '#06202B', letterSpacing: 4,
          }}>
            EIA DATA
          </div>
        </div>

        {/* DATA CARDS */}
        <div style={{
          position: 'absolute', top: 440, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', gap: 50,
        }}>
          {/* Card 1: 3 Weeks */}
          <div style={{
            width: 460, padding: '38px 36px',
            backgroundColor: '#06202B',
            transform: `translateY(${interpolate(data1, [0, 1], [120, 0])}px) scale(${interpolate(data1, [0, 1], [0.85, 1])})`,
            opacity: data1,
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 21,
              color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
            }}>
              TIMEFRAME
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 187, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 0.9, marginTop: 8,
              letterSpacing: -5,
            }}>
              3
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 41, fontWeight: 600,
              color: '#EEEEEE', marginTop: -10,
            }}>
              WEEKS DIGGING
            </div>
            <div style={{
              height: 3, backgroundColor: '#EEEEEE', opacity: 0.3,
              marginTop: 24,
            }} />
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 400,
              color: '#EEEEEE', opacity: 0.8, marginTop: 14,
            }}>
              Through raw EIA reports
            </div>
          </div>

          {/* Card 2: Source */}
          <div style={{
            width: 460, padding: '38px 36px',
            backgroundColor: '#EEEEEE',
            border: '4px solid #000000',
            transform: `translateY(${interpolate(data2, [0, 1], [120, 0])}px) scale(${interpolate(data2, [0, 1], [0.85, 1])})`,
            opacity: data2,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 21,
              color: '#06202B', opacity: 0.6, letterSpacing: 3,
            }}>
              PRIMARY SOURCE
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 77, fontWeight: 900,
              color: '#000000', lineHeight: 0.95, marginTop: 16,
              letterSpacing: -2,
            }}>
              ENERGY<br />INFO ADMIN
            </div>
            <div style={{
              height: 3, backgroundColor: '#06202B', opacity: 0.3,
              marginTop: 24,
            }} />
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 600,
              color: '#06202B', marginTop: 14, letterSpacing: 2,
            }}>
              eia.gov / production data
            </div>
          </div>

          {/* Card 3: Method */}
          <div style={{
            width: 460, padding: '38px 36px',
            backgroundColor: '#000000',
            transform: `translateY(${interpolate(data3, [0, 1], [120, 0])}px) scale(${interpolate(data3, [0, 1], [0.85, 1])})`,
            opacity: data3,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 21,
              color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
            }}>
              METHOD
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 60, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 1, marginTop: 16,
              letterSpacing: -2,
            }}>
              COLD<br />HARD<br />MATH
            </div>
            <div style={{
              height: 3, backgroundColor: '#EEEEEE', opacity: 0.3,
              marginTop: 24,
            }} />
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 400,
              color: '#EEEEEE', opacity: 0.8, marginTop: 14,
            }}>
              No spin. Just spreadsheets.
            </div>
          </div>
        </div>

        {/* BOTTOM TICKER */}
        <div style={{
          position: 'absolute', bottom: 70, left: 0, right: 0,
          height: 50, backgroundColor: '#06202B',
          display: 'flex', alignItems: 'center',
          overflow: 'hidden',
        }}>
          <div style={{
            display: 'flex', gap: 80,
            transform: `translateX(${-((frame * 2) % 1200)}px)`,
            fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
            color: '#EEEEEE', whiteSpace: 'nowrap',
            letterSpacing: 2,
          }}>
            {Array.from({ length: 8 }).map((_, i) => (
              <span key={i}>
                EIA WEEKLY ◆ CRUDE OUTPUT ◆ PERMIAN BASIN ◆ STRATEGIC RESERVE ◆ GULF COAST TERMINALS ◆
              </span>
            ))}
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene23_EIADigging;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24_SourcesNetwork',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24_SourcesNetwork: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleSpring = sp(0);
  const trader1 = sp(25);
  const trader2 = sp(55);
  const fedReveal = sp(110);
  const lineConnect = interpolate(frame, [80, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pan
  const panY = interpolate(frame, [0, durationInFrames], [0, -10]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #EEEEEE 0%, #e2e2e2 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', left: '40%', width: 700, height: 700,
        border: '2px solid #06202B', borderRadius: '50%', opacity: 0.05,
        transform: `translate(-50%, 0) scale(${1 + Math.sin(frame / 50) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', right: '-5%', width: 600, height: 600,
        backgroundColor: '#000000', opacity: 0.04,
        clipPath: 'polygon(0% 100%, 100% 0%, 100% 100%)',
      }} />

      {/* Dotted grid texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.15 }}>
        {Array.from({ length: 18 }).map((_, row) =>
          Array.from({ length: 32 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              top: `${(row / 18) * 100}%`,
              left: `${(col / 32) * 100}%`,
              width: 3, height: 3, borderRadius: '50%',
              backgroundColor: '#06202B',
            }} />
          ))
        )}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = seededRandom(i + 240);
        const x = (seed * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 300) * 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `translateY(${panY}px)`,
      }}>
        {/* HEADLINE */}
        <div style={{
          position: 'absolute', top: 60, left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${titleSpring})`,
          opacity: titleSpring,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#06202B', letterSpacing: 5, opacity: 0.7,
          }}>
            INTELLIGENCE NETWORK
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 14,
            letterSpacing: -3,
          }}>
            THE SOURCES
          </div>
        </div>

        {/* CENTER NODE — KUNAL / FF */}
        <div style={{
          position: 'absolute', top: 540, left: '50%',
          transform: `translate(-50%, -50%) scale(${sp(15)})`,
          width: 280, height: 280, borderRadius: '50%',
          backgroundColor: '#06202B',
          display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          boxShadow: '0 0 0 8px rgba(6,32,43,0.15)',
          zIndex: 5,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 21,
            color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
          }}>
            HQ
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 60, fontWeight: 900,
            color: '#EEEEEE', lineHeight: 1, marginTop: 6,
          }}>
            FAILED<br />FINANCE
          </div>
        </div>

        {/* CONNECTION LINES */}
        <svg style={{
          position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
          pointerEvents: 'none',
        }}>
          <line x1="500" y1="540" x2="930" y2="540"
            stroke="#06202B" strokeWidth="3" strokeDasharray="8 6"
            strokeDashoffset={interpolate(lineConnect, [0, 1], [50, 0])}
            opacity={lineConnect} />
          <line x1="990" y1="540" x2="1420" y2="540"
            stroke="#06202B" strokeWidth="3" strokeDasharray="8 6"
            strokeDashoffset={interpolate(lineConnect, [0, 1], [50, 0])}
            opacity={lineConnect} />
          <line x1="960" y1="690" x2="960" y2="850"
            stroke="#000000" strokeWidth="3" strokeDasharray="8 6"
            strokeDashoffset={interpolate(lineConnect, [0, 1], [50, 0])}
            opacity={lineConnect} />
        </svg>

        {/* LEFT NODE — TRADER 01 */}
        <div style={{
          position: 'absolute', top: 540, left: 220,
          transform: `translateY(-50%) translateX(${interpolate(trader1, [0, 1], [-300, 0])}px)`,
          opacity: trader1,
          width: 280, padding: '28px 24px',
          backgroundColor: '#EEEEEE',
          border: '4px solid #06202B',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 19, fontWeight: 700,
            color: '#06202B', opacity: 0.6, letterSpacing: 3,
          }}>
            NODE 01
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 64, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 8,
            letterSpacing: -2,
          }}>
            TRADER
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 27, fontWeight: 600,
            color: '#06202B', marginTop: 12,
          }}>
            Houston, TX
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 18, fontWeight: 500,
            color: '#000000', opacity: 0.6, marginTop: 8,
          }}>
            FORMER COMMODITY
          </div>
        </div>

        {/* RIGHT NODE — TRADER 02 */}
        <div style={{
          position: 'absolute', top: 540, right: 220,
          transform: `translateY(-50%) translateX(${interpolate(trader2, [0, 1], [300, 0])}px)`,
          opacity: trader2,
          width: 280, padding: '28px 24px',
          backgroundColor: '#EEEEEE',
          border: '4px solid #06202B',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 19, fontWeight: 700,
            color: '#06202B', opacity: 0.6, letterSpacing: 3,
          }}>
            NODE 02
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 64, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 8,
            letterSpacing: -2,
          }}>
            TRADER
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 27, fontWeight: 600,
            color: '#06202B', marginTop: 12,
          }}>
            Houston, TX
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 18, fontWeight: 500,
            color: '#000000', opacity: 0.6, marginTop: 8,
          }}>
            FORMER COMMODITY
          </div>
        </div>

        {/* BOTTOM NODE — FED REPORTS */}
        <div style={{
          position: 'absolute', top: 870, left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(fedReveal, [0, 1], [80, 0])}px)`,
          opacity: fedReveal,
          width: 700, padding: '24px 36px',
          backgroundColor: '#000000',
          display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        }}>
          <div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 19, fontWeight: 700,
              color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
            }}>
              NODE 03 / CROSS-REFERENCE
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 51, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 1, marginTop: 4,
              letterSpacing: -1,
            }}>
              FED REGIONAL REPORTS
            </div>
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
            color: '#EEEEEE', letterSpacing: -3,
          }}>
            ALL
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene24_SourcesNetwork;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25_CrudeProductionSpike',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25_CrudeProductionSpike: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(0);
  const chartDraw = interpolate(frame, [25, 170], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const sciFiStamp = sp(150, { stiffness: 200, damping: 7 });

  // Camera zoom into chart
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Chart data — rising curve from 2014 → 2024
  const dataPoints = [50, 58, 55, 62, 78, 88, 75, 92, 110, 128, 155];
  const chartWidth = 1100;
  const chartHeight = 380;
  const chartLeft = 410;
  const chartTop = 480;

  const pathLength = dataPoints.length - 1;
  const visiblePoints = Math.floor(chartDraw * pathLength) + 1;

  let pathD = '';
  let areaD = '';
  for (let i = 0; i < dataPoints.length; i++) {
    const x = chartLeft + (i / pathLength) * chartWidth;
    const y = chartTop + chartHeight - (dataPoints[i] / 160) * chartHeight;
    if (i === 0) {
      pathD += `M ${x} ${y}`;
      areaD += `M ${x} ${chartTop + chartHeight} L ${x} ${y}`;
    } else if (i < visiblePoints) {
      pathD += ` L ${x} ${y}`;
      areaD += ` L ${x} ${y}`;
    } else if (i === visiblePoints) {
      const prevX = chartLeft + ((i - 1) / pathLength) * chartWidth;
      const prevY = chartTop + chartHeight - (dataPoints[i - 1] / 160) * chartHeight;
      const t = chartDraw * pathLength - (i - 1);
      const interpX = prevX + (x - prevX) * t;
      const interpY = prevY + (y - prevY) * t;
      pathD += ` L ${interpX} ${interpY}`;
      areaD += ` L ${interpX} ${interpY}`;
    }
  }
  if (visiblePoints > 0) {
    const lastIdx = Math.min(visiblePoints, dataPoints.length - 1);
    const lastX = chartLeft + (lastIdx / pathLength) * chartWidth;
    areaD += ` L ${lastX} ${chartTop + chartHeight} Z`;
  }

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-15%', right: '-10%', width: 750, height: 750,
        borderRadius: '50%', backgroundColor: '#06202B', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame / 60) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-20%', left: '-10%', width: 600, height: 600,
        border: '3px solid #000000', opacity: 0.05,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Grid texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 0, bottom: 0,
            left: `${(i / 20) * 100}%`,
            width: 1, backgroundColor: '#06202B',
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 425);
        const x = seed * 1920;
        const y = (seededRandom(i + 525) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
      }}>
        {/* TOP TITLE */}
        <div style={{
          position: 'absolute', top: 70, left: 80,
          opacity: titleOp,
          transform: `translateX(${interpolate(titleOp, [0, 1], [-100, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#06202B', letterSpacing: 4, opacity: 0.7,
          }}>
            U.S. CRUDE OIL PRODUCTION
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 8,
            letterSpacing: -3,
          }}>
            THE SPREADSHEET<br />SCREAMS
          </div>
        </div>

        {/* TOP RIGHT KEY STATS */}
        <div style={{
          position: 'absolute', top: 90, right: 100,
          opacity: titleOp,
          textAlign: 'right',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 19,
            color: '#06202B', opacity: 0.6, letterSpacing: 3,
          }}>
            EIA WEEKLY DATA
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
            color: '#06202B', lineHeight: 1, marginTop: 4,
            letterSpacing: -3,
          }}>
            +210%
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 27, fontWeight: 500,
            color: '#000000', opacity: 0.7, marginTop: 4,
          }}>
            vs 2014 baseline
          </div>
        </div>

        {/* CHART */}
        <svg style={{
          position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        }}>
          {/* Grid horizontal */}
          {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
            <line key={i}
              x1={chartLeft} y1={chartTop + chartHeight * p}
              x2={chartLeft + chartWidth} y2={chartTop + chartHeight * p}
              stroke="#06202B" strokeWidth="1" opacity="0.15" strokeDasharray="4 4" />
          ))}
          {/* Axis */}
          <line x1={chartLeft} y1={chartTop} x2={chartLeft} y2={chartTop + chartHeight}
            stroke="#06202B" strokeWidth="2" />
          <line x1={chartLeft} y1={chartTop + chartHeight}
            x2={chartLeft + chartWidth} y2={chartTop + chartHeight}
            stroke="#06202B" strokeWidth="2" />

          {/* Area under curve */}
          <path d={areaD} fill="#06202B" opacity="0.18" />

          {/* Main line */}
          <path d={pathD} stroke="#06202B" strokeWidth="6" fill="none"
            strokeLinecap="round" strokeLinejoin="round" />

          {/* Endpoint dot */}
          {chartDraw > 0.98 && (
            <g>
              <circle
                cx={chartLeft + chartWidth}
                cy={chartTop + chartHeight - (dataPoints[dataPoints.length - 1] / 160) * chartHeight}
                r={14 + Math.sin(frame / 4) * 3}
                fill="#000000" />
              <circle
                cx={chartLeft + chartWidth}
                cy={chartTop + chartHeight - (dataPoints[dataPoints.length - 1] / 160) * chartHeight}
                r={22 + Math.sin(frame / 4) * 4}
                fill="none" stroke="#000000" strokeWidth="3" opacity="0.5" />
            </g>
          )}

          {/* X axis labels */}
          <text x={chartLeft} y={chartTop + chartHeight + 40}
            fontFamily="JetBrains Mono" fontSize="24" fontWeight="700" fill="#06202B">
            2014
          </text>
          <text x={chartLeft + chartWidth - 60} y={chartTop + chartHeight + 40}
            fontFamily="JetBrains Mono" fontSize="24" fontWeight="700" fill="#000000">
            2024
          </text>
          <text x={chartLeft - 50} y={chartTop + 20}
            fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" fill="#06202B"
            transform={`rotate(-90 ${chartLeft - 50} ${chartTop + 20})`}>
            BARRELS/DAY
          </text>
        </svg>

        {/* SCI-FI STAMP */}
        <div style={{
          position: 'absolute', top: chartTop + 80, right: 150,
          transform: `scale(${sciFiStamp}) rotate(-8deg)`,
          opacity: sciFiStamp * 0.9,
          backgroundColor: '#000000',
          padding: '20px 38px',
          border: '4px solid #06202B',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
            color: '#EEEEEE', opacity: 0.6, letterSpacing: 3,
          }}>
            A DECADE AGO →
          </div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 60, fontWeight: 900,
            color: '#EEEEEE', lineHeight: 1, letterSpacing: -1,
          }}>
            SCIENCE FICTION
          </div>
        </div>

        {/* BOTTOM CAPTION */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, right: 0,
          textAlign: 'center',
          opacity: sp(120),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#06202B',
            padding: '12px 40px',
            fontFamily: 'Inter, Poppins', fontSize: 31, fontWeight: 600,
            color: '#EEEEEE', letterSpacing: 2,
          }}>
            Production at levels nobody thought possible
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene25_CrudeProductionSpike;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26_PermianVsOPEC',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26_PermianVsOPEC: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineOp = sp(0);
  const permianBar = interpolate(sp(30), [0, 1], [0, 1]);
  const opecBar1 = interpolate(sp(70), [0, 1], [0, 1]);
  const opecBar2 = interpolate(sp(95), [0, 1], [0, 1]);
  const opecBar3 = interpolate(sp(120), [0, 1], [0, 1]);
  const shakeImpact = sp(40) > 0.5 && frame < 90 ? Math.sin(frame * 2) * 3 : 0;

  return (
    <AbsoluteFill style={{
      backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp,
      transform: `translate(${shakeImpact}px, ${shakeImpact * 0.5}px)`,
    }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #EEEEEE 0%, #d8d8d8 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '5%', width: 500, height: 500,
        border: '2px solid #06202B', opacity: 0.06,
        transform: `rotate(45deg) scale(${1 + Math.sin(frame / 60) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', right: '5%', width: 600, height: 600,
        borderRadius: '50%', backgroundColor: '#000000', opacity: 0.04,
      }} />

      {/* Horizontal lines texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.08 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: 0, right: 0,
            top: `${(i / 18) * 100}%`,
            height: 1, backgroundColor: '#06202B',
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = seededRandom(i + 626);
        const x = seed * 1920;
        const y = (seededRandom(i + 700) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: headlineOp,
        transform: `translateY(${interpolate(headlineOp, [0, 1], [-60, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
          color: '#06202B', letterSpacing: 5, opacity: 0.7,
        }}>
          DAILY OUTPUT — MILLION BARRELS
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
          color: '#000000', lineHeight: 1, marginTop: 10,
          letterSpacing: -3,
        }}>
          PERMIAN <span style={{ color: '#06202B' }}>vs</span> OPEC
        </div>
      </div>

      {/* HORIZONTAL BAR CHART */}
      <div style={{
        position: 'absolute', top: 320, left: 200, right: 200,
      }}>
        {/* PERMIAN BAR */}
        <div style={{ marginBottom: 28, display: 'flex', alignItems: 'center', gap: 30 }}>
          <div style={{
            width: 320, textAlign: 'right',
            fontFamily: 'Mont Heavy', fontSize: 47, fontWeight: 900,
            color: '#000000', letterSpacing: -1,
          }}>
            PERMIAN<br />
            <span style={{ fontSize: 22, fontFamily: 'JetBrains Mono', color: '#06202B', letterSpacing: 2 }}>USA — 1 REGION</span>
          </div>
          <div style={{ flex: 1, position: 'relative' }}>
            <div style={{
              height: 80,
              width: `${permianBar * 100}%`,
              backgroundColor: '#06202B',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              paddingRight: 24,
              position: 'relative',
            }}>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 51, fontWeight: 900,
                color: '#EEEEEE', letterSpacing: -1,
              }}>
                6.4M
              </div>
            </div>
          </div>
        </div>

        {/* OPEC MEMBER 1 - LIBYA */}
        <div style={{ marginBottom: 22, display: 'flex', alignItems: 'center', gap: 30 }}>
          <div style={{
            width: 320, textAlign: 'right',
            fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900,
            color: '#000000',
          }}>
            LIBYA<br />
            <span style={{ fontSize: 18, fontFamily: 'JetBrains Mono', color: '#06202B', opacity: 0.7, letterSpacing: 2 }}>OPEC MEMBER</span>
          </div>
          <div style={{ flex: 1 }}>
            <div style={{
              height: 60,
              width: `${opecBar1 * 18}%`,
              backgroundColor: '#000000',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              paddingRight: 20,
            }}>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 32, fontWeight: 900,
                color: '#EEEEEE',
              }}>
                1.2M
              </div>
            </div>
          </div>
        </div>

        {/* OPEC MEMBER 2 - ALGERIA */}
        <div style={{ marginBottom: 22, display: 'flex', alignItems: 'center', gap: 30 }}>
          <div style={{
            width: 320, textAlign: 'right',
            fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900,
            color: '#000000',
          }}>
            ALGERIA<br />
            <span style={{ fontSize: 18, fontFamily: 'JetBrains Mono', color: '#06202B', opacity: 0.7, letterSpacing: 2 }}>OPEC MEMBER</span>
          </div>
          <div style={{ flex: 1 }}>
            <div style={{
              height: 60,
              width: `${opecBar2 * 14}%`,
              backgroundColor: '#000000',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              paddingRight: 20,
            }}>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 32, fontWeight: 900,
                color: '#EEEEEE',
              }}>
                0.9M
              </div>
            </div>
          </div>
        </div>

        {/* OPEC MEMBER 3 - VENEZUELA */}
        <div style={{ marginBottom: 22, display: 'flex', alignItems: 'center', gap: 30 }}>
          <div style={{
            width: 320, textAlign: 'right',
            fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900,
            color: '#000000',
          }}>
            VENEZUELA<br />
            <span style={{ fontSize: 18, fontFamily: 'JetBrains Mono', color: '#06202B', opacity: 0.7, letterSpacing: 2 }}>OPEC MEMBER</span>
          </div>
          <div style={{ flex: 1 }}>
            <div style={{
              height: 60,
              width: `${opecBar3 * 12}%`,
              backgroundColor: '#000000',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              paddingRight: 20,
            }}>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 32, fontWeight: 900,
                color: '#EEEEEE',
              }}>
                0.8M
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* BOTTOM TAKEAWAY */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(140),
        transform: `translateY(${interpolate(sp(140), [0, 1], [40, 0])}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          padding: '16px 50px',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 51, fontWeight: 900,
            color: '#EEEEEE', letterSpacing: -1,
          }}>
            ONE BASIN. BIGGER THAN NATIONS.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene26_PermianVsOPEC;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27_HiddenReserves',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27_HiddenReserves: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineOp = sp(0);
  const pieAnim = interpolate(frame, [25, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const hiddenChunk = sp(115);
  const reclassifyTag = sp(150, { stiffness: 200, damping: 8 });

  // Pan
  const panX = interpolate(frame, [0, durationInFrames], [-8, 8]);

  // Pie segments
  const reportedPct = 0.78;
  const hiddenPct = 0.22;
  const cx = 580;
  const cy = 580;
  const r = 220;

  const reportedAngle = pieAnim * Math.PI * 2 * reportedPct;
  const hiddenAngle = pieAnim * Math.PI * 2 * (reportedPct + hiddenPct);

  const polarToCartesian = (centerX: number, centerY: number, radius: number, angle: number) => {
    return {
      x: centerX + radius * Math.cos(angle - Math.PI / 2),
      y: centerY + radius * Math.sin(angle - Math.PI / 2),
    };
  };

  const describeArc = (start: number, end: number) => {
    const startP = polarToCartesian(cx, cy, r, start);
    const endP = polarToCartesian(cx, cy, r, end);
    const largeArc = end - start > Math.PI ? 1 : 0;
    return `M ${cx} ${cy} L ${startP.x} ${startP.y} A ${r} ${r} 0 ${largeArc} 1 ${endP.x} ${endP.y} Z`;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 50%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 65%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '-5%', width: 600, height: 600,
        border: '2px solid #06202B', borderRadius: '50%', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame / 70) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '40%', width: 700, height: 700,
        backgroundColor: '#000000', opacity: 0.04,
        transform: `rotate(${frame * 0.1}deg)`,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
      }} />

      {/* Crosshatch texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.06 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: -100, left: `${i * 60 - 200}px`,
            width: 1, height: 1500, backgroundColor: '#06202B',
            transform: `rotate(45deg)`,
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 827);
        const x = seed * 1920;
        const y = (seededRandom(i + 900) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `translateX(${panX}px)`,
      }}>
        {/* HEADLINE TOP */}
        <div style={{
          position: 'absolute', top: 70, left: 80,
          opacity: headlineOp,
          transform: `translateY(${interpolate(headlineOp, [0, 1], [-50, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#06202B', letterSpacing: 4, opacity: 0.7,
          }}>
            U.S. CRUDE OUTPUT BREAKDOWN
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 102, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 8,
            letterSpacing: -3,
          }}>
            THE MISSING<br />SLICE
          </div>
        </div>

        {/* PIE CHART */}
        <svg style={{
          position: 'absolute', top: 0, left: 0, width: '100%', height: '100%',
        }}>
          {/* Reported segment */}
          {pieAnim > 0 && (
            <path d={describeArc(0, Math.min(reportedAngle, Math.PI * 2 * reportedPct))}
              fill="#06202B" />
          )}
          {/* Hidden segment */}
          {pieAnim > reportedPct && (
            <path d={describeArc(
              Math.PI * 2 * reportedPct,
              Math.min(hiddenAngle, Math.PI * 2)
            )}
              fill="#000000"
              transform={`translate(${hiddenChunk * 30}, ${-hiddenChunk * 30})`} />
          )}
          {/* Center hole */}
          <circle cx={cx} cy={cy} r={100} fill="#EEEEEE" />
          {/* Center label */}
          <text x={cx} y={cy - 5} textAnchor="middle"
            fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#06202B" opacity="0.7"
            letterSpacing="2">
            TOTAL
          </text>
          <text x={cx} y={cy + 30} textAnchor="middle"
            fontFamily="Mont Heavy" fontSize="44" fontWeight="900" fill="#000000">
            OUTPUT
          </text>

          {/* Dashed connector for hidden chunk */}
          {hiddenChunk > 0.3 && (
            <line x1={cx + 230} y1={cy - 110} x2={cx + 410} y2={cy - 180}
              stroke="#000000" strokeWidth="2.5" strokeDasharray="8 6" opacity={hiddenChunk} />
          )}
        </svg>

        {/* RIGHT SIDE — LEGEND + BREAKDOWN */}
        <div style={{
          position: 'absolute', top: 360, right: 100,
          width: 700,
        }}>
          {/* Reported */}
          <div style={{
            opacity: pieAnim,
            display: 'flex', alignItems: 'center', gap: 24,
            marginBottom: 24,
          }}>
            <div style={{
              width: 60, height: 60, backgroundColor: '#06202B',
            }} />
            <div>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 64, fontWeight: 900,
                color: '#000000', lineHeight: 1, letterSpacing: -2,
              }}>
                78%
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 27, fontWeight: 500,
                color: '#06202B', marginTop: 2,
              }}>
                Reported in weekly EIA prints
              </div>
            </div>
          </div>

          {/* Hidden */}
          <div style={{
            opacity: hiddenChunk,
            transform: `translateX(${interpolate(hiddenChunk, [0, 1], [60, 0])}px)`,
            display: 'flex', alignItems: 'center', gap: 24,
            marginBottom: 32,
          }}>
            <div style={{
              width: 60, height: 60, backgroundColor: '#000000',
            }} />
            <div>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
                color: '#000000', lineHeight: 1, letterSpacing: -3,
              }}>
                22%
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 30, fontWeight: 700,
                color: '#000000', marginTop: 2,
              }}>
                The chunk that doesn't show up cleanly
              </div>
            </div>
          </div>

          {/* RECLASSIFIED TAG */}
          <div style={{
            transform: `scale(${reclassifyTag}) rotate(-3deg)`,
            opacity: reclassifyTag,
            display: 'inline-block',
            backgroundColor: '#06202B',
            padding: '18px 32px',
            border: '4px solid #000000',
            marginTop: 10,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 20, fontWeight: 700,
              color: '#EEEEEE', opacity: 0.6, letterSpacing: 3,
            }}>
              MECHANISM →
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 47, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 1, letterSpacing: -1,
              marginTop: 6,
            }}>
              STRATEGIC RESERVES<br />RECLASSIFIED
            </div>
          </div>
        </div>

        {/* BOTTOM BAR */}
        <div style={{
          position: 'absolute', bottom: 60, left: 80, right: 80,
          padding: '14px 30px',
          backgroundColor: '#000000',
          opacity: sp(160),
          display: 'flex', justifyContent: 'space-between', alignItems: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
            color: '#EEEEEE', letterSpacing: 3,
          }}>
            ◆ NOT ON THE PUBLIC SPREADSHEET
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 26, fontWeight: 600,
            color: '#EEEEEE', opacity: 0.7,
          }}>
            Source: EIA classification rules
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene27_HiddenReserves;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28_GulfCoastSecretOil',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28_GulfCoastSecretOil: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(0);
  const flow1 = interpolate(frame, [20, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const flow2 = interpolate(frame, [50, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const secretReveal = sp(140, { stiffness: 200, damping: 8 });

  // Camera tilt
  const tilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #EEEEEE 0%, #d5d5d5 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', left: '-10%', width: 600, height: 600,
        backgroundColor: '#06202B', opacity: 0.05,
        clipPath: 'polygon(0% 0%, 100% 0%, 0% 100%)',
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', right: '-10%', width: 700, height: 700,
        border: '2px solid #000000', borderRadius: '50%', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame / 60) * 0.05})`,
      }} />

      {/* Wave-like texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.07 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: 0, right: 0,
            top: `${10 + i * 12}%`, height: 1,
            backgroundColor: '#06202B',
            transform: `scaleY(${1 + Math.sin(frame / 30 + i) * 0.5})`,
          }} />
        ))}
      </div>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 1028);
        const x = (seed * 1920 + frame * 0.6) % 1920;
        const y = seededRandom(i + 1100) * 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `rotate(${tilt}deg)`,
        transformOrigin: 'center',
      }}>
        {/* HEADLINE */}
        <div style={{
          position: 'absolute', top: 60, left: 0, right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${interpolate(titleOp, [0, 1], [-50, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#06202B', letterSpacing: 5, opacity: 0.7,
          }}>
            GULF COAST EXPORT INFRASTRUCTURE
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 119, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 10,
            letterSpacing: -3,
          }}>
            THE TERMINAL LOOPHOLE
          </div>
        </div>

        {/* FLOW DIAGRAM */}
        <div style={{
          position: 'absolute', top: 330, left: 0, right: 0,
          display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 0,
        }}>
          {/* WELL */}
          <div style={{
            width: 280, height: 280,
            backgroundColor: '#06202B',
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            opacity: flow1,
            transform: `scale(${interpolate(flow1, [0, 1], [0.7, 1])})`,
          }}>
            <svg width="100" height="100" viewBox="0 0 100 100">
              <rect x="42" y="20" width="16" height="50" fill="#EEEEEE" />
              <rect x="20" y="65" width="60" height="20" fill="#EEEEEE" />
              <circle cx="50" cy="80" r="6" fill="#06202B" />
            </svg>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900,
              color: '#EEEEEE', marginTop: 10, letterSpacing: -1,
            }}>
              WELL
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 18,
              color: '#EEEEEE', opacity: 0.6, letterSpacing: 2,
            }}>
              PERMIAN
            </div>
          </div>

          {/* PIPE 1 */}
          <div style={{
            width: 180, height: 40,
            backgroundColor: '#000000',
            position: 'relative',
            transform: `scaleX(${flow1})`,
            transformOrigin: 'left',
          }}>
            {/* Flowing dots */}
            {Array.from({ length: 4 }).map((_, i) => {
              const dx = ((frame * 3 + i * 45) % 180);
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: dx, top: 14,
                  width: 12, height: 12, borderRadius: '50%',
                  backgroundColor: '#06202B',
                }} />
              );
            })}
          </div>

          {/* TERMINAL */}
          <div style={{
            width: 280, height: 280,
            backgroundColor: '#EEEEEE',
            border: '5px solid #000000',
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            opacity: flow1,
            position: 'relative',
          }}>
            <svg width="100" height="100" viewBox="0 0 100 100">
              <rect x="15" y="35" width="25" height="50" fill="#06202B" rx="2" />
              <rect x="45" y="25" width="25" height="60" fill="#06202B" rx="2" />
              <rect x="75" y="40" width="20" height="45" fill="#06202B" rx="2" />
              <line x1="5" y1="85" x2="100" y2="85" stroke="#000000" strokeWidth="3" />
            </svg>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 32, fontWeight: 900,
              color: '#000000', marginTop: 10, letterSpacing: -1,
            }}>
              TERMINAL
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 18,
              color: '#06202B', letterSpacing: 2,
            }}>
              GULF COAST
            </div>
            {/* Classified band */}
            <div style={{
              position: 'absolute', top: 18, left: -8, right: -8,
              backgroundColor: '#000000',
              padding: '6px 0',
              fontFamily: 'JetBrains Mono', fontSize: 16, fontWeight: 700,
              color: '#EEEEEE', textAlign: 'center', letterSpacing: 4,
              transform: 'rotate(-3deg)',
            }}>
              CONTRACT RESTRUCTURE
            </div>
          </div>

          {/* PIPE 2 */}
          <div style={{
            width: 180, height: 40,
            backgroundColor: '#000000',
            position: 'relative',
            transform: `scaleX(${flow2})`,
            transformOrigin: 'left',
          }}>
            {Array.from({ length: 4 }).map((_, i) => {
              const dx = ((frame * 3 + i * 45) % 180);
              return (
                <div key={i} style={{
                  position: 'absolute',
                  left: dx, top: 14,
                  width: 12, height: 12, borderRadius: '50%',
                  backgroundColor: '#06202B',
                }} />
              );
            })}
          </div>

          {/* EXPORT */}
          <div style={{
            width: 280, height: 280,
            backgroundColor: '#000000',
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            opacity: flow2,
            transform: `scale(${interpolate(flow2, [0, 1], [0.7, 1])})`,
          }}>
            <svg width="100" height="100" viewBox="0 0 100 100">
              <path d="M 10 70 L 90 70 L 80 90 L 20 90 Z" fill="#EEEEEE" />
              <rect x="40" y="40" width="20" height="30" fill="#EEEEEE" />
              <line x1="50" y1="20" x2="50" y2="40" stroke="#EEEEEE" strokeWidth="3" />
              <path d="M 50 25 L 70 35 L 50 35 Z" fill="#EEEEEE" />
            </svg>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 36, fontWeight: 900,
              color: '#EEEEEE', marginTop: 10, letterSpacing: -1,
            }}>
              EXPORT
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 18,
              color: '#EEEEEE', opacity: 0.6, letterSpacing: 2,
            }}>
              OFF THE BOOKS
            </div>
          </div>
        </div>

        {/* SECRET OIL REVEAL */}
        <div style={{
          position: 'absolute', bottom: 90, left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${secretReveal})`,
          opacity: secretReveal,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#06202B',
            padding: '20px 50px',
            border: '5px solid #000000',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22,
              color: '#EEEEEE', opacity: 0.5, letterSpacing: 4,
            }}>
              THE PRESIDENT'S CLAIM →
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 77, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 1, letterSpacing: -2, marginTop: 4,
            }}>
              "SECRET OIL" — NOT EXACTLY LYING
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene28_GulfCoastSecretOil;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29_TomClancyFraming',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29_TomClancyFraming: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const realityOp = sp(0);
  const arrow = sp(60);
  const novelOp = sp(90);
  const verdictStamp = sp(150, { stiffness: 200, damping: 7 });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(6,32,43,0.05) 0%, rgba(238,238,238,0) 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '15%', left: '20%', width: 450, height: 450,
        border: '2px solid #06202B', opacity: 0.06,
        transform: `rotate(${30 + frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '15%', width: 500, height: 500,
        backgroundColor: '#000000', opacity: 0.04, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame / 50) * 0.06})`,
      }} />

      {/* Page-rule texture (like book lines) */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.08 }}>
        {Array.from({ length: 22 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: 0, right: 0,
            top: `${(i / 22) * 100}%`,
            height: 1, backgroundColor: '#06202B',
          }} />
        ))}
      </div>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = seededRandom(i + 1229);
        const x = seed * 1920;
        const y = (seededRandom(i + 1300) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      <div style={{
        position: 'absolute', inset: 0,
        transform: `scale(${camScale})`,
      }}>
        {/* HEADLINE */}
        <div style={{
          position: 'absolute', top: 70, left: 0, right: 0,
          textAlign: 'center',
          opacity: sp(0),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#06202B', letterSpacing: 5, opacity: 0.7,
          }}>
            THE RHETORICAL DEVICE
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 110, fontWeight: 900,
            color: '#000000', lineHeight: 1, marginTop: 10,
            letterSpacing: -3,
          }}>
            LOGISTICS DRESSED<br />AS A THRILLER
          </div>
        </div>

        {/* LEFT — REALITY */}
        <div style={{
          position: 'absolute', top: 470, left: 130,
          width: 580,
          opacity: realityOp,
          transform: `translateX(${interpolate(realityOp, [0, 1], [-100, 0])}px)`,
        }}>
          <div style={{
            backgroundColor: '#06202B',
            padding: '14px 28px',
            display: 'inline-block',
            marginBottom: 18,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
              color: '#EEEEEE', letterSpacing: 3,
            }}>
              [ REALITY ]
            </div>
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 60, fontWeight: 900,
            color: '#000000', lineHeight: 1, letterSpacing: -2,
          }}>
            BORING LOGISTICS
          </div>
          <div style={{
            marginTop: 24,
            padding: '24px 28px',
            backgroundColor: '#EEEEEE',
            border: '3px solid #06202B',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 21, fontWeight: 600,
              color: '#06202B', lineHeight: 1.6,
            }}>
              <div>• Export contract reclassification</div>
              <div>• Strategic reserve accounting</div>
              <div>• Terminal pipeline routing</div>
              <div>• EIA reporting timing gaps</div>
            </div>
          </div>
        </div>

        {/* ARROW TRANSFORM */}
        <div style={{
          position: 'absolute', top: 580, left: '50%',
          transform: `translateX(-50%) scale(${arrow})`,
          opacity: arrow,
        }}>
          <svg width="140" height="100" viewBox="0 0 140 100">
            <line x1="10" y1="50" x2="115" y2="50" stroke="#000000" strokeWidth="8" strokeLinecap="round" />
            <polygon points="115,30 135,50 115,70" fill="#000000" />
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 19, fontWeight: 700,
            color: '#06202B', textAlign: 'center', letterSpacing: 3,
            marginTop: 4,
          }}>
            FRAMED AS
          </div>
        </div>

        {/* RIGHT — NOVEL */}
        <div style={{
          position: 'absolute', top: 470, right: 130,
          width: 580,
          opacity: novelOp,
          transform: `translateX(${interpolate(novelOp, [0, 1], [100, 0])}px)`,
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '14px 28px',
            display: 'inline-block',
            marginBottom: 18,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
              color: '#EEEEEE', letterSpacing: 3,
            }}>
              [ THE FRAMING ]
            </div>
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 60, fontWeight: 900,
            color: '#000000', lineHeight: 1, letterSpacing: -2,
          }}>
            TOM CLANCY NOVEL
          </div>
          <div style={{
            marginTop: 24,
            padding: '24px 28px',
            backgroundColor: '#06202B',
            position: 'relative',
            overflow: 'hidden',
          }}>
            <div style={{
              position: 'absolute', top: -10, right: -10,
              fontFamily: 'Mont Heavy', fontSize: 187, fontWeight: 900,
              color: '#EEEEEE', opacity: 0.08, letterSpacing: -5,
            }}>
              "
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 47, fontWeight: 900,
              color: '#EEEEEE', lineHeight: 1.1, letterSpacing: -1,
            }}>
              "SECRET<br />OIL"
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 22, fontWeight: 500,
              color: '#EEEEEE', opacity: 0.8, marginTop: 14,
            }}>
              One word. Maximum drama.
            </div>
          </div>
        </div>

        {/* VERDICT STAMP */}
        <div style={{
          position: 'absolute', bottom: 70, left: '50%',
          transform: `translateX(-50%) scale(${verdictStamp}) rotate(-1deg)`,
          opacity: verdictStamp,
          backgroundColor: '#EEEEEE',
          border: '5px solid #000000',
          padding: '14px 50px',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 47, fontWeight: 900,
            color: '#000000', letterSpacing: -1,
          }}>
            VERDICT: WORD CHOICE = WEAPON
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene29_TomClancyFraming;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30_GasDownCPIUp',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30_GasDownCPIUp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headlineOp = sp(0);
  const leftDraw = interpolate(frame, [25, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const rightDraw = interpolate(frame, [50, 155], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const splitReveal = sp(170, { stiffness: 200, damping: 8 });

  // Gas DOWN data points
  const gasData = [85, 80, 75, 78, 70, 65, 58, 52];
  // CPI UP data points
  const cpiData = [60, 65, 70, 68, 75, 82, 90, 98];

  const buildPath = (data: number[], left: number, top: number, w: number, h: number, progress: number) => {
    const max = 100;
    let d = '';
    const pts = data.length;
    const visible = progress * (pts - 1);
    for (let i = 0; i < pts; i++) {
      const x = left + (i / (pts - 1)) * w;
      const y = top + h - (data[i] / max) * h;
      if (i === 0) d += `M ${x} ${y}`;
      else if (i <= Math.floor(visible)) d += ` L ${x} ${y}`;
      else if (i === Math.floor(visible) + 1) {
        const prevX = left + ((i - 1) / (pts - 1)) * w;
        const prevY = top + h - (data[i - 1] / max) * h;
        const t = visible - (i - 1);
        d += ` L ${prevX + (x - prevX) * t} ${prevY + (y - prevY) * t}`;
      }
    }
    return d;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(6,32,43,0.06) 0%, rgba(238,238,238,0) 50%, rgba(0,0,0,0.06) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '-5%', width: 450, height: 450,
        backgroundColor: '#06202B', borderRadius: '50%', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', top: '20%', right: '-5%', width: 450, height: 450,
        backgroundColor: '#000000', borderRadius: '50%', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '50%', width: 300, height: 300,
        border: '2px solid #06202B', opacity: 0.06,
        transform: `translate(-50%, -50%) rotate(${frame * 0.3}deg)`,
      }} />

      {/* Grid texture */}
      <div style={{ position: 'absolute', inset: 0, opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 0, bottom: 0,
            left: `${(i / 20) * 100}%`,
            width: 1, backgroundColor: '#06202B',
          }} />
        ))}
      </div>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 1430);
        const x = seed * 1920;
        const y = (seededRandom(i + 1500) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={`p${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#06202B', opacity: 0.2,
          }} />
        );
      })}

      {/* TOP HEADLINE */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: headlineOp,
        transform: `translateY(${interpolate(headlineOp, [0, 1], [-50, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
          color: '#06202B', letterSpacing: 5, opacity: 0.7,
        }}>
          THE STRANGEST SPLIT IN 20 YEARS
        </div>
        <div style={{
          fontFamily: 'Mont Heavy, Nexa Bold', fontSize: 110, fontWeight: 900,
          color: '#000000', lineHeight: 1, marginTop: 10,
          letterSpacing: -3,
        }}>
          PUMP <span style={{ color: '#06202B' }}>↓</span>  CPI <span style={{ color: '#000000' }}>↑</span>
        </div>
      </div>

      {/* DUAL CHARTS */}
      <div style={{
        position: 'absolute', top: 320, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 60,
      }}>
        {/* LEFT — GAS DOWN */}
        <div style={{
          width: 720, height: 480,
          backgroundColor: '#06202B',
          padding: '32px 36px',
          opacity: leftDraw > 0 ? 1 : 0,
          transform: `translateX(${interpolate(leftDraw, [0, 1], [-100, 0])}px)`,
        }}>
          <div style={{
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
            marginBottom: 14,
          }}>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 20, fontWeight: 700,
                color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
              }}>
                METRIC A
              </div>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 60, fontWeight: 900,
                color: '#EEEEEE', lineHeight: 1, letterSpacing: -2, marginTop: 2,
              }}>
                GAS / PUMP
              </div>
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
              color: '#EEEEEE', letterSpacing: -3, lineHeight: 1,
            }}>
              ↓
            </div>
          </div>
          <svg width="100%" height="280" viewBox="0 0 640 280">
            {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
              <line key={i} x1="0" y1={p * 260 + 10} x2="640" y2={p * 260 + 10}
                stroke="#EEEEEE" strokeWidth="1" opacity="0.15" strokeDasharray="4 4" />
            ))}
            <path d={buildPath(gasData, 0, 10, 640, 260, leftDraw)}
              stroke="#EEEEEE" strokeWidth="6" fill="none"
              strokeLinecap="round" strokeLinejoin="round" />
            {leftDraw > 0.95 && (
              <circle
                cx={640}
                cy={10 + 260 - (gasData[gasData.length - 1] / 100) * 260}
                r={12 + Math.sin(frame / 4) * 3}
                fill="#EEEEEE" />
            )}
          </svg>
          <div style={{
            fontFamily: 'Inter', fontSize: 27, fontWeight: 600,
            color: '#EEEEEE', opacity: 0.85, marginTop: 8,
          }}>
            Drifting down at the corner station
          </div>
        </div>

        {/* RIGHT — CPI UP */}
        <div style={{
          width: 720, height: 480,
          backgroundColor: '#000000',
          padding: '32px 36px',
          opacity: rightDraw > 0 ? 1 : 0,
          transform: `translateX(${interpolate(rightDraw, [0, 1], [100, 0])}px)`,
        }}>
          <div style={{
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
            marginBottom: 14,
          }}>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 20, fontWeight: 700,
                color: '#EEEEEE', opacity: 0.5, letterSpacing: 3,
              }}>
                METRIC B
              </div>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 60, fontWeight: 900,
                color: '#EEEEEE', lineHeight: 1, letterSpacing: -2, marginTop: 2,
              }}>
                BROADER CPI
              </div>
            </div>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 102, fontWeight: 900,
              color: '#EEEEEE', letterSpacing: -3, lineHeight: 1,
            }}>
              ↑
            </div>
          </div>
          <svg width="100%" height="280" viewBox="0 0 640 280">
            {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
              <line key={i} x1="0" y1={p * 260 + 10} x2="640" y2={p * 260 + 10}
                stroke="#EEEEEE" strokeWidth="1" opacity="0.15" strokeDasharray="4 4" />
            ))}
            <path d={buildPath(cpiData, 0, 10, 640, 260, rightDraw)}
              stroke="#EEEEEE" strokeWidth="6" fill="none"
              strokeLinecap="round" strokeLinejoin="round" />
            {rightDraw > 0.95 && (
              <circle
                cx={640}
                cy={10 + 260 - (cpiData[cpiData.length - 1] / 100) * 260}
                r={12 + Math.sin(frame / 4) * 3}
                fill="#EEEEEE" />
            )}
          </svg>
          <div style={{
            fontFamily: 'Inter', fontSize: 27, fontWeight: 600,
            color: '#EEEEEE', opacity: 0.85, marginTop: 8,
          }}>
            Climbing on everything else
          </div>
        </div>
      </div>

      {/* BOTTOM REVEAL */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: splitReveal,
        transform: `translateY(${interpolate(splitReveal, [0, 1], [40, 0])}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#EEEEEE',
          border: '5px solid #06202B',
          padding: '18px 60px',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 56, fontWeight: 900,
            color: '#000000', letterSpacing: -1,
          }}>
            ENERGY DOWN. EVERYTHING ELSE UP.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene30_GasDownCPIUp;
```

