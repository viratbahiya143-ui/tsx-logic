```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title intro
  const titleY = interpolate(sp(5), [0, 1], [-60, 0]);
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);

  // Stamp impact
  const stampScale = sp(55, { damping: 8, stiffness: 180 });
  const stampRot = interpolate(sp(55), [0, 1], [-18, -8]);

  // Myth strike-through
  const strikeW = interpolate(frame, [95, 125], [0, 920], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Truth reveal
  const truthOp = interpolate(frame, [160, 185], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const truthY = interpolate(sp(160), [0, 1], [40, 0]);

  // Arrow flow state -> federal
  const arrowDash = interpolate(frame, [220, 270], [400, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Radial gradient base */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 45%, #4a505a 0%, #393E46 55%, #2a2e34 100%)',
        }}
      />

      {/* Drifting shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => {
          const r = seededRandom(i + 1);
          const r2 = seededRandom(i + 9);
          const size = 280 + r * 320;
          const x = r * 1700;
          const y = r2 * 900;
          const drift = Math.sin(frame * 0.012 + i) * 28;
          const rot = frame * 0.08 + i * 45;
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: x + drift,
                top: y + Math.cos(frame * 0.01 + i) * 20,
                width: size,
                height: size,
                border: `2px solid #609966`,
                borderRadius: i % 2 === 0 ? '50%' : '20px',
                opacity: 0.05,
                transform: `rotate(${rot}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="grid21" width="80" height="80" patternUnits="userSpaceOnUse">
              <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#93B5C6" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#grid21)" />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const r = seededRandom(i + 50);
          const r2 = seededRandom(i + 77);
          const px = (r * 1920 + frame * 0.6) % 1920;
          const py = r2 * 1080 + Math.sin(frame * 0.02 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 4,
                height: 4,
                borderRadius: '50%',
                backgroundColor: '#93B5C6',
                opacity: 0.35,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main camera frame */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* TOP TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `translateY(${titleY}px)`,
            opacity: titleOp,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 40px',
              border: '2px solid #609966',
              borderRadius: 6,
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              color: '#609966',
              letterSpacing: 6,
              fontWeight: 600,
            }}
          >
            PROGRAM 03  //  THE ULTIMATE HACK
          </div>
        </div>

        {/* STAMP — MYTH BUSTED */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            right: 120,
            transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
          }}
        >
          <div
            style={{
              border: '6px solid #ff0000',
              padding: '14px 36px',
              fontFamily: 'Inter, sans-serif',
              fontWeight: 900,
              fontSize: 44,
              color: '#ff0000',
              letterSpacing: 4,
              backgroundColor: 'rgba(255,0,0,0.06)',
            }}
          >
            MYTH
          </div>
        </div>

        {/* CENTER — Myth statement with strike */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(40), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 64,
              fontWeight: 700,
              color: '#93B5C6',
              position: 'relative',
              display: 'inline-block',
              padding: '0 30px',
            }}
          >
            "Medicaid = Nursing Home"
            <div
              style={{
                position: 'absolute',
                top: '52%',
                left: 30,
                height: 6,
                width: strikeW,
                backgroundColor: '#ff0000',
                borderRadius: 3,
              }}
            />
          </div>
        </div>

        {/* TRUTH REVEAL BLOCK */}
        <div
          style={{
            position: 'absolute',
            top: 440,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: truthOp,
            transform: `translateY(${truthY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              color: '#609966',
              letterSpacing: 8,
              marginBottom: 18,
            }}
          >
            ▶ THE REALITY
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 95,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.05,
              letterSpacing: -1,
            }}
          >
            YOUR STATE PAYS<br />
            <span style={{ color: '#609966' }}>FEDERAL MEDICARE</span>
          </div>
        </div>

        {/* FLOW DIAGRAM — STATE → FEDERAL */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 0,
            opacity: interpolate(frame, [210, 240], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          {/* STATE box */}
          <div
            style={{
              width: 280,
              padding: '24px 20px',
              border: '3px solid #93B5C6',
              borderRadius: 12,
              textAlign: 'center',
              backgroundColor: 'rgba(147,181,198,0.08)',
            }}
          >
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#93B5C6', letterSpacing: 4 }}>SOURCE</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 48, fontWeight: 900, color: '#F5F5F5', marginTop: 6 }}>
              STATE
            </div>
          </div>

          {/* Arrow with flowing dash */}
          <div style={{ width: 400, height: 80, position: 'relative' }}>
            <svg width="400" height="80" style={{ overflow: 'visible' }}>
              <line
                x1="10"
                y1="40"
                x2="370"
                y2="40"
                stroke="#609966"
                strokeWidth="5"
                strokeDasharray="16 10"
                strokeDashoffset={arrowDash}
              />
              <polygon points="370,30 395,40 370,50" fill="#609966" />
              <text
                x="200"
                y="22"
                textAnchor="middle"
                fill="#FFC107"
                fontFamily="JetBrains Mono, monospace"
                fontSize="22"
                fontWeight="700"
              >
                PAYS $$$
              </text>
            </svg>
          </div>

          {/* FEDERAL box */}
          <div
            style={{
              width: 320,
              padding: '24px 20px',
              border: '3px solid #609966',
              borderRadius: 12,
              textAlign: 'center',
              backgroundColor: 'rgba(96,153,102,0.12)',
            }}
          >
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#609966', letterSpacing: 4 }}>RECEIVER</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 48, fontWeight: 900, color: '#F5F5F5', marginTop: 6 }}>
              MEDICARE
            </div>
          </div>
        </div>

        {/* Bottom tag */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [280, 320], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#93B5C6',
            letterSpacing: 3,
            fontWeight: 500,
          }}
        >
          You keep your house. You keep your dignity.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene21;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Family categories
  const cats = ['QMB', 'SLMB', 'QI', 'QDWI'];

  // QMB highlight burst
  const qmbScale = sp(110, { damping: 7, stiffness: 160 });

  // Threshold meter
  const meterFill = interpolate(frame, [200, 270], [0, 0.62], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.3, 0.3]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Diagonal gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #2f343b 0%, #393E46 45%, #454c55 100%)',
        }}
      />

      {/* Shape layer — triangles */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const r = seededRandom(i + 33);
          const drift = Math.sin(frame * 0.01 + i) * 24;
          return (
            <svg
              key={`tri-${i}`}
              width="380"
              height="380"
              style={{
                position: 'absolute',
                left: r * 1600 + drift,
                top: seededRandom(i + 11) * 850,
                opacity: 0.05,
                transform: `rotate(${frame * 0.06 + i * 90}deg)`,
              }}
            >
              <polygon points="190,20 360,360 20,360" fill="none" stroke="#93B5C6" strokeWidth="2" />
            </svg>
          );
        })}
      </AbsoluteFill>

      {/* Diagonal line texture */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="diag22" width="40" height="40" patternUnits="userSpaceOnUse">
              <path d="M 0 40 L 40 0" stroke="#609966" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#diag22)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const r = seededRandom(i + 200);
          const px = (r * 1920 + frame * 0.4) % 1920;
          const py = seededRandom(i + 300) * 1080 + Math.cos(frame * 0.025 + i) * 15;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#609966',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              color: '#93B5C6',
              letterSpacing: 8,
              fontWeight: 500,
              marginBottom: 12,
            }}
          >
            INSIDE THE MEDICAID FAMILY
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 88,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -1,
              lineHeight: 1,
            }}
          >
            MEDICARE <span style={{ color: '#609966' }}>SAVINGS</span> PROGRAMS
          </div>
        </div>

        {/* 4 CATEGORY CARDS */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 24,
          }}
        >
          {cats.map((c, i) => {
            const cardSp = sp(30 + i * 12);
            const isQmb = c === 'QMB';
            const highlight = isQmb ? qmbScale : 1;
            const cardY = interpolate(cardSp, [0, 1], [80, 0]);
            const cardOp = interpolate(cardSp, [0, 1], [0, 1]);
            return (
              <div
                key={c}
                style={{
                  width: 280,
                  height: 240,
                  border: `3px solid ${isQmb ? '#609966' : '#93B5C6'}`,
                  borderRadius: 14,
                  backgroundColor: isQmb ? 'rgba(96,153,102,0.18)' : 'rgba(147,181,198,0.05)',
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'center',
                  alignItems: 'center',
                  transform: `translateY(${cardY}px) scale(${isQmb ? highlight : 1})`,
                  opacity: cardOp,
                  boxShadow: isQmb ? '0 0 60px rgba(96,153,102,0.4)' : 'none',
                  position: 'relative',
                }}
              >
                {isQmb && (
                  <div
                    style={{
                      position: 'absolute',
                      top: -22,
                      backgroundColor: '#FFC107',
                      color: '#1C1C1C',
                      padding: '6px 18px',
                      borderRadius: 20,
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 18,
                      fontWeight: 700,
                      letterSpacing: 2,
                      opacity: interpolate(frame, [110, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                    }}
                  >
                    MOST POWERFUL
                  </div>
                )}
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: isQmb ? 92 : 72,
                    fontWeight: 900,
                    color: isQmb ? '#609966' : '#F5F5F5',
                    letterSpacing: 2,
                  }}
                >
                  {c}
                </div>
                <div
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 16,
                    color: '#9E9E9E',
                    marginTop: 8,
                    letterSpacing: 2,
                  }}
                >
                  TIER 0{i + 1}
                </div>
              </div>
            );
          })}
        </div>

        {/* QUALIFICATION METER */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 240,
            right: 240,
            opacity: interpolate(frame, [180, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              marginBottom: 14,
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#93B5C6',
              letterSpacing: 3,
            }}
          >
            <span>INCOME + RESOURCES</span>
            <span style={{ color: '#609966' }}>BELOW THRESHOLD ✓</span>
          </div>
          <div
            style={{
              width: '100%',
              height: 44,
              backgroundColor: 'rgba(245,245,245,0.08)',
              border: '2px solid #93B5C6',
              borderRadius: 8,
              overflow: 'hidden',
              position: 'relative',
            }}
          >
            <div
              style={{
                width: `${meterFill * 100}%`,
                height: '100%',
                background: 'linear-gradient(90deg, #609966 0%, #FFC107 100%)',
              }}
            />
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: '62%',
                width: 3,
                height: '100%',
                backgroundColor: '#ff0000',
              }}
            />
            <div
              style={{
                position: 'absolute',
                top: -28,
                left: 'calc(62% - 40px)',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 16,
                color: '#ff0000',
                fontWeight: 700,
              }}
            >
              LIMIT
            </div>
          </div>
          <div
            style={{
              marginTop: 22,
              textAlign: 'center',
              fontFamily: 'Inter, sans-serif',
              fontSize: 38,
              fontWeight: 700,
              color: '#F5F5F5',
            }}
          >
            Stay under the line — unlock <span style={{ color: '#609966' }}>full benefits</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene22;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Items to FORBID
  const items = ['DEDUCTIBLES', 'COINSURANCE', 'CO-PAYS'];

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 30%, #4a5058 0%, #393E46 60%, #2c3037 100%)',
        }}
      />

      {/* Rectangle shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const r = seededRandom(i + 80);
          return (
            <div
              key={`rect-${i}`}
              style={{
                position: 'absolute',
                left: r * 1500,
                top: seededRandom(i + 22) * 900,
                width: 400 + r * 200,
                height: 200 + r * 150,
                border: '2px solid #609966',
                opacity: 0.04,
                transform: `rotate(${Math.sin(frame * 0.01 + i) * 8}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Concentric circles texture */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        <svg width="1920" height="1080">
          {[1, 2, 3, 4, 5].map((i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={150 + i * 130}
              fill="none"
              stroke="#93B5C6"
              strokeWidth="1"
              strokeDasharray="6 8"
              transform={`rotate(${frame * 0.1 * (i % 2 === 0 ? 1 : -1)} 960 540)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const r = seededRandom(i + 400);
          const px = (r * 1920 + frame * 0.5) % 1920;
          const py = seededRandom(i + 500) * 1080 + Math.sin(frame * 0.03 + i) * 20;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#93B5C6',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* PART B BAR — premium paid */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 30,
              padding: '20px 40px',
              border: '3px solid #609966',
              borderRadius: 14,
              backgroundColor: 'rgba(96,153,102,0.12)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                color: '#93B5C6',
                letterSpacing: 4,
              }}
            >
              MEDICARE PART B
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 56,
                fontWeight: 900,
                color: '#F5F5F5',
                textDecoration: 'line-through',
                textDecorationColor: '#ff0000',
                textDecorationThickness: 5,
              }}
            >
              $174 / mo
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 44,
                fontWeight: 900,
                color: '#609966',
                opacity: interpolate(sp(35), [0, 1], [0, 1]),
                transform: `scale(${sp(35, { damping: 8 })})`,
              }}
            >
              PAID BY STATE
            </div>
          </div>
        </div>

        {/* BUT IT DOESN'T STOP THERE */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 38,
              color: '#93B5C6',
              fontWeight: 500,
              fontStyle: 'italic',
            }}
          >
            But it doesn't stop there...
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 110,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              marginTop: 14,
              lineHeight: 1,
            }}
          >
            QMB <span style={{ color: '#ff0000' }}>FORBIDS</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 36,
              color: '#93B5C6',
              fontWeight: 600,
              marginTop: 6,
              letterSpacing: 2,
            }}
          >
            doctors & hospitals from charging you
          </div>
        </div>

        {/* THREE FORBIDDEN ITEMS */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 36,
          }}
        >
          {items.map((it, i) => {
            const itemSp = sp(140 + i * 18);
            const itemY = interpolate(itemSp, [0, 1], [60, 0]);
            const itemOp = interpolate(itemSp, [0, 1], [0, 1]);
            const crossOp = interpolate(frame, [200 + i * 15, 225 + i * 15], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp'
            });
            return (
              <div
                key={it}
                style={{
                  width: 380,
                  padding: '30px 20px',
                  border: '3px solid #ff0000',
                  borderRadius: 14,
                  backgroundColor: 'rgba(255,0,0,0.06)',
                  textAlign: 'center',
                  position: 'relative',
                  transform: `translateY(${itemY}px)`,
                  opacity: itemOp,
                }}
              >
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 42,
                    fontWeight: 900,
                    color: '#F5F5F5',
                    letterSpacing: 1,
                  }}
                >
                  {it}
                </div>
                <div
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 18,
                    color: '#ff0000',
                    marginTop: 10,
                    letterSpacing: 3,
                    fontWeight: 700,
                  }}
                >
                  BLOCKED BY LAW
                </div>
                {/* Big X overlay */}
                <svg
                  width="380"
                  height="180"
                  style={{
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    pointerEvents: 'none',
                    opacity: crossOp,
                  }}
                >
                  <line x1="40" y1="40" x2="340" y2="140" stroke="#ff0000" strokeWidth="6" />
                  <line x1="340" y1="40" x2="40" y2="140" stroke="#ff0000" strokeWidth="6" />
                </svg>
              </div>
            );
          })}
        </div>

        {/* Bottom rule strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [280, 310], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#FFC107',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          // FEDERAL LAW — 42 U.S.C. § 1396a(n) //
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene23;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card entrance + tilt
  const cardSp = sp(10);
  const cardY = interpolate(cardSp, [0, 1], [120, 0]);
  const cardScale = cardSp;
  const cardTilt = Math.sin(frame * 0.04) * 2;

  // Zero counter
  const zeroPulse = 1 + Math.sin(frame * 0.15) * 0.04;
  const zeroOp = interpolate(sp(80), [0, 1], [0, 1]);

  // FPL bar fill
  const fplFill = interpolate(frame, [220, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #2e333a 0%, #393E46 50%, #4a5058 100%)',
        }}
      />

      {/* Shape layer — large hexagons */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => {
          const r = seededRandom(i + 60);
          const size = 420 + r * 200;
          return (
            <svg
              key={`hex-${i}`}
              width={size}
              height={size}
              style={{
                position: 'absolute',
                left: r * 1500,
                top: seededRandom(i + 33) * 900,
                opacity: 0.05,
                transform: `rotate(${frame * 0.05 + i * 60}deg)`,
              }}
            >
              <polygon
                points={`${size/2},20 ${size-20},${size*0.27} ${size-20},${size*0.73} ${size/2},${size-20} 20,${size*0.73} 20,${size*0.27}`}
                fill="none"
                stroke="#93B5C6"
                strokeWidth="2"
              />
            </svg>
          );
        })}
      </AbsoluteFill>

      {/* Dot grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="dots24" width="50" height="50" patternUnits="userSpaceOnUse">
              <circle cx="25" cy="25" r="1.5" fill="#609966" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#dots24)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const r = seededRandom(i + 600);
          const px = (r * 1920 + frame * 0.45) % 1920;
          const py = seededRandom(i + 700) * 1080 + Math.cos(frame * 0.02 + i) * 16;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#FFC107',
                opacity: 0.35,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#609966',
            letterSpacing: 8,
            fontWeight: 600,
          }}
        >
          AT THE FRONT DESK
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -1,
            marginTop: 10,
          }}
        >
          Show the card. Pay <span style={{ color: '#609966' }}>nothing.</span>
        </div>
      </div>

      {/* QMB CARD */}
      <div
        style={{
          position: 'absolute',
          top: 260,
          left: 180,
          width: 620,
          height: 380,
          transform: `translateY(${cardY}px) scale(${cardScale}) rotate(${cardTilt}deg)`,
          perspective: 1000,
        }}
      >
        <div
          style={{
            width: '100%',
            height: '100%',
            background: 'linear-gradient(135deg, #609966 0%, #4a7a52 100%)',
            borderRadius: 24,
            padding: 36,
            boxShadow: '0 30px 80px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.15)',
            border: '2px solid rgba(245,245,245,0.2)',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          {/* Shimmer */}
          <div
            style={{
              position: 'absolute',
              top: 0,
              left: -200,
              width: 200,
              height: '100%',
              background: 'linear-gradient(90deg, transparent, rgba(255,255,255,0.18), transparent)',
              transform: `translateX(${(frame * 4) % 1000}px)`,
            }}
          />
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 16, color: 'rgba(245,245,245,0.7)', letterSpacing: 3 }}>
                QUALIFIED MEDICARE BENEFICIARY
              </div>
              <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 76, fontWeight: 900, color: '#F5F5F5', letterSpacing: 2, marginTop: 4 }}>
                QMB CARD
              </div>
            </div>
            <div
              style={{
                width: 60,
                height: 50,
                background: 'linear-gradient(135deg, #FFC107 0%, #d4a000 100%)',
                borderRadius: 6,
              }}
            />
          </div>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#F5F5F5', letterSpacing: 4, fontWeight: 700 }}>
              **** **** **** 2026
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 18 }}>
              <div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 13, color: 'rgba(245,245,245,0.6)', letterSpacing: 2 }}>HOLDER</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 24, color: '#F5F5F5', fontWeight: 700, marginTop: 2 }}>SENIOR USA</div>
              </div>
              <div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 13, color: 'rgba(245,245,245,0.6)', letterSpacing: 2 }}>STATUS</div>
                <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 24, color: '#FFC107', fontWeight: 700, marginTop: 2 }}>ACTIVE</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* BIG ZERO RECEIPT */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          right: 180,
          width: 620,
          opacity: zeroOp,
          transform: `scale(${zeroPulse})`,
        }}
      >
        <div
          style={{
            border: '4px dashed #93B5C6',
            borderRadius: 18,
            padding: '30px 24px',
            backgroundColor: 'rgba(28,28,28,0.55)',
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#93B5C6',
              letterSpacing: 4,
              marginBottom: 10,
            }}
          >
            YOUR BILL TODAY
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 260,
              fontWeight: 900,
              color: '#609966',
              lineHeight: 0.9,
              letterSpacing: -8,
              textShadow: '0 0 40px rgba(96,153,102,0.5)',
            }}
          >
            $0
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              color: '#F5F5F5',
              fontWeight: 600,
              marginTop: 4,
            }}
          >
            Government covers the rest
          </div>
        </div>
      </div>

      {/* FPL BAR */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 180,
          right: 180,
          opacity: interpolate(frame, [200, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#93B5C6',
            letterSpacing: 4,
            marginBottom: 12,
            display: 'flex',
            justifyContent: 'space-between',
          }}
        >
          <span>QMB LIMIT // TIED TO FEDERAL POVERTY LEVEL</span>
          <span style={{ color: '#FFC107' }}>ADJUSTED YEARLY</span>
        </div>
        <div
          style={{
            width: '100%',
            height: 36,
            backgroundColor: 'rgba(245,245,245,0.06)',
            border: '2px solid #93B5C6',
            borderRadius: 6,
            overflow: 'hidden',
            position: 'relative',
          }}
        >
          <div
            style={{
              width: `${fplFill * 100}%`,
              height: '100%',
              background: 'repeating-linear-gradient(45deg, #609966 0, #609966 12px, #4a7a52 12px, #4a7a52 24px)',
            }}
          />
          {[2024, 2025, 2026].map((y, i) => (
            <div
              key={y}
              style={{
                position: 'absolute',
                top: 0,
                left: `${30 + i * 25}%`,
                height: '100%',
                width: 2,
                backgroundColor: '#F5F5F5',
                opacity: 0.7,
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  top: 42,
                  left: -22,
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 16,
                  color: '#F5F5F5',
                  fontWeight: 700,
                }}
              >
                {y}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene24;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // $174 -> $0 counter
  const counterVal = interpolate(frame, [40, 110], [174, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = frame > 100 && frame < 130 ? Math.sin(frame * 1.5) * 6 : 0;

  // Wipe overlay
  const wipeW = interpolate(frame, [80, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Free healthcare badge
  const badgeSp = sp(150, { damping: 7, stiffness: 150 });

  // Program 4 transition
  const prog4Op = interpolate(frame, [260, 300], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const prog4Y = interpolate(sp(260), [0, 1], [60, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, #4a5058 0%, #393E46 55%, #2c3037 100%)',
        }}
      />

      {/* Shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => {
          const r = seededRandom(i + 90);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1600,
                top: seededRandom(i + 44) * 900,
                width: 300 + r * 250,
                height: 300 + r * 250,
                border: '2px solid #93B5C6',
                borderRadius: i % 2 === 0 ? '50%' : 0,
                opacity: 0.04,
                transform: `rotate(${frame * 0.07 + i * 30}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Wave texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {[0, 1, 2, 3].map((i) => (
            <path
              key={i}
              d={`M 0 ${200 + i * 200} Q 480 ${150 + i * 200} 960 ${200 + i * 200} T 1920 ${200 + i * 200}`}
              fill="none"
              stroke="#609966"
              strokeWidth="1.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const r = seededRandom(i + 800);
          const px = (r * 1920 + frame * 0.5) % 1920;
          const py = seededRandom(i + 900) * 1080 + Math.sin(frame * 0.025 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#609966',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '10px 30px',
            border: '2px solid #93B5C6',
            borderRadius: 4,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#93B5C6',
            letterSpacing: 6,
            fontWeight: 600,
          }}
        >
          MONTHLY PART B PREMIUM
        </div>
      </div>

      {/* COUNTER */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `translateX(${shakeX}px)`,
          opacity: interpolate(frame, [180, 200], [1, 0.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 280,
            fontWeight: 900,
            color: counterVal > 50 ? '#ff0000' : '#609966',
            letterSpacing: -8,
            lineHeight: 1,
          }}
        >
          ${Math.round(counterVal)}
        </div>
        {/* Wipe overlay */}
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '32%',
            width: `${wipeW * 36}%`,
            height: 14,
            backgroundColor: '#609966',
            transformOrigin: 'left',
          }}
        />
      </div>

      {/* WIPED OUT badge */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: badgeSp,
          transform: `scale(${badgeSp})`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '18px 50px',
            background: 'linear-gradient(135deg, #609966 0%, #4a7a52 100%)',
            borderRadius: 50,
            fontFamily: 'Inter, sans-serif',
            fontSize: 56,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 3,
            boxShadow: '0 20px 50px rgba(96,153,102,0.45)',
            transform: `rotate(-3deg)`,
          }}
        >
          BILL WIPED OUT
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 64,
            fontWeight: 800,
            color: '#FFC107',
            marginTop: 24,
            letterSpacing: 1,
          }}
        >
          = TRUE FREE HEALTH CARE
        </div>
      </div>

      {/* PROGRAM 4 TRANSITION */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: prog4Op,
          transform: `translateY(${prog4Y}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '24px 50px',
            border: '3px solid #FFC107',
            borderRadius: 14,
            backgroundColor: 'rgba(255,193,7,0.08)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              color: '#FFC107',
              letterSpacing: 6,
              fontWeight: 600,
            }}
          >
            NEXT UP // PROGRAM 04
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 88,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 4,
              marginTop: 6,
              lineHeight: 1,
            }}
          >
            L I H E A P
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene25;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // AC bill rising graph
  const acHeight = interpolate(frame, [110, 180], [60, 280], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  // Heat bill rising
  const heatHeight = interpolate(frame, [200, 270], [60, 320], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Split gradient - sun left, ice right */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, #4a5058 0%, #393E46 50%, #3a4248 100%)',
        }}
      />

      {/* Drifting shapes */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const r = seededRandom(i + 110);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1600,
                top: seededRandom(i + 55) * 900,
                width: 380 + r * 200,
                height: 380 + r * 200,
                border: '2px solid #93B5C6',
                borderRadius: i % 2 === 0 ? '50%' : 12,
                opacity: 0.04,
                transform: `rotate(${frame * 0.06 + i * 45}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="grid26" width="100" height="100" patternUnits="userSpaceOnUse">
              <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#609966" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#grid26)" />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const r = seededRandom(i + 1000);
          const px = (r * 1920 + frame * 0.4) % 1920;
          const py = seededRandom(i + 1100) * 1080 + Math.cos(frame * 0.02 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: i % 2 === 0 ? '#FFC107' : '#93B5C6',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#609966',
            letterSpacing: 6,
            fontWeight: 600,
            marginBottom: 8,
          }}
        >
          LOW INCOME HOME ENERGY ASSISTANCE PROGRAM
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -2,
            lineHeight: 1,
          }}
        >
          L I <span style={{ color: '#609966' }}>H E A P</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 34,
            color: '#93B5C6',
            fontWeight: 500,
            marginTop: 6,
            letterSpacing: 1,
          }}
        >
          The safety net when the weather goes extreme
        </div>
      </div>

      {/* TWO CHARTS — SUMMER vs WINTER */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 80,
          alignItems: 'flex-end',
        }}
      >
        {/* SUMMER PANEL */}
        <div
          style={{
            width: 580,
            opacity: interpolate(sp(60), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(60), [0, 1], [40, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              marginBottom: 18,
            }}
          >
            {/* Sun icon */}
            <svg width="50" height="50">
              <circle cx="25" cy="25" r="12" fill="#FFC107" />
              {[0, 45, 90, 135, 180, 225, 270, 315].map((a, i) => {
                const rad = (a + frame * 0.5) * Math.PI / 180;
                return (
                  <line
                    key={i}
                    x1={25 + Math.cos(rad) * 16}
                    y1={25 + Math.sin(rad) * 16}
                    x2={25 + Math.cos(rad) * 22}
                    y2={25 + Math.sin(rad) * 22}
                    stroke="#FFC107"
                    strokeWidth="3"
                    strokeLinecap="round"
                  />
                );
              })}
            </svg>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 44,
                fontWeight: 900,
                color: '#FFC107',
                letterSpacing: 2,
              }}
            >
              SUMMER // AC BILL
            </div>
          </div>
          {/* Chart */}
          <div
            style={{
              height: 320,
              border: '2px solid #93B5C6',
              borderRadius: 10,
              backgroundColor: 'rgba(255,193,7,0.04)',
              padding: 20,
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'space-around',
              position: 'relative',
            }}
          >
            {[0, 1, 2, 3, 4].map((b) => {
              const grow = interpolate(frame, [100 + b * 12, 160 + b * 12], [40, 60 + b * 50], {
                extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
              });
              return (
                <div
                  key={b}
                  style={{
                    width: 60,
                    height: grow,
                    background: 'linear-gradient(180deg, #FFC107 0%, #d4a000 100%)',
                    borderRadius: '4px 4px 0 0',
                  }}
                />
              );
            })}
            <div
              style={{
                position: 'absolute',
                top: 14,
                right: 18,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 38,
                color: '#FFC107',
                fontWeight: 700,
              }}
            >
              ▲ SKYROCKETS
            </div>
          </div>
        </div>

        {/* WINTER PANEL */}
        <div
          style={{
            width: 580,
            opacity: interpolate(sp(120), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(120), [0, 1], [40, 0])}px)`,
          }}
        >
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 14,
              marginBottom: 18,
            }}
          >
            {/* Snowflake icon */}
            <svg width="50" height="50" style={{ transform: `rotate(${frame * 0.8}deg)`, transformOrigin: '25px 25px' }}>
              {[0, 60, 120].map((a) => (
                <line
                  key={a}
                  x1="25"
                  y1="5"
                  x2="25"
                  y2="45"
                  stroke="#93B5C6"
                  strokeWidth="3"
                  strokeLinecap="round"
                  transform={`rotate(${a} 25 25)`}
                />
              ))}
            </svg>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 44,
                fontWeight: 900,
                color: '#93B5C6',
                letterSpacing: 2,
              }}
            >
              WINTER // HEAT BILL
            </div>
          </div>
          <div
            style={{
              height: 320,
              border: '2px solid #93B5C6',
              borderRadius: 10,
              backgroundColor: 'rgba(147,181,198,0.04)',
              padding: 20,
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'space-around',
              position: 'relative',
            }}
          >
            {[0, 1, 2, 3, 4].map((b) => {
              const grow = interpolate(frame, [190 + b * 12, 250 + b * 12], [50, 80 + b * 45], {
                extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
              });
              return (
                <div
                  key={b}
                  style={{
                    width: 60,
                    height: grow,
                    background: 'linear-gradient(180deg, #93B5C6 0%, #6890a3 100%)',
                    borderRadius: '4px 4px 0 0',
                  }}
                />
              );
            })}
            <div
              style={{
                position: 'absolute',
                top: 14,
                right: 18,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 38,
                color: '#93B5C6',
                fontWeight: 700,
              }}
            >
              ✕ DOUBLES
            </div>
          </div>
        </div>
      </div>

      {/* Bottom strap */}
      <div
        style={{
          position: 'absolute',
          bottom: 40,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [310, 340], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          fontFamily: 'Inter, sans-serif',
          fontSize: 30,
          color: '#609966',
          fontWeight: 700,
          letterSpacing: 3,
        }}
      >
        LIHEAP catches you in both seasons.
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene26;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Versus tilt
  const vsScale = sp(50, { damping: 7, stiffness: 180 });

  // Solution sweep
  const sweepX = interpolate(frame, [220, 280], [-1920, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Crossed-out OR
  const crossOp = interpolate(frame, [260, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, #4a5058 0%, #393E46 50%, #2a2e34 100%)',
        }}
      />

      {/* Shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => {
          const r = seededRandom(i + 130);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1600,
                top: seededRandom(i + 66) * 900,
                width: 320 + r * 280,
                height: 320 + r * 280,
                border: '2px solid #609966',
                borderRadius: i % 3 === 0 ? '50%' : 8,
                opacity: 0.04,
                transform: `rotate(${frame * 0.05 + i * 50}deg) scale(${1 + Math.sin(frame * 0.015 + i) * 0.06})`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Cross-hatch texture */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="cross27" width="40" height="40" patternUnits="userSpaceOnUse">
              <path d="M 0 0 L 40 40 M 40 0 L 0 40" stroke="#93B5C6" strokeWidth="0.8" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#cross27)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const r = seededRandom(i + 1200);
          const px = (r * 1920 + frame * 0.5) % 1920;
          const py = seededRandom(i + 1300) * 1080 + Math.sin(frame * 0.025 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#93B5C6',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE — terrible choice */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#ff0000',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          // FIXED INCOME REALITY //
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 78,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -1,
            marginTop: 8,
          }}
        >
          The <span style={{ color: '#ff0000' }}>Impossible</span> Choice
        </div>
      </div>

      {/* TWO BOXES — HEAT vs GROCERIES with VS */}
      <div
        style={{
          position: 'absolute',
          top: 270,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 0,
        }}
      >
        {/* HEAT */}
        <div
          style={{
            width: 480,
            height: 280,
            border: '3px solid #FFC107',
            borderRadius: 16,
            backgroundColor: 'rgba(255,193,7,0.06)',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            opacity: interpolate(sp(20), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(20), [0, 1], [-100, 0])}px)`,
            position: 'relative',
          }}
        >
          {/* Flame icon */}
          <svg width="80" height="80" style={{ marginBottom: 12 }}>
            <path
              d="M 40 10 Q 25 30 30 50 Q 32 65 40 70 Q 48 65 50 50 Q 55 30 40 10 Z"
              fill="#FFC107"
              style={{ transform: `scale(${1 + Math.sin(frame * 0.2) * 0.05})`, transformOrigin: '40px 40px' }}
            />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 60,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 1,
            }}
          >
            KEEP HEAT ON
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#FFC107',
              marginTop: 6,
              letterSpacing: 3,
            }}
          >
            DON'T FREEZE
          </div>
        </div>

        {/* VS */}
        <div
          style={{
            width: 160,
            textAlign: 'center',
            transform: `scale(${vsScale}) rotate(${Math.sin(frame * 0.08) * 4}deg)`,
            opacity: interpolate(sp(50), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#ff0000',
              letterSpacing: -4,
              textShadow: '0 0 30px rgba(255,0,0,0.5)',
            }}
          >
            OR
          </div>
          {/* Crossed out when solution arrives */}
          <svg
            width="160"
            height="140"
            style={{
              position: 'absolute',
              top: 0,
              left: 0,
              opacity: crossOp,
            }}
          >
            <line x1="20" y1="20" x2="140" y2="120" stroke="#609966" strokeWidth="8" />
            <line x1="140" y1="20" x2="20" y2="120" stroke="#609966" strokeWidth="8" />
          </svg>
        </div>

        {/* GROCERIES */}
        <div
          style={{
            width: 480,
            height: 280,
            border: '3px solid #609966',
            borderRadius: 16,
            backgroundColor: 'rgba(96,153,102,0.06)',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            opacity: interpolate(sp(35), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(35), [0, 1], [100, 0])}px)`,
          }}
        >
          {/* Bag icon */}
          <svg width="80" height="80" style={{ marginBottom: 12 }}>
            <path
              d="M 20 30 L 60 30 L 65 70 L 15 70 Z"
              fill="none"
              stroke="#609966"
              strokeWidth="4"
            />
            <path d="M 28 30 Q 28 18 40 18 Q 52 18 52 30" fill="none" stroke="#609966" strokeWidth="4" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 60,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 1,
            }}
          >
            BUY GROCERIES
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#609966',
              marginTop: 6,
              letterSpacing: 3,
            }}
          >
            DON'T STARVE
          </div>
        </div>
      </div>

      {/* SOLUTION SWEEP BANNER */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          height: 120,
          overflow: 'hidden',
        }}
      >
        <div
          style={{
            transform: `translateX(${sweepX}px)`,
            width: '100%',
            height: '100%',
            background: 'linear-gradient(90deg, #609966 0%, #4a7a52 100%)',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 -10px 40px rgba(96,153,102,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 64,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 3,
            }}
          >
            LIHEAP ELIMINATES THE CHOICE
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene27;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money packet animation along path
  const moneyProgress = interpolate(frame, [80, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const mx = 380 + moneyProgress * 1100;
  const my = 540;

  // Crisis badge
  const crisisSp = sp(220, { damping: 7 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #2f343b 0%, #393E46 60%, #454c55 100%)',
        }}
      />

      {/* Shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const r = seededRandom(i + 150);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1500,
                top: seededRandom(i + 77) * 850,
                width: 350 + r * 220,
                height: 350 + r * 220,
                border: '2px solid #93B5C6',
                borderRadius: 50,
                opacity: 0.04,
                transform: `rotate(${frame * 0.07 + i * 60}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Circuit-like texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 10 }).map((_, i) => (
            <line
              key={`hl-${i}`}
              x1="0"
              y1={108 * i}
              x2="1920"
              y2={108 * i}
              stroke="#609966"
              strokeWidth="0.8"
              strokeDasharray="4 12"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const r = seededRandom(i + 1400);
          const px = (r * 1920 + frame * 0.5) % 1920;
          const py = seededRandom(i + 1500) * 1080 + Math.cos(frame * 0.02 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#609966',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#609966',
            letterSpacing: 8,
            fontWeight: 600,
          }}
        >
          HOW THE MONEY MOVES
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 76,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -1,
            marginTop: 8,
          }}
        >
          Direct to your <span style={{ color: '#609966' }}>utility company</span>
        </div>
      </div>

      {/* MONEY FLOW DIAGRAM */}
      <AbsoluteFill>
        {/* FEDERAL box */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            left: 180,
            width: 260,
            height: 240,
            border: '3px solid #609966',
            borderRadius: 14,
            backgroundColor: 'rgba(96,153,102,0.10)',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            opacity: interpolate(sp(10), [0, 1], [0, 1]),
          }}
        >
          {/* Building icon */}
          <svg width="80" height="80">
            <polygon points="40,10 70,30 70,75 10,75 10,30" fill="none" stroke="#609966" strokeWidth="3" />
            <line x1="30" y1="30" x2="30" y2="75" stroke="#609966" strokeWidth="2" />
            <line x1="50" y1="30" x2="50" y2="75" stroke="#609966" strokeWidth="2" />
          </svg>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 900, color: '#F5F5F5', marginTop: 10 }}>
            FEDERAL
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 16, color: '#93B5C6', letterSpacing: 3 }}>LIHEAP FUND</div>
        </div>

        {/* PATH LINE */}
        <svg
          width="1920"
          height="1080"
          style={{ position: 'absolute', top: 0, left: 0, pointerEvents: 'none' }}
        >
          <line
            x1="440"
            y1="540"
            x2="1480"
            y2="540"
            stroke="#FFC107"
            strokeWidth="4"
            strokeDasharray="14 10"
            strokeDashoffset={interpolate(frame, [60, 200], [400, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            opacity="0.7"
          />
        </svg>

        {/* MOVING MONEY PACKET */}
        <div
          style={{
            position: 'absolute',
            left: mx - 60,
            top: my - 35,
            width: 120,
            height: 70,
            background: 'linear-gradient(135deg, #FFC107 0%, #d4a000 100%)',
            borderRadius: 8,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 32,
            fontWeight: 900,
            color: '#1C1C1C',
            boxShadow: '0 10px 30px rgba(255,193,7,0.5)',
            opacity: interpolate(frame, [80, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          $$$
        </div>

        {/* UTILITY box */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            right: 180,
            width: 260,
            height: 240,
            border: '3px solid #93B5C6',
            borderRadius: 14,
            backgroundColor: 'rgba(147,181,198,0.08)',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            opacity: interpolate(sp(10), [0, 1], [0, 1]),
          }}
        >
          {/* Power icon */}
          <svg width="80" height="80">
            <polygon points="45,10 25,45 38,45 30,75 55,38 42,38 50,10" fill="#93B5C6" />
          </svg>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 900, color: '#F5F5F5', marginTop: 10 }}>
            UTILITY CO.
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 16, color: '#609966', letterSpacing: 3 }}>BILL PAID</div>
        </div>

        {/* BYPASS NOTE */}
        <div
          style={{
            position: 'absolute',
            top: 700,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [180, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '10px 28px',
              border: '2px dashed #ff0000',
              borderRadius: 8,
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#ff0000',
              letterSpacing: 3,
              fontWeight: 700,
            }}
          >
            ✕ MONEY NEVER TOUCHES YOUR HANDS
          </div>
        </div>
      </AbsoluteFill>

      {/* SENIOR CRISIS BADGE */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 100,
          transform: `scale(${crisisSp}) rotate(${interpolate(sp(220), [0, 1], [-15, -6])}deg)`,
          opacity: crisisSp,
        }}
      >
        <div
          style={{
            padding: '20px 30px',
            background: 'linear-gradient(135deg, #ff0000 0%, #c20000 100%)',
            borderRadius: 14,
            boxShadow: '0 20px 50px rgba(255,0,0,0.4)',
            border: '3px solid #FFC107',
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#FFC107', letterSpacing: 3, fontWeight: 700 }}>
            SENIORS ONLY
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 42, color: '#F5F5F5', fontWeight: 900, marginTop: 4, letterSpacing: 1 }}>
            CRISIS FUNDS
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, color: '#F5F5F5', fontWeight: 500, marginTop: 4 }}>
            available year-round
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene28;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Arrows merging
  const arrow1X = interpolate(sp(120), [0, 1], [-300, 0]);
  const arrow2X = interpolate(sp(140), [0, 1], [300, 0]);

  // Auto-approved stamp
  const autoSp = sp(200, { damping: 7, stiffness: 170 });

  // Form crossed out
  const formCross = interpolate(frame, [260, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 60%, #4a5058 0%, #393E46 55%, #2c3037 100%)',
        }}
      />

      {/* Shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const r = seededRandom(i + 170);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1600,
                top: seededRandom(i + 88) * 900,
                width: 360 + r * 240,
                height: 360 + r * 240,
                border: '2px solid #609966',
                borderRadius: i % 2 === 0 ? '50%' : 0,
                opacity: 0.04,
                transform: `rotate(${frame * 0.06 + i * 80}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Dot grid */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        <svg width="1920" height="1080">
          <defs>
            <pattern id="dot29" width="60" height="60" patternUnits="userSpaceOnUse">
              <circle cx="30" cy="30" r="1.2" fill="#93B5C6" />
            </pattern>
          </defs>
          <rect width="1920" height="1080" fill="url(#dot29)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const r = seededRandom(i + 1600);
          const px = (r * 1920 + frame * 0.45) % 1920;
          const py = seededRandom(i + 1700) * 1080 + Math.sin(frame * 0.025 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#FFC107',
                opacity: 0.35,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#FFC107',
            letterSpacing: 6,
            fontWeight: 600,
          }}
        >
          THE HIDDEN SHORTCUT
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -1,
            marginTop: 8,
          }}
        >
          Already in? <span style={{ color: '#609966' }}>You're already in.</span>
        </div>
      </div>

      {/* TWO SOURCE BOXES merging */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 0,
        }}
      >
        {/* EXTRA HELP */}
        <div
          style={{
            width: 360,
            padding: '30px 20px',
            border: '3px solid #93B5C6',
            borderRadius: 14,
            textAlign: 'center',
            opacity: interpolate(sp(30), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(30), [0, 1], [-80, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#93B5C6', letterSpacing: 3 }}>QUALIFIED FOR</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 56, color: '#F5F5F5', fontWeight: 900, marginTop: 6, letterSpacing: 1 }}>
            EXTRA HELP
          </div>
        </div>

        {/* Plus connector */}
        <div style={{ width: 80, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 80,
              fontWeight: 900,
              color: '#FFC107',
              opacity: interpolate(sp(70), [0, 1], [0, 1]),
              transform: `scale(${sp(70, { damping: 7 })})`,
            }}
          >
            /
          </div>
        </div>

        {/* SNAP */}
        <div
          style={{
            width: 360,
            padding: '30px 20px',
            border: '3px solid #93B5C6',
            borderRadius: 14,
            textAlign: 'center',
            opacity: interpolate(sp(50), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(50), [0, 1], [80, 0])}px)`,
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 18, color: '#93B5C6', letterSpacing: 3 }}>QUALIFIED FOR</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 56, color: '#F5F5F5', fontWeight: 900, marginTop: 6, letterSpacing: 1 }}>
            SNAP
          </div>
        </div>
      </div>

      {/* MERGING ARROWS */}
      <svg
        width="1920"
        height="200"
        style={{ position: 'absolute', top: 470, left: 0 }}
      >
        <line
          x1={460 + arrow1X}
          y1="20"
          x2="900"
          y2="160"
          stroke="#609966"
          strokeWidth="5"
          strokeDasharray="12 8"
        />
        <line
          x1={1460 + arrow2X}
          y1="20"
          x2="1020"
          y2="160"
          stroke="#609966"
          strokeWidth="5"
          strokeDasharray="12 8"
        />
      </svg>

      {/* AUTO-APPROVED LIHEAP */}
      <div
        style={{
          position: 'absolute',
          top: 660,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: autoSp,
          transform: `scale(${autoSp})`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '24px 60px',
            background: 'linear-gradient(135deg, #609966 0%, #4a7a52 100%)',
            borderRadius: 14,
            boxShadow: '0 25px 60px rgba(96,153,102,0.45)',
            border: '3px solid #FFC107',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#FFC107',
              letterSpacing: 5,
              fontWeight: 700,
            }}
          >
            ✓ AUTO-APPROVED
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 96,
              color: '#F5F5F5',
              fontWeight: 900,
              letterSpacing: 6,
              lineHeight: 1,
              marginTop: 4,
            }}
          >
            LIHEAP
          </div>
        </div>
      </div>

      {/* Crossed-out paperwork at bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [240, 270], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 38,
              color: '#93B5C6',
              fontWeight: 700,
              letterSpacing: 2,
              padding: '0 20px',
            }}
          >
            NO RE-PROVING INCOME  ·  NO EXTRA PAPERWORK  ·  NO WAITING
          </div>
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${formCross * 100}%`,
              height: 4,
              backgroundColor: '#ff0000',
            }}
          />
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene29;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pressure gauge needle
  const needleAngle = interpolate(frame, [40, 130], [-90, 90], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Relief valve release
  const releaseScale = sp(140, { damping: 7 });

  // Program 5 reveal
  const prog5Op = interpolate(frame, [240, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const prog5Scale = sp(240, { damping: 9, stiffness: 130 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, #4a5058 0%, #393E46 55%, #2a2e34 100%)',
        }}
      />

      {/* Shape drifts */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => {
          const r = seededRandom(i + 190);
          return (
            <div
              key={`sh-${i}`}
              style={{
                position: 'absolute',
                left: r * 1600,
                top: seededRandom(i + 99) * 900,
                width: 320 + r * 280,
                height: 320 + r * 280,
                border: '2px solid #FFC107',
                borderRadius: i % 2 === 0 ? '50%' : 20,
                opacity: 0.04,
                transform: `rotate(${frame * 0.06 + i * 50}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Concentric rings texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {[1, 2, 3, 4, 5, 6].map((i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={120 + i * 110}
              fill="none"
              stroke="#93B5C6"
              strokeWidth="1"
              strokeDasharray="6 6"
              transform={`rotate(${frame * 0.08 * (i % 2 === 0 ? 1 : -1)} 960 540)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const r = seededRandom(i + 1800);
          const px = (r * 1920 + frame * 0.55) % 1920;
          const py = seededRandom(i + 1900) * 1080 + Math.sin(frame * 0.03 + i) * 20;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#609966',
                opacity: 0.4,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#609966',
            letterSpacing: 6,
            fontWeight: 600,
          }}
        >
          THE PRESSURE RELEASE
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 76,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -1,
            marginTop: 8,
          }}
        >
          LIHEAP = a <span style={{ color: '#609966' }}>relief valve</span>
        </div>
      </div>

      {/* PRESSURE GAUGE */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          left: 140,
          width: 480,
          opacity: interpolate(sp(20), [0, 1], [0, 1]),
        }}
      >
        <svg width="480" height="320" viewBox="0 0 480 320">
          {/* Outer arc */}
          <path
            d="M 60 260 A 180 180 0 0 1 420 260"
            fill="none"
            stroke="#93B5C6"
            strokeWidth="4"
          />
          {/* Color zones */}
          <path
            d="M 60 260 A 180 180 0 0 1 180 110"
            fill="none"
            stroke="#609966"
            strokeWidth="14"
            opacity="0.7"
          />
          <path
            d="M 180 110 A 180 180 0 0 1 300 110"
            fill="none"
            stroke="#FFC107"
            strokeWidth="14"
            opacity="0.7"
          />
          <path
            d="M 300 110 A 180 180 0 0 1 420 260"
            fill="none"
            stroke="#ff0000"
            strokeWidth="14"
            opacity="0.7"
          />
          {/* Needle */}
          <line
            x1="240"
            y1="260"
            x2="240"
            y2="90"
            stroke="#F5F5F5"
            strokeWidth="5"
            strokeLinecap="round"
            transform={`rotate(${needleAngle} 240 260)`}
          />
          <circle cx="240" cy="260" r="14" fill="#F5F5F5" />
          {/* Labels */}
          <text x="60" y="290" textAnchor="middle" fill="#609966" fontFamily="JetBrains Mono, monospace" fontSize="20" fontWeight="700">SAFE</text>
          <text x="420" y="290" textAnchor="middle" fill="#ff0000" fontFamily="JetBrains Mono, monospace" fontSize="20" fontWeight="700">CRISIS</text>
        </svg>
        <div
          style={{
            textAlign: 'center',
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            color: '#93B5C6',
            fontWeight: 700,
            marginTop: -10,
            letterSpacing: 2,
          }}
        >
          Bills mount. Pressure rises.
        </div>
      </div>

      {/* RELEASE BURST + outcomes */}
      <div
        style={{
          position: 'absolute',
          top: 240,
          right: 140,
          width: 540,
          textAlign: 'center',
          opacity: releaseScale,
          transform: `scale(${releaseScale})`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '14px 30px',
            backgroundColor: '#609966',
            borderRadius: 30,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#F5F5F5',
            letterSpacing: 4,
            fontWeight: 700,
          }}
        >
          → RELEASED
        </div>
        <div style={{ marginTop: 24, display: 'flex', flexDirection: 'column', gap: 14 }}>
          {['NO freezing house', 'NO sweltering apartment', 'NO choosing pennies over health'].map((t, i) => (
            <div
              key={t}
              style={{
                padding: '14px 18px',
                border: '2px solid #609966',
                borderRadius: 10,
                fontFamily: 'Inter, sans-serif',
                fontSize: 30,
                color: '#F5F5F5',
                fontWeight: 700,
                opacity: interpolate(sp(160 + i * 15), [0, 1], [0, 1]),
                transform: `translateX(${interpolate(sp(160 + i * 15), [0, 1], [40, 0])}px)`,
                backgroundColor: 'rgba(96,153,102,0.08)',
              }}
            >
              <span style={{ color: '#609966', marginRight: 12 }}>✓</span>{t}
            </div>
          ))}
        </div>
      </div>

      {/* PROGRAM 5 TEASE */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: prog5Op,
          transform: `scale(${prog5Scale})`,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '22px 50px',
            background: 'linear-gradient(135deg, #FFC107 0%, #d4a000 100%)',
            borderRadius: 14,
            boxShadow: '0 25px 60px rgba(255,193,7,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#1C1C1C',
              letterSpacing: 5,
              fontWeight: 800,
            }}
          >
            COMING NEXT // PROGRAM 05
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 72,
              color: '#1C1C1C',
              fontWeight: 900,
              letterSpacing: -1,
              lineHeight: 1,
              marginTop: 4,
            }}
          >
            The Ultimate Homeowner Lifesaver
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.35,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene30;
```

