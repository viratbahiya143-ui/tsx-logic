```tsx
import React from 'react';
Audio: "This is the ultimate hack where your state pays your federal Medicare bills. Many think Medicaid means a nursing home or losing your house. That is a complete myth."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene21_StatePaysMyth',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene21_StatePaysMyth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // Headline reveal
  const headlineY = sp(8) * 80 - 80;
  const headlineOp = sp(8);

  // STATE -> FEDERAL arrow flow
  const arrowProgress = interpolate(frame, [60, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const stateBoxScale = sp(45);
  const fedBoxScale = sp(70);

  // Myth stamp
  const mythScale = sp(180, { damping: 8, stiffness: 180 });
  const mythRotate = interpolate(sp(180), [0, 1], [-25, -8]);
  const mythShake = Math.sin(frame * 0.8) * (frame > 180 && frame < 230 ? 2 : 0);

  // Crossing line through myth text
  const crossLine = interpolate(frame, [220, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Floating particles
  const particles = Array.from({ length: 24 }, (_, i) => {
    const r = seededRandom(i + 1);
    const r2 = seededRandom(i + 50);
    return {
      x: r * 1920,
      y: ((r2 * 1080) + frame * (0.3 + r * 0.4)) % 1080,
      size: 2 + r * 4,
      op: 0.15 + r2 * 0.25,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic Gradient Background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 30% 20%, rgba(250,185,91,0.18) 0%, transparent 55%), radial-gradient(circle at 75% 80%, rgba(232,237,242,0.06) 0%, transparent 60%), linear-gradient(135deg, #412D15 0%, #2D1F0E 100%)',
          transform: `scale(${camZoom})`,
        }}
      />

      {/* Shape Layers — large transparent geometry drifting */}
      <div
        style={{
          position: 'absolute',
          top: -200,
          left: -150,
          width: 700,
          height: 700,
          border: '3px solid #FAB95B',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -250,
          right: -200,
          width: 800,
          height: 800,
          border: '2px solid #E8EDF2',
          opacity: 0.05,
          transform: `rotate(${-frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '40%',
          left: '60%',
          width: 400,
          height: 400,
          background: 'linear-gradient(45deg, #FAB95B, transparent)',
          opacity: 0.04,
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Motion Texture — grid lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="grid21" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid21)" />
      </svg>

      {/* Floating Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 8px #FAB95B',
          }}
        />
      ))}

      {/* HEADLINE */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `translateY(${headlineY}px)`,
          opacity: headlineOp,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 700,
            color: '#FAB95B',
            letterSpacing: 8,
            marginBottom: 12,
          }}
        >
          THE ULTIMATE HACK
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 96,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 1.05,
            textShadow: '0 4px 20px rgba(0,0,0,0.5)',
          }}
        >
          THE STATE PAYS YOUR
          <br />
          <span style={{ color: '#FAB95B' }}>FEDERAL BILLS</span>
        </div>
      </div>

      {/* FLOW DIAGRAM: STATE -> FEDERAL */}
      <div
        style={{
          position: 'absolute',
          top: 430,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 60,
        }}
      >
        {/* STATE BOX */}
        <div
          style={{
            width: 340,
            height: 200,
            backgroundColor: '#FAB95B',
            borderRadius: 18,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${stateBoxScale})`,
            boxShadow: '0 12px 40px rgba(250,185,91,0.4)',
          }}
        >
          <svg width="58" height="58" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 8 }}>
            <path d="M3 21h18M5 21V10l7-5 7 5v11M9 21v-6h6v6" stroke="#412D15" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#412D15', letterSpacing: 2 }}>STATE</div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 600, color: '#412D15', opacity: 0.75, marginTop: 4 }}>MEDICAID</div>
        </div>

        {/* ARROW with $ flowing */}
        <div style={{ position: 'relative', width: 280, height: 80, display: 'flex', alignItems: 'center' }}>
          <svg width="280" height="80" viewBox="0 0 280 80">
            <line x1="0" y1="40" x2={260 * arrowProgress} y2="40" stroke="#E8EDF2" strokeWidth="4" strokeDasharray="12 8" />
            <polygon
              points={`${260 * arrowProgress},25 ${280 * arrowProgress},40 ${260 * arrowProgress},55`}
              fill="#E8EDF2"
              opacity={arrowProgress}
            />
          </svg>
          {/* $$ floating tokens */}
          {[0, 1, 2].map((i) => {
            const tokenProgress = ((frame + i * 25) % 90) / 90;
            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  left: tokenProgress * 240,
                  top: 8,
                  width: 36,
                  height: 36,
                  borderRadius: '50%',
                  backgroundColor: '#FAB95B',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 900,
                  color: '#412D15',
                  opacity: arrowProgress * (1 - tokenProgress * 0.5),
                  boxShadow: '0 0 14px rgba(250,185,91,0.7)',
                }}
              >
                $
              </div>
            );
          })}
        </div>

        {/* FEDERAL BOX */}
        <div
          style={{
            width: 340,
            height: 200,
            backgroundColor: 'transparent',
            border: '4px solid #E8EDF2',
            borderRadius: 18,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${fedBoxScale})`,
            boxShadow: '0 12px 40px rgba(232,237,242,0.15)',
          }}
        >
          <svg width="58" height="58" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 8 }}>
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="#E8EDF2" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#E8EDF2', letterSpacing: 2 }}>FEDERAL</div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 600, color: '#E8EDF2', opacity: 0.75, marginTop: 4 }}>MEDICARE</div>
        </div>
      </div>

      {/* MYTH STAMP */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${mythScale}) rotate(${mythRotate + mythShake}deg)`,
          opacity: mythScale,
        }}
      >
        <div style={{ display: 'inline-block', position: 'relative', padding: '18px 60px', border: '5px solid #FF0000', borderRadius: 8 }}>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 72,
              fontWeight: 900,
              color: '#FF0000',
              letterSpacing: 8,
              fontStyle: 'italic',
            }}
          >
            COMPLETE MYTH
          </div>
          {/* Strike through */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              height: 6,
              backgroundColor: '#FF0000',
              width: `${crossLine * 100}%`,
              transform: 'translateY(-50%)',
              opacity: 0.85,
            }}
          />
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 600,
            color: '#E8EDF2',
            marginTop: 16,
            opacity: crossLine,
          }}
        >
          Nursing home? Losing your house? <span style={{ color: '#FAB95B' }}>FALSE.</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene21_StatePaysMyth;
```

```tsx
import React from 'react';
Audio: "There's a category of Medicaid called the Medicare Savings Programs — and the most powerful is QMB, the Qualified Medicare Beneficiary Program. If your income and resources are below the threshold..."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene22_QMBReveal',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene22_QMBReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  // MSP tree branches
  const treeOp = sp(10);
  const branch1 = sp(35);
  const branch2 = sp(55);
  const branch3 = sp(75);

  // QMB spotlight
  const qmbScale = sp(110, { damping: 9, stiffness: 140 });
  const qmbGlow = interpolate(Math.sin(frame * 0.1), [-1, 1], [0.5, 1]);

  // Threshold meter
  const meterFill = interpolate(frame, [160, 230], [0, 0.72], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const meterOp = sp(155);

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const r = seededRandom(i + 100);
    const r2 = seededRandom(i + 200);
    return {
      x: (r * 1920 + frame * 0.5) % 1920,
      y: r2 * 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.2,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 30%, rgba(250,185,91,0.22) 0%, transparent 50%), linear-gradient(180deg, #412D15 0%, #2A1C0A 100%)',
          transform: `translateX(${camX}px)`,
        }}
      />

      {/* Shape Layers — hexagons */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <polygon
          points="960,150 1100,230 1100,390 960,470 820,390 820,230"
          fill="none"
          stroke="#FAB95B"
          strokeWidth="2"
          style={{ transformOrigin: '960px 310px', transform: `rotate(${frame * 0.1}deg)` }}
        />
      </svg>
      <div
        style={{
          position: 'absolute',
          top: '60%',
          left: '10%',
          width: 500,
          height: 500,
          border: '3px solid #E8EDF2',
          opacity: 0.04,
          borderRadius: 30,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      {/* Texture — diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="diag22" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag22)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#E8EDF2',
            opacity: p.op,
          }}
        />
      ))}

      {/* TOP LABEL */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: treeOp,
          transform: `translateY(${(1 - treeOp) * -30}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 600, color: '#FAB95B', letterSpacing: 6 }}>
          MEDICAID CATEGORY
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 76, fontWeight: 900, color: '#E8EDF2', marginTop: 8, lineHeight: 1 }}>
          MEDICARE SAVINGS PROGRAMS
        </div>
      </div>

      {/* TREE STRUCTURE */}
      <div style={{ position: 'absolute', top: 280, left: 0, right: 0, display: 'flex', justifyContent: 'center' }}>
        <svg width="1100" height="180" viewBox="0 0 1100 180">
          {/* trunk */}
          <line x1="550" y1="0" x2="550" y2="60" stroke="#E8EDF2" strokeWidth="3" opacity={treeOp} />
          {/* horizontal */}
          <line x1="180" y1="60" x2={180 + 740 * treeOp} y2="60" stroke="#E8EDF2" strokeWidth="3" opacity={treeOp} />
          {/* branches */}
          <line x1="200" y1="60" x2="200" y2="130" stroke="#E8EDF2" strokeWidth="3" opacity={branch1} />
          <line x1="550" y1="60" x2="550" y2="130" stroke="#FAB95B" strokeWidth="5" opacity={branch2} />
          <line x1="900" y1="60" x2="900" y2="130" stroke="#E8EDF2" strokeWidth="3" opacity={branch3} />
        </svg>
      </div>

      {/* THREE BOXES */}
      <div style={{ position: 'absolute', top: 470, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 100 }}>
        {/* SLMB */}
        <div
          style={{
            width: 240,
            height: 140,
            backgroundColor: '#2D1F0E',
            border: '2px solid #E8EDF2',
            borderRadius: 14,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: branch1,
            transform: `translateY(${(1 - branch1) * 20}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 42, fontWeight: 900, color: '#E8EDF2' }}>SLMB</div>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#E8EDF2', opacity: 0.6, marginTop: 4 }}>Specified Low-Income</div>
        </div>

        {/* QMB — HERO */}
        <div
          style={{
            width: 320,
            height: 180,
            backgroundColor: '#FAB95B',
            borderRadius: 16,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${qmbScale}) translateY(-20px)`,
            boxShadow: `0 0 ${40 * qmbGlow}px rgba(250,185,91,${0.5 * qmbGlow})`,
            position: 'relative',
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#412D15', letterSpacing: 4 }}>MOST POWERFUL</div>
          <div style={{ fontFamily: 'Inter', fontSize: 84, fontWeight: 900, color: '#412D15', lineHeight: 1 }}>QMB</div>
          <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 600, color: '#412D15', opacity: 0.85, marginTop: 6, textAlign: 'center' }}>
            Qualified Medicare Beneficiary
          </div>
          {/* crown indicator */}
          <svg
            width="50"
            height="40"
            viewBox="0 0 24 24"
            style={{ position: 'absolute', top: -28, left: '50%', transform: 'translateX(-50%)' }}
          >
            <path d="M2 18h20l-2-10-5 4-5-8-5 8-5-4 2 10z" fill="#FAB95B" stroke="#412D15" strokeWidth="1.5" strokeLinejoin="round" />
          </svg>
        </div>

        {/* QI */}
        <div
          style={{
            width: 240,
            height: 140,
            backgroundColor: '#2D1F0E',
            border: '2px solid #E8EDF2',
            borderRadius: 14,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: branch3,
            transform: `translateY(${(1 - branch3) * 20}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 42, fontWeight: 900, color: '#E8EDF2' }}>QI</div>
          <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#E8EDF2', opacity: 0.6, marginTop: 4 }}>Qualifying Individual</div>
        </div>
      </div>

      {/* THRESHOLD METER */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          opacity: meterOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#E8EDF2', marginBottom: 12, letterSpacing: 3 }}>
          INCOME &amp; RESOURCES BELOW THRESHOLD
        </div>
        <div
          style={{
            width: 900,
            height: 32,
            backgroundColor: '#2D1F0E',
            borderRadius: 16,
            border: '2px solid #E8EDF2',
            overflow: 'hidden',
            position: 'relative',
          }}
        >
          <div
            style={{
              height: '100%',
              width: `${meterFill * 100}%`,
              background: 'linear-gradient(90deg, #FAB95B 0%, #FAD08B 100%)',
              borderRadius: 14,
              boxShadow: '0 0 20px rgba(250,185,91,0.6)',
            }}
          />
          {/* threshold marker */}
          <div
            style={{
              position: 'absolute',
              top: -8,
              left: '75%',
              width: 4,
              height: 48,
              backgroundColor: '#FF0000',
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: -34,
              left: '75%',
              transform: 'translateX(-50%)',
              fontFamily: 'Inter',
              fontSize: 16,
              fontWeight: 700,
              color: '#FF0000',
            }}
          >
            LIMIT
          </div>
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#FAB95B', marginTop: 18, fontWeight: 600 }}>
          → You qualify
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene22_QMBReveal;
```

```tsx
import React from 'react';
Audio: "...the state pays your Medicare Part B premium every month. But it doesn't stop there. QMB legally forbids doctors and hospitals from charging you deductibles, coinsurance, or co-pays for covered services."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene23_LegallyForbids',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene23_LegallyForbids: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [0, -2]);

  // Part B premium card
  const premiumOp = sp(8);
  const premiumStamp = sp(45, { damping: 8, stiffness: 160 });

  // "But it doesn't stop there" reveal
  const butReveal = sp(95);

  // Three forbidden items
  const item1 = sp(140);
  const item2 = sp(165);
  const item3 = sp(190);

  // FORBIDDEN banner
  const forbiddenScale = sp(230, { damping: 10, stiffness: 150 });
  const forbiddenPulse = 1 + Math.sin(frame * 0.15) * 0.02;

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => {
    const r = seededRandom(i + 300);
    const r2 = seededRandom(i + 400);
    return {
      x: r * 1920,
      y: ((r2 * 1080) + frame * (0.2 + r * 0.5)) % 1080,
      size: 2 + r * 3,
      op: 0.1 + r2 * 0.25,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 20% 80%, rgba(250,185,91,0.18) 0%, transparent 55%), radial-gradient(circle at 80% 20%, rgba(255,0,0,0.08) 0%, transparent 50%), linear-gradient(160deg, #412D15 0%, #301F0C 100%)',
          transform: `rotate(${camTilt}deg) scale(1.05)`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          right: -180,
          width: 600,
          height: 600,
          border: '2px solid #FAB95B',
          opacity: 0.05,
          transform: `rotate(${45 + frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -200,
          left: '15%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(232,237,242,0.06), transparent)',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.1})`,
        }}
      />

      {/* Texture — concentric circles */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        {[100, 200, 300, 400, 500].map((r) => (
          <circle key={r} cx="960" cy="540" r={r} fill="none" stroke="#FAB95B" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* TOP — Part B Premium card */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          opacity: premiumOp,
          transform: `translateY(${(1 - premiumOp) * -40}px)`,
        }}
      >
        <div
          style={{
            width: 920,
            padding: '24px 50px',
            backgroundColor: '#2D1F0E',
            border: '3px solid #FAB95B',
            borderRadius: 18,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            position: 'relative',
          }}
        >
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 600, color: '#E8EDF2', opacity: 0.7, letterSpacing: 3 }}>
              MEDICARE PART B PREMIUM
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 82, fontWeight: 900, color: '#E8EDF2', lineHeight: 1 }}>
              $174<span style={{ fontSize: 38, opacity: 0.6 }}>/mo</span>
            </div>
          </div>
          <div
            style={{
              transform: `scale(${premiumStamp}) rotate(-12deg)`,
              padding: '14px 32px',
              border: '4px solid #FAB95B',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: 4,
            }}
          >
            STATE PAYS
          </div>
        </div>
      </div>

      {/* MIDDLE — But it doesn't stop there */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: butReveal,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 800, color: '#E8EDF2', fontStyle: 'italic' }}>
          But it doesn't stop there<span style={{ color: '#FAB95B' }}>.</span>
        </div>
      </div>

      {/* THREE FORBIDDEN ITEMS */}
      <div
        style={{
          position: 'absolute',
          top: 450,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 40,
        }}
      >
        {[
          { label: 'DEDUCTIBLES', delay: item1, icon: 'M3 12h18M3 6h18M3 18h18' },
          { label: 'COINSURANCE', delay: item2, icon: 'M12 2v20M2 12h20' },
          { label: 'CO-PAYS', delay: item3, icon: 'M4 4h16v16H4zM8 8h8v8H8z' },
        ].map((it, i) => (
          <div
            key={i}
            style={{
              width: 320,
              height: 220,
              backgroundColor: '#2D1F0E',
              border: '3px solid #E8EDF2',
              borderRadius: 16,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              opacity: it.delay,
              transform: `scale(${it.delay}) translateY(${(1 - it.delay) * 30}px)`,
              position: 'relative',
            }}
          >
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 14 }}>
              <path d={it.icon} stroke="#E8EDF2" strokeWidth="2.2" strokeLinecap="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#E8EDF2', letterSpacing: 2 }}>
              {it.label}
            </div>
            {/* Big red X overlay */}
            <svg
              width="280"
              height="180"
              viewBox="0 0 280 180"
              style={{ position: 'absolute', top: 20, opacity: Math.max(0, it.delay - 0.3) }}
            >
              <line x1="40" y1="30" x2="240" y2="150" stroke="#FF0000" strokeWidth="10" strokeLinecap="round" />
              <line x1="240" y1="30" x2="40" y2="150" stroke="#FF0000" strokeWidth="10" strokeLinecap="round" />
            </svg>
          </div>
        ))}
      </div>

      {/* BOTTOM — LEGALLY FORBIDDEN banner */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${forbiddenScale * forbiddenPulse})`,
          opacity: forbiddenScale,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '20px 70px',
            backgroundColor: '#FF0000',
            borderRadius: 12,
            fontFamily: 'Inter',
            fontSize: 56,
            fontWeight: 900,
            color: '#E8EDF2',
            letterSpacing: 8,
            boxShadow: '0 12px 40px rgba(255,0,0,0.5)',
          }}
        >
          LEGALLY FORBIDDEN
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene23_LegallyForbids;
```

```tsx
import React from 'react';
Audio: "You walk in, show your QMB card, and pay absolutely zero dollars. The government covers the entire bill. The QMB income limits are tied to the federal poverty level, adjusted every year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene24_ZeroDollars',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene24_ZeroDollars: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // QMB Card slide in + flip
  const cardX = sp(10);
  const cardRotate = interpolate(sp(10), [0, 1], [-180, 0]);

  // Counter rolling to 0
  const counterStart = 174;
  const counterProgress = interpolate(frame, [70, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const counterValue = Math.round(counterStart * (1 - counterProgress));

  // $0 hero reveal
  const zeroScale = sp(150, { damping: 9, stiffness: 140 });
  const zeroGlow = interpolate(Math.sin(frame * 0.12), [-1, 1], [0.6, 1]);
  const screenShake = frame > 150 && frame < 175 ? Math.sin(frame * 1.5) * 3 : 0;

  // FPL bar reveal
  const fplOp = sp(220);
  const fplFill = interpolate(frame, [240, 310], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particles burst
  const burstFrame = frame - 150;
  const burstParticles = Array.from({ length: 20 }, (_, i) => {
    const angle = (i / 20) * Math.PI * 2;
    const dist = burstFrame > 0 ? Math.min(burstFrame * 8, 400) : 0;
    return {
      x: 960 + Math.cos(angle) * dist,
      y: 480 + Math.sin(angle) * dist,
      op: burstFrame > 0 ? Math.max(0, 1 - burstFrame / 40) : 0,
    };
  });

  // Ambient particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const r = seededRandom(i + 500);
    const r2 = seededRandom(i + 600);
    return {
      x: r * 1920,
      y: ((r2 * 1080) - frame * 0.4 + 1080) % 1080,
      size: 1 + r * 3,
      op: 0.12 + r2 * 0.2,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.22) 0%, transparent 60%), linear-gradient(135deg, #412D15 0%, #2A1B08 100%)',
          transform: `scale(${camZoom})`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '5%',
          width: 450,
          height: 450,
          border: '3px solid #FAB95B',
          borderRadius: 30,
          opacity: 0.05,
          transform: `rotate(${15 + frame * 0.07}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          right: '8%',
          width: 380,
          height: 380,
          border: '2px solid #E8EDF2',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />

      {/* Texture — dots grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="dots24" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#FAB95B" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots24)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#E8EDF2',
            opacity: p.op,
          }}
        />
      ))}

      {/* QMB CARD — top */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          opacity: cardX,
          transform: `perspective(1200px) rotateY(${cardRotate}deg)`,
          transformStyle: 'preserve-3d',
        }}
      >
        <div
          style={{
            width: 560,
            height: 220,
            background: 'linear-gradient(135deg, #FAB95B 0%, #E89F3D 100%)',
            borderRadius: 20,
            padding: '24px 36px',
            boxShadow: '0 20px 50px rgba(0,0,0,0.5)',
            position: 'relative',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#412D15', letterSpacing: 3 }}>
                QUALIFIED MEDICARE BENEFICIARY
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 64, fontWeight: 900, color: '#412D15', lineHeight: 1, marginTop: 4 }}>
                QMB
              </div>
            </div>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5" stroke="#412D15" strokeWidth="2" strokeLinejoin="round" />
            </svg>
          </div>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#412D15', letterSpacing: 6 }}>
              •••• •••• •••• 2026
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 16, fontWeight: 600, color: '#412D15', opacity: 0.7, marginTop: 6 }}>
              ACTIVE BENEFICIARY
            </div>
          </div>
        </div>
      </div>

      {/* COUNTER + HERO ZERO */}
      <div
        style={{
          position: 'absolute',
          top: 360,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `translate(${screenShake}px, ${screenShake * 0.5}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 600, color: '#E8EDF2', opacity: 0.8, letterSpacing: 4, marginBottom: 10 }}>
          YOUR BILL TODAY
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 240,
            fontWeight: 900,
            color: counterValue === 0 ? '#FAB95B' : '#E8EDF2',
            lineHeight: 1,
            textShadow: counterValue === 0 ? `0 0 ${50 * zeroGlow}px rgba(250,185,91,${0.7 * zeroGlow})` : 'none',
            transform: counterValue === 0 ? `scale(${zeroScale})` : 'scale(1)',
          }}
        >
          ${counterValue}
        </div>
      </div>

      {/* Burst particles when hitting zero */}
      {burstParticles.map((p, i) => (
        <div
          key={`burst-${i}`}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: 8,
            height: 8,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
            boxShadow: '0 0 12px #FAB95B',
          }}
        />
      ))}

      {/* FPL BAR — bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          opacity: fplOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#E8EDF2', opacity: 0.8, letterSpacing: 3, marginBottom: 10 }}>
          QMB LIMITS — TIED TO FEDERAL POVERTY LEVEL
        </div>
        <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#FAB95B' }}>2024</div>
          <div
            style={{
              width: 700,
              height: 18,
              backgroundColor: '#2D1F0E',
              borderRadius: 9,
              border: '2px solid #E8EDF2',
              overflow: 'hidden',
              position: 'relative',
            }}
          >
            <div
              style={{
                height: '100%',
                width: `${fplFill * 100}%`,
                background: 'linear-gradient(90deg, #FAB95B, #E89F3D)',
                borderRadius: 7,
              }}
            />
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#E8EDF2' }}>2026</div>
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#E8EDF2', opacity: 0.7, marginTop: 12, fontStyle: 'italic' }}>
          Adjusted every single year
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene24_ZeroDollars;
```

```tsx
import React from 'react';
Audio: "If you're struggling with your $174 Part B premium every month, this wipes that bill out completely and gives you true free health care. Program number four is LIHEAP."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene25_BillWipedLIHEAP',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene25_BillWipedLIHEAP: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [10, -10]);

  // Bill paper appears
  const billOp = sp(8);
  const billRotate = interpolate(sp(8), [0, 1], [-6, -2]);

  // WIPE animation - sweep across bill
  const wipeProgress = interpolate(frame, [90, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Free Health Care badge
  const freeHCScale = sp(180, { damping: 9, stiffness: 160 });

  // LIHEAP transition — number 4 reveal
  const fourScale = sp(245, { damping: 8, stiffness: 130 });
  const liheapOp = sp(275);

  // Snowflake + sun icons
  const iconY = Math.sin(frame * 0.08) * 4;

  // Particles
  const particles = Array.from({ length: 26 }, (_, i) => {
    const r = seededRandom(i + 700);
    const r2 = seededRandom(i + 800);
    return {
      x: ((r * 1920) + frame * 0.6) % 1920,
      y: r2 * 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.22,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 30% 40%, rgba(250,185,91,0.2) 0%, transparent 55%), linear-gradient(120deg, #412D15 0%, #2D1F0E 100%)',
          transform: `translateX(${camPan}px)`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -150,
          right: -100,
          width: 550,
          height: 550,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(250,185,91,0.1), transparent)',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.1})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -200,
          left: -150,
          width: 600,
          height: 600,
          border: '3px solid #E8EDF2',
          opacity: 0.05,
          transform: `rotate(${30 + frame * 0.06}deg)`,
        }}
      />

      {/* Texture — vertical lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="vlines25" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="15" y1="0" x2="15" y2="30" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines25)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* TOP — Struggling text */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: billOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 40, fontWeight: 700, color: '#E8EDF2', letterSpacing: 2 }}>
          Struggling with the <span style={{ color: '#FAB95B' }}>$174</span> Part B premium?
        </div>
      </div>

      {/* THE BILL */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          opacity: billOp,
          transform: `rotate(${billRotate}deg)`,
        }}
      >
        <div
          style={{
            width: 720,
            height: 320,
            backgroundColor: '#E8EDF2',
            borderRadius: 12,
            padding: 36,
            boxShadow: '0 20px 50px rgba(0,0,0,0.5)',
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '2px solid #412D15', paddingBottom: 14 }}>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 900, color: '#412D15', letterSpacing: 2 }}>MEDICARE BILL</div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 600, color: '#412D15', opacity: 0.6 }}>STATEMENT 2026</div>
          </div>
          <div style={{ marginTop: 22, display: 'flex', flexDirection: 'column', gap: 10 }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', fontFamily: 'JetBrains Mono', fontSize: 22, color: '#412D15' }}>
              <span>Part B Monthly Premium</span>
              <span style={{ fontWeight: 700 }}>$174.00</span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', fontFamily: 'JetBrains Mono', fontSize: 22, color: '#412D15' }}>
              <span>Coinsurance</span>
              <span style={{ fontWeight: 700 }}>$48.00</span>
            </div>
            <div style={{ display: 'flex', justifyContent: 'space-between', fontFamily: 'JetBrains Mono', fontSize: 22, color: '#412D15' }}>
              <span>Deductible Share</span>
              <span style={{ fontWeight: 700 }}>$72.00</span>
            </div>
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                fontFamily: 'Inter',
                fontSize: 32,
                color: '#412D15',
                fontWeight: 900,
                borderTop: '2px solid #412D15',
                paddingTop: 14,
                marginTop: 8,
              }}
            >
              <span>TOTAL DUE</span>
              <span>$294.00</span>
            </div>
          </div>

          {/* WIPE OVERLAY */}
          <div
            style={{
              position: 'absolute',
              top: 0,
              left: 0,
              width: `${wipeProgress * 100}%`,
              height: '100%',
              background: 'linear-gradient(90deg, transparent 0%, #FAB95B 50%, #FAB95B 100%)',
              mixBlendMode: 'multiply',
            }}
          />
          {/* WIPED stamp */}
          {wipeProgress > 0.5 && (
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: `translate(-50%, -50%) rotate(-15deg) scale(${interpolate(wipeProgress, [0.5, 1], [0, 1])})`,
                padding: '14px 50px',
                border: '6px solid #FF0000',
                fontFamily: 'Inter',
                fontSize: 72,
                fontWeight: 900,
                color: '#FF0000',
                letterSpacing: 8,
              }}
            >
              WIPED OUT
            </div>
          )}
        </div>
      </div>

      {/* FREE HEALTH CARE BADGE */}
      <div
        style={{
          position: 'absolute',
          top: 540,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${freeHCScale})`,
          opacity: freeHCScale,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '14px 50px',
            backgroundColor: 'transparent',
            border: '4px solid #FAB95B',
            borderRadius: 50,
            fontFamily: 'Inter',
            fontSize: 44,
            fontWeight: 900,
            color: '#FAB95B',
            letterSpacing: 6,
          }}
        >
          TRUE FREE HEALTH CARE
        </div>
      </div>

      {/* PROGRAM 4 LIHEAP REVEAL */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 30,
          opacity: liheapOp,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 200,
            fontWeight: 900,
            color: '#FAB95B',
            lineHeight: 0.85,
            transform: `scale(${fourScale})`,
            textShadow: '0 0 30px rgba(250,185,91,0.4)',
          }}
        >
          4
        </div>
        <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 600, color: '#E8EDF2', opacity: 0.7, letterSpacing: 4 }}>
            PROGRAM NUMBER
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 96, fontWeight: 900, color: '#E8EDF2', lineHeight: 0.9 }}>LIHEAP</div>
          {/* snowflake + sun icons */}
          <div style={{ display: 'flex', gap: 18, marginTop: 8, transform: `translateY(${iconY}px)` }}>
            <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
              <path
                d="M12 2v20M2 12h20M5 5l14 14M19 5L5 19"
                stroke="#E8EDF2"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
            <svg width="42" height="42" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="4" stroke="#FAB95B" strokeWidth="2" />
              <path
                d="M12 2v3M12 19v3M2 12h3M19 12h3M5 5l2 2M17 17l2 2M19 5l-2 2M7 17l-2 2"
                stroke="#FAB95B"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene25_BillWipedLIHEAP;
```

```tsx
import React from 'react';
Audio: "That's the Low Income Home Energy Assistance Program — the safety net for extreme weather. In summer the AC bill skyrockets. In winter the heating bill doubles."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene26_ExtremeWeather',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene26_ExtremeWeather: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Acronym build
  const acronymOp = sp(8);
  const lOp = sp(15);
  const iOp = sp(28);
  const hOp = sp(41);
  const eOp = sp(54);
  const aOp = sp(67);
  const pOp = sp(80);

  // Split screen reveal — summer vs winter
  const splitOp = sp(120);
  const summerBar = interpolate(frame, [150, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const winterBar = interpolate(frame, [180, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Summer rays
  const sunRotate = frame * 0.5;

  // Snow flakes for winter side
  const snowflakes = Array.from({ length: 18 }, (_, i) => {
    const r = seededRandom(i + 900);
    return {
      x: 960 + r * 900,
      y: ((seededRandom(i + 1000) * 1080) + frame * (1 + r * 1.5)) % 1080,
      size: 3 + r * 5,
    };
  });

  // Ambient particles
  const particles = Array.from({ length: 18 }, (_, i) => {
    const r = seededRandom(i + 1100);
    const r2 = seededRandom(i + 1200);
    return {
      x: r * 1920,
      y: ((r2 * 1080) - frame * 0.3 + 1080) % 1080,
      size: 1 + r * 2,
      op: 0.1 + r2 * 0.18,
    };
  });

  const letters = [
    { l: 'L', op: lOp },
    { l: 'I', op: iOp },
    { l: 'H', op: hOp },
    { l: 'E', op: eOp },
    { l: 'A', op: aOp },
    { l: 'P', op: pOp },
  ];

  const fullWords = ['Low', 'Income', 'Home', 'Energy', 'Assistance', 'Program'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Split background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(90deg, rgba(250,185,91,0.15) 0%, rgba(250,185,91,0.05) 49%, rgba(232,237,242,0.05) 51%, rgba(232,237,242,0.12) 100%), linear-gradient(180deg, #412D15 0%, #2D1F0E 100%)',
          transform: `scale(${camZoom})`,
        }}
      />

      {/* Center divider */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          bottom: 0,
          left: '50%',
          width: 2,
          backgroundColor: '#E8EDF2',
          opacity: splitOp * 0.3,
          transform: 'translateX(-50%)',
        }}
      />

      {/* Shape — sun (left) */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '12%',
          width: 300,
          height: 300,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(250,185,91,0.12), transparent 65%)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />
      {/* Shape — snowflake big (right) */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '12%',
          width: 280,
          height: 280,
          opacity: 0.08,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      >
        <svg viewBox="0 0 24 24" width="100%" height="100%">
          <path
            d="M12 2v20M2 12h20M5 5l14 14M19 5L5 19"
            stroke="#E8EDF2"
            strokeWidth="0.8"
            strokeLinecap="round"
          />
        </svg>
      </div>

      {/* Texture grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="grid26" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#E8EDF2" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid26)" />
      </svg>

      {/* Ambient particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* Snowflakes on winter side */}
      {snowflakes.map((s, i) => (
        <div
          key={`snow-${i}`}
          style={{
            position: 'absolute',
            left: s.x,
            top: s.y,
            width: s.size,
            height: s.size,
            borderRadius: '50%',
            backgroundColor: '#E8EDF2',
            opacity: splitOp * 0.6,
          }}
        />
      ))}

      {/* TOP — LIHEAP acronym */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: acronymOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#FAB95B', letterSpacing: 6, marginBottom: 12 }}>
          LOW INCOME HOME ENERGY ASSISTANCE PROGRAM
        </div>
        <div style={{ display: 'flex', justifyContent: 'center', gap: 18 }}>
          {letters.map((it, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                opacity: it.op,
                transform: `translateY(${(1 - it.op) * 20}px)`,
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 120,
                  fontWeight: 900,
                  color: '#E8EDF2',
                  lineHeight: 0.9,
                  textShadow: '0 4px 14px rgba(0,0,0,0.4)',
                }}
              >
                {it.l}
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#FAB95B', marginTop: -2, letterSpacing: 1 }}>
                {fullWords[i]}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* TAGLINE */}
      <div
        style={{
          position: 'absolute',
          top: 430,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: splitOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 700, color: '#E8EDF2', fontStyle: 'italic' }}>
          The Safety Net for <span style={{ color: '#FAB95B' }}>Extreme Weather</span>
        </div>
      </div>

      {/* SUMMER VS WINTER comparison */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'space-around',
          alignItems: 'flex-end',
          padding: '0 100px',
          opacity: splitOp,
        }}
      >
        {/* SUMMER */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 600 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 12 }}>
            <svg width="44" height="44" viewBox="0 0 24 24" style={{ transform: `rotate(${sunRotate}deg)` }}>
              <circle cx="12" cy="12" r="4" fill="#FAB95B" />
              <path
                d="M12 2v3M12 19v3M2 12h3M19 12h3M5 5l2 2M17 17l2 2M19 5l-2 2M7 17l-2 2"
                stroke="#FAB95B"
                strokeWidth="2.5"
                strokeLinecap="round"
              />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#FAB95B', letterSpacing: 4 }}>SUMMER</div>
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#E8EDF2', opacity: 0.7, marginBottom: 10 }}>AC BILL</div>
          <div style={{ width: '100%', height: 36, backgroundColor: '#2D1F0E', borderRadius: 8, overflow: 'hidden', border: '2px solid #FAB95B' }}>
            <div
              style={{
                height: '100%',
                width: `${summerBar * 92}%`,
                background: 'linear-gradient(90deg, #FAB95B, #E89F3D)',
              }}
            />
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#FAB95B', marginTop: 10 }}>
            ↑ SKYROCKETS
          </div>
        </div>

        {/* WINTER */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 600 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 12 }}>
            <svg width="44" height="44" viewBox="0 0 24 24" style={{ transform: `rotate(${-frame * 0.3}deg)` }}>
              <path
                d="M12 2v20M2 12h20M5 5l14 14M19 5L5 19"
                stroke="#E8EDF2"
                strokeWidth="2.5"
                strokeLinecap="round"
              />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#E8EDF2', letterSpacing: 4 }}>WINTER</div>
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#E8EDF2', opacity: 0.7, marginBottom: 10 }}>HEATING BILL</div>
          <div style={{ width: '100%', height: 36, backgroundColor: '#2D1F0E', borderRadius: 8, overflow: 'hidden', border: '2px solid #E8EDF2' }}>
            <div
              style={{
                height: '100%',
                width: `${winterBar * 100}%`,
                background: 'linear-gradient(90deg, #E8EDF2, #B8C2CC)',
              }}
            />
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#E8EDF2', marginTop: 10 }}>
            ↑ DOUBLES
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene26_ExtremeWeather;
```

```tsx
import React from 'react';
Audio: "For a senior on fixed income, this is the moment of a terrible choice: keep the heat on, or buy groceries? LIHEAP eliminates that choice."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene27_TerribleChoice',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene27_TerribleChoice: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [1, -1]);

  // Top label
  const topOp = sp(8);

  // VS scale showing two sides
  const heatScale = sp(40);
  const vsRotate = interpolate(sp(70), [0, 1], [-180, 0]);
  const foodScale = sp(95);

  // Tipping scale animation
  const scaleAngle = Math.sin(frame * 0.05) * 4;

  // "ELIMINATES" smash reveal
  const eliminateScale = sp(200, { damping: 8, stiffness: 180 });
  const eliminateShake = frame > 200 && frame < 230 ? Math.sin(frame * 1.5) * 4 : 0;

  // Cross line through "or"
  const crossLine = interpolate(frame, [210, 270], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => {
    const r = seededRandom(i + 1300);
    const r2 = seededRandom(i + 1400);
    return {
      x: r * 1920,
      y: ((r2 * 1080) + frame * (0.3 + r * 0.4)) % 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.22,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 60%, rgba(255,0,0,0.08) 0%, transparent 50%), radial-gradient(circle at 20% 20%, rgba(250,185,91,0.15) 0%, transparent 55%), linear-gradient(180deg, #412D15 0%, #2D1F0E 100%)',
          transform: `rotate(${camTilt}deg) scale(1.04)`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          left: '8%',
          width: 380,
          height: 380,
          border: '3px solid #FAB95B',
          opacity: 0.06,
          borderRadius: 30,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '12%',
          right: '6%',
          width: 460,
          height: 460,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(232,237,242,0.05), transparent)',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
        }}
      />

      {/* Texture — crosshatch */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        <defs>
          <pattern id="cross27" width="50" height="50" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="25" x2="50" y2="25" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross27)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#E8EDF2',
            opacity: p.op,
          }}
        />
      ))}

      {/* TOP LABEL */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: topOp,
          transform: `translateY(${(1 - topOp) * -30}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 600, color: '#FAB95B', letterSpacing: 6, marginBottom: 6 }}>
          THE FIXED-INCOME DILEMMA
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 64, fontWeight: 900, color: '#E8EDF2', lineHeight: 1, fontStyle: 'italic' }}>
          A terrible choice
        </div>
      </div>

      {/* TIPPING SCALE — center */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        <div style={{ position: 'relative', width: 1100, height: 300, transform: `rotate(${scaleAngle}deg)`, transformOrigin: '550px 200px' }}>
          {/* Scale beam */}
          <div
            style={{
              position: 'absolute',
              top: 100,
              left: 80,
              right: 80,
              height: 6,
              backgroundColor: '#E8EDF2',
              borderRadius: 3,
            }}
          />
          {/* Pivot */}
          <div
            style={{
              position: 'absolute',
              top: 100,
              left: '50%',
              width: 4,
              height: 100,
              backgroundColor: '#E8EDF2',
              transform: 'translateX(-50%)',
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: 195,
              left: '50%',
              width: 100,
              height: 10,
              backgroundColor: '#E8EDF2',
              transform: 'translateX(-50%)',
              borderRadius: 4,
            }}
          />

          {/* LEFT — HEAT */}
          <div
            style={{
              position: 'absolute',
              top: 0,
              left: 30,
              width: 300,
              height: 220,
              backgroundColor: '#2D1F0E',
              border: '4px solid #FAB95B',
              borderRadius: 16,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${heatScale})`,
              boxShadow: '0 12px 30px rgba(250,185,91,0.3)',
            }}
          >
            <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
              <path
                d="M12 2c-1 3-3 4-3 7a3 3 0 006 0c0-1-1-2-1-3M12 14c-3 0-5 2-5 5a5 5 0 0010 0c0-2-1-3-2-4"
                stroke="#FAB95B"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#FAB95B', letterSpacing: 3 }}>HEAT ON</div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#E8EDF2', opacity: 0.7, marginTop: 4 }}>or freeze</div>
          </div>

          {/* CENTER — OR */}
          <div
            style={{
              position: 'absolute',
              top: 50,
              left: '50%',
              transform: `translateX(-50%) rotate(${vsRotate}deg)`,
              width: 110,
              height: 110,
              borderRadius: '50%',
              backgroundColor: '#FF0000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter',
              fontSize: 52,
              fontWeight: 900,
              color: '#E8EDF2',
              boxShadow: '0 10px 30px rgba(255,0,0,0.5)',
              position: 'relative' as const,
            }}
          >
            OR
            {/* cross line */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '-20%',
                width: `${140 * crossLine}%`,
                height: 8,
                backgroundColor: '#FAB95B',
                transform: 'translateY(-50%) rotate(-12deg)',
                borderRadius: 4,
                boxShadow: '0 0 14px rgba(250,185,91,0.7)',
              }}
            />
          </div>

          {/* RIGHT — GROCERIES */}
          <div
            style={{
              position: 'absolute',
              top: 0,
              right: 30,
              width: 300,
              height: 220,
              backgroundColor: '#2D1F0E',
              border: '4px solid #E8EDF2',
              borderRadius: 16,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${foodScale})`,
              boxShadow: '0 12px 30px rgba(232,237,242,0.15)',
            }}
          >
            <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
              <path
                d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4H6zM3 6h18M16 10a4 4 0 01-8 0"
                stroke="#E8EDF2"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 38, fontWeight: 900, color: '#E8EDF2', letterSpacing: 3 }}>GROCERIES</div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#E8EDF2', opacity: 0.7, marginTop: 4 }}>or hunger</div>
          </div>
        </div>
      </div>

      {/* ELIMINATES BANNER */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${eliminateScale}) translate(${eliminateShake}px, ${eliminateShake * 0.5}px)`,
          opacity: eliminateScale,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#FAB95B', letterSpacing: 5, marginBottom: 8 }}>
          LIHEAP
        </div>
        <div
          style={{
            display: 'inline-block',
            padding: '16px 60px',
            backgroundColor: '#FAB95B',
            borderRadius: 12,
            fontFamily: 'Inter',
            fontSize: 72,
            fontWeight: 900,
            color: '#412D15',
            letterSpacing: 6,
            boxShadow: '0 14px 40px rgba(250,185,91,0.5)',
          }}
        >
          ELIMINATES THE CHOICE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene27_TerribleChoice;
```

```tsx
import React from 'react';
Audio: "This federal program provides direct financial assistance for heating and cooling bills. The money goes straight to your utility company. And for seniors, special crisis funds are often available."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene28_MoneyFlowsToUtility',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene28_MoneyFlowsToUtility: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Title
  const titleOp = sp(8);

  // Three nodes
  const govScale = sp(40);
  const utilityScale = sp(110);
  const homeScale = sp(170);

  // Flow lines
  const flow1 = interpolate(frame, [75, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const flow2 = interpolate(frame, [200, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Crisis fund alert
  const crisisOp = sp(290);
  const crisisPulse = 1 + Math.sin(frame * 0.18) * 0.03;

  // Flowing dollars
  const dollarFlows = Array.from({ length: 5 }, (_, i) => ({
    progress: ((frame + i * 18) % 80) / 80,
    delay: i * 18,
  }));

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const r = seededRandom(i + 1500);
    const r2 = seededRandom(i + 1600);
    return {
      x: ((r * 1920) + frame * 0.4) % 1920,
      y: r2 * 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.22,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.2) 0%, transparent 55%), linear-gradient(180deg, #412D15 0%, #2A1B0A 100%)',
          transform: `scale(${camZoom})`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: -100,
          left: '40%',
          width: 600,
          height: 600,
          border: '2px solid #FAB95B',
          opacity: 0.05,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -180,
          right: '8%',
          width: 500,
          height: 500,
          opacity: 0.05,
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          background: '#E8EDF2',
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      {/* Texture — flow lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="flow28" width="80" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="10" x2="80" y2="10" stroke="#FAB95B" strokeWidth="1" strokeDasharray="6 4" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#flow28)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * -30}px)`,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 600, color: '#FAB95B', letterSpacing: 6, marginBottom: 6 }}>
          HOW THE MONEY MOVES
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 72, fontWeight: 900, color: '#E8EDF2', lineHeight: 1 }}>
          Direct to Your <span style={{ color: '#FAB95B' }}>Utility Company</span>
        </div>
      </div>

      {/* THREE NODES — diagram */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 40,
        }}
      >
        {/* GOVERNMENT */}
        <div
          style={{
            width: 280,
            height: 240,
            backgroundColor: '#2D1F0E',
            border: '3px solid #FAB95B',
            borderRadius: 18,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${govScale})`,
            boxShadow: '0 12px 30px rgba(250,185,91,0.25)',
          }}
        >
          <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
            <path
              d="M3 21h18M5 21V10l7-5 7 5v11M8 21v-7h8v7M10 11h4"
              stroke="#FAB95B"
              strokeWidth="2"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#FAB95B', letterSpacing: 2 }}>FEDERAL</div>
          <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#E8EDF2', marginTop: 4 }}>LIHEAP</div>
        </div>

        {/* FLOW 1 */}
        <div style={{ position: 'relative', width: 180, height: 80 }}>
          <svg width="180" height="80" viewBox="0 0 180 80">
            <line x1="0" y1="40" x2={160 * flow1} y2="40" stroke="#FAB95B" strokeWidth="4" strokeDasharray="10 6" />
            <polygon points={`${160 * flow1},25 ${180 * flow1},40 ${160 * flow1},55`} fill="#FAB95B" opacity={flow1} />
          </svg>
          {dollarFlows.map((d, i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: d.progress * 150,
                top: 18,
                width: 38,
                height: 38,
                borderRadius: '50%',
                backgroundColor: '#FAB95B',
                color: '#412D15',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Inter',
                fontSize: 22,
                fontWeight: 900,
                opacity: flow1,
                boxShadow: '0 0 14px rgba(250,185,91,0.7)',
              }}
            >
              $
            </div>
          ))}
        </div>

        {/* UTILITY COMPANY */}
        <div
          style={{
            width: 300,
            height: 260,
            backgroundColor: '#FAB95B',
            borderRadius: 18,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${utilityScale})`,
            boxShadow: '0 14px 40px rgba(250,185,91,0.4)',
            position: 'relative',
          }}
        >
          <svg width="68" height="68" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
            <path
              d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"
              stroke="#412D15"
              strokeWidth="2.2"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 900, color: '#412D15', letterSpacing: 2 }}>UTILITY</div>
          <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#412D15', opacity: 0.8 }}>COMPANY</div>
          <div
            style={{
              marginTop: 10,
              padding: '4px 14px',
              backgroundColor: '#412D15',
              borderRadius: 6,
              fontFamily: 'Inter',
              fontSize: 16,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 2,
            }}
          >
            PAID DIRECT
          </div>
        </div>

        {/* FLOW 2 */}
        <div style={{ position: 'relative', width: 180, height: 80 }}>
          <svg width="180" height="80" viewBox="0 0 180 80">
            <line x1="0" y1="40" x2={160 * flow2} y2="40" stroke="#E8EDF2" strokeWidth="4" strokeDasharray="10 6" />
            <polygon points={`${160 * flow2},25 ${180 * flow2},40 ${160 * flow2},55`} fill="#E8EDF2" opacity={flow2} />
          </svg>
        </div>

        {/* HOME — service stays on */}
        <div
          style={{
            width: 280,
            height: 240,
            backgroundColor: '#2D1F0E',
            border: '3px solid #E8EDF2',
            borderRadius: 18,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `scale(${homeScale})`,
            boxShadow: '0 12px 30px rgba(232,237,242,0.15)',
          }}
        >
          <svg width="64" height="64" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
            <path
              d="M3 12L12 3l9 9M5 10v10h14V10M9 20v-6h6v6"
              stroke="#E8EDF2"
              strokeWidth="2"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 900, color: '#E8EDF2', letterSpacing: 2 }}>YOUR HOME</div>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 700, color: '#FAB95B', marginTop: 6, letterSpacing: 1 }}>
            SERVICE STAYS ON
          </div>
        </div>
      </div>

      {/* CRISIS FUND ALERT */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          opacity: crisisOp,
          transform: `scale(${crisisPulse})`,
        }}
      >
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            padding: '20px 50px',
            backgroundColor: '#FF0000',
            borderRadius: 14,
            boxShadow: '0 12px 40px rgba(255,0,0,0.4)',
          }}
        >
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
            <path
              d="M12 2L1 21h22L12 2zM12 9v4M12 17h.01"
              stroke="#E8EDF2"
              strokeWidth="2.5"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#E8EDF2', opacity: 0.85, letterSpacing: 3 }}>
              SENIOR BONUS
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 42, fontWeight: 900, color: '#E8EDF2', letterSpacing: 2, lineHeight: 1 }}>
              SPECIAL CRISIS FUNDS AVAILABLE
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene28_MoneyFlowsToUtility;
```

```tsx
import React from 'react';
Audio: "...if your income suddenly drops or you face a medical emergency. In many states, if you already get Extra Help or SNAP, you're automatically eligible for LIHEAP — no re-proving income."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene29_AutoEligible',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene29_AutoEligible: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-12, 12]);

  // Income drop chart
  const chartOp = sp(8);
  const lineProgress = interpolate(frame, [30, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Emergency icon
  const emergencyScale = sp(95);
  const emergencyPulse = 1 + Math.sin(frame * 0.18) * 0.04;

  // ELSE IF logic — Extra Help + SNAP boxes
  const ehOp = sp(150);
  const snapOp = sp(170);
  const plusOp = sp(195);

  // = AUTO QUALIFY
  const equalsOp = sp(225);
  const autoScale = sp(245, { damping: 8, stiffness: 160 });

  // No paperwork stamp
  const stampScale = sp(290, { damping: 7, stiffness: 170 });
  const stampRotate = interpolate(sp(290), [0, 1], [-25, -8]);

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const r = seededRandom(i + 1700);
    const r2 = seededRandom(i + 1800);
    return {
      x: r * 1920,
      y: ((r2 * 1080) + frame * 0.4) % 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.22,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 70% 30%, rgba(250,185,91,0.18) 0%, transparent 55%), linear-gradient(160deg, #412D15 0%, #2D1F0E 100%)',
          transform: `translateX(${camPan}px)`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '8%',
          right: -120,
          width: 480,
          height: 480,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(250,185,91,0.1), transparent)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          left: '-5%',
          width: 500,
          height: 500,
          border: '3px solid #E8EDF2',
          opacity: 0.05,
          borderRadius: 30,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {/* Texture — dot grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="dots29" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.2" fill="#E8EDF2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots29)" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: chartOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#E8EDF2', lineHeight: 1 }}>
          Already in the System? <span style={{ color: '#FAB95B' }}>You're In.</span>
        </div>
      </div>

      {/* INCOME DROP MINI CHART (left) */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          left: 120,
          width: 440,
          height: 240,
          backgroundColor: '#2D1F0E',
          border: '2px solid #FAB95B',
          borderRadius: 14,
          padding: 24,
          opacity: chartOp,
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 700, color: '#FAB95B', letterSpacing: 2, marginBottom: 8 }}>
          INCOME DROPS
        </div>
        <svg width="100%" height="160" viewBox="0 0 400 160">
          <line x1="0" y1="140" x2="400" y2="140" stroke="#E8EDF2" strokeWidth="1.5" opacity="0.5" />
          <line x1="20" y1="0" x2="20" y2="140" stroke="#E8EDF2" strokeWidth="1.5" opacity="0.5" />
          <path
            d={`M 20 ${40 + (1 - lineProgress) * 0} L ${20 + 380 * lineProgress} ${40 + lineProgress * 90}`}
            stroke="#FF0000"
            strokeWidth="4"
            fill="none"
            strokeLinecap="round"
          />
          <circle cx={20 + 380 * lineProgress} cy={40 + lineProgress * 90} r="8" fill="#FF0000" opacity={lineProgress} />
        </svg>
      </div>

      {/* MEDICAL EMERGENCY (right) */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 120,
          width: 440,
          height: 240,
          backgroundColor: '#2D1F0E',
          border: '2px solid #FF0000',
          borderRadius: 14,
          padding: 24,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `scale(${emergencyScale * emergencyPulse})`,
          opacity: emergencyScale,
        }}
      >
        <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
          <path d="M12 5v14M5 12h14" stroke="#FF0000" strokeWidth="3.5" strokeLinecap="round" />
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 900, color: '#FF0000', letterSpacing: 3, marginTop: 10 }}>
          MEDICAL EMERGENCY
        </div>
      </div>

      {/* LOGIC EQUATION row */}
      <div
        style={{
          position: 'absolute',
          top: 510,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 24,
        }}
      >
        {/* Extra Help */}
        <div
          style={{
            padding: '18px 30px',
            backgroundColor: '#FAB95B',
            borderRadius: 12,
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 900,
            color: '#412D15',
            letterSpacing: 2,
            opacity: ehOp,
            transform: `scale(${ehOp})`,
          }}
        >
          EXTRA HELP
        </div>

        {/* + */}
        <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#E8EDF2', opacity: plusOp }}>+</div>

        {/* SNAP */}
        <div
          style={{
            padding: '18px 30px',
            backgroundColor: '#FAB95B',
            borderRadius: 12,
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 900,
            color: '#412D15',
            letterSpacing: 2,
            opacity: snapOp,
            transform: `scale(${snapOp})`,
          }}
        >
          SNAP
        </div>

        {/* = */}
        <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 900, color: '#E8EDF2', opacity: equalsOp }}>=</div>

        {/* AUTO LIHEAP */}
        <div
          style={{
            padding: '20px 38px',
            background: 'linear-gradient(135deg, #FAB95B, #E89F3D)',
            borderRadius: 12,
            fontFamily: 'Inter',
            fontSize: 38,
            fontWeight: 900,
            color: '#412D15',
            letterSpacing: 3,
            transform: `scale(${autoScale})`,
            opacity: autoScale,
            boxShadow: '0 10px 30px rgba(250,185,91,0.5)',
          }}
        >
          AUTO LIHEAP
        </div>
      </div>

      {/* NO PAPERWORK STAMP */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${stampScale}) rotate(${stampRotate}deg)`,
          opacity: stampScale,
        }}
      >
        <div
          style={{
            display: 'inline-block',
            padding: '14px 50px',
            border: '5px solid #E8EDF2',
            borderRadius: 10,
            fontFamily: 'Inter',
            fontSize: 52,
            fontWeight: 900,
            color: '#E8EDF2',
            letterSpacing: 6,
          }}
        >
          NO RE-PROVING INCOME
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene29_AutoEligible;
```

```tsx
import React from 'react';
Audio: "It's a massive relief valve so you never sit in a freezing house or sweltering apartment to save a few bucks. And program number five — the one almost nobody talks about, the ultimate lifesaver for homeowners."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene30_ReliefValveTo5',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene30_ReliefValveTo5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // Pressure gauge sweep
  const gaugeOp = sp(8);
  const needleSweep = interpolate(frame, [40, 130], [-90, 60], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  // then pull back to safe zone after relief
  const needleRelief = interpolate(frame, [140, 200], [60, -45], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const needleAngle = frame < 140 ? needleSweep : needleRelief;

  // VALVE label
  const valveOp = sp(180);

  // Transition divider
  const dividerProgress = interpolate(frame, [220, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Number 5 hero
  const fiveScale = sp(280, { damping: 8, stiffness: 130 });
  const fiveGlow = interpolate(Math.sin(frame * 0.15), [-1, 1], [0.6, 1]);

  // "Lifesaver" reveal
  const lifesaverOp = sp(330);

  // Steam particles for pressure
  const steamParticles = Array.from({ length: 14 }, (_, i) => {
    const r = seededRandom(i + 1900);
    const lifecycle = ((frame + i * 8) % 60) / 60;
    return {
      x: 580 + r * 60 - 30,
      y: 380 - lifecycle * 200,
      size: 6 + lifecycle * 14,
      op: (1 - lifecycle) * 0.4,
    };
  });

  // Ambient particles
  const particles = Array.from({ length: 24 }, (_, i) => {
    const r = seededRandom(i + 2000);
    const r2 = seededRandom(i + 2100);
    return {
      x: r * 1920,
      y: ((r2 * 1080) - frame * 0.5 + 1080) % 1080,
      size: 1 + r * 3,
      op: 0.1 + r2 * 0.22,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 30% 50%, rgba(255,0,0,0.08) 0%, transparent 50%), radial-gradient(circle at 75% 50%, rgba(250,185,91,0.18) 0%, transparent 55%), linear-gradient(135deg, #412D15 0%, #2A1B08 100%)',
          transform: `scale(${camZoom})`,
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '12%',
          right: '8%',
          width: 380,
          height: 380,
          border: '3px solid #FAB95B',
          opacity: 0.05,
          borderRadius: 30,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '8%',
          left: '4%',
          width: 460,
          height: 460,
          border: '2px solid #E8EDF2',
          opacity: 0.04,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
        }}
      />

      {/* Texture — concentric rings */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
        {[140, 280, 420, 560].map((r) => (
          <circle key={r} cx="600" cy="540" r={r} fill="none" stroke="#FAB95B" strokeWidth="1" />
        ))}
      </svg>

      {/* Ambient particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FAB95B',
            opacity: p.op,
          }}
        />
      ))}

      {/* LEFT SIDE — pressure gauge */}
      <div
        style={{
          position: 'absolute',
          top: 140,
          left: 80,
          width: 1000 * (1 - dividerProgress),
          opacity: 1 - dividerProgress * 0.7,
          overflow: 'hidden',
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 600, color: '#FAB95B', letterSpacing: 5, opacity: gaugeOp, marginBottom: 10 }}>
          THE RELIEF VALVE
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 62, fontWeight: 900, color: '#E8EDF2', lineHeight: 1, opacity: gaugeOp, marginBottom: 30 }}>
          Pressure → Released
        </div>

        {/* GAUGE */}
        <div style={{ position: 'relative', width: 480, height: 280, marginLeft: 60 }}>
          <svg width="480" height="280" viewBox="0 0 480 280">
            {/* gauge arc background */}
            <path
              d="M 60 220 A 180 180 0 0 1 420 220"
              fill="none"
              stroke="#2D1F0E"
              strokeWidth="28"
              strokeLinecap="round"
            />
            {/* colored zones */}
            <path
              d="M 60 220 A 180 180 0 0 1 180 70"
              fill="none"
              stroke="#FAB95B"
              strokeWidth="28"
              strokeLinecap="round"
              opacity="0.85"
            />
            <path
              d="M 180 70 A 180 180 0 0 1 300 70"
              fill="none"
              stroke="#E8EDF2"
              strokeWidth="28"
              strokeLinecap="round"
              opacity="0.5"
            />
            <path
              d="M 300 70 A 180 180 0 0 1 420 220"
              fill="none"
              stroke="#FF0000"
              strokeWidth="28"
              strokeLinecap="round"
              opacity="0.85"
            />
            {/* Tick labels */}
            <text x="60" y="255" fill="#FAB95B" fontSize="20" fontFamily="Inter" fontWeight="700">SAFE</text>
            <text x="380" y="255" fill="#FF0000" fontSize="20" fontFamily="Inter" fontWeight="700">CRISIS</text>
            {/* Needle */}
            <g transform={`translate(240,220) rotate(${needleAngle})`}>
              <line x1="0" y1="0" x2="0" y2="-170" stroke="#E8EDF2" strokeWidth="6" strokeLinecap="round" />
              <circle cx="0" cy="-170" r="8" fill="#E8EDF2" />
            </g>
            {/* Center cap */}
            <circle cx="240" cy="220" r="18" fill="#FAB95B" stroke="#412D15" strokeWidth="3" />
          </svg>

          {/* Steam particles */}
          {steamParticles.map((p, i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: p.x - 240,
                top: p.y,
                width: p.size,
                height: p.size,
                borderRadius: '50%',
                backgroundColor: '#E8EDF2',
                opacity: p.op,
                filter: 'blur(2px)',
              }}
            />
          ))}
        </div>

        <div
          style={{
            marginTop: 20,
            marginLeft: 60,
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            color: '#E8EDF2',
            opacity: valveOp,
          }}
        >
          Never sit in a <span style={{ color: '#FAB95B' }}>freezing</span> or <span style={{ color: '#FF0000' }}>sweltering</span> home.
        </div>
      </div>

      {/* DIVIDER LINE — sweeping */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          bottom: 100,
          left: `${50 + (1 - dividerProgress) * 60}%`,
          width: 3,
          backgroundColor: '#FAB95B',
          opacity: dividerProgress > 0.05 ? 0.9 : 0,
          boxShadow: '0 0 20px rgba(250,185,91,0.7)',
        }}
      />

      {/* RIGHT SIDE — Program #5 reveal */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          right: 0,
          bottom: 0,
          width: `${dividerProgress * 50}%`,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          overflow: 'hidden',
        }}
      >
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 600, color: '#FAB95B', letterSpacing: 6, marginBottom: 4, opacity: fiveScale }}>
          PROGRAM NUMBER
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 380,
            fontWeight: 900,
            color: '#FAB95B',
            lineHeight: 0.85,
            transform: `scale(${fiveScale})`,
            textShadow: `0 0 ${40 * fiveGlow}px rgba(250,185,91,${0.6 * fiveGlow})`,
          }}
        >
          5
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 900,
            color: '#E8EDF2',
            letterSpacing: 4,
            opacity: lifesaverOp,
            marginTop: -10,
            textAlign: 'center',
          }}
        >
          THE ULTIMATE
          <br />
          <span style={{ color: '#FAB95B' }}>LIFESAVER</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 20,
            fontWeight: 600,
            color: '#E8EDF2',
            opacity: lifesaverOp * 0.7,
            marginTop: 14,
            letterSpacing: 3,
          }}
        >
          FOR HOMEOWNERS
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene30_ReliefValveTo5;
```

