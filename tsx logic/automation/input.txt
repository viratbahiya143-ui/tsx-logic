```tsx
import React from 'react';
SCÈNE 21 | [09:59 - 10:29]
Audio : "Crée de la friction pour dépenser et de la fluidité pour épargner."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S21FrictionFluidite',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S21FrictionFluidite: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in + micro pan
  const camScale = interpolate(frame, [0, durationInFrames], [1.07, 1.0], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.007) * 16;
  const camY = Math.cos(frame * 0.005) * 10;

  const titleIn = sp(6);
  const laneA = sp(48);
  const laneB = sp(78);
  const payoff = sp(640, { damping: 14 });

  // Looping travel: friction lane stutters, fluid lane glides
  const loop = frame % 300;
  const coinA = interpolate(
    loop,
    [30, 80, 100, 140, 162, 205, 228, 285],
    [0, 92, 98, 178, 186, 252, 259, 300],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const coinB = interpolate(loop, [30, 120], [0, 640], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const impact = interpolate(loop, [120, 126, 138], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = impact * Math.sin(frame * 2.2) * 7;

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 22% 18%, rgba(201,184,255,0.35) 0%, rgba(80,64,153,0) 55%), radial-gradient(circle at 82% 84%, rgba(255,201,77,0.22) 0%, rgba(80,64,153,0) 60%), linear-gradient(140deg, #5A48AA 0%, #504099 45%, #392C74 100%)',
        }}
      />

      {/* Layer 2 — Animated transparent shape layers (drifting geometry) */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute', left: -180, top: 120, width: 760, height: 760, borderRadius: '50%',
            border: '3px solid #F5F5F5', opacity: 0.06,
            transform: `rotate(${frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.012) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute', right: -140, top: -110, width: 620, height: 620,
            backgroundColor: '#000000', opacity: 0.05,
            transform: `rotate(${-frame * 0.08 + 18}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 720, bottom: -260, width: 0, height: 0,
            borderLeft: '300px solid transparent', borderRight: '300px solid transparent',
            borderBottom: '480px solid #FFC94D', opacity: 0.05,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (punk halftone dot grid) */}
      <AbsoluteFill style={{ opacity: 0.14 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="dots21" width="26" height="26" patternUnits="userSpaceOnUse">
              <circle cx="4" cy="4" r="2.2" fill="#F5F5F5" />
            </pattern>
          </defs>
          <rect
            x="0" y="0" width="100%" height="100%" fill="url(#dots21)"
            transform={`translate(${Math.sin(frame * 0.01) * 14}, ${frame * 0.16 % 26})`}
          />
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 3);
          const s2 = seededRandom(i + 91);
          const size = 4 + s * 9;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: s * 1920,
                top: ((s2 * 1080 + frame * (0.35 + s * 0.7)) % 1140) - 60,
                width: size, height: size,
                backgroundColor: i % 3 === 0 ? '#FFC94D' : '#F5F5F5',
                opacity: 0.18 + s2 * 0.2,
                borderRadius: i % 2 === 0 ? '50%' : 0,
                transform: `rotate(${frame * (0.4 + s)}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${camX + shake}px, ${camY}px)`,
          padding: '90px 120px',
          display: 'flex', flexDirection: 'column', justifyContent: 'center',
        }}
      >
        {/* Collage kicker tape */}
        <div
          style={{
            alignSelf: 'flex-start', backgroundColor: '#000000', color: '#F5F5F5',
            fontFamily: 'Space Mono, monospace', fontSize: 30, letterSpacing: 4, padding: '10px 22px',
            transform: `rotate(-2.4deg) translateY(${interpolate(titleIn, [0, 1], [-60, 0])}px)`,
            opacity: titleIn,
          }}
        >
          PRINCIPE 04 / ARCHITECTURE
        </div>

        <div
          style={{
            fontFamily: 'Anton, sans-serif', fontSize: 112, lineHeight: 1.02, color: '#F5F5F5',
            marginTop: 26, textTransform: 'uppercase',
            transform: `translateX(${interpolate(titleIn, [0, 1], [-120, 0])}px)`, opacity: titleIn,
          }}
        >
          FRICTION POUR DÉPENSER
        </div>
        <div
          style={{
            fontFamily: 'Anton, sans-serif', fontSize: 112, lineHeight: 1.02, color: '#000000',
            backgroundColor: '#FFC94D', alignSelf: 'flex-start', padding: '4px 20px', marginTop: 10,
            transform: `rotate(-1.2deg) translateX(${interpolate(sp(26), [0, 1], [140, 0])}px)`, opacity: sp(26),
          }}
        >
          FLUIDITÉ POUR ÉPARGNER
        </div>

        {/* Lane A — friction path */}
        <div style={{ marginTop: 58, opacity: laneA, transform: `translateY(${interpolate(laneA, [0, 1], [50, 0])}px)` }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 34, color: '#F5F5F5', letterSpacing: 2 }}>
            ACHAT EN LIGNE
          </div>
          <svg width="1400" height="90" style={{ marginTop: 8 }}>
            <path
              d="M0 62 L60 62 L80 34 L100 62 L160 62 L180 34 L200 62 L260 62 L280 34 L300 62 L360 62 L380 34 L400 62 L1360 62"
              stroke="#F5F5F5" strokeWidth="5" fill="none" opacity="0.85"
            />
            <circle cx={40 + coinA} cy="46" r="17" fill="#FF6B6B" stroke="#000000" strokeWidth="4" />
          </svg>
        </div>

        {/* Lane B — automated path */}
        <div style={{ marginTop: 14, opacity: laneB, transform: `translateY(${interpolate(laneB, [0, 1], [50, 0])}px)` }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 34, color: '#FFC94D', letterSpacing: 2 }}>
            VIREMENT AUTOMATIQUE
          </div>
          <svg width="1400" height="90" style={{ marginTop: 8 }}>
            <line x1="0" y1="58" x2="1330" y2="58" stroke="#F5F5F5" strokeWidth="5" opacity="0.85" />
            <rect x="1330" y="24" width="62" height="68" fill="#000000" stroke="#F5F5F5" strokeWidth="4" />
            <circle cx={40 + coinB} cy="58" r="17" fill="#FFC94D" stroke="#000000" strokeWidth="4" />
          </svg>
        </div>

        {/* Payoff beat */}
        <div
          style={{
            marginTop: 34, fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 46,
            color: '#F5F5F5', opacity: payoff, transform: `scale(${interpolate(payoff, [0, 1], [0.86, 1])})`,
            transformOrigin: 'left center',
          }}
        >
          Le comportement suit toujours le chemin de moindre résistance.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)' }}
      />

      {/* Layer 7 — Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
          color: '#9E9E9E', opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S21FrictionFluidite;
```

```tsx
import React from 'react';
SCÈNE 22 | [10:29 - 10:59]
Audio : "Supprime les cartes enregistrées, les applications d'achat et les newsletters promotionnelles."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S22TroisSuppressions',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S22TroisSuppressions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle tilt-pan across the collage board
  const camRot = interpolate(frame, [0, durationInFrames], [1.4, -1.1]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  const head = sp(8);

  const cards = [
    { label: 'CARTES ENREGISTRÉES', sub: '3 clics = achat', delay: 90, kill: 260, rot: -2.5 },
    { label: 'APPLIS D’ACHAT', sub: 'notification = envie', delay: 150, kill: 400, rot: 1.8 },
    { label: 'NEWSLETTERS', sub: 'soldes fictives', delay: 210, kill: 540, rot: -1.4 },
  ];

  const finale = sp(660, { damping: 14 });

  const Icon: React.FC<{ i: number; t: number }> = ({ i, t }) => {
    if (i === 0) {
      return (
        <svg width="120" height="90" viewBox="0 0 120 90">
          <rect x="4" y="8" width="112" height="74" rx="10" fill="#000000" stroke="#F5F5F5" strokeWidth="5" />
          <rect x="4" y="26" width="112" height="16" fill="#F5F5F5" />
          <rect x="18" y="56" width={22 + t * 34} height="10" fill="#FFC94D" />
        </svg>
      );
    }
    if (i === 1) {
      return (
        <svg width="120" height="90" viewBox="0 0 120 90">
          <rect x="30" y="4" width="60" height="82" rx="10" fill="#000000" stroke="#F5F5F5" strokeWidth="5" />
          {Array.from({ length: 6 }).map((_, k) => (
            <rect
              key={k} x={40 + (k % 2) * 22} y={18 + Math.floor(k / 2) * 20} width="14" height="14"
              fill={k === 1 ? '#FF6B6B' : '#F5F5F5'} opacity={0.5 + 0.5 * Math.abs(Math.sin(t * 3 + k))}
            />
          ))}
        </svg>
      );
    }
    return (
      <svg width="120" height="90" viewBox="0 0 120 90">
        <rect x="6" y="14" width="108" height="66" fill="#000000" stroke="#F5F5F5" strokeWidth="5" />
        <path d="M6 14 L60 56 L114 14" stroke="#FFC94D" strokeWidth="6" fill="none" />
        <circle cx="104" cy="22" r={9 + Math.sin(t * 4) * 2} fill="#FF6B6B" stroke="#000000" strokeWidth="3" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(115deg, #3B2E7E 0%, #504099 40%, #6552B8 78%, #4A3A8F 100%)',
        }}
      />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 120, top: -160, width: 520, height: 520,
          border: '4px solid #FFC94D', opacity: 0.07,
          transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.01) * 0.06})`,
        }} />
        <div style={{
          position: 'absolute', right: 80, bottom: -180, width: 640, height: 640, borderRadius: '50%',
          backgroundColor: '#F5F5F5', opacity: 0.045,
          transform: `translateY(${Math.sin(frame * 0.012) * 22}px)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (torn newspaper stripes) */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute', left: 0, top: i * 82 + ((frame * 0.4) % 82),
              width: '100%', height: 3, backgroundColor: '#F5F5F5',
              transform: `rotate(${-0.6 + seededRandom(i) * 1.2}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 17);
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: ((s * 1920) + Math.sin(frame * 0.02 + i) * 30) % 1920,
                top: ((seededRandom(i + 44) * 1080 - frame * 0.5) % 1140 + 1140) % 1140,
                width: 6, height: 6, backgroundColor: '#FFC94D', opacity: 0.25,
                transform: `rotate(45deg) scale(${0.6 + s})`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) rotate(${camRot}deg)`,
          padding: '80px 110px', display: 'flex', flexDirection: 'column',
        }}
      >
        <div style={{ opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-70, 0])}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 108, color: '#F5F5F5', textTransform: 'uppercase' }}>
            SUPPRIME LES RACCOURCIS
          </div>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 32, color: '#000000',
            backgroundColor: '#F5F5F5', alignSelf: 'flex-start', display: 'inline-block',
            padding: '6px 16px', marginTop: 12, transform: 'rotate(-1.6deg)',
          }}>
            L’ENVIRONNEMENT DÉCIDE À TA PLACE
          </div>
        </div>

        <div style={{ display: 'flex', gap: 44, marginTop: 62 }}>
          {cards.map((c, i) => {
            const enter = sp(c.delay);
            const killT = interpolate(frame, [c.kill, c.kill + 18], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const stamp = sp(c.kill + 6, { damping: 9, stiffness: 160 });
            return (
              <div
                key={i}
                style={{
                  position: 'relative', flex: 1, backgroundColor: '#F5F5F5',
                  border: '6px solid #000000', padding: '34px 30px', minHeight: 360,
                  transform: `rotate(${c.rot}deg) translateY(${interpolate(enter, [0, 1], [90, 0])}px) scale(${interpolate(killT, [0, 1], [1, 0.96])})`,
                  opacity: enter * (1 - killT * 0.25),
                  boxShadow: '14px 14px 0px rgba(0,0,0,0.35)',
                }}
              >
                <Icon i={i} t={frame / 30} />
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 46, color: '#000000', marginTop: 22, lineHeight: 1.05 }}>
                  {c.label}
                </div>
                <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#504099', marginTop: 10, fontWeight: 600 }}>
                  {c.sub}
                </div>

                {/* strike-through kill line */}
                <svg width="100%" height="100%" style={{ position: 'absolute', left: 0, top: 0 }}>
                  <line
                    x1="6%" y1="12%" x2={`${6 + 88 * killT}%`} y2={`${12 + 76 * killT}%`}
                    stroke="#FF6B6B" strokeWidth="12" strokeLinecap="round"
                  />
                </svg>

                <div style={{
                  position: 'absolute', bottom: 24, right: 22,
                  fontFamily: 'Anton, sans-serif', fontSize: 38, color: '#FF6B6B',
                  border: '5px solid #FF6B6B', padding: '2px 12px',
                  transform: `rotate(-9deg) scale(${interpolate(stamp, [0, 1], [2.2, 1])})`, opacity: stamp,
                }}>
                  SUPPRIMÉ
                </div>
              </div>
            );
          })}
        </div>

        <div style={{
          marginTop: 46, fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700,
          fontSize: 48, color: '#FFC94D', opacity: finale,
          transform: `translateX(${interpolate(finale, [0, 1], [-90, 0])}px)`,
        }}>
          Moins d’accès, moins d’achats. Sans effort de volonté.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 45%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S22TroisSuppressions;
```

```tsx
import React from 'react';
SCÈNE 23 | [10:59 - 11:29]
Audio : "Programme un virement automatique dès le jour de la paie, avant que ton cerveau voie l'argent."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S23VirementAuto',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S23VirementAuto: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit-like sway
  const camX = Math.sin(frame * 0.008) * 26;
  const camScale = interpolate(frame, [0, 300, durationInFrames], [1.04, 1.0, 1.06]);

  const head = sp(8);
  const cal = sp(70);
  const vault = sp(150);

  // Coin pipeline loop
  const loop = frame % 120;
  const coinT = interpolate(loop, [0, 70], [0, 1], { extrapolateRight: 'clamp' });
  const coinX = interpolate(coinT, [0, 1], [560, 1310]);
  const coinY = 620 - Math.sin(coinT * Math.PI) * 130;
  const drop = interpolate(loop, [70, 78, 92], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const cycles = Math.min(8, Math.floor(Math.max(0, frame - 150) / 120) + 1);
  const total = cycles * 450;

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background:
          'radial-gradient(ellipse at 70% 20%, rgba(255,201,77,0.20) 0%, rgba(80,64,153,0) 55%), linear-gradient(200deg, #6250BE 0%, #504099 48%, #33266A 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '38%', top: '10%', width: 700, height: 700,
          border: '3px dashed #F5F5F5', opacity: 0.07, borderRadius: '50%',
          transform: `rotate(${frame * 0.16}deg)`,
        }} />
        <div style={{
          position: 'absolute', left: -100, bottom: -120, width: 480, height: 480,
          backgroundColor: '#FFC94D', opacity: 0.05,
          transform: `rotate(${18 + frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.014) * 0.07})`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (blueprint grid) */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="grid23" width="70" height="70" patternUnits="userSpaceOnUse">
              <path d="M70 0 L0 0 0 70" fill="none" stroke="#F5F5F5" strokeWidth="1.4" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid23)" transform={`translate(${(frame * 0.3) % 70}, 0)`} />
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const s = seededRandom(i + 61);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 8) * 1080 + frame * (0.4 + s)) % 1160) - 40,
              width: 5 + s * 6, height: 5 + s * 6, borderRadius: '50%',
              border: '2px solid #F5F5F5', opacity: 0.2,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)`, padding: '70px 110px' }}>
        <div style={{ opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-60, 0])}px)` }}>
          <div style={{ fontFamily: 'Clash Display, sans-serif', fontWeight: 700, fontSize: 100, color: '#F5F5F5', lineHeight: 1 }}>
            JOUR DE PAIE = JOUR DE VIREMENT
          </div>
          <div style={{
            display: 'inline-block', marginTop: 14, backgroundColor: '#000000', color: '#FFC94D',
            fontFamily: 'Space Mono, monospace', fontSize: 30, padding: '8px 18px', transform: 'rotate(-1.3deg)',
          }}>
            L’ARGENT PART AVANT D’ÊTRE VU
          </div>
        </div>

        {/* Calendar block */}
        <div style={{
          position: 'absolute', left: 120, top: 330, opacity: cal,
          transform: `translateY(${interpolate(cal, [0, 1], [70, 0])}px) rotate(-1.8deg)`,
          backgroundColor: '#F5F5F5', border: '6px solid #000000', padding: 24, boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
        }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 34, color: '#000000', marginBottom: 12 }}>MOIS TYPE</div>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 46px)', gap: 8 }}>
            {Array.from({ length: 28 }).map((_, i) => {
              const isPay = i === 1;
              const pulse = isPay ? 1 + Math.sin(frame * 0.16) * 0.09 : 1;
              return (
                <div key={i} style={{
                  width: 46, height: 46, backgroundColor: isPay ? '#FFC94D' : '#504099',
                  opacity: isPay ? 1 : 0.35, transform: `scale(${pulse})`,
                  border: isPay ? '4px solid #000000' : 'none',
                }} />
              );
            })}
          </div>
        </div>

        {/* Gear + pipeline + vault */}
        <svg width="1700" height="520" style={{ position: 'absolute', left: 110, top: 560 }}>
          <g transform={`translate(470, 60) rotate(${frame * 1.6})`} opacity={cal}>
            {Array.from({ length: 8 }).map((_, i) => (
              <rect key={i} x="-8" y="-58" width="16" height="20" fill="#F5F5F5" transform={`rotate(${i * 45})`} />
            ))}
            <circle cx="0" cy="0" r="40" fill="none" stroke="#F5F5F5" strokeWidth="8" />
          </g>
          <path d="M450 60 C 700 -60, 1000 -60, 1200 60" stroke="#F5F5F5" strokeWidth="5" fill="none" opacity={0.7 * vault} strokeDasharray="14 12" />
          <circle cx={coinX - 110} cy={coinY - 560} r="20" fill="#FFC94D" stroke="#000000" strokeWidth="5" opacity={vault} />
        </svg>

        {/* Vault */}
        <div style={{
          position: 'absolute', right: 150, top: 470, opacity: vault,
          transform: `translateY(${interpolate(vault, [0, 1], [80, 0])}px) scale(${1 + drop * 0.05})`,
        }}>
          <div style={{ width: 420, height: 300, backgroundColor: '#000000', border: '6px solid #F5F5F5', position: 'relative' }}>
            <div style={{ position: 'absolute', inset: 26, border: '4px solid #FFC94D', opacity: 0.6 }} />
            <div style={{
              position: 'absolute', left: '50%', top: '50%', transform: `translate(-50%,-50%) rotate(${frame * 0.9}deg)`,
              width: 90, height: 90, borderRadius: '50%', border: '10px solid #FFC94D',
            }} />
            <div style={{
              position: 'absolute', bottom: 16, left: 0, width: '100%', textAlign: 'center',
              fontFamily: 'Anton, sans-serif', fontSize: 40, color: '#F5F5F5',
            }}>
              PEA
            </div>
          </div>
          <div style={{
            marginTop: 16, fontFamily: 'Space Mono, monospace', fontSize: 46, color: '#FFC94D', textAlign: 'center',
          }}>
            {total.toLocaleString('fr-FR')} €
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.6) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S23VirementAuto;
```

```tsx
import React from 'react';
SCÈNE 24 | [11:29 - 11:59]
Audio : "Cinquième principe : compte tes possessions, même si cela paraît absurde."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S24CompteTesObjets',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S24CompteTesObjets: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow tilt + push
  const camRot = interpolate(frame, [0, durationInFrames], [-1.6, 1.2]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.09]);

  const head = sp(8);
  const gridIn = sp(90);
  const verdict = sp(680, { damping: 14 });

  const COLS = 24;
  const ROWS = 11;
  const totalCells = COLS * ROWS;
  const filled = Math.floor(interpolate(frame, [110, 620], [0, totalCells], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const counter = Math.floor(interpolate(frame, [110, 620], [0, 1247], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const tick = 1 + Math.sin(frame * 0.6) * 0.012;

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(0deg, #2E2360 0%, #504099 55%, #6A57C6 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: -220, top: 60, width: 820, height: 820, borderRadius: '50%',
          backgroundColor: '#F5F5F5', opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.01) * 0.06})`,
        }} />
        <div style={{
          position: 'absolute', left: 60, bottom: 40, width: 360, height: 360,
          border: '5px solid #FFC94D', opacity: 0.08, transform: `rotate(${frame * 0.1}deg)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (vertical scan lines) */}
      <AbsoluteFill style={{ opacity: 0.09 }}>
        {Array.from({ length: 32 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: 0, left: (i * 62 + (frame * 0.5) % 62), width: 2, height: '100%',
            backgroundColor: '#F5F5F5',
          }} />
        ))}
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 111);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 5) * 1080 - frame * (0.3 + s * 0.6)) % 1160 + 1160) % 1160,
              width: 8, height: 2, backgroundColor: '#FFC94D', opacity: 0.3,
              transform: `rotate(${s * 90}deg)`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)`, padding: '70px 110px' }}>
        <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-between' }}>
          <div style={{ opacity: head, transform: `translateX(${interpolate(head, [0, 1], [-100, 0])}px)` }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 116, color: '#F5F5F5', lineHeight: 0.98 }}>
              COMPTE TES
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 116, color: '#000000', backgroundColor: '#FFC94D',
              display: 'inline-block', padding: '0 18px', transform: 'rotate(-1.5deg)',
            }}>
              POSSESSIONS
            </div>
          </div>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 150, color: '#F5F5F5',
            transform: `scale(${tick})`, opacity: gridIn,
          }}>
            {counter.toLocaleString('fr-FR')}
          </div>
        </div>

        <div style={{
          fontFamily: 'Montserrat, sans-serif', fontSize: 38, fontWeight: 600, color: '#C9B8FF',
          marginTop: 10, opacity: head,
        }}>
          Objets présents dans un logement français moyen — estimation d’ordre de grandeur.
        </div>

        {/* Object grid filling up */}
        <div style={{
          marginTop: 44, display: 'grid', gridTemplateColumns: `repeat(${COLS}, 1fr)`, gap: 12,
          opacity: gridIn, transform: `translateY(${interpolate(gridIn, [0, 1], [60, 0])}px)`,
        }}>
          {Array.from({ length: totalCells }).map((_, i) => {
            const on = i < filled;
            const s = seededRandom(i + 2);
            return (
              <div key={i} style={{
                height: 34,
                backgroundColor: on ? (s > 0.86 ? '#FF6B6B' : s > 0.6 ? '#FFC94D' : '#F5F5F5') : 'rgba(245,245,245,0.10)',
                transform: on ? `scale(${1 - Math.max(0, Math.min(1, (filled - i) / 8)) * 0 + 1 * 0}) rotate(${(s - 0.5) * 8}deg)` : 'none',
                borderRadius: 2,
              }} />
            );
          })}
        </div>

        <div style={{
          marginTop: 40, display: 'flex', gap: 24, alignItems: 'center',
          opacity: verdict, transform: `translateY(${interpolate(verdict, [0, 1], [40, 0])}px)`,
        }}>
          <div style={{ width: 18, height: 78, backgroundColor: '#FFC94D' }} />
          <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 50, color: '#F5F5F5' }}>
            Le prochain achat n’est plus un objet. C’est l’objet numéro 1 248.
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S24CompteTesObjets;
```

```tsx
import React from 'react';
SCÈNE 25 | [11:59 - 12:29]
Audio : "Combien de tee-shirts, de chaussures, de mugs et de câbles dorment vraiment chez toi ?"
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S25InventaireQuatre',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S25InventaireQuatre: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: quadrant sweep
  const camScale = interpolate(frame, [0, 400, durationInFrames], [1.12, 1.02, 1.07]);
  const camX = interpolate(frame, [0, 400, durationInFrames], [40, -20, 20]);

  const head = sp(6);
  const closing = sp(700, { damping: 14 });

  const items = [
    { name: 'TEE-SHIRTS', guess: 20, real: 63, delay: 90 },
    { name: 'CHAUSSURES', guess: 8, real: 24, delay: 160 },
    { name: 'MUGS', guess: 6, real: 19, delay: 230 },
    { name: 'CÂBLES', guess: 5, real: 41, delay: 300 },
  ];

  const CardIcon: React.FC<{ i: number; t: number }> = ({ i, t }) => {
    const wob = Math.sin(t * 2 + i) * 3;
    if (i === 0) {
      return (
        <svg width="110" height="100" viewBox="0 0 110 100" style={{ transform: `rotate(${wob}deg)` }}>
          <path d="M20 18 L40 8 L70 8 L90 18 L100 38 L82 46 L82 92 L28 92 L28 46 L10 38 Z" fill="#504099" stroke="#000000" strokeWidth="5" />
        </svg>
      );
    }
    if (i === 1) {
      return (
        <svg width="110" height="100" viewBox="0 0 110 100" style={{ transform: `rotate(${-wob}deg)` }}>
          <path d="M12 70 L12 34 L40 34 L58 52 L92 62 L100 78 L12 78 Z" fill="#504099" stroke="#000000" strokeWidth="5" />
          <line x1="26" y1="40" x2="26" y2="70" stroke="#000000" strokeWidth="4" />
        </svg>
      );
    }
    if (i === 2) {
      return (
        <svg width="110" height="100" viewBox="0 0 110 100" style={{ transform: `translateY(${wob}px)` }}>
          <rect x="18" y="24" width="56" height="60" rx="6" fill="#504099" stroke="#000000" strokeWidth="5" />
          <path d="M74 38 C 100 38, 100 70, 74 70" fill="none" stroke="#000000" strokeWidth="6" />
        </svg>
      );
    }
    return (
      <svg width="110" height="100" viewBox="0 0 110 100">
        <path d={`M14 84 C 40 ${52 + wob}, 60 ${86 - wob}, 96 40`} fill="none" stroke="#504099" strokeWidth="9" strokeLinecap="round" />
        <rect x="88" y="24" width="18" height="26" rx="4" fill="#000000" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background:
          'radial-gradient(circle at 12% 88%, rgba(255,107,107,0.20) 0%, rgba(80,64,153,0) 55%), linear-gradient(60deg, #453587 0%, #504099 50%, #6857BE 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', top: '50%', width: 900, height: 900, marginLeft: -450, marginTop: -450,
          border: '6px solid #F5F5F5', opacity: 0.05, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.011) * 0.08}) rotate(${frame * 0.07}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 120, top: 80, width: 300, height: 300, backgroundColor: '#FFC94D',
          opacity: 0.05, transform: `rotate(${45 + frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (cross ticks) */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="cross25" width="90" height="90" patternUnits="userSpaceOnUse">
              <path d="M45 38 L45 52 M38 45 L52 45" stroke="#F5F5F5" strokeWidth="2" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#cross25)" transform={`translate(0, ${(frame * 0.25) % 90})`} />
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const s = seededRandom(i + 202);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 33) * 1080 + frame * (0.35 + s * 0.5)) % 1160) - 40,
              width: 10, height: 10, border: '2px solid #FFC94D', opacity: 0.28,
              transform: `rotate(${frame * (0.5 + s)}deg)`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)`, padding: '64px 110px' }}>
        <div style={{ opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-60, 0])}px)` }}>
          <div style={{ fontFamily: 'Clash Display, sans-serif', fontWeight: 700, fontSize: 104, color: '#F5F5F5', lineHeight: 1 }}>
            CE QUE TU CROIS · CE QUE TU AS
          </div>
        </div>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 30, marginTop: 46 }}>
          {items.map((it, i) => {
            const enter = sp(it.delay);
            const guessOp = interpolate(frame, [it.delay + 30, it.delay + 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const realN = Math.floor(interpolate(frame, [it.delay + 90, it.delay + 190], [it.guess, it.real], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
            const alert = interpolate(frame, [it.delay + 185, it.delay + 200, it.delay + 230], [0, 1, 0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{
                backgroundColor: '#F5F5F5', border: '6px solid #000000', padding: '28px 24px',
                transform: `translateY(${interpolate(enter, [0, 1], [110, 0])}px) rotate(${(i % 2 === 0 ? -1 : 1) * 1.6}deg)`,
                opacity: enter, boxShadow: '12px 12px 0 rgba(0,0,0,0.32)',
              }}>
                <CardIcon i={i} t={frame / 30} />
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 40, color: '#000000', marginTop: 16 }}>{it.name}</div>
                <div style={{
                  fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 28, color: '#9E9E9E',
                  marginTop: 12, opacity: guessOp, textDecoration: 'line-through',
                }}>
                  ESTIMATION : {it.guess}
                </div>
                <div style={{
                  fontFamily: 'Space Mono, monospace', fontSize: 78, color: '#000000', marginTop: 4,
                  transform: `scale(${1 + alert * 0.08})`,
                }}>
                  {realN}
                </div>
                <div style={{ height: 12, backgroundColor: '#FF6B6B', width: `${(realN / 70) * 100}%`, marginTop: 6 }} />
              </div>
            );
          })}
        </div>

        <div style={{
          marginTop: 40, fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 48,
          color: '#FFC94D', opacity: closing, transform: `translateX(${interpolate(closing, [0, 1], [-80, 0])}px)`,
        }}>
          On sous-estime presque toujours de moitié. L’absurdité devient visible.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S25InventaireQuatre;
```

```tsx
import React from 'react';
SCÈNE 26 | [12:29 - 12:59]
Audio : "Fixer des plafonds casse la dilatation naturelle du mode de vie."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S26PlafondsFixes',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S26PlafondsFixes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: vertical tilt following the growth
  const camY = interpolate(frame, [0, 500, durationInFrames], [30, -26, 0]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.01]);

  const head = sp(8);
  const ceiling = sp(180, { damping: 10 });

  // Left bar: uncapped lifestyle creep. Right bar: capped.
  const freeH = interpolate(frame, [120, 640], [60, 620], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cappedRaw = interpolate(frame, [120, 640], [60, 620], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cappedH = Math.min(cappedRaw, 360);
  const hitFrame = 120 + ((360 - 60) / (620 - 60)) * (640 - 120);
  const clampHit = interpolate(frame, [hitFrame, hitFrame + 10, hitFrame + 34], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = clampHit * Math.sin(frame * 2.4) * 8;

  const closing = sp(690, { damping: 14 });
  const rules = ['MAX 30 VÊTEMENTS', 'MAX 15 LIVRES', 'RÈGLE 1 POUR 1'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #6E5AD0 0%, #504099 42%, #2C2160 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: -160, top: 200, width: 640, height: 640,
          border: '4px solid #FFC94D', opacity: 0.07, borderRadius: 40,
          transform: `rotate(${12 + frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.013) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', right: -120, bottom: 120, width: 520, height: 520, borderRadius: '50%',
          backgroundColor: '#F5F5F5', opacity: 0.045, transform: `translateY(${Math.sin(frame * 0.01) * 24}px)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (measuring ruler ticks) */}
      <AbsoluteFill style={{ opacity: 0.2 }}>
        {Array.from({ length: 26 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: 70, top: 120 + i * 36,
            width: i % 5 === 0 ? 54 : 26, height: 3, backgroundColor: '#F5F5F5',
            opacity: 0.5,
          }} />
        ))}
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 303);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 12) * 1080 - frame * (0.4 + s * 0.7)) % 1160 + 1160) % 1160,
              width: 4 + s * 6, height: 4 + s * 6, backgroundColor: '#C9B8FF', opacity: 0.3, borderRadius: '50%',
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY + shake}px)`, padding: '64px 120px' }}>
        <div style={{ opacity: head, transform: `translateX(${interpolate(head, [0, 1], [-110, 0])}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 112, color: '#F5F5F5', lineHeight: 1 }}>
            LE MODE DE VIE SE DILATE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 112, color: '#000000', backgroundColor: '#FFC94D',
            display: 'inline-block', padding: '0 18px', marginTop: 8, transform: 'rotate(-1.4deg)',
          }}>
            SAUF SI TU POSES UN PLAFOND
          </div>
        </div>

        {/* Bars comparison */}
        <div style={{ position: 'absolute', left: 220, bottom: 120, display: 'flex', gap: 180, alignItems: 'flex-end' }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{
              width: 230, height: freeH, backgroundColor: '#FF6B6B', border: '6px solid #000000',
              transition: 'none',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 40, color: '#F5F5F5', marginTop: 14 }}>SANS RÈGLE</div>
          </div>

          <div style={{ textAlign: 'center', position: 'relative' }}>
            <div style={{
              width: 230, height: cappedH, backgroundColor: '#FFC94D', border: '6px solid #000000',
              transform: `scaleX(${1 + clampHit * 0.06})`, transformOrigin: 'bottom center',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 40, color: '#F5F5F5', marginTop: 14 }}>AVEC PLAFOND</div>

            {/* Ceiling bar */}
            <div style={{
              position: 'absolute', left: -70, bottom: 360 + 6, width: 380, height: 16,
              backgroundColor: '#F5F5F5', opacity: ceiling,
              transform: `scaleX(${interpolate(ceiling, [0, 1], [0, 1])})`,
            }} />
            <div style={{
              position: 'absolute', left: 320, bottom: 350, fontFamily: 'Space Mono, monospace',
              fontSize: 32, color: '#F5F5F5', opacity: ceiling, whiteSpace: 'nowrap',
            }}>
              PLAFOND FIXÉ
            </div>
          </div>
        </div>

        {/* Rule chips */}
        <div style={{ position: 'absolute', right: 130, top: 400, display: 'flex', flexDirection: 'column', gap: 22 }}>
          {rules.map((r, i) => {
            const en = sp(300 + i * 70);
            return (
              <div key={i} style={{
                backgroundColor: '#F5F5F5', color: '#000000', border: '5px solid #000000',
                fontFamily: 'Anton, sans-serif', fontSize: 42, padding: '10px 24px',
                transform: `rotate(${(i % 2 === 0 ? -2 : 2)}deg) translateX(${interpolate(en, [0, 1], [180, 0])}px)`,
                opacity: en, boxShadow: '10px 10px 0 rgba(0,0,0,0.3)',
              }}>
                {r}
              </div>
            );
          })}
        </div>

        <div style={{
          position: 'absolute', left: 120, bottom: 42,
          fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 46, color: '#C9B8FF',
          opacity: closing,
        }}>
          Une contrainte arbitraire vaut mieux qu’une expansion infinie.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.6) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S26PlafondsFixes;
```

```tsx
import React from 'react';
SCÈNE 27 | [12:59 - 13:29]
Audio : "Sixième principe : loue ou emprunte avant d'acheter, car beaucoup de passions sont éphémères."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S27LouerAvantAcheter',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S27LouerAvantAcheter: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: diagonal drift
  const camX = interpolate(frame, [0, durationInFrames], [-24, 24]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.0]);

  const head = sp(8);
  const curveIn = sp(200);
  const closing = sp(690, { damping: 14 });

  // Passion decay curve
  const DASH = 1400;
  const draw = interpolate(frame, [210, 520], [DASH, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dotT = interpolate(frame, [210, 520], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dotX = 60 + dotT * 1080;
  const dotY = 60 + Math.pow(dotT, 0.55) * 300;

  const gear = [
    { n: 'SAC À DOS', p: '260 €' },
    { n: 'CHAUSSURES', p: '180 €' },
    { n: 'TENTE', p: '240 €' },
    { n: 'RÉCHAUD', p: '120 €' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background:
          'radial-gradient(circle at 85% 15%, rgba(201,184,255,0.30) 0%, rgba(80,64,153,0) 50%), linear-gradient(220deg, #5B49AE 0%, #504099 45%, #362A72 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 200, top: -200, width: 700, height: 700,
          backgroundColor: '#FFC94D', opacity: 0.045, borderRadius: 60,
          transform: `rotate(${frame * 0.07}deg)`,
        }} />
        <div style={{
          position: 'absolute', right: 100, bottom: -240, width: 0, height: 0,
          borderLeft: '280px solid transparent', borderRight: '280px solid transparent',
          borderBottom: '440px solid #F5F5F5', opacity: 0.05,
          transform: `rotate(${180 + Math.sin(frame * 0.01) * 8}deg)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (topographic contour lines) */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="100%" height="100%">
          {Array.from({ length: 9 }).map((_, i) => (
            <path
              key={i}
              d={`M-50 ${180 + i * 100} Q 480 ${100 + i * 100 + Math.sin(frame * 0.02 + i) * 26} 980 ${190 + i * 100} T 2000 ${170 + i * 100}`}
              stroke="#F5F5F5" strokeWidth="2" fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const s = seededRandom(i + 404);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 71) * 1080 + frame * (0.3 + s * 0.6)) % 1160) - 40,
              width: 7, height: 7, backgroundColor: '#F5F5F5', opacity: 0.22,
              transform: `rotate(${45 + frame * s}deg)`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)`, padding: '64px 110px' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 26, opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-60, 0])}px)` }}>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#000000', backgroundColor: '#FFC94D',
            padding: '2px 20px', transform: 'rotate(-2deg)',
          }}>
            LOUER
          </div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 60, color: '#F5F5F5' }}>AVANT</div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#F5F5F5',
            border: '6px solid #F5F5F5', padding: '0 20px', transform: 'rotate(1.6deg)',
            textDecoration: 'line-through', textDecorationColor: '#FF6B6B',
          }}>
            ACHETER
          </div>
        </div>

        {/* Passion decay chart */}
        <div style={{ marginTop: 46, opacity: curveIn, transform: `translateY(${interpolate(curveIn, [0, 1], [60, 0])}px)` }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 36, color: '#C9B8FF', letterSpacing: 2 }}>
            COURBE D’ENTHOUSIASME MOYENNE
          </div>
          <svg width="1200" height="420" style={{ marginTop: 12 }}>
            <line x1="60" y1="380" x2="1160" y2="380" stroke="#F5F5F5" strokeWidth="4" opacity="0.6" />
            <line x1="60" y1="40" x2="60" y2="380" stroke="#F5F5F5" strokeWidth="4" opacity="0.6" />
            <path
              d="M60 60 C 300 120, 520 300, 760 350 S 1080 378, 1140 380"
              stroke="#FF6B6B" strokeWidth="10" fill="none" strokeLinecap="round"
              strokeDasharray={DASH} strokeDashoffset={draw}
            />
            <circle cx={dotX} cy={Math.min(dotY, 380)} r={14 + Math.sin(frame * 0.3) * 2} fill="#FFC94D" stroke="#000000" strokeWidth="5" />
            {['MOIS 1', 'MOIS 3', 'MOIS 6', 'MOIS 12'].map((m, i) => (
              <text key={i} x={80 + i * 280} y="412" fill="#F5F5F5" fontFamily="Space Mono, monospace" fontSize="26" opacity="0.8">
                {m}
              </text>
            ))}
          </svg>
        </div>

        {/* Gear collage receipt */}
        <div style={{ position: 'absolute', right: 120, top: 330, width: 520 }}>
          <div style={{
            backgroundColor: '#F5F5F5', border: '6px solid #000000', padding: '26px 28px',
            transform: `rotate(2deg) translateY(${interpolate(sp(340), [0, 1], [90, 0])}px)`, opacity: sp(340),
            boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
          }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 44, color: '#000000' }}>ACHAT IMPULSIF</div>
            <div style={{ height: 5, backgroundColor: '#000000', margin: '12px 0 18px' }} />
            {gear.map((g, i) => {
              const en = sp(380 + i * 46);
              return (
                <div key={i} style={{
                  display: 'flex', justifyContent: 'space-between',
                  fontFamily: 'Space Mono, monospace', fontSize: 32, color: '#504099', marginBottom: 10,
                  opacity: en, transform: `translateX(${interpolate(en, [0, 1], [40, 0])}px)`,
                }}>
                  <span>{g.n}</span><span>{g.p}</span>
                </div>
              );
            })}
            <div style={{ height: 5, backgroundColor: '#000000', margin: '10px 0 14px' }} />
            <div style={{
              display: 'flex', justifyContent: 'space-between', fontFamily: 'Anton, sans-serif',
              fontSize: 46, color: '#FF6B6B', opacity: sp(580),
            }}>
              <span>TOTAL</span><span>800 €</span>
            </div>
          </div>
        </div>

        <div style={{
          position: 'absolute', left: 110, bottom: 36,
          fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 46, color: '#FFC94D',
          opacity: closing,
        }}>
          Deux locations coûtent moins cher qu’une passion abandonnée.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.57) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S27LouerAvantAcheter;
```

```tsx
import React from 'react';
SCÈNE 28 | [13:29 - 13:59]
Audio : "Teste ta motivation réelle avant d'engager des centaines ou des milliers d'euros."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S28TestMotivation',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S28TestMotivation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit around the gauge
  const camX = Math.sin(frame * 0.009) * 30;
  const camY = Math.cos(frame * 0.011) * 16;
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  const head = sp(8);
  const gauge = sp(120);
  const verdict = sp(620, { damping: 13 });

  // Needle sweeps, tests, then settles low
  const needle = interpolate(
    frame,
    [140, 220, 280, 340, 400, 470, 560, 640],
    [-90, 40, -20, 62, 5, 48, -34, -52],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const jitter = Math.sin(frame * 0.9) * (frame > 640 ? 1.2 : 3.2);

  const steps = [
    { k: '01', t: 'LOUE LE MATÉRIEL', d: 200 },
    { k: '02', t: 'TESTE 2 OU 3 SORTIES', d: 300 },
    { k: '03', t: 'OBSERVE TON ENVIE RÉELLE', d: 400 },
    { k: '04', t: 'ALORS SEULEMENT, DÉCIDE', d: 500 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 60%, rgba(255,201,77,0.18) 0%, rgba(80,64,153,0) 52%), linear-gradient(160deg, #3F3183 0%, #504099 50%, #6A57C6 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 380, top: 120, width: 760, height: 760, borderRadius: '50%',
          border: '5px solid #F5F5F5', opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.012) * 0.07})`,
        }} />
        <div style={{
          position: 'absolute', right: 40, top: 260, width: 420, height: 420,
          backgroundColor: '#000000', opacity: 0.06, transform: `rotate(${frame * 0.08}deg)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (radial rays) */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <svg width="100%" height="100%">
          <g transform={`translate(640, 620) rotate(${frame * 0.25})`}>
            {Array.from({ length: 24 }).map((_, i) => (
              <rect key={i} x="0" y="-2" width="900" height="4" fill="#F5F5F5" transform={`rotate(${i * 15})`} />
            ))}
          </g>
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const s = seededRandom(i + 505);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 90) * 1080 - frame * (0.35 + s * 0.5)) % 1160 + 1160) % 1160,
              width: 6, height: 6, borderRadius: '50%', backgroundColor: '#FFC94D', opacity: 0.25,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX}px, ${camY}px)`, padding: '60px 110px' }}>
        <div style={{ opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-60, 0])}px)` }}>
          <div style={{ fontFamily: 'Clash Display, sans-serif', fontWeight: 700, fontSize: 98, color: '#F5F5F5', lineHeight: 1 }}>
            TESTE TA MOTIVATION,
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif', fontWeight: 700, fontSize: 98, color: '#000000',
            backgroundColor: '#F5F5F5', display: 'inline-block', padding: '0 18px', transform: 'rotate(-1.2deg)',
          }}>
            PAS TON IMAGINATION
          </div>
        </div>

        {/* Gauge */}
        <div style={{
          position: 'absolute', left: 160, top: 380, opacity: gauge,
          transform: `translateY(${interpolate(gauge, [0, 1], [80, 0])}px)`,
        }}>
          <svg width="700" height="420" viewBox="0 0 700 420">
            <path d="M60 360 A 290 290 0 0 1 640 360" fill="none" stroke="#000000" strokeWidth="46" opacity="0.35" />
            <path d="M60 360 A 290 290 0 0 1 260 92" fill="none" stroke="#FF6B6B" strokeWidth="46" />
            <path d="M260 92 A 290 290 0 0 1 440 92" fill="none" stroke="#FFC94D" strokeWidth="46" />
            <path d="M440 92 A 290 290 0 0 1 640 360" fill="none" stroke="#F5F5F5" strokeWidth="46" />
            <g transform={`translate(350, 360) rotate(${needle + jitter})`}>
              <polygon points="-10,0 10,0 0,-250" fill="#F5F5F5" stroke="#000000" strokeWidth="6" />
            </g>
            <circle cx="350" cy="360" r="28" fill="#000000" stroke="#F5F5F5" strokeWidth="7" />
            <text x="70" y="404" fill="#F5F5F5" fontFamily="Space Mono, monospace" fontSize="28">ENVIE PASSAGÈRE</text>
            <text x="470" y="404" fill="#F5F5F5" fontFamily="Space Mono, monospace" fontSize="28">ENVIE RÉELLE</text>
          </svg>
        </div>

        {/* Protocol steps */}
        <div style={{ position: 'absolute', right: 130, top: 340, width: 700 }}>
          {steps.map((s, i) => {
            const en = sp(s.d);
            return (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 22, marginBottom: 22,
                opacity: en, transform: `translateX(${interpolate(en, [0, 1], [140, 0])}px)`,
              }}>
                <div style={{
                  fontFamily: 'Space Mono, monospace', fontSize: 34, color: '#000000',
                  backgroundColor: '#FFC94D', padding: '6px 12px', border: '4px solid #000000',
                }}>
                  {s.k}
                </div>
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 46, color: '#F5F5F5' }}>{s.t}</div>
              </div>
            );
          })}
        </div>

        {/* Price gate */}
        <div style={{
          position: 'absolute', right: 150, bottom: 90, opacity: verdict,
          transform: `rotate(-3deg) scale(${interpolate(verdict, [0, 1], [1.5, 1])})`,
          backgroundColor: '#F5F5F5', border: '7px solid #000000', padding: '14px 30px',
          fontFamily: 'Anton, sans-serif', fontSize: 62, color: '#000000',
          boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
        }}>
          2 500 € EN ATTENTE
        </div>

        <div style={{
          position: 'absolute', left: 160, bottom: 46,
          fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 44, color: '#C9B8FF',
          opacity: verdict, maxWidth: 900,
        }}>
          Louer, c’est acheter du temps pour dire non.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S28TestMotivation;
```

```tsx
import React from 'react';
SCÈNE 29 | [13:59 - 14:29]
Audio : "Ces principes sont un cadre de réflexion, pas une prescription personnalisée."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S29CadreDeReflexion',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S29CadreDeReflexion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: quiet, respectful push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06]);
  const camY = Math.sin(frame * 0.006) * 10;

  const head = sp(10);
  const stamp = sp(430, { damping: 9, stiffness: 150 });
  const closing = sp(640, { damping: 14 });

  // Frame drawing (4 sides)
  const side = (d: number) => interpolate(frame, [d, d + 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const s1 = side(120), s2 = side(155), s3 = side(190), s4 = side(225);

  const chips = [
    'TON ÂGE', 'TES OBJECTIFS', 'TON PROFIL DE RISQUE', 'TA SITUATION FISCALE',
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #2F2468 0%, #504099 55%, #4A3A93 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '10%', top: '18%', width: 560, height: 560,
          border: '4px solid #F5F5F5', opacity: 0.05,
          transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.009) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', right: '8%', bottom: '10%', width: 460, height: 460, borderRadius: '50%',
          backgroundColor: '#FFC94D', opacity: 0.05,
          transform: `translateY(${Math.sin(frame * 0.013) * 20}px)`,
        }} />
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (paper fibre hatching) */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="hatch29" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(${20 + Math.sin(frame * 0.008) * 5})`}>
              <line x1="0" y1="0" x2="0" y2="40" stroke="#F5F5F5" strokeWidth="2" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#hatch29)" />
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const s = seededRandom(i + 606);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 22) * 1080 + frame * (0.25 + s * 0.4)) % 1160) - 40,
              width: 5, height: 5, backgroundColor: '#F5F5F5', opacity: 0.25, borderRadius: '50%',
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{
        transform: `scale(${camScale}) translateY(${camY}px)`,
        display: 'flex', alignItems: 'center', justifyContent: 'center',
      }}>
        {/* Drawing frame */}
        <div style={{ position: 'relative', width: 1360, height: 700 }}>
          <div style={{ position: 'absolute', left: 0, top: 0, height: 10, width: `${s1 * 100}%`, backgroundColor: '#FFC94D' }} />
          <div style={{ position: 'absolute', right: 0, top: 0, width: 10, height: `${s2 * 100}%`, backgroundColor: '#FFC94D' }} />
          <div style={{ position: 'absolute', right: 0, bottom: 0, height: 10, width: `${s3 * 100}%`, backgroundColor: '#FFC94D' }} />
          <div style={{ position: 'absolute', left: 0, bottom: 0, width: 10, height: `${s4 * 100}%`, backgroundColor: '#FFC94D' }} />

          <div style={{
            position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center', padding: '0 90px', textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Space Mono, monospace', fontSize: 32, color: '#C9B8FF', letterSpacing: 6,
              opacity: head, transform: `translateY(${interpolate(head, [0, 1], [-40, 0])}px)`,
            }}>
              AVERTISSEMENT UTILE
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 128, color: '#F5F5F5', lineHeight: 0.98, marginTop: 16,
              opacity: sp(60), transform: `scale(${interpolate(sp(60), [0, 1], [0.88, 1])})`,
            }}>
              UN CADRE DE RÉFLEXION
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 84, color: '#000000', backgroundColor: '#F5F5F5',
              padding: '4px 22px', marginTop: 18, transform: 'rotate(-1.1deg)',
              opacity: sp(280),
            }}>
              PAS UNE PRESCRIPTION
            </div>

            <div style={{ display: 'flex', gap: 18, marginTop: 40, flexWrap: 'wrap', justifyContent: 'center' }}>
              {chips.map((c, i) => {
                const en = sp(330 + i * 45);
                return (
                  <div key={i} style={{
                    fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 30, color: '#F5F5F5',
                    border: '3px solid #F5F5F5', padding: '8px 18px', opacity: en,
                    transform: `translateY(${interpolate(en, [0, 1], [30, 0])}px)`,
                  }}>
                    {c}
                  </div>
                );
              })}
            </div>
          </div>

          {/* Collage stamp */}
          <div style={{
            position: 'absolute', right: -40, bottom: -50,
            fontFamily: 'Anton, sans-serif', fontSize: 40, color: '#FF6B6B',
            border: '6px solid #FF6B6B', padding: '8px 20px', backgroundColor: 'rgba(80,64,153,0.75)',
            transform: `rotate(-8deg) scale(${interpolate(stamp, [0, 1], [2.4, 1])})`, opacity: stamp,
          }}>
            CHAQUE SITUATION EST UNIQUE
          </div>
        </div>

        <div style={{
          position: 'absolute', bottom: 60, left: 0, width: '100%', textAlign: 'center',
          fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 42, color: '#FFC94D',
          opacity: closing,
        }}>
          Un professionnel agréé affine ce qui te concerne vraiment.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.6) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S29CadreDeReflexion;
```

```tsx
import React from 'react';
SCÈNE 30 | [14:29 - 14:59]
Audio : "Septième principe : chaque objet majeur traîne derrière lui un cortège de dépenses satellites."
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S30DepensesSatellites',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S30DepensesSatellites: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit pull-back
  const camScale = interpolate(frame, [0, 420, durationInFrames], [1.22, 1.0, 1.05]);
  const camX = Math.sin(frame * 0.007) * 22;

  const head = sp(8);
  const core = sp(90, { damping: 10 });
  const closing = sp(700, { damping: 14 });

  const satellites = [
    { label: 'ASSURANCE', cost: '1 100 €', delay: 190 },
    { label: 'ENTRETIEN', cost: '900 €', delay: 250 },
    { label: 'CARBURANT', cost: '1 600 €', delay: 310 },
    { label: 'STATIONNEMENT', cost: '840 €', delay: 370 },
    { label: 'ACCESSOIRES', cost: '460 €', delay: 430 },
  ];

  const orbit = frame * 0.28;
  const totalStart = 5000;
  const total = Math.floor(interpolate(frame, [190, 620], [totalStart, 9900], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const pulse = 1 + Math.sin(frame * 0.14) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Layer 1 — Dynamic gradient background */}
      <AbsoluteFill style={{
        background:
          'radial-gradient(circle at 50% 52%, rgba(201,184,255,0.32) 0%, rgba(80,64,153,0) 48%), linear-gradient(200deg, #5D4BB4 0%, #504099 46%, #2B2160 100%)',
      }} />

      {/* Layer 2 — Animated transparent shape layers */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div key={i} style={{
            position: 'absolute', left: '50%', top: '54%',
            width: 620 + i * 260, height: 620 + i * 260, marginLeft: -(310 + i * 130), marginTop: -(310 + i * 130),
            border: '3px solid #F5F5F5', borderRadius: '50%', opacity: 0.07 - i * 0.015,
            transform: `rotate(${frame * (0.06 + i * 0.03)}deg) scale(${1 + Math.sin(frame * 0.01 + i) * 0.03})`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Layer 3 — Motion texture (concentric tick marks) */}
      <AbsoluteFill style={{ opacity: 0.14 }}>
        <svg width="100%" height="100%">
          <g transform={`translate(960, 583) rotate(${-frame * 0.18})`}>
            {Array.from({ length: 60 }).map((_, i) => (
              <rect key={i} x="430" y="-1.5" width={i % 5 === 0 ? 26 : 12} height="3" fill="#F5F5F5" transform={`rotate(${i * 6})`} />
            ))}
          </g>
        </svg>
      </AbsoluteFill>

      {/* Layer 4 — Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const s = seededRandom(i + 707);
          return (
            <div key={i} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 55) * 1080 - frame * (0.3 + s * 0.6)) % 1160 + 1160) % 1160,
              width: 4 + s * 5, height: 4 + s * 5, backgroundColor: i % 4 === 0 ? '#FF6B6B' : '#FFC94D',
              opacity: 0.28, borderRadius: '50%',
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Layer 5 — Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>
        <div style={{
          position: 'absolute', left: 90, top: 60, opacity: head,
          transform: `translateY(${interpolate(head, [0, 1], [-60, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 30, color: '#000000', backgroundColor: '#FFC94D',
            display: 'inline-block', padding: '6px 16px', transform: 'rotate(-2deg)',
          }}>
            PRINCIPE 07 / EFFET MULTIPLICATEUR
          </div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: '#F5F5F5', lineHeight: 1, marginTop: 14 }}>
            UN OBJET N’ARRIVE
          </div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: '#FFC94D', lineHeight: 1 }}>
            JAMAIS SEUL
          </div>
        </div>

        {/* Core object */}
        <div style={{
          position: 'absolute', left: '50%', top: '54%', marginLeft: -160, marginTop: -160,
          width: 320, height: 320, borderRadius: '50%', backgroundColor: '#F5F5F5',
          border: '8px solid #000000', display: 'flex', flexDirection: 'column',
          alignItems: 'center', justifyContent: 'center',
          transform: `scale(${interpolate(core, [0, 1], [0.2, 1]) * pulse})`, opacity: core,
          boxShadow: '0 0 0 18px rgba(255,201,77,0.18)',
        }}>
          <svg width="150" height="80" viewBox="0 0 150 80">
            <path d="M14 56 L26 30 L110 30 L128 56 Z" fill="#504099" stroke="#000000" strokeWidth="5" />
            <circle cx="42" cy="60" r="12" fill="#000000" />
            <circle cx="106" cy="60" r="12" fill="#000000" />
          </svg>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 44, color: '#000000', marginTop: 6 }}>ACHAT</div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 34, color: '#504099' }}>5 000 €</div>
        </div>

        {/* Satellites */}
        {satellites.map((s, i) => {
          const en = sp(s.delay);
          const angle = ((i / satellites.length) * 360 + orbit) * (Math.PI / 180);
          const rx = 520 * en;
          const ry = 320 * en;
          const x = 960 + Math.cos(angle) * rx;
          const y = 583 + Math.sin(angle) * ry;
          return (
            <div key={i} style={{
              position: 'absolute', left: x, top: y, transform: 'translate(-50%, -50%)',
              opacity: en,
            }}>
              <div style={{
                backgroundColor: '#000000', border: '5px solid #F5F5F5', padding: '12px 20px',
                transform: `rotate(${(i % 2 === 0 ? -2.4 : 2.4)}deg) scale(${interpolate(en, [0, 1], [0.4, 1])})`,
                textAlign: 'center', minWidth: 240,
              }}>
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 36, color: '#F5F5F5' }}>{s.label}</div>
                <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 32, color: '#FFC94D' }}>{s.cost}</div>
              </div>
            </div>
          );
        })}

        {/* Total counter */}
        <div style={{
          position: 'absolute', right: 100, bottom: 150, textAlign: 'right',
          opacity: sp(560), transform: `translateY(${interpolate(sp(560), [0, 1], [50, 0])}px)`,
        }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontWeight: 700, fontSize: 34, color: '#C9B8FF', letterSpacing: 3 }}>
            COÛT RÉEL DE L’ÉCOSYSTÈME
          </div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 118, color: '#FF6B6B', lineHeight: 1 }}>
            {total.toLocaleString('fr-FR')} €
          </div>
        </div>

        <div style={{
          position: 'absolute', left: 90, bottom: 46, maxWidth: 1000,
          fontFamily: 'Bricolage Grotesque, sans-serif', fontWeight: 700, fontSize: 46, color: '#F5F5F5',
          opacity: closing,
        }}>
          Le prix affiché n’est que la surface visible de l’iceberg.
        </div>
      </AbsoluteFill>

      {/* Layer 6 — Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 38%, rgba(0,0,0,0.62) 100%)' }} />

      {/* Layer 7 — Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E',
        opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default S30DepensesSatellites;
```

