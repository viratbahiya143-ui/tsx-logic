```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'RupeeFallReality',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const RupeeFallReality: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Counter animation 0 → 7.2%
  const counterProg = spring({ frame: Math.max(0, frame - 50), fps, config: { damping: 18, mass: 0.8, stiffness: 80 } });
  const counterVal = (counterProg * 7.2).toFixed(1);

  // Falling line graph progress
  const lineProg = interpolate(frame, [60, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Headline reveal
  const headOp = sp(15);
  const subOp = sp(35);
  const counterOp = sp(50);
  const graphOp = sp(60);

  // Glitch shake on impact
  const shake = frame > 50 && frame < 65 ? Math.sin(frame * 3) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(255,193,7,0.12) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(255,0,0,0.08) 0%, transparent 55%)',
        }}
      />

      {/* Shape layers — drifting geometry */}
      <div
        style={{
          position: 'absolute',
          top: '-10%',
          left: '-5%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '2px solid #1C1C1C',
          opacity: 0.05,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-15%',
          right: '-8%',
          width: 700,
          height: 700,
          background: '#1C1C1C',
          opacity: 0.04,
          transform: `rotate(${-frame * 0.15}deg)`,
          clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '40%',
          left: '70%',
          width: 250,
          height: 250,
          background: '#FFC107',
          opacity: 0.06,
          transform: `translateY(${Math.sin(frame * 0.03) * 30}px) rotate(${frame * 0.3}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px + drift,
              top: py,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#1C1C1C',
              opacity: 0.25,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)` }}>
        {/* Top eyebrow tag */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 120,
            opacity: headOp,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div style={{ width: 12, height: 12, background: '#ff0000', borderRadius: '50%', boxShadow: '0 0 20px #ff0000' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 700,
              color: '#1C1C1C',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            LIVE DATA · MAY 2026
          </div>
        </div>

        {/* Main headline */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: 120,
            opacity: headOp,
            transform: `translateY(${(1 - headOp) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 102,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.05,
              letterSpacing: -1,
            }}
          >
            रुपया गिर रहा है
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 102,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1.05,
              letterSpacing: -1,
              marginTop: -10,
            }}
          >
            हर हफ्ते
          </div>
        </div>

        {/* Big counter card */}
        <div
          style={{
            position: 'absolute',
            top: 440,
            left: 120,
            width: 720,
            background: '#1C1C1C',
            padding: '50px 60px',
            opacity: counterOp,
            transform: `translateY(${(1 - counterOp) * 60}px)`,
            border: '4px solid #1C1C1C',
            boxShadow: '12px 12px 0px #FFC107',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 600,
              color: '#9E9E9E',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 20,
            }}
          >
            YTD गिरावट
          </div>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 10 }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 220,
                fontWeight: 900,
                color: '#ff0000',
                lineHeight: 0.9,
                letterSpacing: -4,
              }}
            >
              −{counterVal}
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 110,
                fontWeight: 700,
                color: '#DFF1F1',
                lineHeight: 1,
              }}
            >
              %
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 500,
              color: '#DFF1F1',
              marginTop: 20,
            }}
          >
            एशिया की सबसे कमजोर करेंसी
          </div>
        </div>

        {/* Right side — falling line graph */}
        <div
          style={{
            position: 'absolute',
            top: 440,
            right: 120,
            width: 720,
            height: 480,
            background: '#DFF1F1',
            border: '4px solid #1C1C1C',
            padding: 40,
            opacity: graphOp,
            transform: `translateY(${(1 - graphOp) * 60}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#1C1C1C',
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginBottom: 10,
            }}
          >
            INR / USD · 12 MONTHS
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 700,
              color: '#1C1C1C',
            }}
          >
            83.50 → 96.72
          </div>

          <svg width="100%" height="320" viewBox="0 0 640 320" style={{ marginTop: 20 }}>
            {/* horizontal guides */}
            {[0, 1, 2, 3, 4].map((i) => (
              <line
                key={i}
                x1="0"
                y1={i * 70 + 20}
                x2="640"
                y2={i * 70 + 20}
                stroke="#1C1C1C"
                strokeWidth="1"
                opacity="0.15"
                strokeDasharray="4 6"
              />
            ))}
            {/* Falling line path */}
            <path
              d="M 0 60 L 80 70 L 160 90 L 240 110 L 320 140 L 400 175 L 480 215 L 560 255 L 640 290"
              fill="none"
              stroke="#ff0000"
              strokeWidth="6"
              strokeLinecap="round"
              strokeDasharray="1200"
              strokeDashoffset={1200 * (1 - lineProg)}
            />
            {/* End dot */}
            {lineProg > 0.95 && (
              <>
                <circle cx="640" cy="290" r="14" fill="#ff0000" />
                <circle cx="640" cy="290" r={20 + Math.sin(frame * 0.2) * 6} fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
              </>
            )}
            {/* Down arrow icon */}
            <g transform={`translate(580, 30) scale(${lineProg})`}>
              <path d="M 0 0 L 30 0 L 15 25 Z" fill="#ff0000" />
            </g>
          </svg>

          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 26,
              fontWeight: 600,
              color: '#1C1C1C',
              marginTop: 10,
              opacity: 0.7,
            }}
          >
            हर महीना नया low
          </div>
        </div>

        {/* Bottom subhead */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 120,
            opacity: subOp,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          <div style={{ width: 60, height: 4, background: '#1C1C1C' }} />
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 42,
              fontWeight: 600,
              color: '#1C1C1C',
              letterSpacing: 1,
            }}
          >
            ये कोई फिल्म की स्क्रिप्ट नहीं है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default RupeeFallReality;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'AsiaWorstCurrency',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const AsiaWorstCurrency: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Currency bar chart data
  const currencies = [
    { name: 'INR', value: -7.2, color: '#ff0000', flag: '🇮🇳' },
    { name: 'IDR', value: -3.1, color: '#1C1C1C' },
    { name: 'THB', value: -2.4, color: '#1C1C1C' },
    { name: 'PHP', value: -1.8, color: '#1C1C1C' },
    { name: 'MYR', value: -1.2, color: '#1C1C1C' },
    { name: 'CNY', value: -0.6, color: '#1C1C1C' },
  ];

  const titleOp = sp(10);
  const chartOp = sp(40);

  // Pan from left to right
  const panX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(255,193,7,0.08) 0%, transparent 40%, rgba(255,0,0,0.06) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '-10%',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: '#1C1C1C',
          opacity: 0.04,
          transform: `translateX(${Math.sin(frame * 0.02) * 30}px)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          right: '-5%',
          width: 400,
          height: 400,
          border: '3px solid #FFC107',
          opacity: 0.15,
          transform: `rotate(${frame * 0.3}deg)`,
        }}
      />

      {/* Dot grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dots" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 100) * 1920;
        const py = seededRandom(i + 200) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + Math.sin(frame * 0.02 + i) * 25,
              width: 3,
              height: 3,
              background: '#1C1C1C',
              opacity: 0.2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${panX}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 120,
            opacity: titleOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div
            style={{
              padding: '10px 24px',
              background: '#ff0000',
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#DFF1F1',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            ASIA RANKING
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 600,
              color: '#1C1C1C',
              letterSpacing: 3,
              textTransform: 'uppercase',
              opacity: 0.6,
            }}
          >
            YTD PERFORMANCE · 2026
          </div>
        </div>

        {/* Main headline */}
        <div
          style={{
            position: 'absolute',
            top: 140,
            left: 120,
            opacity: titleOp,
            transform: `translateY(${(1 - titleOp) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            एशिया की <span style={{ color: '#ff0000' }}>सबसे बुरी</span>
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            परफॉर्मिंग करेंसी
          </div>
        </div>

        {/* Bar chart */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 120,
            right: 120,
            opacity: chartOp,
            transform: `translateY(${(1 - chartOp) * 40}px)`,
          }}
        >
          {currencies.map((c, i) => {
            const barDelay = 50 + i * 8;
            const barProg = sp(barDelay, { damping: 14, stiffness: 100 });
            const maxWidth = 1400;
            const widthVal = (Math.abs(c.value) / 7.2) * maxWidth * barProg;
            const isWorst = c.name === 'INR';

            return (
              <div
                key={c.name}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  marginBottom: 18,
                  height: 58,
                }}
              >
                <div
                  style={{
                    width: 110,
                    fontFamily: 'JetBrains Mono',
                    fontSize: isWorst ? 42 : 34,
                    fontWeight: 800,
                    color: isWorst ? '#ff0000' : '#1C1C1C',
                  }}
                >
                  {c.name}
                </div>
                <div style={{ position: 'relative', flex: 1, height: 50, display: 'flex', alignItems: 'center' }}>
                  <div
                    style={{
                      height: isWorst ? 50 : 36,
                      width: widthVal,
                      background: c.color,
                      boxShadow: isWorst ? '6px 6px 0px #1C1C1C' : 'none',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'flex-end',
                      paddingRight: 20,
                    }}
                  >
                    {barProg > 0.6 && (
                      <span
                        style={{
                          fontFamily: 'JetBrains Mono',
                          fontSize: isWorst ? 32 : 24,
                          fontWeight: 800,
                          color: '#DFF1F1',
                        }}
                      >
                        {c.value}%
                      </span>
                    )}
                  </div>
                  {isWorst && barProg > 0.9 && (
                    <div
                      style={{
                        position: 'absolute',
                        left: widthVal + 20,
                        top: -8,
                        padding: '8px 20px',
                        background: '#FFC107',
                        fontFamily: 'Noto Sans Devanagari',
                        fontSize: 28,
                        fontWeight: 700,
                        color: '#1C1C1C',
                        transform: `scale(${1 + Math.sin(frame * 0.15) * 0.04})`,
                      }}
                    >
                      ← WORST
                    </div>
                  )}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom reason */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 120,
            opacity: sp(110),
            background: '#1C1C1C',
            padding: '18px 36px',
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 800,
              color: '#FFC107',
            }}
          >
            REASON →
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              fontWeight: 600,
              color: '#DFF1F1',
            }}
          >
            इंडिया नेट एनर्जी इम्पोर्टर है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default AsiaWorstCurrency;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'OilTradeDeficit',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OilTradeDeficit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Oil drop rising counter
  const oilProg = spring({ frame: Math.max(0, frame - 40), fps, config: { damping: 20, stiffness: 80 } });
  const oilPrice = (60 + oilProg * 32).toFixed(0);

  const deficitProg = spring({ frame: Math.max(0, frame - 80), fps, config: { damping: 20, stiffness: 70 } });
  const deficitVal = (deficitProg * 38.4).toFixed(1);

  // Tilt camera
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  const titleOp = sp(10);
  const leftCardOp = sp(40);
  const arrowOp = sp(70);
  const rightCardOp = sp(80);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(255,193,7,0.10) 0%, transparent 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '10%',
          left: '40%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          background: '#FFC107',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '5%',
          left: '5%',
          width: 300,
          height: 300,
          background: '#1C1C1C',
          opacity: 0.05,
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Line texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diagLines" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diagLines)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 300) * 1920;
        const py = (seededRandom(i + 400) * 1080 + frame * 0.5) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 5,
              height: 5,
              background: '#FFC107',
              opacity: 0.3,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 120,
            opacity: titleOp,
            transform: `translateY(${(1 - titleOp) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            CAUSE & EFFECT — CHAIN REACTION
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 96,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -1,
            }}
          >
            तेल चढ़ा → घाटा बढ़ा
          </div>
        </div>

        {/* Left card — Oil Price */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 120,
            width: 680,
            height: 480,
            background: '#1C1C1C',
            border: '4px solid #1C1C1C',
            padding: 50,
            opacity: leftCardOp,
            transform: `translateY(${(1 - leftCardOp) * 50}px)`,
            boxShadow: '10px 10px 0px #FFC107',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 700,
              color: '#FFC107',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            BRENT CRUDE · USD/BARREL
          </div>

          {/* Oil drop icon */}
          <svg width="120" height="140" viewBox="0 0 100 120" style={{ marginBottom: 10 }}>
            <path
              d="M 50 5 Q 20 50 20 80 Q 20 110 50 110 Q 80 110 80 80 Q 80 50 50 5 Z"
              fill="#FFC107"
              stroke="#FFC107"
              strokeWidth="2"
            />
            <ellipse cx="38" cy="70" rx="8" ry="14" fill="#DFF1F1" opacity="0.4" />
          </svg>

          <div style={{ display: 'flex', alignItems: 'baseline', gap: 12 }}>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                fontWeight: 700,
                color: '#9E9E9E',
              }}
            >
              $
            </span>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 180,
                fontWeight: 900,
                color: '#DFF1F1',
                lineHeight: 0.9,
              }}
            >
              {oilPrice}
            </span>
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 600,
              color: '#FFC107',
              marginTop: 14,
            }}
          >
            ↑ surging — middle east tension
          </div>
        </div>

        {/* Center arrow */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            left: 820,
            opacity: arrowOp,
            transform: `scale(${arrowOp}) translateX(${Math.sin(frame * 0.15) * 8}px)`,
          }}
        >
          <svg width="160" height="80" viewBox="0 0 160 80">
            <line x1="0" y1="40" x2="120" y2="40" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
            <path d="M 110 15 L 155 40 L 110 65 Z" fill="#ff0000" />
          </svg>
        </div>

        {/* Right card — Trade Deficit */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 120,
            width: 680,
            height: 480,
            background: '#DFF1F1',
            border: '4px solid #1C1C1C',
            padding: 50,
            opacity: rightCardOp,
            transform: `translateY(${(1 - rightCardOp) * 50}px)`,
            boxShadow: '10px 10px 0px #ff0000',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            TRADE DEFICIT · USD BN
          </div>

          {/* Mini ascending bars */}
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 10, height: 110, marginBottom: 10 }}>
            {[30, 45, 60, 78, 95].map((h, i) => {
              const barProg = sp(85 + i * 6);
              return (
                <div
                  key={i}
                  style={{
                    width: 40,
                    height: h * barProg,
                    background: i === 4 ? '#ff0000' : '#1C1C1C',
                  }}
                />
              );
            })}
          </div>

          <div style={{ display: 'flex', alignItems: 'baseline', gap: 12 }}>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 60,
                fontWeight: 700,
                color: '#9E9E9E',
              }}
            >
              $
            </span>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 180,
                fontWeight: 900,
                color: '#1C1C1C',
                lineHeight: 0.9,
              }}
            >
              {deficitVal}
            </span>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 70,
                fontWeight: 700,
                color: '#1C1C1C',
              }}
            >
              BN
            </span>
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 600,
              color: '#ff0000',
              marginTop: 14,
            }}
          >
            ↑ लगातार बढ़ रहा है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default OilTradeDeficit;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'FIIOutflow',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const FIIOutflow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter
  const counterProg = spring({ frame: Math.max(0, frame - 60), fps, config: { damping: 20, stiffness: 70 } });
  const counterVal = (counterProg * 23).toFixed(1);

  // Zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0]);

  // Money particles flowing right
  const flowFrame = frame;

  const titleOp = sp(10);
  const indiaBoxOp = sp(35);
  const usaBoxOp = sp(50);
  const counterOp = sp(60);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(255,0,0,0.08) 0%, transparent 50%, rgba(255,193,7,0.10) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '15%',
          right: '10%',
          width: 450,
          height: 450,
          borderRadius: '50%',
          border: '3px solid #1C1C1C',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          left: '20%',
          width: 600,
          height: 600,
          background: '#FFC107',
          opacity: 0.04,
          clipPath: 'polygon(50% 0%, 100% 38%, 82% 100%, 18% 100%, 0% 38%)',
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />

      {/* Crosshatch texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="crosshatch" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="60" y2="60" stroke="#1C1C1C" strokeWidth="1" />
            <line x1="60" y1="0" x2="0" y2="60" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#crosshatch)" />
      </svg>

      {/* Flowing money particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 500;
        const startY = 400 + seededRandom(seed) * 300;
        const speed = 3 + seededRandom(seed + 1) * 3;
        const startX = -50 + ((flowFrame * speed + seededRandom(seed + 2) * 1920) % 2200);
        const sz = 12 + seededRandom(seed + 3) * 18;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: startX,
              top: startY + Math.sin(flowFrame * 0.05 + i) * 15,
              width: sz,
              height: sz * 0.6,
              background: '#1C1C1C',
              opacity: 0.15,
              borderRadius: 2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top tag */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 120,
            opacity: titleOp,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div style={{ padding: '8px 20px', background: '#ff0000' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 24,
                fontWeight: 800,
                color: '#DFF1F1',
                letterSpacing: 4,
                textTransform: 'uppercase',
              }}
            >
              CAPITAL FLIGHT
            </div>
          </div>
        </div>

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 145,
            left: 120,
            opacity: titleOp,
            transform: `translateY(${(1 - titleOp) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 96,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -1,
            }}
          >
            FII का पैसा निकल रहा है
          </div>
        </div>

        {/* India box (left) */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 120,
            width: 480,
            height: 480,
            background: '#DFF1F1',
            border: '5px solid #1C1C1C',
            padding: 40,
            opacity: indiaBoxOp,
            transform: `translateX(${(1 - indiaBoxOp) * -40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 800,
              color: '#9E9E9E',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 30,
            }}
          >
            FROM
          </div>
          {/* India flag-inspired bars */}
          <div style={{ width: 200, marginBottom: 30 }}>
            <div style={{ height: 24, background: '#FFC107' }} />
            <div style={{ height: 24, background: '#DFF1F1', borderTop: '2px solid #1C1C1C', borderBottom: '2px solid #1C1C1C', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              <div style={{ width: 16, height: 16, borderRadius: '50%', border: '2px solid #1C1C1C' }} />
            </div>
            <div style={{ height: 24, background: '#1C1C1C' }} />
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 84,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
            }}
          >
            INDIAN
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 84,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
            }}
          >
            STOCKS
          </div>
        </div>

        {/* USA box (right) */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            right: 120,
            width: 480,
            height: 480,
            background: '#1C1C1C',
            border: '5px solid #1C1C1C',
            padding: 40,
            opacity: usaBoxOp,
            transform: `translateX(${(1 - usaBoxOp) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 800,
              color: '#FFC107',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 30,
            }}
          >
            TO
          </div>
          {/* Dollar block */}
          <div style={{ width: 180, height: 180, background: '#FFC107', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: 24 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 140, fontWeight: 900, color: '#1C1C1C', lineHeight: 1 }}>$</span>
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 64,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1.0,
            }}
          >
            DOLLAR
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 64,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 1.0,
            }}
          >
            ASSETS
          </div>
        </div>

        {/* Center mega counter */}
        <div
          style={{
            position: 'absolute',
            top: 540,
            left: '50%',
            transform: `translateX(-50%) scale(${counterOp})`,
            opacity: counterOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            OUTFLOW · YTD
          </div>
          <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: 6 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 70, fontWeight: 900, color: '#ff0000' }}>$</span>
            <span
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 200,
                fontWeight: 900,
                color: '#ff0000',
                lineHeight: 0.9,
                letterSpacing: -3,
                textShadow: '6px 6px 0px #1C1C1C',
              }}
            >
              {counterVal}
            </span>
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 50,
              fontWeight: 800,
              color: '#1C1C1C',
              marginTop: -10,
            }}
          >
            BILLION
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 22,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 3,
              marginTop: 4,
              textTransform: 'uppercase',
            }}
          >
            ALL-TIME RECORD
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default FIIOutflow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CapitalFleesCycle',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CapitalFleesCycle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Orbit camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  const titleOp = sp(10);
  const step1 = sp(40);
  const step2 = sp(75);
  const step3 = sp(110);
  const step4 = sp(145);

  // Cycle ring rotation
  const ringRot = frame * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(255,0,0,0.06) 0%, transparent 70%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 1200,
          height: 1200,
          borderRadius: '50%',
          border: '2px dashed #1C1C1C',
          opacity: 0.08,
          transform: `translate(-50%, -50%) rotate(${ringRot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '1px solid #1C1C1C',
          opacity: 0.06,
          transform: `translate(-50%, -50%) rotate(${-ringRot * 0.6}deg)`,
        }}
      />

      {/* Hexagon texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="hex" width="80" height="70" patternUnits="userSpaceOnUse">
            <polygon points="40,5 75,25 75,55 40,75 5,55 5,25" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 600) * 1920;
        const py = seededRandom(i + 700) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px + Math.cos(frame * 0.02 + i) * 20,
              top: py + Math.sin(frame * 0.02 + i) * 20,
              width: 4,
              height: 4,
              background: '#1C1C1C',
              opacity: 0.2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 120,
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 24,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 10,
            }}
          >
            THE VICIOUS CYCLE
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 88,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -1,
            }}
          >
            कैसे चलता है यह चक्र?
          </div>
        </div>

        {/* 4-step cycle, arranged in diamond */}
        {/* Step 1 — Top */}
        <CycleNode
          x={960}
          y={310}
          number="1"
          title="अनसर्टेंटी"
          subtitle="Global uncertainty"
          color="#1C1C1C"
          textColor="#DFF1F1"
          opacity={step1}
        />
        {/* Step 2 — Right */}
        <CycleNode
          x={1430}
          y={620}
          number="2"
          title="FII बेचते हैं"
          subtitle="Capital flees India"
          color="#ff0000"
          textColor="#DFF1F1"
          opacity={step2}
        />
        {/* Step 3 — Bottom */}
        <CycleNode
          x={960}
          y={930}
          number="3"
          title="रुपया गिरता है"
          subtitle="Rupee weakens"
          color="#FFC107"
          textColor="#1C1C1C"
          opacity={step3}
        />
        {/* Step 4 — Left */}
        <CycleNode
          x={490}
          y={620}
          number="4"
          title="और कमजोरी"
          subtitle="More uncertainty"
          color="#1C1C1C"
          textColor="#DFF1F1"
          opacity={step4}
        />

        {/* Connecting curved arrows */}
        <svg width="100%" height="100%" style={{ position: 'absolute', top: 0, left: 0, pointerEvents: 'none' }}>
          {/* 1 → 2 */}
          <ArrowPath d="M 1080 360 Q 1380 380 1410 560" delay={50} sp={sp} />
          {/* 2 → 3 */}
          <ArrowPath d="M 1410 720 Q 1380 900 1080 920" delay={85} sp={sp} />
          {/* 3 → 4 */}
          <ArrowPath d="M 840 920 Q 540 900 510 720" delay={120} sp={sp} />
          {/* 4 → 1 */}
          <ArrowPath d="M 510 560 Q 540 380 840 360" delay={155} sp={sp} />
        </svg>

        {/* Center label */}
        <div
          style={{
            position: 'absolute',
            top: 580,
            left: '50%',
            transform: 'translateX(-50%)',
            textAlign: 'center',
            opacity: sp(160),
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 800,
              color: '#ff0000',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            LOOP
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 56,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1,
            }}
          >
            जो टूटता नहीं
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

const CycleNode: React.FC<{
  x: number; y: number; number: string; title: string; subtitle: string;
  color: string; textColor: string; opacity: number;
}> = ({ x, y, number, title, subtitle, color, textColor, opacity }) => {
  return (
    <div
      style={{
        position: 'absolute',
        left: x - 220,
        top: y - 80,
        width: 440,
        height: 160,
        background: color,
        opacity,
        transform: `scale(${opacity})`,
        padding: 20,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
        boxShadow: '6px 6px 0px #1C1C1C',
      }}
    >
      <div
        style={{
          width: 90,
          height: 90,
          background: textColor,
          color: color,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontFamily: 'JetBrains Mono',
          fontSize: 64,
          fontWeight: 900,
          flexShrink: 0,
        }}
      >
        {number}
      </div>
      <div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 38,
            fontWeight: 900,
            color: textColor,
            lineHeight: 1.0,
          }}
        >
          {title}
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 18,
            fontWeight: 600,
            color: textColor,
            opacity: 0.7,
            letterSpacing: 2,
            textTransform: 'uppercase',
            marginTop: 6,
          }}
        >
          {subtitle}
        </div>
      </div>
    </div>
  );
};

const ArrowPath: React.FC<{ d: string; delay: number; sp: (delay: number) => number }> = ({ d, delay, sp }) => {
  const prog = sp(delay);
  return (
    <path
      d={d}
      fill="none"
      stroke="#1C1C1C"
      strokeWidth="4"
      strokeDasharray="1000"
      strokeDashoffset={1000 * (1 - prog)}
      opacity={0.6}
      markerEnd="url(#arrowhead)"
    />
  );
};

export default CapitalFleesCycle;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PetrolDieselRise',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PetrolDieselRise: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Petrol price climbing counter
  const petrolProg = spring({ frame: Math.max(0, frame - 50), fps, config: { damping: 22, stiffness: 80 } });
  const petrolVal = (96 + petrolProg * 12.4).toFixed(2);

  const dieselProg = spring({ frame: Math.max(0, frame - 70), fps, config: { damping: 22, stiffness: 80 } });
  const dieselVal = (89 + dieselProg * 10.2).toFixed(2);

  // Fuel pump tilt
  const pumpTilt = Math.sin(frame * 0.08) * 2;

  const titleOp = sp(10);
  const pumpOp = sp(30);
  const petrolCardOp = sp(50);
  const dieselCardOp = sp(70);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(255,193,7,0.08) 0%, transparent 50%, rgba(255,0,0,0.10) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '5%',
          right: '-10%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: '#FFC107',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '10%',
          left: '-5%',
          width: 400,
          height: 400,
          border: '3px solid #ff0000',
          opacity: 0.12,
          transform: `rotate(${frame * 0.25}deg)`,
        }}
      />

      {/* Vertical line texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="vlines" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines)" />
      </svg>

      {/* Rising particles like heat */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 800) * 1920;
        const baseY = 1080;
        const py = (baseY - ((frame * 2 + seededRandom(i + 900) * 1080) % 1080));
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 6,
              height: 6,
              background: '#FFC107',
              opacity: 0.25,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 120,
          opacity: titleOp,
          transform: `translateY(${(1 - titleOp) * 30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 700,
            color: '#ff0000',
            letterSpacing: 5,
            textTransform: 'uppercase',
            marginBottom: 12,
          }}
        >
          DIRECT IMPACT — आपकी जेब पर
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.0,
            letterSpacing: -2,
          }}
        >
          पेट्रोल · डीजल चढ़ गया
        </div>
      </div>

      {/* Fuel pump SVG center */}
      <div
        style={{
          position: 'absolute',
          top: 400,
          left: '50%',
          transform: `translateX(-50%) rotate(${pumpTilt}deg) scale(${pumpOp})`,
          opacity: pumpOp,
        }}
      >
        <svg width="280" height="380" viewBox="0 0 200 280">
          {/* Pump body */}
          <rect x="40" y="60" width="100" height="180" rx="6" fill="#1C1C1C" />
          {/* Display screen */}
          <rect x="55" y="80" width="70" height="40" fill="#FFC107" />
          <text x="90" y="108" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="22" fontWeight="900" fill="#1C1C1C">
            {petrolVal}
          </text>
          {/* Nozzle */}
          <rect x="140" y="100" width="50" height="14" fill="#ff0000" />
          <rect x="180" y="100" width="14" height="60" fill="#ff0000" />
          {/* Base */}
          <rect x="30" y="240" width="120" height="20" fill="#1C1C1C" />
          {/* Top */}
          <rect x="40" y="40" width="100" height="20" fill="#1C1C1C" />
        </svg>
      </div>

      {/* Petrol card (left) */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          left: 120,
          width: 540,
          background: '#1C1C1C',
          padding: '40px 50px',
          opacity: petrolCardOp,
          transform: `translateX(${(1 - petrolCardOp) * -50}px)`,
          boxShadow: '8px 8px 0px #FFC107',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#FFC107',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 12,
          }}
        >
          PETROL · ₹/LITRE
        </div>
        <div style={{ display: 'flex', alignItems: 'baseline', gap: 8 }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 50, fontWeight: 700, color: '#9E9E9E' }}>₹</span>
          <span
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 150,
              fontWeight: 900,
              color: '#DFF1F1',
              lineHeight: 0.9,
            }}
          >
            {petrolVal}
          </span>
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 28,
            fontWeight: 600,
            color: '#ff0000',
            marginTop: 10,
          }}
        >
          ↑ +0.90 हफ्ते में दूसरी बार
        </div>
      </div>

      {/* Diesel card (right) */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          right: 120,
          width: 540,
          background: '#DFF1F1',
          padding: '40px 50px',
          border: '4px solid #1C1C1C',
          opacity: dieselCardOp,
          transform: `translateX(${(1 - dieselCardOp) * 50}px)`,
          boxShadow: '8px 8px 0px #ff0000',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 800,
            color: '#ff0000',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 12,
          }}
        >
          DIESEL · ₹/LITRE
        </div>
        <div style={{ display: 'flex', alignItems: 'baseline', gap: 8 }}>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 50, fontWeight: 700, color: '#9E9E9E' }}>₹</span>
          <span
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 150,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 0.9,
            }}
          >
            {dieselVal}
          </span>
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 28,
            fontWeight: 600,
            color: '#ff0000',
            marginTop: 10,
          }}
        >
          ↑ हर ट्रक, हर सब्जी महंगी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default PetrolDieselRise;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ImportsPriceUp',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ImportsPriceUp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan camera
  const camY = interpolate(frame, [0, durationInFrames], [10, -10]);

  const titleOp = sp(10);

  const items = [
    { name: 'मोबाइल', sub: 'SMARTPHONES', pct: '+8%', delay: 35, icon: 'phone' },
    { name: 'टीवी', sub: 'TV · APPLIANCES', pct: '+12%', delay: 50, icon: 'tv' },
    { name: 'लैपटॉप', sub: 'LAPTOPS', pct: '+10%', delay: 65, icon: 'laptop' },
    { name: 'खाद्य तेल', sub: 'EDIBLE OIL', pct: '+15%', delay: 80, icon: 'oil' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(45deg, rgba(255,193,7,0.06) 0%, transparent 50%, rgba(255,0,0,0.08) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-10%',
          right: '20%',
          width: 500,
          height: 500,
          background: '#1C1C1C',
          opacity: 0.04,
          clipPath: 'polygon(25% 0%, 100% 0%, 75% 100%, 0% 100%)',
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          left: '10%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '4px solid #FFC107',
          opacity: 0.1,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="grid3" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 1100) * 1920;
        const py = seededRandom(i + 1200) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + Math.sin(frame * 0.02 + i) * 18,
              width: 3,
              height: 3,
              background: '#1C1C1C',
              opacity: 0.22,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 120,
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            IMPORTED GOODS · सब महंगा
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 102,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            हर चीज पर असर
          </div>
        </div>

        {/* 4-card grid */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 120,
            right: 120,
            display: 'grid',
            gridTemplateColumns: 'repeat(4, 1fr)',
            gap: 30,
          }}
        >
          {items.map((item, i) => {
            const op = sp(item.delay);
            return (
              <div
                key={item.name}
                style={{
                  background: i === 3 ? '#FFC107' : '#1C1C1C',
                  padding: '40px 30px',
                  height: 540,
                  opacity: op,
                  transform: `translateY(${(1 - op) * 60}px)`,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'space-between',
                  boxShadow: '6px 6px 0px #1C1C1C',
                }}
              >
                {/* Icon SVG */}
                <div style={{ display: 'flex', justifyContent: 'center', marginTop: 20 }}>
                  <ItemIcon type={item.icon} color={i === 3 ? '#1C1C1C' : '#DFF1F1'} />
                </div>

                {/* Text */}
                <div>
                  <div
                    style={{
                      fontFamily: 'Yantra One',
                      fontSize: 56,
                      fontWeight: 900,
                      color: i === 3 ? '#1C1C1C' : '#DFF1F1',
                      lineHeight: 1.0,
                    }}
                  >
                    {item.name}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 18,
                      fontWeight: 700,
                      color: i === 3 ? '#1C1C1C' : '#9E9E9E',
                      letterSpacing: 3,
                      textTransform: 'uppercase',
                      marginTop: 6,
                    }}
                  >
                    {item.sub}
                  </div>
                  {/* Price tag */}
                  <div
                    style={{
                      marginTop: 24,
                      background: i === 3 ? '#1C1C1C' : '#ff0000',
                      padding: '14px 20px',
                      display: 'inline-block',
                    }}
                  >
                    <span
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 48,
                        fontWeight: 900,
                        color: i === 3 ? '#FFC107' : '#DFF1F1',
                      }}
                    >
                      {item.pct}
                    </span>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(28,28,28,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

const ItemIcon: React.FC<{ type: string; color: string }> = ({ type, color }) => {
  if (type === 'phone') {
    return (
      <svg width="110" height="160" viewBox="0 0 80 120">
        <rect x="10" y="5" width="60" height="110" rx="10" fill="none" stroke={color} strokeWidth="5" />
        <rect x="18" y="18" width="44" height="70" fill={color} opacity="0.3" />
        <circle cx="40" cy="100" r="5" fill={color} />
      </svg>
    );
  }
  if (type === 'tv') {
    return (
      <svg width="160" height="140" viewBox="0 0 120 100">
        <rect x="5" y="5" width="110" height="70" rx="4" fill="none" stroke={color} strokeWidth="5" />
        <rect x="13" y="13" width="94" height="54" fill={color} opacity="0.3" />
        <rect x="50" y="80" width="20" height="10" fill={color} />
        <rect x="30" y="88" width="60" height="6" rx="2" fill={color} />
      </svg>
    );
  }
  if (type === 'laptop') {
    return (
      <svg width="170" height="130" viewBox="0 0 130 100">
        <rect x="15" y="10" width="100" height="65" rx="3" fill="none" stroke={color} strokeWidth="5" />
        <rect x="22" y="18" width="86" height="50" fill={color} opacity="0.3" />
        <rect x="5" y="78" width="120" height="10" rx="2" fill={color} />
      </svg>
    );
  }
  // oil bottle
  return (
    <svg width="100" height="160" viewBox="0 0 80 120">
      <rect x="30" y="5" width="20" height="18" fill={color} />
      <path d="M 22 25 L 22 35 Q 12 40 12 55 L 12 110 Q 12 115 17 115 L 63 115 Q 68 115 68 110 L 68 55 Q 68 40 58 35 L 58 25 Z" fill="none" stroke={color} strokeWidth="5" />
      <path d="M 18 70 L 62 70 L 62 110 Q 62 113 60 113 L 20 113 Q 18 113 18 110 Z" fill={color} opacity="0.4" />
    </svg>
  );
};

export default ImportsPriceUp;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CommonManHit',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CommonManHit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Zoom toward center
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0]);

  const titleOp = sp(10);
  const houseOp = sp(35);
  const billsOp1 = sp(55);
  const billsOp2 = sp(70);
  const billsOp3 = sp(85);
  const billsOp4 = sp(100);

  // Pressure pulse
  const pulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(255,0,0,0.10) 0%, transparent 65%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 800,
          height: 800,
          borderRadius: '50%',
          background: '#ff0000',
          opacity: 0.04,
          transform: `translate(-50%, -50%) scale(${pulse})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 1100,
          height: 1100,
          borderRadius: '50%',
          border: '2px solid #ff0000',
          opacity: 0.08,
          transform: `translate(-50%, -50%) scale(${pulse * 0.95})`,
        }}
      />

      {/* Concentric squares texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="sqGrid" width="120" height="120" patternUnits="userSpaceOnUse">
            <rect x="10" y="10" width="100" height="100" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#sqGrid)" />
      </svg>

      {/* Particles falling like pressure */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 1300) * 1920;
        const py = ((frame * 1.5 + seededRandom(i + 1400) * 1080) % 1080);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 4,
              height: 12,
              background: '#1C1C1C',
              opacity: 0.18,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - titleOp) * 30}px)`,
            opacity: titleOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#ff0000',
              letterSpacing: 5,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            THE REAL VICTIM
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 110,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            आम आदमी की कमर टूटी
          </div>
        </div>

        {/* Center house with arrows hitting it */}
        <div
          style={{
            position: 'absolute',
            top: 430,
            left: '50%',
            transform: `translateX(-50%) scale(${houseOp})`,
            opacity: houseOp,
          }}
        >
          <svg width="380" height="380" viewBox="0 0 200 200">
            {/* House body */}
            <rect x="40" y="90" width="120" height="100" fill="#1C1C1C" />
            {/* Roof */}
            <polygon points="30,90 100,30 170,90" fill="#FFC107" />
            {/* Door */}
            <rect x="85" y="135" width="30" height="55" fill="#DFF1F1" />
            <circle cx="108" cy="163" r="2" fill="#1C1C1C" />
            {/* Windows */}
            <rect x="55" y="110" width="22" height="22" fill="#DFF1F1" />
            <rect x="123" y="110" width="22" height="22" fill="#DFF1F1" />
            <line x1="66" y1="110" x2="66" y2="132" stroke="#1C1C1C" strokeWidth="1" />
            <line x1="55" y1="121" x2="77" y2="121" stroke="#1C1C1C" strokeWidth="1" />
            <line x1="134" y1="110" x2="134" y2="132" stroke="#1C1C1C" strokeWidth="1" />
            <line x1="123" y1="121" x2="145" y2="121" stroke="#1C1C1C" strokeWidth="1" />
            {/* Chimney */}
            <rect x="130" y="50" width="14" height="30" fill="#1C1C1C" />
          </svg>
        </div>

        {/* Bill pressure boxes — 4 sides pointing inward */}
        {/* Top-left: किराना */}
        <PressureBox
          x={250} y={420}
          label="किराना"
          sub="GROCERIES"
          pct="+18%"
          opacity={billsOp1}
          direction="tr"
        />
        {/* Top-right: पेट्रोल */}
        <PressureBox
          x={1380} y={420}
          label="पेट्रोल"
          sub="FUEL"
          pct="+11%"
          opacity={billsOp2}
          direction="tl"
        />
        {/* Bottom-left: बिजली */}
        <PressureBox
          x={250} y={720}
          label="बिजली"
          sub="ELECTRICITY"
          pct="+9%"
          opacity={billsOp3}
          direction="br"
        />
        {/* Bottom-right: EMI */}
        <PressureBox
          x={1380} y={720}
          label="EMI"
          sub="LOAN"
          pct="+7%"
          opacity={billsOp4}
          direction="bl"
        />

        {/* Inward arrows */}
        <svg width="100%" height="100%" style={{ position: 'absolute', top: 0, left: 0, pointerEvents: 'none' }}>
          <InwardArrow x1={510} y1={490} x2={830} y2={560} delay={60} sp={sp} />
          <InwardArrow x1={1370} y1={490} x2={1080} y2={560} delay={75} sp={sp} />
          <InwardArrow x1={510} y1={780} x2={830} y2={680} delay={90} sp={sp} />
          <InwardArrow x1={1370} y1={780} x2={1080} y2={680} delay={105} sp={sp} />
        </svg>

        {/* Bottom tag */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: sp(115),
            background: '#1C1C1C',
            padding: '20px 50px',
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div style={{ width: 12, height: 12, background: '#ff0000', borderRadius: '50%', boxShadow: `0 0 ${10 + Math.sin(frame * 0.2) * 6}px #ff0000` }} />
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 38,
              fontWeight: 700,
              color: '#DFF1F1',
            }}
          >
            हर तरफ से दबाव — चक्र चल रहा है
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 45%, rgba(28,28,28,0.30) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

const PressureBox: React.FC<{
  x: number; y: number; label: string; sub: string; pct: string; opacity: number; direction: string;
}> = ({ x, y, label, sub, pct, opacity }) => {
  return (
    <div
      style={{
        position: 'absolute',
        left: x - 130,
        top: y - 70,
        width: 260,
        background: '#DFF1F1',
        border: '4px solid #1C1C1C',
        padding: '20px 24px',
        opacity,
        transform: `scale(${opacity})`,
        boxShadow: '6px 6px 0px #ff0000',
      }}
    >
      <div
        style={{
          fontFamily: 'Yantra One',
          fontSize: 44,
          fontWeight: 900,
          color: '#1C1C1C',
          lineHeight: 1.0,
        }}
      >
        {label}
      </div>
      <div
        style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 14,
          fontWeight: 700,
          color: '#9E9E9E',
          letterSpacing: 3,
          textTransform: 'uppercase',
          marginTop: 4,
        }}
      >
        {sub}
      </div>
      <div
        style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 38,
          fontWeight: 900,
          color: '#ff0000',
          marginTop: 8,
        }}
      >
        {pct}
      </div>
    </div>
  );
};

const InwardArrow: React.FC<{ x1: number; y1: number; x2: number; y2: number; delay: number; sp: (d: number) => number }> = ({ x1, y1, x2, y2, delay, sp }) => {
  const prog = sp(delay);
  const cx = x1 + (x2 - x1) * prog;
  const cy = y1 + (y2 - y1) * prog;
  return (
    <>
      <line
        x1={x1}
        y1={y1}
        x2={cx}
        y2={cy}
        stroke="#ff0000"
        strokeWidth="4"
        strokeDasharray="8 6"
      />
      {prog > 0.9 && (
        <circle cx={x2} cy={y2} r="8" fill="#ff0000" />
      )}
    </>
  );
};

export default CommonManHit;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PauseAndAsk',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PauseAndAsk: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow drift camera
  const camX = interpolate(frame, [0, durationInFrames], [-8, 8]);

  // Question mark scale pulse
  const qmPulse = 1 + Math.sin(frame * 0.1) * 0.05;

  // Stop / pause icon throb
  const pauseScale = sp(15);

  const stopOp = sp(10);
  const lineOp = sp(30);
  const headOp = sp(45);
  const subOp = sp(70);
  const ctaOp = sp(95);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 40%, rgba(255,193,7,0.12) 0%, transparent 60%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '20%',
          left: '10%',
          width: 350,
          height: 350,
          borderRadius: '50%',
          border: '3px solid #1C1C1C',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          top: '60%',
          right: '8%',
          width: 400,
          height: 400,
          background: '#FFC107',
          opacity: 0.05,
          clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Concentric circles texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="conCirc" width="120" height="120" patternUnits="userSpaceOnUse">
            <circle cx="60" cy="60" r="30" fill="none" stroke="#1C1C1C" strokeWidth="1" />
            <circle cx="60" cy="60" r="15" fill="none" stroke="#1C1C1C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#conCirc)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i + 1500) * 1920;
        const py = seededRandom(i + 1600) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px + Math.cos(frame * 0.02 + i) * 18,
              top: py + Math.sin(frame * 0.02 + i) * 18,
              width: 5,
              height: 5,
              background: '#1C1C1C',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* STOP / Pause icon top */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: '50%',
            transform: `translateX(-50%) scale(${pauseScale})`,
            opacity: stopOp,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          {/* Pause icon */}
          <svg width="80" height="80" viewBox="0 0 60 60">
            <rect x="12" y="10" width="14" height="40" fill="#ff0000" />
            <rect x="34" y="10" width="14" height="40" fill="#ff0000" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 900,
              color: '#ff0000',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            PAUSE — सोचिए
          </div>
        </div>

        {/* Divider line */}
        <div
          style={{
            position: 'absolute',
            top: 235,
            left: '50%',
            transform: `translateX(-50%) scaleX(${lineOp})`,
            width: 600,
            height: 4,
            background: '#1C1C1C',
            opacity: lineOp,
          }}
        />

        {/* Big question */}
        <div
          style={{
            position: 'absolute',
            top: 300,
            left: 0,
            right: 0,
            opacity: headOp,
            transform: `translateY(${(1 - headOp) * 40}px)`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 130,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.0,
              letterSpacing: -2,
            }}
          >
            आपके घर में
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 130,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1.0,
              letterSpacing: -2,
              marginTop: -8,
            }}
          >
            कितनी REAL VALUE है?
          </div>
        </div>

        {/* Floating question marks */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 180,
            opacity: headOp * 0.4,
            transform: `scale(${qmPulse}) rotate(-15deg)`,
            fontFamily: 'Yantra One',
            fontSize: 280,
            fontWeight: 900,
            color: '#FFC107',
            lineHeight: 1,
          }}
        >
          ?
        </div>
        <div
          style={{
            position: 'absolute',
            top: 330,
            right: 200,
            opacity: headOp * 0.4,
            transform: `scale(${qmPulse * 0.9}) rotate(20deg)`,
            fontFamily: 'Yantra One',
            fontSize: 240,
            fontWeight: 900,
            color: '#FFC107',
            lineHeight: 1,
          }}
        >
          ?
        </div>

        {/* Sub-question */}
        <div
          style={{
            position: 'absolute',
            top: 660,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - subOp) * 30}px)`,
            opacity: subOp,
            textAlign: 'center',
            width: 1400,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 52,
              fontWeight: 600,
              color: '#1C1C1C',
              lineHeight: 1.3,
            }}
          >
            अगर कल बैंक बंद हो जाएं, ATM 15 दिन काम न करें
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 52,
              fontWeight: 700,
              color: '#ff0000',
              lineHeight: 1.3,
              marginTop: 10,
            }}
          >
            तो आपके पास क्या है?
          </div>
        </div>

        {/* CTA tag */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${ctaOp})`,
            opacity: ctaOp,
            background: '#FFC107',
            padding: '18px 50px',
            border: '4px solid #1C1C1C',
            boxShadow: '6px 6px 0px #1C1C1C',
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 800,
              color: '#1C1C1C',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            ↓ COMMENT में honestly बताइए
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default PauseAndAsk;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TwoCategories',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TwoCategories: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera split open
  const splitProg = sp(25, { damping: 16 });

  const titleOp = sp(8);
  const leftOp = sp(40);
  const rightOp = sp(55);
  const vsOp = sp(75);
  const bottomOp = sp(105);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(255,0,0,0.08) 0%, transparent 30%, transparent 70%, rgba(255,193,7,0.10) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          top: '-10%',
          left: '40%',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '3px solid #1C1C1C',
          opacity: 0.06,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: '-10%',
          left: '45%',
          width: 500,
          height: 500,
          background: '#1C1C1C',
          opacity: 0.04,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Vertical center divider texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="dotsV" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#1C1C1C" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dotsV)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 1700) * 1920;
        const py = seededRandom(i + 1800) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + Math.sin(frame * 0.02 + i) * 22,
              width: 4,
              height: 4,
              background: '#1C1C1C',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Top headline */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - titleOp) * 30}px)`,
          opacity: titleOp,
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 26,
            fontWeight: 700,
            color: '#ff0000',
            letterSpacing: 6,
            textTransform: 'uppercase',
            marginBottom: 14,
          }}
        >
          THE BIG DIVIDE
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 96,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.0,
            letterSpacing: -2,
          }}
        >
          आप किस कैटेगरी में हैं?
        </div>
      </div>

      {/* Left category — Paper assets */}
      <div
        style={{
          position: 'absolute',
          top: 340,
          left: 120,
          width: 800,
          height: 620,
          background: '#1C1C1C',
          padding: 50,
          opacity: leftOp,
          transform: `translateX(${(1 - splitProg) * -200}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 24,
            fontWeight: 800,
            color: '#ff0000',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 10,
          }}
        >
          CATEGORY A
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 76,
            fontWeight: 900,
            color: '#DFF1F1',
            lineHeight: 1.0,
            marginBottom: 30,
          }}
        >
          कागजी दौलत
        </div>

        {/* Paper stack icon */}
        <svg width="180" height="160" viewBox="0 0 140 120" style={{ marginBottom: 20 }}>
          <rect x="20" y="30" width="90" height="70" fill="#9E9E9E" />
          <rect x="28" y="22" width="90" height="70" fill="#DFF1F1" opacity="0.7" />
          <rect x="36" y="14" width="90" height="70" fill="#DFF1F1" />
          <line x1="44" y1="32" x2="118" y2="32" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="44" y1="44" x2="118" y2="44" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="44" y1="56" x2="100" y2="56" stroke="#1C1C1C" strokeWidth="2" />
        </svg>

        {[
          'Bank Deposits',
          'Shares · Bonds',
          'Mutual Funds',
          'Paper Contracts',
        ].map((item, i) => (
          <div
            key={item}
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#DFF1F1',
              opacity: sp(50 + i * 8),
              marginBottom: 12,
              display: 'flex',
              alignItems: 'center',
              gap: 14,
            }}
          >
            <div style={{ width: 14, height: 14, background: '#ff0000' }} />
            {item}
          </div>
        ))}

        <div
          style={{
            marginTop: 30,
            padding: '16px 24px',
            background: '#ff0000',
            display: 'inline-block',
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#DFF1F1',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            SYSTEM-DEPENDENT
          </span>
        </div>
      </div>

      {/* Right category — Real assets */}
      <div
        style={{
          position: 'absolute',
          top: 340,
          right: 120,
          width: 800,
          height: 620,
          background: '#FFC107',
          padding: 50,
          opacity: rightOp,
          transform: `translateX(${(1 - splitProg) * 200}px)`,
          boxShadow: '10px 10px 0px #1C1C1C',
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 24,
            fontWeight: 800,
            color: '#1C1C1C',
            letterSpacing: 4,
            textTransform: 'uppercase',
            marginBottom: 10,
            opacity: 0.7,
          }}
        >
          CATEGORY B
        </div>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 76,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.0,
            marginBottom: 30,
          }}
        >
          असली चीज़ें
        </div>

        {/* Real asset icon - mountain/land */}
        <svg width="180" height="160" viewBox="0 0 140 120" style={{ marginBottom: 20 }}>
          <polygon points="10,100 50,40 75,75 100,30 130,100" fill="#1C1C1C" />
          <polygon points="50,40 56,50 44,50" fill="#DFF1F1" />
          <polygon points="100,30 108,42 92,42" fill="#DFF1F1" />
          <line x1="0" y1="105" x2="140" y2="105" stroke="#1C1C1C" strokeWidth="3" />
        </svg>

        {[
          'जमीन · Land',
          'सोना · Gold',
          'हुनर · Skill',
          'खाना उगाने की ताकत',
        ].map((item, i) => (
          <div
            key={item}
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              fontWeight: 600,
              color: '#1C1C1C',
              opacity: sp(65 + i * 8),
              marginBottom: 12,
              display: 'flex',
              alignItems: 'center',
              gap: 14,
            }}
          >
            <div style={{ width: 14, height: 14, background: '#1C1C1C' }} />
            {item}
          </div>
        ))}

        <div
          style={{
            marginTop: 30,
            padding: '16px 24px',
            background: '#1C1C1C',
            display: 'inline-block',
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 800,
              color: '#FFC107',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            CRISIS-PROOF
          </span>
        </div>
      </div>

      {/* Center VS */}
      <div
        style={{
          position: 'absolute',
          top: 600,
          left: '50%',
          transform: `translateX(-50%) translateY(-50%) scale(${vsOp})`,
          opacity: vsOp,
          width: 140,
          height: 140,
          borderRadius: '50%',
          background: '#DFF1F1',
          border: '5px solid #1C1C1C',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '6px 6px 0px #ff0000',
        }}
      >
        <span
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 56,
            fontWeight: 900,
            color: '#1C1C1C',
          }}
        >
          VS
        </span>
      </div>

      {/* Bottom message */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - bottomOp) * 30}px)`,
          opacity: bottomOp,
          textAlign: 'center',
          background: '#1C1C1C',
          padding: '20px 60px',
        }}
      >
        <span
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            fontWeight: 700,
            color: '#DFF1F1',
          }}
        >
          आपका जवाब ही आपकी कैटेगरी है
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 50%, rgba(28,28,28,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default TwoCategories;
```

