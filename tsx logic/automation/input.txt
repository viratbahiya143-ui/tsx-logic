```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'EconomicMoatIntro',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const EconomicMoatIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { extrapolateRight: 'clamp' });

  const ringPulse = interpolate(frame % 90, [0, 45, 90], [0.9, 1.05, 0.9]);
  const titleSp = sp(6);
  const subSp = sp(28);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 40%, #FFB347 0%, #FF9E20 55%, #E8830B 100%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Shape Layers - concentric moat rings */}
      {[1, 2, 3, 4].map((i) => {
        const r = 200 + i * 160 * ringPulse;
        const rot = interpolate(frame, [0, durationInFrames], [0, i % 2 === 0 ? 25 : -25]);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: '50%',
              left: '68%',
              width: r,
              height: r,
              marginLeft: -r / 2,
              marginTop: -r / 2,
              borderRadius: '50%',
              border: `3px solid #1D2128`,
              opacity: 0.06,
              transform: `rotate(${rot}deg)`,
            }}
          />
        );
      })}

      {/* Motion Texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={i * 96} y1="0" x2={i * 96} y2="1080" stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i * 13.7;
        const px = seededRandom(seed) * 1920;
        const startY = 1080 + seededRandom(seed + 1) * 200;
        const py = interpolate((frame + seed * 10) % 220, [0, 220], [startY, -100]);
        const size = 3 + seededRandom(seed + 2) * 6;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: '#1D2128',
              opacity: 0.15,
            }}
          />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'flex-start', padding: '0 140px' }}>
        {/* Shield/Moat Icon */}
        <svg
          width="140"
          height="140"
          viewBox="0 0 24 24"
          style={{
            marginBottom: 30,
            transform: `scale(${sp(0)}) rotate(${interpolate(frame, [0, 40], [-8, 0])}deg)`,
            opacity: sp(0),
          }}
        >
          <path
            d="M12 2L4 5v6c0 5.5 3.8 10.7 8 12 4.2-1.3 8-6.5 8-12V5l-8-3z"
            fill="none"
            stroke="#1D2128"
            strokeWidth="1.5"
          />
          <circle cx="12" cy="11" r="4" fill="#1D2128" opacity="0.9" />
        </svg>

        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 100,
            color: '#1D2128',
            fontWeight: 900,
            lineHeight: 1.05,
            maxWidth: 1300,
            transform: `translateY(${interpolate(titleSp, [0, 1], [60, 0])}px)`,
            opacity: titleSp,
          }}
        >
          THE ECONOMIC MOAT
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 48,
            color: '#1D2128',
            marginTop: 26,
            maxWidth: 1150,
            fontWeight: 600,
            transform: `translateY(${interpolate(subSp, [0, 1], [40, 0])}px)`,
            opacity: subSp,
          }}
        >
          The competitive advantage that protects a business from rivals.
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 40,
            color: '#F4F2F2',
            marginTop: 34,
            maxWidth: 1100,
            fontWeight: 500,
            backgroundColor: '#1D2128',
            padding: '18px 30px',
            borderRadius: 14,
            opacity: sp(55),
            transform: `translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
          }}
        >
          Buffett avoided tech stocks — he couldn't predict how long a tech moat would last.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, durationInFrames], [0.3, 0.3]),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default EconomicMoatIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'MoatWidening',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MoatWidening: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // moat width expands over time
  const moatWidth = interpolate(frame, [0, durationInFrames], [140, 420], { extrapolateRight: 'clamp' });
  const humanFade = interpolate(frame, [40, 90], [1, 0.08], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const panX = interpolate(frame, [0, durationInFrames], [0, -60]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #FFB84D 0%, #FF9E20 50%, #D97706 100%)',
          transform: `translateX(${panX}px)`,
        }}
      />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute',
          top: -200,
          right: -150,
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '2px solid #1D2128',
          opacity: 0.05,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [0, 30])}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: -180,
          left: -120,
          width: 0,
          height: 0,
          borderLeft: '260px solid transparent',
          borderRight: '260px solid transparent',
          borderBottom: '450px solid #1D2128',
          opacity: 0.04,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [10, -10])}deg)`,
        }}
      />

      {/* Motion Texture - concentric dashed circles as widening moat */}
      <svg width="1920" height="1080" style={{ position: 'absolute' }}>
        <circle cx="960" cy="540" r={moatWidth} fill="none" stroke="#1D2128" strokeWidth="26" opacity="0.12" strokeDasharray="4 14" />
        <circle cx="960" cy="540" r={moatWidth + 90} fill="none" stroke="#1D2128" strokeWidth="10" opacity="0.08" />
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i * 21.3;
        const px = seededRandom(seed) * 1920;
        const startY = 1080 + seededRandom(seed + 1) * 200;
        const py = interpolate((frame + seed * 9) % 200, [0, 200], [startY, -80]);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py,
              width: 4 + seededRandom(seed + 2) * 5,
              height: 4 + seededRandom(seed + 2) * 5,
              borderRadius: '50%',
              backgroundColor: '#1D2128',
              opacity: 0.12,
            }}
          />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {/* Brain icon fading = human variable eliminated */}
        <svg
          width="150"
          height="150"
          viewBox="0 0 24 24"
          style={{
            position: 'absolute',
            top: 140,
            opacity: humanFade,
            transform: `scale(${interpolate(humanFade, [0.08, 1], [0.7, 1])})`,
          }}
        >
          <path
            d="M9 2a4 4 0 00-4 4v1a3 3 0 00-2 2.8V13a3 3 0 002 2.8v1A4 4 0 0011 21a4 4 0 004-4V6a4 4 0 00-4-4H9z"
            fill="#1D2128"
          />
        </svg>

        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 118,
            color: '#1D2128',
            fontWeight: 900,
            textAlign: 'center',
            transform: `scale(${sp(10)})`,
            opacity: sp(10),
          }}
        >
          THE MOAT IS WIDENING
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 46,
            color: '#1D2128',
            marginTop: 26,
            maxWidth: 1200,
            textAlign: 'center',
            fontWeight: 600,
            opacity: sp(35),
            transform: `translateY(${interpolate(sp(35), [0, 1], [30, 0])}px)`,
          }}
        >
          Eliminating the human variable — completely.
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            color: '#F4F2F2',
            marginTop: 32,
            backgroundColor: '#1D2128',
            padding: '16px 34px',
            borderRadius: 40,
            fontWeight: 500,
            opacity: sp(60),
            transform: `translateY(${interpolate(sp(60), [0, 1], [24, 0])}px)`,
          }}
        >
          Your brain. Your education. Fading fast.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default MoatWidening;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SECFilingSpeedRun',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SECFilingSpeedRun: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // pages stack collapsing into chip between frame 60-150
  const collapseProgress = interpolate(frame, [70, 150], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const countdown = Math.max(0, 4 - Math.floor(interpolate(frame, [150, 240], [0, 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  const shake = interpolate(frame, [148, 152, 156], [0, 8, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 30%, #FFC670 0%, #FF9E20 60%, #E8830B 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: -200,
          width: 550,
          height: 550,
          borderRadius: '50%',
          border: '2px solid #1D2128',
          opacity: 0.05,
          transform: `scale(${interpolate(frame % 150, [0, 75, 150], [1, 1.1, 1])})`,
        }}
      />

      {/* Motion Texture - circuit lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <rect key={i} x={i * 240} y="0" width="2" height="1080" fill="#1D2128" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const seed = i * 17.1;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 8) % 190, [0, 190], [1080, -80]);
        return (
          <div
            key={i}
            style={{
              position: 'absolute', left: px, top: py,
              width: 4, height: 4, borderRadius: '50%',
              backgroundColor: '#1D2128', opacity: 0.15,
            }}
          />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{ position: 'relative', width: 500, height: 380 }}>
          {/* Stack of document pages collapsing */}
          {Array.from({ length: 6 }).map((_, i) => {
            const offset = i * 12;
            const scaleDoc = interpolate(collapseProgress, [0, 1], [1, 0.05]);
            const opacityDoc = interpolate(collapseProgress, [0, 0.8, 1], [1, 0.4, 0]);
            const rotDoc = interpolate(collapseProgress, [0, 1], [0, i % 2 === 0 ? 40 : -40]);
            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: offset,
                  left: offset * 0.5,
                  width: 340,
                  height: 440,
                  backgroundColor: '#1D2128',
                  opacity: opacityDoc * 0.85,
                  borderRadius: 8,
                  transform: `scale(${scaleDoc}) rotate(${rotDoc}deg)`,
                }}
              >
                <div style={{ padding: 24 }}>
                  {Array.from({ length: 8 }).map((_, li) => (
                    <div key={li} style={{ height: 6, backgroundColor: '#F4F2F2', opacity: 0.3, marginBottom: 14, width: `${70 + seededRandom(li) * 25}%`, borderRadius: 3 }} />
                  ))}
                </div>
              </div>
            );
          })}

          {/* Chip / AI core appearing */}
          <svg
            width="180"
            height="180"
            viewBox="0 0 24 24"
            style={{
              position: 'absolute',
              top: 100,
              left: 160,
              opacity: interpolate(collapseProgress, [0.5, 1], [0, 1]),
              transform: `scale(${interpolate(collapseProgress, [0.5, 1], [0.4, 1])})`,
            }}
          >
            <rect x="7" y="7" width="10" height="10" fill="none" stroke="#1D2128" strokeWidth="1.5" rx="1" />
            <rect x="10" y="10" width="4" height="4" fill="#1D2128" />
            {[2, 5, 8, 11, 14, 17, 20].map((p, idx) => (
              <line key={idx} x1={p} y1="2" x2={p} y2="7" stroke="#1D2128" strokeWidth="1" />
            ))}
          </svg>
        </div>

        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 128,
            color: '#1D2128',
            fontWeight: 900,
            marginTop: 20,
            opacity: interpolate(collapseProgress, [0.6, 1], [0, 1]),
            transform: `translateY(${interpolate(collapseProgress, [0.6, 1], [30, 0])}px)`,
          }}
        >
          {countdown > 0 ? countdown + 's' : '500 PAGES → GONE'}
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 42,
            color: '#F4F2F2',
            backgroundColor: '#1D2128',
            padding: '16px 36px',
            borderRadius: 14,
            marginTop: 26,
            fontWeight: 600,
            opacity: sp(220),
          }}
        >
          SEC filing read + summarized. Instantly.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default SECFilingSpeedRun;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'WealthTransferFlow',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WealthTransferFlow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const evapOpacity = interpolate(frame, [0, 70], [1, 0.1], { extrapolateRight: 'clamp' });
  const evapY = interpolate(frame, [0, 70], [0, -80]);
  const flowProgress = interpolate(frame, [40, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #FFB84D 0%, #FF9E20 50%, #D97706 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', bottom: -220, right: -160, width: 600, height: 600, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [0, 20])}deg)`,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <circle key={i} cx={200 + i * 180} cy="900" r="60" fill="none" stroke="#1D2128" strokeWidth="1.5" />
        ))}
      </svg>

      {/* Floating Particles - flowing upward from bottom to top representing wealth flow */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i * 9.4;
        const startX = 300 + seededRandom(seed) * 400;
        const endX = 1400 + seededRandom(seed + 1) * 300;
        const px = interpolate(flowProgress, [0, 1], [startX, endX]);
        const py = interpolate((frame + seed * 6) % 150, [0, 150], [800, 300]);
        return (
          <div
            key={i}
            style={{
              position: 'absolute', left: px, top: py,
              width: 6, height: 6, borderRadius: '50%',
              backgroundColor: '#1D2128', opacity: 0.25,
            }}
          />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', flexDirection: 'row' }}>
        {/* Left side: evaporating brain/moat icon */}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: evapOpacity, transform: `translateY(${evapY}px)` }}>
          <svg width="130" height="130" viewBox="0 0 24 24">
            <path d="M9 2a4 4 0 00-4 4v1a3 3 0 00-2 2.8V13a3 3 0 002 2.8v1A4 4 0 0011 21a4 4 0 004-4V6a4 4 0 00-4-4H9z" fill="#1D2128" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 34, color: '#1D2128', marginTop: 16, fontWeight: 700 }}>
            YOUR MOAT
          </div>
        </div>

        {/* Arrow flow icon */}
        <svg width="200" height="80" viewBox="0 0 200 80" style={{ opacity: sp(30) }}>
          <line x1="10" y1="40" x2="180" y2="40" stroke="#1D2128" strokeWidth="4" />
          <polygon points="180,40 155,25 155,55" fill="#1D2128" />
        </svg>

        {/* Right side: capital owners rising */}
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${sp(50)})`, opacity: sp(50) }}>
          <svg width="150" height="150" viewBox="0 0 24 24">
            <path d="M3 17l6-6 4 4 8-8" stroke="#1D2128" strokeWidth="2" fill="none" />
            <polygon points="21,7 21,3 17,3" fill="#1D2128" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 34, color: '#1D2128', marginTop: 16, fontWeight: 700 }}>
            CAPITAL OWNERS
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ justifyContent: 'flex-end', alignItems: 'center', paddingBottom: 90 }}>
        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 96,
            color: '#1D2128',
            fontWeight: 900,
            textAlign: 'center',
            opacity: sp(80),
            transform: `translateY(${interpolate(sp(80), [0, 1], [40, 0])}px)`,
          }}
        >
          MOAT EVAPORATES OVERNIGHT
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            color: '#F4F2F2',
            backgroundColor: '#1D2128',
            padding: '14px 30px',
            borderRadius: 14,
            marginTop: 20,
            fontWeight: 500,
            opacity: sp(100),
          }}
        >
          Shareholders capture the productivity gains.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default WealthTransferFlow;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'MortgageDebtPressure',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MortgageDebtPressure: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const countUp = Math.min(7, interpolate(frame, [10, 70], [0, 7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const carDrop = interpolate(frame, [90, 200], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 70%, #FFB84D 0%, #FF9E20 55%, #E8830B 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', top: -150, right: -100, width: 500, height: 500,
          border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [0, -25])}deg)`,
        }}
      />

      {/* Motion Texture - descending bar grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 90} x2="1920" y2={i * 90} stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const seed = i * 15.5;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 7) % 180, [0, 180], [1080, -60]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 5, height: 5, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.14 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ flexDirection: 'row', justifyContent: 'space-evenly', alignItems: 'center' }}>
        {/* Interest rate percentage card */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `scale(${sp(0)})`, opacity: sp(0) }}>
          <svg width="120" height="120" viewBox="0 0 24 24">
            <path d="M3 21L21 3M7 7a3 3 0 100-6 3 3 0 000 6zM17 23a3 3 0 100-6 3 3 0 000 6z" fill="none" stroke="#1D2128" strokeWidth="1.5" />
          </svg>
          <div style={{ fontFamily: 'Mont Heavy', fontSize: 168, color: '#1D2128', fontWeight: 900 }}>
            {countUp.toFixed(1)}%
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 40, color: '#1D2128', fontWeight: 700 }}>
            MORTGAGE RATE
          </div>
        </div>

        {/* Depreciating car icon dropping */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <svg
            width="220"
            height="120"
            viewBox="0 0 24 24"
            style={{ transform: `translateY(${carDrop * 0.5}px)`, opacity: interpolate(carDrop, [0, 100], [1, 0.3]) }}
          >
            <path
              d="M3 13l1.5-4.5A2 2 0 016.4 7h11.2a2 2 0 011.9 1.5L21 13v5a1 1 0 01-1 1h-1a1 1 0 01-1-1v-1H6v1a1 1 0 01-1 1H4a1 1 0 01-1-1v-5z"
              fill="none" stroke="#1D2128" strokeWidth="1.4"
            />
            <circle cx="7" cy="18" r="1.6" fill="#1D2128" />
            <circle cx="17" cy="18" r="1.6" fill="#1D2128" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 40, color: '#1D2128', fontWeight: 700, marginTop: 20 }}>
            DEPRECIATING CAR
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ justifyContent: 'flex-end', alignItems: 'center', paddingBottom: 80 }}>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 46,
            color: '#F4F2F2',
            backgroundColor: '#1D2128',
            padding: '20px 40px',
            borderRadius: 16,
            fontWeight: 600,
            maxWidth: 1300,
            textAlign: 'center',
            opacity: sp(150),
            transform: `translateY(${interpolate(sp(150), [0, 1], [30, 0])}px)`,
          }}
        >
          Banking on your salary to survive the next decade?
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default MortgageDebtPressure;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'LoadedCylinderRisk',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const LoadedCylinderRisk: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const cylinderRotate = interpolate(frame, [0, 150], [0, 360 * 2], { extrapolateRight: 'clamp' });
  const shake = frame > 155 && frame < 165 ? seededRandom(frame) * 14 - 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #FFB347 0%, #FF9E20 55%, #C9700A 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', top: -180, left: -180, width: 500, height: 500, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <circle key={i} cx="960" cy="540" r={100 + i * 80} fill="none" stroke="#1D2128" strokeWidth="1.5" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const seed = i * 22.2;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 5) % 170, [0, 170], [1080, -50]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.13 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', transform: `translateX(${shake}px)` }}>
        {/* Loaded cylinder - abstract chamber ring */}
        <svg
          width="360"
          height="360"
          viewBox="0 0 100 100"
          style={{ transform: `rotate(${cylinderRotate}deg) scale(${sp(0)})`, opacity: sp(0) }}
        >
          <circle cx="50" cy="50" r="42" fill="none" stroke="#1D2128" strokeWidth="4" />
          {Array.from({ length: 6 }).map((_, i) => {
            const angle = (i / 6) * Math.PI * 2;
            const cx = 50 + Math.cos(angle) * 28;
            const cy = 50 + Math.sin(angle) * 28;
            return <circle key={i} cx={cx} cy={cy} r="9" fill="#1D2128" opacity="0.85" />;
          })}
          <circle cx="50" cy="50" r="6" fill="#1D2128" />
        </svg>

        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 108,
            color: '#1D2128',
            fontWeight: 900,
            marginTop: 40,
            textAlign: 'center',
            opacity: sp(60),
            transform: `translateY(${interpolate(sp(60), [0, 1], [40, 0])}px)`,
          }}
        >
          FULLY LOADED CYLINDER
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 40,
            color: '#F4F2F2',
            backgroundColor: '#1D2128',
            padding: '16px 34px',
            borderRadius: 14,
            marginTop: 24,
            fontWeight: 600,
            maxWidth: 1250,
            textAlign: 'center',
            opacity: sp(90),
          }}
        >
          CEOs stay silent — no bragging, no scrutiny, no PR disaster.
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 50%, rgba(29,33,40,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default LoadedCylinderRisk;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CorporateCodeWords',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CorporateCodeWords: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const phrases = [
    'DRIVING OPERATIONAL EFFICIENCY',
    'FLATTENING THE ORGANIZATION',
    'RESTRUCTURING FOR AGILITY',
  ];
  const strike1 = interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike2 = interpolate(frame, [75, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike3 = interpolate(frame, [110, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strikes = [strike1, strike2, strike3];

  const aiReveal = sp(150);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #FFC670 0%, #FF9E20 50%, #E8830B 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', bottom: -150, right: -150, width: 480, height: 480, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={i} x1={i * 140} y1="0" x2={i * 140} y2="1080" stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const seed = i * 12.9;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 6) % 180, [0, 180], [1080, -60]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.13 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'flex-start', padding: '0 130px' }}>
        <div style={{ fontFamily: 'Inter', fontSize: 38, color: '#1D2128', fontWeight: 700, marginBottom: 30, opacity: sp(0) }}>
          THE CORPORATE CODE WORDS:
        </div>
        {phrases.map((p, i) => (
          <div key={i} style={{ position: 'relative', marginBottom: 28, opacity: sp(i * 20) }}>
            <div style={{ fontFamily: 'Mont Heavy', fontSize: 68, color: '#1D2128', fontWeight: 900 }}>
              {p}
            </div>
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 6,
                backgroundColor: '#1D2128',
                width: `${strikes[i] * 100}%`,
                transform: 'translateY(-50%)',
              }}
            />
          </div>
        ))}

        <div
          style={{
            fontFamily: 'Mont Heavy',
            fontSize: 130,
            color: '#F4F2F2',
            fontWeight: 900,
            backgroundColor: '#1D2128',
            padding: '20px 50px',
            borderRadius: 18,
            marginTop: 20,
            opacity: aiReveal,
            transform: `scale(${aiReveal})`,
          }}
        >
          IT'S ALL AI
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default CorporateCodeWords;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'CorporateLadderIntact',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CorporateLadderIntact: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rungCount = 6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 80% 20%, #FFC670 0%, #FF9E20 55%, #E8830B 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', top: 60, left: -160, width: 420, height: 420, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [0, 20])}deg)`,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 108} x2="1920" y2={i * 108} stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const seed = i * 18.8;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 7) % 175, [0, 175], [1080, -60]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.13 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ flexDirection: 'row', alignItems: 'center', justifyContent: 'center', paddingLeft: 100 }}>
        {/* Ladder graphic - all rungs present, climbing dot */}
        <div style={{ position: 'relative', width: 300, height: 700, marginRight: 100 }}>
          <div style={{ position: 'absolute', left: 30, top: 0, width: 8, height: 700, backgroundColor: '#1D2128', opacity: 0.85 }} />
          <div style={{ position: 'absolute', right: 30, top: 0, width: 8, height: 700, backgroundColor: '#1D2128', opacity: 0.85 }} />
          {Array.from({ length: rungCount }).map((_, i) => {
            const rungSp = sp(i * 15);
            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: i * (700 / (rungCount - 1)),
                  left: 30,
                  width: 240,
                  height: 8,
                  backgroundColor: '#1D2128',
                  opacity: rungSp * 0.85,
                  transform: `scaleX(${rungSp})`,
                }}
              />
            );
          })}
          {/* climbing marker */}
          <div
            style={{
              position: 'absolute',
              left: 110,
              top: interpolate(frame, [80, durationInFrames - 20], [680, 20], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              width: 50,
              height: 50,
              borderRadius: '50%',
              backgroundColor: '#1D2128',
            }}
          />
        </div>

        <div style={{ maxWidth: 900 }}>
          <div
            style={{
              fontFamily: 'Mont Heavy',
              fontSize: 100,
              color: '#1D2128',
              fontWeight: 900,
              opacity: sp(20),
              transform: `translateX(${interpolate(sp(20), [0, 1], [60, 0])}px)`,
            }}
          >
            THE TRAINING GROUND
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              color: '#1D2128',
              marginTop: 26,
              fontWeight: 600,
              opacity: sp(50),
              transform: `translateX(${interpolate(sp(50), [0, 1], [50, 0])}px)`,
            }}
          >
            Learn the ropes. Make mistakes. Rise to leadership.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default CorporateLadderIntact;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'LadderMissingRungs',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const LadderMissingRungs: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rungCount = 6;
  // bottom 2 rungs disappear
  const rungGone = (i: number) => i >= rungCount - 2;
  const disappear = interpolate(frame, [30, 80], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame > 28 && frame < 40 ? seededRandom(frame) * 10 - 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #FFB84D 0%, #FF9E20 55%, #D97706 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', bottom: -200, left: -150, width: 500, height: 500, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 108} x2="1920" y2={i * 108} stroke="#1D2128" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const seed = i * 19.1;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 7) % 175, [0, 175], [1080, -60]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.13 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ flexDirection: 'row', alignItems: 'center', justifyContent: 'center', paddingLeft: 100, transform: `translateX(${shake}px)` }}>
        <div style={{ position: 'relative', width: 300, height: 700, marginRight: 100 }}>
          <div style={{ position: 'absolute', left: 30, top: 0, width: 8, height: 700, backgroundColor: '#1D2128', opacity: 0.85 }} />
          <div style={{ position: 'absolute', right: 30, top: 0, width: 8, height: 700, backgroundColor: '#1D2128', opacity: 0.85 }} />
          {Array.from({ length: rungCount }).map((_, i) => {
            const isGone = rungGone(i);
            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: i * (700 / (rungCount - 1)),
                  left: 30,
                  width: 240,
                  height: 8,
                  backgroundColor: '#1D2128',
                  opacity: isGone ? 0.85 * disappear : 0.85,
                }}
              />
            );
          })}
          {/* stranded figure-less marker stuck at gap */}
          <div
            style={{
              position: 'absolute',
              left: 110,
              top: 550,
              width: 50,
              height: 50,
              borderRadius: '50%',
              border: '4px solid #1D2128',
              opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          />
        </div>

        <div style={{ maxWidth: 950 }}>
          <div
            style={{
              fontFamily: 'Mont Heavy',
              fontSize: 96,
              color: '#1D2128',
              fontWeight: 900,
              opacity: sp(60),
              transform: `translateX(${interpolate(sp(60), [0, 1], [60, 0])}px)`,
            }}
          >
            BOTTOM RUNGS: GONE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              color: '#1D2128',
              marginTop: 26,
              fontWeight: 600,
              opacity: sp(90),
            }}
          >
            How does a 22-year-old break in now?
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              color: '#F4F2F2',
              backgroundColor: '#1D2128',
              padding: '16px 30px',
              borderRadius: 14,
              marginTop: 24,
              fontWeight: 500,
              opacity: sp(120),
            }}
          >
            This isn't the future. This is now.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default LadderMissingRungs;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'JensenCodingWarning',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const JensenCodingWarning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const needleAngle = interpolate(frame, [0, 150], [-90, 70], { extrapolateRight: 'clamp' });
  const crackProgress = interpolate(frame, [130, 190], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF9E20', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 60% 60%, #FFC670 0%, #FF9E20 55%, #E8830B 100%)' }} />

      {/* Shape Layers */}
      <div
        style={{
          position: 'absolute', top: -180, left: -180, width: 520, height: 520, borderRadius: '50%',
          border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${interpolate(frame, [0, durationInFrames], [0, 15])}deg)`,
        }}
      />

      {/* Motion Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <rect key={i} x={i * 240} y="0" width="2" height="1080" fill="#1D2128" />
        ))}
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const seed = i * 14.4;
        const px = seededRandom(seed) * 1920;
        const py = interpolate((frame + seed * 6) % 175, [0, 175], [1080, -60]);
        return (
          <div key={i} style={{ position: 'absolute', left: px, top: py, width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1D2128', opacity: 0.13 }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', flexDirection: 'column' }}>
        {/* Speedometer - speed of transition */}
        <svg width="300" height="180" viewBox="0 0 200 120" style={{ opacity: sp(0) }}>
          <path d="M20 100 A80 80 0 0 1 180 100" fill="none" stroke="#1D2128" strokeWidth="8" opacity="0.3" />
          <line
            x1="100" y1="100"
            x2={100 + 70 * Math.cos((needleAngle * Math.PI) / 180)}
            y2={100 + 70 * Math.sin((needleAngle * Math.PI) / 180)}
            stroke="#1D2128" strokeWidth="6"
          />
          <circle cx="100" cy="100" r="8" fill="#1D2128" />
        </svg>
        <div style={{ fontFamily: 'Inter', fontSize: 34, color: '#1D2128', fontWeight: 700, marginTop: -10 }}>
          SPEED OF TRANSITION
        </div>

        <div style={{ position: 'relative', marginTop: 40 }}>
          <div
            style={{
              fontFamily: 'Mont Heavy',
              fontSize: 140,
              color: '#1D2128',
              fontWeight: 900,
              opacity: sp(60),
              transform: `scale(${sp(60)})`,
            }}
          >
            {'</>'}
          </div>
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: '100%',
              height: 8,
              backgroundColor: '#1D2128',
              transform: `translateY(-50%) rotate(-8deg) scaleX(${crackProgress})`,
              transformOrigin: 'left',
            }}
          />
        </div>

        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 46,
            color: '#1D2128',
            marginTop: 30,
            fontWeight: 600,
            textAlign: 'center',
            opacity: sp(100),
          }}
        >
          "Coding as a career might be dead."
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            color: '#F4F2F2',
            backgroundColor: '#1D2128',
            padding: '14px 32px',
            borderRadius: 14,
            marginTop: 22,
            fontWeight: 500,
            opacity: sp(130),
          }}
        >
          — Jensen Huang, NVIDIA
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle, transparent 55%, rgba(29,33,40,0.45) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default JensenCodingWarning;
```

