```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S1_StoryIntro',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S1_StoryIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Curtain reveal effect
  const curtainLeft = interpolate(frame, [0, 35], [0, -960], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const curtainRight = interpolate(frame, [0, 35], [0, 960], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bookOpen = sp(20, { damping: 14, stiffness: 80 });
  const titleDrop = sp(38, { damping: 10, stiffness: 100 });
  const subFade = interpolate(frame, [55, 75], [0, 1], {
    easing: Easing.out(Easing.quad),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const lineWidth = interpolate(frame, [60, 90], [0, 500], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Floating ink drops
  const inkParticles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: seededRandom(i * 3 + 3) * 8 + 3,
    speed: seededRandom(i * 5 + 4) * 0.03 + 0.01,
    phase: seededRandom(i * 7 + 5) * Math.PI * 2,
  }));

  // Slow drift geometric shapes
  const shapeRotate = frame * 0.08;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep cinematic gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, #2a1a0a 0%, #1C1C1C 55%, #0a0a0a 100%)',
      }} />

      {/* Warm amber vein light */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 700px 400px at 25% 55%, rgba(233,215,0,0.06) 0%, transparent 70%)',
      }} />

      {/* Geometric shape layers */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Large rotating hexagon */}
          <polygon
            points="960,200 1100,275 1100,425 960,500 820,425 820,275"
            fill="none"
            stroke="rgba(233,215,0,0.04)"
            strokeWidth="1.5"
            transform={`rotate(${shapeRotate}, 960, 350)`}
          />
          {/* Drifting rectangle */}
          <rect
            x={80 + Math.sin(frame * 0.012) * 20}
            y={150 + Math.cos(frame * 0.009) * 15}
            width="220" height="140"
            fill="none"
            stroke="rgba(245,245,245,0.03)"
            strokeWidth="1"
            rx="4"
          />
          {/* Bottom triangle */}
          <polygon
            points="1700,900 1820,1060 1580,1060"
            fill="rgba(255,0,0,0.03)"
            transform={`rotate(${-shapeRotate * 0.5}, 1700, 980)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Grid texture lines */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.025 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }, (_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 60} x2="1920" y2={i * 60} stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
          {Array.from({ length: 33 }, (_, i) => (
            <line key={`v${i}`} x1={i * 60} y1="0" x2={i * 60} y2="1080" stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating ink particle dots */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {inkParticles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 18}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 12}
              r={p.r}
              fill={i % 3 === 0 ? 'rgba(233,215,0,0.25)' : i % 3 === 1 ? 'rgba(245,245,245,0.1)' : 'rgba(255,165,0,0.12)'}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* CURTAIN REVEAL — Left panel */}
      <div style={{
        position: 'absolute',
        left: 0, top: 0,
        width: 960, height: 1080,
        background: 'linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%)',
        transform: `translateX(${curtainLeft}px)`,
        zIndex: 50,
      }} />

      {/* CURTAIN REVEAL — Right panel */}
      <div style={{
        position: 'absolute',
        right: 0, top: 0,
        width: 960, height: 1080,
        background: 'linear-gradient(225deg, #0a0a0a 0%, #1a1a1a 100%)',
        transform: `translateX(${curtainRight}px)`,
        zIndex: 50,
      }} />

      {/* Open Book SVG */}
      <div style={{
        position: 'absolute',
        left: 130, top: '50%',
        transform: `translateY(-50%) scaleY(${bookOpen})`,
        transformOrigin: 'center center',
      }}>
        <svg width="320" height="240" viewBox="0 0 320 240">
          {/* Book spine */}
          <rect x="155" y="20" width="10" height="200" fill="#e9d700" rx="2" />
          {/* Left page */}
          <rect x="30" y="20" width="125" height="200" fill="#F5F5F5" rx="4 0 0 4" />
          {/* Left page lines */}
          {[60, 80, 100, 120, 140, 160, 180].map((y, i) => (
            <line key={i} x1="50" y1={y} x2="140" y2={y} stroke="#ccc" strokeWidth="1" />
          ))}
          <line x1="50" y1="45" x2="140" y2="45" stroke="#e9d700" strokeWidth="2.5" />
          {/* Right page */}
          <rect x="165" y="20" width="125" height="200" fill="#f0f0f0" rx="0 4 4 0" />
          {[60, 80, 100, 120, 140, 160, 180].map((y, i) => (
            <line key={i} x1="175" y1={y} x2="270" y2={y} stroke="#ccc" strokeWidth="1" />
          ))}
          {/* Red bookmark */}
          <polygon points="260,20 275,20 275,60 267,50 260,60" fill="#ff0000" />
          {/* Quill pen */}
          <line x1="200" y1="45" x2="250" y2="45" stroke="#e9d700" strokeWidth="2" />
          <polygon points="200,40 210,45 200,50" fill="#e9d700" />
        </svg>
      </div>

      {/* Main story headline */}
      <div style={{
        position: 'absolute',
        left: 520, top: '50%',
        transform: `translateY(calc(-50% + ${(1 - titleDrop) * 80}px))`,
        opacity: titleDrop,
      }}>
        {/* Label */}
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 8,
          textTransform: 'uppercase',
          marginBottom: 16,
        }}>
          ● एक असली कहानी
        </div>

        {/* Giant title */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.05,
          letterSpacing: -2,
        }}>
          एक कहानी
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1.05,
          letterSpacing: -2,
        }}>
          जो याद रही
        </div>

        {/* Animated underline */}
        <div style={{
          marginTop: 20,
          height: 4,
          width: lineWidth,
          background: 'linear-gradient(90deg, #e9d700, #ff0000)',
          borderRadius: 2,
        }} />

        {/* Sub text */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          fontWeight: 400,
          color: '#9E9E9E',
          marginTop: 22,
          opacity: subFade,
        }}>
          क्योंकि यह असली है...
        </div>
      </div>

      {/* Stick Figure — Storyteller pose */}
      <div style={{
        position: 'absolute',
        right: 80, bottom: 60,
        transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
        opacity: sp(50),
      }}>
        <svg width="110" height="175" viewBox="0 0 110 175">
          {/* Golden glow */}
          <circle cx="55" cy="40" r="38" fill="rgba(233,215,0,0.08)" />
          {/* Head */}
          <circle cx="55" cy="32" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
            transform={`rotate(${Math.sin(frame * 0.04) * 4}, 55, 32)`} />
          {/* Glasses */}
          <ellipse cx="47" cy="32" rx="7" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="63" cy="32" rx="7" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="54" y1="32" x2="56" y2="32" stroke="#e9d700" strokeWidth="1.5" />
          {/* Body */}
          <line x1="55" y1="54" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
          {/* Left arm — raised storytelling gesture */}
          <line x1="55" y1="75" x2="22" y2="58" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="22" y1="58" x2="10" y2="42" stroke="#e9d700" strokeWidth="2.5" />
          {/* Right arm */}
          <line x1="55" y1="75" x2="85" y2="90" stroke="#F5F5F5" strokeWidth="3" />
          {/* Legs */}
          <line x1="55" y1="115" x2="38" y2="155" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="55" y1="115" x2="72" y2="155" stroke="#F5F5F5" strokeWidth="3" />
          {/* Speech bubble suggestion */}
          <circle cx="10" cy="38" r="6" fill="rgba(233,215,0,0.4)" />
          <circle cx="18" cy="30" r="4" fill="rgba(233,215,0,0.25)" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S1_StoryIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S2_HaroldIntro',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S2_HaroldIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card slide-in from left
  const cardX = interpolate(frame, [10, 45], [-600, 0], {
    easing: Easing.out(Easing.back(1.2)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nameScale = sp(25, { damping: 8, stiffness: 200, mass: 0.3 });
  const moneyPop = sp(50, { damping: 7, stiffness: 250, mass: 0.25 });
  const ageBadge = sp(40, { damping: 10, stiffness: 150 });

  // Coin particles raining
  const coins = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 4 + 1) * 1400 + 250,
    startY: -50,
    speed: seededRandom(i * 4 + 2) * 1.2 + 0.4,
    size: seededRandom(i * 4 + 3) * 20 + 12,
    phase: seededRandom(i * 4 + 4) * 60,
  }));

  const bgPan = frame * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Warm amber background */}
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #1a1200 0%, #1C1C1C 40%, #0d0d0d 100%)`,
        transform: `translateX(${-bgPan}px)`,
      }} />

      {/* Radial spotlight on Harold */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 800px 600px at 62% 48%, rgba(233,215,0,0.07) 0%, transparent 70%)',
      }} />

      {/* Diagonal stripe texture */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 30 }, (_, i) => (
            <line
              key={i}
              x1={i * 70 - 200} y1="0"
              x2={i * 70 + 400} y2="1080"
              stroke="#F5F5F5" strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Geometric shape layer — large circle drift */}
      <div style={{
        position: 'absolute',
        right: -100 + Math.sin(frame * 0.008) * 30,
        top: 100,
        width: 600, height: 600,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.05)',
      }} />
      <div style={{
        position: 'absolute',
        right: 50,
        top: 200,
        width: 400, height: 400,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.04)',
      }} />

      {/* Raining coins */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {coins.map((c, i) => {
            const y = ((frame * c.speed + c.phase) % 1150) - 50;
            return (
              <g key={i}>
                <ellipse
                  cx={c.x}
                  cy={y}
                  rx={c.size * 0.5}
                  ry={c.size * 0.22}
                  fill="rgba(233,215,0,0.18)"
                />
                <ellipse
                  cx={c.x}
                  cy={y}
                  rx={c.size * 0.5}
                  ry={c.size * 0.22}
                  fill="none"
                  stroke="rgba(233,215,0,0.35)"
                  strokeWidth="1"
                />
              </g>
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Harold Profile Card */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: '50%',
        transform: `translateY(-50%) translateX(${cardX}px)`,
        width: 480,
        background: 'linear-gradient(145deg, #2D2D2D, #1a1a1a)',
        borderRadius: 20,
        border: '1px solid rgba(233,215,0,0.2)',
        padding: '44px 40px',
        boxShadow: '0 0 60px rgba(233,215,0,0.1)',
      }}>
        {/* Harold stick figure */}
        <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 24 }}>
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="30" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            <ellipse cx="37" cy="30" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <ellipse cx="53" cy="30" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="43" y1="30" x2="47" y2="30" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="45" y1="50" x2="45" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="68" x2="18" y2="82" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="68" x2="72" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="95" x2="30" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="95" x2="60" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Suitcase */}
            <rect x="58" y="72" width="20" height="15" fill="none" stroke="#e9d700" strokeWidth="1.5" rx="2" />
            <line x1="62" y1="72" x2="74" y2="72" stroke="#e9d700" strokeWidth="1.5" />
          </svg>
        </div>

        {/* Name */}
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 52,
          fontWeight: 900,
          color: '#F5F5F5',
          textAlign: 'center',
          transform: `scale(${nameScale})`,
          transformOrigin: 'center',
          letterSpacing: 2,
        }}>
          HAROLD
        </div>

        {/* Age badge */}
        <div style={{
          display: 'flex', justifyContent: 'center', marginTop: 14,
          opacity: ageBadge,
          transform: `translateY(${(1 - ageBadge) * 20}px)`,
        }}>
          <div style={{
            background: 'rgba(255,0,0,0.15)',
            border: '1px solid #ff0000',
            borderRadius: 30,
            padding: '8px 28px',
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#ff0000',
            fontWeight: 700,
          }}>
            आयु: 68 वर्ष
          </div>
        </div>

        {/* Divider */}
        <div style={{
          height: 1,
          background: 'linear-gradient(90deg, transparent, rgba(233,215,0,0.4), transparent)',
          margin: '20px 0',
        }} />

        {/* Retired label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 26,
          color: '#9E9E9E',
          textAlign: 'center',
        }}>
          सेवा निवर्त ✓
        </div>
      </div>

      {/* Money amount — BIG RIGHT SIDE */}
      <div style={{
        position: 'absolute',
        right: 140, top: '50%',
        transform: `translateY(-50%) scale(${moneyPop})`,
        transformOrigin: 'center',
        textAlign: 'center',
      }}>
        {/* Rupee symbol */}
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 80,
          fontWeight: 900,
          color: 'rgba(233,215,0,0.4)',
          lineHeight: 1,
        }}>₹</div>

        {/* Amount */}
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 160,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 0.9,
          letterSpacing: -4,
          textShadow: '0 0 80px rgba(233,215,0,0.3)',
        }}>
          23
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 64,
          fontWeight: 700,
          color: '#F5F5F5',
          letterSpacing: 2,
        }}>
          लाख रुपए
        </div>

        {/* Confidence label */}
        <div style={{
          marginTop: 20,
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#9E9E9E',
        }}>
          सब कुछ ठीक लग रहा था...
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S2_HaroldIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S3_Portfolio60',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S3_Portfolio60: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pie chart animation
  const pieProgress = interpolate(frame, [15, 70], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const warningPop = sp(75, { damping: 6, stiffness: 300, mass: 0.2 });
  const labelFade = interpolate(frame, [55, 80], [0, 1], {
    easing: Easing.out(Easing.quad),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Draw pie slices using SVG arc
  const drawArc = (cx: number, cy: number, r: number, startAngle: number, endAngle: number) => {
    const start = { x: cx + r * Math.cos(startAngle), y: cy + r * Math.sin(startAngle) };
    const end = { x: cx + r * Math.cos(endAngle), y: cy + r * Math.sin(endAngle) };
    const large = endAngle - startAngle > Math.PI ? 1 : 0;
    return `M ${cx} ${cy} L ${start.x} ${start.y} A ${r} ${r} 0 ${large} 1 ${end.x} ${end.y} Z`;
  };

  const cx = 340, cy = 500, r = 240;
  const start = -Math.PI / 2;

  // 60% stocks, 25% bonds, 15% cash
  const stockEnd = start + (2 * Math.PI * 0.6 * pieProgress);
  const bondEnd = stockEnd + (2 * Math.PI * 0.25 * pieProgress);
  const cashEnd = bondEnd + (2 * Math.PI * 0.15 * pieProgress);

  // Floating particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    size: seededRandom(i * 3 + 3) * 4 + 1,
    speed: seededRandom(i * 5) * 0.025 + 0.008,
    phase: seededRandom(i * 7) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0d0d1a 0%, #1C1C1C 50%, #1a0d0d 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 1 }}>
        <svg width="1920" height="1080">
          {/* Large background diamond */}
          <polygon
            points="960,50 1400,540 960,1030 520,540"
            fill="none"
            stroke="rgba(255,0,0,0.04)"
            strokeWidth="1"
            transform={`rotate(${frame * 0.04}, 960, 540)`}
          />
          {/* Top-right accent circle */}
          <circle
            cx={1750 + Math.sin(frame * 0.01) * 20}
            cy={180 + Math.cos(frame * 0.008) * 15}
            r="120"
            fill="rgba(255,0,0,0.03)"
          />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 25}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 18}
              r={p.size}
              fill={i % 2 === 0 ? 'rgba(255,0,0,0.15)' : 'rgba(233,215,0,0.1)'}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* PIE CHART */}
      <div style={{ position: 'absolute', left: 80, top: '50%', transform: 'translateY(-50%)' }}>
        <svg width="700" height="700" viewBox="0 0 680 1000">
          {/* Drop shadow */}
          <circle cx={cx} cy={cy} r={r + 8} fill="rgba(0,0,0,0.4)" />

          {/* 60% Stocks — RED danger */}
          <path
            d={drawArc(cx, cy, r, start, stockEnd)}
            fill="#ff0000"
            opacity="0.85"
          />

          {/* 25% Bonds */}
          <path
            d={drawArc(cx, cy, r, stockEnd, bondEnd)}
            fill="#9E9E9E"
            opacity="0.7"
          />

          {/* 15% Cash */}
          <path
            d={drawArc(cx, cy, r, bondEnd, cashEnd)}
            fill="#e9d700"
            opacity="0.8"
          />

          {/* Center donut hole */}
          <circle cx={cx} cy={cy} r={110} fill="#1C1C1C" />
          <text x={cx} y={cy - 15} textAnchor="middle" fill="#F5F5F5" fontSize="40" fontFamily="Inter">
            Portfolio
          </text>
          <text x={cx} y={cy + 35} textAnchor="middle" fill="#9E9E9E" fontSize="28" fontFamily="Noto Sans Devanagari">
            विभाजन
          </text>

          {/* Legend */}
          <rect x="50" y="760" width="22" height="22" fill="#ff0000" rx="3" />
          <text x="84" y="778" fill="#F5F5F5" fontSize="30" fontFamily="Noto Sans Devanagari" opacity={labelFade}>
            शेयर — 60%
          </text>

          <rect x="50" y="810" width="22" height="22" fill="#9E9E9E" rx="3" />
          <text x="84" y="828" fill="#F5F5F5" fontSize="30" fontFamily="Noto Sans Devanagari" opacity={labelFade}>
            बॉंड — 25%
          </text>

          <rect x="50" y="860" width="22" height="22" fill="#e9d700" rx="3" />
          <text x="84" y="878" fill="#F5F5F5" fontSize="30" fontFamily="Noto Sans Devanagari" opacity={labelFade}>
            नकद — 15%
          </text>
        </svg>
      </div>

      {/* Right side — Alert content */}
      <div style={{
        position: 'absolute',
        right: 100, top: '50%',
        transform: 'translateY(-50%)',
        width: 800,
      }}>

        {/* Advisor says label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 34,
          color: '#9E9E9E',
          marginBottom: 16,
          opacity: sp(8),
        }}>
          सलाहकार ने कहा:
        </div>

        {/* Quote */}
        <div style={{
          background: 'linear-gradient(135deg, rgba(45,45,45,0.8), rgba(30,30,30,0.8))',
          border: '1px solid rgba(233,215,0,0.2)',
          borderRadius: 16,
          padding: '32px 40px',
          marginBottom: 36,
          opacity: sp(12),
          transform: `translateX(${(1 - sp(12)) * 60}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 48,
            fontWeight: 700,
            color: '#e9d700',
            lineHeight: 1.3,
          }}>
            "यह संतुलित है"
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 34,
            color: '#9E9E9E',
            marginTop: 8,
          }}>
            — लेकिन असल में...
          </div>
        </div>

        {/* BIG WARNING */}
        <div style={{
          transform: `scale(${warningPop})`,
          transformOrigin: 'left center',
        }}>
          <div style={{
            background: 'linear-gradient(135deg, rgba(255,0,0,0.2), rgba(255,0,0,0.08))',
            border: '2px solid rgba(255,0,0,0.6)',
            borderRadius: 16,
            padding: '28px 40px',
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}>
            {/* Warning SVG icon */}
            <svg width="60" height="60" viewBox="0 0 60 60">
              <polygon points="30,5 55,52 5,52" fill="rgba(255,0,0,0.3)" stroke="#ff0000" strokeWidth="2" />
              <text x="30" y="44" textAnchor="middle" fill="#ff0000" fontSize="28" fontWeight="900">!</text>
            </svg>
            <div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 80,
                fontWeight: 900,
                color: '#ff0000',
                lineHeight: 1,
              }}>60%</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 38,
                color: '#F5F5F5',
                fontWeight: 700,
              }}>
                शेयरों में — 70 साल में!
              </div>
            </div>
          </div>
        </div>

        {/* Sub caption */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 34,
          color: '#9E9E9E',
          marginTop: 24,
          opacity: labelFade,
        }}>
          सत्तर की उम्र के लिए बहुत आक्रामक था
        </div>
      </div>

      {/* Stick Figure — worried advisor pose */}
      <div style={{
        position: 'absolute',
        left: 680, bottom: 50,
        transform: `translateY(${Math.sin(frame * 0.06) * 3}px)`,
        opacity: sp(55),
      }}>
        <svg width="85" height="140" viewBox="0 0 85 140">
          <circle cx="42" cy="28" r="18" fill="none" stroke="#9E9E9E" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 42, 28)`} />
          <ellipse cx="35" cy="28" rx="5" ry="4" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <ellipse cx="49" cy="28" rx="5" ry="4" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="40" y1="28" x2="44" y2="28" stroke="#e9d700" strokeWidth="1.5" />
          {/* Shrug arms */}
          <line x1="42" y1="46" x2="42" y2="95" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="42" y1="65" x2="14" y2="55" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="42" y1="65" x2="70" y2="55" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="42" y1="95" x2="28" y2="130" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="42" y1="95" x2="56" y2="130" stroke="#9E9E9E" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S3_Portfolio60;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S4_Crash2008',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S4_Crash2008: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on crash
  const shakeX = frame > 20 && frame < 40
    ? Math.sin(frame * 1.8) * (8 - (frame - 20) * 0.4)
    : 0;
  const shakeY = frame > 20 && frame < 40
    ? Math.cos(frame * 2.1) * (6 - (frame - 20) * 0.3)
    : 0;

  // Year 2008 smash-in
  const yearScale = sp(5, { damping: 6, stiffness: 400, mass: 0.3 });
  const yearOpacity = interpolate(frame, [3, 10], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart crash line data
  const chartProgress = interpolate(frame, [18, 80], [0, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const crashAmount = sp(55, { damping: 10, stiffness: 180 });
  const remainAmount = sp(72, { damping: 10, stiffness: 180 });

  // Chart points: starts high, drops 40%
  const chartW = 700, chartH = 340;
  const chartPoints = [
    { t: 0, v: 1.0 },
    { t: 0.15, v: 1.05 },
    { t: 0.3, v: 0.98 },
    { t: 0.5, v: 0.95 },
    { t: 0.65, v: 0.72 },
    { t: 0.8, v: 0.58 },
    { t: 1.0, v: 0.60 },
  ];

  const getChartY = (v: number) => chartH - v * chartH * 0.82;
  const getChartX = (t: number) => t * chartW;

  const visiblePoints = chartPoints.filter(p => p.t <= chartProgress);
  if (visiblePoints.length > 0 && chartProgress < 1) {
    const last = chartPoints.find(p => p.t > chartProgress);
    const prev = [...chartPoints].reverse().find(p => p.t <= chartProgress);
    if (last && prev) {
      const interp = (chartProgress - prev.t) / (last.t - prev.t);
      visiblePoints.push({ t: chartProgress, v: prev.v + (last.v - prev.v) * interp });
    }
  }

  const polyline = visiblePoints
    .map(p => `${getChartX(p.t)},${getChartY(p.v)}`)
    .join(' ');

  // Flying debris particles
  const debris = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    speed: seededRandom(i * 3 + 3) * 2.5 + 0.8,
    size: seededRandom(i * 5 + 4) * 6 + 2,
    phase: seededRandom(i * 7 + 5) * 60,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#0d0000',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${shakeX}px, ${shakeY}px)`,
    }}>

      {/* Blood red dramatic background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, #1a0000 0%, #0d0000 60%, #000000 100%)',
      }} />

      {/* Red pulse glow */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 900px 500px at 50% 50%, rgba(255,0,0,${0.04 + Math.sin(frame * 0.15) * 0.03}) 0%, transparent 70%)`,
      }} />

      {/* Shape layers — cracking rectangles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <rect x="50" y="50" width="300" height="200"
            fill="none" stroke="rgba(255,0,0,0.06)" strokeWidth="1"
            transform={`rotate(${frame * 0.06}, 200, 150)`}
          />
          <rect x="1550" y="700" width="250" height="180"
            fill="none" stroke="rgba(255,0,0,0.05)" strokeWidth="1"
            transform={`rotate(${-frame * 0.04}, 1675, 790)`}
          />
          {/* Diagonal crack lines */}
          <line x1="800" y1="0" x2="1100" y2="1080"
            stroke="rgba(255,0,0,0.06)" strokeWidth="1"
            opacity={interpolate(frame, [20, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />
        </svg>
      </AbsoluteFill>

      {/* Debris particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {debris.map((d, i) => {
            const activeFrame = Math.max(0, frame - 20);
            const y = (d.y + activeFrame * d.speed + d.phase) % 1100;
            return (
              <rect
                key={i}
                x={d.x}
                y={y}
                width={d.size}
                height={d.size * 0.4}
                fill="rgba(255,0,0,0.2)"
                transform={`rotate(${frame * 2 + i * 15}, ${d.x + d.size / 2}, ${y + d.size * 0.2})`}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* 2008 — Big crash year */}
      <div style={{
        position: 'absolute',
        top: 60, left: '50%',
        transform: `translateX(-50%) scale(${yearScale})`,
        opacity: yearOpacity,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 130,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: 8,
          textShadow: '0 0 60px rgba(255,0,0,0.6)',
          lineHeight: 1,
        }}>2008</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          color: '#F5F5F5',
          letterSpacing: 4,
          marginTop: -8,
        }}>
          वित्तीय संकट
        </div>
      </div>

      {/* CRASH CHART */}
      <div style={{
        position: 'absolute',
        left: 100, top: 300,
      }}>
        <svg width="750" height="400" viewBox={`0 0 ${chartW} ${chartH + 60}`}>
          {/* Grid */}
          {[0.25, 0.5, 0.75, 1.0].map((v, i) => (
            <line
              key={i}
              x1="0" y1={getChartY(v)}
              x2={chartW} y2={getChartY(v)}
              stroke="rgba(255,255,255,0.06)"
              strokeWidth="1"
            />
          ))}
          {/* Axes */}
          <line x1="0" y1={chartH} x2={chartW} y2={chartH} stroke="rgba(255,255,255,0.2)" strokeWidth="1.5" />
          <line x1="0" y1="0" x2="0" y2={chartH} stroke="rgba(255,255,255,0.2)" strokeWidth="1.5" />

          {/* Area fill */}
          {visiblePoints.length > 1 && (
            <polygon
              points={`0,${chartH} ${polyline} ${getChartX(visiblePoints[visiblePoints.length - 1].t)},${chartH}`}
              fill="rgba(255,0,0,0.12)"
            />
          )}

          {/* Crash line */}
          {visiblePoints.length > 1 && (
            <polyline
              points={polyline}
              fill="none"
              stroke={chartProgress > 0.5 ? '#ff0000' : '#e9d700'}
              strokeWidth="4"
              strokeLinejoin="round"
            />
          )}

          {/* Arrow down at crash */}
          {chartProgress > 0.7 && (
            <g>
              <text x={getChartX(0.85)} y={getChartY(0.58) - 12}
                fill="#ff0000" fontSize="28" textAnchor="middle">▼ -40%</text>
            </g>
          )}

          {/* X-axis label */}
          <text x={chartW * 0.5} y={chartH + 40} fill="#9E9E9E" fontSize="20" textAnchor="middle">
            2006 → 2008 → 2009
          </text>
        </svg>
      </div>

      {/* Money transformation — RIGHT SIDE */}
      <div style={{
        position: 'absolute',
        right: 120, top: '50%',
        transform: 'translateY(-40%)',
        textAlign: 'right',
      }}>
        {/* Was */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#9E9E9E',
          opacity: sp(20),
        }}>था:</div>

        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 110,
          fontWeight: 900,
          color: '#9E9E9E',
          textDecoration: 'line-through',
          textDecorationColor: '#ff0000',
          opacity: sp(25),
        }}>
          ₹23L
        </div>

        {/* Arrow down */}
        <div style={{ opacity: crashAmount, transform: `scale(${crashAmount})` }}>
          <svg width="80" height="60" style={{ display: 'block', marginLeft: 'auto' }}>
            <line x1="40" y1="5" x2="40" y2="45" stroke="#ff0000" strokeWidth="5" />
            <polygon points="25,35 40,58 55,35" fill="#ff0000" />
          </svg>
        </div>

        {/* Lost label */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#ff0000',
          opacity: crashAmount,
          marginBottom: 8,
        }}>40% खो गया!</div>

        {/* Now */}
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 130,
          fontWeight: 900,
          color: '#ff0000',
          textShadow: '0 0 50px rgba(255,0,0,0.4)',
          opacity: remainAmount,
          transform: `scale(${remainAmount})`,
          transformOrigin: 'right center',
        }}>
          ₹14L
        </div>

        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#F5F5F5',
          opacity: remainAmount,
        }}>
          72 साल की उम्र में
        </div>
      </div>

      {/* Stick Figure — panic pose */}
      <div style={{
        position: 'absolute',
        left: 860, bottom: 60,
        transform: `translateY(${Math.sin(frame * 0.12) * 4}px) rotate(${Math.sin(frame * 0.08) * 4}deg)`,
        opacity: sp(45),
      }}>
        <svg width="100" height="160" viewBox="0 0 100 160">
          <circle cx="50" cy="28" r="20" fill="none" stroke="#ff0000" strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.12) * 8}, 50, 28)`} />
          <ellipse cx="42" cy="27" rx="6" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="58" cy="27" rx="6" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
          {/* Mouth open in panic */}
          <ellipse cx="50" cy="36" rx="5" ry="3" fill="rgba(255,0,0,0.5)" />
          <line x1="50" y1="48" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms up panic */}
          <line x1="50" y1="68" x2="18" y2="48" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="68" x2="82" y2="48" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="32" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="68" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S4_Crash2008;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S5_PanicSell',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S5_PanicSell: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stamp animation — SOLD OUT
  const stampScale = sp(30, { damping: 5, stiffness: 500, mass: 0.2 });
  const stampRotate = interpolate(frame, [28, 35], [25, -4], {
    easing: Easing.out(Easing.bounce),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Market recovery line
  const recoveryProgress = interpolate(frame, [50, 100], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // SOLD label
  const soldFade = interpolate(frame, [25, 38], [0, 1], {
    easing: Easing.out(Easing.quad),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Floating money-loss particles
  const moneyParticles = Array.from({ length: 16 }, (_, i) => ({
    x: 200 + seededRandom(i * 3 + 1) * 500,
    y: 400 + seededRandom(i * 3 + 2) * 300,
    speed: seededRandom(i * 3 + 3) * 1.5 + 0.5,
    phase: seededRandom(i * 7) * 80,
  }));

  // Mini market chart
  const chartW = 700, chartH = 280;
  const crashThenRecover = [
    { t: 0, v: 0.9 },
    { t: 0.2, v: 0.95 },
    { t: 0.45, v: 0.5 },  // crash bottom
    { t: 0.7, v: 0.65 },
    { t: 0.85, v: 0.8 },
    { t: 1.0, v: 0.92 },  // recovery
  ];

  const getY = (v: number) => chartH - v * chartH * 0.9;
  const getX = (t: number) => t * chartW;

  const visiblePts = crashThenRecover.filter(p => p.t <= recoveryProgress);
  if (visiblePts.length > 0 && recoveryProgress < 1) {
    const last = crashThenRecover.find(p => p.t > recoveryProgress);
    const prev = [...crashThenRecover].reverse().find(p => p.t <= recoveryProgress);
    if (last && prev) {
      const interp2 = (recoveryProgress - prev.t) / (last.t - prev.t);
      visiblePts.push({ t: recoveryProgress, v: prev.v + (last.v - prev.v) * interp2 });
    }
  }
  const poly = visiblePts.map(p => `${getX(p.t)},${getY(p.v)}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Dark orange-tinted background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(145deg, #120800 0%, #1C1C1C 45%, #0d0d0d 100%)',
      }} />

      {/* Large circle shape layer */}
      <div style={{
        position: 'absolute',
        left: -150 + Math.cos(frame * 0.007) * 20,
        top: -150 + Math.sin(frame * 0.009) * 15,
        width: 700, height: 700,
        borderRadius: '50%',
        border: '1px solid rgba(255,100,0,0.05)',
      }} />

      {/* Floating money-down particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {moneyParticles.map((p, i) => {
            const y = (p.y + (frame * p.speed + p.phase)) % 800;
            return (
              <text
                key={i}
                x={p.x}
                y={y}
                fill="rgba(255,0,0,0.2)"
                fontSize="24"
                fontFamily="Inter"
              >↓₹</text>
            );
          })}
          {/* Background dot texture */}
          {Array.from({ length: 40 }, (_, i) => (
            <circle
              key={`d${i}`}
              cx={seededRandom(i * 5) * 1920}
              cy={seededRandom(i * 5 + 1) * 1080}
              r={seededRandom(i * 5 + 2) * 2 + 1}
              fill="rgba(255,255,255,0.04)"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left side — PANIC SELL visual */}
      <div style={{
        position: 'absolute',
        left: 100, top: '50%',
        transform: 'translateY(-50%)',
        width: 680,
      }}>
        {/* Card */}
        <div style={{
          background: 'linear-gradient(135deg, #1a1a1a, #2D2D2D)',
          border: '1px solid rgba(255,80,0,0.3)',
          borderRadius: 20,
          padding: '40px 44px',
          position: 'relative',
          overflow: 'hidden',
        }}>
          {/* SOLD stamp */}
          <div style={{
            position: 'absolute',
            top: 30, right: 30,
            transform: `scale(${stampScale}) rotate(${stampRotate}deg)`,
            transformOrigin: 'center center',
            opacity: soldFade,
          }}>
            <div style={{
              border: '5px solid #ff0000',
              borderRadius: 8,
              padding: '10px 20px',
              color: '#ff0000',
              fontFamily: 'Inter, sans-serif',
              fontSize: 54,
              fontWeight: 900,
              letterSpacing: 4,
              opacity: 0.85,
              textShadow: '0 0 20px rgba(255,0,0,0.4)',
            }}>SOLD</div>
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 38,
            color: '#9E9E9E',
            marginBottom: 16,
            opacity: sp(5),
          }}>हरोल्ड का फैसला:</div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1.15,
            opacity: sp(10),
          }}>
            घबराहट में
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.15,
            opacity: sp(18),
          }}>
            सब बेच दिया
          </div>

          <div style={{
            height: 3,
            background: 'linear-gradient(90deg, #ff0000, transparent)',
            margin: '24px 0',
            opacity: sp(25),
            width: interpolate(frame, [22, 50], [0, 400], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            }),
          }} />

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 40,
            color: '#9E9E9E',
            opacity: sp(30),
          }}>
            ...बिल्कुल नीचे की कीमत पर
          </div>
        </div>
      </div>

      {/* Right side — Recovery chart Harold missed */}
      <div style={{
        position: 'absolute',
        right: 80, top: '50%',
        transform: 'translateY(-50%)',
        width: 800,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#9E9E9E',
          marginBottom: 20,
          opacity: sp(42),
        }}>
          बाजार ठीक हो गया — लेकिन हरोल्ड...
        </div>

        <svg width="730" height="340" viewBox={`0 0 ${chartW} ${chartH + 60}`}>
          {/* Grid */}
          {[0.5, 0.75, 1.0].map((v, i) => (
            <line key={i} x1="0" y1={getY(v)} x2={chartW} y2={getY(v)}
              stroke="rgba(255,255,255,0.05)" strokeWidth="1" />
          ))}

          {/* Area */}
          {visiblePts.length > 1 && (
            <polygon
              points={`0,${chartH} ${poly} ${getX(visiblePts[visiblePts.length - 1].t)},${chartH}`}
              fill={recoveryProgress > 0.5 ? 'rgba(233,215,0,0.08)' : 'rgba(255,0,0,0.1)'}
            />
          )}

          {/* Line */}
          {visiblePts.length > 1 && (
            <polyline
              points={poly}
              fill="none"
              stroke={recoveryProgress > 0.5 ? '#e9d700' : '#ff0000'}
              strokeWidth="4"
            />
          )}

          {/* HAROLD SOLD marker */}
          {recoveryProgress > 0.4 && (
            <g>
              <line x1={getX(0.45)} y1="0" x2={getX(0.45)} y2={chartH}
                stroke="rgba(255,0,0,0.5)" strokeWidth="2" strokeDasharray="6,4" />
              <text x={getX(0.45) + 8} y="30"
                fill="#ff0000" fontSize="22" fontFamily="Noto Sans Devanagari">
                ← यहाँ बेचा
              </text>
            </g>
          )}

          {/* Recovery label */}
          {recoveryProgress > 0.85 && (
            <text x={getX(0.9)} y={getY(0.85) - 15}
              fill="#e9d700" fontSize="24" fontFamily="Noto Sans Devanagari">
              ठीक हुआ ✓
            </text>
          )}

          <text x={chartW * 0.5} y={chartH + 40}
            fill="#9E9E9E" fontSize="20" textAnchor="middle" fontFamily="Inter">
            2008 → 2009 → 2010 → 2011
          </text>
        </svg>

        {/* Missed gains */}
        <div style={{
          marginTop: 20,
          background: 'rgba(233,215,0,0.08)',
          border: '1px solid rgba(233,215,0,0.3)',
          borderRadius: 12,
          padding: '18px 28px',
          opacity: interpolate(frame, [95, 115], [0, 1], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          }),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 38,
            color: '#e9d700',
            fontWeight: 700,
          }}>
            ⚠ पुनः निवेश नहीं कर सका
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 30,
            color: '#9E9E9E',
            marginTop: 6,
          }}>
            रिकवरी का फायदा उठाने का मौका खो दिया
          </div>
        </div>
      </div>

      {/* Stick Figure — head in hands */}
      <div style={{
        position: 'absolute',
        left: 750, bottom: 50,
        opacity: sp(48),
        transform: `translateY(${Math.sin(frame * 0.07) * 2}px)`,
      }}>
        <svg width="95" height="155" viewBox="0 0 95 155">
          <circle cx="47" cy="28" r="20" fill="none" stroke="#ff0000" strokeWidth="2.5" />
          <ellipse cx="39" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="55" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          {/* Sad mouth */}
          <path d="M 40 36 Q 47 32 54 36" fill="none" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="47" y1="48" x2="47" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms down defeated */}
          <line x1="47" y1="70" x2="22" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="47" y1="70" x2="72" y2="88" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="47" y1="100" x2="30" y2="138" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="47" y1="100" x2="64" y2="138" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Sweat drop */}
          <ellipse cx="68" cy="22" rx="4" ry="6" fill="rgba(100,150,255,0.4)" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S5_PanicSell;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S6_HaroldFear',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S6_HaroldFear: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Lost dreams items stagger
  const item1 = sp(15);
  const item2 = sp(30);
  const item3 = sp(45);
  const item4 = sp(60);

  // Cross marks slam
  const cross1 = sp(22, { damping: 6, stiffness: 350 });
  const cross2 = sp(37, { damping: 6, stiffness: 350 });
  const cross3 = sp(52, { damping: 6, stiffness: 350 });
  const cross4 = sp(67, { damping: 6, stiffness: 350 });

  // Heartbeat line
  const heartProgress = frame * 2.5;
  const heartPoints = Array.from({ length: 80 }, (_, i) => {
    const t = i / 79;
    const x = t * 400;
    const beat = Math.sin(t * 12 * Math.PI) * Math.exp(-((t % 0.333) * 8));
    return `${x},${120 - beat * 40}`;
  }).join(' ');

  // Shadow clock
  const clockAngle = frame * 3;

  // Floating dark particles
  const darkParticles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: seededRandom(i * 3 + 3) * 5 + 2,
    speed: seededRandom(i * 5) * 0.02 + 0.005,
    phase: seededRandom(i * 7) * Math.PI * 2,
  }));

  const dreamItems = [
    { icon: '✈', label: 'छुट्टियां बंद', cross: cross1, item: item1 },
    { icon: '👨‍👩‍👧‍👦', label: 'परिवार से दूर', cross: cross2, item: item2 },
    { icon: '⛽', label: 'पेट्रोल के पैसे नहीं', cross: cross3, item: item3 },
    { icon: '🛒', label: 'जरूरी खर्च भी कटे', cross: cross4, item: item4 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Heavy dark gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #0a0a0a 0%, #1C1C1C 50%, #0d0a00 100%)',
      }} />

      {/* Shape layers — oppressive rectangles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <rect x="0" y="0" width="1920" height="6"
            fill="rgba(255,0,0,0.15)" />
          <rect x="0" y="1074" width="1920" height="6"
            fill="rgba(255,0,0,0.15)" />
          <rect
            x={100 + Math.sin(frame * 0.007) * 10}
            y={100 + Math.cos(frame * 0.009) * 8}
            width="180" height="180"
            fill="none" stroke="rgba(255,0,0,0.04)" strokeWidth="1"
            transform={`rotate(${frame * 0.05}, 190, 190)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Floating dark particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {darkParticles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 20}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 14}
              r={p.r}
              fill={`rgba(${i % 2 === 0 ? '255,0,0' : '150,100,0'},0.08)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left side — Harold's fear state */}
      <div style={{
        position: 'absolute',
        left: 100, top: 80,
        width: 550,
      }}>
        {/* Heading */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 62,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.2,
          opacity: sp(5),
        }}>
          एक लखपती
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 62,
          fontWeight: 900,
          color: '#ff0000',
          lineHeight: 1.2,
          opacity: sp(10),
        }}>
          डरकर जिया
        </div>

        <div style={{
          height: 3,
          width: interpolate(frame, [12, 45], [0, 450], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          }),
          background: 'linear-gradient(90deg, #ff0000, transparent)',
          marginTop: 16, marginBottom: 30,
        }} />

        {/* Harold stick figure — old, bent */}
        <div style={{
          display: 'flex', justifyContent: 'center',
          opacity: sp(18),
        }}>
          <svg width="130" height="200" viewBox="0 0 130 200">
            <circle cx="65" cy="30" r="22"
              fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <ellipse cx="56" cy="29" rx="7" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
            <ellipse cx="74" cy="29" rx="7" ry="5" fill="none" stroke="#e9d700" strokeWidth="2" />
            {/* Worried wrinkles */}
            <path d="M 52 22 Q 56 18 60 22" fill="none" stroke="#9E9E9E" strokeWidth="1" />
            <path d="M 70 22 Q 74 18 78 22" fill="none" stroke="#9E9E9E" strokeWidth="1" />
            {/* Bent body — old */}
            <path d="M 65 52 Q 70 85 75 115" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            {/* Cane */}
            <line x1="75" y1="115" x2="100" y2="180" stroke="#e9d700" strokeWidth="2" />
            <line x1="90" y1="180" x2="110" y2="180" stroke="#e9d700" strokeWidth="2" />
            {/* Arms drooping */}
            <line x1="65" y1="75" x2="38" y2="95" stroke="#9E9E9E" strokeWidth="2.5" />
            <line x1="75" y1="80" x2="80" y2="105" stroke="#9E9E9E" strokeWidth="2" />
            {/* One leg on cane */}
            <line x1="75" y1="115" x2="55" y2="175" stroke="#9E9E9E" strokeWidth="2.5" />
            {/* Worry cloud */}
            <circle cx="30" cy="48" r="12" fill="rgba(255,0,0,0.15)" />
            <circle cx="20" cy="40" r="8" fill="rgba(255,0,0,0.12)" />
            <circle cx="38" cy="38" r="9" fill="rgba(255,0,0,0.1)" />
            <text x="22" y="52" fill="#ff0000" fontSize="16" fontFamily="Inter">₹?</text>
          </svg>
        </div>

        {/* Heartbeat line */}
        <div style={{ marginTop: 20, opacity: sp(35) }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26,
            color: '#9E9E9E',
            marginBottom: 8,
          }}>रात को नींद नहीं...</div>
          <svg width="420" height="240" viewBox="0 0 400 240">
            <rect x="0" y="0" width="400" height="240" fill="rgba(0,0,0,0.3)" rx="8" />
            <line x1="0" y1="120" x2="400" y2="120" stroke="rgba(255,255,255,0.05)" strokeWidth="1" />
            <polyline
              points={heartPoints}
              fill="none"
              stroke="#ff0000"
              strokeWidth="2.5"
              strokeDasharray="400"
              strokeDashoffset={400 - (heartProgress % 400)}
              opacity="0.8"
            />
          </svg>
        </div>
      </div>

      {/* Right side — Crossed out dreams list */}
      <div style={{
        position: 'absolute',
        right: 80, top: '50%',
        transform: 'translateY(-50%)',
        width: 900,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 42,
          color: '#9E9E9E',
          marginBottom: 28,
          opacity: sp(8),
        }}>
          जो सपने थे... सब खत्म हो गए:
        </div>

        {dreamItems.map((d, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            marginBottom: 28,
            opacity: d.item,
            transform: `translateX(${(1 - d.item) * 80}px)`,
            background: 'rgba(45,45,45,0.5)',
            borderRadius: 14,
            padding: '20px 28px',
            border: '1px solid rgba(255,0,0,0.15)',
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Icon */}
            <div style={{ fontSize: 50, filter: 'grayscale(1) opacity(0.4)' }}>{d.icon}</div>

            {/* Label */}
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 46,
              color: '#F5F5F5',
              fontWeight: 600,
              flex: 1,
            }}>
              {d.label}
            </div>

            {/* BIG RED X */}
            <div style={{
              transform: `scale(${d.cross})`,
              transformOrigin: 'center',
            }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <line x1="8" y1="8" x2="52" y2="52" stroke="#ff0000" strokeWidth="7" strokeLinecap="round" />
                <line x1="52" y1="8" x2="8" y2="52" stroke="#ff0000" strokeWidth="7" strokeLinecap="round" />
              </svg>
            </div>

            {/* Strikethrough */}
            <div style={{
              position: 'absolute',
              left: 0, top: '50%',
              height: 3,
              background: '#ff0000',
              width: `${d.cross * 100}%`,
              opacity: 0.6,
              transform: 'translateY(-50%)',
            }} />
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.8) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S6_HaroldFear;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S7_EleanorStory',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S7_EleanorStory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split reveal — two characters
  const leftReveal = interpolate(frame, [5, 35], [-550, 0], {
    easing: Easing.out(Easing.back(1.1)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const rightReveal = interpolate(frame, [5, 35], [550, 0], {
    easing: Easing.out(Easing.back(1.1)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 18L display
  const savingsCount = sp(40, { damping: 10, stiffness: 160 });
  // Loss pop
  const lossPop = sp(72, { damping: 6, stiffness: 300, mass: 0.2 });

  // Bar chart before/after
  const barProg = interpolate(frame, [40, 80], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const afterBarProg = interpolate(frame, [68, 95], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Floating particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: seededRandom(i * 3 + 3) * 5 + 1.5,
    speed: seededRandom(i * 5) * 0.018 + 0.006,
    phase: seededRandom(i * 7) * Math.PI * 2,
  }));

  const barH = 300;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Split dual-tone background */}
      <AbsoluteFill style={{ display: 'flex' }}>
        <div style={{
          flex: 1,
          background: 'linear-gradient(180deg, #0d1a0d 0%, #1C1C1C 100%)',
        }} />
        <div style={{
          flex: 1,
          background: 'linear-gradient(180deg, #1a0000 0%, #1C1C1C 100%)',
        }} />
      </AbsoluteFill>

      {/* Center divider */}
      <div style={{
        position: 'absolute',
        left: '50%', top: 0,
        width: 2, height: '100%',
        background: 'linear-gradient(180deg, transparent, rgba(255,255,255,0.06), transparent)',
        transform: 'translateX(-50%)',
      }} />

      {/* Geometric shape — large background rings */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <circle cx="960" cy="540" r="400" fill="none"
            stroke="rgba(255,255,255,0.02)" strokeWidth="1"
            transform={`rotate(${frame * 0.04}, 960, 540)`} />
          <circle cx="960" cy="540" r="280" fill="none"
            stroke="rgba(255,255,255,0.015)" strokeWidth="1"
            transform={`rotate(${-frame * 0.03}, 960, 540)`} />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 22}
              cy={p.y + Math.cos(frame * p.speed + p.phase) * 16}
              r={p.r}
              fill={i % 2 === 0 ? 'rgba(100,200,100,0.08)' : 'rgba(255,0,0,0.08)'}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Left — Eleanor + Husband couple intro */}
      <div style={{
        position: 'absolute',
        left: 60, top: '50%',
        transform: `translateY(-50%) translateX(${leftReveal}px)`,
        width: 600,
      }}>
        {/* Two stick figures — couple */}
        <div style={{ display: 'flex', justifyContent: 'center', gap: 30, marginBottom: 24 }}>
          {/* Eleanor */}
          <svg width="90" height="155" viewBox="0 0 90 155">
            <circle cx="45" cy="28" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 45, 28)`} />
            <ellipse cx="37" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <ellipse cx="53" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            {/* Smile */}
            <path d="M 38 36 Q 45 42 52 36" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="45" y1="48" x2="45" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Dress line */}
            <polygon points="35,100 55,100 60,130 30,130" fill="none" stroke="#9E9E9E" strokeWidth="1.5" />
            <line x1="45" y1="68" x2="20" y2="82" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="68" x2="68" y2="78" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>

          {/* Heart */}
          <svg width="40" height="155" viewBox="0 0 40 155" style={{ alignSelf: 'center' }}>
            <path d="M20 65 C20 55 10 45 5 55 C0 65 10 75 20 85 C30 75 40 65 35 55 C30 45 20 55 20 65Z"
              fill="rgba(233,215,0,0.4)" />
          </svg>

          {/* Husband */}
          <svg width="90" height="155" viewBox="0 0 90 155">
            <circle cx="45" cy="28" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              transform={`rotate(${Math.sin(frame * 0.035) * 2}, 45, 28)`} />
            <ellipse cx="37" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <ellipse cx="53" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <path d="M 38 36 Q 45 42 52 36" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="45" y1="48" x2="45" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="68" x2="20" y2="82" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="68" x2="70" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Briefcase engineer */}
            <rect x="66" y="75" width="18" height="13" fill="none" stroke="#e9d700" strokeWidth="1.5" rx="2" />
            <line x1="45" y1="105" x2="30" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="105" x2="60" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>

        {/* Names */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 58,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 3,
          }}>ELEANOR</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 32,
            color: '#9E9E9E',
            marginTop: 4,
          }}>और उनके पती (इंजीनियर)</div>
          <div style={{
            marginTop: 14,
            background: 'rgba(0,180,0,0.12)',
            border: '1px solid rgba(0,200,0,0.3)',
            borderRadius: 10,
            padding: '10px 24px',
            display: 'inline-block',
          }}>
            <span style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 28,
              color: '#4CAF50',
            }}>65 साल में सेवा निवर्त ✓</span>
          </div>
        </div>

        {/* Savings */}
        <div style={{
          marginTop: 32,
          textAlign: 'center',
          opacity: savingsCount,
          transform: `scale(${savingsCount})`,
          transformOrigin: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 100,
            fontWeight: 900,
            color: '#e9d700',
            textShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>₹18L</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 32,
            color: '#9E9E9E',
          }}>मेहनत की बचत</div>
        </div>
      </div>

      {/* Right — Portfolio breakdown + Loss */}
      <div style={{
        position: 'absolute',
        right: 60, top: '50%',
        transform: `translateY(-50%) translateX(${rightReveal}px)`,
        width: 720,
      }}>
        {/* 70% stocks warning */}
        <div style={{
          background: 'rgba(255,0,0,0.1)',
          border: '1px solid rgba(255,0,0,0.3)',
          borderRadius: 14,
          padding: '22px 32px',
          marginBottom: 30,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 34,
            color: '#9E9E9E',
          }}>पोर्टफोलियो था:</div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 90,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
          }}>70%</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 40,
            color: '#F5F5F5',
            fontWeight: 700,
          }}>शेयरों में</div>
        </div>

        {/* Bar chart before/after */}
        <svg width="680" height="360" viewBox="0 0 680 360">
          {/* Before bar */}
          <rect x="60" y={360 - 300 * barProg} width="180" height={300 * barProg}
            fill="#e9d700" rx="6" />
          <text x="150" y="340" textAnchor="middle" fill="#F5F5F5" fontSize="22"
            fontFamily="Noto Sans Devanagari">पहले</text>
          <text x="150" y={360 - 300 * barProg - 10} textAnchor="middle"
            fill="#e9d700" fontSize="28" fontFamily="JetBrains Mono" fontWeight="700">₹18L</text>

          {/* After bar */}
          <rect x="340" y={360 - 165 * afterBarProg} width="180" height={165 * afterBarProg}
            fill="#ff0000" rx="6" />
          <text x="430" y="340" textAnchor="middle" fill="#F5F5F5" fontSize="22"
            fontFamily="Noto Sans Devanagari">बाद में</text>
          {afterBarProg > 0.8 && (
            <text x="430" y={360 - 165 * afterBarProg - 10} textAnchor="middle"
              fill="#ff0000" fontSize="28" fontFamily="JetBrains Mono" fontWeight="700">₹11L</text>
          )}

          {/* Loss arrow */}
          {afterBarProg > 0.5 && (
            <g>
              <line x1="240" y1="210" x2="340" y2="230"
                stroke="#ff0000" strokeWidth="2" strokeDasharray="5,3" />
              <polygon points="338,222 346,235 332,238" fill="#ff0000" />
              <text x="280" y="200" fill="#ff0000" fontSize="24"
                fontFamily="Noto Sans Devanagari" textAnchor="middle">-₹7L खो गए</text>
            </g>
          )}
        </svg>

        {/* Loss impact */}
        <div style={{
          transform: `scale(${lossPop})`,
          transformOrigin: 'center',
          background: 'rgba(255,0,0,0.15)',
          border: '2px solid #ff0000',
          borderRadius: 14,
          padding: '18px 30px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 70,
            fontWeight: 900,
            color: '#ff0000',
          }}>-₹7 लाख</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 36,
            color: '#F5F5F5',
          }}>संकट में खो गए</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S7_EleanorStory;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S8_EleanorLetter',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S8_EleanorLetter: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Letter unfold animation
  const letterScale = interpolate(frame, [8, 45], [0.4, 1], {
    easing: Easing.out(Easing.back(1.1)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const letterOp = interpolate(frame, [5, 25], [0, 1], {
    easing: Easing.out(Easing.quad),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Text lines appearing one by one
  const line1 = interpolate(frame, [35, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const line2 = interpolate(frame, [52, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const line3 = interpolate(frame, [68, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const line4 = interpolate(frame, [82, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Candle flicker
  const flicker = 0.85 + Math.sin(frame * 0.23) * 0.08 + Math.sin(frame * 0.47) * 0.05;

  // Teardrops
  const teardrops = Array.from({ length: 8 }, (_, i) => ({
    x: 80 + seededRandom(i * 3 + 1) * 600,
    y: seededRandom(i * 3 + 2) * 400 + 200,
    speed: seededRandom(i * 3 + 3) * 0.8 + 0.3,
    phase: seededRandom(i * 5) * 90,
    delay: seededRandom(i * 7) * 40,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Deep somber gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, #0f0a05 0%, #0a0a0a 60%, #000000 100%)',
      }} />

      {/* Candlelight warm glow */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 600px 700px at 75% 40%, rgba(255,${Math.floor(140 * flicker)},0,${0.06 * flicker}) 0%, transparent 65%)`,
      }} />

      {/* Geometric shape — subtle cross / memorial */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <line x1="1700" y1="100" x2="1700" y2="300"
            stroke="rgba(255,255,255,0.04)" strokeWidth="1" />
          <line x1="1620" y1="200" x2="1780" y2="200"
            stroke="rgba(255,255,255,0.04)" strokeWidth="1" />
          {/* Fading dots texture */}
          {Array.from({ length: 35 }, (_, i) => (
            <circle key={i}
              cx={seededRandom(i * 4) * 1920}
              cy={seededRandom(i * 4 + 1) * 1080}
              r={seededRandom(i * 4 + 2) * 2 + 0.5}
              fill="rgba(255,200,100,0.06)"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Teardrop animation */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {teardrops.map((t, i) => {
            const y = (t.y + Math.max(0, frame - t.delay) * t.speed) % 900;
            return (
              <ellipse key={i}
                cx={t.x} cy={y}
                rx="4" ry="7"
                fill="rgba(100,150,255,0.18)"
                opacity={frame > t.delay ? 1 : 0}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* THE LETTER — center */}
      <div style={{
        position: 'absolute',
        left: '50%', top: '50%',
        transform: `translate(-50%, -50%) scale(${letterScale})`,
        opacity: letterOp,
        width: 840,
      }}>
        <div style={{
          background: 'linear-gradient(160deg, #f5f0e8 0%, #ede8d8 100%)',
          borderRadius: 8,
          padding: '50px 60px',
          boxShadow: '0 20px 80px rgba(0,0,0,0.8), 0 0 0 1px rgba(255,200,100,0.2)',
          position: 'relative',
        }}>
          {/* Aged paper lines */}
          {[120, 165, 210, 255, 300].map((y, i) => (
            <div key={i} style={{
              position: 'absolute',
              left: 40, right: 40, top: y,
              height: 1,
              background: 'rgba(0,0,0,0.06)',
            }} />
          ))}

          {/* Letter head */}
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            color: '#555',
            marginBottom: 12,
            opacity: line1,
          }}>
            प्रिय कुनाल जी,
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 38,
            color: '#1a1a1a',
            lineHeight: 1.55,
            opacity: line2,
          }}>
            "मेरे पती एक अच्छे इंजीनियर थे।
            उन्होंने पूरी जिंदगी मेहनत की..."
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 36,
            color: '#333',
            lineHeight: 1.55,
            marginTop: 20,
            opacity: line3,
          }}>
            "लेकिन वो इस वित्तीय नुकसान को
            सहन नहीं कर सके। वो उदास हो गए..."
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 36,
            color: '#333',
            lineHeight: 1.55,
            marginTop: 20,
            opacity: line4,
          }}>
            "2011 में वो चले गए।"
          </div>

          {/* Signature */}
          <div style={{
            marginTop: 30,
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#555',
            fontStyle: 'italic',
            opacity: line4,
            textAlign: 'right',
          }}>
            — एलेनोर
          </div>

          {/* Tear stain effect */}
          <div style={{
            position: 'absolute',
            bottom: 60, left: 80,
            width: 40, height: 40,
            borderRadius: '50%',
            background: 'rgba(150,180,255,0.25)',
            filter: 'blur(8px)',
            opacity: line3,
          }} />
        </div>
      </div>

      {/* Right — Candle SVG */}
      <div style={{
        position: 'absolute',
        right: 160, top: '50%',
        transform: `translateY(-50%)`,
        opacity: sp(12),
      }}>
        <svg width="120" height="280" viewBox="0 0 120 280">
          {/* Flame */}
          <ellipse cx="60" cy="45"
            rx={10 + Math.sin(frame * 0.3) * 2}
            ry={22 + Math.sin(frame * 0.23) * 3}
            fill={`rgba(255,${160 + Math.floor(Math.sin(frame * 0.4) * 30)},0,${flicker})`}
          />
          <ellipse cx="60" cy="52"
            rx={5 + Math.sin(frame * 0.4) * 1}
            ry={10}
            fill={`rgba(255,240,180,${flicker * 0.9})`}
          />
          {/* Candle body */}
          <rect x="40" y="60" width="40" height="160" fill="#d4c4a0" rx="4" />
          <rect x="40" y="60" width="40" height="20" fill="#e8d8b0" rx="4" />
          {/* Wax drips */}
          <ellipse cx="48" cy="75" rx="4" ry="8" fill="#e8d8b0" />
          <ellipse cx="72" cy="88" rx="3" ry="6" fill="#e8d8b0" />
          {/* Wick */}
          <line x1="60" y1="60" x2="60" y2="72" stroke="#333" strokeWidth="2" />
          {/* Candle base */}
          <rect x="30" y="220" width="60" height="16" fill="#aaa" rx="3" />
          {/* Glow halo */}
          <circle cx="60" cy="48" r="40"
            fill={`rgba(255,180,0,${0.08 * flicker})`} />
        </svg>
      </div>

      {/* Bottom truth statement */}
      <div style={{
        position: 'absolute',
        bottom: 70, left: '50%',
        transform: `translateX(-50%)`,
        opacity: interpolate(frame, [95, 115], [0, 1], {
          extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
        }),
        textAlign: 'center',
        width: 1100,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 40,
          color: '#9E9E9E',
          fontStyle: 'italic',
        }}>
          "बुढ़ापे में निवेश की गलतियां — केवल संख्या में नुकसान नहीं देती"
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 30%, rgba(0,0,0,0.85) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S8_EleanorLetter;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S9_TheTrueCost',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S9_TheTrueCost: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Four damage items slam in sequence
  const d1 = sp(8, { damping: 7, stiffness: 280, mass: 0.25 });
  const d2 = sp(22, { damping: 7, stiffness: 280, mass: 0.25 });
  const d3 = sp(36, { damping: 7, stiffness: 280, mass: 0.25 });
  const d4 = sp(52, { damping: 6, stiffness: 320, mass: 0.2 });

  // Title
  const titleSlam = sp(3, { damping: 6, stiffness: 400, mass: 0.2 });

  // Shatter particles at each impact
  const shatter = Array.from({ length: 28 }, (_, i) => ({
    angle: (i / 28) * Math.PI * 2,
    speed: seededRandom(i * 3 + 1) * 3 + 1,
    size: seededRandom(i * 3 + 2) * 8 + 3,
    col: i % 4,
  }));

  const damages = [
    { icon: '🫀', label: 'स्वास्थ', sublabel: 'नष्ट होता है', color: '#ff0000', delay: d1, bg: 'rgba(255,0,0,0.12)' },
    { icon: '💍', label: 'विवाह', sublabel: 'टूट जाता है', color: '#FFC107', delay: d2, bg: 'rgba(255,193,7,0.1)' },
    { icon: '🧠', label: 'आत्मविश्वास', sublabel: 'डूब जाता है', color: '#9E9E9E', delay: d3, bg: 'rgba(158,158,158,0.1)' },
    { icon: '🕊', label: 'शांती', sublabel: 'चुरा लेते हैं', color: '#e9d700', delay: d4, bg: 'rgba(233,215,0,0.12)' },
  ];

  // Floating shards
  const shards = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: seededRandom(i * 3 + 3) * 4 + 1,
    speed: seededRandom(i * 5) * 0.02 + 0.006,
    phase: seededRandom(i * 7) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Heavy dark gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #000000 0%, #1C1C1C 40%, #0d0000 100%)',
      }} />

      {/* Horizontal scan lines texture */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 54 }, (_, i) => (
            <line key={i} x1="0" y1={i * 20} x2="1920" y2={i * 20}
              stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Shape layer — large jagged shape */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          <polygon
            points="960,50 1150,250 1100,500 960,600 820,500 770,250"
            fill="none" stroke="rgba(255,0,0,0.04)" strokeWidth="1.5"
            transform={`rotate(${frame * 0.05}, 960, 325)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Floating shards */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {shards.map((s, i) => (
            <circle key={i}
              cx={s.x + Math.sin(frame * s.speed + s.phase) * 20}
              cy={s.y + Math.cos(frame * s.speed + s.phase) * 14}
              r={s.r}
              fill={`rgba(${i % 3 === 0 ? '255,0,0' : i % 3 === 1 ? '233,215,0' : '100,100,100'},0.1)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Headline — TOP CENTER */}
      <div style={{
        position: 'absolute',
        top: 55, left: '50%',
        transform: `translateX(-50%) scale(${titleSlam})`,
        transformOrigin: 'center top',
        textAlign: 'center',
        width: 1200,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 68,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.1,
        }}>
          निवेश की गलती की असली कीमत
        </div>
        <div style={{
          height: 4,
          background: 'linear-gradient(90deg, transparent, #ff0000, transparent)',
          width: 600,
          margin: '12px auto 0',
          borderRadius: 2,
        }} />
      </div>

      {/* Four damage cards — 2x2 grid */}
      <div style={{
        position: 'absolute',
        left: '50%', top: '55%',
        transform: 'translate(-50%, -50%)',
        display: 'grid',
        gridTemplateColumns: '1fr 1fr',
        gap: 28,
        width: 1300,
      }}>
        {damages.map((d, i) => (
          <div key={i} style={{
            background: d.bg,
            border: `2px solid ${d.color}40`,
            borderRadius: 20,
            padding: '32px 36px',
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            transform: `scale(${d.delay}) translateY(${(1 - d.delay) * 40}px)`,
            opacity: d.delay,
          }}>
            {/* Icon */}
            <div style={{ fontSize: 64, filter: 'drop-shadow(0 0 8px rgba(0,0,0,0.5))' }}>
              {d.icon}
            </div>

            <div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 54,
                fontWeight: 900,
                color: d.color,
                lineHeight: 1.1,
              }}>
                {d.label}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 34,
                color: '#9E9E9E',
                marginTop: 4,
              }}>
                {d.sublabel}
              </div>
            </div>

            {/* Damage indicator */}
            <div style={{ marginLeft: 'auto' }}>
              <svg width="50" height="50">
                <line x1="8" y1="8" x2="42" y2="42" stroke={d.color} strokeWidth="5" strokeLinecap="round" />
                <line x1="42" y1="8" x2="8" y2="42" stroke={d.color} strokeWidth="5" strokeLinecap="round" />
              </svg>
            </div>
          </div>
        ))}
      </div>

      {/* Stick Figure — devastated */}
      <div style={{
        position: 'absolute',
        right: 70, bottom: 55,
        opacity: sp(55),
        transform: `translateY(${Math.sin(frame * 0.06) * 2}px)`,
      }}>
        <svg width="95" height="155" viewBox="0 0 95 155">
          <circle cx="47" cy="28" r="20" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
          <ellipse cx="39" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="55" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
          {/* Sad mouth */}
          <path d="M 40 37 Q 47 32 54 37" fill="none" stroke="#9E9E9E" strokeWidth="2" />
          <line x1="47" y1="48" x2="47" y2="100" stroke="#9E9E9E" strokeWidth="2.5" />
          {/* Drooping arms */}
          <line x1="47" y1="70" x2="20" y2="95" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="47" y1="70" x2="74" y2="95" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="47" y1="100" x2="30" y2="140" stroke="#9E9E9E" strokeWidth="2.5" />
          <line x1="47" y1="100" x2="64" y2="140" stroke="#9E9E9E" strokeWidth="2.5" />
          {/* Tears */}
          <ellipse cx="37" cy="35" rx="2" ry="4" fill="rgba(100,150,255,0.5)" />
          <ellipse cx="57" cy="35" rx="2" ry="4" fill="rgba(100,150,255,0.5)" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.82) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S9_TheTrueCost;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S10_ChapterClose',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S10_ChapterClose: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Full dramatic wipe background
  const bgReveal = interpolate(frame, [0, 30], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bigTextScale = sp(10, { damping: 8, stiffness: 180, mass: 0.5 });
  const contrastLine = sp(35, { damping: 12, stiffness: 150 });
  const greenLine = sp(55, { damping: 12, stiffness: 150 });

  const lineW = interpolate(frame, [32, 65], [0, 780], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const lineW2 = interpolate(frame, [52, 80], [0, 680], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Golden glow particles
  const goldParticles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: seededRandom(i * 3 + 3) * 6 + 2,
    speed: seededRandom(i * 5) * 0.022 + 0.008,
    phase: seededRandom(i * 7) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Split dramatic background */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column' }}>
        {/* Top half — dark red warning */}
        <div style={{
          flex: 1,
          background: 'linear-gradient(135deg, #1a0000 0%, #0d0000 100%)',
          opacity: bgReveal,
        }} />
        {/* Bottom half — dark gold hope */}
        <div style={{
          flex: 1,
          background: 'linear-gradient(135deg, #0d0d00 0%, #1a1200 100%)',
          opacity: bgReveal,
        }} />
      </AbsoluteFill>

      {/* Horizontal divider */}
      <div style={{
        position: 'absolute',
        left: 0, top: '50%',
        height: 3,
        width: `${bgReveal * 100}%`,
        background: 'linear-gradient(90deg, #ff0000, #e9d700)',
        transform: 'translateY(-50%)',
      }} />

      {/* Concentric shape layers */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {[200, 350, 500].map((r, i) => (
            <circle key={i}
              cx="960" cy="540"
              r={r}
              fill="none"
              stroke="rgba(255,255,255,0.02)"
              strokeWidth="1"
              transform={`rotate(${frame * (0.03 + i * 0.01) * (i % 2 === 0 ? 1 : -1)}, 960, 540)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Golden particles rising */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080">
          {goldParticles.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 25}
              cy={p.y - (frame * p.speed * 0.5) < 0 ? p.y : (p.y - frame * p.speed * 0.5) % 1100}
              r={p.r}
              fill={i % 2 === 0 ? 'rgba(233,215,0,0.12)' : 'rgba(255,0,0,0.08)'}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* TOP half — The Mistake */}
      <div style={{
        position: 'absolute',
        left: 120, top: 80,
        width: 900,
        opacity: sp(8),
        transform: `translateY(${(1 - sp(8)) * -50}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#ff0000',
          letterSpacing: 4,
          fontWeight: 700,
          marginBottom: 12,
        }}>❌ सबसे बड़ी गलती</div>

        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 64,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.2,
          transform: `scale(${bigTextScale})`,
          transformOrigin: 'left top',
        }}>
          शांतिपूर्ण वर्षों को
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 64,
          fontWeight: 900,
          color: '#ff0000',
          lineHeight: 1.2,
        }}>
          वित्तीय जटके से
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 64,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.2,
        }}>
          जहर देना
        </div>

        {/* Red line */}
        <div style={{
          marginTop: 18,
          height: 4,
          width: lineW,
          background: '#ff0000',
          borderRadius: 2,
        }} />

        <div style={{
          marginTop: 14,
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 40,
          color: '#9E9E9E',
          opacity: contrastLine,
        }}>
          उत्तेजना के लिए नहीं बनाओ पोर्टफोलियो
        </div>
      </div>

      {/* BOTTOM half — The Solution principle */}
      <div style={{
        position: 'absolute',
        left: 120, bottom: 80,
        width: 960,
        opacity: sp(48),
        transform: `translateY(${(1 - sp(48)) * 50}px)`,
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36,
          color: '#e9d700',
          letterSpacing: 4,
          fontWeight: 700,
          marginBottom: 12,
        }}>✓ सही सोच</div>

        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 68,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1.2,
        }}>
          आर्थिक जीवन के लिए
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 68,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1.2,
        }}>
          बनाओ पोर्टफोलियो
        </div>

        {/* Gold line */}
        <div style={{
          marginTop: 18,
          height: 4,
          width: lineW2,
          background: 'linear-gradient(90deg, #e9d700, rgba(233,215,0,0.3))',
          borderRadius: 2,
        }} />

        <div style={{
          marginTop: 14,
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          color: '#9E9E9E',
          opacity: greenLine,
        }}>
          नींद, शांती, और सुरक्षा — यही है असली लक्ष्य
        </div>
      </div>

      {/* RIGHT — Big closing number */}
      <div style={{
        position: 'absolute',
        right: 100, top: '50%',
        transform: `translateY(-50%) scale(${sp(20, { damping: 8, stiffness: 200 })})`,
        textAlign: 'center',
        opacity: sp(20),
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 44,
          color: '#9E9E9E',
          marginBottom: 10,
        }}>अध्याय 3</div>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 220,
          fontWeight: 900,
          color: 'rgba(255,255,255,0.06)',
          lineHeight: 1,
          letterSpacing: -10,
        }}>03</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 40,
          color: '#9E9E9E',
          marginTop: -20,
        }}>समाप्त</div>

        {/* Stick figure — calm, arms folded */}
        <div style={{
          marginTop: 20,
          transform: `translateY(${Math.sin(frame * 0.04) * 2}px)`,
        }}>
          <svg width="100" height="160" viewBox="0 0 100 160">
            <circle cx="50" cy="28" r="20" fill="none" stroke="#e9d700" strokeWidth="2.5"
              transform={`rotate(${Math.sin(frame * 0.04) * 2}, 50, 28)`} />
            <ellipse cx="42" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <ellipse cx="58" cy="27" rx="6" ry="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            {/* Gentle smile */}
            <path d="M 42 36 Q 50 42 58 36" fill="none" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="50" y1="48" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Crossed arms — wise pose */}
            <line x1="50" y1="72" x2="22" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="22" y1="80" x2="38" y2="68" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="50" y1="72" x2="78" y2="80" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="78" y1="80" x2="62" y2="68" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="50" y1="105" x2="34" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="50" y1="105" x2="66" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Golden halo */}
            <circle cx="50" cy="28" r="28" fill="none" stroke="rgba(233,215,0,0.15)" strokeWidth="1" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.78) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>

    </AbsoluteFill>
  );
};

export default C3S10_ChapterClose;
```

