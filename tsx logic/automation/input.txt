```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S1_TharavAdvantage',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S1_TharavAdvantage: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Horizontal race track pan
  const panX = interpolate(frame, [0, durationInFrames], [0, -80], { extrapolateRight: 'clamp' });

  // Word reveal stagger
  const word1Op = sp(5);
  const word2Op = sp(18);
  const word3Op = sp(31);

  // Advantage bar fill
  const barWidth = interpolate(sp(40), [0, 1], [0, 680]);

  // Floating number particles
  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 13) * 1080,
    size: 18 + seededRandom(i * 3) * 22,
    speed: 0.3 + seededRandom(i * 5) * 0.4,
    opacity: 0.04 + seededRandom(i * 11) * 0.05,
  }));

  // Racing lane lines scroll
  const laneScroll = interpolate(frame, [0, durationInFrames], [0, 400]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 80% 60% at 20% 50%, #1a2a0a 0%, #1C1C1C 55%, #0a0a1a 100%)',
      }} />

      {/* === HORIZONTAL RACE TRACK TEXTURE === */}
      <AbsoluteFill style={{ transform: `translateX(${panX}px)` }}>
        {/* Lane dividers scrolling */}
        {[0, 1, 2, 3].map(i => (
          <div key={i} style={{
            position: 'absolute',
            top: 200 + i * 180,
            left: -50,
            width: 2100,
            height: 2,
            background: 'rgba(233, 215, 0, 0.06)',
          }} />
        ))}
        {/* Dashed center line */}
        {Array.from({ length: 30 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            top: 540,
            left: -laneScroll + i * 80,
            width: 50,
            height: 3,
            background: 'rgba(233, 215, 0, 0.08)',
            borderRadius: 2,
          }} />
        ))}
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Large diagonal rhombus */}
      <div style={{
        position: 'absolute',
        top: -200,
        right: -100,
        width: 700,
        height: 700,
        background: 'rgba(233, 215, 0, 0.03)',
        transform: `rotate(${45 + frame * 0.04}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})`,
        borderRadius: 40,
      }} />
      {/* Small accent rectangle */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 80,
        width: 300,
        height: 180,
        border: '1px solid rgba(233, 215, 0, 0.05)',
        transform: `rotate(${-12 + Math.sin(frame * 0.03) * 2}deg)`,
      }} />

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * p.speed * 30) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(233, 215, 0, ${p.opacity})`,
        }} />
      ))}

      {/* === MAIN CONTENT === */}
      {/* Left side: Hindi words stagger */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 220,
        transform: `translateX(${panX * 0.3}px)`,
      }}>
        {/* ठैराव */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 220,
          color: '#e9d700',
          fontWeight: 900,
          lineHeight: 1,
          opacity: word1Op,
          transform: `translateY(${interpolate(word1Op, [0, 1], [60, 0])}px)`,
          textShadow: '0 0 80px rgba(233,215,0,0.3)',
          letterSpacing: -4,
        }}>ठैराव</div>

        {/* = ADVANTAGE */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          marginTop: 16,
          opacity: word2Op,
          transform: `translateX(${interpolate(word2Op, [0, 1], [-40, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 72,
            color: '#F5F5F5',
            fontWeight: 300,
            letterSpacing: 8,
          }}>=</div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 72,
            color: '#F5F5F5',
            fontWeight: 800,
            letterSpacing: 6,
          }}>ADVANTAGE</div>
        </div>

        {/* Sub text */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 52,
          color: '#9E9E9E',
          marginTop: 20,
          opacity: word3Op,
          transform: `translateY(${interpolate(word3Op, [0, 1], [20, 0])}px)`,
          maxWidth: 700,
          lineHeight: 1.5,
        }}>जिंदगी की रेस में सबसे बड़ा हथियार</div>
      </div>

      {/* === ADVANTAGE BAR === */}
      <div style={{
        position: 'absolute',
        bottom: 200,
        left: 120,
        opacity: interpolate(frame, [35, 45], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          color: '#9E9E9E',
          letterSpacing: 4,
          marginBottom: 12,
          textTransform: 'uppercase',
        }}>COMPETITIVE EDGE</div>
        <div style={{
          width: 680,
          height: 8,
          background: 'rgba(255,255,255,0.08)',
          borderRadius: 4,
          overflow: 'hidden',
        }}>
          <div style={{
            width: barWidth,
            height: '100%',
            background: 'linear-gradient(90deg, #e9d700, #FFC107)',
            borderRadius: 4,
            boxShadow: '0 0 20px rgba(233,215,0,0.5)',
          }} />
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#e9d700',
          marginTop: 10,
        }}>{Math.round(interpolate(barWidth, [0, 680], [0, 100], { extrapolateRight: 'clamp' }))}%</div>
      </div>

      {/* === STICK FIGURE — RIGHT SIDE, STRIDING FORWARD === */}
      <svg
        style={{
          position: 'absolute',
          right: 140,
          bottom: 100,
          opacity: sp(30),
          transform: `translateY(${interpolate(sp(30), [0, 1], [80, 0])}px)`,
        }}
        width="180" height="320" viewBox="0 0 180 320"
      >
        {/* Golden glow */}
        <ellipse cx="90" cy="290" rx="60" ry="12" fill="rgba(233,215,0,0.15)" />
        {/* Body */}
        <line x1="90" y1="90" x2="90" y2="200" stroke="#F5F5F5" strokeWidth="5" strokeLinecap="round" />
        {/* Head */}
        <circle cx="90" cy={70 + Math.sin(frame * 0.04) * 2} r="28" fill="none" stroke="#F5F5F5" strokeWidth="4" />
        {/* Glasses */}
        <rect x="74" y="63" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <rect x="92" y="63" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <line x1="88" y1="67" x2="92" y2="67" stroke="#e9d700" strokeWidth="2" />
        {/* Arms — raised like winning */}
        <line x1="90" y1="130" x2={60 + Math.sin(frame * 0.06) * 4} y2="95" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
        <line x1="90" y1="130" x2={120 + Math.sin(frame * 0.06 + 1) * 4} y2="95" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
        {/* Legs — stride */}
        <line x1="90" y1="200" x2={65 + Math.sin(frame * 0.08) * 10} y2="275" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
        <line x1="90" y1="200" x2={115 - Math.sin(frame * 0.08) * 10} y2="275" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
        {/* Speed lines */}
        <line x1="20" y1="150" x2="55" y2="150" stroke="#e9d700" strokeWidth="2" opacity="0.4" />
        <line x1="10" y1="165" x2="50" y2="165" stroke="#e9d700" strokeWidth="1.5" opacity="0.3" />
        <line x1="25" y1="180" x2="55" y2="180" stroke="#e9d700" strokeWidth="1" opacity="0.2" />
      </svg>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 55%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S1_TharavAdvantage;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S2_NegotiationCalm',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S2_NegotiationCalm: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen reveal — left nervous, right calm
  const leftReveal = interpolate(sp(5), [0, 1], [0, 1]);
  const rightReveal = interpolate(sp(20), [0, 1], [0, 1]);
  const dividerScale = sp(12, { stiffness: 200, damping: 20 });

  // Nervous side shake
  const nervousShake = frame > 10 && frame < 50
    ? Math.sin(frame * 0.8) * 4 * interpolate(frame, [10, 50], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
    : 0;

  // Heartbeat pulse on nervous side
  const heartBeat = Math.abs(Math.sin(frame * 0.25)) * 0.06;

  // Calm side slow breathe
  const calmBreathe = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 10 }, (_, i) => ({
    x: seededRandom(i * 17) * 1920,
    y: seededRandom(i * 23) * 1080,
    r: 8 + seededRandom(i) * 12,
    op: 0.03 + seededRandom(i * 7) * 0.04,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill style={{
        background: 'linear-gradient(110deg, #1a0a0a 0%, #1C1C1C 45%, #0a1a0a 100%)',
      }} />

      {/* === LEFT HALF — NERVOUS / DESPERATE === */}
      <div style={{
        position: 'absolute',
        left: 0, top: 0,
        width: 920, height: 1080,
        opacity: leftReveal,
        clipPath: `inset(0 ${interpolate(leftReveal, [0, 1], [100, 0])}% 0 0)`,
        background: 'rgba(255,0,0,0.04)',
        overflow: 'hidden',
        transform: `translateX(${nervousShake}px)`,
      }}>
        {/* Grid texture — chaotic */}
        {Array.from({ length: 20 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(i * 3) * 920,
            top: seededRandom(i * 7) * 1080,
            width: 1,
            height: 60 + seededRandom(i) * 80,
            background: 'rgba(255,0,0,0.08)',
            transform: `rotate(${seededRandom(i * 11) * 360}deg)`,
          }} />
        ))}

        {/* Label */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 80,
          fontFamily: 'Inter',
          fontSize: 28,
          fontWeight: 700,
          color: '#ff0000',
          letterSpacing: 5,
          textTransform: 'uppercase',
          opacity: 0.7,
        }}>DESPERATION</div>

        {/* Big word */}
        <div style={{
          position: 'absolute',
          top: 260,
          left: 60,
          fontFamily: 'Yantra One',
          fontSize: 130,
          fontWeight: 900,
          color: '#ff0000',
          opacity: 0.15 + heartBeat,
          lineHeight: 1,
          letterSpacing: -3,
        }}>बेताबी</div>

        {/* Nervous figure — multiple overlapping */}
        <svg style={{ position: 'absolute', left: 160, top: 360 }} width="200" height="300" viewBox="0 0 200 300">
          {/* Shaky lines */}
          {[0, 1, 2].map(k => (
            <g key={k} opacity={k === 1 ? 0.9 : 0.15} transform={`translate(${k * nervousShake * 0.5}, 0)`}>
              <circle cx="100" cy="50" r="26" fill="none" stroke="#ff0000" strokeWidth="3.5" />
              <line x1="100" y1="76" x2="100" y2="180" stroke="#ff0000" strokeWidth="4" />
              <line x1="100" y1="110" x2="60" y2="145" stroke="#ff0000" strokeWidth="3.5" />
              <line x1="100" y1="110" x2="140" y2="145" stroke="#ff0000" strokeWidth="3.5" />
              <line x1="100" y1="180" x2="75" y2="260" stroke="#ff0000" strokeWidth="3.5" />
              <line x1="100" y1="180" x2="125" y2="260" stroke="#ff0000" strokeWidth="3.5" />
            </g>
          ))}
          {/* Sweat drops */}
          <ellipse cx="140" cy="30" rx="4" ry="7" fill="#ff0000" opacity="0.4" transform={`translateY(${Math.sin(frame * 0.2) * 3})`} />
          <ellipse cx="155" cy="45" rx="3" ry="5" fill="#ff0000" opacity="0.25" />
        </svg>

        {/* Desperation stat */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 80,
          fontFamily: 'JetBrains Mono',
          fontSize: 36,
          color: '#ff0000',
          opacity: 0.6,
        }}>WIN RATE: 23%</div>
      </div>

      {/* === DIVIDER LINE === */}
      <div style={{
        position: 'absolute',
        left: 940,
        top: 0,
        width: 4,
        height: 1080,
        background: 'linear-gradient(180deg, transparent, rgba(233,215,0,0.6), transparent)',
        transform: `scaleY(${dividerScale})`,
        transformOrigin: 'center',
        boxShadow: '0 0 20px rgba(233,215,0,0.3)',
      }} />

      {/* === RIGHT HALF — CALM / CONFIDENT === */}
      <div style={{
        position: 'absolute',
        left: 960, top: 0,
        width: 960, height: 1080,
        opacity: rightReveal,
        overflow: 'hidden',
        background: 'rgba(233,215,0,0.02)',
      }}>
        {/* Grid texture — ordered */}
        {Array.from({ length: 8 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: 40 + i * 110,
            top: 0,
            width: 1,
            height: 1080,
            background: 'rgba(233,215,0,0.04)',
          }} />
        ))}

        {/* Label */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 60,
          fontFamily: 'Inter',
          fontSize: 28,
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 5,
          textTransform: 'uppercase',
          opacity: 0.7,
        }}>CALM POWER</div>

        {/* Big word */}
        <div style={{
          position: 'absolute',
          top: 260,
          left: 50,
          fontFamily: 'Yantra One',
          fontSize: 130,
          fontWeight: 900,
          color: '#e9d700',
          opacity: 0.18,
          lineHeight: 1,
          letterSpacing: -3,
        }}>शांति</div>

        {/* Calm figure */}
        <svg
          style={{
            position: 'absolute',
            left: 200,
            top: 330,
            transform: `translateY(${calmBreathe}px)`,
          }}
          width="200" height="320" viewBox="0 0 200 320"
        >
          <ellipse cx="100" cy="310" rx="55" ry="10" fill="rgba(233,215,0,0.15)" />
          <circle cx="100" cy={50 + Math.sin(frame * 0.04) * 1.5} r="28" fill="none" stroke="#F5F5F5" strokeWidth="4" />
          {/* Glasses */}
          <rect x="83" y="43" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="101" y="43" width="13" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="96" y1="47" x2="101" y2="47" stroke="#e9d700" strokeWidth="2" />
          {/* Smile */}
          <path d="M88 61 Q100 70 112 61" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="100" y1="78" x2="100" y2="185" stroke="#F5F5F5" strokeWidth="5" />
          <line x1="100" y1="120" x2="65" y2="155" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="100" y1="120" x2="135" y2="155" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="100" y1="185" x2="75" y2="270" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="100" y1="185" x2="125" y2="270" stroke="#F5F5F5" strokeWidth="4" />
          {/* Calm aura rings */}
          <circle cx="100" cy="140" r={60 + Math.sin(frame * 0.05) * 5} fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.1" />
          <circle cx="100" cy="140" r={85 + Math.sin(frame * 0.04 + 1) * 6} fill="none" stroke="#e9d700" strokeWidth="0.5" opacity="0.06" />
        </svg>

        {/* Calm stat */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 60,
          fontFamily: 'JetBrains Mono',
          fontSize: 36,
          color: '#e9d700',
          opacity: 0.7,
        }}>WIN RATE: 78%</div>
      </div>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * 0.4) % 1080,
          width: p.r,
          height: p.r,
          borderRadius: '50%',
          background: `rgba(233,215,0,${p.op})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 95% 90% at 50% 50%, transparent 50%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S2_NegotiationCalm;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S3_InvestWithoutFear',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S3_InvestWithoutFear: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stock chart drawing animation
  const chartProgress = interpolate(frame, [15, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Chart points — volatile then steady
  const chartPoints = [
    { x: 0, y: 300 }, { x: 80, y: 180 }, { x: 160, y: 340 }, { x: 240, y: 120 },
    { x: 320, y: 280 }, { x: 400, y: 90 }, { x: 480, y: 220 }, { x: 560, y: 160 },
    { x: 640, y: 280 }, { x: 720, y: 100 }, { x: 800, y: 200 }, { x: 880, y: 80 },
  ];

  const totalLength = 880;
  const visibleLength = chartProgress * totalLength;
  const pathD = chartPoints.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  // Neighbor figure bouncing
  const neighborBounce = Math.abs(Math.sin(frame * 0.15)) * 8;

  // Calm investor breathe
  const investorBreathe = Math.sin(frame * 0.04) * 2;

  // Orbit camera zoom
  const sceneZoom = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 31) * 1920,
    y: seededRandom(i * 17) * 1080,
    size: 6 + seededRandom(i * 3) * 10,
    speed: 0.2 + seededRandom(i * 9) * 0.5,
    op: 0.03 + seededRandom(i * 13) * 0.05,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp, transform: `scale(${sceneZoom})` }}>

      {/* === GRADIENT === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 70% 80% at 50% 30%, #0d1a2a 0%, #1C1C1C 60%, #1a0d00 100%)',
      }} />

      {/* === GRID BACKGROUND === */}
      <AbsoluteFill>
        {Array.from({ length: 15 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: i * 128,
            top: 0,
            width: 1,
            height: 1080,
            background: 'rgba(255,255,255,0.025)',
          }} />
        ))}
        {Array.from({ length: 9 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            top: i * 135,
            left: 0,
            width: 1920,
            height: 1,
            background: 'rgba(255,255,255,0.025)',
          }} />
        ))}
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        top: -100,
        left: -100,
        width: 500,
        height: 500,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.05)',
        transform: `rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: -50,
        right: -50,
        width: 400,
        height: 400,
        background: 'rgba(255,193,7,0.04)',
        transform: `rotate(${45 + frame * 0.03}deg)`,
        borderRadius: 30,
      }} />

      {/* === STOCK CHART === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 200,
        opacity: interpolate(frame, [10, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        {/* Axes */}
        <svg width="960" height="420" viewBox="0 0 960 420">
          {/* Y axis */}
          <line x1="0" y1="0" x2="0" y2="380" stroke="rgba(255,255,255,0.15)" strokeWidth="2" />
          {/* X axis */}
          <line x1="0" y1="380" x2="900" y2="380" stroke="rgba(255,255,255,0.15)" strokeWidth="2" />

          {/* Horizontal dashed guides */}
          {[100, 200, 300].map(y => (
            <line key={y} x1="0" y1={y} x2="900" y2={y} stroke="rgba(255,255,255,0.04)" strokeWidth="1" strokeDasharray="8 8" />
          ))}

          {/* Chart fill gradient */}
          <defs>
            <linearGradient id="chartGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.2" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
            </linearGradient>
            <clipPath id="chartClip">
              <rect x="0" y="0" width={visibleLength * (900 / 880)} height="380" />
            </clipPath>
          </defs>

          {/* Fill area */}
          <path
            d={`${pathD} L 880 380 L 0 380 Z`}
            fill="url(#chartGrad)"
            clipPath="url(#chartClip)"
          />

          {/* Chart line */}
          <path
            d={pathD}
            fill="none"
            stroke="#e9d700"
            strokeWidth="3"
            strokeLinecap="round"
            strokeLinejoin="round"
            clipPath="url(#chartClip)"
          />

          {/* Moving dot at end of chart */}
          {chartProgress > 0.1 && (() => {
            const idx = Math.min(Math.floor(chartProgress * (chartPoints.length - 1)), chartPoints.length - 2);
            const t = (chartProgress * (chartPoints.length - 1)) - idx;
            const cx = chartPoints[idx].x + t * (chartPoints[idx + 1].x - chartPoints[idx].x);
            const cy = chartPoints[idx].y + t * (chartPoints[idx + 1].y - chartPoints[idx].y);
            return (
              <>
                <circle cx={cx} cy={cy} r="8" fill="#e9d700" />
                <circle cx={cx} cy={cy} r={8 + Math.sin(frame * 0.15) * 6} fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.4" />
              </>
            );
          })()}
        </svg>

        {/* Labels */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 24,
          color: '#9E9E9E',
          marginTop: 8,
          letterSpacing: 3,
          opacity: 0.6,
        }}>MARKET VOLATILITY — NOISE</div>
      </div>

      {/* === MAIN TEXT — RIGHT === */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 200,
        width: 700,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 110,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          opacity: sp(8),
          transform: `translateX(${interpolate(sp(8), [0, 1], [60, 0])}px)`,
          letterSpacing: -2,
        }}>घबराहट</div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 72,
          fontWeight: 900,
          color: '#e9d700',
          marginTop: 8,
          opacity: sp(18),
          transform: `translateX(${interpolate(sp(18), [0, 1], [40, 0])}px)`,
        }}>नहीं होती</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 44,
          color: '#9E9E9E',
          marginTop: 24,
          lineHeight: 1.6,
          opacity: sp(30),
        }}>जब मकसद<br />पड़ोसी नहीं,<br />आजादी है।</div>
      </div>

      {/* === NEIGHBOR FIGURE (LEFT, ANXIOUS) === */}
      <svg
        style={{
          position: 'absolute',
          left: 80,
          bottom: 80,
          opacity: sp(50) * 0.7,
        }}
        width="120" height="220" viewBox="0 0 120 220"
      >
        <circle cx="60" cy={30 + neighborBounce * 0.3} r="20" fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        <line x1="60" y1="50" x2="60" y2="140" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        <line x1="60" y1="80" x2="35" y2="110" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        <line x1="60" y1="80" x2="85" y2="110" stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        <line x1="60" y1="140" x2="40" y2={190 + neighborBounce} stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        <line x1="60" y1="140" x2="80" y2={190 - neighborBounce} stroke="#ff0000" strokeWidth="3" opacity="0.5" />
        {/* X over head */}
        <line x1="45" y1="15" x2="55" y2="25" stroke="#ff0000" strokeWidth="2" opacity="0.6" />
        <line x1="55" y1="15" x2="45" y2="25" stroke="#ff0000" strokeWidth="2" opacity="0.6" />
        <text x="10" y="210" fontFamily="Inter" fontSize="14" fill="#ff0000" opacity="0.5">पड़ोसी</text>
      </svg>

      {/* === CALM INVESTOR FIGURE === */}
      <svg
        style={{
          position: 'absolute',
          right: 140,
          bottom: 60,
          opacity: sp(55),
          transform: `translateY(${investorBreathe}px)`,
        }}
        width="160" height="280" viewBox="0 0 160 280"
      >
        <ellipse cx="80" cy="272" rx="50" ry="10" fill="rgba(233,215,0,0.15)" />
        <circle cx="80" cy={44 + investorBreathe * 0.3} r="26" fill="none" stroke="#F5F5F5" strokeWidth="4" />
        {/* Glasses */}
        <rect x="64" y="38" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <rect x="80" y="38" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <line x1="76" y1="42" x2="80" y2="42" stroke="#e9d700" strokeWidth="2" />
        <line x1="80" y1="70" x2="80" y2="170" stroke="#F5F5F5" strokeWidth="4.5" />
        <line x1="80" y1="110" x2="50" y2="140" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="80" y1="110" x2="110" y2="140" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="80" y1="170" x2="58" y2="248" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="80" y1="170" x2="102" y2="248" stroke="#F5F5F5" strokeWidth="3.5" />
        {/* Gold checkmark */}
        <path d="M65 100 L75 112 L98 88" fill="none" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" />
      </svg>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * p.speed * 20) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(233,215,0,${p.op})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 85% at 50% 50%, transparent 45%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S3_InvestWithoutFear;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S4_PanicVsPatience',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S4_PanicVsPatience: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake at start (panic moment)
  const shakeAmount = interpolate(frame, [0, 25], [8, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = frame < 25 ? Math.sin(frame * 1.2) * shakeAmount : 0;
  const shakeY = frame < 25 ? Math.cos(frame * 0.9) * shakeAmount * 0.5 : 0;

  // Falling arrows — panic crowd
  const fallingArrows = Array.from({ length: 8 }, (_, i) => ({
    x: 100 + i * 100,
    speed: 0.8 + seededRandom(i * 7) * 0.6,
    startFrame: i * 5,
    opacity: 0.5 + seededRandom(i * 3) * 0.4,
  }));

  // Long term arrow rise
  const longTermY = interpolate(frame, [40, 90], [0, -120], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.quad),
  });
  const longTermOp = interpolate(frame, [35, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Crowd figures
  const crowdFigures = Array.from({ length: 6 }, (_, i) => ({
    x: 80 + i * 140,
    panicPhase: seededRandom(i * 11) * Math.PI * 2,
  }));

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 19) * 1920,
    y: seededRandom(i * 11) * 1080,
    size: 4 + seededRandom(i) * 8,
    op: 0.03 + seededRandom(i * 7) * 0.04,
    speed: 0.15 + seededRandom(i * 5) * 0.3,
    falling: seededRandom(i * 13) > 0.5,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${shakeX}px, ${shakeY}px)`,
    }}>

      {/* === GRADIENT — RED TO BLACK PANIC FADE === */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 100% 80% at 50% 0%, rgba(180,0,0,${interpolate(frame, [0, 40], [0.15, 0.04], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}) 0%, #1C1C1C 60%)`,
      }} />
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 60% 60% at 80% 80%, rgba(233,215,0,${interpolate(frame, [30, 70], [0, 0.08], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}) 0%, transparent 70%)`,
      }} />

      {/* === CIRCULAR TEXTURE === */}
      {[200, 350, 500, 650].map((r, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 960 - r,
          top: 540 - r,
          width: r * 2,
          height: r * 2,
          borderRadius: '50%',
          border: `1px solid rgba(255,255,255,${0.02 + i * 0.005})`,
          transform: `rotate(${frame * (0.02 + i * 0.01)}deg)`,
        }} />
      ))}

      {/* === FALLING RED ARROWS (PANIC) === */}
      <div style={{ position: 'absolute', left: 80, top: 100, width: 900, height: 500 }}>
        {fallingArrows.map((arrow, i) => {
          const arrowY = ((frame * arrow.speed * 12 + arrow.startFrame * 40) % 500);
          return (
            <svg
              key={i}
              style={{
                position: 'absolute',
                left: arrow.x,
                top: arrowY,
                opacity: arrow.opacity * interpolate(frame, [0, 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
              width="40" height="60" viewBox="0 0 40 60"
            >
              <line x1="20" y1="0" x2="20" y2="45" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
              <polyline points="8,35 20,55 32,35" fill="none" stroke="#ff0000" strokeWidth="3" strokeLinejoin="round" />
            </svg>
          );
        })}
        {/* PANIC label */}
        <div style={{
          position: 'absolute',
          top: 10,
          left: 10,
          fontFamily: 'Inter',
          fontSize: 32,
          fontWeight: 900,
          color: '#ff0000',
          letterSpacing: 6,
          opacity: 0.6,
          textTransform: 'uppercase',
        }}>PANIC SELL</div>
      </div>

      {/* === CROWD FIGURES — RUNNING === */}
      <div style={{ position: 'absolute', left: 0, bottom: 160 }}>
        {crowdFigures.map((fig, i) => (
          <svg
            key={i}
            style={{
              position: 'absolute',
              left: fig.x,
              bottom: 0,
              opacity: interpolate(frame, [i * 4, i * 4 + 12], [0, 0.55], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `scaleX(-1) translateX(${Math.sin(frame * 0.15 + fig.panicPhase) * 6}px)`,
            }}
            width="80" height="160" viewBox="0 0 80 160"
          >
            <circle cx="40" cy="20" r="16" fill="none" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="40" y1="36" x2="40" y2="100" stroke="#ff0000" strokeWidth="3" />
            <line x1="40" y1="60" x2="15" y2="85" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="40" y1="60" x2="65" y2="80" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="40" y1="100" x2={20 + Math.sin(frame * 0.2 + fig.panicPhase) * 12} y2="145" stroke="#ff0000" strokeWidth="2.5" />
            <line x1="40" y1="100" x2={60 - Math.sin(frame * 0.2 + fig.panicPhase) * 12} y2="145" stroke="#ff0000" strokeWidth="2.5" />
          </svg>
        ))}
      </div>

      {/* === DIVIDER === */}
      <div style={{
        position: 'absolute',
        left: 980,
        top: 80,
        width: 3,
        height: 920,
        background: 'rgba(233,215,0,0.2)',
        boxShadow: '0 0 15px rgba(233,215,0,0.2)',
        transform: `scaleY(${sp(20)})`,
        transformOrigin: 'top',
      }} />

      {/* === RIGHT SIDE — LONG TERM CALM === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 120,
        width: 860,
        opacity: longTermOp,
      }}>
        {/* Title */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 28,
          fontWeight: 700,
          color: '#e9d700',
          letterSpacing: 5,
          textTransform: 'uppercase',
          marginBottom: 30,
          opacity: 0.7,
        }}>LONG TERM VISION</div>

        {/* Rising arrow */}
        <svg width="700" height="300" viewBox="0 0 700 300" style={{ transform: `translateY(${longTermY}px)` }}>
          <defs>
            <linearGradient id="riseGrad" x1="0" y1="1" x2="1" y2="0">
              <stop offset="0%" stopColor="#e9d700" stopOpacity="0.3" />
              <stop offset="100%" stopColor="#e9d700" stopOpacity="0.9" />
            </linearGradient>
          </defs>
          {/* Steady upward line */}
          <path d="M 50 280 Q 200 260 350 200 Q 500 140 650 40" fill="none" stroke="url(#riseGrad)" strokeWidth="5" strokeLinecap="round" />
          {/* Arrow head */}
          <polyline points="630,25 660,45 640,70" fill="none" stroke="#e9d700" strokeWidth="4" strokeLinejoin="round" />
          {/* Checkpoint dots */}
          {[[200, 220], [400, 155], [600, 60]].map(([x, y], i) => (
            <g key={i}>
              <circle cx={x} cy={y} r="8" fill="#e9d700" opacity={0.7} />
              <circle cx={x} cy={y} r={8 + Math.sin(frame * 0.06 + i) * 5} fill="none" stroke="#e9d700" strokeWidth="1.5" opacity="0.3" />
            </g>
          ))}
        </svg>

        {/* Main text */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 100,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 1,
          letterSpacing: -2,
          marginTop: 20,
        }}>शांति से</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 52,
          color: '#F5F5F5',
          marginTop: 12,
          lineHeight: 1.5,
        }}>टिके रहते हैं</div>

        {/* Calm investor figure */}
        <svg
          style={{
            position: 'absolute',
            right: 40,
            bottom: -80,
            transform: `translateY(${Math.sin(frame * 0.04) * 3}px)`,
          }}
          width="150" height="280" viewBox="0 0 150 280"
        >
          <ellipse cx="75" cy="272" rx="48" ry="9" fill="rgba(233,215,0,0.18)" />
          <circle cx="75" cy="40" r="26" fill="none" stroke="#F5F5F5" strokeWidth="4" />
          <rect x="60" y="33" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <rect x="76" y="33" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="72" y1="37" x2="76" y2="37" stroke="#e9d700" strokeWidth="2" />
          <line x1="75" y1="66" x2="75" y2="170" stroke="#F5F5F5" strokeWidth="5" />
          <line x1="75" y1="110" x2="45" y2="150" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="75" y1="110" x2="105" y2="150" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="75" y1="170" x2="52" y2="250" stroke="#F5F5F5" strokeWidth="4" />
          <line x1="75" y1="170" x2="98" y2="250" stroke="#F5F5F5" strokeWidth="4" />
          {/* Sitting on chair */}
          <rect x="30" y="195" width="90" height="8" rx="4" fill="#2D2D2D" stroke="#e9d700" strokeWidth="1.5" opacity="0.6" />
        </svg>
      </div>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: p.falling
            ? (p.y + frame * p.speed * 25) % 1080
            : (p.y - frame * p.speed * 15 + 1080) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(${p.falling ? '255,0,0' : '233,215,0'},${p.op})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 85% at 50% 50%, transparent 45%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S4_PanicVsPatience;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S5_SilentSIP',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S5_SilentSIP: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow upward camera tilt
  const tiltY = interpolate(frame, [0, durationInFrames], [0, -20], { extrapolateRight: 'clamp' });

  // SIP counter growing
  const sipMultiplier = interpolate(frame, [30, 90], [1, 2.34], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Party noise — random bubbles
  const partyBubbles = Array.from({ length: 12 }, (_, i) => ({
    x: 1100 + seededRandom(i * 7) * 700,
    y: 100 + seededRandom(i * 13) * 700,
    size: 20 + seededRandom(i * 3) * 40,
    phase: seededRandom(i * 17) * Math.PI * 2,
    opacity: 0.06 + seededRandom(i * 23) * 0.06,
  }));

  // Coins raining
  const coins = Array.from({ length: 10 }, (_, i) => ({
    x: 100 + seededRandom(i * 41) * 800,
    startY: -50 - seededRandom(i * 29) * 200,
    speed: 1.2 + seededRandom(i * 17) * 0.8,
    delay: seededRandom(i * 53) * 60,
    size: 24 + seededRandom(i * 7) * 16,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translateY(${tiltY}px)`,
    }}>

      {/* === GRADIENT === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 60% 70% at 25% 60%, #0d1a10 0%, #1C1C1C 55%, #0a0d1a 100%)',
      }} />

      {/* === DOT GRID TEXTURE === */}
      <AbsoluteFill>
        {Array.from({ length: 40 }, (_, col) =>
          Array.from({ length: 22 }, (_, row) => (
            <div key={`${col}-${row}`} style={{
              position: 'absolute',
              left: col * 48 + 24,
              top: row * 48 + 24,
              width: 2,
              height: 2,
              borderRadius: '50%',
              background: 'rgba(255,255,255,0.04)',
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        left: -150,
        top: 200,
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.06)',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.02}) rotate(${frame * 0.03}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        right: 400,
        bottom: -200,
        width: 700,
        height: 700,
        background: 'rgba(233,215,0,0.025)',
        transform: `rotate(${30 + frame * 0.02}deg)`,
        clipPath: 'polygon(50% 0%, 0% 100%, 100% 100%)',
      }} />

      {/* === LEFT SIDE: SIP SILENT GROWTH === */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 160,
      }}>
        {/* SIP tag */}
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 16,
          background: 'rgba(233,215,0,0.1)',
          border: '1px solid rgba(233,215,0,0.25)',
          borderRadius: 12,
          padding: '12px 30px',
          marginBottom: 30,
          opacity: sp(8),
          transform: `translateY(${interpolate(sp(8), [0, 1], [-20, 0])}px)`,
        }}>
          {/* Rupee SVG icon */}
          <svg width="36" height="36" viewBox="0 0 36 36">
            <text x="6" y="27" fontFamily="Inter" fontSize="28" fontWeight="900" fill="#e9d700">₹</text>
          </svg>
          <span style={{ fontFamily: 'JetBrains Mono', fontSize: 30, color: '#e9d700', fontWeight: 700, letterSpacing: 4 }}>SIP</span>
        </div>

        {/* Growing number */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 140,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          opacity: sp(15),
          letterSpacing: -4,
        }}>
          {(sipMultiplier).toFixed(2)}x
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 90,
          fontWeight: 900,
          color: '#e9d700',
          marginTop: 8,
          opacity: sp(20),
          transform: `translateX(${interpolate(sp(20), [0, 1], [-30, 0])}px)`,
        }}>खामोशी में</div>

        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 52,
          color: '#9E9E9E',
          marginTop: 16,
          lineHeight: 1.6,
          opacity: sp(30),
          maxWidth: 700,
        }}>पार्टी में चर्चा नहीं —<br />लेकिन रात को भी<br />पैसा बना रही है।</div>

        {/* Progress bar */}
        <div style={{
          marginTop: 40,
          opacity: sp(35),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 3,
            marginBottom: 10,
          }}>COMPOUNDING PROGRESS</div>
          <div style={{
            width: 700,
            height: 10,
            background: 'rgba(255,255,255,0.06)',
            borderRadius: 5,
            overflow: 'hidden',
          }}>
            <div style={{
              width: `${interpolate(sipMultiplier, [1, 2.34], [0, 100], { extrapolateRight: 'clamp' })}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #1C8C2C, #e9d700)',
              borderRadius: 5,
              boxShadow: '0 0 15px rgba(233,215,0,0.4)',
            }} />
          </div>
        </div>
      </div>

      {/* === RIGHT SIDE: PARTY NOISE (MUTED) === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 100,
        width: 780,
        height: 860,
        opacity: 0.35,
      }}>
        {/* Party scene box */}
        <div style={{
          border: '1px solid rgba(255,255,255,0.06)',
          borderRadius: 20,
          padding: 40,
          height: '100%',
          position: 'relative',
          overflow: 'hidden',
        }}>
          {/* PARTY label */}
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 5,
            textTransform: 'uppercase',
            marginBottom: 30,
          }}>PARTY TALK</div>

          {/* Chat bubbles */}
          {[
            { text: 'मेरी नई BMW देखी?', x: 40, y: 80, w: 300 },
            { text: 'Crypto में डाला ₹5L!', x: 180, y: 200, w: 340 },
            { text: 'विदेश घूम के आया', x: 60, y: 320, w: 280 },
            { text: 'Latest iPhone ले लिया', x: 200, y: 440, w: 320 },
          ].map((bubble, i) => (
            <div key={i} style={{
              position: 'absolute',
              left: bubble.x,
              top: bubble.y,
              background: 'rgba(255,255,255,0.06)',
              borderRadius: 12,
              padding: '14px 20px',
              maxWidth: bubble.w,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 28,
              color: '#9E9E9E',
              transform: `translateY(${Math.sin(frame * 0.05 + i * 0.8) * 3}px)`,
            }}>
              {bubble.text}
              {/* Bubble tail */}
              <div style={{
                position: 'absolute',
                bottom: -10,
                left: 20,
                width: 0,
                height: 0,
                borderLeft: '10px solid transparent',
                borderRight: '10px solid transparent',
                borderTop: '10px solid rgba(255,255,255,0.06)',
              }} />
            </div>
          ))}

          {/* MUTE overlay */}
          <div style={{
            position: 'absolute',
            inset: 0,
            background: 'rgba(28,28,28,0.3)',
            borderRadius: 20,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}>
            {/* Mute icon SVG */}
            <svg width="80" height="80" viewBox="0 0 80 80" opacity={0.4}>
              <circle cx="40" cy="40" r="38" fill="none" stroke="#9E9E9E" strokeWidth="3" />
              <line x1="15" y1="15" x2="65" y2="65" stroke="#9E9E9E" strokeWidth="3" />
              <text x="18" y="50" fontFamily="Inter" fontSize="30" fill="#9E9E9E">🔇</text>
            </svg>
          </div>
        </div>
      </div>

      {/* === RAINING COINS === */}
      {coins.map((coin, i) => {
        const y = (coin.startY + (Math.max(0, frame - coin.delay)) * coin.speed * 8) % 1200;
        if (y < -60 || y > 1150) return null;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: coin.x,
            top: y,
            width: coin.size,
            height: coin.size,
            borderRadius: '50%',
            background: 'radial-gradient(circle at 35% 35%, #FFC107, #e9d700, #b8920a)',
            boxShadow: '0 0 8px rgba(233,215,0,0.3)',
            opacity: 0.6,
            transform: `rotate(${frame * 3 + i * 40}deg)`,
          }} />
        );
      })}

      {/* === STICK FIGURE — READING QUIETLY === */}
      <svg
        style={{
          position: 'absolute',
          left: 100,
          bottom: 60,
          opacity: sp(45),
          transform: `translateY(${Math.sin(frame * 0.04) * 3}px)`,
        }}
        width="160" height="280" viewBox="0 0 160 280"
      >
        <ellipse cx="80" cy="272" rx="50" ry="10" fill="rgba(233,215,0,0.15)" />
        <circle cx="80" cy="44" r="26" fill="none" stroke="#F5F5F5" strokeWidth="4" />
        <rect x="65" y="37" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <rect x="81" y="37" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <line x1="77" y1="41" x2="81" y2="41" stroke="#e9d700" strokeWidth="2" />
        {/* Book/phone in hand */}
        <rect x="100" y="120" width="40" height="50" rx="6" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
        <line x1="108" y1="132" x2="132" y2="132" stroke="#e9d700" strokeWidth="1.5" opacity="0.6" />
        <line x1="108" y1="142" x2="132" y2="142" stroke="#e9d700" strokeWidth="1.5" opacity="0.6" />
        <line x1="108" y1="152" x2="122" y2="152" stroke="#e9d700" strokeWidth="1.5" opacity="0.6" />
        <line x1="80" y1="68" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="5" />
        <line x1="80" y1="110" x2="50" y2="135" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="80" y1="110" x2="110" y2="130" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="80" y1="175" x2="58" y2="252" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="80" y1="175" x2="102" y2="252" stroke="#F5F5F5" strokeWidth="4" />
      </svg>

      {/* === PARTY BUBBLES === */}
      {partyBubbles.map((b, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: b.x,
          top: b.y + Math.sin(frame * 0.05 + b.phase) * 8,
          width: b.size,
          height: b.size,
          borderRadius: '50%',
          background: `rgba(255,255,255,${b.opacity})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 85% at 50% 50%, transparent 50%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S5_SilentSIP;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S6_MoneyWhileSleep',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S6_MoneyWhileSleep: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Night scene — slow drift
  const driftX = interpolate(frame, [0, durationInFrames], [0, -30], { extrapolateRight: 'clamp' });
  const driftY = interpolate(frame, [0, durationInFrames], [0, -15], { extrapolateRight: 'clamp' });

  // Stars twinkle
  const stars = Array.from({ length: 60 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 13) * 600,
    size: 1 + seededRandom(i * 3) * 3,
    phase: seededRandom(i * 17) * Math.PI * 2,
    speed: 0.03 + seededRandom(i * 23) * 0.06,
  }));

  // Counter ticking while sleeping
  const moneyCount = interpolate(frame, [20, 90], [10000, 14782], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Sleep breathing
  const breathe = Math.sin(frame * 0.06) * 4;
  const zFloat = Math.sin(frame * 0.08);

  // Moon glow pulse
  const moonGlow = 60 + Math.sin(frame * 0.04) * 10;

  return (
    <AbsoluteFill style={{
      backgroundColor: '#0a0a18',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translate(${driftX}px, ${driftY}px)`,
    }}>

      {/* === NIGHT SKY GRADIENT === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 80% 60% at 70% 20%, #1a1a3a 0%, #0a0a18 50%, #050510 100%)',
      }} />
      {/* Ground glow — warm */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 100% 40% at 50% 100%, rgba(233,215,0,0.06) 0%, transparent 60%)',
      }} />

      {/* === STARS === */}
      {stars.map((star, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: star.x,
          top: star.y,
          width: star.size,
          height: star.size,
          borderRadius: '50%',
          background: '#F5F5F5',
          opacity: 0.3 + Math.sin(frame * star.speed + star.phase) * 0.3,
        }} />
      ))}

      {/* === MOON === */}
      <div style={{
        position: 'absolute',
        right: 220,
        top: 80,
        opacity: sp(5),
      }}>
        <div style={{
          width: 120,
          height: 120,
          borderRadius: '50%',
          background: 'radial-gradient(circle at 35% 35%, #FFF8DC, #FFC107)',
          boxShadow: `0 0 ${moonGlow}px rgba(255,193,7,0.4)`,
        }} />
        {/* Moon glow rings */}
        {[160, 210, 270].map((r, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: 60 - r / 2,
            top: 60 - r / 2,
            width: r,
            height: r,
            borderRadius: '50%',
            border: `1px solid rgba(255,193,7,${0.08 - i * 0.02})`,
            transform: `scale(${1 + Math.sin(frame * 0.03 + i) * 0.02})`,
          }} />
        ))}
      </div>

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        left: 300,
        top: -80,
        width: 800,
        height: 3,
        background: 'rgba(233,215,0,0.06)',
        transform: `rotate(${-8 + Math.sin(frame * 0.02) * 1}deg)`,
        transformOrigin: 'left center',
      }} />

      {/* === SLEEPING SCENE — LEFT === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 300,
        opacity: sp(8),
        transform: `translateY(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
      }}>
        {/* Bed */}
        <svg width="500" height="280" viewBox="0 0 500 280">
          {/* Bed frame */}
          <rect x="20" y="160" width="460" height="80" rx="10" fill="#2D2D2D" stroke="rgba(255,255,255,0.08)" strokeWidth="2" />
          {/* Pillow */}
          <rect x="40" y="135" width="160" height="50" rx="20" fill="#3a3a4a" stroke="rgba(255,255,255,0.06)" strokeWidth="1.5" />
          {/* Blanket */}
          <path d="M 200 160 Q 360 150 460 165 L 460 200 Q 360 185 200 195 Z" fill="#1a2a3a" stroke="rgba(255,255,255,0.05)" strokeWidth="1" />

          {/* Sleeping stick figure */}
          <g transform={`translate(0, ${breathe * 0.3})`}>
            {/* Head on pillow */}
            <circle cx="100" cy="125" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Glasses */}
            <rect x="85" y="119" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="100" y="119" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="96" y1="122" x2="100" y2="122" stroke="#e9d700" strokeWidth="1.5" />
            {/* Closed eyes */}
            <path d="M87 122 Q92 118 97 122" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
            <path d="M102 122 Q107 118 112 122" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
            {/* Body horizontal */}
            <line x1="124" y1="145" x2="340" y2="155" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" />
            <line x1="200" y1="148" x2="220" y2="128" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          </g>

          {/* ZZZ floating */}
          {['z', 'z', 'Z'].map((z, i) => (
            <text
              key={i}
              x={155 + i * 28}
              y={110 - i * 25 + zFloat * (5 + i * 3)}
              fontFamily="Inter"
              fontSize={18 + i * 8}
              fontWeight="900"
              fill="#9E9E9E"
              opacity={0.4 + Math.sin(frame * 0.06 + i) * 0.2}
            >{z}</text>
          ))}
        </svg>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 80,
          fontWeight: 900,
          color: '#F5F5F5',
          marginTop: 24,
          lineHeight: 1,
          letterSpacing: -2,
        }}>चैन की नींद</div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 46,
          color: '#9E9E9E',
          marginTop: 12,
          opacity: sp(20),
        }}>आप सो रहे हैं और...</div>
      </div>

      {/* === RIGHT SIDE: MONEY WORKING === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 180,
        width: 760,
        opacity: sp(25),
        transform: `translateY(${interpolate(sp(25), [0, 1], [50, 0])}px)`,
      }}>
        {/* Working machine */}
        <div style={{
          background: 'rgba(233,215,0,0.07)',
          border: '1px solid rgba(233,215,0,0.2)',
          borderRadius: 20,
          padding: '40px 50px',
          marginBottom: 30,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#9E9E9E',
            letterSpacing: 4,
            marginBottom: 16,
            textTransform: 'uppercase',
          }}>LIVE PORTFOLIO</div>

          {/* Ticker */}
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 100,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            ₹{Math.floor(moneyCount).toLocaleString('en-IN')}
          </div>

          {/* Blinking cursor */}
          <span style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 100,
            color: '#e9d700',
            opacity: Math.sin(frame * 0.2) > 0 ? 1 : 0,
          }}>_</span>

          {/* Growth indicator */}
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
            marginTop: 20,
          }}>
            <svg width="28" height="28" viewBox="0 0 28 28">
              <polyline points="4,22 14,8 24,14 28,4" fill="none" stroke="#1C8C2C" strokeWidth="3" strokeLinejoin="round" />
            </svg>
            <span style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 36,
              color: '#1C8C2C',
              fontWeight: 700,
            }}>+{interpolate(moneyCount, [10000, 14782], [0, 47.82], { extrapolateRight: 'clamp' }).toFixed(2)}%</span>
          </div>
        </div>

        {/* Status */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 16,
        }}>
          {/* Pulsing green dot */}
          <div style={{
            width: 16,
            height: 16,
            borderRadius: '50%',
            background: '#1C8C2C',
            boxShadow: `0 0 ${8 + Math.sin(frame * 0.12) * 6}px #1C8C2C`,
          }} />
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#9E9E9E',
            letterSpacing: 3,
          }}>COMPOUNDING ACTIVE 24/7</div>
        </div>

        {/* Big text */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 80,
          fontWeight: 900,
          color: '#e9d700',
          marginTop: 30,
          lineHeight: 1.1,
          letterSpacing: -1,
        }}>पैसा काम<br />कर रहा है।</div>
      </div>

      {/* === FLOATING PARTICLES — COINS RISING === */}
      {Array.from({ length: 12 }, (_, i) => {
        const x = 900 + seededRandom(i * 19) * 700;
        const startY = 1100;
        const y = startY - ((frame * (0.8 + seededRandom(i * 7) * 0.6) + seededRandom(i * 31) * 200) % 1200);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 12 + seededRandom(i * 3) * 8,
            height: 12 + seededRandom(i * 3) * 8,
            borderRadius: '50%',
            background: 'rgba(233,215,0,0.2)',
            opacity: 0.4 + seededRandom(i * 11) * 0.3,
          }} />
        );
      })}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 85% at 50% 50%, transparent 45%, rgba(0,0,10,0.75) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S6_MoneyWhileSleep;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S7_CarVsAsset',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S7_CarVsAsset: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big reveal slam
  const slamScale = sp(5, { stiffness: 200, damping: 15, mass: 0.6 });
  const slamOp = interpolate(frame, [3, 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Car depreciation bar
  const deprBar = interpolate(sp(30), [0, 1], [100, 37]);
  // Asset growth bar
  const assetBar = interpolate(sp(40), [0, 1], [100, 168]);

  // Question mark pulse
  const qPulse = 1 + Math.sin(frame * 0.1) * 0.05;

  // Slow zoom out
  const zoomOut = interpolate(frame, [0, durationInFrames], [1.06, 1], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 23) * 1920,
    y: seededRandom(i * 37) * 1080,
    size: 5 + seededRandom(i * 7) * 10,
    speed: 0.2 + seededRandom(i * 13) * 0.4,
    op: 0.025 + seededRandom(i * 19) * 0.04,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `scale(${zoomOut})`,
    }}>

      {/* === GRADIENT === */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #1a0505 0%, #1C1C1C 40%, #051005 100%)',
      }} />

      {/* === DIAGONAL STRIPE TEXTURE === */}
      <AbsoluteFill style={{ overflow: 'hidden' }}>
        {Array.from({ length: 20 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            left: -200 + i * 130,
            top: 0,
            width: 2,
            height: 1530,
            background: 'rgba(255,255,255,0.02)',
            transform: 'rotate(-35deg)',
            transformOrigin: 'top',
          }} />
        ))}
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        top: 100,
        left: -100,
        width: 700,
        height: 700,
        borderRadius: '50%',
        border: '80px solid rgba(255,0,0,0.03)',
        transform: `rotate(${frame * 0.02}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: -100,
        right: 200,
        width: 400,
        height: 400,
        background: 'rgba(233,215,0,0.04)',
        transform: `rotate(${45 + frame * 0.04}deg)`,
        borderRadius: 40,
      }} />

      {/* === LEFT: LUXURY CAR PANEL === */}
      <div style={{
        position: 'absolute',
        left: 80,
        top: 120,
        width: 840,
        opacity: slamOp,
      }}>
        {/* Car SVG */}
        <svg
          width="420" height="220" viewBox="0 0 420 220"
          style={{ transform: `scale(${slamScale})`, transformOrigin: 'left center' }}
        >
          {/* Car body */}
          <path d="M30 140 L30 100 L90 60 L280 60 L360 100 L390 100 L390 140 Z"
            fill="#2D2D2D" stroke="rgba(255,255,255,0.1)" strokeWidth="2" />
          {/* Windshield */}
          <path d="M100 100 L90 65 L270 65 L280 100 Z" fill="rgba(100,160,220,0.2)" stroke="rgba(255,255,255,0.08)" strokeWidth="1" />
          {/* Wheels */}
          <circle cx="100" cy="145" r="32" fill="#1a1a1a" stroke="#e9d700" strokeWidth="3" />
          <circle cx="100" cy="145" r="18" fill="#2D2D2D" stroke="rgba(233,215,0,0.4)" strokeWidth="2" />
          <circle cx="320" cy="145" r="32" fill="#1a1a1a" stroke="#e9d700" strokeWidth="3" />
          <circle cx="320" cy="145" r="18" fill="#2D2D2D" stroke="rgba(233,215,0,0.4)" strokeWidth="2" />
          {/* Headlight */}
          <circle cx="378" cy="108" r="10" fill="#FFC107" opacity="0.7" />
          <line x1="388" y1="108" x2="418" y2="105" stroke="#FFC107" strokeWidth="2" opacity="0.5" />
          {/* Luxury label */}
          <text x="160" y="115" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#e9d700" textAnchor="middle">LUXURY</text>
        </svg>

        {/* Depreciation bar */}
        <div style={{ marginTop: 40 }}>
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#9E9E9E',
            marginBottom: 10,
            letterSpacing: 2,
          }}>
            <span>VALUE AFTER 5 YRS</span>
            <span style={{ color: '#ff0000' }}>{Math.round(deprBar)}%</span>
          </div>
          <div style={{ width: 700, height: 12, background: 'rgba(255,255,255,0.06)', borderRadius: 6, overflow: 'hidden' }}>
            <div style={{
              width: `${deprBar}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #ff0000, #ff4444)',
              borderRadius: 6,
              transition: 'width 0.1s',
            }} />
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            color: '#ff0000',
            marginTop: 16,
          }}>₹ घटता जाता है ↓</div>
        </div>

        {/* Question */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 160,
          fontWeight: 900,
          color: '#ff0000',
          opacity: sp(15) * 0.25,
          lineHeight: 1,
          letterSpacing: -4,
          transform: `scale(${qPulse})`,
          transformOrigin: 'left center',
          marginTop: 20,
        }}>?</div>
      </div>

      {/* === DIVIDER — VS === */}
      <div style={{
        position: 'absolute',
        left: 920,
        top: 0,
        width: 80,
        height: 1080,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 20,
      }}>
        <div style={{
          width: 2,
          height: 380,
          background: 'rgba(255,255,255,0.08)',
          transform: `scaleY(${sp(10)})`,
          transformOrigin: 'bottom',
        }} />
        <div style={{
          fontFamily: 'Inter',
          fontSize: 52,
          fontWeight: 900,
          color: '#9E9E9E',
          opacity: sp(15) * 0.5,
          letterSpacing: 2,
        }}>VS</div>
        <div style={{
          width: 2,
          height: 380,
          background: 'rgba(255,255,255,0.08)',
          transform: `scaleY(${sp(10)})`,
          transformOrigin: 'top',
        }} />
      </div>

      {/* === RIGHT: ASSET PANEL === */}
      <div style={{
        position: 'absolute',
        right: 80,
        top: 120,
        width: 840,
        opacity: sp(18),
        transform: `translateX(${interpolate(sp(18), [0, 1], [60, 0])}px)`,
      }}>
        {/* Asset icon — growing chart */}
        <svg width="420" height="220" viewBox="0 0 420 220">
          {/* Background rect */}
          <rect x="10" y="10" width="400" height="200" rx="16" fill="#0a1a0a" stroke="rgba(233,215,0,0.15)" strokeWidth="2" />
          {/* Growth chart */}
          <path d="M 30 180 Q 120 160 200 120 Q 280 80 380 30"
            fill="none" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
          {/* Fill */}
          <path d="M 30 180 Q 120 160 200 120 Q 280 80 380 30 L 380 195 L 30 195 Z"
            fill="rgba(233,215,0,0.08)" />
          {/* Dots */}
          {[[100, 162], [200, 120], [300, 70], [380, 30]].map(([x, y], i) => (
            <circle key={i} cx={x} cy={y} r="7" fill="#e9d700" />
          ))}
          <text x="200" y="100" fontFamily="JetBrains Mono" fontSize="24" fill="#e9d700" textAnchor="middle" fontWeight="700">INDEX FUND / SIP</text>
        </svg>

        {/* Asset growth bar */}
        <div style={{ marginTop: 40 }}>
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#9E9E9E',
            marginBottom: 10,
            letterSpacing: 2,
          }}>
            <span>VALUE AFTER 5 YRS</span>
            <span style={{ color: '#1C8C2C' }}>{Math.round(assetBar)}%</span>
          </div>
          <div style={{ width: 700, height: 12, background: 'rgba(255,255,255,0.06)', borderRadius: 6, overflow: 'hidden' }}>
            <div style={{
              width: `${Math.min(assetBar, 100)}%`,
              height: '100%',
              background: 'linear-gradient(90deg, #1C8C2C, #e9d700)',
              borderRadius: 6,
            }} />
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            color: '#1C8C2C',
            marginTop: 16,
          }}>₹ बढ़ता जाता है ↑</div>
        </div>

        {/* Kattva Sach */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 90,
          fontWeight: 900,
          color: '#e9d700',
          marginTop: 24,
          opacity: sp(45),
          lineHeight: 1,
          letterSpacing: -2,
        }}>कड़वा सच</div>
      </div>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y + frame * p.speed * 20) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(233,215,0,${p.op})`,
        }} />
      ))}

      {/* === STICK FIGURE — CONFUSED LOOK === */}
      <svg
        style={{
          position: 'absolute',
          left: 840,
          bottom: 60,
          opacity: sp(55),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px)`,
        }}
        width="80" height="200" viewBox="0 0 80 200"
      >
        <circle cx="40" cy="32" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
        <rect x="27" y="26" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
        <rect x="41" y="26" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
        <line x1="37" y1="29" x2="41" y2="29" stroke="#e9d700" strokeWidth="1.5" />
        {/* Question mark above head */}
        <text x="58" y="20" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.08) * 0.2}>?</text>
        <line x1="40" y1="54" x2="40" y2="135" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="40" y1="85" x2="18" y2={108 + Math.sin(frame * 0.06) * 6} stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="40" y1="85" x2="62" y2={108 - Math.sin(frame * 0.06) * 6} stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="40" y1="135" x2="22" y2="185" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="40" y1="135" x2="58" y2="185" stroke="#F5F5F5" strokeWidth="3.5" />
      </svg>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 92% 88% at 50% 50%, transparent 48%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S7_CarVsAsset;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S8_ThinkLikeRich',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S8_ThinkLikeRich: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Words cascade from top
  const words = [
    { text: 'आज', delay: 5, color: '#F5F5F5', size: 200 },
    { text: 'नहीं', delay: 18, color: '#ff0000', size: 160 },
    { text: '—', delay: 28, color: '#9E9E9E', size: 120 },
    { text: 'सोच', delay: 35, color: '#e9d700', size: 180 },
    { text: 'बदलो', delay: 48, color: '#e9d700', size: 180 },
  ];

  // Brain/lightbulb glow
  const bulbGlow = 40 + Math.sin(frame * 0.08) * 20;
  const bulbPulse = 1 + Math.sin(frame * 0.08) * 0.04;

  // Thought cloud bubbles
  const thoughtBubbles = [
    { text: 'क्या यह Asset है?', x: 1100, y: 180, delay: 35 },
    { text: 'Long Term?', x: 1300, y: 320, delay: 45 },
    { text: 'Compounding?', x: 1080, y: 470, delay: 55 },
    { text: 'Freedom?', x: 1350, y: 580, delay: 65 },
  ];

  // Orbit animation for rich mind symbols
  const orbitAngle = frame * 0.04;

  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 11) * 1920,
    y: seededRandom(i * 17) * 1080,
    size: 3 + seededRandom(i) * 8,
    speed: 0.1 + seededRandom(i * 7) * 0.35,
    op: 0.02 + seededRandom(i * 23) * 0.05,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT — DARK PURPLE / MIDNIGHT === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 70% 80% at 35% 50%, #140d2a 0%, #1C1C1C 55%, #0a1510 100%)',
      }} />

      {/* === CONCENTRIC CIRCLE TEXTURE === */}
      {[150, 300, 450, 600, 750, 900].map((r, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 480 - r,
          top: 540 - r,
          width: r * 2,
          height: r * 2,
          borderRadius: '50%',
          border: `1px solid rgba(233,215,0,${0.025 - i * 0.003})`,
          transform: `rotate(${frame * (0.01 + i * 0.005) * (i % 2 === 0 ? 1 : -1)}deg)`,
        }} />
      ))}

      {/* === SHAPE LAYER — TRIANGLE === */}
      <div style={{
        position: 'absolute',
        right: -100,
        top: -100,
        width: 600,
        height: 600,
        background: 'rgba(233,215,0,0.03)',
        clipPath: 'polygon(50% 0%, 0% 100%, 100% 100%)',
        transform: `rotate(${frame * 0.02}deg)`,
      }} />

      {/* === LEFT: WORD CASCADE === */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 100,
        width: 900,
      }}>
        {words.map((w, i) => (
          <div key={i} style={{
            fontFamily: 'Yantra One',
            fontSize: w.size,
            fontWeight: 900,
            color: w.color,
            lineHeight: 0.95,
            opacity: sp(w.delay),
            transform: `translateY(${interpolate(sp(w.delay), [0, 1], [50, 0])}px)`,
            letterSpacing: -4,
            display: 'inline',
            marginRight: 20,
            textShadow: w.color === '#e9d700' ? `0 0 60px rgba(233,215,0,0.3)` : 'none',
          }}>{w.text} </div>
        ))}
      </div>

      {/* === BRAIN / BULB ICON — CENTER === */}
      <div style={{
        position: 'absolute',
        left: 380,
        bottom: 120,
        transform: `scale(${bulbPulse})`,
        transformOrigin: 'center bottom',
        opacity: sp(50),
      }}>
        <svg width="200" height="260" viewBox="0 0 200 260">
          {/* Glow */}
          <ellipse cx="100" cy="110" rx={bulbGlow * 1.5} ry={bulbGlow * 1.5} fill="rgba(233,215,0,0.08)" />
          {/* Bulb */}
          <path d="M60 120 Q55 80 100 50 Q145 80 140 120 Q140 150 130 165 L70 165 Q60 150 60 120 Z"
            fill="none" stroke="#e9d700" strokeWidth="4" />
          {/* Filament */}
          <path d="M80 155 Q90 140 100 155 Q110 140 120 155" fill="none" stroke="#e9d700" strokeWidth="3" />
          {/* Base */}
          <rect x="75" y="165" width="50" height="12" rx="4" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          <rect x="80" y="177" width="40" height="10" rx="3" fill="#2D2D2D" stroke="#e9d700" strokeWidth="1.5" />
          {/* Inner glow */}
          <ellipse cx="100" cy="105" rx="25" ry="30" fill="rgba(233,215,0,0.15)" />
          {/* Rays */}
          {[0, 45, 90, 135, 180, 225, 270, 315].map((angle, i) => (
            <line
              key={i}
              x1={100 + Math.cos(angle * Math.PI / 180) * 45}
              y1={100 + Math.sin(angle * Math.PI / 180) * 45}
              x2={100 + Math.cos(angle * Math.PI / 180) * (55 + Math.sin(frame * 0.06 + i) * 5)}
              y2={100 + Math.sin(angle * Math.PI / 180) * (55 + Math.sin(frame * 0.06 + i) * 5)}
              stroke="#e9d700"
              strokeWidth="2"
              opacity="0.3"
            />
          ))}
        </svg>
      </div>

      {/* === THOUGHT BUBBLES — RIGHT === */}
      {thoughtBubbles.map((bubble, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: bubble.x,
          top: bubble.y + Math.sin(frame * 0.05 + i * 0.7) * 6,
          background: 'rgba(233,215,0,0.08)',
          border: '1px solid rgba(233,215,0,0.2)',
          borderRadius: 50,
          padding: '16px 30px',
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 36,
          color: '#F5F5F5',
          whiteSpace: 'nowrap',
          opacity: sp(bubble.delay) * 0.85,
          transform: `scale(${sp(bubble.delay)})`,
          transformOrigin: 'left center',
        }}>
          {bubble.text}
        </div>
      ))}

      {/* === ORBITING SYMBOLS AROUND BULB === */}
      {['₹', '%', '↑', '∞'].map((sym, i) => {
        const angle = orbitAngle + i * (Math.PI / 2);
        const cx = 480;
        const cy = 810;
        const rx = 120;
        const ry = 60;
        const x = cx + Math.cos(angle) * rx;
        const y = cy + Math.sin(angle) * ry;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x - 20,
            top: y - 20,
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 900,
            color: '#e9d700',
            opacity: sp(55) * (0.3 + Math.abs(Math.sin(angle)) * 0.4),
          }}>{sym}</div>
        );
      })}

      {/* === STICK FIGURE — THINKING POSE === */}
      <svg
        style={{
          position: 'absolute',
          right: 120,
          bottom: 80,
          opacity: sp(40),
          transform: `translateY(${Math.sin(frame * 0.04) * 3}px)`,
        }}
        width="160" height="300" viewBox="0 0 160 300"
      >
        <ellipse cx="80" cy="292" rx="52" ry="10" fill="rgba(233,215,0,0.15)" />
        {/* Head tilted thinking */}
        <circle cx={80 + Math.sin(frame * 0.04) * 2} cy="44" r="28" fill="none" stroke="#F5F5F5" strokeWidth="4" />
        <rect x="64" y="37" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <rect x="80" y="37" width="12" height="8" rx="3.5" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <line x1="76" y1="41" x2="80" y2="41" stroke="#e9d700" strokeWidth="2" />
        {/* Hand on chin */}
        <line x1="80" y1="72" x2="80" y2="175" stroke="#F5F5F5" strokeWidth="5" />
        <line x1="80" y1="110" x2="48" y2="145" stroke="#F5F5F5" strokeWidth="4" />
        {/* Right arm — hand at chin */}
        <line x1="80" y1="105" x2="108" y2="120" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="108" y1="120" x2="100" y2="75" stroke="#F5F5F5" strokeWidth="3.5" />
        <circle cx="100" cy="75" r="6" fill="#e9d700" />
        <line x1="80" y1="175" x2="55" y2="255" stroke="#F5F5F5" strokeWidth="4" />
        <line x1="80" y1="175" x2="105" y2="255" stroke="#F5F5F5" strokeWidth="4" />
        {/* Thought dots */}
        {[0, 1, 2].map(i => (
          <circle
            key={i}
            cx={115 + i * 14}
            cy={60 - i * 16}
            r={4 + i * 2}
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity={0.4 + Math.sin(frame * 0.08 + i) * 0.2}
          />
        ))}
      </svg>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y - frame * p.speed * 15 + 1080) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(233,215,0,${p.op})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 88% 85% at 50% 50%, transparent 48%, rgba(0,0,0,0.68) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S8_ThinkLikeRich;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S9_HabitsBeforeWealth',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S9_HabitsBeforeWealth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline line drawing
  const timelineProgress = interpolate(frame, [10, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Habit items stagger
  const habits = [
    { label: 'दबाव में शांत रहना', icon: '🧘', delay: 20, color: '#e9d700' },
    { label: 'बिना दर्शक खर्च संभालना', icon: '👁', delay: 35, color: '#FFC107' },
    { label: 'आमदनी बढ़े तो संयम', icon: '📈', delay: 50, color: '#e9d700' },
    { label: 'खुद के लिए जीना', icon: '🎯', delay: 65, color: '#FFC107' },
  ];

  // Camera slow left pan
  const panX = interpolate(frame, [0, durationInFrames], [0, -40], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 29) * 1920,
    y: seededRandom(i * 41) * 1080,
    size: 4 + seededRandom(i) * 9,
    speed: 0.15 + seededRandom(i * 13) * 0.35,
    op: 0.025 + seededRandom(i * 7) * 0.045,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `translateX(${panX}px)`,
    }}>

      {/* === GRADIENT === */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, #0a0a1a 0%, #1C1C1C 40%, #1a1000 100%)',
      }} />

      {/* === HORIZONTAL LINE TEXTURE === */}
      <AbsoluteFill>
        {Array.from({ length: 18 }, (_, i) => (
          <div key={i} style={{
            position: 'absolute',
            top: 60 + i * 60,
            left: 0,
            width: 1920,
            height: 1,
            background: 'rgba(255,255,255,0.02)',
          }} />
        ))}
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        left: -200,
        bottom: -200,
        width: 800,
        height: 800,
        borderRadius: '50%',
        border: '60px solid rgba(233,215,0,0.03)',
        transform: `rotate(${frame * 0.025}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        right: -100,
        top: 200,
        width: 500,
        height: 500,
        background: 'rgba(255,193,7,0.03)',
        clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
        transform: `rotate(${frame * 0.03}deg)`,
      }} />

      {/* === LEFT: BIG HEADLINE === */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 120,
        width: 820,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#9E9E9E',
          letterSpacing: 5,
          textTransform: 'uppercase',
          marginBottom: 20,
          opacity: sp(5),
        }}>CHAPTER 3 — KEY INSIGHT</div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 160,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 0.9,
          opacity: sp(8),
          transform: `translateY(${interpolate(sp(8), [0, 1], [60, 0])}px)`,
          letterSpacing: -5,
          textShadow: '0 0 100px rgba(233,215,0,0.25)',
        }}>आदतें</div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 100,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          opacity: sp(16),
          transform: `translateY(${interpolate(sp(16), [0, 1], [40, 0])}px)`,
          letterSpacing: -3,
          marginTop: 8,
        }}>पहले आती हैं</div>

        <div style={{
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 50,
          color: '#9E9E9E',
          marginTop: 24,
          lineHeight: 1.6,
          opacity: sp(24),
          maxWidth: 760,
        }}>पैसे से बहुत पहले —<br />जब कोई नहीं देख रहा।</div>
      </div>

      {/* === VERTICAL TIMELINE — RIGHT === */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: 80,
        width: 820,
      }}>
        {/* Timeline vertical bar */}
        <div style={{
          position: 'absolute',
          left: 40,
          top: 30,
          width: 4,
          height: 880,
          background: 'rgba(233,215,0,0.1)',
          borderRadius: 2,
          overflow: 'hidden',
        }}>
          <div style={{
            width: '100%',
            height: `${timelineProgress * 100}%`,
            background: 'linear-gradient(180deg, #e9d700, #FFC107)',
            borderRadius: 2,
            boxShadow: '0 0 10px rgba(233,215,0,0.5)',
          }} />
        </div>

        {/* Timeline start label */}
        <div style={{
          position: 'absolute',
          left: 0,
          top: 10,
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#9E9E9E',
          letterSpacing: 3,
          opacity: 0.5,
        }}>TODAY</div>

        {/* Habit items */}
        {habits.map((habit, i) => {
          const itemOp = sp(habit.delay);
          const dotY = 60 + i * 200;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: 0,
              top: dotY,
              opacity: itemOp,
              transform: `translateX(${interpolate(itemOp, [0, 1], [30, 0])}px)`,
            }}>
              {/* Timeline dot */}
              <div style={{
                position: 'absolute',
                left: 28,
                top: 12,
                width: 24,
                height: 24,
                borderRadius: '50%',
                background: habit.color,
                boxShadow: `0 0 15px ${habit.color}66`,
              }} />
              {/* Content card */}
              <div style={{
                marginLeft: 80,
                background: 'rgba(255,255,255,0.04)',
                border: `1px solid rgba(233,215,0,0.12)`,
                borderRadius: 16,
                padding: '20px 28px',
                display: 'flex',
                alignItems: 'center',
                gap: 20,
              }}>
                {/* SVG icon instead of emoji */}
                <div style={{
                  width: 48,
                  height: 48,
                  borderRadius: '50%',
                  background: `rgba(233,215,0,0.12)`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  flexShrink: 0,
                }}>
                  {i === 0 && (
                    <svg width="28" height="28" viewBox="0 0 28 28">
                      <circle cx="14" cy="14" r="12" fill="none" stroke="#e9d700" strokeWidth="2.5" />
                      <path d="M8 14 Q14 8 20 14 Q14 20 8 14 Z" fill="rgba(233,215,0,0.3)" />
                    </svg>
                  )}
                  {i === 1 && (
                    <svg width="28" height="28" viewBox="0 0 28 28">
                      <ellipse cx="14" cy="14" rx="12" ry="7" fill="none" stroke="#e9d700" strokeWidth="2.5" />
                      <circle cx="14" cy="14" r="4" fill="#e9d700" />
                    </svg>
                  )}
                  {i === 2 && (
                    <svg width="28" height="28" viewBox="0 0 28 28">
                      <polyline points="4,22 10,14 17,18 24,6" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinejoin="round" />
                    </svg>
                  )}
                  {i === 3 && (
                    <svg width="28" height="28" viewBox="0 0 28 28">
                      <circle cx="14" cy="14" r="11" fill="none" stroke="#e9d700" strokeWidth="2.5" />
                      <circle cx="14" cy="14" r="4" fill="#e9d700" />
                      <line x1="14" y1="3" x2="14" y2="7" stroke="#e9d700" strokeWidth="2" />
                      <line x1="14" y1="21" x2="14" y2="25" stroke="#e9d700" strokeWidth="2" />
                    </svg>
                  )}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 40,
                  color: '#F5F5F5',
                  fontWeight: 700,
                }}>{habit.label}</div>
              </div>
            </div>
          );
        })}

        {/* Timeline end */}
        <div style={{
          position: 'absolute',
          left: 0,
          bottom: 30,
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          color: '#e9d700',
          letterSpacing: 3,
          opacity: interpolate(timelineProgress, [0.8, 1], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>WEALTH →</div>
      </div>

      {/* === STICK FIGURE — WALKING FORWARD === */}
      <svg
        style={{
          position: 'absolute',
          left: 120,
          bottom: 60,
          opacity: sp(70),
          transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
        }}
        width="140" height="260" viewBox="0 0 140 260"
      >
        <ellipse cx="70" cy="252" rx="44" ry="9" fill="rgba(233,215,0,0.15)" />
        <circle cx="70" cy="38" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
        <rect x="57" y="32" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <rect x="71" y="32" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.5" />
        <line x1="67" y1="35" x2="71" y2="35" stroke="#e9d700" strokeWidth="2" />
        <line x1="70" y1="62" x2="70" y2="155" stroke="#F5F5F5" strokeWidth="4.5" />
        <line x1="70" y1="95" x2="44" y2="125" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="70" y1="95" x2="96" y2="125" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="70" y1="155" x2={48 + Math.sin(frame * 0.1) * 10} y2="232" stroke="#F5F5F5" strokeWidth="3.5" />
        <line x1="70" y1="155" x2={92 - Math.sin(frame * 0.1) * 10} y2="232" stroke="#F5F5F5" strokeWidth="3.5" />
        {/* Arrow pointing forward */}
        <polyline points="95,95 115,82 95,70" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinejoin="round" />
      </svg>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x,
          top: (p.y - frame * p.speed * 18 + 1080) % 1080,
          width: p.size,
          height: p.size,
          borderRadius: '50%',
          background: `rgba(233,215,0,${p.op})`,
        }} />
      ))}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 90% 85% at 50% 50%, transparent 48%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S9_HabitsBeforeWealth;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S10_ChangeTheGame',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S10_ChangeTheGame: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Dramatic zoom in
  const heroZoom = interpolate(frame, [0, 60], [1.1, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Two-choice flip
  const choice1Op = sp(30);
  const choice2Op = sp(50);

  // Big question pulse
  const questionPulse = 1 + Math.sin(frame * 0.07) * 0.03;

  // Mirror effect — two worlds
  const leftWorld = 'PERFORMANCE'; // for others
  const rightWorld = 'FREEDOM'; // for self

  // Final glow
  const finalGlow = interpolate(frame, [80, 110], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Rising particles
  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    startY: 1080 + seededRandom(i * 7) * 300,
    speed: 0.5 + seededRandom(i * 19) * 1.2,
    size: 3 + seededRandom(i * 3) * 8,
    op: 0.04 + seededRandom(i * 23) * 0.08,
    delay: seededRandom(i * 37) * 60,
    gold: seededRandom(i * 43) > 0.4,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C',
      overflow: 'hidden',
      opacity: exitOp,
      transform: `scale(${heroZoom})`,
    }}>

      {/* === GRADIENT — DRAMATIC SPLIT === */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 80% 70% at 50% 50%, #1a1000 0%, #1C1C1C 55%, #050515 100%)`,
      }} />
      {/* Final glow burst */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse 60% 60% at 50% 50%, rgba(233,215,0,${finalGlow * 0.12}) 0%, transparent 70%)`,
      }} />

      {/* === OCTAGON TEXTURE === */}
      {Array.from({ length: 6 }, (_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 960 - (200 + i * 120),
          top: 540 - (200 + i * 120),
          width: (400 + i * 240),
          height: (400 + i * 240),
          border: `1px solid rgba(233,215,0,${0.03 - i * 0.004})`,
          borderRadius: 40,
          transform: `rotate(${45 + frame * (0.01 + i * 0.005)}deg)`,
        }} />
      ))}

      {/* === SHAPE LAYERS === */}
      <div style={{
        position: 'absolute',
        left: -80,
        top: -80,
        width: 600,
        height: 600,
        background: 'rgba(255,0,0,0.03)',
        clipPath: 'polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)',
        transform: `rotate(${frame * 0.02}deg)`,
      }} />

      {/* === CENTER: MAIN HEADLINE === */}
      <div style={{
        position: 'absolute',
        left: 0,
        right: 0,
        top: 80,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        textAlign: 'center',
      }}>
        {/* पहले */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          color: '#9E9E9E',
          letterSpacing: 8,
          textTransform: 'uppercase',
          opacity: sp(5),
          marginBottom: 10,
        }}>CHAPTER 3 — FINAL</div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 240,
          fontWeight: 900,
          color: '#e9d700',
          lineHeight: 0.85,
          opacity: sp(8),
          transform: `scale(${interpolate(sp(8), [0, 1], [0.8, 1])}) translateY(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
          letterSpacing: -8,
          textShadow: `0 0 ${80 + finalGlow * 60}px rgba(233,215,0,${0.2 + finalGlow * 0.3})`,
        }}>खेल</div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 160,
          fontWeight: 900,
          color: '#F5F5F5',
          lineHeight: 1,
          opacity: sp(14),
          transform: `translateY(${interpolate(sp(14), [0, 1], [30, 0])}px)`,
          letterSpacing: -5,
        }}>बदलो</div>
      </div>

      {/* === TWO CHOICE PANELS === */}
      {/* Left — Performance */}
      <div style={{
        position: 'absolute',
        left: 60,
        bottom: 100,
        width: 800,
        opacity: choice1Op,
        transform: `translateX(${interpolate(choice1Op, [0, 1], [-60, 0])}px)`,
      }}>
        <div style={{
          border: '2px solid rgba(255,0,0,0.3)',
          borderRadius: 24,
          padding: '32px 40px',
          background: 'rgba(255,0,0,0.05)',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 24,
            color: '#ff0000',
            letterSpacing: 5,
            textTransform: 'uppercase',
            marginBottom: 16,
            opacity: 0.7,
          }}>OPTION A</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
            letterSpacing: -2,
            opacity: 0.8,
          }}>प्रदर्शन</div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            color: '#9E9E9E',
            marginTop: 12,
            lineHeight: 1.5,
          }}>बाकी सब के लिए —<br />दिखावे की ज़िंदगी</div>

          {/* Sad figure */}
          <svg style={{ marginTop: 20 }} width="100" height="160" viewBox="0 0 100 160">
            <circle cx="50" cy="28" r="20" fill="none" stroke="#ff0000" strokeWidth="3" opacity="0.6" />
            <path d="M38 34 Q50 28 62 34" fill="none" stroke="#ff0000" strokeWidth="2" opacity="0.5" />
            <line x1="50" y1="48" x2="50" y2="110" stroke="#ff0000" strokeWidth="3" opacity="0.6" />
            <line x1="50" y1="72" x2="28" y2="92" stroke="#ff0000" strokeWidth="2.5" opacity="0.6" />
            <line x1="50" y1="72" x2="72" y2="92" stroke="#ff0000" strokeWidth="2.5" opacity="0.6" />
            <line x1="50" y1="110" x2="32" y2="148" stroke="#ff0000" strokeWidth="2.5" opacity="0.6" />
            <line x1="50" y1="110" x2="68" y2="148" stroke="#ff0000" strokeWidth="2.5" opacity="0.6" />
            {/* Chains */}
            <circle cx="22" cy="92" r="6" fill="none" stroke="#ff0000" strokeWidth="2" opacity="0.4" />
            <circle cx="78" cy="92" r="6" fill="none" stroke="#ff0000" strokeWidth="2" opacity="0.4" />
          </svg>
        </div>
      </div>

      {/* Right — Freedom */}
      <div style={{
        position: 'absolute',
        right: 60,
        bottom: 100,
        width: 800,
        opacity: choice2Op,
        transform: `translateX(${interpolate(choice2Op, [0, 1], [60, 0])}px)`,
      }}>
        <div style={{
          border: '2px solid rgba(233,215,0,0.35)',
          borderRadius: 24,
          padding: '32px 40px',
          background: 'rgba(233,215,0,0.06)',
          boxShadow: `0 0 ${40 + finalGlow * 40}px rgba(233,215,0,${0.05 + finalGlow * 0.1})`,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 24,
            color: '#e9d700',
            letterSpacing: 5,
            textTransform: 'uppercase',
            marginBottom: 16,
            opacity: 0.8,
          }}>OPTION B ✓</div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            letterSpacing: -2,
            textShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>आज़ादी</div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38,
            color: '#F5F5F5',
            marginTop: 12,
            lineHeight: 1.5,
          }}>अपने लिए —<br />असली ज़िंदगी</div>

          {/* Happy stick figure */}
          <svg
            style={{
              marginTop: 20,
              transform: `translateY(${Math.sin(frame * 0.05) * 3}px)`,
            }}
            width="100" height="160" viewBox="0 0 100 160"
          >
            <ellipse cx="50" cy="154" rx="32" ry="7" fill="rgba(233,215,0,0.2)" />
            <circle cx="50" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Glasses */}
            <rect x="37" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="51" y="23" width="10" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="47" y1="26" x2="51" y2="26" stroke="#e9d700" strokeWidth="1.5" />
            {/* Smile */}
            <path d="M38 35 Q50 44 62 35" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="50" y1="50" x2="50" y2="110" stroke="#F5F5F5" strokeWidth="4" />
            {/* Arms up — victory */}
            <line x1="50" y1="72" x2={24 + Math.sin(frame * 0.06) * 3} y2="48" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="50" y1="72" x2={76 - Math.sin(frame * 0.06) * 3} y2="48" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="50" y1="110" x2="33" y2="148" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="50" y1="110" x2="67" y2="148" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Stars/sparkles */}
            <text x="8" y="20" fontFamily="Inter" fontSize="16" fill="#e9d700" opacity={0.6 + Math.sin(frame * 0.1) * 0.2}>✦</text>
            <text x="72" y="18" fontFamily="Inter" fontSize="12" fill="#e9d700" opacity={0.4 + Math.sin(frame * 0.08 + 1) * 0.2}>✦</text>
          </svg>
        </div>
      </div>

      {/* === VS SEPARATOR === */}
      <div style={{
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 265,
        display: 'flex',
        justifyContent: 'center',
        opacity: sp(45),
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 40,
          fontWeight: 900,
          color: '#9E9E9E',
          letterSpacing: 3,
          opacity: 0.4,
          transform: `scale(${questionPulse})`,
        }}>या ?</div>
      </div>

      {/* === RISING PARTICLES === */}
      {particles.map((p, i) => {
        const y = p.startY - (Math.max(0, frame - p.delay) * p.speed * 8);
        if (y > 1150 || y < -50) return null;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: p.x,
            top: y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            background: p.gold ? `rgba(233,215,0,${p.op})` : `rgba(245,245,245,${p.op * 0.5})`,
          }} />
        );
      })}

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse 88% 85% at 50% 50%, transparent 45%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
        letterSpacing: 2,
      }}>ProBuffet HQ</div>

    </AbsoluteFill>
  );
};

export default C3S10_ChangeTheGame;
```

