```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, 90, durationInFrames - 20], [1.08, 1, 1.03], { extrapolateRight: 'clamp' });
  const camY = interpolate(frame, [0, durationInFrames], [20, -15], { extrapolateRight: 'clamp' });

  const numScale = sp(8, { stiffness: 90, damping: 10 });
  const numOp = interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' });
  const capitalOp = sp(35);
  const lossBar = interpolate(frame, [50, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const melt = interpolate(frame, [70, 140], [0, 1], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 28 }, (_, i) => {
    const x = seededRandom(i * 3.1) * 1920;
    const y = seededRandom(i * 7.2) * 1080;
    const size = 2 + seededRandom(i * 2.3) * 4;
    const speed = 0.3 + seededRandom(i) * 0.6;
    return { x, y: y + Math.sin(frame * 0.02 + i) * 30 * speed, size, op: 0.15 + seededRandom(i * 4) * 0.25 };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 30% 20%, #3A3F6B 0%, #2B2E4A 50%, #1F2238 100%)`,
        transform: `scale(${camScale}) translateY(${camY}px)`,
      }} />

      {/* Animated shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(244,206,20,0.07) 0%, transparent 70%)',
        left: -180, top: -120, transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: 40,
        border: '2px solid rgba(253,246,240,0.04)',
        right: -80, bottom: -60, transform: `rotate(${-frame * 0.05}deg)`,
      }} />

      {/* Motion texture - subtle grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(rgba(253,246,240,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(253,246,240,0.03) 1px, transparent 1px)`,
        backgroundSize: '80px 80px',
        opacity: 0.4,
        transform: `translateY(${frame * 0.15}px)`,
      }} />

      {/* Particles */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute', left: p.x, top: p.y, width: p.size, height: p.size,
          borderRadius: '50%', backgroundColor: '#F4CE14', opacity: p.op,
        }} />
      ))}

      {/* Main content */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
        transform: `scale(${camScale}) translateY(${camY}px)`,
      }}>
        {/* Big loss number */}
        <div style={{
          fontFamily: 'Anton, sans-serif', fontSize: 210, color: '#F4CE14',
          transform: `scale(${numScale})`, opacity: numOp,
          textShadow: '0 0 60px rgba(244,206,20,0.4)', letterSpacing: -4,
          lineHeight: 0.9,
        }}>
          1 147 €
        </div>

        <div style={{
          fontFamily: 'Clash Display, sans-serif', fontSize: 42, color: '#FDF6F0',
          marginTop: 18, opacity: sp(20), fontWeight: 600,
          letterSpacing: 1,
        }}>
          PERDUS EN 5 ANS
        </div>

        {/* Capital visual */}
        <div style={{
          marginTop: 55, display: 'flex', alignItems: 'center', gap: 40,
          opacity: capitalOp,
        }}>
          <div style={{
            background: 'rgba(253,246,240,0.08)', borderRadius: 20, padding: '22px 40px',
            border: '1px solid rgba(253,246,240,0.12)',
          }}>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 22, color: '#FDF6F0', opacity: 0.7 }}>CAPITAL</div>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 48, color: '#FDF6F0', marginTop: 4 }}>22 950 €</div>
          </div>

          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 60, color: '#F4CE14', opacity: 0.8 }}>→</div>

          {/* Melting bar */}
          <div style={{ width: 280, height: 90, position: 'relative' }}>
            <div style={{
              position: 'absolute', bottom: 0, left: 0, right: 0, height: `${100 - melt * 35}%`,
              background: `linear-gradient(180deg, #F4CE14 0%, #E63946 100%)`,
              borderRadius: 12, transition: 'none',
              boxShadow: '0 0 30px rgba(230,57,70,0.3)',
            }} />
            <div style={{
              position: 'absolute', top: '50%', left: '50%', transform: 'translate(-50%, -50%)',
              fontFamily: 'Space Mono, monospace', fontSize: 28, color: '#FDF6F0', fontWeight: 700,
            }}>
              -1 147 €
            </div>
          </div>
        </div>

        {/* Bottom label */}
        <div style={{
          marginTop: 50, fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: '#FDF6F0',
          opacity: sp(70), letterSpacing: 0.5,
        }}>
          Livret A • 5 années d’immobilisation
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene01;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.45, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = Math.sin(frame * 0.015) * 12;
  const progress = interpolate(frame, [20, 160], [0, 1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 4.2) * 1920,
    y: (seededRandom(i * 1.7) * 1080 + frame * (0.4 + seededRandom(i) * 0.5)) % 1200 - 100,
    size: 1.5 + seededRandom(i * 3) * 3.5,
    op: 0.12 + seededRandom(i * 5) * 0.2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(160deg, #2B2E4A 0%, #353A5C 40%, #1E2135 100%)`,
        transform: `translateX(${camX}px)`,
      }} />

      {/* Large slow shapes */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(244,206,20,0.05) 0%, transparent 65%)',
        left: '50%', top: '40%', transform: `translate(-50%, -50%) scale(${1.1 + Math.sin(frame * 0.018) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        border: '1px solid rgba(253,246,240,0.06)', borderRadius: 80,
        right: 100, top: 80, transform: `rotate(${frame * 0.04}deg)`,
      }} />

      {/* Texture lines */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(0deg, transparent, transparent 40px, rgba(253,246,240,0.025) 40px, rgba(253,246,240,0.025) 41px)`,
        opacity: 0.6,
      }} />

      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute', left: p.x, top: p.y, width: p.size, height: p.size,
          borderRadius: '50%', backgroundColor: i % 3 === 0 ? '#F4CE14' : '#FDF6F0', opacity: p.op,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Silent thief concept */}
        <div style={{
          fontFamily: 'Clash Display, sans-serif', fontSize: 78, color: '#FDF6F0',
          opacity: sp(5), fontWeight: 700, letterSpacing: -1,
          textAlign: 'center', lineHeight: 1.1,
        }}>
          VOL SILENCIEUX
        </div>

        <div style={{
          marginTop: 30, display: 'flex', gap: 60, opacity: sp(25),
        }}>
          {['Aucune alarme', 'Aucune trace', 'Accord de l’État'].map((txt, i) => (
            <div key={i} style={{
              display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12,
              transform: `translateY(${(1 - sp(30 + i * 12)) * 40}px)`,
            }}>
              <div style={{
                width: 90, height: 90, borderRadius: '50%',
                border: '2px solid rgba(244,206,20,0.4)',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                background: 'rgba(244,206,20,0.08)',
              }}>
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                  {i === 0 && <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-1-13h2v6h-2zm0 8h2v2h-2z" fill="#F4CE14" opacity="0.3"/>}
                  {i === 1 && <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" fill="#F4CE14" opacity="0.3"/>}
                  {i === 2 && <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z" fill="#F4CE14" opacity="0.3"/>}
                  <circle cx="12" cy="12" r="8" stroke="#F4CE14" strokeWidth="1.5" fill="none" />
                  {i === 0 && <path d="M12 8v4M12 16h.01" stroke="#F4CE14" strokeWidth="2" strokeLinecap="round"/>}
                  {i === 1 && <path d="M8 12h8" stroke="#F4CE14" strokeWidth="2" strokeLinecap="round"/>}
                  {i === 2 && <path d="M9 12l2 2 4-4" stroke="#F4CE14" strokeWidth="2" strokeLinecap="round"/>}
                </svg>
              </div>
              <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 26, color: '#FDF6F0', fontWeight: 500 }}>
                {txt}
              </div>
            </div>
          ))}
        </div>

        {/* Slow progress bar - silent erosion */}
        <div style={{
          marginTop: 70, width: 700, height: 14, background: 'rgba(253,246,240,0.1)',
          borderRadius: 20, overflow: 'hidden', opacity: sp(55),
        }}>
          <div style={{
            width: `${progress * 100}%`, height: '100%',
            background: 'linear-gradient(90deg, #F4CE14, #E63946)',
            borderRadius: 20,
            boxShadow: '0 0 20px rgba(230,57,70,0.4)',
          }} />
        </div>
        <div style={{
          marginTop: 16, fontFamily: 'Space Mono, monospace', fontSize: 24, color: '#F4CE14',
          opacity: sp(65),
        }}>
          ÉROSION LENTE • JOUR APRÈS JOUR
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene02;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 130, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const logoScale = sp(0, { stiffness: 80 });
  const bite = interpolate(frame, [40, 100], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.back(1.5)) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 70% 30%, #3D4268 0%, #2B2E4A 55%, #1A1D30 100%)`,
      }} />

      {/* Geometric shapes */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '3px solid rgba(244,206,20,0.08)', borderRadius: '30%',
        left: -100, bottom: -150, transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.025) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 350, height: 350, borderRadius: '50%',
        background: 'rgba(253,246,240,0.03)', right: 50, top: 100,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const x = seededRandom(i * 9) * 1920;
        const yBase = seededRandom(i * 2.2) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: yBase + Math.sin(frame * 0.03 + i) * 25,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#F4CE14', opacity: 0.2 + seededRandom(i) * 0.25,
          }} />
        );
      })}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Channel branding */}
        <div style={{
          fontFamily: 'Clash Display, sans-serif', fontSize: 92, color: '#F4CE14',
          transform: `scale(${logoScale})`, fontWeight: 700, letterSpacing: -2,
          textShadow: '0 0 40px rgba(244,206,20,0.35)',
        }}>
          MATURES FINANCE
        </div>

        <div style={{
          marginTop: 25, fontFamily: 'Montserrat, sans-serif', fontSize: 36, color: '#FDF6F0',
          opacity: sp(18), fontWeight: 500, letterSpacing: 3, textTransform: 'uppercase',
        }}>
          Décortiquer • Comprendre • Protéger
        </div>

        {/* Visual: power of purchase being bitten */}
        <div style={{
          marginTop: 70, display: 'flex', alignItems: 'center', gap: 30,
          opacity: sp(40),
        }}>
          <div style={{
            width: 220, height: 140, background: 'rgba(253,246,240,0.09)',
            borderRadius: 16, border: '1px solid rgba(253,246,240,0.15)',
            display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
            position: 'relative', overflow: 'hidden',
          }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 28, color: '#FDF6F0' }}>POUVOIR</div>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 42, color: '#F4CE14' }}>D’ACHAT</div>
            {/* Bite mark */}
            <div style={{
              position: 'absolute', right: -20, top: '30%',
              width: 80 * bite, height: 80 * bite, borderRadius: '50%',
              background: '#2B2E4A', border: '4px solid #E63946',
              transform: 'translateY(-50%)',
              boxShadow: 'inset 0 0 20px rgba(230,57,70,0.5)',
            }} />
          </div>

          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 70, color: '#E63946',
            opacity: sp(55),
          }}>
            ✕
          </div>

          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: '#FDF6F0',
            maxWidth: 380, lineHeight: 1.35, opacity: sp(50),
          }}>
            Éviter que ton argent soit<br />
            <span style={{ color: '#F4CE14', fontWeight: 700 }}>grignoté en silence</span>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene03;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const rateScale = sp(25, { stiffness: 100 });
  const drop = interpolate(frame, [40, 90], [1.7, 1.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowY = interpolate(frame, [50, 100], [-30, 20], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(135deg, #2B2E4A 0%, #32365A 50%, #24273F 100%)`,
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 800, height: 400,
        background: 'rgba(244,206,20,0.04)', borderRadius: 100,
        left: -200, top: 200, transform: `rotate(-15deg) scale(${1 + Math.sin(frame * 0.02) * 0.02})`,
      }} />
      <div style={{
        position: 'absolute', width: 300, height: 300, borderRadius: '50%',
        border: '2px solid rgba(253,246,240,0.05)', right: 80, bottom: 100,
        transform: `rotate(${frame * 0.07}deg)`,
      }} />

      {/* Texture */}
      <AbsoluteFill style={{
        backgroundImage: `radial-gradient(circle, rgba(253,246,240,0.04) 1px, transparent 1px)`,
        backgroundSize: '50px 50px', opacity: 0.5,
      }} />

      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 6) * 1920,
          top: seededRandom(i * 3.3) * 1080 + Math.sin(frame * 0.025 + i) * 20,
          width: 2 + seededRandom(i) * 3, height: 2 + seededRandom(i) * 3,
          borderRadius: '50%', backgroundColor: '#F4CE14', opacity: 0.18,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: '#FDF6F0', opacity: sp(5),
          letterSpacing: 4, textTransform: 'uppercase',
        }}>
          DEPUIS LE 1ER FÉVRIER 2026
        </div>

        <div style={{
          marginTop: 20, fontFamily: 'Clash Display, sans-serif', fontSize: 68, color: '#FDF6F0',
          opacity: sp(12), fontWeight: 600,
        }}>
          LIVRET A
        </div>

        {/* Rate drop visual */}
        <div style={{
          marginTop: 50, display: 'flex', alignItems: 'flex-end', gap: 50,
          opacity: sp(25),
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 36, color: '#FDF6F0', opacity: 0.6 }}>AVANT</div>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 110, color: '#FDF6F0', lineHeight: 1 }}>1,7%</div>
          </div>

          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 80, color: '#E63946',
            transform: `translateY(${arrowY}px)`,
          }}>
            ↓
          </div>

          <div style={{ textAlign: 'center', transform: `scale(${rateScale})` }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 36, color: '#F4CE14' }}>MAINTENANT</div>
            <div style={{
              fontFamily: 'Anton, sans-serif', fontSize: 160, color: '#F4CE14', lineHeight: 0.9,
              textShadow: '0 0 50px rgba(244,206,20,0.4)',
            }}>
              1,5%
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: '#FDF6F0', marginTop: 8 }}>NET</div>
          </div>
        </div>

        <div style={{
          marginTop: 50, fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#FDF6F0',
          opacity: sp(70), maxWidth: 800, textAlign: 'center', lineHeight: 1.4,
        }}>
          Le symbole parfait du grignotage silencieux
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene04;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 11, mass: 0.35, stiffness: 140, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const calcOp = sp(15);
  const resultScale = sp(70, { stiffness: 90, damping: 9 });
  const bar1 = interpolate(frame, [30, 80], [0, 1.5], { extrapolateRight: 'clamp' });
  const bar2 = interpolate(frame, [40, 90], [0, 2.4], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 0%, #3A3F65 0%, #2B2E4A 60%)`,
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: 60,
        background: 'rgba(230,57,70,0.05)', left: -100, top: -80,
        transform: `rotate(${frame * 0.03}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400, borderRadius: '50%',
        border: '1px solid rgba(244,206,20,0.07)', right: -50, bottom: -50,
      }} />

      {Array.from({ length: 16 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: seededRandom(i * 8) * 1920,
          top: seededRandom(i * 4) * 1080 + Math.cos(frame * 0.02 + i) * 15,
          width: 2.5, height: 2.5, borderRadius: '50%',
          backgroundColor: i % 2 ? '#F4CE14' : '#FDF6F0', opacity: 0.2,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Comparison bars */}
        <div style={{ display: 'flex', gap: 80, opacity: calcOp, alignItems: 'flex-end' }}>
          {/* Livret A bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 180 }}>
            <div style={{
              width: 100, height: bar1 * 80,
              background: 'linear-gradient(180deg, #F4CE14, #D4B000)',
              borderRadius: '12px 12px 0 0',
              boxShadow: '0 0 25px rgba(244,206,20,0.3)',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 52, color: '#F4CE14', marginTop: 12 }}>1,5%</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 22, color: '#FDF6F0', opacity: 0.7 }}>LIVRET A</div>
          </div>

          {/* Inflation bar */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 180 }}>
            <div style={{
              width: 100, height: bar2 * 80,
              background: 'linear-gradient(180deg, #E63946, #B82A36)',
              borderRadius: '12px 12px 0 0',
              boxShadow: '0 0 25px rgba(230,57,70,0.35)',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 52, color: '#E63946', marginTop: 12 }}>2,4%</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 22, color: '#FDF6F0', opacity: 0.7 }}>INFLATION</div>
          </div>
        </div>

        {/* Equation */}
        <div style={{
          marginTop: 60, display: 'flex', alignItems: 'center', gap: 30,
          opacity: sp(55),
        }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 48, color: '#FDF6F0' }}>1,5</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#F4CE14' }}>−</div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 48, color: '#FDF6F0' }}>2,4</div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#F4CE14' }}>=</div>
        </div>

        {/* Result */}
        <div style={{
          marginTop: 25, transform: `scale(${resultScale})`,
          fontFamily: 'Anton, sans-serif', fontSize: 140, color: '#E63946',
          textShadow: '0 0 60px rgba(230,57,70,0.5)', letterSpacing: -3,
        }}>
          − 0,9 %
        </div>

        <div style={{
          marginTop: 15, fontFamily: 'Montserrat, sans-serif', fontSize: 32, color: '#FDF6F0',
          opacity: sp(90),
        }}>
          RENDEMENT RÉEL NÉGATIF
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.58) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene05;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 115, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const countUp = Math.min(344, Math.floor(interpolate(frame, [30, 100], [0, 344], { extrapolateRight: 'clamp' })));
  const coinScale = sp(20);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(180deg, #2B2E4A 0%, #33385C 40%, #252840 100%)`,
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(244,206,20,0.06) 0%, transparent 70%)',
        left: '50%', top: '60%', transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 250, height: 250,
        border: '2px solid rgba(253,246,240,0.05)', borderRadius: 30,
        left: 100, top: 150, transform: `rotate(${frame * 0.05}deg)`,
      }} />

      {Array.from({ length: 24 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i * 5.5) * 1920,
          top: (seededRandom(i * 2.8) * 1080 + frame * 0.3) % 1100,
          width: 2 + seededRandom(i * 1.5) * 3, height: 2 + seededRandom(i * 1.5) * 3,
          borderRadius: '50%', backgroundColor: '#F4CE14', opacity: 0.15 + seededRandom(i) * 0.2,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#FDF6F0', opacity: sp(5),
          letterSpacing: 2,
        }}>
          PLAFOND LIVRET A • INTÉRÊTS ANNUELS
        </div>

        {/* Big interest number */}
        <div style={{
          marginTop: 30, fontFamily: 'Anton, sans-serif', fontSize: 200, color: '#F4CE14',
          transform: `scale(${coinScale})`, letterSpacing: -4,
          textShadow: '0 0 50px rgba(244,206,20,0.35)',
        }}>
          {countUp} €
        </div>

        <div style={{
          fontFamily: 'Clash Display, sans-serif', fontSize: 42, color: '#FDF6F0',
          opacity: sp(40), marginTop: 10,
        }}>
          d’intérêts bruts
        </div>

        {/* Visual coins / stack representation */}
        <div style={{
          marginTop: 50, display: 'flex', gap: 12, opacity: sp(55),
        }}>
          {Array.from({ length: 8 }).map((_, i) => (
            <div key={i} style={{
              width: 48, height: 48 + i * 6,
              background: `linear-gradient(180deg, #F4CE14, #C9A800)`,
              borderRadius: '50% 50% 8px 8px',
              transform: `translateY(${(1 - sp(60 + i * 4)) * 40}px)`,
              boxShadow: '0 4px 15px rgba(244,206,20,0.3)',
              opacity: 0.7 + i * 0.03,
            }} />
          ))}
        </div>

        <div style={{
          marginTop: 40, fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: '#FDF6F0',
          opacity: sp(80), background: 'rgba(230,57,70,0.15)', padding: '12px 30px', borderRadius: 30,
          border: '1px solid rgba(230,57,70,0.3)',
        }}>
          Mais le rendement réel reste négatif
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene06;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const gainOp = sp(10);
  const lossOp = sp(40);
  const netScale = sp(75, { stiffness: 100 });

  const gainH = interpolate(frame, [20, 70], [0, 180], { extrapolateRight: 'clamp' });
  const lossH = interpolate(frame, [50, 110], [0, 290], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 20% 80%, #3A2A3A 0%, #2B2E4A 50%, #1F2238 100%)`,
      }} />

      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        background: 'rgba(230,57,70,0.06)', right: -150, top: -100,
        transform: `scale(${1 + Math.sin(frame * 0.022) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 350, height: 350,
        border: '1px solid rgba(244,206,20,0.06)', left: 50, bottom: 80,
        borderRadius: 40, transform: `rotate(${-frame * 0.04}deg)`,
      }} />

      {Array.from({ length: 20 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: seededRandom(i * 7) * 1920,
          top: seededRandom(i * 3.1) * 1080 + Math.sin(frame * 0.028 + i) * 22,
          width: 2 + seededRandom(i) * 3, height: 2 + seededRandom(i) * 3,
          borderRadius: '50%', backgroundColor: '#FDF6F0', opacity: 0.15,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Clash Display, sans-serif', fontSize: 48, color: '#FDF6F0',
          opacity: sp(5), marginBottom: 40,
        }}>
          ILLUSION VS RÉALITÉ
        </div>

        <div style={{ display: 'flex', gap: 100, alignItems: 'flex-end' }}>
          {/* Gain column */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: gainOp }}>
            <div style={{
              width: 140, height: gainH,
              background: 'linear-gradient(180deg, #4CAF50, #2E7D32)',
              borderRadius: '16px 16px 0 0',
              boxShadow: '0 0 30px rgba(76,175,80,0.3)',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#4CAF50', marginTop: 15 }}>+344 €</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0', opacity: 0.75 }}>INTÉRÊTS</div>
          </div>

          {/* Loss column */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', opacity: lossOp }}>
            <div style={{
              width: 140, height: lossH,
              background: 'linear-gradient(180deg, #E63946, #9B1D2A)',
              borderRadius: '16px 16px 0 0',
              boxShadow: '0 0 30px rgba(230,57,70,0.35)',
            }} />
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#E63946', marginTop: 15 }}>−550 €</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0', opacity: 0.75 }}>INFLATION</div>
          </div>
        </div>

        {/* Net result */}
        <div style={{
          marginTop: 55, transform: `scale(${netScale})`,
          background: 'rgba(230,57,70,0.12)', border: '2px solid rgba(230,57,70,0.4)',
          borderRadius: 20, padding: '20px 50px',
        }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 26, color: '#FDF6F0', textAlign: 'center' }}>
            PERTE RÉELLE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 90, color: '#E63946', textAlign: 'center',
            textShadow: '0 0 40px rgba(230,57,70,0.4)',
          }}>
            ≈ 200 €
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.58) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene07;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 14, mass: 0.45, stiffness: 110, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const items = [
    { label: 'Baguette', icon: '🥖', delay: 15 },
    { label: 'Essence', icon: '⛽', delay: 28 },
    { label: 'Abonnements', icon: '📱', delay: 41 },
    { label: 'Courses', icon: '🛒', delay: 54 },
  ];

  // No emojis - pure SVG icons
  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `linear-gradient(145deg, #2B2E4A 0%, #2F3355 50%, #23263C 100%)`,
      }} />

      <div style={{
        position: 'absolute', width: 900, height: 500,
        background: 'rgba(244,206,20,0.03)', borderRadius: 200,
        left: -200, top: 300, transform: `rotate(-8deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 300, height: 300, borderRadius: '50%',
        border: '2px solid rgba(253,246,240,0.04)', right: 120, top: 80,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />

      {Array.from({ length: 18 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: seededRandom(i * 4.7) * 1920,
          top: seededRandom(i * 2.9) * 1080 + Math.sin(frame * 0.03 + i) * 18,
          width: 2, height: 2, borderRadius: '50%',
          backgroundColor: '#F4CE14', opacity: 0.2,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Anton, sans-serif', fontSize: 72, color: '#E63946',
          opacity: sp(5), letterSpacing: -1,
        }}>
          200 € DISSOUS
        </div>

        <div style={{
          marginTop: 15, fontFamily: 'Montserrat, sans-serif', fontSize: 30, color: '#FDF6F0',
          opacity: sp(15),
        }}>
          dans le quotidien, sans bruit
        </div>

        {/* 4 categories with SVG icons */}
        <div style={{
          marginTop: 60, display: 'flex', gap: 40,
        }}>
          {[
            { label: 'Baguette', path: 'M4 12c0-2 2-4 6-4s6 2 6 4-2 4-6 4-6-2-6-4zm2 0c0 1 1.5 2 4 2s4-1 4-2-1.5-2-4-2-4 1-4 2z' },
            { label: 'Essence', path: 'M3 5h12v12H3V5zm2 2v8h8V7H5zm12 1h2v9a2 2 0 01-2 2h-1v-2h1V8z' },
            { label: 'Abonnements', path: 'M4 4h16v2H4V4zm0 4h16v12H4V8zm2 2v8h12v-8H6z' },
            { label: 'Courses', path: 'M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.3 2.3c-.6.6-.2 1.7.7 1.7H17M17 13v6a2 2 0 01-2 2H9a2 2 0 01-2-2v-6' },
          ].map((item, i) => (
            <div key={i} style={{
              width: 200, height: 220,
              background: 'rgba(253,246,240,0.06)', borderRadius: 24,
              border: '1px solid rgba(253,246,240,0.1)',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
              transform: `translateY(${(1 - sp(25 + i * 12)) * 50}px)`,
              opacity: sp(25 + i * 12),
            }}>
              <div style={{
                width: 80, height: 80, borderRadius: '50%',
                background: 'rgba(244,206,20,0.12)',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                marginBottom: 20,
              }}>
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#F4CE14" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
                  <path d={item.path} />
                </svg>
              </div>
              <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 26, color: '#FDF6F0', fontWeight: 600 }}>
                {item.label}
              </div>
            </div>
          ))}
        </div>

        <div style={{
          marginTop: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: '#F4CE14',
          opacity: sp(85),
        }}>
          Tu te crois prudent… pendant que ça fond
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene08;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 13, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const meltProgress = interpolate(frame, [40, 160], [0, 1], { extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });
  const percent = Math.floor(interpolate(frame, [50, 120], [0, 90], { extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at 50% 100%, #3A3040 0%, #2B2E4A 50%, #1C1F32 100%)`,
      }} />

      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        background: 'rgba(244,206,20,0.04)', left: '50%', top: -200,
        transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.015) * 0.03})`,
      }} />

      {Array.from({ length: 25 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: seededRandom(i * 3.8) * 1920,
          top: seededRandom(i * 6.1) * 1080 + frame * (0.2 + seededRandom(i) * 0.4),
          width: 1.5 + seededRandom(i) * 2.5, height: 1.5 + seededRandom(i) * 2.5,
          borderRadius: '50%', backgroundColor: '#F4CE14', opacity: 0.12 + seededRandom(i * 2) * 0.15,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{
          fontFamily: 'Montserrat, sans-serif', fontSize: 28, color: '#FDF6F0', opacity: sp(5),
          letterSpacing: 3, textTransform: 'uppercase',
        }}>
          LE MYTHE DE LA SÉCURITÉ
        </div>

        <div style={{
          marginTop: 20, fontFamily: 'Clash Display, sans-serif', fontSize: 64, color: '#FDF6F0',
          opacity: sp(12), textAlign: 'center', lineHeight: 1.15, maxWidth: 1100,
        }}>
          Placement des gens sérieux
        </div>

        {/* Melting billions visual */}
        <div style={{
          marginTop: 50, width: 600, height: 160, position: 'relative',
          opacity: sp(30),
        }}>
          <div style={{
            position: 'absolute', bottom: 0, left: 0, right: 0,
            height: `${100 - meltProgress * 55}%`,
            background: `linear-gradient(180deg, #F4CE14 0%, #E63946 90%)`,
            borderRadius: 20,
            boxShadow: '0 0 40px rgba(230,57,70,0.3)',
          }} />
          <div style={{
            position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Anton, sans-serif', fontSize: 58, color: '#FDF6F0',
            textShadow: '0 2px 10px rgba(0,0,0,0.5)',
          }}>
            DES MILLIARDS €
          </div>
        </div>

        <div style={{
          marginTop: 40, display: 'flex', gap: 60, opacity: sp(55),
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 72, color: '#F4CE14' }}>{percent}%</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0' }}>des Français<br />en possèdent un</div>
          </div>
          <div style={{
            width: 2, height: 100, background: 'rgba(253,246,240,0.2)',
          }} />
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 72, color: '#E63946' }}>FOND</div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0' }}>en silence<br />chaque jour</div>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.58) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene09;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10',
  durationInSeconds: 14.696,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();
  
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 125, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const shieldScale = sp(20, { stiffness: 90 });
  const crack = interpolate(frame, [60, 120], [0, 1], { extrapolateRight: 'clamp' });
  const realOp = sp(70);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2B2E4A', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% 40%, #353A5E 0%, #2B2E4A 55%, #1A1D2E 100%)`,
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        background: 'rgba(244,206,20,0.05)', left: '50%', top: '45%',
        transform: `translate(-50%, -50%) scale(${1.05 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 200,
        border: '1px solid rgba(253,246,240,0.05)', left: 80, bottom: 120,
        borderRadius: 100, transform: `rotate(${frame * 0.03}deg)`,
      }} />

      {Array.from({ length: 22 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute', left: seededRandom(i * 5.2) * 1920,
          top: seededRandom(i * 3.7) * 1080 + Math.sin(frame * 0.025 + i * 0.5) * 20,
          width: 2 + seededRandom(i) * 2.5, height: 2 + seededRandom(i) * 2.5,
          borderRadius: '50%', backgroundColor: i % 3 === 0 ? '#F4CE14' : '#FDF6F0', opacity: 0.18,
        }} />
      ))}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
        {/* Two columns comparison */}
        <div style={{ display: 'flex', gap: 80, opacity: sp(10) }}>
          {/* Nominal */}
          <div style={{
            width: 380, padding: '40px 30px', borderRadius: 24,
            background: 'rgba(253,246,240,0.06)', border: '1px solid rgba(253,246,240,0.12)',
            textAlign: 'center', transform: `scale(${shieldScale})`,
          }}>
            <div style={{
              width: 90, height: 90, margin: '0 auto 20px', borderRadius: '50%',
              background: 'rgba(76,175,80,0.15)', display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}>
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#4CAF50" strokeWidth="1.8">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
                <path d="M9 12l2 2 4-4" />
              </svg>
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0', opacity: 0.7 }}>
              CAPITAL NOMINAL
            </div>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#4CAF50', marginTop: 8 }}>
              INTACT
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 22, color: '#FDF6F0', marginTop: 12, opacity: 0.6 }}>
              Les chiffres ne bougent pas
            </div>
          </div>

          {/* Real power */}
          <div style={{
            width: 380, padding: '40px 30px', borderRadius: 24,
            background: 'rgba(230,57,70,0.1)', border: '1px solid rgba(230,57,70,0.3)',
            textAlign: 'center', opacity: realOp,
            position: 'relative', overflow: 'hidden',
          }}>
            <div style={{
              width: 90, height: 90, margin: '0 auto 20px', borderRadius: '50%',
              background: 'rgba(230,57,70,0.15)', display: 'flex', alignItems: 'center', justifyContent: 'center',
              position: 'relative',
            }}>
              <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#E63946" strokeWidth="1.8">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
              </svg>
              {/* Crack overlay */}
              <div style={{
                position: 'absolute', inset: 0, opacity: crack,
                background: `linear-gradient(45deg, transparent 40%, #E63946 45%, transparent 50%, #E63946 55%, transparent 60%)`,
              }} />
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 24, color: '#FDF6F0', opacity: 0.7 }}>
              POUVOIR D’ACHAT
            </div>
            <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#E63946', marginTop: 8 }}>
              EN BAISSE
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 22, color: '#FDF6F0', marginTop: 12, opacity: 0.6 }}>
              La vraie sécurité absente
            </div>
          </div>
        </div>

        <div style={{
          marginTop: 55, fontFamily: 'Clash Display, sans-serif', fontSize: 38, color: '#F4CE14',
          opacity: sp(90), textAlign: 'center', maxWidth: 900, lineHeight: 1.3,
        }}>
          En 2026, le Livret A ne protège plus<br />ce qui compte vraiment
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.58) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.35,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene10;
```

