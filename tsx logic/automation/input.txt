```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S1_SurakshaShabda',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S1_SurakshaShabda: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Word dissolve animation
  const wordReveal = interpolate(frame, [0, 40], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Blur-to-sharp on the word "सुरक्षी"
  const blurAmount = interpolate(frame, [20, 60], [20, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Crack line grow
  const crackWidth = interpolate(frame, [50, 120], [0, 900], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.expo),
  });

  // Shake on crack
  const shakeX = frame > 50 && frame < 70
    ? Math.sin(frame * 0.8) * interpolate(frame, [50, 70], [6, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })
    : 0;

  // Floating particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 3) * 1920,
    y: seededRandom(i * 3 + 1) * 1080,
    size: seededRandom(i * 3 + 2) * 4 + 2,
    speed: seededRandom(i * 7) * 0.3 + 0.1,
    phase: seededRandom(i * 5) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background — deep grey to charcoal diagonal */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg1" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#1C1C1C" />
              <stop offset="50%" stopColor="#2D2D2D" />
              <stop offset="100%" stopColor="#1a1a2e" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg1)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — large transparent circles drifting */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <circle
            cx={300 + Math.sin(frame * 0.008) * 30}
            cy={200 + Math.cos(frame * 0.006) * 20}
            r="380"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity="0.04"
          />
          <circle
            cx={1650 + Math.cos(frame * 0.007) * 25}
            cy={800 + Math.sin(frame * 0.009) * 18}
            r="280"
            fill="#e9d700"
            opacity="0.03"
          />
          <rect
            x={800 + Math.sin(frame * 0.005) * 15}
            y={50}
            width="600"
            height="3"
            fill="#e9d700"
            opacity="0.06"
            transform={`rotate(${Math.sin(frame * 0.004) * 2} 1100 51)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — diagonal grid lines */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.04">
          {Array.from({ length: 12 }, (_, i) => (
            <line
              key={i}
              x1={i * 175 - 100}
              y1="0"
              x2={i * 175 + 300}
              y2="1080"
              stroke="#9E9E9E"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 12}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 8}
              r={p.size}
              fill="#e9d700"
              opacity={0.08 + Math.sin(frame * p.speed + p.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `translateX(${shakeX}px)`,
        }}
      >
        {/* Top label */}
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 6,
            textTransform: 'uppercase',
            marginBottom: 40,
            opacity: wordReveal,
          }}
        >
          FINANCE WORLD
        </div>

        {/* "सुरक्षी" — hero word with blur-to-sharp reveal */}
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 220,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            filter: `blur(${blurAmount}px)`,
            opacity: wordReveal,
            letterSpacing: -4,
            position: 'relative',
          }}
        >
          सुरक्षी
          {/* Yellow underline that grows */}
          <div
            style={{
              position: 'absolute',
              bottom: -8,
              left: '50%',
              transform: 'translateX(-50%)',
              height: 6,
              width: `${interpolate(frame, [60, 120], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}%`,
              backgroundColor: '#e9d700',
            }}
          />
        </div>

        {/* Crack SVG across the word */}
        <div style={{ position: 'absolute', top: '42%', left: '50%', transform: 'translateX(-50%)' }}>
          <svg width="900" height="20" overflow="visible">
            <path
              d={`M 0 10 L ${crackWidth * 0.15} 2 L ${crackWidth * 0.3} 14 L ${crackWidth * 0.5} 0 L ${crackWidth * 0.65} 16 L ${crackWidth * 0.8} 5 L ${crackWidth} 10`}
              stroke="#ff0000"
              strokeWidth="2.5"
              fill="none"
              strokeDasharray="4 3"
              opacity={interpolate(frame, [50, 65], [0, 0.7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            />
          </svg>
        </div>

        {/* Subtitle */}
        <div
          style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 52,
            fontWeight: 400,
            color: '#9E9E9E',
            marginTop: 32,
            opacity: interpolate(frame, [70, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            letterSpacing: 1,
          }}
        >
          शब्द का इस्तिमाल बहुत{' '}
          <span style={{ color: '#ff0000', fontWeight: 700 }}>धीला ढाला</span> है
        </div>
      </AbsoluteFill>

      {/* Stick figure — Warren Buffett character, left side */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="140"
          height="200"
          style={{
            position: 'absolute',
            left: 80,
            bottom: 180,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(40)})`,
            transformOrigin: 'bottom center',
          }}
        >
          {/* Glow */}
          <ellipse cx="70" cy="185" rx="40" ry="10" fill="#FFC107" opacity="0.18" />
          {/* Body */}
          <line x1="70" y1="90" x2="70" y2="155" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms — shrug gesture */}
          <line x1="70" y1="105" x2="35" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="70" y1="105" x2="105" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="70" y1="155" x2="50" y2="185" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="70" y1="155" x2="90" y2="185" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Head */}
          <circle cx="70" cy="68" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Face — confused expression */}
          <path d="M 60 75 Q 70 70 80 75" stroke="#F5F5F5" strokeWidth="2" fill="none" />
          <circle cx="63" cy="63" r="2.5" fill="#F5F5F5" />
          <circle cx="77" cy="63" r="2.5" fill="#F5F5F5" />
          {/* Golden glasses */}
          <rect x="56" y="59" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="71" y="59" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="69" y1="63" x2="71" y2="63" stroke="#FFC107" strokeWidth="2" />
          {/* Question mark above head */}
          <text
            x="70"
            y={40 - Math.sin(frame * 0.07) * 3}
            textAnchor="middle"
            fontSize="20"
            fill="#e9d700"
            fontWeight="bold"
            opacity={interpolate(frame, [30, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >
            ?
          </text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig1" cx="50%" cy="50%" r="70%">
              <stop offset="60%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig1)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S1_SurakshaShabda;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S2_SalesmanLie',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S2_SalesmanLie: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Salesman figure slides in from right
  const salesmanX = interpolate(frame, [0, 35], [400, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.5)),
  });

  // Speech bubble pop
  const bubbleScale = sp(40);

  // Red stamp "MISLEADING" drops in
  const stampScale = sp(100, { damping: 8, stiffness: 200 });
  const stampRotate = interpolate(frame, [100, 115], [25, -5], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.bounce),
  });

  // Three "reasons" stagger in
  const r1op = interpolate(frame, [50, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const r2op = interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const r3op = interpolate(frame, [90, 115], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 15 }, (_, i) => ({
    x: seededRandom(i * 4) * 1920,
    y: seededRandom(i * 4 + 1) * 1080,
    size: seededRandom(i * 4 + 2) * 3 + 1.5,
    speed: seededRandom(i * 6) * 0.25 + 0.08,
    phase: seededRandom(i * 4 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — dark red gradient, danger feel */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg2" x1="100%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stopColor="#1C1C1C" />
              <stop offset="60%" stopColor="#2D2D2D" />
              <stop offset="100%" stopColor="#1a0a0a" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg2)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — triangles and rectangles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <polygon
            points={`${150 + Math.sin(frame * 0.006) * 20},80 ${350 + Math.sin(frame * 0.006) * 20},400 ${-50 + Math.sin(frame * 0.006) * 20},400`}
            fill="#ff0000"
            opacity="0.04"
          />
          <rect
            x={1500}
            y={600 + Math.cos(frame * 0.007) * 20}
            width="500"
            height="500"
            fill="#e9d700"
            opacity="0.03"
            transform={`rotate(${frame * 0.04} 1750 850)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — horizontal scan lines */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.03">
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={i}
              x1="0"
              y1={i * 56}
              x2="1920"
              y2={i * 56}
              stroke="#9E9E9E"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.8 + p.phase) * 7}
              r={p.size}
              fill="#ff0000"
              opacity={0.06 + Math.sin(frame * p.speed + p.phase) * 0.03}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* LEFT — "Salesman says" panel */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) translateX(${-salesmanX}px)`,
          width: 600,
        }}
      >
        {/* Salesman SVG */}
        <svg width="120" height="170" style={{ display: 'block', marginBottom: 16 }}>
          <ellipse cx="60" cy="160" rx="35" ry="8" fill="#ff0000" opacity="0.15" />
          <line x1="60" y1="78" x2="60" y2="132" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arm extended forward — pointing/selling */}
          <line x1="60" y1="92" x2="25" y2="115" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="60" y1="92" x2="110" y2="78" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="60" y1="132" x2="42" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="60" y1="132" x2="78" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="60" cy="56" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Big smile — fake cheerful */}
          <path d="M 50 62 Q 60 72 70 62" stroke="#ff0000" strokeWidth="2.5" fill="none" />
          <circle cx="53" cy="52" r="2.5" fill="#F5F5F5" />
          <circle cx="67" cy="52" r="2.5" fill="#F5F5F5" />
          {/* Glasses */}
          <rect x="46" y="48" width="12" height="8" rx="3.5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="62" y="48" width="12" height="8" rx="3.5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="58" y1="52" x2="62" y2="52" stroke="#FFC107" strokeWidth="2" />
          {/* Tie */}
          <polygon points="60,78 55,88 60,102 65,88" fill="#ff0000" opacity="0.7" />
        </svg>

        {/* Speech bubble */}
        <div
          style={{
            backgroundColor: '#2D2D2D',
            border: '2px solid #ff0000',
            borderRadius: 16,
            padding: '20px 28px',
            position: 'relative',
            transform: `scale(${bubbleScale})`,
            transformOrigin: 'top left',
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: 20,
              left: -16,
              width: 0,
              height: 0,
              borderTop: '8px solid transparent',
              borderBottom: '8px solid transparent',
              borderRight: '16px solid #ff0000',
            }}
          />
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 38,
              fontWeight: 700,
              color: '#F5F5F5',
              lineHeight: 1.4,
            }}
          >
            "ये{' '}
            <span style={{ color: '#e9d700' }}>सुरक्षित</span> है!"
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 28,
              color: '#9E9E9E',
              marginTop: 8,
            }}
          >
            बिक्री प्रतिनिधि
          </div>
        </div>
      </div>

      {/* RIGHT — Three fake reasons */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 700,
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 44,
            fontWeight: 900,
            color: '#9E9E9E',
            letterSpacing: 3,
            marginBottom: 32,
            opacity: r1op,
          }}
        >
          वो कहते हैं "सुरक्षित" क्योंकि...
        </div>

        {[
          { text: 'आय देता है', op: r1op },
          { text: 'बड़ा संस्थान दे रहा है', op: r2op },
          { text: 'कागज पर स्थिर दिखता है', op: r3op },
        ].map((item, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              marginBottom: 22,
              opacity: item.op,
              transform: `translateX(${interpolate(item.op, [0, 1], [60, 0])}px)`,
            }}
          >
            {/* X icon */}
            <svg width="44" height="44" viewBox="0 0 44 44">
              <circle cx="22" cy="22" r="20" fill="#ff0000" opacity="0.15" stroke="#ff0000" strokeWidth="2" />
              <line x1="13" y1="13" x2="31" y2="31" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
              <line x1="31" y1="13" x2="13" y2="31" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 48,
                fontWeight: 700,
                color: '#F5F5F5',
                textDecoration: 'line-through',
                textDecorationColor: '#ff0000',
                textDecorationThickness: 3,
              }}
            >
              {item.text}
            </div>
          </div>
        ))}
      </div>

      {/* Red MISLEADING stamp */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          bottom: 160,
          transform: `rotate(${stampRotate}deg) scale(${stampScale})`,
          transformOrigin: 'center center',
          border: '5px solid #ff0000',
          borderRadius: 8,
          padding: '10px 28px',
          opacity: 0.85,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 44,
            fontWeight: 900,
            color: '#ff0000',
            letterSpacing: 6,
          }}
        >
          भ्रामक
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig2" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.7" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig2)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S2_SalesmanLie;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S3_70AgeShift',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S3_70AgeShift: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split screen reveal — left slides from left, right from right
  const leftSlide = interpolate(frame, [0, 45], [-960, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const rightSlide = interpolate(frame, [0, 45], [960, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Center divider line pulse
  const dividerGlow = interpolate(
    frame,
    [0, 30, 60, 90, 120, 150, 180, 210, 240, 270, 300],
    [0.4, 1, 0.4, 1, 0.4, 1, 0.4, 1, 0.4, 1, 0.4],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // "70+" number springs in
  const numScale = sp(60, { damping: 8, stiffness: 180 });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 5) * 1920,
    y: seededRandom(i * 5 + 1) * 1080,
    size: seededRandom(i * 5 + 2) * 3 + 1.5,
    speed: seededRandom(i * 7) * 0.2 + 0.07,
    phase: seededRandom(i * 5 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* LEFT half — "General" definition of safety (grey/dim) */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: 960,
          height: 1080,
          backgroundColor: '#1C1C1C',
          transform: `translateX(${leftSlide}px)`,
          overflow: 'hidden',
        }}
      >
        <svg width="960" height="1080" style={{ position: 'absolute', inset: 0 }}>
          <defs>
            <linearGradient id="leftBg" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#1C1C1C" />
              <stop offset="100%" stopColor="#242424" />
            </linearGradient>
          </defs>
          <rect width="960" height="1080" fill="url(#leftBg)" />
          {/* Dim hexagon pattern */}
          {Array.from({ length: 6 }, (_, i) => (
            <polygon
              key={i}
              points={`${150 + i * 140},${200 + (i % 2) * 120} ${190 + i * 140},${170 + (i % 2) * 120} ${230 + i * 140},${200 + (i % 2) * 120} ${230 + i * 140},${260 + (i % 2) * 120} ${190 + i * 140},${290 + (i % 2) * 120} ${150 + i * 140},${260 + (i % 2) * 120}`}
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1"
              opacity="0.06"
            />
          ))}
        </svg>

        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 5,
              textTransform: 'uppercase',
            }}
          >
            General Definition
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 100,
              fontWeight: 900,
              color: '#9E9E9E',
              lineHeight: 1.1,
              textAlign: 'center',
              opacity: 0.5,
            }}
          >
            सुरक्षा
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 38,
              color: '#9E9E9E',
              textAlign: 'center',
              opacity: 0.5,
              lineHeight: 1.5,
              padding: '0 60px',
            }}
          >
            आय देना<br />बड़ा संस्थान<br />कागज पर स्थिर
          </div>
          {/* Big X overlay */}
          <svg width="300" height="300" style={{ position: 'absolute', opacity: 0.12 }}>
            <line x1="0" y1="0" x2="300" y2="300" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
            <line x1="300" y1="0" x2="0" y2="300" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* RIGHT half — "70+ True" definition (yellow/lit) */}
      <div
        style={{
          position: 'absolute',
          left: 960,
          top: 0,
          width: 960,
          height: 1080,
          transform: `translateX(${rightSlide}px)`,
          overflow: 'hidden',
        }}
      >
        <svg width="960" height="1080" style={{ position: 'absolute', inset: 0 }}>
          <defs>
            <linearGradient id="rightBg" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#1a1500" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          <rect width="960" height="1080" fill="url(#rightBg)" />
          <circle
            cx={480 + Math.sin(frame * 0.006) * 15}
            cy={540 + Math.cos(frame * 0.008) * 12}
            r="380"
            fill="#e9d700"
            opacity="0.04"
          />
        </svg>

        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 24,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#e9d700',
              letterSpacing: 5,
              textTransform: 'uppercase',
            }}
          >
            70+ Real Definition
          </div>
          {/* 70+ big number */}
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 160,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1,
              transform: `scale(${numScale})`,
              transformOrigin: 'center',
            }}
          >
            70+
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 80,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.1,
              textAlign: 'center',
            }}
          >
            सुरक्षा का मतलब
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 38,
              color: '#F5F5F5',
              textAlign: 'center',
              lineHeight: 1.6,
              padding: '0 40px',
              opacity: interpolate(frame, [80, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}
          >
            <span style={{ color: '#e9d700', fontWeight: 700 }}>बिल्कुल अलग</span> है
          </div>
        </div>
      </div>

      {/* Center divider with glow */}
      <div
        style={{
          position: 'absolute',
          left: 957,
          top: 0,
          width: 6,
          height: 1080,
          backgroundColor: '#e9d700',
          opacity: dividerGlow,
          boxShadow: `0 0 ${24 * dividerGlow}px #e9d700`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill={i % 2 === 0 ? '#e9d700' : '#9E9E9E'}
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Stick figure bottom-left */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="130"
          height="185"
          style={{
            position: 'absolute',
            left: 60,
            bottom: 150,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(50)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="65" cy="175" rx="35" ry="8" fill="#FFC107" opacity="0.15" />
          <line x1="65" y1="82" x2="65" y2="145" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="100" x2="30" y2="120" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="100" x2="100" y2="120" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="145" x2="45" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="145" x2="85" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="65" cy="60" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Thoughtful expression */}
          <path d="M 55 68 Q 65 74 75 68" stroke="#F5F5F5" strokeWidth="2" fill="none" />
          <circle cx="58" cy="55" r="2.5" fill="#F5F5F5" />
          <circle cx="72" cy="55" r="2.5" fill="#F5F5F5" />
          <rect x="51" y="51" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="66" y="51" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="64" y1="55" x2="66" y2="55" stroke="#FFC107" strokeWidth="2" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig3" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig3)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S3_70AgeShift;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S4_ThreeTests',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S4_ThreeTests: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline drops from top
  const headlineY = interpolate(frame, [0, 35], [-120, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.2)),
  });

  // Three circles draw in sequence
  const circle1Draw = interpolate(frame, [40, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const circle2Draw = interpolate(frame, [65, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const circle3Draw = interpolate(frame, [90, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "3" counter animates
  const threeScale = sp(25, { damping: 6, stiffness: 300 });

  const particles = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    size: seededRandom(i * 3 + 3) * 4 + 1.5,
    speed: seededRandom(i * 7 + 1) * 0.22 + 0.07,
    phase: seededRandom(i * 3 + 4) * Math.PI * 2,
  }));

  const circleR = 440;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — deep gold tint */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="bg4" cx="50%" cy="50%" r="60%">
              <stop offset="0%" stopColor="#1a1500" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg4)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — three large faint circles suggesting the 3 tests */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <circle cx="480" cy="620" r="280" fill="none" stroke="#e9d700" strokeWidth="1.5" opacity={0.05 + circle1Draw * 0.05} />
          <circle cx="960" cy="620" r="280" fill="none" stroke="#e9d700" strokeWidth="1.5" opacity={0.05 + circle2Draw * 0.05} />
          <circle cx="1440" cy="620" r="280" fill="none" stroke="#e9d700" strokeWidth="1.5" opacity={0.05 + circle3Draw * 0.05} />
          {/* Background large circle */}
          <circle
            cx={960 + Math.sin(frame * 0.006) * 10}
            cy={540 + Math.cos(frame * 0.007) * 8}
            r="520"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="1"
            opacity="0.03"
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — concentric arc lines */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.04">
          {Array.from({ length: 8 }, (_, i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={100 + i * 85}
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1"
              strokeDasharray="8 12"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#e9d700"
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Headline */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `translateY(${headlineY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 54,
            fontWeight: 400,
            color: '#9E9E9E',
            letterSpacing: 2,
          }}
        >
          एक निवेश तभी सच में सुरक्षित है जब...
        </div>
      </div>

      {/* Big "3" counter center */}
      <div
        style={{
          position: 'absolute',
          top: '30%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${threeScale})`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 260,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            opacity: 0.12,
          }}
        >
          3
        </div>
      </div>

      {/* Three test circles */}
      {[
        { label: '01', title: 'परीक्षा', draw: circle1Draw, x: 480, delay: 40 },
        { label: '02', title: 'परीक्षा', draw: circle2Draw, x: 960, delay: 65 },
        { label: '03', title: 'परीक्षा', draw: circle3Draw, x: 1440, delay: 90 },
      ].map((item, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: item.x - 160,
            top: '42%',
            width: 320,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            gap: 16,
            opacity: item.draw,
            transform: `translateY(${interpolate(item.draw, [0, 1], [40, 0])}px)`,
          }}
        >
          {/* Animated circle border */}
          <svg width="200" height="200" viewBox="0 0 200 200">
            <circle cx="100" cy="100" r="85" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3" opacity="0.5" />
            <circle
              cx="100"
              cy="100"
              r="85"
              fill="none"
              stroke="#e9d700"
              strokeWidth="5"
              strokeDasharray={`${534 * item.draw} 534`}
              strokeDashoffset="133.5"
              transform="rotate(-90 100 100)"
            />
            <text x="100" y="90" textAnchor="middle" fontSize="36" fontWeight="900" fill="#e9d700" fontFamily="JetBrains Mono, monospace">
              {item.label}
            </text>
            <text x="100" y="128" textAnchor="middle" fontSize="26" fill="#F5F5F5" fontFamily="'Noto Sans Devanagari', sans-serif">
              {item.title}
            </text>
          </svg>

          {/* Loading bar */}
          <div style={{ width: 200, height: 4, backgroundColor: '#2D2D2D', borderRadius: 2 }}>
            <div
              style={{
                width: `${item.draw * 100}%`,
                height: '100%',
                backgroundColor: '#e9d700',
                borderRadius: 2,
                transition: 'width 0.1s',
              }}
            />
          </div>
        </div>
      ))}

      {/* Bottom caption */}
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [110, 145], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 80,
            fontWeight: 900,
            color: '#F5F5F5',
          }}
        >
          तीनों परीक्षाएं{' '}
          <span style={{ color: '#e9d700' }}>पास</span> करें
        </div>
      </div>

      {/* Stick figure — right side, arms open welcoming */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="130"
          height="185"
          style={{
            position: 'absolute',
            right: 70,
            bottom: 155,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(55)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="65" cy="175" rx="35" ry="8" fill="#FFC107" opacity="0.2" />
          <line x1="65" y1="82" x2="65" y2="145" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms wide open */}
          <line x1="65" y1="95" x2="18" y2="75" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="95" x2="112" y2="75" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="145" x2="45" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="65" y1="145" x2="85" y2="175" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="65" cy="60" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <path d="M 55 66 Q 65 76 75 66" stroke="#e9d700" strokeWidth="2.5" fill="none" />
          <circle cx="58" cy="55" r="2.5" fill="#F5F5F5" />
          <circle cx="72" cy="55" r="2.5" fill="#F5F5F5" />
          <rect x="51" y="51" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="66" y="51" width="13" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="64" y1="55" x2="66" y2="55" stroke="#FFC107" strokeWidth="2" />
          {/* 3 fingers up */}
          <line x1="18" y1="75" x2="10" y2="55" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="18" y1="75" x2="18" y2="53" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="18" y1="75" x2="26" y2="55" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig4" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig4)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S4_ThreeTests;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S5_Test1Principal',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S5_Test1Principal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Test badge flips in
  const badgeScale = sp(10, { damping: 10, stiffness: 200 });

  // Shield draw animation
  const shieldProgress = interpolate(frame, [25, 90], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Market wave animation
  const waveOffset = frame * 2.2;

  // Checkmark draws at end
  const checkProgress = interpolate(frame, [100, 145], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "01" number entrance
  const numOp = sp(5, { damping: 14, stiffness: 100 });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 6) * 1920,
    y: seededRandom(i * 6 + 1) * 1080,
    size: seededRandom(i * 6 + 2) * 3 + 1.5,
    speed: seededRandom(i * 8) * 0.2 + 0.08,
    phase: seededRandom(i * 6 + 3) * Math.PI * 2,
  }));

  // Shield SVG path
  const shieldPathLength = 400;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — deep navy */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg5" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#0a0a18" />
              <stop offset="50%" stopColor="#1C1C1C" />
              <stop offset="100%" stopColor="#1a1200" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg5)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — large shield outline background */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <path
            d="M 960 80 L 1260 220 L 1260 580 Q 1260 800 960 940 Q 660 800 660 580 L 660 220 Z"
            fill="#e9d700"
            opacity="0.03"
            transform={`scale(${1 + Math.sin(frame * 0.008) * 0.01}) translate(${Math.sin(frame * 0.006) * 8} ${Math.cos(frame * 0.007) * 6})`}
            style={{ transformOrigin: '960px 540px' }}
          />
          <rect
            x="0"
            y={100 + Math.sin(frame * 0.005) * 10}
            width="1920"
            height="2"
            fill="#e9d700"
            opacity="0.05"
          />
          <rect
            x="0"
            y={980 - Math.sin(frame * 0.005) * 10}
            width="1920"
            height="2"
            fill="#e9d700"
            opacity="0.05"
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — dot grid */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.04">
          {Array.from({ length: 20 }, (_, row) =>
            Array.from({ length: 35 }, (_, col) => (
              <circle
                key={`${row}-${col}`}
                cx={col * 56 + 28}
                cy={row * 56 + 28}
                r="1.5"
                fill="#9E9E9E"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#e9d700"
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* LEFT — Test number badge */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: `translateY(-50%) scale(${numOp})`,
          transformOrigin: 'center',
        }}
      >
        <div
          style={{
            width: 180,
            height: 180,
            borderRadius: '50%',
            border: '5px solid #e9d700',
            backgroundColor: '#2D2D2D',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 40px rgba(233,215,0,0.2)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 80,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1,
            }}
          >
            01
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 22,
              color: '#9E9E9E',
              letterSpacing: 1,
            }}
          >
            परीक्षा
          </div>
        </div>
      </div>

      {/* CENTER — Shield with checkmark */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: 'translate(-50%, -55%)',
        }}
      >
        <svg width="300" height="340" viewBox="0 0 300 340">
          {/* Shield fill */}
          <path
            d="M 150 20 L 280 80 L 280 200 Q 280 290 150 330 Q 20 290 20 200 L 20 80 Z"
            fill="#1C1C1C"
            stroke="#e9d700"
            strokeWidth={5 * shieldProgress}
            opacity={shieldProgress}
            strokeDasharray={`${shieldPathLength * shieldProgress} ${shieldPathLength}`}
          />
          {/* Inner shield glow */}
          <path
            d="M 150 20 L 280 80 L 280 200 Q 280 290 150 330 Q 20 290 20 200 L 20 80 Z"
            fill="#e9d700"
            opacity={0.05 * shieldProgress}
          />
          {/* Rupee symbol inside shield */}
          <text
            x="150"
            y="185"
            textAnchor="middle"
            fontSize="90"
            fontWeight="900"
            fill="#e9d700"
            opacity={shieldProgress}
            fontFamily="Inter, Poppins"
          >
            ₹
          </text>
          {/* Checkmark */}
          <polyline
            points="90,185 130,225 210,140"
            stroke="#e9d700"
            strokeWidth="10"
            fill="none"
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeDasharray={`${200 * checkProgress} 200`}
            opacity={checkProgress}
          />
        </svg>
      </div>

      {/* RIGHT — Content */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 650,
        }}
      >
        {/* Headline */}
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            transform: `translateX(${interpolate(sp(20), [0, 1], [80, 0])}px)`,
            opacity: sp(20),
          }}
        >
          मूलधन की{' '}
          <span style={{ color: '#e9d700' }}>रक्षा</span>
        </div>

        {/* Market wave illustration */}
        <div
          style={{
            marginTop: 28,
            opacity: interpolate(frame, [50, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              color: '#9E9E9E',
              marginBottom: 12,
              letterSpacing: 1,
            }}
          >
            बाजार के उतार चढ़ाओ →
          </div>
          <svg width="600" height="80" viewBox="0 0 600 80">
            {/* Volatile market line */}
            <polyline
              points={`0,40 60,${40 + Math.sin((waveOffset + 0) * 0.08) * 28} 120,${40 + Math.sin((waveOffset + 40) * 0.09) * 32} 180,${40 + Math.sin((waveOffset + 80) * 0.07) * 20} 240,${40 + Math.sin((waveOffset + 120) * 0.1) * 35} 300,${40 + Math.sin((waveOffset + 160) * 0.08) * 25} 360,${40 + Math.sin((waveOffset + 200) * 0.09) * 30} 420,${40 + Math.sin((waveOffset + 240) * 0.07) * 18} 480,${40 + Math.sin((waveOffset + 280) * 0.1) * 28} 540,${40 + Math.sin((waveOffset + 320) * 0.08) * 22} 600,40`}
              stroke="#ff0000"
              strokeWidth="2.5"
              fill="none"
              strokeDasharray="5 3"
            />
            {/* Safe flat line */}
            <line x1="0" y1="40" x2="600" y2="40" stroke="#e9d700" strokeWidth="3" strokeDasharray="none" />
            <text x="605" y="44" fontSize="18" fill="#e9d700" fontFamily="Inter, Poppins" fontWeight="700">SAFE</text>
          </svg>
        </div>

        {/* Description */}
        <div
          style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 44,
            fontWeight: 700,
            color: '#F5F5F5',
            marginTop: 20,
            lineHeight: 1.5,
            opacity: interpolate(frame, [80, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          असली पैसा{' '}
          <span style={{ color: '#e9d700' }}>सुरक्षित</span> रहे
        </div>
      </div>

      {/* Stick figure bottom left */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="125"
          height="180"
          style={{
            position: 'absolute',
            left: 85,
            bottom: 150,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(60)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="62" cy="170" rx="33" ry="8" fill="#FFC107" opacity="0.15" />
          <line x1="62" y1="78" x2="62" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="95" x2="28" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="95" x2="96" y2="80" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="43" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="81" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="62" cy="57" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <path d="M 52 64 Q 62 72 72 64" stroke="#e9d700" strokeWidth="2.5" fill="none" />
          <circle cx="55" cy="52" r="2.5" fill="#F5F5F5" />
          <circle cx="69" cy="52" r="2.5" fill="#F5F5F5" />
          <rect x="48" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="63" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="60" y1="52" x2="63" y2="52" stroke="#FFC107" strokeWidth="2" />
          {/* Shield held up */}
          <rect x="96" y="68" width="22" height="26" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" opacity={shieldProgress} />
          <line x1="103" y1="76" x2="110" y2="84" stroke="#e9d700" strokeWidth="1.5" strokeLinecap="round" opacity={checkProgress} />
          <line x1="110" y1="84" x2="116" y2="72" stroke="#e9d700" strokeWidth="1.5" strokeLinecap="round" opacity={checkProgress} />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig5" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig5)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S5_Test1Principal;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S6_Test2Income',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S6_Test2Income: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calendar cells reveal
  const calReveal = interpolate(frame, [20, 110], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Coin drop animations
  const coin1Y = interpolate(frame, [60, 90], [-80, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.bounce),
  });
  const coin2Y = interpolate(frame, [80, 110], [-80, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.bounce),
  });
  const coin3Y = interpolate(frame, [100, 130], [-80, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.bounce),
  });

  // "Reliable" text entrance
  const reliableScale = sp(120, { damping: 8, stiffness: 220 });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 7 + 1) * 1080,
    size: seededRandom(i * 7 + 2) * 3 + 1.5,
    speed: seededRandom(i * 9) * 0.2 + 0.07,
    phase: seededRandom(i * 7 + 3) * Math.PI * 2,
  }));

  const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — dark green tint for income */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg6" x1="0%" y1="100%" x2="100%" y2="0%">
              <stop offset="0%" stopColor="#001a08" />
              <stop offset="60%" stopColor="#1C1C1C" />
              <stop offset="100%" stopColor="#0a1200" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg6)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — overlapping rectangles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <rect
            x={-100 + Math.sin(frame * 0.006) * 15}
            y={300}
            width="600"
            height="500"
            fill="#e9d700"
            opacity="0.03"
            transform={`rotate(${15 + Math.sin(frame * 0.004) * 2} 200 550)`}
          />
          <rect
            x={1500 + Math.cos(frame * 0.007) * 12}
            y={100}
            width="500"
            height="400"
            fill="#e9d700"
            opacity="0.03"
            transform={`rotate(${-20 + Math.cos(frame * 0.005) * 2} 1750 300)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — vertical bar chart background */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.03">
          {Array.from({ length: 22 }, (_, i) => (
            <rect key={i} x={i * 88} y={1080 - (40 + (i % 5) * 55)} width="44" height={40 + (i % 5) * 55} fill="#9E9E9E" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#e9d700"
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* LEFT — Test 02 badge */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(5)})`,
          transformOrigin: 'center',
        }}
      >
        <div
          style={{
            width: 180,
            height: 180,
            borderRadius: '50%',
            border: '5px solid #e9d700',
            backgroundColor: '#2D2D2D',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 40px rgba(233,215,0,0.18)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 80,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1,
            }}
          >
            02
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 22, color: '#9E9E9E' }}>
            परीक्षा
          </div>
        </div>
      </div>

      {/* CENTER — Calendar grid with coin drops */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: 'translate(-50%, -50%)',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 20,
        }}
      >
        {/* Calendar header */}
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 28,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 4,
            opacity: calReveal,
          }}
        >
          MONTHLY INCOME SCHEDULE
        </div>

        {/* Month grid */}
        <div style={{ display: 'flex', gap: 20 }}>
          {months.map((month, i) => {
            const cellReveal = interpolate(frame, [20 + i * 12, 50 + i * 12], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            const coinY = [coin1Y, coin2Y, coin3Y, coin1Y, coin2Y, coin3Y][i];
            return (
              <div
                key={i}
                style={{
                  width: 140,
                  height: 170,
                  backgroundColor: '#2D2D2D',
                  border: '2px solid #e9d700',
                  borderRadius: 12,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'space-between',
                  padding: '14px 10px',
                  opacity: cellReveal,
                  transform: `translateY(${interpolate(cellReveal, [0, 1], [30, 0])}px)`,
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 700, color: '#9E9E9E' }}>
                  {month}
                </div>
                {/* Coin SVG dropping in */}
                <svg
                  width="60"
                  height="60"
                  viewBox="0 0 60 60"
                  style={{ transform: `translateY(${coinY}px)`, opacity: cellReveal }}
                >
                  <circle cx="30" cy="30" r="26" fill="#e9d700" opacity="0.9" />
                  <circle cx="30" cy="30" r="20" fill="none" stroke="#FFC107" strokeWidth="2" />
                  <text x="30" y="35" textAnchor="middle" fontSize="18" fontWeight="900" fill="#1C1C1C" fontFamily="Inter, Poppins">₹</text>
                </svg>
                {/* Checkmark */}
                <svg width="32" height="32" viewBox="0 0 32 32">
                  <circle cx="16" cy="16" r="14" fill="#e9d700" opacity="0.15" />
                  <polyline
                    points="8,16 13,21 24,10"
                    stroke="#e9d700"
                    strokeWidth="3"
                    fill="none"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                  />
                </svg>
              </div>
            );
          })}
        </div>
      </div>

      {/* RIGHT — Content */}
      <div
        style={{
          position: 'absolute',
          right: 90,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 550,
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            opacity: sp(15),
            transform: `translateX(${interpolate(sp(15), [0, 1], [60, 0])}px)`,
          }}
        >
          विश्वसनीय{' '}
          <span style={{ color: '#e9d700' }}>आय</span>
        </div>

        {/* No surprises */}
        <div style={{ marginTop: 28 }}>
          {[
            { text: 'कोई अचानक कटौती नहीं', delay: 70 },
            { text: 'कोई आश्चर्य नहीं', delay: 95 },
            { text: 'वादा = वास्तविकता', delay: 120 },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                marginBottom: 18,
                opacity: interpolate(frame, [item.delay, item.delay + 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                transform: `translateX(${interpolate(frame, [item.delay, item.delay + 25], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
              }}
            >
              <svg width="36" height="36" viewBox="0 0 36 36">
                <circle cx="18" cy="18" r="16" fill="#e9d700" opacity="0.15" />
                <polyline points="9,18 14,23 27,11" stroke="#e9d700" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: '#F5F5F5' }}>
                {item.text}
              </div>
            </div>
          ))}
        </div>

        {/* RELIABLE stamp */}
        <div
          style={{
            marginTop: 20,
            transform: `scale(${reliableScale})`,
            transformOrigin: 'left center',
            display: 'inline-block',
            border: '4px solid #e9d700',
            padding: '8px 24px',
            borderRadius: 6,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900, color: '#e9d700', letterSpacing: 5 }}>
            RELIABLE
          </div>
        </div>
      </div>

      {/* Stick figure bottom right */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="125"
          height="180"
          style={{
            position: 'absolute',
            right: 75,
            bottom: 145,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(50)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="62" cy="170" rx="33" ry="8" fill="#FFC107" opacity="0.15" />
          <line x1="62" y1="78" x2="62" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms counting coins */}
          <line x1="62" y1="96" x2="25" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="96" x2="99" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="43" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="81" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="62" cy="57" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <path d="M 52 64 Q 62 72 72 64" stroke="#e9d700" strokeWidth="2.5" fill="none" />
          <circle cx="55" cy="52" r="2.5" fill="#F5F5F5" />
          <circle cx="69" cy="52" r="2.5" fill="#F5F5F5" />
          <rect x="48" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="63" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="60" y1="52" x2="63" y2="52" stroke="#FFC107" strokeWidth="2" />
          {/* Coin held in right hand */}
          <circle cx="99" cy={110 + interpolate(frame, [60, 75], [-20, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} r="10" fill="#e9d700" opacity="0.8" />
          <text x="99" y={113 + interpolate(frame, [60, 75], [-20, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} textAnchor="middle" fontSize="9" fill="#1C1C1C" fontWeight="900" fontFamily="Inter">₹</text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig6" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig6)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S6_Test2Income;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S7_Test3Liquidity',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S7_Test3Liquidity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tap animation — hand tapping
  const tapY = Math.sin(frame * 0.2) * 8;

  // ATM door opens
  const doorOpen = interpolate(frame, [30, 80], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Cash slides out
  const cashX = interpolate(frame, [90, 140], [-200, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.back(1.3)),
  });

  // No penalty stamp
  const penaltyScale = sp(140, { damping: 8, stiffness: 250 });

  const particles = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 8) * 1920,
    y: seededRandom(i * 8 + 1) * 1080,
    size: seededRandom(i * 8 + 2) * 3 + 1.5,
    speed: seededRandom(i * 10) * 0.2 + 0.07,
    phase: seededRandom(i * 8 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — deep teal */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg7" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#001214" />
              <stop offset="60%" stopColor="#1C1C1C" />
              <stop offset="100%" stopColor="#0f1400" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg7)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — flowing wave shapes */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <ellipse
            cx={960 + Math.sin(frame * 0.006) * 20}
            cy={800 + Math.cos(frame * 0.007) * 15}
            rx="900"
            ry="200"
            fill="#e9d700"
            opacity="0.03"
          />
          <path
            d={`M 0 ${400 + Math.sin(frame * 0.01) * 30} Q 480 ${350 + Math.cos(frame * 0.008) * 25} 960 ${400 + Math.sin(frame * 0.01) * 30} Q 1440 ${450 + Math.cos(frame * 0.008) * 25} 1920 ${400 + Math.sin(frame * 0.01) * 30}`}
            stroke="#e9d700"
            strokeWidth="2"
            fill="none"
            opacity="0.05"
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — flowing horizontal lines */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.03">
          {Array.from({ length: 8 }, (_, i) => (
            <path
              key={i}
              d={`M 0 ${100 + i * 120} Q 480 ${80 + i * 120} 960 ${100 + i * 120} Q 1440 ${120 + i * 120} 1920 ${100 + i * 120}`}
              stroke="#9E9E9E"
              strokeWidth="1.5"
              fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#e9d700"
              opacity={0.07}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* LEFT — Test 03 badge */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: `translateY(-50%) scale(${sp(5)})`,
          transformOrigin: 'center',
        }}
      >
        <div
          style={{
            width: 180,
            height: 180,
            borderRadius: '50%',
            border: '5px solid #e9d700',
            backgroundColor: '#2D2D2D',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            boxShadow: '0 0 40px rgba(233,215,0,0.18)',
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 80, fontWeight: 900, color: '#e9d700', lineHeight: 1 }}>03</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 22, color: '#9E9E9E' }}>परीक्षा</div>
        </div>
      </div>

      {/* CENTER — ATM / Withdraw animation */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: 'translate(-50%, -55%)',
        }}
      >
        <svg width="280" height="380" viewBox="0 0 280 380">
          {/* ATM body */}
          <rect x="20" y="20" width="240" height="340" rx="18" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3" opacity={sp(5)} />
          <rect x="50" y="50" width="180" height="100" rx="8" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="2" opacity={sp(5)} />
          <text x="140" y="115" textAnchor="middle" fontSize="52" fill="#e9d700" fontFamily="JetBrains Mono, monospace" fontWeight="900" opacity={sp(5)}>₹</text>

          {/* ATM slot */}
          <rect x="60" y="200" width="160" height="25" rx="4" fill="#1C1C1C" stroke="#9E9E9E" strokeWidth="2" opacity={sp(10)} />

          {/* Cash sliding out */}
          <rect
            x={60 + cashX}
            y="205"
            width="160"
            height="15"
            rx="3"
            fill="#e9d700"
            opacity={interpolate(frame, [90, 100], [0, 0.9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />

          {/* Keypad */}
          {Array.from({ length: 9 }, (_, i) => (
            <rect key={i} x={65 + (i % 3) * 50} y={255 + Math.floor(i / 3) * 28} width="30" height="18" rx="4" fill="#3D3D3D" opacity={sp(15)} />
          ))}

          {/* Door opening animation */}
          <rect
            x="60"
            y="170"
            width={160 * doorOpen}
            height="8"
            rx="2"
            fill="#e9d700"
            opacity={0.6 * doorOpen}
          />
        </svg>

        {/* Hand tapping SVG */}
        <div style={{ position: 'absolute', right: -80, top: 80, transform: `translateY(${tapY}px)` }}>
          <svg width="80" height="100" viewBox="0 0 80 100">
            <path d="M 20 80 L 15 60 L 20 30 L 25 20 L 30 20 L 30 45 L 35 45 L 35 25 L 40 25 L 40 45 L 45 45 L 45 30 L 50 30 L 50 45 L 55 45 L 55 35 L 60 35 L 60 55 L 55 65 L 55 80 Z"
              fill="#F5F5F5" opacity="0.7" />
          </svg>
        </div>
      </div>

      {/* RIGHT — Content */}
      <div
        style={{
          position: 'absolute',
          right: 90,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 620,
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 90,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            opacity: sp(15),
            transform: `translateX(${interpolate(sp(15), [0, 1], [60, 0])}px)`,
          }}
        >
          तरलता{' '}
          <span style={{ color: '#e9d700' }}>(Liquidity)</span>
        </div>

        <div
          style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontSize: 46,
            fontWeight: 700,
            color: '#F5F5F5',
            lineHeight: 1.5,
            marginTop: 24,
            opacity: interpolate(frame, [40, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          जब{' '}
          <span style={{ color: '#e9d700' }}>चाहिए</span> तब निकालो
        </div>

        {/* Two NO penalty badges */}
        {[
          { text: 'कोई बड़ा नुकसान नहीं', delay: 75 },
          { text: 'कोई दंड नहीं', delay: 100 },
        ].map((item, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 16,
              marginTop: 18,
              opacity: interpolate(frame, [item.delay, item.delay + 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateX(${interpolate(frame, [item.delay, item.delay + 25], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            }}
          >
            <svg width="40" height="40" viewBox="0 0 40 40">
              <circle cx="20" cy="20" r="18" fill="#e9d700" opacity="0.15" stroke="#e9d700" strokeWidth="2" />
              <line x1="12" y1="12" x2="28" y2="28" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
              <line x1="28" y1="12" x2="12" y2="28" stroke="#ff0000" strokeWidth="3" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 44,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              {item.text}
            </div>
          </div>
        ))}

        {/* LIQUID stamp */}
        <div
          style={{
            marginTop: 28,
            transform: `scale(${penaltyScale})`,
            transformOrigin: 'left center',
            display: 'inline-block',
            border: '4px solid #e9d700',
            padding: '8px 24px',
            borderRadius: 6,
          }}
        >
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900, color: '#e9d700', letterSpacing: 5 }}>
            LIQUID ✓
          </div>
        </div>
      </div>

      {/* Stick figure bottom left */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="125"
          height="180"
          style={{
            position: 'absolute',
            left: 80,
            bottom: 145,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(45)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="62" cy="170" rx="33" ry="8" fill="#FFC107" opacity="0.15" />
          <line x1="62" y1="78" x2="62" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="98" x2="22" y2="116" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="98" x2="102" y2="88" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="43" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="81" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="62" cy="57" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <path d="M 52 64 Q 62 72 72 64" stroke="#e9d700" strokeWidth="2.5" fill="none" />
          <circle cx="55" cy="52" r="2.5" fill="#F5F5F5" />
          <circle cx="69" cy="52" r="2.5" fill="#F5F5F5" />
          <rect x="48" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="63" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="60" y1="52" x2="63" y2="52" stroke="#FFC107" strokeWidth="2" />
          {/* Thumb up from right hand */}
          <path d="M 102 88 L 108 78 L 112 76 L 112 86 L 108 88 Z" fill="#e9d700" opacity="0.8" />
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig7" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.65" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig7)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S7_Test3Liquidity;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S8_PopularFail',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S8_PopularFail: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Investments fall in from top one by one
  const investments = [
    { name: 'Dividend Stocks', nameHi: 'डिविडेंड शेयर', fail: 'लाभांश कम हो सकता है', delay: 10 },
    { name: 'Long-term Bonds', nameHi: 'लंबी अवधि बॉन्ड', fail: 'ब्याज बढ़ने पर कीमत गिरे', delay: 30 },
    { name: 'Real Estate', nameHi: 'संपत्ति', fail: 'तरलता नहीं', delay: 50 },
    { name: 'Annuities', nameHi: 'वार्षिकी', fail: 'पैसा 10 साल बंद', delay: 70 },
  ];

  // Three test icons
  const testIcons = ['₹', '₹', '↑'];

  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 9 + 1) * 1080,
    size: seededRandom(i * 9 + 2) * 3 + 1.5,
    speed: seededRandom(i * 11) * 0.2 + 0.07,
    phase: seededRandom(i * 9 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — dark red, danger */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="bg8" cx="50%" cy="50%" r="70%">
              <stop offset="0%" stopColor="#1a0505" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg8)" />
        </svg>
      </AbsoluteFill>

      {/* Shape layer — warning triangle shapes */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <polygon
            points={`${1700 + Math.sin(frame * 0.005) * 15},100 ${1850 + Math.sin(frame * 0.005) * 15},350 ${1550 + Math.sin(frame * 0.005) * 15},350`}
            fill="#ff0000"
            opacity="0.04"
          />
          <polygon
            points={`${100 + Math.cos(frame * 0.006) * 12},700 ${250 + Math.cos(frame * 0.006) * 12},950 ${-50 + Math.cos(frame * 0.006) * 12},950`}
            fill="#ff0000"
            opacity="0.04"
          />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — diagonal danger stripes */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.02">
          {Array.from({ length: 18 }, (_, i) => (
            <line
              key={i}
              x1={i * 115 - 100}
              y1="0"
              x2={i * 115 + 400}
              y2="1080"
              stroke="#ff0000"
              strokeWidth="8"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#ff0000"
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Top headline */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(5),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
          }}
        >
          तीनों परीक्षाएं लागू करो →{' '}
          <span style={{ color: '#ff0000' }}>क्या होता है?</span>
        </div>
      </div>

      {/* Three test column headers */}
      <div
        style={{
          position: 'absolute',
          top: 175,
          left: 500,
          right: 80,
          display: 'flex',
          gap: 0,
          opacity: interpolate(frame, [15, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        {['मूलधन रक्षा', 'विश्वसनीय आय', 'तरलता'].map((label, i) => (
          <div
            key={i}
            style={{
              flex: 1,
              textAlign: 'center',
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 30,
              fontWeight: 700,
              color: '#e9d700',
              borderBottom: '2px solid #e9d700',
              paddingBottom: 10,
            }}
          >
            {label}
          </div>
        ))}
      </div>

      {/* Investment rows */}
      <div
        style={{
          position: 'absolute',
          top: 250,
          left: 80,
          right: 80,
          display: 'flex',
          flexDirection: 'column',
          gap: 16,
        }}
      >
        {investments.map((inv, i) => {
          const rowOp = interpolate(frame, [inv.delay, inv.delay + 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          const rowY = interpolate(frame, [inv.delay, inv.delay + 25], [-40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

          // Which tests fail: varied per investment
          const testResults = [
            [true, false, true],   // Dividend stocks — income unreliable
            [false, true, false],  // Long bonds — principal & liquidity fail
            [true, true, false],   // Real estate — liquidity fail
            [true, true, false],   // Annuities — liquidity fail
          ][i];

          return (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                opacity: rowOp,
                transform: `translateY(${rowY}px)`,
                backgroundColor: '#2D2D2D',
                border: '1px solid #3D3D3D',
                borderRadius: 10,
                overflow: 'hidden',
              }}
            >
              {/* Investment name */}
              <div
                style={{
                  width: 420,
                  padding: '16px 24px',
                  borderRight: '1px solid #3D3D3D',
                }}
              >
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 36, fontWeight: 700, color: '#F5F5F5' }}>
                  {inv.nameHi}
                </div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, color: '#9E9E9E' }}>{inv.name}</div>
              </div>

              {/* Three test results */}
              {testResults.map((pass, j) => (
                <div
                  key={j}
                  style={{
                    flex: 1,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    padding: '16px',
                    borderRight: j < 2 ? '1px solid #3D3D3D' : 'none',
                    backgroundColor: pass ? 'rgba(233,215,0,0.05)' : 'rgba(255,0,0,0.08)',
                  }}
                >
                  <svg width="40" height="40" viewBox="0 0 40 40">
                    {pass ? (
                      <>
                        <circle cx="20" cy="20" r="18" fill="#e9d700" opacity="0.12" />
                        <polyline points="10,20 16,26 30,12" stroke="#e9d700" strokeWidth="3.5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                      </>
                    ) : (
                      <>
                        <circle cx="20" cy="20" r="18" fill="#ff0000" opacity="0.12" />
                        <line x1="12" y1="12" x2="28" y2="28" stroke="#ff0000" strokeWidth="3.5" strokeLinecap="round" />
                        <line x1="28" y1="12" x2="12" y2="28" stroke="#ff0000" strokeWidth="3.5" strokeLinecap="round" />
                      </>
                    )}
                  </svg>
                </div>
              ))}

              {/* Fail reason */}
              <div
                style={{
                  width: 320,
                  padding: '14px 18px',
                  borderLeft: '1px solid #3D3D3D',
                }}
              >
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 26, color: '#ff0000' }}>
                  {inv.fail}
                </div>
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom call out */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [145, 175], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 62,
            fontWeight: 900,
            color: '#ff0000',
          }}
        >
          अचानक{' '}
          <span style={{ color: '#F5F5F5' }}>सुरक्षित नहीं</span> दिखते
        </div>
      </div>

      {/* Stick figure — shocked, right side */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="125"
          height="180"
          style={{
            position: 'absolute',
            right: 50,
            bottom: 145,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(60)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="62" cy="170" rx="33" ry="8" fill="#ff0000" opacity="0.12" />
          <line x1="62" y1="78" x2="62" y2="140" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Arms thrown up in shock */}
          <line x1="62" y1="92" x2="18" y2="62" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="92" x2="106" y2="62" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="43" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="62" y1="140" x2="81" y2="170" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="62" cy="57" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Shocked 'O' mouth */}
          <circle cx="62" cy="68" r="5" fill="none" stroke="#ff0000" strokeWidth="2" />
          <circle cx="55" cy="52" r="2.5" fill="#F5F5F5" />
          <circle cx="69" cy="52" r="2.5" fill="#F5F5F5" />
          <rect x="48" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="63" y="48" width="12" height="9" rx="4" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="60" y1="52" x2="63" y2="52" stroke="#FFC107" strokeWidth="2" />
          {/* !! above head */}
          <text x="62" y={36 - Math.abs(Math.sin(frame * 0.1)) * 4} textAnchor="middle" fontSize="22" fill="#ff0000" fontWeight="900" fontFamily="Inter">!!</text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig8" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.7" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig8)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S8_PopularFail;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S9_TrueSafety',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S9_TrueSafety: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Moon/night atmosphere — peaceful scene
  const moonGlow = interpolate(frame, [0, 60], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Stars twinkle
  const stars = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i * 5 + 7) * 1920,
    y: seededRandom(i * 5 + 8) * 400,
    size: seededRandom(i * 5 + 9) * 2.5 + 1,
    phase: seededRandom(i * 5 + 10) * Math.PI * 2,
    speed: seededRandom(i * 5 + 11) * 0.08 + 0.03,
  }));

  // Three pillar reveals
  const p1 = interpolate(frame, [30, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const p2 = interpolate(frame, [55, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const p3 = interpolate(frame, [80, 115], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 10) * 1920,
    y: seededRandom(i * 10 + 1) * 1080,
    size: seededRandom(i * 10 + 2) * 2.5 + 1,
    speed: seededRandom(i * 12) * 0.15 + 0.05,
    phase: seededRandom(i * 10 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — peaceful night sky gradient */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <linearGradient id="bg9" x1="0%" y1="0%" x2="0%" y2="100%">
              <stop offset="0%" stopColor="#050818" />
              <stop offset="50%" stopColor="#0d1a0d" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </linearGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg9)" />
        </svg>
      </AbsoluteFill>

      {/* Stars */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {stars.map((s, i) => (
            <circle
              key={i}
              cx={s.x}
              cy={s.y}
              r={s.size}
              fill="#F5F5F5"
              opacity={(0.3 + Math.sin(frame * s.speed + s.phase) * 0.3) * moonGlow}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Shape layer — moon shape */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <circle
            cx={1750 + Math.sin(frame * 0.005) * 5}
            cy={130 + Math.cos(frame * 0.006) * 4}
            r="90"
            fill="#F5F5F5"
            opacity={0.06 * moonGlow}
          />
          <circle
            cx={1750 + Math.sin(frame * 0.005) * 5}
            cy={130 + Math.cos(frame * 0.006) * 4}
            r="110"
            fill="none"
            stroke="#e9d700"
            strokeWidth="1.5"
            opacity={0.08 * moonGlow}
          />
          {/* Ground/horizon */}
          <line x1="0" y1="900" x2="1920" y2="900" stroke="#9E9E9E" strokeWidth="1" opacity="0.1" />
          {/* Simple house silhouette */}
          <polygon points="700,900 700,750 800,680 900,750 900,900" fill="#2D2D2D" opacity={0.4 * moonGlow} />
          <rect x="740" y="810" width="40" height="90" fill="#1C1C1C" opacity="0.6" />
          {/* House window — warm light */}
          <rect x="820" y="780" width="40" height="35" rx="3" fill="#e9d700" opacity={0.3 * moonGlow + Math.sin(frame * 0.08) * 0.1} />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — soft wavy lines */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080" opacity="0.03">
          {Array.from({ length: 5 }, (_, i) => (
            <path
              key={i}
              d={`M 0 ${200 + i * 170} Q 480 ${180 + i * 170} 960 ${200 + i * 170} Q 1440 ${220 + i * 170} 1920 ${200 + i * 170}`}
              stroke="#9E9E9E"
              strokeWidth="1"
              fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 8}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 6}
              r={p.size}
              fill="#e9d700"
              opacity={0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Central content */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          left: 0,
          right: 0,
          bottom: 200,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 32,
        }}
      >
        {/* Main headline */}
        <div
          style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 96,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
            opacity: sp(5),
            transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
          }}
        >
          सच्ची{' '}
          <span style={{ color: '#e9d700' }}>सुरक्षा</span> का मतलब
        </div>

        {/* Three peaceful pillars */}
        <div style={{ display: 'flex', gap: 48, alignItems: 'stretch' }}>
          {[
            { icon: '₹', text: 'जब चाहिए\nतब पैसा वहाँ है', op: p1 },
            { icon: '📅', text: 'आय समय पर\nआती है', op: p2 },
            { icon: '🌙', text: 'बिना सोचे\nसो सकते हो', op: p3 },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                width: 380,
                backgroundColor: 'rgba(45,45,45,0.7)',
                border: '2px solid #e9d700',
                borderRadius: 20,
                padding: '32px 28px',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 18,
                opacity: item.op,
                transform: `translateY(${interpolate(item.op, [0, 1], [50, 0])}px)`,
                backdropFilter: 'blur(4px)',
              }}
            >
              {/* Icon circle */}
              <svg width="80" height="80" viewBox="0 0 80 80">
                <circle cx="40" cy="40" r="36" fill="#e9d700" opacity="0.12" stroke="#e9d700" strokeWidth="2" />
                <text x="40" y="48" textAnchor="middle" fontSize="32" fill="#e9d700" fontFamily="Inter, Poppins" fontWeight="900">
                  {i === 0 ? '₹' : i === 1 ? '✓' : '◑'}
                </text>
              </svg>

              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 38,
                  fontWeight: 700,
                  color: '#F5F5F5',
                  textAlign: 'center',
                  lineHeight: 1.4,
                  whiteSpace: 'pre-line',
                }}
              >
                {item.text}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Peaceful sleeping stick figure bottom center */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="160"
          height="90"
          style={{
            position: 'absolute',
            left: '50%',
            bottom: 60,
            transform: `translateX(-50%) scale(${sp(100)})`,
            transformOrigin: 'bottom center',
          }}
        >
          {/* Sleeping figure lying down */}
          <ellipse cx="80" cy="65" rx="60" ry="18" fill="#2D2D2D" stroke="#F5F5F5" strokeWidth="2" opacity="0.7" />
          <circle cx="130" cy="48" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Closed eyes — sleeping */}
          <line x1="122" y1="46" x2="128" y2="46" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          <line x1="132" y1="46" x2="138" y2="46" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
          {/* Smile */}
          <path d="M 124 54 Q 130 60 136 54" stroke="#e9d700" strokeWidth="2" fill="none" />
          {/* Glasses */}
          <rect x="118" y="42" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.5" />
          <rect x="130" y="42" width="10" height="7" rx="3" fill="none" stroke="#FFC107" strokeWidth="1.5" />
          {/* ZZZ */}
          <text
            x="155"
            y={30 - Math.sin(frame * 0.06) * 4}
            fontSize="18"
            fill="#e9d700"
            fontWeight="900"
            fontFamily="Inter"
            opacity={interpolate(frame, [90, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          >
            zzz
          </text>
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig9" cx="50%" cy="50%" r="70%">
              <stop offset="55%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.7" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig9)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S9_TrueSafety;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C3S10_SimpleDefinition',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C3S10_SimpleDefinition: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Cinematic full-screen text reveal — word by word
  const word1 = interpolate(frame, [5, 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word2 = interpolate(frame, [20, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word3 = interpolate(frame, [35, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const word4 = interpolate(frame, [50, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Gold line expand
  const lineWidth = interpolate(frame, [60, 110], [0, 1920], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.expo),
  });

  // "सब कुछ" punch scale
  const sabScale = sp(80, { damping: 6, stiffness: 280 });

  // Screen flash on "सब कुछ"
  const flash = interpolate(frame, [80, 82, 90, 95], [0, 0.18, 0.05, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 11) * 1920,
    y: seededRandom(i * 11 + 1) * 1080,
    size: seededRandom(i * 11 + 2) * 4 + 1.5,
    speed: seededRandom(i * 13) * 0.25 + 0.08,
    phase: seededRandom(i * 11 + 3) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Background — pure charcoal with gold tint */}
      <AbsoluteFill>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="bg10" cx="50%" cy="50%" r="60%">
              <stop offset="0%" stopColor="#1a1500" />
              <stop offset="100%" stopColor="#1C1C1C" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#bg10)" />
        </svg>
      </AbsoluteFill>

      {/* Flash overlay */}
      <AbsoluteFill style={{ backgroundColor: '#e9d700', opacity: flash, pointerEvents: 'none' }} />

      {/* Shape layer — starburst/radiating lines from center */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 24 }, (_, i) => {
            const angle = (i / 24) * Math.PI * 2;
            const len = 500 + Math.sin(frame * 0.01 + i * 0.5) * 50;
            return (
              <line
                key={i}
                x1="960"
                y1="540"
                x2={960 + Math.cos(angle) * len}
                y2={540 + Math.sin(angle) * len}
                stroke="#e9d700"
                strokeWidth="1"
                opacity={0.03 + word4 * 0.02}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Motion texture — large circle rings */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {[200, 350, 500, 700].map((r, i) => (
            <circle
              key={i}
              cx="960"
              cy="540"
              r={r + Math.sin(frame * 0.008 + i) * 8}
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1"
              opacity="0.04"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x + Math.sin(frame * p.speed + p.phase) * 10}
              cy={p.y + Math.cos(frame * p.speed * 0.7 + p.phase) * 7}
              r={p.size}
              fill="#e9d700"
              opacity={0.06}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Gold horizontal divider line sweeping */}
      <div
        style={{
          position: 'absolute',
          top: '50%',
          left: 0,
          height: 3,
          width: lineWidth,
          backgroundColor: '#e9d700',
          opacity: 0.5,
          transform: 'translateY(-50%)',
          marginTop: 80,
        }}
      />

      {/* MAIN TEXT — cinematic center */}
      <div
        style={{
          position: 'absolute',
          inset: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 8,
        }}
      >
        {/* Line 1 */}
        <div style={{ display: 'flex', gap: 24, alignItems: 'baseline' }}>
          <span
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 80,
              fontWeight: 400,
              color: '#9E9E9E',
              opacity: word1,
              transform: `translateY(${interpolate(word1, [0, 1], [30, 0])}px)`,
              display: 'inline-block',
            }}
          >
            यह एक
          </span>
          <span
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 120,
              fontWeight: 900,
              color: '#F5F5F5',
              opacity: word2,
              transform: `translateY(${interpolate(word2, [0, 1], [30, 0])}px)`,
              display: 'inline-block',
            }}
          >
            सरल
          </span>
          <span
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 80,
              fontWeight: 400,
              color: '#9E9E9E',
              opacity: word2,
              transform: `translateY(${interpolate(word2, [0, 1], [30, 0])}px)`,
              display: 'inline-block',
            }}
          >
            परिभाषा है
          </span>
        </div>

        {/* Dash */}
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 60,
            color: '#e9d700',
            opacity: word3,
          }}
        >
          —
        </div>

        {/* Line 2 */}
        <div style={{ display: 'flex', gap: 24, alignItems: 'baseline' }}>
          <span
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 80,
              fontWeight: 400,
              color: '#9E9E9E',
              opacity: word3,
              transform: `translateY(${interpolate(word3, [0, 1], [30, 0])}px)`,
              display: 'inline-block',
            }}
          >
            लेकिन यह
          </span>
          <span
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 200,
              fontWeight: 900,
              color: '#e9d700',
              opacity: word4,
              transform: `scale(${sabScale}) translateY(${interpolate(word4, [0, 1], [20, 0])}px)`,
              display: 'inline-block',
              transformOrigin: 'center bottom',
              lineHeight: 1,
              textShadow: '0 0 60px rgba(233,215,0,0.4)',
            }}
          >
            सब कुछ
          </span>
          <span
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 80,
              fontWeight: 400,
              color: '#9E9E9E',
              opacity: word4,
              transform: `translateY(${interpolate(word4, [0, 1], [30, 0])}px)`,
              display: 'inline-block',
            }}
          >
            है
          </span>
        </div>
      </div>

      {/* Stick figure — small, triumphant, bottom-right */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg
          width="110"
          height="160"
          style={{
            position: 'absolute',
            right: 80,
            bottom: 140,
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(70)})`,
            transformOrigin: 'bottom center',
          }}
        >
          <ellipse cx="55" cy="152" rx="30" ry="7" fill="#FFC107" opacity="0.2" />
          <line x1="55" y1="72" x2="55" y2="128" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Both arms raised in victory */}
          <line x1="55" y1="84" x2="18" y2="56" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="84" x2="92" y2="56" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="128" x2="37" y2="152" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="128" x2="73" y2="152" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <circle cx="55" cy="52" r="20" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          <path d="M 46 58 Q 55 68 64 58" stroke="#e9d700" strokeWidth="2.5" fill="none" />
          <circle cx="49" cy="47" r="2.5" fill="#F5F5F5" />
          <circle cx="61" cy="47" r="2.5" fill="#F5F5F5" />
          <rect x="42" y="43" width="11" height="8" rx="3.5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <rect x="56" y="43" width="11" height="8" rx="3.5" fill="none" stroke="#FFC107" strokeWidth="2" />
          <line x1="53" y1="47" x2="56" y2="47" stroke="#FFC107" strokeWidth="2" />
          {/* Stars above raised hands */}
          {[0, 1].map(i => (
            <text
              key={i}
              x={i === 0 ? 14 : 92}
              y={48 - Math.abs(Math.sin(frame * 0.08 + i * 1.5)) * 5}
              textAnchor="middle"
              fontSize="16"
              fill="#e9d700"
              opacity={word4}
              fontFamily="Inter"
            >
              ★
            </text>
          ))}
        </svg>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          <defs>
            <radialGradient id="vig10" cx="50%" cy="50%" r="65%">
              <stop offset="50%" stopColor="transparent" />
              <stop offset="100%" stopColor="#000000" stopOpacity="0.7" />
            </radialGradient>
          </defs>
          <rect width="1920" height="1080" fill="url(#vig10)" />
        </svg>
      </AbsoluteFill>

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C3S10_SimpleDefinition;
```

