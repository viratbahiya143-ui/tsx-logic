```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_01',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_01: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Horizontal scan line
  const scanY = interpolate(frame, [0, durationInFrames], [0, 1080], { extrapolateRight: 'clamp' });

  // Title reveal
  const titleX = interpolate(sp(10), [0, 1], [-120, 0]);
  const titleOp = interpolate(sp(10), [0, 1], [0, 1]);

  // Sub reveal
  const subOp = interpolate(sp(25), [0, 1], [0, 1]);
  const subY = interpolate(sp(25), [0, 1], [30, 0]);

  // CTA bar scale
  const ctaScale = sp(40, { stiffness: 200, damping: 18 });

  // Pulse ring
  const pulseR = interpolate(frame % 60, [0, 60], [0, 80]);
  const pulseOp = interpolate(frame % 60, [0, 30, 60], [0.6, 0.2, 0]);

  // Icon micro bounce
  const iconBounce = Math.sin(frame * 0.12) * 4;
  const iconRotate = Math.sin(frame * 0.07) * 3;

  // Character
  const charY = Math.sin(frame * 0.05) * 3;
  const headTilt = Math.sin(frame * 0.04) * 4;
  const charOp = interpolate(sp(45), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F0F0F0', overflow: 'hidden', opacity: exitOp }}>

      {/* Scan line texture */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        {Array.from({ length: 28 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute',
            top: i * 40,
            left: 0,
            width: '100%',
            height: 1,
            backgroundColor: '#D0D0D0',
            opacity: 0.5,
          }} />
        ))}
      </AbsoluteFill>

      {/* Moving scan highlight */}
      <div style={{
        position: 'absolute',
        top: scanY - 3,
        left: 0,
        width: '100%',
        height: 6,
        background: 'linear-gradient(90deg, transparent, #e9d700 40%, #e9d700 60%, transparent)',
        opacity: 0.3,
      }} />

      {/* Left accent block */}
      <div style={{
        position: 'absolute',
        left: 0,
        top: 0,
        width: 14,
        height: '100%',
        backgroundColor: '#1C1C1C',
      }} />

      {/* Geometric shape — large circle */}
      <div style={{
        position: 'absolute',
        right: -180,
        top: -180,
        width: 600,
        height: 600,
        borderRadius: '50%',
        border: '3px solid #1C1C1C',
        opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Geometric shape — rectangle drift */}
      <div style={{
        position: 'absolute',
        left: 60,
        bottom: 60,
        width: 300,
        height: 300,
        border: '2px solid #1C1C1C',
        opacity: 0.04,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i * 3) * 1920;
        const py = seededRandom(i * 3 + 1) * 1080;
        const drift = Math.sin(frame * 0.03 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py,
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: '#1C1C1C',
            opacity: 0.08,
          }} />
        );
      })}

      {/* Main content */}
      <div style={{
        position: 'absolute',
        left: 140,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 900,
      }}>

        {/* Tag */}
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 10,
          backgroundColor: '#1C1C1C',
          padding: '8px 22px',
          marginBottom: 32,
          opacity: titleOp,
          transform: `translateX(${titleX}px)`,
        }}>
          <div style={{ width: 8, height: 8, borderRadius: '50%', backgroundColor: '#e9d700' }} />
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#F5F5F5', letterSpacing: 3 }}>
            CHAPTER 3
          </span>
        </div>

        {/* Hero text */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 88,
          fontWeight: 900,
          color: '#1C1C1C',
          lineHeight: 1.05,
          opacity: titleOp,
          transform: `translateX(${titleX}px)`,
        }}>
          दुनिया के सबक<br />
          <span style={{ color: '#e9d700', WebkitTextStroke: '2px #1C1C1C' }}>भारत का हाल</span>
        </div>

        {/* Divider */}
        <div style={{
          width: interpolate(sp(30), [0, 1], [0, 420]),
          height: 4,
          backgroundColor: '#1C1C1C',
          marginTop: 28,
          marginBottom: 28,
        }} />

        {/* Sub */}
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 38,
          fontWeight: 600,
          color: '#2D2D2D',
          opacity: subOp,
          transform: `translateY(${subY}px)`,
          lineHeight: 1.4,
        }}>
          गहरी Financial सच्चाईयां — साफ भाषा में
        </div>

        {/* CTA bar */}
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 16,
          backgroundColor: '#e9d700',
          padding: '14px 32px',
          marginTop: 44,
          transform: `scaleX(${ctaScale})`,
          transformOrigin: 'left',
        }}>
          {/* Bell icon SVG */}
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
            <path d="M13.73 21a2 2 0 0 1-3.46 0" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800, color: '#1C1C1C', letterSpacing: 1 }}>
            SUBSCRIBE करिए
          </span>
        </div>
      </div>

      {/* Right side — icon + pulse */}
      <div style={{
        position: 'absolute',
        right: 200,
        top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 24,
        opacity: subOp,
      }}>
        {/* Pulse rings */}
        <div style={{ position: 'relative', width: 200, height: 200, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          <div style={{
            position: 'absolute',
            width: 120 + pulseR,
            height: 120 + pulseR,
            borderRadius: '50%',
            border: '2px solid #1C1C1C',
            opacity: pulseOp,
          }} />
          <div style={{
            position: 'absolute',
            width: 80 + pulseR * 0.5,
            height: 80 + pulseR * 0.5,
            borderRadius: '50%',
            border: '1.5px solid #e9d700',
            opacity: pulseOp * 0.7,
          }} />
          {/* Globe icon */}
          <div style={{
            width: 100,
            height: 100,
            transform: `rotate(${iconRotate}deg) translateY(${iconBounce}px)`,
          }}>
            <svg width="100" height="100" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#1C1C1C" strokeWidth="2"/>
              <path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" stroke="#1C1C1C" strokeWidth="2"/>
            </svg>
          </div>
        </div>

        {/* Stick figure Buffett */}
        <div style={{ opacity: charOp, transform: `translateY(${charY}px)` }}>
          <svg width="90" height="140" viewBox="0 0 90 140">
            {/* Glow */}
            <circle cx="45" cy="28" r="26" fill="#e9d700" opacity="0.18" />
            {/* Head */}
            <circle cx="45" cy="28" r="18" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
              transform={`rotate(${headTilt}, 45, 28)`} />
            {/* Eyes */}
            <circle cx="39" cy="26" r="2.5" fill="#1C1C1C" />
            <circle cx="51" cy="26" r="2.5" fill="#1C1C1C" />
            {/* Smile */}
            <path d="M39 33 Q45 38 51 33" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
            {/* Glasses */}
            <rect x="35" y="22" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <rect x="47" y="22" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="43" y1="25" x2="47" y2="25" stroke="#e9d700" strokeWidth="1.5" />
            {/* Body */}
            <line x1="45" y1="46" x2="45" y2="95" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            {/* Arms */}
            <line x1="45" y1="62" x2="22" y2="78" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="45" y1="62" x2="68" y2="78" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            {/* Legs */}
            <line x1="45" y1="95" x2="30" y2="128" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="45" y1="95" x2="60" y2="128" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 60%, rgba(0,0,0,0.12) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_01;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_02',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_02: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // DANGER cross-out animation on "एक सिस्टम"
  const crossWidth = interpolate(sp(50), [0, 1], [0, 340]);
  
  const q1Op = interpolate(sp(8), [0, 1], [0, 1]);
  const q1Y = interpolate(sp(8), [0, 1], [40, 0]);
  const ansOp = interpolate(sp(22), [0, 1], [0, 1]);
  const ruleOp = interpolate(sp(38), [0, 1], [0, 1]);
  const ruleScale = sp(38, { stiffness: 200, damping: 16 });

  // Warning icon shake
  const shakeX = frame > 35 && frame < 55 ? Math.sin(frame * 0.9) * 5 : 0;

  // Grid pattern
  const gridOffset = (frame * 0.5) % 60;

  // Char
  const charBob = Math.sin(frame * 0.05) * 3;
  const headNod = Math.sin(frame * 0.06) * 5;
  const charOp = interpolate(sp(50), [0, 1], [0, 1]);
  const armWave = Math.sin(frame * 0.08) * 10;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EBEBEB', overflow: 'hidden', opacity: exitOp }}>

      {/* Animated grid */}
      <AbsoluteFill style={{ position: 'absolute', opacity: 0.07 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }).map((_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 60 + gridOffset} x2="1920" y2={i * 60 + gridOffset}
              stroke="#1C1C1C" strokeWidth="1" />
          ))}
          {Array.from({ length: 36 }).map((_, i) => (
            <line key={`v${i}`} x1={i * 60 - gridOffset} y1="0" x2={i * 60 - gridOffset} y2="1080"
              stroke="#1C1C1C" strokeWidth="1" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 7) * 1800 + 60;
        const py = seededRandom(i * 7 + 2) * 900 + 60;
        const drift = Math.sin(frame * 0.04 + i * 1.3) * 10;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: px + drift,
            top: py,
            width: seededRandom(i) > 0.5 ? 6 : 3,
            height: seededRandom(i) > 0.5 ? 6 : 3,
            backgroundColor: '#1C1C1C',
            opacity: 0.07,
          }} />
        );
      })}

      {/* Top accent bar */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        width: interpolate(sp(0), [0, 1], [0, 1920]),
        height: 10,
        backgroundColor: '#1C1C1C',
      }} />

      {/* Left column content */}
      <div style={{
        position: 'absolute',
        left: 120,
        top: 100,
        width: 860,
      }}>

        {/* Big question */}
        <div style={{
          opacity: q1Op,
          transform: `translateY(${q1Y}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 20,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            सबसे बड़ा सवाल
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1,
          }}>
            करें क्या?
          </div>
        </div>

        {/* Divider animated */}
        <div style={{
          width: interpolate(sp(20), [0, 1], [0, 500]),
          height: 5,
          backgroundColor: '#e9d700',
          margin: '28px 0',
        }} />

        {/* Answer */}
        <div style={{ opacity: ansOp }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            color: '#2D2D2D',
            lineHeight: 1.45,
          }}>
            अपनी रेटायर्मेंट को
          </div>
          {/* Crossed out word */}
          <div style={{ position: 'relative', display: 'inline-block', marginBottom: 8 }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 68,
              fontWeight: 900,
              color: '#1C1C1C',
            }}>
              किसी एक सिस्टम
            </div>
            {/* Cross line */}
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: crossWidth,
              height: 6,
              backgroundColor: '#ff0000',
              transformOrigin: 'left',
            }} />
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            color: '#2D2D2D',
          }}>
            के भरोसे मत छोड़िये
          </div>
        </div>

        {/* Rule card */}
        <div style={{
          marginTop: 40,
          padding: '20px 30px',
          backgroundColor: '#1C1C1C',
          display: 'inline-flex',
          alignItems: 'center',
          gap: 18,
          opacity: ruleOp,
          transform: `scale(${ruleScale})`,
          transformOrigin: 'left',
        }}>
          {/* Shield icon */}
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none" style={{ transform: `translateX(${shakeX}px)` }}>
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
            <path d="M9 12l2 2 4-4" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 32,
            fontWeight: 700,
            color: '#F5F5F5',
          }}>
            एक हिस्सा — पूरी प्लानिंग नहीं
          </span>
        </div>
      </div>

      {/* Right — visual metaphor: single basket vs multiple */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 20,
        opacity: ruleOp,
      }}>

        {/* Basket 1 — single danger */}
        <div style={{
          width: 240,
          padding: '18px 28px',
          backgroundColor: '#F5F5F5',
          border: '3px solid #ff0000',
          display: 'flex',
          alignItems: 'center',
          gap: 14,
        }}>
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2.5"/>
            <line x1="15" y1="9" x2="9" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round"/>
            <line x1="9" y1="9" x2="15" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round"/>
          </svg>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#ff0000' }}>सिर्फ EPF</span>
        </div>

        {/* Arrow down */}
        <svg width="24" height="30" viewBox="0 0 24 30" fill="none">
          <path d="M12 0v22M5 16l7 8 7-8" stroke="#9E9E9E" strokeWidth="2.5" strokeLinecap="round" />
        </svg>

        {/* Multiple baskets — green */}
        {['EPF', 'NPS', 'Equity', 'Gold'].map((item, i) => {
          const itemOp = interpolate(sp(55 + i * 8), [0, 1], [0, 1]);
          const itemX = interpolate(sp(55 + i * 8), [0, 1], [30, 0]);
          return (
            <div key={item} style={{
              width: 240,
              padding: '12px 20px',
              backgroundColor: '#1C1C1C',
              display: 'flex',
              alignItems: 'center',
              gap: 12,
              opacity: itemOp,
              transform: `translateX(${itemX}px)`,
            }}>
              <div style={{ width: 10, height: 10, borderRadius: '50%', backgroundColor: '#e9d700' }} />
              <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 700, color: '#F5F5F5' }}>{item}</span>
            </div>
          );
        })}

        {/* Stick figure */}
        <div style={{ opacity: charOp, transform: `translateY(${charBob}px)`, marginTop: 10 }}>
          <svg width="80" height="120" viewBox="0 0 80 120">
            <circle cx="40" cy="22" r="16" fill="#e9d700" opacity="0.2" />
            <circle cx="40" cy="22" r="14" fill="none" stroke="#1C1C1C" strokeWidth="2.2"
              transform={`rotate(${headNod}, 40, 22)`} />
            <circle cx="35" cy="20" r="2" fill="#1C1C1C" />
            <circle cx="45" cy="20" r="2" fill="#1C1C1C" />
            <path d="M35 27 Q40 31 45 27" stroke="#1C1C1C" strokeWidth="1.8" fill="none" strokeLinecap="round" />
            <rect x="31" y="17" width="7" height="5" rx="1.5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
            <rect x="42" y="17" width="7" height="5" rx="1.5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="38" y1="19.5" x2="42" y2="19.5" stroke="#e9d700" strokeWidth="1.2" />
            <line x1="40" y1="36" x2="40" y2="80" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="40" y1="52" x2={22 - armWave * 0.3} y2="66" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="40" y1="52" x2={58 + armWave * 0.3} y2="66" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="40" y1="80" x2="28" y2="108" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="40" y1="80" x2="52" y2="108" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_02;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_03',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_03: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Calculator number count-up animation
  const pensionAmount = Math.floor(interpolate(sp(30), [0, 1], [0, 45000]));
  const expenseAmount = Math.floor(interpolate(sp(45), [0, 1], [0, 80000]));
  const gapAmount = Math.floor(interpolate(sp(60), [0, 1], [0, 35000]));

  // Bar widths
  const pensionBar = interpolate(sp(32), [0, 1], [0, 380]);
  const expenseBar = interpolate(sp(47), [0, 1], [0, 620]);
  const gapBar = interpolate(sp(62), [0, 1], [0, 240]);

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [50, 0]);

  // Calc icon float
  const calcFloat = Math.sin(frame * 0.07) * 5;

  // char
  const charY = Math.sin(frame * 0.05) * 3;
  const headTilt = Math.sin(frame * 0.04) * 4;
  const charOp = interpolate(sp(55), [0, 1], [0, 1]);

  // Circles texture
  const circleR = 30 + Math.sin(frame * 0.03) * 5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>

      {/* Circle texture background */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 8 }).map((_, row) =>
            Array.from({ length: 14 }).map((_, col) => (
              <circle key={`${row}-${col}`}
                cx={col * 150 + 75}
                cy={row * 140 + 70}
                r={circleR}
                fill="none"
                stroke="#1C1C1C"
                strokeWidth="1"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating dots */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i * 5) * 1800 + 60;
        const py = seededRandom(i * 5 + 1) * 900 + 60;
        const drift = Math.sin(frame * 0.035 + i) * 8;
        return (
          <div key={i} style={{
            position: 'absolute', left: px + drift, top: py,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#1C1C1C', opacity: 0.07,
          }} />
        );
      })}

      {/* Right vertical stripe */}
      <div style={{
        position: 'absolute', right: 0, top: 0,
        width: 12, height: '100%', backgroundColor: '#1C1C1C',
      }} />

      {/* Left — title and instruction */}
      <div style={{
        position: 'absolute', left: 120, top: 90,
        opacity: titleOp, transform: `translateY(${titleY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 14,
        }}>
          STEP 2 — कागज पर, आज
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 80, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.05,
        }}>
          Calculate करिए<br />
          <span style={{ color: '#1C1C1C', fontSize: 60, fontWeight: 700 }}>असली पेंशन vs खर्च</span>
        </div>
        <div style={{
          width: interpolate(sp(15), [0, 1], [0, 460]),
          height: 5, backgroundColor: '#e9d700', marginTop: 22,
        }} />
      </div>

      {/* Center — bar chart comparison */}
      <div style={{
        position: 'absolute', left: 120, top: 340,
        display: 'flex', flexDirection: 'column', gap: 32,
      }}>

        {/* Pension bar */}
        <div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 10 }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 30, fontWeight: 700, color: '#2D2D2D', width: 220,
            }}>
              अनुमानित पेंशन
            </div>
            <div style={{
              width: pensionBar, height: 52,
              backgroundColor: '#1C1C1C',
              display: 'flex', alignItems: 'center', paddingLeft: 16,
              transition: 'width 0.1s',
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 28, fontWeight: 800, color: '#e9d700',
                whiteSpace: 'nowrap',
              }}>
                ₹{pensionAmount.toLocaleString('en-IN')}
              </span>
            </div>
          </div>
        </div>

        {/* Expense bar */}
        <div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 10 }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 30, fontWeight: 700, color: '#2D2D2D', width: 220,
            }}>
              मासिक खर्च
            </div>
            <div style={{
              width: expenseBar, height: 52,
              backgroundColor: '#2D2D2D',
              display: 'flex', alignItems: 'center', paddingLeft: 16,
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 28, fontWeight: 800, color: '#F5F5F5',
                whiteSpace: 'nowrap',
              }}>
                ₹{expenseAmount.toLocaleString('en-IN')}
              </span>
            </div>
          </div>
        </div>

        {/* Gap bar */}
        <div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 10 }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 30, fontWeight: 700, color: '#ff0000', width: 220,
            }}>
              GAP ⚠
            </div>
            <div style={{
              width: gapBar, height: 52,
              backgroundColor: '#ff0000',
              display: 'flex', alignItems: 'center', paddingLeft: 16,
            }}>
              <span style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 28, fontWeight: 800, color: '#F5F5F5',
                whiteSpace: 'nowrap',
              }}>
                ₹{gapAmount.toLocaleString('en-IN')}
              </span>
            </div>
          </div>
        </div>

        {/* Insight */}
        <div style={{
          marginTop: 10,
          padding: '18px 26px',
          backgroundColor: '#1C1C1C',
          maxWidth: 680,
          opacity: interpolate(sp(72), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(72), [0, 1], [20, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 30, fontWeight: 700, color: '#F5F5F5',
          }}>
            ज़्यादातर लोगों को पता चलता है — GAP बहुत बड़ा है
          </div>
        </div>
      </div>

      {/* Right — calculator icon + character */}
      <div style={{
        position: 'absolute', right: 160, top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 30,
        opacity: interpolate(sp(20), [0, 1], [0, 1]),
      }}>

        {/* Calculator SVG icon */}
        <div style={{ transform: `translateY(${calcFloat}px)` }}>
          <svg width="140" height="160" viewBox="0 0 140 160" fill="none">
            <rect x="10" y="10" width="120" height="140" rx="12" fill="#1C1C1C" />
            <rect x="22" y="22" width="96" height="36" rx="6" fill="#e9d700" />
            <text x="110" y="49" textAnchor="end"
              style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 22, fontWeight: 800, fill: '#1C1C1C' }}>
              GAP
            </text>
            {/* Buttons */}
            {[0, 1, 2, 3, 4, 5, 6, 7, 8].map(i => (
              <rect key={i}
                x={22 + (i % 3) * 35} y={74 + Math.floor(i / 3) * 30}
                width={26} height={22} rx={4}
                fill={i === 8 ? '#e9d700' : '#2D2D2D'}
              />
            ))}
          </svg>
        </div>

        {/* Stick figure */}
        <div style={{ opacity: charOp, transform: `translateY(${charY}px)` }}>
          <svg width="90" height="130" viewBox="0 0 90 130">
            <circle cx="45" cy="24" r="20" fill="#e9d700" opacity="0.16" />
            <circle cx="45" cy="24" r="15" fill="none" stroke="#1C1C1C" strokeWidth="2.2"
              transform={`rotate(${headTilt}, 45, 24)`} />
            <circle cx="39" cy="22" r="2.2" fill="#1C1C1C" />
            <circle cx="51" cy="22" r="2.2" fill="#1C1C1C" />
            <path d="M39 29 Q45 34 51 29" stroke="#1C1C1C" strokeWidth="1.8" fill="none" strokeLinecap="round" />
            <rect x="35" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
            <rect x="47" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
            <line x1="43" y1="21" x2="47" y2="21" stroke="#e9d700" strokeWidth="1.2" />
            <line x1="45" y1="39" x2="45" y2="88" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            {/* Arm pointing at calculator */}
            <line x1="45" y1="58" x2="20" y2="46" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="58" x2="65" y2="70" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="88" x2="32" y2="118" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="88" x2="58" y2="118" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 58%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_03;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_04',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_04: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleX = interpolate(sp(5), [0, 1], [-80, 0]);

  const assets = [
    { label: 'Equity', sub: 'शेयर बाजार', pct: 40, color: '#1C1C1C', icon: <svg width="40" height="40" viewBox="0 0 24 24" fill="none"><polyline points="22 7 13.5 15.5 8.5 10.5 2 17" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/><polyline points="16 7 22 7 22 13" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/></svg>, delay: 15 },
    { label: 'Debt', sub: 'बॉन्ड / FD', pct: 25, color: '#2D2D2D', icon: <svg width="40" height="40" viewBox="0 0 24 24" fill="none"><rect x="2" y="7" width="20" height="14" rx="2" stroke="#e9d700" strokeWidth="2.5"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16" stroke="#e9d700" strokeWidth="2.5"/></svg>, delay: 25 },
    { label: 'Real Estate', sub: 'प्रॉपर्टी / REIT', pct: 20, color: '#1C1C1C', icon: <svg width="40" height="40" viewBox="0 0 24 24" fill="none"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/><polyline points="9 22 9 12 15 12 15 22" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/></svg>, delay: 35 },
    { label: 'Gold', sub: 'सोना / गोल्ड ETF', pct: 15, color: '#e9d700', icon: <svg width="40" height="40" viewBox="0 0 24 24" fill="none"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2" stroke="#1C1C1C" strokeWidth="2" fill="#1C1C1C"/></svg>, delay: 45 },
  ];

  // Pie chart segments (simplified as arc indicators)
  const totalPct = 100;
  let cumulative = 0;

  // Char
  const charBob = Math.sin(frame * 0.05) * 3;
  const charOp = interpolate(sp(55), [0, 1], [0, 1]);
  const headNod = Math.sin(frame * 0.06) * 4;
  const armL = Math.sin(frame * 0.09) * 8;

  // Background diagonal lines
  const diagOffset = (frame * 0.4) % 80;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EBEBEB', overflow: 'hidden', opacity: exitOp }}>

      {/* Diagonal lines texture */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 30 }).map((_, i) => (
            <line key={i}
              x1={i * 80 - diagOffset} y1="0"
              x2={i * 80 - diagOffset - 300} y2="1080"
              stroke="#1C1C1C" strokeWidth="2"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 9) * 1800;
        const py = seededRandom(i * 9 + 3) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 10,
            width: 4, height: 4, backgroundColor: '#1C1C1C', opacity: 0.07,
          }} />
        );
      })}

      {/* Top bar */}
      <div style={{
        position: 'absolute', top: 0, left: 0,
        width: '100%', height: 10, backgroundColor: '#1C1C1C',
      }} />
      <div style={{
        position: 'absolute', bottom: 0, left: 0,
        width: '100%', height: 10, backgroundColor: '#e9d700',
      }} />

      {/* Title */}
      <div style={{
        position: 'absolute', left: 120, top: 60,
        opacity: titleOp, transform: `translateX(${titleX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 10,
        }}>
          STEP 3 — खुद निवेश करिये
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 84, fontWeight: 900, color: '#1C1C1C', lineHeight: 1,
        }}>
          Diversified<br />Portfolio
        </div>
        <div style={{
          width: interpolate(sp(15), [0, 1], [0, 360]),
          height: 5, backgroundColor: '#e9d700', marginTop: 18,
        }} />
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 34, fontWeight: 600, color: '#2D2D2D', marginTop: 14,
          opacity: interpolate(sp(20), [0, 1], [0, 1]),
        }}>
          Intentional होना पड़ता है
        </div>
      </div>

      {/* Asset cards — 4 columns */}
      <div style={{
        position: 'absolute', left: 120, bottom: 80,
        display: 'flex', gap: 24, alignItems: 'flex-end',
      }}>
        {assets.map((asset, i) => {
          const cardOp = interpolate(sp(asset.delay), [0, 1], [0, 1]);
          const cardY = interpolate(sp(asset.delay), [0, 1], [60, 0]);
          const barH = interpolate(sp(asset.delay + 5), [0, 1], [0, asset.pct * 3.2]);

          return (
            <div key={asset.label} style={{
              display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10,
              opacity: cardOp, transform: `translateY(${cardY}px)`,
            }}>
              {/* Percentage */}
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 44, fontWeight: 900, color: '#1C1C1C',
              }}>
                {Math.floor(interpolate(sp(asset.delay), [0, 1], [0, asset.pct]))}%
              </div>

              {/* Bar */}
              <div style={{
                width: 200,
                height: barH,
                backgroundColor: asset.color,
                display: 'flex', flexDirection: 'column',
                alignItems: 'center', justifyContent: 'flex-start',
                padding: 16, gap: 8,
                minHeight: 40,
              }}>
                {asset.icon}
              </div>

              {/* Label */}
              <div style={{ textAlign: 'center' }}>
                <div style={{
                  fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800,
                  color: '#1C1C1C',
                }}>
                  {asset.label}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 20,
                  color: '#9E9E9E', fontWeight: 600,
                }}>
                  {asset.sub}
                </div>
              </div>
            </div>
          );
        })}
      </div>

      {/* Right — Buffett quote + character */}
      <div style={{
        position: 'absolute', right: 100, top: '50%',
        transform: 'translateY(-50%)',
        display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 28,
        width: 420,
      }}>

        {/* Quote card */}
        <div style={{
          backgroundColor: '#1C1C1C',
          padding: '28px 30px',
          opacity: interpolate(sp(55), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
        }}>
          {/* Quote icon */}
          <svg width="40" height="32" viewBox="0 0 40 32" fill="none" style={{ marginBottom: 12 }}>
            <path d="M0 32V20C0 8.954 6.716 2.716 20.148 0L22 4.148C14.324 6.228 10 10.84 10 18H18V32H0zM22 32V20C22 8.954 28.716 2.716 42.148 0L44 4.148C36.324 6.228 32 10.84 32 18H40V32H22z" fill="#e9d700" opacity="0.4" />
          </svg>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 700,
            color: '#F5F5F5', lineHeight: 1.4, marginBottom: 16,
            fontStyle: 'italic',
          }}>
            "Spend what is left after saving"
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 600,
            color: '#e9d700',
          }}>
            — Warren Buffett
          </div>
        </div>

        {/* Stick figure */}
        <div style={{ opacity: charOp, transform: `translateY(${charBob}px)` }}>
          <svg width="90" height="130" viewBox="0 0 90 130">
            <circle cx="45" cy="24" r="22" fill="#e9d700" opacity="0.18" />
            <circle cx="45" cy="24" r="16" fill="none" stroke="#1C1C1C" strokeWidth="2.2"
              transform={`rotate(${headNod}, 45, 24)`} />
            <circle cx="39" cy="22" r="2.2" fill="#1C1C1C" />
            <circle cx="51" cy="22" r="2.2" fill="#1C1C1C" />
            <path d="M39 29 Q45 34 51 29" stroke="#1C1C1C" strokeWidth="1.8" fill="none" strokeLinecap="round" />
            <rect x="35" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
            <rect x="47" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
            <line x1="43" y1="21" x2="47" y2="21" stroke="#e9d700" strokeWidth="1.2" />
            <line x1="45" y1="40" x2="45" y2="88" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="56" x2={22 + armL} y2="44" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="56" x2="68" y2="68" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="88" x2="32" y2="116" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
            <line x1="45" y1="88" x2="58" y2="116" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_04;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_05',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_05: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big question reveal — stamping effect
  const qScale = sp(20, { stiffness: 300, damping: 14, mass: 0.3 });
  const qOp = interpolate(sp(20), [0, 0.2], [0, 1]);

  // WARNING stamp
  const stampScale = sp(45, { stiffness: 400, damping: 10, mass: 0.2 });
  const stampOp = interpolate(sp(45), [0, 0.3], [0, 1]);

  // Options reveal
  const opt1Op = interpolate(sp(55), [0, 1], [0, 1]);
  const opt2Op = interpolate(sp(65), [0, 1], [0, 1]);

  // Pulsing red warning
  const warningPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [40, 0]);

  // char
  const charBob = Math.sin(frame * 0.05) * 3;
  const headTilt = Math.sin(frame * 0.04) * 5;
  const charOp = interpolate(sp(60), [0, 1], [0, 1]);

  // Hexagonal pattern bg
  const hexOffset = (frame * 0.3) % 100;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F0F0F0', overflow: 'hidden', opacity: exitOp }}>

      {/* Hex pattern */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }).map((_, row) =>
            Array.from({ length: 10 }).map((_, col) => {
              const cx = col * 200 + (row % 2) * 100 + hexOffset;
              const cy = row * 150;
              const pts = Array.from({ length: 6 }).map((__, j) => {
                const a = (j * 60 - 30) * Math.PI / 180;
                return `${cx + 60 * Math.cos(a)},${cy + 60 * Math.sin(a)}`;
              }).join(' ');
              return <polygon key={`${row}-${col}`} points={pts} fill="none" stroke="#1C1C1C" strokeWidth="1" />;
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 11) * 1800;
        const py = seededRandom(i * 11 + 2) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 8,
            width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1C1C1C', opacity: 0.08,
          }} />
        );
      })}

      {/* Left stripe */}
      <div style={{
        position: 'absolute', left: 0, top: 0,
        width: 14, height: '100%', backgroundColor: '#e9d700',
      }} />

      {/* Left content */}
      <div style={{
        position: 'absolute', left: 90, top: 80,
        opacity: titleOp, transform: `translateY(${titleY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 14,
        }}>
          STEP 4 — Political Filter
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 72, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.1,
        }}>
          वादे सुनें<br />
          <span style={{ color: '#ff0000' }}>पर सवाल पूछें</span>
        </div>
        <div style={{
          width: interpolate(sp(15), [0, 1], [0, 440]),
          height: 5, backgroundColor: '#1C1C1C', marginTop: 18,
        }} />
      </div>

      {/* Center — Big question box */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) scale(${qScale})`,
        opacity: qOp,
        backgroundColor: '#1C1C1C',
        padding: '36px 52px',
        textAlign: 'center',
        width: 760,
        boxShadow: '8px 8px 0px #e9d700',
      }}>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 58, fontWeight: 900, color: '#F5F5F5', lineHeight: 1.2,
        }}>
          पैसा कहाँ से<br />आएगा?
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 36, fontWeight: 700, color: '#e9d700', marginTop: 14,
        }}>
          Fund है या नहीं?
        </div>
      </div>

      {/* STAMP overlay */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '28%',
        transform: `translate(-20%, -50%) rotate(-18deg) scale(${stampScale})`,
        opacity: stampOp * warningPulse,
        border: '6px solid #ff0000',
        padding: '12px 28px',
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 52, fontWeight: 900, color: '#ff0000',
          letterSpacing: 4,
        }}>
          VERIFY!
        </div>
      </div>

      {/* Bottom — two scenario cards */}
      <div style={{
        position: 'absolute', bottom: 80, left: 90,
        display: 'flex', gap: 24,
      }}>

        {/* Fund EXISTS card */}
        <div style={{
          padding: '22px 30px',
          backgroundColor: '#1C1C1C',
          width: 400,
          opacity: opt1Op,
          transform: `translateY(${interpolate(sp(55), [0, 1], [30, 0])}px)`,
          display: 'flex', flexDirection: 'column', gap: 12,
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
              <polyline points="22 4 12 14.01 9 11.01" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800, color: '#e9d700' }}>
              Fund है
            </span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 24, fontWeight: 600, color: '#F5F5F5',
          }}>
            तो वादा कुछ हद तक सुरक्षित है
          </div>
        </div>

        {/* Fund NOT EXISTS card */}
        <div style={{
          padding: '22px 30px',
          backgroundColor: '#ff0000',
          width: 400,
          opacity: opt2Op,
          transform: `translateY(${interpolate(sp(65), [0, 1], [30, 0])}px)`,
          display: 'flex', flexDirection: 'column', gap: 12,
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#F5F5F5" strokeWidth="2.5"/>
              <line x1="15" y1="9" x2="9" y2="15" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"/>
              <line x1="9" y1="9" x2="15" y2="15" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, fontWeight: 800, color: '#F5F5F5' }}>
              Fund नहीं
            </span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 24, fontWeight: 600, color: '#F5F5F5',
          }}>
            वादा कागज पर है — हवा में है
          </div>
        </div>
      </div>

      {/* Right — Character */}
      <div style={{
        position: 'absolute', right: 140, bottom: 100,
        opacity: charOp, transform: `translateY(${charBob}px)`,
      }}>
        <svg width="100" height="148" viewBox="0 0 100 148">
          <circle cx="50" cy="26" r="24" fill="#e9d700" opacity="0.18" />
          <circle cx="50" cy="26" r="18" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
            transform={`rotate(${headTilt}, 50, 26)`} />
          {/* Skeptical eyes */}
          <ellipse cx="43" cy="23" rx="2.5" ry="3" fill="#1C1C1C" />
          <ellipse cx="57" cy="23" rx="2.5" ry="3" fill="#1C1C1C" />
          {/* Skeptical mouth */}
          <path d="M43 32 Q50 29 57 32" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
          <rect x="38" y="18" width="9" height="7" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <rect x="53" y="18" width="9" height="7" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <line x1="47" y1="21.5" x2="53" y2="21.5" stroke="#e9d700" strokeWidth="1.4" />
          {/* Raised eyebrow effect */}
          <path d="M38 15 Q43 12 48 15" stroke="#1C1C1C" strokeWidth="1.8" fill="none" />
          <path d="M52 15 Q57 12 62 15" stroke="#1C1C1C" strokeWidth="1.8" fill="none" />
          <line x1="50" y1="44" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          {/* Arms folded skeptic pose */}
          <line x1="50" y1="64" x2="26" y2="76" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="64" x2="74" y2="76" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="100" x2="36" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="100" x2="64" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.12) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_05;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_06',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_06: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Myth bust animation
  const mythOp = interpolate(sp(5), [0, 1], [0, 1]);
  const mythY = interpolate(sp(5), [0, 1], [50, 0]);

  // BUST line growing through myth text
  const bustWidth = interpolate(sp(38), [0, 1], [0, 900]);

  // Reality card entrance
  const realityOp = interpolate(sp(50), [0, 1], [0, 1]);
  const realityY = interpolate(sp(50), [0, 1], [40, 0]);

  // Split facts
  const fact1Op = interpolate(sp(60), [0, 1], [0, 1]);
  const fact2Op = interpolate(sp(70), [0, 1], [0, 1]);

  // Screen shake on bust (frames 36-42)
  const shakeX = frame >= 36 && frame <= 42 ? Math.sin(frame * 2.5) * 6 : 0;
  const shakeY = frame >= 36 && frame <= 42 ? Math.sin(frame * 3.2) * 4 : 0;

  // Background — moving dots
  const dotMove = frame * 0.8;

  // char
  const charBob = Math.sin(frame * 0.05) * 3;
  const headTilt = Math.sin(frame * 0.04) * 4;
  const charOp = interpolate(sp(60), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{
      backgroundColor: '#EBEBEB', overflow: 'hidden', opacity: exitOp,
      transform: `translate(${shakeX}px, ${shakeY}px)`,
    }}>

      {/* Moving dot grid */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {Array.from({ length: 10 }).map((_, row) =>
          Array.from({ length: 18 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute',
              left: col * 110 + (dotMove % 110),
              top: row * 110,
              width: 6, height: 6,
              borderRadius: '50%',
              backgroundColor: '#1C1C1C',
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 13) * 1800;
        const py = seededRandom(i * 13 + 1) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 5, height: 5,
            backgroundColor: '#1C1C1C', opacity: 0.07,
          }} />
        );
      })}

      {/* Big left accent */}
      <div style={{
        position: 'absolute', left: 0, top: 0,
        width: 16, height: '100%', backgroundColor: '#ff0000',
      }} />

      {/* Main content */}
      <div style={{
        position: 'absolute', left: 90, top: 70,
        width: 1000,
      }}>

        {/* Label */}
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 18,
          opacity: mythOp,
        }}>
          MYTH vs REALITY
        </div>

        {/* MYTH block */}
        <div style={{ position: 'relative', opacity: mythOp, transform: `translateY(${mythY}px)` }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: 14,
            backgroundColor: '#1C1C1C', padding: '10px 20px', marginBottom: 12,
          }}>
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
              <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" stroke="#ff0000" strokeWidth="2" fill="none"/>
              <line x1="12" y1="9" x2="12" y2="13" stroke="#ff0000" strokeWidth="2" strokeLinecap="round"/>
              <line x1="12" y1="17" x2="12.01" y2="17" stroke="#ff0000" strokeWidth="2" strokeLinecap="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 800, color: '#ff0000', letterSpacing: 3 }}>
              MYTH
            </span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 76, fontWeight: 900, color: '#1C1C1C', lineHeight: 1,
            marginBottom: 8,
          }}>
            सरकार संभाल लेगी
          </div>
          {/* Cross-out bust line */}
          <div style={{
            position: 'absolute',
            top: '68%',
            left: 0,
            width: bustWidth,
            height: 8,
            backgroundColor: '#ff0000',
          }} />
        </div>

        {/* Divider */}
        <div style={{
          width: interpolate(sp(40), [0, 1], [0, 600]),
          height: 4, backgroundColor: '#9E9E9E',
          margin: '28px 0',
        }} />

        {/* REALITY */}
        <div style={{ opacity: realityOp, transform: `translateY(${realityY}px)` }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: 14,
            backgroundColor: '#e9d700', padding: '10px 20px', marginBottom: 12,
          }}>
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round"/>
              <polyline points="22 4 12 14.01 9 11.01" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 800, color: '#1C1C1C', letterSpacing: 3 }}>
              REALITY
            </span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 52, fontWeight: 800, color: '#1C1C1C', lineHeight: 1.3,
          }}>
            कभी कभी संभाल लेती है —<br />कभी कभी नहीं संभाल पाती
          </div>
        </div>

        {/* Two facts */}
        <div style={{ display: 'flex', gap: 20, marginTop: 32 }}>
          <div style={{
            padding: '16px 24px', backgroundColor: '#1C1C1C',
            flex: 1, opacity: fact1Op,
            transform: `translateX(${interpolate(sp(60), [0, 1], [-20, 0])}px)`,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 26, fontWeight: 700, color: '#F5F5F5',
            }}>
              जो सबसे ज्यादा भरोसा करते हैं...
            </div>
          </div>
          <div style={{
            padding: '16px 24px', backgroundColor: '#ff0000',
            flex: 1, opacity: fact2Op,
            transform: `translateX(${interpolate(sp(70), [0, 1], [20, 0])}px)`,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 26, fontWeight: 700, color: '#F5F5F5',
            }}>
              ...वही सबसे पहले तकलीफ में होते हैं
            </div>
          </div>
        </div>
      </div>

      {/* Right — character worried */}
      <div style={{
        position: 'absolute', right: 140, bottom: 80,
        opacity: charOp, transform: `translateY(${charBob}px)`,
      }}>
        <svg width="100" height="148" viewBox="0 0 100 148">
          <circle cx="50" cy="26" r="22" fill="#ff0000" opacity="0.12" />
          <circle cx="50" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
            transform={`rotate(${headTilt}, 50, 26)`} />
          {/* Worried eyes */}
          <ellipse cx="43" cy="23" rx="2.8" ry="3" fill="#1C1C1C" />
          <ellipse cx="57" cy="23" rx="2.8" ry="3" fill="#1C1C1C" />
          {/* Worried brows */}
          <path d="M39 17 Q43 14 47 17" stroke="#1C1C1C" strokeWidth="1.8" fill="none" />
          <path d="M53 17 Q57 14 61 17" stroke="#1C1C1C" strokeWidth="1.8" fill="none" />
          {/* Worried mouth */}
          <path d="M43 33 Q50 30 57 33" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
          <rect x="39" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
          <rect x="53" y="18" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
          <line x1="47" y1="21" x2="53" y2="21" stroke="#e9d700" strokeWidth="1.2" />
          <line x1="50" y1="43" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          {/* Arms down worried */}
          <line x1="50" y1="62" x2="28" y2="80" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="62" x2="72" y2="80" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="100" x2="36" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="100" x2="64" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.12) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_06;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_07',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_07: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleX = interpolate(sp(5), [0, 1], [-60, 0]);

  // Players reveal
  const players = [
    { name: 'नेता', role: 'आज वोट जीतना', icon: <svg width="36" height="36" viewBox="0 0 24 24" fill="none"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" stroke="#F5F5F5" strokeWidth="2.2" strokeLinecap="round"/><circle cx="9" cy="7" r="4" stroke="#F5F5F5" strokeWidth="2.2"/><path d="M23 21v-2a4 4 0 0 0-3-3.87" stroke="#F5F5F5" strokeWidth="2.2" strokeLinecap="round"/><path d="M16 3.13a4 4 0 0 1 0 7.75" stroke="#F5F5F5" strokeWidth="2.2" strokeLinecap="round"/></svg>, delay: 15 },
    { name: 'यूनियन', role: 'मेंबर्स को बेनिफिट', icon: <svg width="36" height="36" viewBox="0 0 24 24" fill="none"><rect x="3" y="3" width="7" height="7" rx="1" stroke="#F5F5F5" strokeWidth="2.2"/><rect x="14" y="3" width="7" height="7" rx="1" stroke="#F5F5F5" strokeWidth="2.2"/><rect x="14" y="14" width="7" height="7" rx="1" stroke="#F5F5F5" strokeWidth="2.2"/><rect x="3" y="14" width="7" height="7" rx="1" stroke="#F5F5F5" strokeWidth="2.2"/></svg>, delay: 25 },
    { name: 'Fund Manager', role: 'AUM और Fees', icon: <svg width="36" height="36" viewBox="0 0 24 24" fill="none"><line x1="12" y1="1" x2="12" y2="23" stroke="#F5F5F5" strokeWidth="2.2" strokeLinecap="round"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" stroke="#F5F5F5" strokeWidth="2.2" strokeLinecap="round"/></svg>, delay: 35 },
  ];

  // YOU card
  const youOp = interpolate(sp(55), [0, 1], [0, 1]);
  const youScale = sp(55, { stiffness: 250, damping: 14 });

  // char
  const charBob = Math.sin(frame * 0.05) * 3;
  const headNod = Math.sin(frame * 0.06) * 4;
  const charOp = interpolate(sp(60), [0, 1], [0, 1]);
  const armPoint = Math.sin(frame * 0.08) * 5;

  // Triangle bg pattern
  const triOffset = frame * 0.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F0F0F0', overflow: 'hidden', opacity: exitOp }}>

      {/* Triangle pattern */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }).map((_, i) => {
            const x = seededRandom(i * 4) * 1920;
            const y = seededRandom(i * 4 + 1) * 1080;
            const size = 60 + seededRandom(i * 4 + 2) * 80;
            return (
              <polygon key={i}
                points={`${x},${y - size} ${x - size * 0.866},${y + size * 0.5} ${x + size * 0.866},${y + size * 0.5}`}
                fill="none" stroke="#1C1C1C" strokeWidth="1"
                transform={`rotate(${triOffset + i * 18}, ${x}, ${y})`}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 15) * 1800;
        const py = seededRandom(i * 15 + 2) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.035 + i) * 8,
            width: 4, height: 4, backgroundColor: '#1C1C1C', opacity: 0.08,
          }} />
        );
      })}

      {/* Top bottom bars */}
      <div style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: 8, backgroundColor: '#1C1C1C' }} />
      <div style={{ position: 'absolute', bottom: 0, left: 0, width: '100%', height: 8, backgroundColor: '#1C1C1C' }} />

      {/* Title */}
      <div style={{
        position: 'absolute', left: 120, top: 70,
        opacity: titleOp, transform: `translateX(${titleX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 12,
        }}>
          INCENTIVE STRUCTURE
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 72, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.05,
        }}>
          सब Rational हैं —<br />
          <span style={{ color: '#e9d700', WebkitTextStroke: '1.5px #1C1C1C', fontSize: 80 }}>आप भी हो सकते हैं</span>
        </div>
        <div style={{
          width: interpolate(sp(15), [0, 1], [0, 480]),
          height: 5, backgroundColor: '#1C1C1C', marginTop: 18,
        }} />
      </div>

      {/* Players row */}
      <div style={{
        position: 'absolute', left: 120, bottom: 160,
        display: 'flex', gap: 24, alignItems: 'flex-end',
      }}>
        {players.map((p) => {
          const pOp = interpolate(sp(p.delay), [0, 1], [0, 1]);
          const pY = interpolate(sp(p.delay), [0, 1], [40, 0]);
          return (
            <div key={p.name} style={{
              padding: '24px 28px',
              backgroundColor: '#1C1C1C',
              width: 260,
              opacity: pOp,
              transform: `translateY(${pY}px)`,
              display: 'flex', flexDirection: 'column', gap: 14,
            }}>
              {p.icon}
              <div style={{
                fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800, color: '#F5F5F5',
              }}>{p.name}</div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 22, fontWeight: 600, color: '#9E9E9E',
              }}>{p.role}</div>
            </div>
          );
        })}

        {/* Arrow */}
        <div style={{
          opacity: interpolate(sp(48), [0, 1], [0, 1]),
          display: 'flex', alignItems: 'center',
        }}>
          <svg width="60" height="24" viewBox="0 0 60 24" fill="none">
            <path d="M0 12h52M42 2l10 10-10 10" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* YOU card — highlighted */}
        <div style={{
          padding: '24px 28px',
          backgroundColor: '#e9d700',
          width: 280,
          opacity: youOp,
          transform: `scale(${youScale})`,
          transformOrigin: 'bottom center',
          display: 'flex', flexDirection: 'column', gap: 14,
          boxShadow: '0 0 30px rgba(233,215,0,0.4)',
        }}>
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round"/>
            <circle cx="12" cy="7" r="4" stroke="#1C1C1C" strokeWidth="2.5"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 32, fontWeight: 900, color: '#1C1C1C',
          }}>आप</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 24, fontWeight: 800, color: '#1C1C1C',
          }}>खुद की तैयारी</div>
        </div>
      </div>

      {/* Right — character with raised fist */}
      <div style={{
        position: 'absolute', right: 140, top: '50%',
        transform: 'translateY(-50%)',
        opacity: charOp,
      }}>
        <div style={{ transform: `translateY(${charBob}px)` }}>
          <svg width="100" height="148" viewBox="0 0 100 148">
            <circle cx="50" cy="26" r="22" fill="#e9d700" opacity="0.22" />
            <circle cx="50" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
              transform={`rotate(${headNod}, 50, 26)`} />
            <circle cx="44" cy="23" r="2.5" fill="#1C1C1C" />
            <circle cx="56" cy="23" r="2.5" fill="#1C1C1C" />
            <path d="M44 31 Q50 36 56 31" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
            <rect x="39" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <rect x="52" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="48" y1="21" x2="52" y2="21" stroke="#e9d700" strokeWidth="1.4" />
            <line x1="50" y1="43" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            {/* Arm raised — pointing up */}
            <line x1="50" y1="58" x2={28 + armPoint} y2="30" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="58" x2="72" y2="72" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="100" x2="36" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="100" x2="64" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            {/* Fist at raised arm */}
            <circle cx={28 + armPoint} cy="28" r="6" fill="none" stroke="#1C1C1C" strokeWidth="2" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_07;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_08',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_08: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Wave animation for tide metaphor
  const waveOffset = frame * 2;
  const wavePoints = Array.from({ length: 40 }).map((_, i) => {
    const x = (i / 39) * 1920;
    const y = 680 + Math.sin((i * 0.3) + waveOffset * 0.05) * 30
      + Math.sin((i * 0.15) + waveOffset * 0.03) * 20;
    return `${x},${y}`;
  }).join(' ');

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [60, 0]);

  // Buffett quote
  const quoteOp = interpolate(sp(25), [0, 1], [0, 1]);
  const quoteX = interpolate(sp(25), [0, 1], [60, 0]);

  // Call to action
  const ctaOp = interpolate(sp(45), [0, 1], [0, 1]);
  const ctaScale = sp(45, { stiffness: 200, damping: 15 });

  // Wave tide receding
  const tideY = interpolate(sp(30), [0, 1], [0, 80]);

  // char swimming
  const charBob = Math.sin(frame * 0.08) * 6;
  const charX = interpolate(sp(35), [0, 1], [0, 20]);
  const headTilt = Math.sin(frame * 0.06) * 8;
  const charOp = interpolate(sp(40), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>

      {/* Wave / tide - fills bottom */}
      <AbsoluteFill style={{ position: 'absolute' }}>
        <svg width="1920" height="1080" style={{ position: 'absolute', bottom: 0 }}>
          <defs>
            <linearGradient id="waveGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stopColor="#1C1C1C" stopOpacity="0.12" />
              <stop offset="100%" stopColor="#1C1C1C" stopOpacity="0.04" />
            </linearGradient>
          </defs>
          {/* Tide receding */}
          <polyline
            points={wavePoints}
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3"
            style={{ transform: `translateY(${tideY}px)` }}
          />
          {/* Water fill */}
          <polygon
            points={`0,1080 ${wavePoints} 1920,1080`}
            fill="url(#waveGrad)"
            style={{ transform: `translateY(${tideY}px)` }}
          />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 17) * 1800;
        const py = seededRandom(i * 17 + 2) * 600;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 8,
            width: 4, height: 4, borderRadius: '50%', backgroundColor: '#1C1C1C', opacity: 0.07,
          }} />
        );
      })}

      {/* Left accent */}
      <div style={{
        position: 'absolute', left: 0, top: 0,
        width: 14, height: '100%', backgroundColor: '#1C1C1C',
      }} />

      {/* Main headline */}
      <div style={{
        position: 'absolute', left: 100, top: 80,
        opacity: titleOp, transform: `translateY(${titleY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 16,
        }}>
          BUFFETT'S TIDE PRINCIPLE
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 96, fontWeight: 900, color: '#1C1C1C', lineHeight: 1,
        }}>
          टाइड उतरने से<br />
          <span style={{ color: '#e9d700', WebkitTextStroke: '2px #1C1C1C' }}>पहले</span> तैरो
        </div>
        <div style={{
          width: interpolate(sp(15), [0, 1], [0, 500]),
          height: 6, backgroundColor: '#1C1C1C', marginTop: 20,
        }} />
      </div>

      {/* Quote block */}
      <div style={{
        position: 'absolute', left: 100, top: 380,
        width: 860,
        opacity: quoteOp, transform: `translateX(${quoteX}px)`,
      }}>
        <div style={{
          backgroundColor: '#1C1C1C', padding: '30px 36px',
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 30, fontWeight: 700,
            color: '#F5F5F5', lineHeight: 1.4, fontStyle: 'italic', marginBottom: 14,
          }}>
            "Only when the tide goes out do you discover<br />who's been swimming naked."
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
            color: '#e9d700',
          }}>
            — Warren Buffett
          </div>
        </div>
      </div>

      {/* CTA bottom */}
      <div style={{
        position: 'absolute', left: 100, bottom: 120,
        opacity: ctaOp, transform: `scale(${ctaScale})`, transformOrigin: 'left',
      }}>
        <div style={{
          display: 'inline-flex', alignItems: 'center', gap: 20,
          backgroundColor: '#e9d700', padding: '20px 36px',
        }}>
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#1C1C1C" strokeWidth="2.5"/>
            <polyline points="12 6 12 12 16 14" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round"/>
          </svg>
          <span style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 36, fontWeight: 900, color: '#1C1C1C',
          }}>
            अपनी तैयारी पर भरोसा करें — आज से
          </span>
        </div>
      </div>

      {/* Character swimming at wave top */}
      <div style={{
        position: 'absolute',
        right: 200,
        top: 580 + tideY - charBob,
        opacity: charOp,
        transform: `translateX(${charX}px)`,
      }}>
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="24" r="20" fill="#e9d700" opacity="0.22" />
          <circle cx="45" cy="24" r="16" fill="none" stroke="#1C1C1C" strokeWidth="2.2"
            transform={`rotate(${headTilt}, 45, 24)`} />
          <circle cx="39" cy="21" r="2.2" fill="#1C1C1C" />
          <circle cx="51" cy="21" r="2.2" fill="#1C1C1C" />
          {/* Happy swimming smile */}
          <path d="M39 28 Q45 34 51 28" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
          <rect x="35" y="17" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
          <rect x="47" y="17" width="8" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.6" />
          <line x1="43" y1="20" x2="47" y2="20" stroke="#e9d700" strokeWidth="1.2" />
          {/* Swimming body — horizontal */}
          <line x1="45" y1="40" x2="45" y2="80" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round"
            transform="rotate(-25, 45, 60)" />
          {/* Swimming arms extended */}
          <line x1="45" y1="54" x2="10" y2="48" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
          <line x1="45" y1="54" x2="78" y2="44" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" />
          {/* Splash lines */}
          <line x1="8" y1="56" x2="2" y2="48" stroke="#1C1C1C" strokeWidth="1.5" strokeLinecap="round" opacity="0.4" />
          <line x1="82" y1="52" x2="88" y2="44" stroke="#1C1C1C" strokeWidth="1.5" strokeLinecap="round" opacity="0.4" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 58%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_08;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_09',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_09: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Direct address — mirror / question to viewer
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);
  const titleY = interpolate(sp(5), [0, 1], [60, 0]);

  // YES / NO split reveal
  const yesOp = interpolate(sp(30), [0, 1], [0, 1]);
  const yesX = interpolate(sp(30), [0, 1], [-60, 0]);
  const noOp = interpolate(sp(40), [0, 1], [0, 1]);
  const noX = interpolate(sp(40), [0, 1], [60, 0]);

  // Bottom question
  const q2Op = interpolate(sp(55), [0, 1], [0, 1]);

  // Blinking cursor
  const cursorOp = Math.floor(frame / 18) % 2 === 0 ? 1 : 0;

  // char
  const charBob = Math.sin(frame * 0.05) * 3;
  const headTilt = Math.sin(frame * 0.04) * 4;
  const charOp = interpolate(sp(50), [0, 1], [0, 1]);

  // BG — concentric circles growing
  const circleR = (frame * 1.2) % 400;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EBEBEB', overflow: 'hidden', opacity: exitOp }}>

      {/* Concentric circles breathing */}
      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', opacity: 0.05 }}>
        {[0, 1, 2, 3].map(i => (
          <div key={i} style={{
            position: 'absolute',
            width: circleR + i * 200,
            height: circleR + i * 200,
            borderRadius: '50%',
            border: '2px solid #1C1C1C',
            opacity: Math.max(0, 1 - (circleR + i * 200) / 800),
          }} />
        ))}
      </AbsoluteFill>

      {/* Grid static */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 18 }).map((_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 64} x2="1920" y2={i * 64} stroke="#1C1C1C" strokeWidth="1" />
          ))}
          {Array.from({ length: 32 }).map((_, i) => (
            <line key={`v${i}`} x1={i * 64} y1="0" x2={i * 64} y2="1080" stroke="#1C1C1C" strokeWidth="1" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const px = seededRandom(i * 19) * 1800;
        const py = seededRandom(i * 19 + 2) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 8,
            width: 4, height: 4, backgroundColor: '#1C1C1C', opacity: 0.08,
          }} />
        );
      })}

      {/* Top strip */}
      <div style={{
        position: 'absolute', top: 0, left: 0,
        width: '100%', height: 12,
        background: 'linear-gradient(90deg, #1C1C1C 50%, #e9d700 50%)',
      }} />

      {/* Center content */}
      <div style={{
        position: 'absolute', left: 120, top: 80,
        width: 1100,
        opacity: titleOp, transform: `translateY(${titleY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 18,
        }}>
          एक सवाल — आपसे सीधे
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 72, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.1,
        }}>
          क्या आपने Retirement Planning<br />
          <span style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 80, fontWeight: 900, color: '#1C1C1C',
          }}>
            Calculate की है?
          </span>
          <span style={{
            display: 'inline-block',
            width: 4, height: 72,
            backgroundColor: '#e9d700',
            marginLeft: 6,
            opacity: cursorOp,
            verticalAlign: 'middle',
          }} />
        </div>
      </div>

      {/* YES / NO split */}
      <div style={{
        position: 'absolute', left: 120, top: 420,
        display: 'flex', gap: 32,
      }}>

        {/* YES */}
        <div style={{
          width: 440, padding: '30px 36px',
          backgroundColor: '#1C1C1C',
          opacity: yesOp, transform: `translateX(${yesX}px)`,
          display: 'flex', alignItems: 'flex-start', gap: 18, flexDirection: 'column',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <svg width="38" height="38" viewBox="0 0 24 24" fill="none">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"/>
              <polyline points="22 4 12 14.01 9 11.01" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#e9d700' }}>हाँ</span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26, fontWeight: 600, color: '#F5F5F5', lineHeight: 1.4,
          }}>
            आप तैयार हैं — अब और बेहतर करें
          </div>
        </div>

        {/* NO */}
        <div style={{
          width: 620, padding: '30px 36px',
          backgroundColor: '#ff0000',
          opacity: noOp, transform: `translateX(${noX}px)`,
          display: 'flex', alignItems: 'flex-start', gap: 18, flexDirection: 'column',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
            <svg width="38" height="38" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#F5F5F5" strokeWidth="2.5"/>
              <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3" stroke="none"/>
              <line x1="12" y1="8" x2="12" y2="12" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"/>
              <line x1="12" y1="16" x2="12.01" y2="16" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 900, color: '#F5F5F5' }}>नहीं</span>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26, fontWeight: 600, color: '#F5F5F5', lineHeight: 1.4,
          }}>
            'बाद में देखेंगे' — यही सबसे बड़ी गलती है
          </div>
        </div>
      </div>

      {/* Bottom CTA comment */}
      <div style={{
        position: 'absolute', left: 120, bottom: 90,
        opacity: q2Op,
        display: 'flex', alignItems: 'center', gap: 18,
      }}>
        <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" stroke="#1C1C1C" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"/>
        </svg>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 32, fontWeight: 700, color: '#1C1C1C',
        }}>
          Comment में बताईए — आपका जवाब क्या है?
        </div>
      </div>

      {/* Character */}
      <div style={{
        position: 'absolute', right: 140, top: '50%',
        transform: 'translateY(-50%)',
        opacity: charOp,
      }}>
        <div style={{ transform: `translateY(${charBob}px)` }}>
          <svg width="100" height="148" viewBox="0 0 100 148">
            <circle cx="50" cy="26" r="22" fill="#e9d700" opacity="0.2" />
            <circle cx="50" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
              transform={`rotate(${headTilt}, 50, 26)`} />
            <circle cx="44" cy="23" r="2.5" fill="#1C1C1C" />
            <circle cx="56" cy="23" r="2.5" fill="#1C1C1C" />
            {/* Questioning expression */}
            <path d="M44 32 Q50 36 56 32" stroke="#1C1C1C" strokeWidth="2" fill="none" strokeLinecap="round" />
            <path d="M56 15 Q60 11 60 16 Q60 20 56 20" stroke="#1C1C1C" strokeWidth="1.8" fill="none" strokeLinecap="round" />
            <circle cx="56" cy="24" r="1.5" fill="#1C1C1C" />
            <rect x="39" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <rect x="52" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
            <line x1="48" y1="21" x2="52" y2="21" stroke="#e9d700" strokeWidth="1.4" />
            <line x1="50" y1="43" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            {/* Shrug-like arms */}
            <line x1="50" y1="58" x2="24" y2="50" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="58" x2="76" y2="50" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="100" x2="36" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="50" y1="100" x2="64" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 58%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_09;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_10',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Final impactful scene — 3 action steps in bold
  const heroOp = interpolate(sp(5), [0, 1], [0, 1]);
  const heroY = interpolate(sp(5), [0, 1], [70, 0]);

  const steps = [
    { num: '01', text: 'खुद जागरूक होना होगा', icon: <svg width="44" height="44" viewBox="0 0 24 24" fill="none"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" stroke="#e9d700" strokeWidth="2.5"/><circle cx="12" cy="12" r="3" stroke="#e9d700" strokeWidth="2.5"/></svg>, delay: 20 },
    { num: '02', text: 'खुद सवाल पूछने होंगे', icon: <svg width="44" height="44" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2.5"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"/><line x1="12" y1="17" x2="12.01" y2="17" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round"/></svg>, delay: 35 },
    { num: '03', text: 'खुद तैयारी करनी होगी', icon: <svg width="44" height="44" viewBox="0 0 24 24" fill="none"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/></svg>, delay: 50 },
  ];

  // BACKUP question
  const backupOp = interpolate(sp(65), [0, 1], [0, 1]);
  const backupScale = sp(65, { stiffness: 220, damping: 14 });

  // char — confident final pose
  const charBob = Math.sin(frame * 0.05) * 3;
  const headNod = Math.sin(frame * 0.04) * 3;
  const charOp = interpolate(sp(55), [0, 1], [0, 1]);
  const armPose = Math.sin(frame * 0.07) * 5;

  // BG lines sweeping
  const lineOffset = (frame * 1.5) % 1920;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>

      {/* Sweeping lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 8 }).map((_, i) => (
            <line key={i}
              x1={-200 + i * 280 + lineOffset}
              y1="0"
              x2={-200 + i * 280 + lineOffset - 400}
              y2="1080"
              stroke="#1C1C1C" strokeWidth="80"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i * 21) * 1800;
        const py = seededRandom(i * 21 + 2) * 900;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.035 + i) * 8,
            width: seededRandom(i * 21 + 3) > 0.5 ? 6 : 3,
            height: seededRandom(i * 21 + 3) > 0.5 ? 6 : 3,
            backgroundColor: '#1C1C1C', opacity: 0.07,
          }} />
        );
      })}

      {/* Yellow left accent — thick */}
      <div style={{
        position: 'absolute', left: 0, top: 0,
        width: 18, height: '100%', backgroundColor: '#e9d700',
      }} />

      {/* Hero text */}
      <div style={{
        position: 'absolute', left: 90, top: 70,
        opacity: heroOp, transform: `translateY(${heroY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 20, fontWeight: 700,
          color: '#9E9E9E', letterSpacing: 4, marginBottom: 14,
        }}>
          FINAL — आपके लिए
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari, sans-serif',
          fontSize: 88, fontWeight: 900, color: '#1C1C1C', lineHeight: 1,
        }}>
          Backup क्या है<br />
          <span style={{ color: '#1C1C1C', fontSize: 52, fontWeight: 700 }}>
            अगर सिस्टम कम दे?
          </span>
        </div>
        <div style={{
          width: interpolate(sp(12), [0, 1], [0, 560]),
          height: 5, backgroundColor: '#e9d700', marginTop: 18,
        }} />
      </div>

      {/* 3 action steps */}
      <div style={{
        position: 'absolute', left: 90, bottom: 120,
        display: 'flex', gap: 28,
      }}>
        {steps.map((step) => {
          const sOp = interpolate(sp(step.delay), [0, 1], [0, 1]);
          const sY = interpolate(sp(step.delay), [0, 1], [50, 0]);
          return (
            <div key={step.num} style={{
              width: 380,
              padding: '28px 30px',
              backgroundColor: '#1C1C1C',
              opacity: sOp,
              transform: `translateY(${sY}px)`,
              display: 'flex', flexDirection: 'column', gap: 16,
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 52, fontWeight: 900, color: '#e9d700', lineHeight: 1,
              }}>
                {step.num}
              </div>
              {step.icon}
              <div style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 30, fontWeight: 800, color: '#F5F5F5', lineHeight: 1.3,
              }}>
                {step.text}
              </div>
            </div>
          );
        })}
      </div>

      {/* Backup answer card */}
      <div style={{
        position: 'absolute', right: 100, top: '42%',
        transform: `translateY(-50%) scale(${backupScale})`,
        transformOrigin: 'right center',
        opacity: backupOp,
        width: 400,
      }}>
        <div style={{
          backgroundColor: '#e9d700',
          padding: '32px 36px',
          boxShadow: '6px 6px 0px #1C1C1C',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44, fontWeight: 900, color: '#1C1C1C', lineHeight: 1.1,
          }}>
            आपकी<br />खुद की<br />तैयारी
          </div>
          {/* Arrow */}
          <div style={{ marginTop: 20 }}>
            <svg width="50" height="20" viewBox="0 0 50 20" fill="none">
              <path d="M0 10h42M32 2l10 8-10 8" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 22, fontWeight: 700, color: '#2D2D2D', marginTop: 10,
          }}>
            यही असली Security है
          </div>
        </div>
      </div>

      {/* Stick figure — confident final */}
      <div style={{
        position: 'absolute', right: 160, bottom: 100,
        opacity: charOp, transform: `translateY(${charBob}px)`,
      }}>
        <svg width="100" height="148" viewBox="0 0 100 148">
          {/* Strong golden glow */}
          <circle cx="50" cy="26" r="30" fill="#e9d700" opacity="0.22" />
          <circle cx="50" cy="26" r="18" fill="none" stroke="#1C1C1C" strokeWidth="2.5"
            transform={`rotate(${headNod}, 50, 26)`} />
          <circle cx="44" cy="23" r="2.5" fill="#1C1C1C" />
          <circle cx="56" cy="23" r="2.5" fill="#1C1C1C" />
          {/* Big confident smile */}
          <path d="M42 31 Q50 38 58 31" stroke="#1C1C1C" strokeWidth="2.5" fill="none" strokeLinecap="round" />
          <rect x="39" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <rect x="52" y="18" width="9" height="6" rx="2" fill="none" stroke="#e9d700" strokeWidth="1.8" />
          <line x1="48" y1="21" x2="52" y2="21" stroke="#e9d700" strokeWidth="1.4" />
          <line x1="50" y1="44" x2="50" y2="100" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          {/* Arms wide open triumphant */}
          <line x1="50" y1="58" x2={18 - armPose} y2="36" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="58" x2={82 + armPose} y2="36" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          {/* Fists */}
          <circle cx={18 - armPose} cy="34" r="6" fill="none" stroke="#1C1C1C" strokeWidth="2" />
          <circle cx={82 + armPose} cy="34" r="6" fill="none" stroke="#1C1C1C" strokeWidth="2" />
          <line x1="50" y1="100" x2="36" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="50" y1="100" x2="64" y2="134" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 56%, rgba(0,0,0,0.1) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_10;
```

