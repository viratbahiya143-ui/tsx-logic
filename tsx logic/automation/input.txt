```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 14, mass: 0.5, stiffness: 130, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // City silhouette parallax
  const cityX = interpolate(frame, [0, durationInFrames], [0, -40], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Road scroll
  const roadX = interpolate(frame, [0, durationInFrames], [0, -120], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Car entrance from right
  const carX = sp(10) * 0 + interpolate(sp(10), [0, 1], [600, 0]);
  const carBounce = Math.sin(frame * 0.18) * 3;

  // Heading slide in
  const headY = interpolate(sp(25), [0, 1], [-80, 0]);
  const headOp = interpolate(sp(25), [0, 1], [0, 1]);

  // City lights flicker
  const flicker = (seed: number) =>
    0.4 + Math.abs(Math.sin(frame * (0.07 + seed * 0.03) + seed)) * 0.6;

  // Grid lines
  const gridOp = interpolate(frame, [0, 20], [0, 0.06], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particle drift
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 31) * 1920,
    y: seededRandom(i * 37) * 1080,
    r: 2 + seededRandom(i * 41) * 5,
    speed: 0.3 + seededRandom(i * 43) * 0.6,
    op: 0.15 + seededRandom(i * 47) * 0.25,
  }));

  // Tag badge pop
  const badgeS = sp(45);
  const badgeScale = interpolate(badgeS, [0, 1], [0, 1]);

  // Stick figure
  const figureX = sp(50);
  const figureSlide = interpolate(figureX, [0, 1], [-200, 0]);
  const headNod = Math.sin(frame * 0.05) * 4;
  const bodyBreathe = Math.sin(frame * 0.06) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === GRADIENT BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 80% 60% at 30% 40%, #2a1a00 0%, #1C1C1C 55%, #0a0a1a 100%)',
          }}
        />
        {/* Deep city amber glow bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 0,
            left: 0,
            right: 0,
            height: 340,
            background:
              'linear-gradient(0deg, rgba(233,215,0,0.10) 0%, transparent 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: gridOp }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 28 }, (_, i) => (
            <line
              key={`v${i}`}
              x1={i * 70}
              y1={0}
              x2={i * 70}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="0.6"
            />
          ))}
          {Array.from({ length: 16 }, (_, i) => (
            <line
              key={`h${i}`}
              x1={0}
              y1={i * 70}
              x2={1920}
              y2={i * 70}
              stroke="#e9d700"
              strokeWidth="0.6"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CITY SILHOUETTE === */}
      <AbsoluteFill
        style={{ transform: `translateX(${cityX}px)` }}
      >
        <svg
          width="2060"
          height="1080"
          style={{ position: 'absolute', bottom: 0 }}
        >
          {/* Buildings */}
          {[
            { x: 0, w: 90, h: 260, win: [[15, 40], [15, 80], [55, 40], [55, 80]] },
            { x: 100, w: 70, h: 340, win: [[10, 30], [10, 70], [10, 110], [40, 30], [40, 70]] },
            { x: 180, w: 120, h: 200, win: [[15, 30], [55, 30], [15, 70], [55, 70], [90, 30]] },
            { x: 310, w: 80, h: 420, win: [[10, 30], [10, 80], [10, 130], [45, 30], [45, 80]] },
            { x: 400, w: 110, h: 280, win: [[15, 40], [60, 40], [15, 90], [60, 90]] },
            { x: 520, w: 60, h: 380, win: [[10, 30], [35, 30], [10, 80], [35, 80]] },
            { x: 590, w: 130, h: 220, win: [[15, 35], [70, 35], [15, 75], [70, 75]] },
            { x: 730, w: 90, h: 460, win: [[10, 30], [50, 30], [10, 90], [50, 90], [10, 150]] },
            { x: 830, w: 75, h: 310, win: [[10, 30], [45, 30], [10, 80], [45, 80]] },
            { x: 915, w: 100, h: 250, win: [[15, 40], [60, 40], [15, 90], [60, 90]] },
            { x: 1025, w: 85, h: 390, win: [[10, 30], [50, 30], [10, 90]] },
            { x: 1120, w: 120, h: 180, win: [[15, 30], [70, 30], [15, 70]] },
            { x: 1250, w: 70, h: 340, win: [[10, 40], [40, 40], [10, 90]] },
            { x: 1330, w: 110, h: 270, win: [[15, 40], [65, 40], [15, 90]] },
            { x: 1450, w: 90, h: 410, win: [[10, 30], [55, 30], [10, 90]] },
            { x: 1550, w: 140, h: 200, win: [[15, 35], [80, 35], [15, 80]] },
            { x: 1700, w: 80, h: 360, win: [[10, 30], [50, 30], [10, 85]] },
            { x: 1790, w: 100, h: 290, win: [[15, 40], [60, 40]] },
            { x: 1900, w: 130, h: 230, win: [[15, 35], [75, 35]] },
          ].map((b, bi) => (
            <g key={bi}>
              <rect
                x={b.x}
                y={1080 - b.h}
                width={b.w}
                height={b.h}
                fill="#2D2D2D"
                stroke="#3a3a3a"
                strokeWidth="1"
              />
              {b.win.map(([wx, wy], wi) => (
                <rect
                  key={wi}
                  x={b.x + wx}
                  y={1080 - b.h + wy}
                  width={18}
                  height={12}
                  fill={`rgba(233,215,0,${flicker(bi * 7 + wi * 3)})`}
                />
              ))}
            </g>
          ))}
          {/* Ground */}
          <rect x={0} y={1080 - 120} width={2060} height={120} fill="#252525" />
          {/* Road dashes */}
          {Array.from({ length: 20 }, (_, i) => (
            <rect
              key={i}
              x={i * 110 + ((roadX * 0.5) % 110) + 0}
              y={1080 - 60}
              width={60}
              height={8}
              rx={4}
              fill="rgba(233,215,0,0.5)"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={
                (p.x + frame * p.speed * (i % 2 === 0 ? 1 : -0.7)) % 1920
              }
              cy={p.y - frame * 0.3 < 0 ? p.y + 1080 : p.y - frame * 0.3}
              r={p.r}
              fill="#e9d700"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CAR SVG — CENTER STAGE === */}
      <AbsoluteFill
        style={{
          transform: `translateX(${carX}px) translateY(${carBounce}px)`,
          bottom: 100,
          top: 'auto',
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 130,
        }}
      >
        <svg width="700" height="300" viewBox="0 0 700 300">
          {/* Car glow */}
          <ellipse cx="350" cy="270" rx="280" ry="28" fill="rgba(233,215,0,0.12)" />
          {/* Body */}
          <rect x="80" y="160" width="540" height="110" rx="18" fill="#e9d700" />
          {/* Roof */}
          <path
            d="M200 160 Q220 90 350 80 Q480 90 500 160 Z"
            fill="#FFC107"
          />
          {/* Windows */}
          <path
            d="M215 155 Q225 100 340 92 L340 155 Z"
            fill="rgba(28,28,28,0.7)"
            stroke="#333"
            strokeWidth="2"
          />
          <path
            d="M345 155 L345 92 Q460 100 490 155 Z"
            fill="rgba(28,28,28,0.7)"
            stroke="#333"
            strokeWidth="2"
          />
          {/* Wheels */}
          <circle cx="185" cy="270" r="52" fill="#1C1C1C" />
          <circle cx="185" cy="270" r="36" fill="#2D2D2D" />
          <circle cx="185" cy="270" r="18" fill="#9E9E9E" />
          <circle cx="515" cy="270" r="52" fill="#1C1C1C" />
          <circle cx="515" cy="270" r="36" fill="#2D2D2D" />
          <circle cx="515" cy="270" r="18" fill="#9E9E9E" />
          {/* Headlight */}
          <ellipse cx="620" cy="200" rx="22" ry="14" fill="rgba(255,240,120,0.9)" />
          <ellipse cx="80" cy="200" rx="16" ry="10" fill="rgba(255,100,100,0.7)" />
          {/* Door lines */}
          <line x1="350" y1="163" x2="350" y2="268" stroke="#1C1C1C" strokeWidth="3" />
          {/* Door handle */}
          <rect x="290" y="205" width="32" height="8" rx="4" fill="#1C1C1C" />
          <rect x="385" y="205" width="32" height="8" rx="4" fill="#1C1C1C" />
        </svg>
      </AbsoluteFill>

      {/* === HEADING === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'flex-start',
          justifyContent: 'flex-start',
          padding: '60px 80px',
          transform: `translateY(${headY}px)`,
          opacity: headOp,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 38,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 3,
            textTransform: 'uppercase',
            marginBottom: 10,
          }}
        >
          CHAPTER 3
        </div>
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 82,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            maxWidth: 900,
          }}
        >
          नई कार का सपना
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 38,
            fontWeight: 500,
            color: '#e9d700',
            marginTop: 16,
          }}
        >
          Delhi · Mumbai · IT Sector · Govt Job
        </div>
      </AbsoluteFill>

      {/* === BADGE — "DREAM PURCHASE" === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-start',
          justifyContent: 'flex-end',
          padding: '70px 80px',
          transform: `scale(${badgeScale})`,
          transformOrigin: 'top right',
          opacity: badgeScale,
        }}
      >
        <div
          style={{
            background: 'linear-gradient(135deg, #ff0000 0%, #aa0000 100%)',
            borderRadius: 14,
            padding: '18px 34px',
            fontFamily: '"Inter", sans-serif',
            fontSize: 34,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 2,
            boxShadow: '0 8px 40px rgba(255,0,0,0.4)',
          }}
        >
          DREAM PURCHASE ↓
        </div>
      </AbsoluteFill>

      {/* === CITY LABELS === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 28,
          opacity: interpolate(sp(35), [0, 1], [0, 1]),
        }}
      >
        {['🏙 Delhi', '🌆 Mumbai', '💻 IT Sector', '🏛 Govt Job'].map(
          (label, i) => (
            <div
              key={i}
              style={{
                fontFamily: '"Inter", sans-serif',
                fontSize: 28,
                fontWeight: 600,
                color: '#9E9E9E',
                marginRight: 48,
                transform: `translateY(${interpolate(
                  sp(35 + i * 8),
                  [0, 1],
                  [30, 0]
                )}px)`,
                opacity: interpolate(sp(35 + i * 8), [0, 1], [0, 1]),
              }}
            >
              {label}
            </div>
          )
        )}
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          transform: `translateX(${figureSlide}px)`,
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 80,
          paddingBottom: 130,
        }}
      >
        <svg width="100" height="200" viewBox="0 0 100 200">
          {/* Glow */}
          <ellipse cx="50" cy="185" rx="36" ry="10" fill="rgba(233,215,0,0.18)" />
          {/* Body */}
          <g transform={`translateY(${bodyBreathe}px)`}>
            {/* Head */}
            <g transform={`rotate(${headNod}, 50, 30)`}>
              <circle cx="50" cy="30" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              {/* Golden glasses */}
              <ellipse cx="41" cy="30" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="59" cy="30" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="50" y1="30" x2="50" y2="30" stroke="#FFC107" strokeWidth="2" />
              <line x1="32" y1="30" x2="28" y2="28" stroke="#FFC107" strokeWidth="2" />
              <line x1="68" y1="30" x2="72" y2="28" stroke="#FFC107" strokeWidth="2" />
              {/* Smile */}
              <path d="M43 37 Q50 44 57 37" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            {/* Neck */}
            <line x1="50" y1="52" x2="50" y2="70" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Body */}
            <line x1="50" y1="70" x2="50" y2="130" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Arms */}
            <line
              x1="50"
              y1="80"
              x2="20"
              y2="108"
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            <line
              x1="50"
              y1="80"
              x2="80"
              y2="105"
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            {/* Legs */}
            <line x1="50" y1="130" x2="30" y2="175" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="50" y1="130" x2="70" y2="175" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 50%, rgba(0,0,0,0.72) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S1;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S2',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Three car cards staggered
  const cars = [
    { name: 'Maruti Swift', price: '₹8.5L', color: '#e9d700', accent: '#b8a800' },
    { name: 'Honda City', price: '₹12L', color: '#F5F5F5', accent: '#9E9E9E' },
    { name: 'Hyundai Creta', price: '₹16L', color: '#ff0000', accent: '#aa0000' },
  ];

  // Diagonal stripe BG
  const stripeOffset = interpolate(frame, [0, durationInFrames], [0, 80], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Heading
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [40, 0]);

  // Shine sweep on cards
  const shine = interpolate(frame, [20, 80], [-200, 900], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Confetti particles
  const confetti = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i * 23) * 1920,
    y: (seededRandom(i * 29) * 1080 + frame * (1 + seededRandom(i * 31))) % 1080,
    size: 6 + seededRandom(i * 37) * 10,
    color: ['#e9d700', '#FFC107', '#F5F5F5', '#ff0000'][i % 4],
    rot: frame * (2 + seededRandom(i * 41) * 3),
  }));

  // "My Life is Good" badge
  const badgeS = sp(55);

  // Stick figure with thumbs up
  const figSlide = interpolate(sp(50), [0, 1], [250, 0]);
  const headNod = Math.sin(frame * 0.07) * 6;
  const bodyBob = Math.sin(frame * 0.09) * 3;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG GRADIENT === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(135deg, #1a1a00 0%, #1C1C1C 40%, #1a0000 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === DIAGONAL STRIPES === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 22 }, (_, i) => (
            <line
              key={i}
              x1={i * 92 - 200 + stripeOffset}
              y1={0}
              x2={i * 92 - 200 + stripeOffset - 400}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="28"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === CONFETTI PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {confetti.map((c, i) => (
            <rect
              key={i}
              x={c.x}
              y={c.y}
              width={c.size}
              height={c.size * 0.5}
              rx={1}
              fill={c.color}
              opacity={0.18}
              transform={`rotate(${c.rot}, ${c.x + c.size / 2}, ${c.y + c.size / 4})`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === HEADING === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 52,
          opacity: headOp,
          transform: `translateY(${headY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          कौन सी कार? सब अच्छी लगती हैं!
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 34,
            color: '#e9d700',
            fontWeight: 600,
            marginTop: 10,
          }}
        >
          The Dream Showroom
        </div>
      </AbsoluteFill>

      {/* === 3 CAR CARDS === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 0,
          paddingTop: 160,
        }}
      >
        {cars.map((car, i) => {
          const cardS = sp(18 + i * 14);
          const cardScale = interpolate(cardS, [0, 1], [0.7, 1]);
          const cardOp = interpolate(cardS, [0, 1], [0, 1]);
          const cardY = interpolate(cardS, [0, 1], [60, 0]);
          const isCenter = i === 1;
          return (
            <div
              key={i}
              style={{
                width: isCenter ? 440 : 360,
                height: isCenter ? 460 : 380,
                background:
                  i === 0
                    ? 'linear-gradient(145deg, #2a2800 0%, #1C1C1C 100%)'
                    : i === 1
                    ? 'linear-gradient(145deg, #2D2D2D 0%, #1C1C1C 100%)'
                    : 'linear-gradient(145deg, #2a0000 0%, #1C1C1C 100%)',
                borderRadius: 24,
                border: `3px solid ${car.color}`,
                margin: i === 1 ? '0 -8px' : 0,
                zIndex: isCenter ? 2 : 1,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${cardScale}) translateY(${cardY}px)`,
                opacity: cardOp,
                boxShadow: `0 20px 60px ${car.color}33`,
                overflow: 'hidden',
                position: 'relative',
              }}
            >
              {/* Shine */}
              <div
                style={{
                  position: 'absolute',
                  top: 0,
                  left: shine - 200,
                  width: 120,
                  height: '100%',
                  background:
                    'linear-gradient(90deg, transparent, rgba(255,255,255,0.08), transparent)',
                  transform: 'skewX(-15deg)',
                }}
              />
              {/* Car Icon SVG */}
              <svg
                width={isCenter ? 260 : 210}
                height={isCenter ? 130 : 105}
                viewBox="0 0 260 130"
                style={{ marginBottom: 16 }}
              >
                <ellipse cx="130" cy="120" rx="110" ry="12" fill={`${car.color}22`} />
                <rect x="20" y="60" width="220" height="55" rx="10" fill={car.color} />
                <path
                  d="M60 60 Q75 20 130 15 Q185 20 200 60 Z"
                  fill={car.accent}
                />
                <path d="M65 57 Q78 25 125 18 L125 57 Z" fill="rgba(0,0,0,0.5)" />
                <path d="M135 57 L135 18 Q182 25 196 57 Z" fill="rgba(0,0,0,0.5)" />
                <circle cx="65" cy="115" r="22" fill="#1C1C1C" />
                <circle cx="65" cy="115" r="14" fill="#2D2D2D" />
                <circle cx="65" cy="115" r="6" fill="#9E9E9E" />
                <circle cx="195" cy="115" r="22" fill="#1C1C1C" />
                <circle cx="195" cy="115" r="14" fill="#2D2D2D" />
                <circle cx="195" cy="115" r="6" fill="#9E9E9E" />
              </svg>
              <div
                style={{
                  fontFamily: '"Inter", sans-serif',
                  fontSize: isCenter ? 36 : 30,
                  fontWeight: 900,
                  color: '#F5F5F5',
                  marginBottom: 8,
                }}
              >
                {car.name}
              </div>
              <div
                style={{
                  fontFamily: '"JetBrains Mono", monospace',
                  fontSize: isCenter ? 42 : 34,
                  fontWeight: 700,
                  color: car.color,
                }}
              >
                {car.price}
              </div>
              {isCenter && (
                <div
                  style={{
                    marginTop: 14,
                    background: '#e9d700',
                    color: '#1C1C1C',
                    fontFamily: '"Inter", sans-serif',
                    fontSize: 22,
                    fontWeight: 900,
                    padding: '8px 22px',
                    borderRadius: 8,
                    letterSpacing: 1,
                  }}
                >
                  MOST POPULAR
                </div>
              )}
            </div>
          );
        })}
      </AbsoluteFill>

      {/* === "MY LIFE IS GOOD" BADGE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 42,
          opacity: interpolate(badgeS, [0, 1], [0, 1]),
          transform: `scale(${interpolate(badgeS, [0, 1], [0.8, 1])})`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 46,
            fontWeight: 800,
            color: '#e9d700',
            background: 'rgba(233,215,0,0.08)',
            border: '2px solid rgba(233,215,0,0.3)',
            borderRadius: 12,
            padding: '14px 40px',
            letterSpacing: 1,
          }}
        >
          "मेरी जिन्दगी अच्छी चल रही है" ✨
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE — EXCITED === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-end',
          paddingRight: 80,
          paddingBottom: 42,
          transform: `translateX(${figSlide}px)`,
        }}
      >
        <svg width="110" height="210" viewBox="0 0 110 210">
          <ellipse cx="55" cy="198" rx="38" ry="10" fill="rgba(233,215,0,0.15)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 55, 32)`}>
              <circle cx="55" cy="32" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="45" cy="32" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="65" cy="32" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="54" y1="32" x2="56" y2="32" stroke="#FFC107" strokeWidth="2" />
              <line x1="36" y1="32" x2="32" y2="30" stroke="#FFC107" strokeWidth="2" />
              <line x1="74" y1="32" x2="78" y2="30" stroke="#FFC107" strokeWidth="2" />
              {/* Big grin */}
              <path d="M44 40 Q55 50 66 40" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            </g>
            <line x1="55" y1="56" x2="55" y2="76" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="55" y1="76" x2="55" y2="140" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Arms up (excited) */}
            <line x1="55" y1="88" x2="18" y2="64" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="88" x2="92" y2="64" stroke="#F5F5F5" strokeWidth="3" />
            {/* Thumbs up fists */}
            <circle cx="16" cy="62" r="7" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <circle cx="94" cy="62" r="7" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="55" y1="140" x2="36" y2="188" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="55" y1="140" x2="74" y2="188" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 85% 85% at 50% 50%, transparent 45%, rgba(0,0,0,0.75) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S2;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S3',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 14, mass: 0.5, stiffness: 130, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Falling value bar animation
  const barDrop = interpolate(frame, [20, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: (t) => t * t,
  });

  // Price tag before/after
  const beforeOp = interpolate(sp(10), [0, 1], [0, 1]);
  const arrowScale = interpolate(sp(30), [0, 1], [0, 1]);
  const afterS = sp(45);

  // Depreciation percentage counter
  const depPct = interpolate(frame, [40, 90], [0, 15], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Cracked screen effect on value drop
  const crackOp = interpolate(frame, [85, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles — red downward
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: 200 + seededRandom(i * 19) * 1500,
    y: (seededRandom(i * 23) * 400 + frame * (2 + seededRandom(i * 29))) % 800 + 200,
    size: 4 + seededRandom(i * 31) * 8,
    op: 0.15 + seededRandom(i * 37) * 0.2,
  }));

  // Concentric circle pulse
  const pulse = Math.sin(frame * 0.1) * 0.5 + 0.5;

  // Stick figure — shocked
  const figSlide = interpolate(sp(55), [0, 1], [-180, 0]);
  const headShock = Math.sin(frame * 0.12) * 3;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 70% 70% at 60% 50%, #200000 0%, #1C1C1C 65%, #0d0d0d 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === CONCENTRIC CIRCLES — IMPACT RIPPLE === */}
      <AbsoluteFill style={{ pointerEvents: 'none', opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {[180, 300, 430, 570, 720].map((r, i) => (
            <circle
              key={i}
              cx="1100"
              cy="540"
              r={r + pulse * 20}
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FALLING RED PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle
              key={i}
              cx={p.x}
              cy={p.y}
              r={p.size}
              fill="#ff0000"
              opacity={p.op}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === QUESTION HEADLINE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'flex-start',
          justifyContent: 'flex-start',
          padding: '55px 80px',
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.15,
            maxWidth: 860,
          }}
        >
          सच में कितना खर्च कर रहे हो?
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 34,
            color: '#ff0000',
            fontWeight: 700,
            marginTop: 12,
            letterSpacing: 1,
          }}
        >
          DEPRECIATION STARTS IMMEDIATELY
        </div>
      </AbsoluteFill>

      {/* === SHOWROOM → ROAD VALUE DROP VISUAL === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          paddingTop: 140,
        }}
      >
        {/* BEFORE CARD */}
        <div
          style={{
            opacity: beforeOp,
            transform: `translateX(${interpolate(sp(10), [0, 1], [-80, 0])}px)`,
          }}
        >
          <div
            style={{
              width: 320,
              height: 320,
              background: 'linear-gradient(145deg, #2D2D2D 0%, #1C1C1C 100%)',
              borderRadius: 20,
              border: '2px solid #e9d700',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 0 40px rgba(233,215,0,0.2)',
            }}
          >
            {/* Showroom icon */}
            <svg width="80" height="80" viewBox="0 0 80 80">
              <rect x="5" y="30" width="70" height="45" rx="4" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
              <polygon points="40,5 5,30 75,30" fill="#e9d700" opacity="0.8" />
              <rect x="30" y="50" width="20" height="25" rx="2" fill="#1C1C1C" />
            </svg>
            <div
              style={{
                fontFamily: '"Inter", sans-serif',
                fontSize: 28,
                color: '#9E9E9E',
                marginTop: 14,
              }}
            >
              Showroom Price
            </div>
            <div
              style={{
                fontFamily: '"JetBrains Mono", monospace',
                fontSize: 52,
                fontWeight: 900,
                color: '#e9d700',
                marginTop: 8,
              }}
            >
              ₹10L
            </div>
          </div>
        </div>

        {/* ARROW */}
        <div
          style={{
            margin: '0 32px',
            transform: `scale(${arrowScale})`,
            opacity: arrowScale,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
          }}
        >
          <svg width="120" height="80" viewBox="0 0 120 80">
            <defs>
              <marker id="arrowRed" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
                <path d="M0,0 L0,6 L8,3 z" fill="#ff0000" />
              </marker>
            </defs>
            <path
              d="M10 40 Q60 10 110 40"
              fill="none"
              stroke="#ff0000"
              strokeWidth="3"
              markerEnd="url(#arrowRed)"
              strokeDasharray="6 4"
            />
          </svg>
          <div
            style={{
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: 42,
              fontWeight: 900,
              color: '#ff0000',
            }}
          >
            -{Math.floor(depPct)}%
          </div>
          <div
            style={{
              fontFamily: '"Inter", sans-serif',
              fontSize: 22,
              color: '#9E9E9E',
              marginTop: 4,
            }}
          >
            INSTANT LOSS
          </div>
        </div>

        {/* AFTER CARD */}
        <div
          style={{
            opacity: interpolate(afterS, [0, 1], [0, 1]),
            transform: `translateX(${interpolate(afterS, [0, 1], [80, 0])}px)`,
          }}
        >
          <div
            style={{
              width: 320,
              height: 320,
              background: 'linear-gradient(145deg, #2a0000 0%, #1C1C1C 100%)',
              borderRadius: 20,
              border: '2px solid #ff0000',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 0 40px rgba(255,0,0,0.25)',
              overflow: 'hidden',
              position: 'relative',
            }}
          >
            {/* Crack overlay */}
            <div style={{ opacity: crackOp, position: 'absolute', inset: 0 }}>
              <svg width="320" height="320">
                <path
                  d="M160 0 L140 80 L190 100 L130 180 L170 200 L110 320"
                  fill="none"
                  stroke="#ff0000"
                  strokeWidth="2"
                  opacity="0.5"
                />
                <path
                  d="M200 40 L170 110 L210 130"
                  fill="none"
                  stroke="#ff0000"
                  strokeWidth="1.5"
                  opacity="0.4"
                />
              </svg>
            </div>
            {/* Road icon */}
            <svg width="80" height="80" viewBox="0 0 80 80">
              <rect x="5" y="5" width="70" height="70" rx="4" fill="#2D2D2D" stroke="#ff0000" strokeWidth="2" />
              <line x1="40" y1="10" x2="40" y2="70" stroke="#e9d700" strokeWidth="3" strokeDasharray="8 6" />
              <line x1="10" y1="50" x2="70" y2="50" stroke="#9E9E9E" strokeWidth="2" />
            </svg>
            <div
              style={{
                fontFamily: '"Inter", sans-serif',
                fontSize: 28,
                color: '#9E9E9E',
                marginTop: 14,
              }}
            >
              After Showroom Exit
            </div>
            <div
              style={{
                fontFamily: '"JetBrains Mono", monospace',
                fontSize: 52,
                fontWeight: 900,
                color: '#ff0000',
                marginTop: 8,
              }}
            >
              ₹8.5L
            </div>
            <div
              style={{
                fontFamily: '"Inter", sans-serif',
                fontSize: 22,
                color: '#ff0000',
                marginTop: 6,
              }}
            >
              ▼ ₹1.5L GONE
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* === BOTTOM BAR — DEPRECIATION METER === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 48,
          opacity: interpolate(sp(35), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            width: 900,
            background: '#2D2D2D',
            borderRadius: 12,
            height: 22,
            overflow: 'hidden',
            border: '1px solid #3a3a3a',
          }}
        >
          <div
            style={{
              width: `${barDrop * 100}%`,
              height: '100%',
              background:
                'linear-gradient(90deg, #e9d700 0%, #ff0000 60%, #aa0000 100%)',
              borderRadius: 12,
              transition: 'none',
            }}
          />
        </div>
        <div
          style={{
            fontFamily: '"JetBrains Mono", monospace',
            fontSize: 32,
            fontWeight: 700,
            color: '#ff0000',
            marginLeft: 22,
          }}
        >
          {Math.floor(barDrop * 15)}% ↓
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE — SHOCKED === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 80,
          paddingBottom: 48,
          transform: `translateX(${figSlide}px)`,
        }}
      >
        <svg width="100" height="195" viewBox="0 0 100 195">
          <ellipse cx="50" cy="185" rx="34" ry="9" fill="rgba(255,0,0,0.12)" />
          <g transform={`rotate(${headShock}, 50, 28)`}>
            <circle cx="50" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
            <ellipse cx="40" cy="26" rx="8" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <ellipse cx="60" cy="26" rx="8" ry="7" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="32" y1="26" x2="28" y2="24" stroke="#FFC107" strokeWidth="2" />
            <line x1="68" y1="26" x2="72" y2="24" stroke="#FFC107" strokeWidth="2" />
            {/* O-mouth shocked */}
            <ellipse cx="50" cy="38" rx="6" ry="5" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          </g>
          <line x1="50" y1="50" x2="50" y2="70" stroke="#F5F5F5" strokeWidth="3.5" />
          <line x1="50" y1="70" x2="50" y2="128" stroke="#F5F5F5" strokeWidth="3.5" />
          {/* Arms out in shock */}
          <line x1="50" y1="82" x2="16" y2="96" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="50" y1="82" x2="84" y2="96" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="50" y1="128" x2="32" y2="178" stroke="#F5F5F5" strokeWidth="3.5" />
          <line x1="50" y1="128" x2="68" y2="178" stroke="#F5F5F5" strokeWidth="3.5" />
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 48%, rgba(0,0,0,0.78) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S3;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S4',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  const costs = [
    {
      label: 'Insurance',
      hindi: 'बीमा',
      amount: '₹6,000/yr',
      icon: 'shield',
      color: '#FFC107',
      delay: 10,
    },
    {
      label: 'Petrol',
      hindi: 'पेट्रोल',
      amount: '₹4,000/mo',
      icon: 'fuel',
      color: '#ff0000',
      delay: 22,
    },
    {
      label: 'Service',
      hindi: 'सर्विसिंग',
      amount: '₹8,000/yr',
      icon: 'wrench',
      color: '#e9d700',
      delay: 34,
    },
    {
      label: 'Tyres',
      hindi: 'टायर',
      amount: '₹12,000',
      icon: 'tyre',
      color: '#9E9E9E',
      delay: 46,
    },
    {
      label: 'Repair Bill',
      hindi: 'रिपेर बिल',
      amount: '₹25,000+',
      icon: 'alert',
      color: '#ff0000',
      delay: 58,
    },
  ];

  // Scrolling ticker
  const tickerX = interpolate(frame, [0, durationInFrames], [0, -600], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Animated coin drain
  const drainY = (frame * 2) % 300;

  // BG hexagon texture
  const hexOp = interpolate(frame, [0, 25], [0, 0.05], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Stick figure
  const figX = interpolate(sp(65), [0, 1], [250, 0]);
  const bodyBreathe = Math.sin(frame * 0.06) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Title
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG GRADIENT === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(160deg, #0a1a00 0%, #1C1C1C 40%, #1a0a00 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === HEX GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: hexOp }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 120 }, (_, i) => {
            const col = i % 14;
            const row = Math.floor(i / 14);
            const cx = col * 140 + (row % 2 === 0 ? 0 : 70);
            const cy = row * 120;
            const pts = Array.from({ length: 6 }, (_, j) => {
              const a = (Math.PI / 3) * j;
              return `${cx + 55 * Math.cos(a)},${cy + 55 * Math.sin(a)}`;
            }).join(' ');
            return (
              <polygon
                key={i}
                points={pts}
                fill="none"
                stroke="#e9d700"
                strokeWidth="1"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === TITLE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 48,
          opacity: titleOp,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 74,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          कार के असली खर्चे
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 32,
            color: '#ff0000',
            fontWeight: 700,
            marginTop: 8,
            letterSpacing: 2,
          }}
        >
          HIDDEN COST BREAKDOWN
        </div>
      </AbsoluteFill>

      {/* === COST CARDS GRID === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexWrap: 'wrap',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 24,
          paddingTop: 180,
          paddingBottom: 120,
        }}
      >
        {costs.map((cost, i) => {
          const cS = sp(cost.delay);
          const cScale = interpolate(cS, [0, 1], [0.6, 1]);
          const cOp = interpolate(cS, [0, 1], [0, 1]);
          const cY = interpolate(cS, [0, 1], [50, 0]);

          return (
            <div
              key={i}
              style={{
                width: 280,
                height: 200,
                background: `linear-gradient(145deg, rgba(${
                  cost.color === '#ff0000'
                    ? '42,0,0'
                    : cost.color === '#e9d700'
                    ? '42,40,0'
                    : cost.color === '#FFC107'
                    ? '40,30,0'
                    : '30,30,30'
                }) 0%, #1C1C1C 100%)`,
                borderRadius: 18,
                border: `2px solid ${cost.color}44`,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${cScale}) translateY(${cY}px)`,
                opacity: cOp,
                boxShadow: `0 10px 35px ${cost.color}22`,
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Top color bar */}
              <div
                style={{
                  position: 'absolute',
                  top: 0,
                  left: 0,
                  right: 0,
                  height: 4,
                  background: cost.color,
                  borderRadius: '18px 18px 0 0',
                }}
              />
              {/* Icon area */}
              <svg
                width="52"
                height="52"
                viewBox="0 0 52 52"
                style={{ marginBottom: 10 }}
              >
                <circle cx="26" cy="26" r="25" fill={`${cost.color}22`} />
                {cost.icon === 'shield' && (
                  <path
                    d="M26 8 L42 14 L42 26 Q42 38 26 44 Q10 38 10 26 L10 14 Z"
                    fill="none"
                    stroke={cost.color}
                    strokeWidth="2.5"
                  />
                )}
                {cost.icon === 'fuel' && (
                  <>
                    <rect x="14" y="12" width="18" height="28" rx="3" fill="none" stroke={cost.color} strokeWidth="2.5" />
                    <path d="M32 16 L40 12 L40 28 Q40 32 36 32" fill="none" stroke={cost.color} strokeWidth="2.5" />
                    <rect x="19" y="18" width="8" height="14" rx="2" fill={cost.color} opacity="0.5" />
                  </>
                )}
                {cost.icon === 'wrench' && (
                  <path
                    d="M36 12 Q40 18 38 24 L20 42 Q16 44 14 40 Q12 36 16 34 L34 16 Q36 14 36 12Z"
                    fill="none"
                    stroke={cost.color}
                    strokeWidth="2.5"
                  />
                )}
                {cost.icon === 'tyre' && (
                  <>
                    <circle cx="26" cy="26" r="17" fill="none" stroke={cost.color} strokeWidth="2.5" />
                    <circle cx="26" cy="26" r="9" fill="none" stroke={cost.color} strokeWidth="2" />
                    {[0, 60, 120, 180, 240, 300].map((angle, j) => (
                      <line
                        key={j}
                        x1={26 + 9 * Math.cos((angle * Math.PI) / 180)}
                        y1={26 + 9 * Math.sin((angle * Math.PI) / 180)}
                        x2={26 + 17 * Math.cos((angle * Math.PI) / 180)}
                        y2={26 + 17 * Math.sin((angle * Math.PI) / 180)}
                        stroke={cost.color}
                        strokeWidth="2"
                      />
                    ))}
                  </>
                )}
                {cost.icon === 'alert' && (
                  <>
                    <polygon
                      points="26,10 44,40 8,40"
                      fill="none"
                      stroke={cost.color}
                      strokeWidth="2.5"
                    />
                    <line x1="26" y1="22" x2="26" y2="32" stroke={cost.color} strokeWidth="3" strokeLinecap="round" />
                    <circle cx="26" cy="37" r="2.5" fill={cost.color} />
                  </>
                )}
              </svg>
              <div
                style={{
                  fontFamily: '"Noto Sans Devanagari", sans-serif',
                  fontSize: 28,
                  fontWeight: 700,
                  color: '#F5F5F5',
                }}
              >
                {cost.hindi}
              </div>
              <div
                style={{
                  fontFamily: '"JetBrains Mono", monospace',
                  fontSize: 32,
                  fontWeight: 900,
                  color: cost.color,
                  marginTop: 4,
                }}
              >
                {cost.amount}
              </div>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* === COIN DRAIN ANIMATION RIGHT SIDE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'flex-end',
          paddingRight: 40,
          opacity: interpolate(sp(20), [0, 1], [0, 1]),
        }}
      >
        <svg width="80" height="320" viewBox="0 0 80 320">
          {Array.from({ length: 6 }, (_, i) => (
            <ellipse
              key={i}
              cx="40"
              cy={(drainY + i * 52) % 320}
              rx="22"
              ry="10"
              fill="#e9d700"
              opacity={0.18 + i * 0.04}
            />
          ))}
          <line x1="40" y1="0" x2="40" y2="320" stroke="#e9d700" strokeWidth="1.5" strokeDasharray="4 6" opacity="0.2" />
          <text x="50" y="300" fill="#ff0000" fontSize="20" fontFamily="Inter" fontWeight="700" opacity="0.7">
            ↓ DRAIN
          </text>
        </svg>
      </AbsoluteFill>

      {/* === BOTTOM TICKER === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          overflow: 'hidden',
          opacity: interpolate(sp(10), [0, 1], [0, 1]),
        }}
      >
        <div
          style={{
            width: '100%',
            background: 'rgba(255,0,0,0.12)',
            borderTop: '1px solid rgba(255,0,0,0.3)',
            padding: '14px 0',
            transform: `translateX(${tickerX}px)`,
            whiteSpace: 'nowrap',
            fontFamily: '"JetBrains Mono", monospace',
            fontSize: 26,
            fontWeight: 700,
            color: '#ff0000',
            letterSpacing: 2,
          }}
        >
          {[...Array(4)].map((_, i) => (
            <span key={i}>
              {' '}
              Insurance ₹6k/yr • Petrol ₹48k/yr • Service ₹8k/yr • Tyres ₹12k • Repairs ₹25k+ • EMI ₹15k/mo{' '}
              ████{' '}
            </span>
          ))}
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 60,
          paddingBottom: 70,
          transform: `translateX(${figX}px)`,
        }}
      >
        <svg width="95" height="188" viewBox="0 0 95 188">
          <ellipse cx="47" cy="178" rx="32" ry="9" fill="rgba(233,215,0,0.12)" />
          <g transform={`translateY(${bodyBreathe}px)`}>
            <g transform={`rotate(${headNod}, 47, 26)`}>
              <circle cx="47" cy="26" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="38" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="56" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="47" y1="25" x2="47" y2="25" stroke="#FFC107" strokeWidth="2" />
              <line x1="30" y1="25" x2="26" y2="23" stroke="#FFC107" strokeWidth="2" />
              <line x1="64" y1="25" x2="68" y2="23" stroke="#FFC107" strokeWidth="2" />
              <path d="M40 33 Q47 39 54 33" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="47" y1="47" x2="47" y2="66" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="66" x2="47" y2="122" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="78" x2="18" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="78" x2="76" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="122" x2="30" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="122" x2="64" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 50%, rgba(0,0,0,0.72) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S4;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S5',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // 7-year stacked bar chart
  const years = [1, 2, 3, 4, 5, 6, 7];
  const yearCosts = [4.5, 4.2, 5.1, 4.8, 6.3, 5.5, 7.8]; // lakhs per year
  const maxVal = 8;

  // Counter animation
  const totalCount = interpolate(frame, [30, 100], [0, 45], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake at reveal
  const shakeX =
    frame > 85 && frame < 100
      ? Math.sin(frame * 1.8) * (frame < 93 ? 6 : 3)
      : 0;
  const shakeY =
    frame > 85 && frame < 100
      ? Math.cos(frame * 2.1) * (frame < 93 ? 4 : 2)
      : 0;

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 17) * 1920,
    y: (seededRandom(i * 23) * 500 + 300 + frame * seededRandom(i * 29) * 1.5) % 900,
    r: 2 + seededRandom(i * 31) * 5,
    op: 0.08 + seededRandom(i * 37) * 0.14,
  }));

  // Stick figure
  const figOp = interpolate(sp(60), [0, 1], [0, 1]);
  const figY = interpolate(sp(60), [0, 1], [60, 0]);
  const headNod = Math.sin(frame * 0.05) * 4;
  const bodyBob = Math.sin(frame * 0.07) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translateX(${shakeX}px) translateY(${shakeY}px)`,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(200deg, #1a0000 0%, #1C1C1C 50%, #001a00 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === ISOMETRIC GRID TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 30 }, (_, i) => (
            <line
              key={`d1${i}`}
              x1={i * 66}
              y1={0}
              x2={i * 66 + 400}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
          {Array.from({ length: 30 }, (_, i) => (
            <line
              key={`d2${i}`}
              x1={i * 66}
              y1={0}
              x2={i * 66 - 400}
              y2={1080}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#ff0000" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === TITLE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 45,
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-28, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 66,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          7 साल में कितना उड़ा?
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            color: '#9E9E9E',
            fontWeight: 600,
            marginTop: 6,
            letterSpacing: 2,
          }}
        >
          YEAR-BY-YEAR COST BREAKDOWN
        </div>
      </AbsoluteFill>

      {/* === BAR CHART === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 130,
          paddingTop: 140,
          gap: 0,
        }}
      >
        {years.map((yr, i) => {
          const barH = (yearCosts[i] / maxVal) * 400;
          const barS = sp(12 + i * 10);
          const animH = interpolate(barS, [0, 1], [0, barH]);
          const barOp = interpolate(barS, [0, 1], [0, 1]);
          const isLast = i === 6;

          return (
            <div
              key={i}
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                marginRight: 18,
                opacity: barOp,
              }}
            >
              {/* Value label */}
              <div
                style={{
                  fontFamily: '"JetBrains Mono", monospace',
                  fontSize: 26,
                  fontWeight: 700,
                  color: isLast ? '#ff0000' : '#e9d700',
                  marginBottom: 8,
                  opacity: interpolate(barS, [0.8, 1], [0, 1]),
                }}
              >
                ₹{yearCosts[i]}L
              </div>
              {/* Bar */}
              <div
                style={{
                  width: 90,
                  height: animH,
                  background: isLast
                    ? 'linear-gradient(0deg, #aa0000 0%, #ff0000 100%)'
                    : `linear-gradient(0deg, #b8a800 0%, #e9d700 100%)`,
                  borderRadius: '6px 6px 0 0',
                  boxShadow: isLast
                    ? '0 0 25px rgba(255,0,0,0.5)'
                    : '0 0 15px rgba(233,215,0,0.3)',
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                {/* Shine */}
                <div
                  style={{
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    width: '40%',
                    height: '100%',
                    background: 'rgba(255,255,255,0.1)',
                  }}
                />
                {isLast && (
                  <div
                    style={{
                      position: 'absolute',
                      top: 8,
                      left: '50%',
                      transform: 'translateX(-50%)',
                    }}
                  >
                    <svg width="28" height="28" viewBox="0 0 28 28">
                      <polygon points="14,3 16,10 24,10 18,15 20,23 14,18 8,23 10,15 4,10 12,10" fill="#F5F5F5" />
                    </svg>
                  </div>
                )}
              </div>
              {/* Year label */}
              <div
                style={{
                  fontFamily: '"Inter", sans-serif',
                  fontSize: 24,
                  fontWeight: 700,
                  color: isLast ? '#ff0000' : '#9E9E9E',
                  marginTop: 10,
                }}
              >
                Yr {yr}
              </div>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* === TOTAL COUNTER === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 42,
          flexDirection: 'column',
          paddingRight: 100,
        }}
      >
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'flex-end',
            opacity: interpolate(sp(28), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(28), [0, 1], [30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: '"Noto Sans Devanagari", sans-serif',
              fontSize: 36,
              color: '#9E9E9E',
              fontWeight: 600,
            }}
          >
            कुल नुकसान
          </div>
          <div
            style={{
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: 110,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1,
              textShadow: '0 0 40px rgba(255,0,0,0.5)',
            }}
          >
            ₹{Math.floor(totalCount)}L
          </div>
          <div
            style={{
              fontFamily: '"Inter", sans-serif',
              fontSize: 28,
              color: '#9E9E9E',
              fontWeight: 500,
            }}
          >
            in 7 years · one car
          </div>
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 60,
          paddingBottom: 42,
          opacity: figOp,
          transform: `translateY(${figY}px)`,
        }}
      >
        <svg width="95" height="188" viewBox="0 0 95 188">
          <ellipse cx="47" cy="178" rx="32" ry="9" fill="rgba(255,0,0,0.14)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 47, 26)`}>
              <circle cx="47" cy="26" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="38" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="56" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="30" y1="25" x2="26" y2="23" stroke="#FFC107" strokeWidth="2" />
              <line x1="64" y1="25" x2="68" y2="23" stroke="#FFC107" strokeWidth="2" />
              {/* Sad mouth */}
              <path d="M40 35 Q47 30 54 35" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="47" y1="47" x2="47" y2="66" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="66" x2="47" y2="122" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="80" x2="20" y2="104" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="80" x2="74" y2="104" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="122" x2="30" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="122" x2="64" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 48%, rgba(0,0,0,0.78) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S5;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S6',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Falling value curve animation
  const curveProgress = interpolate(frame, [10, 90], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // The depreciation curve points (SVG path)
  // X: 0 to 700, Y: 50 (top) to 380 (bottom)
  // Curve: rapid fall then slow
  const curvePoints = (t: number) => {
    const x = t * 700;
    const y = 50 + 330 * (1 - Math.exp(-3.5 * t));
    return { x, y };
  };

  // Build path up to curveProgress
  const pathD = (() => {
    let d = '';
    const steps = 60;
    for (let i = 0; i <= steps; i++) {
      const t = (i / steps) * curveProgress;
      const { x, y } = curvePoints(t);
      d += i === 0 ? `M ${x} ${y}` : ` L ${x} ${y}`;
    }
    return d;
  })();

  // Current point on curve
  const dotPt = curvePoints(curveProgress);

  // Value labels
  const startVal = 12;
  const currentVal = Math.max(3, 12 - curveProgress * 9);

  // Question mark big bounce
  const qS = sp(65);
  const qScale = interpolate(qS, [0, 1], [0, 1]);

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 19) * 1920,
    y: (seededRandom(i * 23) * 1080 + frame * 0.5) % 1080,
    r: 2 + seededRandom(i * 29) * 4,
    op: 0.08 + seededRandom(i * 37) * 0.12,
  }));

  // Stick figure
  const figX = interpolate(sp(55), [0, 1], [220, 0]);
  const headNod = Math.sin(frame * 0.05) * 3;
  const bodyBob = Math.sin(frame * 0.07) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 90% 80% at 50% 60%, #1a0800 0%, #1C1C1C 60%, #000a1a 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === DOT TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 240 }, (_, i) => {
            const col = i % 20;
            const row = Math.floor(i / 20);
            return (
              <circle
                key={i}
                cx={col * 98 + 48}
                cy={row * 90 + 45}
                r={2.5}
                fill="#e9d700"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* === FLOATING PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#9E9E9E" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 50,
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 68,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          रीसेल वैल्यू — ₹3-4 लाक?
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            color: '#ff0000',
            fontWeight: 700,
            marginTop: 8,
            letterSpacing: 2,
          }}
        >
          THE DEPRECIATION CURVE
        </div>
      </AbsoluteFill>

      {/* === DEPRECIATION GRAPH === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          paddingTop: 150,
        }}
      >
        <svg width="820" height="460" viewBox="0 0 820 460">
          {/* Axes */}
          <line x1="60" y1="50" x2="60" y2="410" stroke="#3a3a3a" strokeWidth="2" />
          <line x1="60" y1="410" x2="780" y2="410" stroke="#3a3a3a" strokeWidth="2" />

          {/* Y-axis labels */}
          {[12, 10, 8, 6, 4, 2].map((v, i) => (
            <g key={i}>
              <line x1="54" y1={50 + i * 60} x2="66" y2={50 + i * 60} stroke="#3a3a3a" strokeWidth="1.5" />
              <text x="42" y={50 + i * 60 + 6} fill="#9E9E9E" fontSize="18" textAnchor="middle" fontFamily="JetBrains Mono">
                ₹{v}L
              </text>
              <line x1="66" y1={50 + i * 60} x2="770" y2={50 + i * 60} stroke="#2a2a2a" strokeWidth="1" />
            </g>
          ))}

          {/* X-axis labels */}
          {[0, 1, 2, 3, 4, 5, 6, 7].map((yr, i) => (
            <text
              key={i}
              x={60 + i * 102}
              y={432}
              fill="#9E9E9E"
              fontSize="18"
              textAnchor="middle"
              fontFamily="Inter"
            >
              Yr{yr}
            </text>
          ))}

          {/* Area fill */}
          <path
            d={`${pathD} L ${dotPt.x + 60} 410 L 60 410 Z`}
            fill="rgba(255,0,0,0.08)"
            transform="translate(60,0)"
          />

          {/* Curve line */}
          <path
            d={pathD}
            fill="none"
            stroke="#ff0000"
            strokeWidth="4"
            strokeLinecap="round"
            transform="translate(60,0)"
          />

          {/* Moving dot */}
          <circle
            cx={dotPt.x + 60}
            cy={dotPt.y}
            r="10"
            fill="#ff0000"
            filter="url(#glow)"
          />
          <defs>
            <filter id="glow">
              <feGaussianBlur stdDeviation="4" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          {/* Start value tag */}
          <rect x="68" y="30" width="90" height="32" rx="6" fill="#e9d700" />
          <text x="113" y="51" fill="#1C1C1C" fontSize="18" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">₹12L</text>

          {/* End value tag */}
          {curveProgress > 0.85 && (
            <>
              <rect x={dotPt.x + 68} y={dotPt.y - 20} width="80" height="32" rx="6" fill="#ff0000" />
              <text x={dotPt.x + 108} y={dotPt.y + 1} fill="#F5F5F5" fontSize="18" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">₹3L</text>
            </>
          )}

          {/* Labels */}
          <text x="420" y="240" fill="#ff0000" fontSize="24" fontWeight="bold" textAnchor="middle" fontFamily="Noto Sans Devanagari" opacity="0.4">
            75% मूल्य खो गया
          </text>
        </svg>
      </AbsoluteFill>

      {/* === QUESTION MARK SLAM === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-end',
          paddingRight: 80,
          paddingBottom: 60,
          transform: `scale(${qScale})`,
          transformOrigin: 'bottom right',
          opacity: qScale,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 220,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            opacity: 0.9,
            textShadow: '0 0 60px rgba(233,215,0,0.4)',
          }}
        >
          ?
        </div>
      </AbsoluteFill>

      {/* === VERDICT LABEL === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 80,
          paddingBottom: 44,
          opacity: interpolate(sp(50), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(50), [0, 1], [30, 0])}px)`,
        }}
      >
        <div
          style={{
            background: 'linear-gradient(135deg, #ff0000, #880000)',
            borderRadius: 14,
            padding: '16px 38px',
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 40,
            fontWeight: 900,
            color: '#F5F5F5',
            boxShadow: '0 10px 40px rgba(255,0,0,0.4)',
          }}
        >
          ये संपत्ती नहीं — ये चूसने वाली मशीन है
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 80,
          paddingBottom: 120,
          transform: `translateX(${figX}px)`,
        }}
      >
        <svg width="95" height="188" viewBox="0 0 95 188">
          <ellipse cx="47" cy="178" rx="32" ry="9" fill="rgba(255,0,0,0.12)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 47, 26)`}>
              <circle cx="47" cy="26" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="38" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="56" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="30" y1="25" x2="26" y2="23" stroke="#FFC107" strokeWidth="2" />
              <line x1="64" y1="25" x2="68" y2="23" stroke="#FFC107" strokeWidth="2" />
              <path d="M40 36 Q47 31 54 36" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="47" y1="47" x2="47" y2="66" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="66" x2="47" y2="122" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="80" x2="20" y2="104" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="80" x2="74" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="122" x2="30" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="122" x2="64" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 46%, rgba(0,0,0,0.78) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S6;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S7',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S7: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Chain links animation — slavery visual
  const chainShift = Math.sin(frame * 0.08) * 5;
  const chainPull = interpolate(frame, [0, 60], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Transition — second half shows flat building
  const splitProgress = interpolate(frame, [65, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 19) * 1920,
    y: (seededRandom(i * 23) * 800 + frame * seededRandom(i * 29) * 0.8) % 900,
    r: 2 + seededRandom(i * 31) * 5,
    op: 0.08 + seededRandom(i * 37) * 0.14,
  }));

  // Stick figure — chained
  const figSlide = interpolate(sp(8), [0, 1], [-200, 0]);
  const headNod = Math.sin(frame * 0.05) * 3;
  const bodyDrag = Math.sin(frame * 0.06) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG — DUAL SPLIT === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(170deg, #1a0000 0%, #1C1C1C 45%, #00001a 100%)',
          }}
        />
        {/* Building reveal panel right */}
        <div
          style={{
            position: 'absolute',
            top: 0,
            right: 0,
            width: `${splitProgress * 50}%`,
            bottom: 0,
            background:
              'linear-gradient(135deg, #001a0a 0%, #1C1C1C 100%)',
            borderLeft: `3px solid rgba(233,215,0,${splitProgress * 0.4})`,
            overflow: 'hidden',
          }}
        />
      </AbsoluteFill>

      {/* === SCAN LINES TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04, pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 54 }, (_, i) => (
            <line
              key={i}
              x1={0}
              y1={i * 20}
              x2={1920}
              y2={i * 20}
              stroke="#F5F5F5"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#9E9E9E" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === LEFT — SLAVERY VISUAL === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'flex-start',
          justifyContent: 'center',
          paddingLeft: 100,
          paddingRight: splitProgress > 0.5 ? '55%' : 80,
        }}
      >
        {/* Headline */}
        <div
          style={{
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: '"Noto Sans Devanagari", sans-serif',
              fontSize: 82,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.1,
            }}
          >
            तुम मालिक नहीं
          </div>
          <div
            style={{
              fontFamily: '"Noto Sans Devanagari", sans-serif',
              fontSize: 82,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1.1,
            }}
          >
            तुम गुलाम हो!
          </div>
        </div>

        {/* Chain SVG */}
        <div
          style={{
            marginTop: 40,
            transform: `translateX(${chainShift}px)`,
            opacity: interpolate(sp(18), [0, 1], [0, 1]),
          }}
        >
          <svg width="500" height="110" viewBox="0 0 500 110">
            {/* Chain links */}
            {Array.from({ length: 8 }, (_, i) => (
              <g key={i} transform={`translate(${i * 62 * chainPull * 0.8 + i * 10}, 0)`}>
                <ellipse
                  cx={40 + i * 0}
                  cy={55}
                  rx={28}
                  ry={14}
                  fill="none"
                  stroke="#9E9E9E"
                  strokeWidth="5"
                  transform={`rotate(${i % 2 === 0 ? 0 : 90}, ${40}, 55)`}
                />
              </g>
            ))}
            {/* Car at end */}
            <g transform={`translate(350, 20)`}>
              <svg width="130" height="70" viewBox="0 0 130 70">
                <rect x="15" y="28" width="100" height="38" rx="6" fill="#e9d700" />
                <path d="M35 28 Q42 8 65 5 Q88 8 95 28 Z" fill="#FFC107" />
                <circle cx="38" cy="66" r="10" fill="#1C1C1C" />
                <circle cx="92" cy="66" r="10" fill="#1C1C1C" />
              </svg>
            </g>
          </svg>
        </div>

        {/* Chain tag */}
        <div
          style={{
            marginTop: 24,
            opacity: interpolate(sp(30), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(30), [0, 1], [20, 0])}px)`,
            background: 'rgba(255,0,0,0.12)',
            border: '1.5px solid rgba(255,0,0,0.4)',
            borderRadius: 10,
            padding: '14px 28px',
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#ff0000',
          }}
        >
          EMI · Insurance · Petrol · Repairs — FOREVER
        </div>
      </AbsoluteFill>

      {/* === RIGHT — FLAT BUILDING REVEAL === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          paddingLeft: '55%',
          opacity: splitProgress,
          transform: `translateX(${interpolate(splitProgress, [0, 1], [60, 0])}px)`,
        }}
      >
        {/* Building icon */}
        <svg width="260" height="380" viewBox="0 0 260 380">
          {/* Building */}
          <rect x="30" y="80" width="200" height="280" rx="4" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          {/* Windows grid */}
          {Array.from({ length: 5 }, (_, row) =>
            Array.from({ length: 4 }, (_, col) => (
              <rect
                key={`${row}-${col}`}
                x={50 + col * 44}
                y={100 + row * 50}
                width={28}
                height={30}
                rx={2}
                fill={seededRandom(row * 7 + col * 11) > 0.5 ? 'rgba(233,215,0,0.6)' : '#1C1C1C'}
              />
            ))
          )}
          {/* Roof */}
          <polygon points="130,10 30,80 230,80" fill="#e9d700" opacity="0.8" />
          {/* Door */}
          <rect x="105" y="310" width="50" height="50" rx="2" fill="#1C1C1C" />
          {/* Price tag */}
          <g transform="translate(200, 30)">
            <rect x="0" y="0" width="90" height="40" rx="8" fill="#e9d700" />
            <text x="45" y="27" fill="#1C1C1C" fontSize="20" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">₹1 Cr</text>
          </g>
        </svg>
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 44,
            fontWeight: 900,
            color: '#e9d700',
            marginTop: 16,
            textAlign: 'center',
          }}
        >
          दिल्ली का फ्लाट
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 26,
            color: '#9E9E9E',
            marginTop: 8,
          }}
        >
          Next example incoming...
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE — CHAINED === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 80,
          paddingBottom: 42,
          transform: `translateX(${figSlide}px)`,
        }}
      >
        <svg width="110" height="210" viewBox="0 0 110 210">
          <ellipse cx="55" cy="200" rx="36" ry="10" fill="rgba(255,0,0,0.14)" />
          <g transform={`translateY(${bodyDrag}px)`}>
            <g transform={`rotate(${headNod}, 55, 28)`}>
              <circle cx="55" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="45" cy="27" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="65" cy="27" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="36" y1="27" x2="32" y2="25" stroke="#FFC107" strokeWidth="2" />
              <line x1="74" y1="27" x2="78" y2="25" stroke="#FFC107" strokeWidth="2" />
              {/* Sad */}
              <path d="M46 37 Q55 31 64 37" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="55" y1="50" x2="55" y2="70" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="55" y1="70" x2="55" y2="132" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Arms down, defeated */}
            <line x1="55" y1="85" x2="28" y2="118" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="85" x2="82" y2="118" stroke="#F5F5F5" strokeWidth="3" />
            {/* Chain on wrists */}
            <circle cx="27" cy="120" r="7" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <circle cx="83" cy="120" r="7" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <line x1="55" y1="132" x2="36" y2="182" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="55" y1="132" x2="74" y2="182" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Chains on ankles */}
            <circle cx="35" cy="184" r="7" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
            <circle cx="75" cy="184" r="7" fill="none" stroke="#9E9E9E" strokeWidth="2.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 48%, rgba(0,0,0,0.76) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S7;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S8',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Donut/pie chart — down payment vs loan
  const pieProgress = interpolate(frame, [15, 75], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  // 50-50 split → down payment angle = 180 deg
  const pieAngle = pieProgress * 180;

  // EMI counter scroll
  const emiCount = interpolate(frame, [40, 95], [0, 25], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Interest total
  const interestCount = interpolate(frame, [50, 105], [0, 52], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Floating number particles
  const numParticles = ['₹', '%', 'L', 'EMI', '7%', '25yr'].map((n, i) => ({
    label: n,
    x: 80 + seededRandom(i * 19) * 1760,
    y: (100 + seededRandom(i * 23) * 880 + frame * 0.4) % 1080,
    op: 0.06 + seededRandom(i * 31) * 0.08,
    size: 20 + seededRandom(i * 37) * 18,
  }));

  // Stick figure
  const figX = interpolate(sp(60), [0, 1], [250, 0]);
  const headNod = Math.sin(frame * 0.05) * 3;
  const bodyBob = Math.sin(frame * 0.07) * 2;

  // Pie math helpers
  const cx = 300;
  const cy = 300;
  const r = 220;
  const toRad = (deg: number) => (deg * Math.PI) / 180;
  const slicePath = (start: number, end: number) => {
    const s = toRad(start - 90);
    const e = toRad(end - 90);
    const x1 = cx + r * Math.cos(s);
    const y1 = cy + r * Math.sin(s);
    const x2 = cx + r * Math.cos(e);
    const y2 = cy + r * Math.sin(e);
    const large = end - start > 180 ? 1 : 0;
    return `M${cx},${cy} L${x1},${y1} A${r},${r} 0 ${large},1 ${x2},${y2} Z`;
  };

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(145deg, #001a0a 0%, #1C1C1C 50%, #1a1a00 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === CIRCUIT TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 14 }, (_, i) => (
            <g key={i}>
              <line x1={i * 140} y1={0} x2={i * 140} y2={1080} stroke="#e9d700" strokeWidth="1" />
              <line x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#e9d700" strokeWidth="1" />
              <circle cx={i * 140} cy={(i % 3) * 240 + 120} r={5} fill="#e9d700" />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* === NUMBER FLOAT PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        {numParticles.map((p, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y,
              fontFamily: '"JetBrains Mono", monospace',
              fontSize: p.size,
              fontWeight: 700,
              color: '#e9d700',
              opacity: p.op,
              userSelect: 'none',
            }}
          >
            {p.label}
          </div>
        ))}
      </AbsoluteFill>

      {/* === HEADLINE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 48,
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-28, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 70,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          ₹1 करोड का फ्लाट — असली हिसाब
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            color: '#e9d700',
            fontWeight: 700,
            marginTop: 8,
            letterSpacing: 2,
          }}
        >
          HOME LOAN MATH DECODED
        </div>
      </AbsoluteFill>

      {/* === DONUT PIE CHART === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingLeft: 120,
          paddingTop: 140,
        }}
      >
        <div style={{ position: 'relative', width: 600, height: 600 }}>
          <svg width="600" height="600" viewBox="0 0 600 600">
            {/* Glow BG */}
            <circle cx={cx} cy={cy} r={r + 30} fill="rgba(233,215,0,0.04)" />
            {/* Down payment slice */}
            <path
              d={slicePath(0, pieAngle)}
              fill="#e9d700"
              opacity="0.9"
            />
            {/* Loan slice */}
            <path
              d={slicePath(pieAngle, 360)}
              fill="#ff0000"
              opacity="0.85"
            />
            {/* Donut hole */}
            <circle cx={cx} cy={cy} r={110} fill="#1C1C1C" />
            {/* Center label */}
            <text x={cx} y={cy - 16} fill="#F5F5F5" fontSize="34" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">₹1 Cr</text>
            <text x={cx} y={cy + 20} fill="#9E9E9E" fontSize="22" textAnchor="middle" fontFamily="Noto Sans Devanagari">कुल कीमत</text>

            {/* Legend labels */}
            <rect x={20} y={540} width={24} height={24} rx={4} fill="#e9d700" />
            <text x={52} y={558} fill="#F5F5F5" fontSize="22" fontFamily="Noto Sans Devanagari">Down Payment ₹50L</text>

            <rect x={240} y={540} width={24} height={24} rx={4} fill="#ff0000" />
            <text x={272} y={558} fill="#F5F5F5" fontSize="22" fontFamily="Noto Sans Devanagari">Loan ₹50L</text>
          </svg>
        </div>

        {/* RIGHT — Loan info cards */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            gap: 24,
            marginLeft: 60,
            opacity: interpolate(sp(30), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(30), [0, 1], [50, 0])}px)`,
          }}
        >
          {[
            { label: 'ब्याज दर', value: '7-8%', color: '#e9d700', sub: 'Annual' },
            {
              label: 'लोन अवधि',
              value: `${Math.floor(emiCount)} साल`,
              color: '#FFC107',
              sub: 'Repayment Period',
            },
            {
              label: 'कुल ब्याज',
              value: `₹${Math.floor(interestCount)}L+`,
              color: '#ff0000',
              sub: 'Total Interest Paid',
            },
            {
              label: 'Monthly EMI',
              value: '₹38,000',
              color: '#9E9E9E',
              sub: 'Per Month Outflow',
            },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                width: 380,
                background: 'rgba(45,45,45,0.5)',
                borderRadius: 14,
                border: `2px solid ${item.color}33`,
                padding: '18px 26px',
                display: 'flex',
                alignItems: 'center',
                gap: 20,
                transform: `translateX(${interpolate(
                  sp(30 + i * 10),
                  [0, 1],
                  [40, 0]
                )}px)`,
                opacity: interpolate(sp(30 + i * 10), [0, 1], [0, 1]),
              }}
            >
              <div
                style={{
                  width: 8,
                  height: 60,
                  background: item.color,
                  borderRadius: 4,
                  flexShrink: 0,
                }}
              />
              <div>
                <div
                  style={{
                    fontFamily: '"Noto Sans Devanagari", sans-serif',
                    fontSize: 24,
                    color: '#9E9E9E',
                    fontWeight: 500,
                  }}
                >
                  {item.label}
                </div>
                <div
                  style={{
                    fontFamily: '"JetBrains Mono", monospace',
                    fontSize: 44,
                    fontWeight: 900,
                    color: item.color,
                    lineHeight: 1.1,
                  }}
                >
                  {item.value}
                </div>
                <div
                  style={{
                    fontFamily: '"Inter", sans-serif',
                    fontSize: 20,
                    color: '#6a6a6a',
                  }}
                >
                  {item.sub}
                </div>
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-end',
          paddingRight: 80,
          paddingBottom: 42,
          transform: `translateX(${figX}px)`,
        }}
      >
        <svg width="95" height="188" viewBox="0 0 95 188">
          <ellipse cx="47" cy="178" rx="32" ry="9" fill="rgba(233,215,0,0.12)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 47, 26)`}>
              <circle cx="47" cy="26" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="38" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="56" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="30" y1="25" x2="26" y2="23" stroke="#FFC107" strokeWidth="2" />
              <line x1="64" y1="25" x2="68" y2="23" stroke="#FFC107" strokeWidth="2" />
              <path d="M40 34 Q47 40 54 34" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="47" y1="47" x2="47" y2="66" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="66" x2="47" y2="122" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="80" x2="20" y2="104" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="80" x2="74" y2="104" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="122" x2="30" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="122" x2="64" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 50%, rgba(0,0,0,0.72) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S8;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S9',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S9: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Waterfall / stacked cost total
  const costs = [
    { label: 'Property Price', hindi: 'मूल कीमत', val: 50, color: '#e9d700' },
    { label: 'Loan Interest', hindi: 'कुल ब्याज', val: 52, color: '#ff0000' },
    { label: 'Interiors', hindi: 'इंटीरियर्स', val: 15, color: '#FFC107' },
    { label: 'Maintenance', hindi: 'मेंटिनेंस', val: 8, color: '#9E9E9E' },
    { label: 'Special Charges', hindi: 'स्पेशल चार्जेस', val: 5, color: '#ff6600' },
  ];
  const total = costs.reduce((a, c) => a + c.val, 0); // 130

  // Counter
  const totalCount = interpolate(frame, [50, 110], [50, 150], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Stacked bars
  const barWidth = 260;

  // Particles
  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 17) * 1920,
    y: (seededRandom(i * 23) * 1000 + frame * 0.4) % 1080,
    r: 1.5 + seededRandom(i * 29) * 4,
    op: 0.06 + seededRandom(i * 37) * 0.1,
  }));

  // Stick figure
  const figY = interpolate(sp(58), [0, 1], [120, 0]);
  const figOp = interpolate(sp(58), [0, 1], [0, 1]);
  const headNod = Math.sin(frame * 0.05) * 4;
  const bodyBob = Math.sin(frame * 0.07) * 2;

  // Scale up impact moment
  const impactShake =
    frame > 88 && frame < 105
      ? Math.sin(frame * 2.2) * 5
      : 0;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translateX(${impactShake}px)`,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 70% 80% at 70% 50%, #1a0a00 0%, #1C1C1C 55%, #001a0a 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === CHEVRON TEXTURE === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 16 }, (_, i) => (
            <polyline
              key={i}
              points={`0,${i * 70} 60,${i * 70 - 35} 120,${i * 70}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="2"
              transform={`translate(${i * 120}, 0)`}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill="#9E9E9E" opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === TITLE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 48,
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-28, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 66,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          25 साल का असली बिल
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            color: '#ff0000',
            fontWeight: 700,
            letterSpacing: 2,
            marginTop: 8,
          }}
        >
          COMPLETE PROPERTY COST PICTURE
        </div>
      </AbsoluteFill>

      {/* === WATERFALL BAR CHART === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          paddingTop: 155,
          gap: 0,
        }}
      >
        {/* Individual cost bars */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            gap: 0,
            marginRight: 60,
          }}
        >
          {costs.map((c, i) => {
            const cS = sp(15 + i * 12);
            const barW = interpolate(cS, [0, 1], [0, (c.val / total) * 700]);
            const cOp = interpolate(cS, [0, 1], [0, 1]);
            return (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  marginBottom: 18,
                  opacity: cOp,
                }}
              >
                {/* Label */}
                <div
                  style={{
                    width: 280,
                    textAlign: 'right',
                    paddingRight: 20,
                    flexShrink: 0,
                  }}
                >
                  <div
                    style={{
                      fontFamily: '"Noto Sans Devanagari", sans-serif',
                      fontSize: 26,
                      fontWeight: 700,
                      color: '#F5F5F5',
                    }}
                  >
                    {c.hindi}
                  </div>
                  <div
                    style={{
                      fontFamily: '"Inter", sans-serif',
                      fontSize: 20,
                      color: '#6a6a6a',
                    }}
                  >
                    {c.label}
                  </div>
                </div>
                {/* Bar */}
                <div
                  style={{
                    width: barW,
                    height: 52,
                    background: `linear-gradient(90deg, ${c.color}cc, ${c.color})`,
                    borderRadius: '0 8px 8px 0',
                    display: 'flex',
                    alignItems: 'center',
                    paddingLeft: 14,
                    boxShadow: `0 4px 20px ${c.color}44`,
                    minWidth: 0,
                    overflow: 'hidden',
                    transition: 'none',
                  }}
                >
                  <span
                    style={{
                      fontFamily: '"JetBrains Mono", monospace',
                      fontSize: 26,
                      fontWeight: 900,
                      color: '#1C1C1C',
                      whiteSpace: 'nowrap',
                    }}
                  >
                    ₹{c.val}L
                  </span>
                </div>
              </div>
            );
          })}

          {/* Total bar */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              marginTop: 16,
              opacity: interpolate(sp(75), [0, 1], [0, 1]),
              transform: `translateY(${interpolate(sp(75), [0, 1], [20, 0])}px)`,
            }}
          >
            <div
              style={{
                width: 280,
                textAlign: 'right',
                paddingRight: 20,
                flexShrink: 0,
              }}
            >
              <div
                style={{
                  fontFamily: '"Noto Sans Devanagari", sans-serif',
                  fontSize: 30,
                  fontWeight: 900,
                  color: '#ff0000',
                }}
              >
                TOTAL
              </div>
            </div>
            <div
              style={{
                width: interpolate(sp(75), [0, 1], [0, 700]),
                height: 68,
                background:
                  'linear-gradient(90deg, #880000, #ff0000)',
                borderRadius: '0 10px 10px 0',
                display: 'flex',
                alignItems: 'center',
                paddingLeft: 20,
                boxShadow: '0 6px 30px rgba(255,0,0,0.5)',
              }}
            >
              <span
                style={{
                  fontFamily: '"JetBrains Mono", monospace',
                  fontSize: 38,
                  fontWeight: 900,
                  color: '#F5F5F5',
                  whiteSpace: 'nowrap',
                }}
              >
                ₹{Math.floor(totalCount)}L — ₹1.5Cr
              </span>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* === SHOCK LABEL === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 42,
          opacity: interpolate(sp(80), [0, 1], [0, 1]),
          transform: `scale(${interpolate(sp(80), [0, 1], [0.8, 1])})`,
        }}
      >
        <div
          style={{
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid rgba(255,0,0,0.5)',
            borderRadius: 12,
            padding: '14px 42px',
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 40,
            fontWeight: 900,
            color: '#ff0000',
            textAlign: 'center',
          }}
        >
          ₹50L की property पर ₹1.5 Cr का खर्च 😳
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 60,
          paddingBottom: 120,
          opacity: figOp,
          transform: `translateY(${figY}px)`,
        }}
      >
        <svg width="95" height="188" viewBox="0 0 95 188">
          <ellipse cx="47" cy="178" rx="32" ry="9" fill="rgba(233,215,0,0.12)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 47, 26)`}>
              <circle cx="47" cy="26" r="21" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="38" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="56" cy="25" rx="8" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="30" y1="25" x2="26" y2="23" stroke="#FFC107" strokeWidth="2" />
              <line x1="64" y1="25" x2="68" y2="23" stroke="#FFC107" strokeWidth="2" />
              {/* Shocked O mouth */}
              <ellipse cx="47" cy="37" rx="6" ry="5" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="47" y1="47" x2="47" y2="66" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="66" x2="47" y2="122" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="82" x2="16" y2="96" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="82" x2="78" y2="96" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="47" y1="122" x2="30" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="47" y1="122" x2="64" y2="168" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 48%, rgba(0,0,0,0.78) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S9;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene_C3_S10',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C3_S10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, cfg?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...cfg },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Two columns: EMI path vs SIP path
  // Growth curves
  const sipGrowth = (t: number) => 40 * Math.exp(0.145 * t * 25); // 14.5% CAGR
  const emiSunk = (t: number) => 130 * t; // linear cost

  const curveMaxY = 600;
  const maxSIP = sipGrowth(1);
  const maxEMI = 150;

  const progress = interpolate(frame, [20, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Build SIP curve
  const sipPath = (() => {
    let d = '';
    const steps = 50;
    for (let i = 0; i <= steps; i++) {
      const t = (i / steps) * progress;
      const x = t * 680;
      const rawY = sipGrowth(t);
      const y = curveMaxY - (rawY / maxSIP) * (curveMaxY - 60);
      d += i === 0 ? `M ${x} ${y}` : ` L ${x} ${y}`;
    }
    return d;
  })();

  // Build EMI line
  const emiPath = (() => {
    let d = '';
    const steps = 50;
    for (let i = 0; i <= steps; i++) {
      const t = (i / steps) * progress;
      const x = t * 680;
      const rawY = emiSunk(t);
      const y = 60 + (rawY / maxEMI) * (curveMaxY - 60);
      d += i === 0 ? `M ${x} ${y}` : ` L ${x} ${y}`;
    }
    return d;
  })();

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 19) * 1920,
    y: (seededRandom(i * 23) * 800 + frame * seededRandom(i * 29) * 0.6) % 900,
    r: 2 + seededRandom(i * 31) * 4,
    op: 0.07 + seededRandom(i * 37) * 0.1,
    col: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#9E9E9E' : '#ff0000',
  }));

  // Bottom truth banner
  const bannerS = sp(80);

  // Stick figure — pointing at chart
  const figX = interpolate(sp(55), [0, 1], [260, 0]);
  const headNod = Math.sin(frame * 0.05) * 3;
  const bodyBob = Math.sin(frame * 0.07) * 2;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* === BG === */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'linear-gradient(140deg, #001a00 0%, #1C1C1C 45%, #1a0a00 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === DIAGONAL GRID === */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }, (_, i) => (
            <line
              key={`a${i}`}
              x1={0}
              y1={i * 56}
              x2={1920}
              y2={i * 56 + 100}
              stroke="#e9d700"
              strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === PARTICLES === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <svg width="1920" height="1080">
          {particles.map((p, i) => (
            <circle key={i} cx={p.x} cy={p.y} r={p.r} fill={p.col} opacity={p.op} />
          ))}
        </svg>
      </AbsoluteFill>

      {/* === TITLE === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'flex-start',
          paddingTop: 46,
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(5), [0, 1], [-28, 0])}px)`,
        }}
      >
        <div
          style={{
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 66,
            fontWeight: 900,
            color: '#F5F5F5',
            textAlign: 'center',
          }}
        >
          EMI vs SIP — कौन जीता?
        </div>
        <div
          style={{
            fontFamily: '"Inter", sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#e9d700',
            marginTop: 8,
            letterSpacing: 2,
          }}
        >
          UNCOMFORTABLE TRUTH · 25 YEAR COMPARISON
        </div>
      </AbsoluteFill>

      {/* === COMPARISON CHART === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          paddingTop: 150,
          paddingBottom: 130,
        }}
      >
        <svg width="800" height="580" viewBox="0 0 800 580">
          {/* Grid */}
          {Array.from({ length: 6 }, (_, i) => (
            <line
              key={i}
              x1={60}
              y1={60 + i * 100}
              x2={760}
              y2={60 + i * 100}
              stroke="#2a2a2a"
              strokeWidth="1"
            />
          ))}
          {/* Axes */}
          <line x1="60" y1="60" x2="60" y2="580" stroke="#3a3a3a" strokeWidth="2" />
          <line x1="60" y1="580" x2="760" y2="580" stroke="#3a3a3a" strokeWidth="2" />

          {/* X labels */}
          {[0, 5, 10, 15, 20, 25].map((yr, i) => (
            <text
              key={i}
              x={60 + i * 136}
              y={600}
              fill="#9E9E9E"
              fontSize="18"
              textAnchor="middle"
              fontFamily="Inter"
            >
              Yr{yr}
            </text>
          ))}

          {/* SIP Curve — Green/Gold */}
          <path
            d={sipPath}
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            strokeLinecap="round"
            transform="translate(60,0)"
          />
          {/* SIP area */}
          <path
            d={`${sipPath} L ${progress * 680 + 60} 580 L 60 580 Z`}
            fill="rgba(233,215,0,0.06)"
            transform="translate(60,0) translate(-60,0)"
          />

          {/* EMI line — Red */}
          <path
            d={emiPath}
            fill="none"
            stroke="#ff0000"
            strokeWidth="4"
            strokeLinecap="round"
            strokeDasharray="10 6"
            transform="translate(60,0)"
          />

          {/* Legend */}
          <rect x={80} y={80} width={16} height={16} rx={3} fill="#e9d700" />
          <text x={104} y={93} fill="#e9d700" fontSize="20" fontFamily="Noto Sans Devanagari" fontWeight="bold">SIP Returns → ₹1.5 Cr+</text>

          <rect x={80} y={112} width={16} height={16} rx={3} fill="#ff0000" />
          <text x={104} y={125} fill="#ff0000" fontSize="20" fontFamily="Noto Sans Devanagari" fontWeight="bold">EMI Total Cost → ₹1.5 Cr spent</text>

          {/* End labels */}
          {progress > 0.9 && (
            <>
              <g transform="translate(60,0)">
                <rect x={682} y={40} width={90} height={32} rx={6} fill="#e9d700" />
                <text x={727} y={61} fill="#1C1C1C" fontSize="18" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">₹1.5Cr</text>
              </g>
              <g transform="translate(60,0)">
                <rect x={682} y={526} width={90} height={32} rx={6} fill="#ff0000" />
                <text x={727} y={547} fill="#F5F5F5" fontSize="18" fontWeight="bold" textAnchor="middle" fontFamily="JetBrains Mono">-₹1.5Cr</text>
              </g>
            </>
          )}
        </svg>

        {/* Right side city comparison */}
        <div
          style={{
            marginLeft: 60,
            display: 'flex',
            flexDirection: 'column',
            gap: 20,
            opacity: interpolate(sp(35), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(35), [0, 1], [50, 0])}px)`,
          }}
        >
          {[
            { city: 'Bengaluru', result: '✅ Property beats SIP', color: '#e9d700' },
            { city: 'Kolkata', result: '❌ SIP beats property', color: '#ff0000' },
            { city: 'Patna', result: '❌ SIP beats property', color: '#ff0000' },
            { city: 'Jaipur', result: '⚠️ Depends on area', color: '#FFC107' },
          ].map((c, i) => (
            <div
              key={i}
              style={{
                width: 380,
                background: 'rgba(45,45,45,0.5)',
                borderRadius: 12,
                border: `1.5px solid ${c.color}33`,
                padding: '14px 22px',
                transform: `translateX(${interpolate(sp(35 + i * 8), [0, 1], [40, 0])}px)`,
                opacity: interpolate(sp(35 + i * 8), [0, 1], [0, 1]),
              }}
            >
              <div
                style={{
                  fontFamily: '"Inter", sans-serif',
                  fontSize: 26,
                  fontWeight: 900,
                  color: '#F5F5F5',
                }}
              >
                {c.city}
              </div>
              <div
                style={{
                  fontFamily: '"Noto Sans Devanagari", sans-serif',
                  fontSize: 22,
                  color: c.color,
                  fontWeight: 700,
                  marginTop: 4,
                }}
              >
                {c.result}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* === UNCOMFORTABLE TRUTH BANNER === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'center',
          paddingBottom: 38,
          opacity: interpolate(bannerS, [0, 1], [0, 1]),
          transform: `translateY(${interpolate(bannerS, [0, 1], [30, 0])}px)`,
        }}
      >
        <div
          style={{
            background: 'linear-gradient(90deg, #e9d700 0%, #FFC107 50%, #e9d700 100%)',
            borderRadius: 12,
            padding: '16px 52px',
            fontFamily: '"Noto Sans Devanagari", sans-serif',
            fontSize: 42,
            fontWeight: 900,
            color: '#1C1C1C',
            textAlign: 'center',
            boxShadow: '0 10px 40px rgba(233,215,0,0.35)',
          }}
        >
          Uncomfortable Truth — पर ये सच है!
        </div>
      </AbsoluteFill>

      {/* === STICK FIGURE — POINTING === */}
      <AbsoluteFill
        style={{
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'flex-start',
          paddingLeft: 60,
          paddingBottom: 110,
          transform: `translateX(${figX}px)`,
        }}
      >
        <svg width="105" height="200" viewBox="0 0 105 200">
          <ellipse cx="52" cy="190" rx="34" ry="9" fill="rgba(233,215,0,0.14)" />
          <g transform={`translateY(${bodyBob}px)`}>
            <g transform={`rotate(${headNod}, 52, 28)`}>
              <circle cx="52" cy="28" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3.5" />
              <ellipse cx="42" cy="27" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <ellipse cx="62" cy="27" rx="9" ry="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <line x1="33" y1="27" x2="29" y2="25" stroke="#FFC107" strokeWidth="2" />
              <line x1="71" y1="27" x2="75" y2="25" stroke="#FFC107" strokeWidth="2" />
              {/* Determined/speaking */}
              <path d="M44 36 Q52 42 60 36" fill="none" stroke="#F5F5F5" strokeWidth="2" />
            </g>
            <line x1="52" y1="50" x2="52" y2="70" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="52" y1="70" x2="52" y2="130" stroke="#F5F5F5" strokeWidth="3.5" />
            {/* Arm pointing right at chart */}
            <line x1="52" y1="84" x2="22" y2="102" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="52" y1="84" x2="96" y2="74" stroke="#F5F5F5" strokeWidth="3" />
            {/* Pointing finger */}
            <circle cx="100" cy="72" r="5" fill="#e9d700" />
            <line x1="52" y1="130" x2="34" y2="180" stroke="#F5F5F5" strokeWidth="3.5" />
            <line x1="52" y1="130" x2="70" y2="180" stroke="#F5F5F5" strokeWidth="3.5" />
          </g>
        </svg>
      </AbsoluteFill>

      {/* === VIGNETTE === */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div
          style={{
            position: 'absolute',
            inset: 0,
            background:
              'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 46%, rgba(0,0,0,0.78) 100%)',
          }}
        />
      </AbsoluteFill>

      {/* === WATERMARK === */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: '"Inter", sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene_C3_S10;
```

